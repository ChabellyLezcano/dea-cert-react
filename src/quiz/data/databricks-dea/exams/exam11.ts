// Practice exam 11 — Databricks Academy official retired practice exam
// (45 questions, single-choice). Explanations authored to match the style
// of the other exam files, since the source PDF only listed correct
// letters without rationale.
import type { RawQuestion } from '@/quiz/quiz.types';

export const exam11: RawQuestion[] = [
  {
    n: 1,
    d: 'P',
    m: 0,
    q: 'Which of the following describes a benefit of a data lakehouse that is unavailable in a traditional data warehouse?',
    o: [
      'A data lakehouse provides a relational system of data management.',
      'A data lakehouse captures snapshots of data for version control purposes.',
      'A data lakehouse couples storage and compute for complete control.',
      'A data lakehouse utilizes proprietary storage formats for data.',
      'A data lakehouse enables both batch and streaming analytics.',
    ],
    a: [4],
    x: 'Un data lakehouse soporta de forma nativa cargas de trabajo tanto batch como streaming sobre la misma copia de datos, algo que un data warehouse tradicional no ofrece. Las otras opciones describen características de un warehouse clásico (sistema relacional) o son directamente falsas: un lakehouse desacopla almacenamiento y cómputo, y usa formatos abiertos (no propietarios).',
  },
  {
    n: 2,
    d: 'P',
    m: 0,
    q: 'Which of the following locations hosts the driver and worker nodes of a Databricks-managed cluster?',
    o: [
      'Data plane',
      'Control plane',
      'Databricks Filesystem',
      'JDBC data source',
      'Databricks web application',
    ],
    a: [0],
    x: 'El driver y los workers de un clúster se ejecutan en el data plane (dentro de la cuenta cloud del cliente en compute clásico), mientras que el control plane aloja la aplicación web, el scheduler y el metastore, sin ejecutar código de usuario.',
  },
  {
    n: 3,
    d: 'P',
    m: 0,
    q: 'A data architect is designing a data model that works for both video-based machine learning workloads and highly audited batch ETL/ELT workloads.\n\nWhich of the following describes how using a data lakehouse can help the data architect meet the needs of both workloads?',
    o: [
      'A data lakehouse requires very little data modeling.',
      'A data lakehouse combines compute and storage for simple governance.',
      'A data lakehouse provides autoscaling for compute clusters.',
      'A data lakehouse stores unstructured data and is ACID-compliant.',
      'A data lakehouse fully exists in the cloud.',
    ],
    a: [3],
    x: 'La combinación de soporte para datos no estructurados (video) y garantías ACID (auditoría estricta de ETL/ELT) es precisamente lo que permite a un lakehouse cubrir ambos casos de uso con una sola plataforma. El resto de opciones son verdaderas de forma parcial o irrelevantes para el requisito planteado.',
  },
  {
    n: 4,
    d: 'P',
    m: 0,
    q: 'Which of the following describes a scenario in which a data engineer will want to use a Job cluster instead of an all-purpose cluster?',
    o: [
      'An ad-hoc analytics report needs to be developed while minimizing compute costs.',
      'A data team needs to collaborate on the development of a machine learning model.',
      'An automated workflow needs to be run every 30 minutes.',
      'A Databricks SQL query needs to be scheduled for upward reporting.',
      'A data engineer needs to manually investigate a production error.',
    ],
    a: [2],
    x: 'Un job cluster es el más adecuado para cargas automatizadas y programadas: se crea al iniciar la ejecución y se termina al finalizar, sin coste de tiempo ocioso. El desarrollo ad-hoc, la colaboración o la investigación manual necesitan un clúster all-purpose que permanezca activo entre sesiones.',
  },
  {
    n: 5,
    d: 'GOV',
    m: 0,
    q: 'A data engineer has created a Delta table as part of a data pipeline. Downstream data analysts now need SELECT permission on the Delta table.\n\nAssuming the data engineer is the Delta table owner, which part of the Databricks Lakehouse Platform can the data engineer use to grant the data analysts the appropriate access?',
    o: ['Repos', 'Jobs', 'Data Explorer', 'Databricks Filesystem', 'Dashboards'],
    a: [2],
    x: 'Data Explorer (hoy Catalog Explorer) es la interfaz donde el propietario de una tabla puede gestionar y conceder permisos como SELECT sobre sus objetos. Repos, Jobs y Dashboards no tienen relación con la gestión de permisos de tablas.',
  },
  {
    n: 6,
    d: 'P',
    m: 0,
    q: 'Two junior data engineers are authoring separate parts of a single data pipeline notebook. They are working on separate Git branches so they can pair program on the same notebook simultaneously. A senior data engineer experienced in Databricks suggests there is a better alternative for this type of collaboration.\n\nWhich of the following supports the senior data engineer\u2019s claim?',
    o: [
      'Databricks Notebooks support automatic change-tracking and versioning',
      'Databricks Notebooks support real-time coauthoring on a single notebook',
      'Databricks Notebooks support commenting and notification comments',
      'Databricks Notebooks support the use of multiple languages in the same notebook',
      'Databricks Notebooks support the creation of interactive data visualizations',
    ],
    a: [1],
    x: 'Los notebooks de Databricks permiten coautoría en tiempo real sobre el mismo notebook, evitando la necesidad de ramas Git separadas solo para poder trabajar simultáneamente en el mismo fichero. Las otras opciones son características reales de los notebooks, pero no resuelven el problema concreto de colaborar en tiempo real sobre el mismo documento.',
  },
  {
    n: 7,
    d: 'CICD',
    m: 0,
    q: 'Which of the following describes how Databricks Repos can help facilitate CI/CD workflows on the Databricks Lakehouse Platform?',
    o: [
      'Databricks Repos can facilitate the pull request, review, and approval process before merging branches',
      'Databricks Repos can merge changes from a secondary Git branch into a main Git branch',
      'Databricks Repos can be used to design, develop, and trigger Git automation pipelines',
      'Databricks Repos can store the single-source-of-truth Git repository',
      'Databricks Repos can commit or push code changes to trigger a CI/CD process',
    ],
    a: [4],
    x: 'Databricks Repos permite hacer commit y push de cambios de código, lo que puede disparar un proceso de CI/CD configurado en el proveedor Git. Operaciones como pull requests, merges o el diseño de pipelines de automatización deben realizarse en el propio proveedor Git (GitHub, GitLab...), no dentro de Databricks Repos.',
  },
  {
    n: 8,
    d: 'TRA',
    m: 0,
    q: 'Which of the following statements describes Delta Lake?',
    o: [
      'Delta Lake is an open source analytics engine used for big data workloads.',
      'Delta Lake is an open format storage layer that delivers reliability, security, and performance.',
      'Delta Lake is an open source platform to help manage the complete machine learning lifecycle.',
      'Delta Lake is an open source data storage format for distributed data.',
      'Delta Lake is an open format storage layer that processes data.',
    ],
    a: [1],
    x: 'Delta Lake es una capa de almacenamiento de formato abierto que añade fiabilidad (transacciones ACID), seguridad y rendimiento sobre archivos Parquet. No es un motor de analítica ni una plataforma de gestión del ciclo de vida de ML (eso sería MLflow), y su rol no es "procesar" datos sino gobernar cómo se almacenan y versionan.',
  },
  {
    n: 9,
    d: 'TRA',
    m: 0,
    q: 'A data architect has determined that a table of the following format is necessary: id (STRING), birthDate (DATE), avgRating (FLOAT) — example rows: a1 / 1990-01-06 / 5.5, a2 / 1974-11-21 / 7.1.\n\nWhich of the following code blocks uses SQL DDL commands to create an empty Delta table in the above format regardless of whether a table already exists with this name?',
    o: [
      'CREATE OR REPLACE TABLE table_name AS SELECT id STRING, birthDate DATE, avgRating FLOAT USING DELTA',
      'CREATE OR REPLACE TABLE table_name (id STRING, birthDate DATE, avgRating FLOAT)',
      'CREATE TABLE IF NOT EXISTS table_name (id STRING, birthDate DATE, avgRating FLOAT)',
      'CREATE TABLE table_name AS SELECT id STRING, birthDate DATE, avgRating FLOAT',
      'CREATE OR REPLACE TABLE table_name WITH COLUMNS (id STRING, birthDate DATE, avgRating FLOAT) USING DELTA',
    ],
    a: [1],
    x: '`CREATE OR REPLACE TABLE` con una lista explícita de columnas crea una tabla vacía con ese esquema exacto, sustituyendo cualquier tabla previa con el mismo nombre. Las variantes con `AS SELECT` intentarían poblar la tabla con el resultado de una consulta (sintaxis inválida aquí, ya que se listan tipos de columna, no una query real), y `WITH COLUMNS` no es sintaxis válida de Delta DDL.',
  },
  {
    n: 10,
    d: 'TRA',
    m: 0,
    q: 'Which of the following SQL keywords can be used to append new rows to an existing Delta table?',
    o: ['UPDATE', 'COPY', 'INSERT INTO', 'DELETE', 'UNION'],
    a: [2],
    x: '`INSERT INTO` añade nuevas filas a una tabla existente sin afectar a las ya presentes. `UPDATE` y `DELETE` modifican/eliminan filas existentes, `COPY` no es una keyword SQL de Delta, y `UNION` combina resultados de consultas, no escribe en una tabla.',
  },
  {
    n: 11,
    d: 'TRO',
    m: 0,
    q: 'A data engineering team needs to query a Delta table to extract rows that all meet the same condition. However, the team has noticed that the query is running slowly. The team has already tuned the size of the data files. Upon investigating, the team has concluded that the rows meeting the condition are sparsely located throughout each of the data files.\n\nBased on the scenario, which of the following optimization techniques could speed up the query?',
    o: ['Data skipping', 'Z-Ordering', 'Bin-packing', 'Write as a Parquet file', 'Tuning the file size'],
    a: [1],
    x: 'Z-Ordering co-localiza físicamente los valores similares de una columna dentro de los archivos, de modo que las estadísticas de min/max por archivo permiten saltarse (data skipping) más archivos en la consulta. El data skipping por sí solo ya se usa automáticamente, pero aquí el problema es que las filas relevantes están dispersas dentro de cada archivo; Z-Ordering es la técnica que resuelve exactamente ese problema de dispersión.',
  },
  {
    n: 12,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to create a database called customer360 at the location /customer/customer360. The data engineer is unsure if one of their colleagues has already created the database.\n\nWhich of the following commands should the data engineer run to complete this task?',
    o: [
      "CREATE DATABASE customer360 LOCATION '/customer/customer360';",
      'CREATE DATABASE IF NOT EXISTS customer360;',
      "CREATE DATABASE IF NOT EXISTS customer360 LOCATION '/customer/customer360';",
      "CREATE DATABASE IF NOT EXISTS customer360 DELTA LOCATION '/customer/customer360';",
      "CREATE DATABASE customer360 DELTA LOCATION '/customer/customer360';",
    ],
    a: [2],
    x: '`CREATE DATABASE IF NOT EXISTS ... LOCATION ...` cubre ambos requisitos a la vez: evita el error si la base de datos ya existe (`IF NOT EXISTS`) y especifica la ubicación exacta pedida. La keyword `DELTA` no es válida en `CREATE DATABASE`, ya que ese comando no se asocia a un formato de tabla concreto.',
  },
  {
    n: 13,
    d: 'TRA',
    m: 0,
    q: 'A junior data engineer needs to create a Spark SQL table my_table for which Spark manages both the data and the metadata. The metadata and data should also be stored in the Databricks Filesystem (DBFS).\n\nWhich of the following commands should a senior data engineer share with the junior data engineer to complete this task?',
    o: [
      'CREATE TABLE my_table (id STRING, value STRING) USING org.apache.spark.sql.parquet OPTIONS (PATH "storage-path");',
      'CREATE MANAGED TABLE my_table (id STRING, value STRING) USING org.apache.spark.sql.parquet OPTIONS (PATH "storage-path");',
      'CREATE MANAGED TABLE my_table (id STRING, value STRING);',
      'CREATE TABLE my_table (id STRING, value STRING) USING DBFS;',
      'CREATE TABLE my_table (id STRING, value STRING);',
    ],
    a: [4],
    x: 'Una sentencia `CREATE TABLE` simple, sin especificar una ruta externa (`PATH`) ni usar la keyword inválida `MANAGED`, crea automáticamente una tabla managed cuyos datos y metadatos gestiona Spark en el almacenamiento por defecto (DBFS en este contexto). Especificar un `PATH` externo convertiría la tabla en external, y `CREATE MANAGED TABLE`/`USING DBFS` no son sintaxis válidas.',
  },
  {
    n: 14,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to create a relational object by pulling data from two tables. The relational object must be used by other data engineers in other sessions. In order to save on storage costs, the data engineer wants to avoid copying and storing physical data.\n\nWhich of the following relational objects should the data engineer create?',
    o: ['View', 'Temporary view', 'Delta Table', 'Database', 'Spark SQL Table'],
    a: [0],
    x: 'Una vista (View) es un objeto puramente lógico: no almacena datos físicos y se recalcula bajo demanda, pero al contrario que una vista temporal, persiste y es accesible desde otras sesiones. Una tabla Delta o Spark SQL sí copiaría y almacenaría los datos físicamente, y una vista temporal no sobrevive más allá de la sesión actual.',
  },
  {
    n: 15,
    d: 'TRA',
    m: 0,
    q: 'A data engineering team has created a series of tables using Parquet data stored in an external system. The team is noticing that after appending new rows to the data in the external system, their queries within Databricks are not returning the new rows. They identify the caching of the previous data as the cause of this issue.\n\nWhich of the following approaches will ensure that the data returned by queries is always up-to-date?',
    o: [
      'The tables should be converted to the Delta format',
      'The tables should be stored in a cloud-based external system',
      'The tables should be refreshed in the writing cluster before the next query is run',
      'The tables should be altered to include metadata to not cache',
      'The tables should be updated before the next query is run',
    ],
    a: [0],
    x: 'Convertir las tablas al formato Delta resuelve el problema de raíz: Delta Lake mantiene un log de transacciones que siempre refleja el estado actual de los datos, evitando el problema de caché obsoleta típico de leer directamente archivos Parquet externos sin ese control transaccional.',
  },
  {
    n: 16,
    d: 'TRA',
    m: 0,
    q: 'A table customerLocations exists with the following schema: id STRING, date STRING, city STRING, country STRING. A senior data engineer wants to create a new table using: CREATE TABLE customersPerCountry AS SELECT country, COUNT(*) AS customers FROM customerLocations GROUP BY country;\n\nA junior data engineer asks why the schema is not being declared for the new table. Which of the following responses explains why declaring the schema is not necessary?',
    o: [
      'CREATE TABLE AS SELECT statements adopt schema details from the source table and query.',
      'CREATE TABLE AS SELECT statements infer the schema by scanning the data.',
      'CREATE TABLE AS SELECT statements result in tables where schemas are optional.',
      'CREATE TABLE AS SELECT statements assign all columns the type STRING.',
      'CREATE TABLE AS SELECT statements result in tables that do not support schemas.',
    ],
    a: [0],
    x: 'Una sentencia CTAS (`CREATE TABLE AS SELECT`) adopta automáticamente el esquema resultante de la propia consulta (nombres y tipos de columna calculados por la query), por lo que nunca es necesario declararlo manualmente. No se trata de inferencia por escaneo de datos, ni de que el esquema sea "opcional" o inexistente: siempre existe, solo que se deriva de la query.',
  },
  {
    n: 17,
    d: 'TRA',
    m: 0,
    q: 'A data engineer is overwriting data in a table by deleting the table and recreating the table. Another data engineer suggests that this is inefficient and the table should simply be overwritten instead.\n\nWhich of the following reasons to overwrite the table instead of deleting and recreating the table is INCORRECT?',
    o: [
      'Overwriting a table is efficient because no files need to be deleted.',
      'Overwriting a table results in a clean table history for logging and audit purposes.',
      'Overwriting a table maintains the old version of the table for Time Travel.',
      'Overwriting a table is an atomic operation and will not leave the table in an unfinished state.',
      'Overwriting a table allows for concurrent queries to be completed while in progress.',
    ],
    a: [1],
    x: 'Sobrescribir una tabla Delta NO produce un historial "limpio": al contrario, preserva todo el historial de versiones anteriores en el log de transacciones (permitiendo Time Travel), a diferencia de borrar y recrear la tabla, que sí eliminaría ese historial. Esa es la afirmación incorrecta del enunciado; el resto de razones (atomicidad, eficiencia, no bloquear consultas concurrentes) son ventajas reales de `overwrite`.',
  },
  {
    n: 18,
    d: 'TRA',
    m: 0,
    q: 'Which of the following commands will return records from an existing Delta table my_table where duplicates have been removed?',
    o: [
      'DROP DUPLICATES FROM my_table;',
      'SELECT * FROM my_table WHERE duplicate = False;',
      'SELECT DISTINCT * FROM my_table;',
      'MERGE INTO my_table a USING new_records b ON a.id = b.id WHEN NOT MATCHED THEN INSERT *;',
      'MERGE INTO my_table a USING new_records b;',
    ],
    a: [2],
    x: '`SELECT DISTINCT *` devuelve las filas de la tabla eliminando duplicados exactos en el resultado de la consulta. `DROP DUPLICATES` no es sintaxis SQL válida, y las opciones con `MERGE` sirven para fusionar datos de otra tabla, no para deduplicar la tabla actual.',
  },
  {
    n: 19,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to horizontally combine two tables as a part of a query. They want to use a shared column as a key column, and they only want the query result to contain rows whose value in the key column is present in both tables.\n\nWhich of the following SQL commands can they use to accomplish this task?',
    o: ['INNER JOIN', 'OUTER JOIN', 'LEFT JOIN', 'MERGE', 'UNION'],
    a: [0],
    x: 'Un INNER JOIN devuelve únicamente las filas cuya clave existe en ambas tablas, que es exactamente el requisito planteado. LEFT/OUTER JOIN incluirían además filas sin coincidencia en el otro lado, MERGE se usa para upserts (no para combinar horizontalmente en una consulta), y UNION apila filas en vertical en vez de combinar columnas.',
  },
  {
    n: 20,
    d: 'TRA',
    m: 0,
    q: 'A junior data engineer has ingested a JSON file into a table raw_table with schema: cart_id STRING, items ARRAY<item_id:STRING>. They would like to unnest the items column to result in a new table with schema: cart_id STRING, item_id STRING.\n\nWhich of the following commands should the junior data engineer run to complete this task?',
    o: [
      'SELECT cart_id, filter(items) AS item_id FROM raw_table;',
      'SELECT cart_id, flatten(items) AS item_id FROM raw_table;',
      'SELECT cart_id, reduce(items) AS item_id FROM raw_table;',
      'SELECT cart_id, explode(items) AS item_id FROM raw_table;',
      'SELECT cart_id, slice(items) AS item_id FROM raw_table;',
    ],
    a: [3],
    x: '`explode()` convierte cada elemento de un array en una fila independiente, produciendo exactamente el cambio de grano de "un carrito con varios items" a "una fila por item" que pide el enunciado. `filter`/`reduce`/`slice` no cambian el número de filas, y `flatten` solo aplana arrays anidados sin generar filas nuevas.',
  },
  {
    n: 21,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has ingested a JSON file into a table raw_table with schema: transaction_id STRING, payload ARRAY<customer_id:STRING, date:TIMESTAMP, store_id:STRING>. The data engineer wants to efficiently extract the date of each transaction into a table with schema: transaction_id STRING, date TIMESTAMP.\n\nWhich of the following commands should the data engineer run to complete this task?',
    o: [
      'SELECT transaction_id, explode(payload) FROM raw_table;',
      'SELECT transaction_id, payload.date FROM raw_table;',
      'SELECT transaction_id, date FROM raw_table;',
      'SELECT transaction_id, payload[date] FROM raw_table;',
      'SELECT transaction_id, date from payload FROM raw_table;',
    ],
    a: [1],
    x: 'La notación de punto (`payload.date`) accede directamente al campo `date` dentro de una columna STRUCT anidada, que es la forma correcta y eficiente de extraerlo sin necesidad de explotar el array. `payload[date]` no es sintaxis SQL válida para acceso a campos, y `explode()` cambiaría el grano de la tabla innecesariamente.',
  },
  {
    n: 22,
    d: 'TRA',
    m: 0,
    q: 'A data analyst has provided the query: SELECT district, avg(sales) FROM store_sales_20220101 GROUP BY district; They would like the data engineering team to run this query every day, with the date at the end of the table name (20220101) automatically replaced with the current date each time.\n\nWhich of the following approaches could be used to efficiently automate this process?',
    o: [
      "They could wrap the query using PySpark and use Python's string variable system to automatically update the table name.",
      "They could manually replace the date within the table name with the current day's date.",
      'They could request that the data analyst rewrites the query to be run less frequently.',
      'They could replace the string-formatted date in the table with a timestamp-formatted date.',
      'They could pass the table into PySpark and develop a robustly tested module on the existing query.',
    ],
    a: [0],
    x: 'Envolver la consulta en PySpark y construir dinámicamente el nombre de la tabla con variables de cadena (p. ej. un f-string con la fecha actual) automatiza el proceso sin intervención manual diaria. Reemplazar la fecha manualmente cada día no es una automatización real, y las demás opciones no resuelven el problema concreto planteado.',
  },
  {
    n: 23,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has ingested data from an external source into a PySpark DataFrame raw_df. They need to briefly make this data available in SQL for a data analyst to perform a quality assurance check on the data.\n\nWhich of the following commands should the data engineer run to make this data available in SQL for only the remainder of the Spark session?',
    o: [
      'raw_df.createOrReplaceTempView("raw_df")',
      'raw_df.createTable("raw_df")',
      'raw_df.write.save("raw_df")',
      'raw_df.saveAsTable("raw_df")',
      'There is no way to share data between PySpark and SQL.',
    ],
    a: [0],
    x: '`createOrReplaceTempView` registra el DataFrame como una vista temporal, consultable con SQL solo durante la sesión de Spark actual, sin persistir nada en disco. `saveAsTable` y `write.save` sí escriben datos físicamente y persisten más allá de la sesión, lo cual excede lo que pide el enunciado ("briefly", "only for the remainder of the session").',
  },
  {
    n: 24,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to dynamically create a table name string using three Python variables: region, store, and year. Example: region="nyc", store="100", year="2021" should produce nyc100_sales_2021.\n\nWhich of the following commands should the data engineer use to construct the table name in Python?',
    o: [
      '"{region}+{store}+_sales_+{year}"',
      'f"{region}+{store}+_sales_+{year}"',
      '"{region}{store}_sales_{year}"',
      'f"{region}{store}_sales_{year}"',
      '{region}+{store}+"_sales_"+{year}',
    ],
    a: [3],
    x: 'Un f-string de Python (`f"..."`) con las variables directamente dentro de llaves, sin operadores `+` de por medio, produce la concatenación literal esperada (`nyc100_sales_2021`). Las variantes sin el prefijo `f` tratarían las llaves como texto literal, y las que usan `+` insertarían el carácter "+" real en el resultado.',
  },
  {
    n: 25,
    d: 'ING',
    m: 0,
    q: 'A data engineer has developed a code block to perform a streaming read on a data source: spark.read.schema(schema).format("cloudFiles").option("cloudFiles.format", "json").load(dataSource). The code block is returning an error.\n\nWhich of the following changes should be made to the code block to configure the block to successfully perform a streaming read?',
    o: [
      'The .read line should be replaced with .readStream.',
      'A new .stream line should be added after the .read line.',
      'The .format("cloudFiles") line should be replaced with .format("stream").',
      'A new .stream line should be added after the spark line.',
      'A new .stream line should be added after the .load(dataSource) line.',
    ],
    a: [0],
    x: 'Para realizar una lectura de streaming con Auto Loader hay que usar `spark.readStream` en vez de `spark.read` — este último es para lecturas batch/estáticas y no es compatible con la fuente `cloudFiles`, de ahí el error. No existe ningún método `.stream` independiente que se pueda añadir a la cadena.',
  },
  {
    n: 26,
    d: 'ING',
    m: 0,
    q: 'A data engineer has configured a Structured Streaming job to read from a table, manipulate the data, and then perform a streaming write into a new table: spark.table("sales").withColumn(...).writeStream.option("checkpointLocation", checkpointPath).outputMode("complete")._____.table("new_sales")\n\nIf the data engineer only wants the query to execute a single micro-batch to process all of the available data, which line of code should fill in the blank?',
    o: [
      'trigger(once=True)',
      'trigger(continuous="once")',
      'processingTime("once")',
      'trigger(processingTime="once")',
      'processingTime(1)',
    ],
    a: [0],
    x: '`trigger(once=True)` procesa todos los datos disponibles en un único micro-batch y detiene el stream a continuación — el comportamiento exacto pedido. `processingTime` y `continuous` son parámetros de otros modos de trigger que mantienen el stream corriendo continuamente, no lo detienen tras un solo batch.',
  },
  {
    n: 27,
    d: 'ING',
    m: 0,
    q: 'A data engineer is designing a data pipeline. The source system generates files in a shared directory that is also used by other processes, so files accumulate and should be kept as is. The data engineer needs to identify which files are new since the previous run and ingest only those.\n\nWhich of the following tools can the data engineer use to solve this problem?',
    o: ['Databricks SQL', 'Delta Lake', 'Unity Catalog', 'Data Explorer', 'Auto Loader'],
    a: [4],
    x: 'Auto Loader está diseñado exactamente para este escenario: detecta e ingiere incrementalmente solo los archivos nuevos desde la última ejecución, llevando el seguimiento mediante un checkpoint, sin necesidad de mover ni borrar los archivos originales del directorio compartido.',
  },
  {
    n: 28,
    d: 'ING',
    m: 0,
    q: 'A data engineering team is converting their existing data pipeline to utilize Auto Loader. A data engineer finds this code block in the documentation: streaming_df = spark.readStream.format("cloudFiles").option("cloudFiles.format", "json").option("cloudFiles.schemaLocation", schemaLocation).load(sourcePath)\n\nAssuming schemaLocation and sourcePath are set correctly, which changes are needed to convert this code block to use Auto Loader?',
    o: [
      'The data engineer needs to change the format("cloudFiles") line to format("autoLoader").',
      'There is no change required. Databricks automatically uses Auto Loader for streaming reads.',
      'There is no change required. The inclusion of format("cloudFiles") enables the use of Auto Loader.',
      'The data engineer needs to add the .autoLoader line before the .load(sourcePath) line.',
      'There is no change required. The data engineer needs to ask their administrator to turn on Auto Loader.',
    ],
    a: [2],
    x: 'El código ya usa Auto Loader correctamente: `format("cloudFiles")` es precisamente el identificador de la fuente de streaming de Auto Loader, no un formato distinto que haya que activar. No existe un `format("autoLoader")` ni un método `.autoLoader`, y no requiere ninguna activación administrativa especial.',
  },
  {
    n: 29,
    d: 'TRA',
    m: 0,
    q: 'Which of the following data workloads will utilize a Bronze table as its source?',
    o: [
      'A job that aggregates cleaned data to create standard summary statistics',
      'A job that queries aggregated data to publish key insights into a dashboard',
      'A job that ingests raw data from a streaming source into the Lakehouse',
      'A job that develops a feature set for a machine learning application',
      'A job that enriches data by parsing its timestamps into a human-readable format',
    ],
    a: [4],
    x: 'Enriquecer datos parseando timestamps es una transformación típica de Bronze a Silver, por lo que su fuente es una tabla Bronze (datos crudos, tal cual llegaron). Ingerir datos crudos desde streaming es lo que *produce* la tabla Bronze (no la usa como fuente), y las opciones de agregación/features consumen datos ya limpios (Silver), no Bronze.',
  },
  {
    n: 30,
    d: 'TRA',
    m: 0,
    q: 'Which of the following data workloads will utilize a Silver table as its source?',
    o: [
      'A job that enriches data by parsing its timestamps into a human-readable format',
      'A job that queries aggregated data that already feeds into a dashboard',
      'A job that ingests raw data from a streaming source into the Lakehouse',
      'A job that aggregates cleaned data to create standard summary statistics',
      'A job that cleans data by removing malformatted records',
    ],
    a: [3],
    x: 'Agregar datos ya limpios para crear estadísticas resumen es un trabajo típico de Silver a Gold, por lo que su fuente es una tabla Silver (datos ya conformados y depurados). Limpiar registros mal formados es lo que *produce* la tabla Silver a partir de Bronze, no lo que la consume como fuente.',
  },
  {
    n: 31,
    d: 'TRA',
    m: 0,
    q: 'Which of the following Structured Streaming queries is performing a hop from a Bronze table to a Silver table?',
    o: [
      'spark.table("sales").groupBy("store").agg(sum("sales")).writeStream...outputMode("complete").table("aggregatedSales")',
      'spark.table("sales").agg(sum("sales"), sum("units")).writeStream...outputMode("complete").table("aggregatedSales")',
      'spark.table("sales").withColumn("avgPrice", col("sales") / col("units")).writeStream...outputMode("append").table("cleanedSales")',
      'spark.readStream.load(rawSalesLocation).writeStream...outputMode("append").table("uncleanedSales")',
      'spark.read.load(rawSalesLocation).writeStream...outputMode("append").table("uncleanedSales")',
    ],
    a: [2],
    x: 'La opción que añade una columna calculada (limpieza/enriquecimiento ligero) y escribe en una tabla llamada "cleanedSales" con `outputMode("append")` es el patrón típico de un salto Bronze→Silver. Las que agregan datos (`groupBy`/`agg`) van de Silver a Gold, y la que solo carga datos crudos sin transformar (`uncleanedSales`) es la ingesta hacia Bronze, no un hop hacia Silver.',
  },
  {
    n: 32,
    d: 'TRA',
    m: 0,
    q: 'Which of the following benefits does Delta Live Tables provide for ELT pipelines over standard data pipelines that utilize Spark and Delta Lake on Databricks?',
    o: [
      'The ability to declare and maintain data table dependencies',
      'The ability to write pipelines in Python and/or SQL',
      'The ability to access previous versions of data tables',
      'The ability to automatically scale compute resources',
      'The ability to perform batch and streaming queries',
    ],
    a: [0],
    x: 'La ventaja diferencial de Delta Live Tables (hoy Lakeflow Declarative Pipelines) es declarar el resultado deseado de cada tabla y dejar que la plataforma gestione automáticamente el orden de ejecución y las dependencias entre ellas. Las demás capacidades (Python/SQL, time travel, autoscaling, batch+streaming) ya existen en Spark y Delta Lake sin necesidad de DLT.',
  },
  {
    n: 33,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has three notebooks in an ELT pipeline. The notebooks need to be executed in a specific order for the pipeline to complete successfully. The data engineer would like to use Delta Live Tables to manage this process.\n\nWhich of the following steps must the data engineer take as part of implementing this pipeline using Delta Live Tables?',
    o: [
      'They need to create a Delta Live Tables pipeline from the Data page.',
      'They need to create a Delta Live Tables pipeline from the Jobs page.',
      'They need to create a Delta Live tables pipeline from the Compute page.',
      'They need to refactor their notebook to use Python and the dlt library.',
      'They need to refactor their notebook to use SQL and CREATE LIVE TABLE keyword.',
    ],
    a: [1],
    x: 'Un pipeline de Delta Live Tables se crea desde la página de Jobs/Workflows (donde se gestionan también los pipelines declarativos), no desde Data ni Compute. No es obligatorio migrar a Python+dlt ni a SQL con CREATE LIVE TABLE: cualquiera de los dos lenguajes es válido, no es un paso obligatorio elegir uno en concreto.',
  },
  {
    n: 34,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has written the query: SELECT * FROM json.`/path/to/json/file.json`; They ask a colleague for help to convert this query for use in a Delta Live Tables (DLT) pipeline, as the first table in the pipeline.\n\nWhich of the following describes the change the colleague needs to make to the query?',
    o: [
      'They need to add a COMMENT line at the beginning of the query.',
      'They need to add a CREATE LIVE TABLE table_name AS line at the beginning of the query.',
      'They need to add a live. prefix prior to json. in the FROM line.',
      'They need to add a CREATE DELTA LIVE TABLE table_name AS line at the beginning of the query.',
      'They need to add the cloud_files(...) wrapper to the JSON file path.',
    ],
    a: [1],
    x: 'Para declarar una tabla dentro de un pipeline DLT en SQL, hay que anteponer `CREATE LIVE TABLE table_name AS` a la consulta. `CREATE DELTA LIVE TABLE` no es la sintaxis correcta (la keyword es solo `LIVE TABLE`), y el prefijo `live.` se usa para referenciar tablas de DLT en el FROM, no fuentes de archivos externos como esta.',
  },
  {
    n: 35,
    d: 'TRA',
    m: 0,
    q: "A dataset has been defined using Delta Live Tables and includes an expectations clause: CONSTRAINT valid_timestamp EXPECT (timestamp > '2020-01-01')\n\nWhat is the expected behavior when a batch of data containing data that violates these constraints is processed?",
    o: [
      'Records that violate the expectation are added to the target dataset and recorded as invalid in the event log.',
      'Records that violate the expectation are dropped from the target dataset and recorded as invalid in the event log.',
      'Records that violate the expectation cause the job to fail.',
      'Records that violate the expectation are added to the target dataset and flagged as invalid in a field added to the target dataset.',
      'Records that violate the expectation are dropped from the target dataset and loaded into a quarantine table.',
    ],
    a: [0],
    x: 'Un `EXPECT` simple (sin `ON VIOLATION DROP ROW` ni `FAIL UPDATE`) solo registra la violación en el log de eventos del pipeline para su monitorización, pero **mantiene** el registro en la tabla destino. Para descartar filas hace falta `ON VIOLATION DROP ROW`, y para fallar el pipeline, `ON VIOLATION FAIL UPDATE` — ninguna de esas cláusulas aparece en el enunciado.',
  },
  {
    n: 36,
    d: 'TRA',
    m: 0,
    q: 'A Delta Live Table pipeline includes two datasets defined using STREAMING LIVE TABLE. Three datasets are defined against Delta Lake table sources using LIVE TABLE. The pipeline is configured to run in Development mode using the Triggered Pipeline Mode.\n\nAssuming previously unprocessed data exists and all definitions are valid, what is the expected outcome after clicking Start to update the pipeline?',
    o: [
      'All datasets will be updated once and the pipeline will shut down. The compute resources will be terminated.',
      'All datasets will be updated at set intervals until the pipeline is shut down. The compute resources will be deployed for the update and terminated when the pipeline is stopped.',
      'All datasets will be updated at set intervals until the pipeline is shut down. The compute resources will persist after the pipeline is stopped to allow for additional testing.',
      'All datasets will be updated once and the pipeline will shut down. The compute resources will persist to allow for additional testing.',
      'All datasets will be updated continuously and the pipeline will not shut down. The compute resources will persist with the pipeline.',
    ],
    a: [3],
    x: 'En modo Triggered, el pipeline procesa todos los datos disponibles una sola vez y se detiene. La particularidad del modo Development es que el clúster se mantiene activo (no se termina) tras la ejecución, precisamente para facilitar iteraciones rápidas de desarrollo sin pagar el coste de arranque en cada prueba — a diferencia del modo Production, donde el clúster sí se termina.',
  },
  {
    n: 37,
    d: 'JOBS',
    m: 0,
    q: 'A data engineer has a Job with multiple tasks that runs nightly. One of the tasks unexpectedly fails during 10 percent of the runs.\n\nWhich of the following actions can the data engineer perform to ensure the Job completes each night while minimizing compute costs?',
    o: [
      'They can institute a retry policy for the entire Job',
      'They can observe the task as it runs to try and determine why it is failing',
      'They can set up the Job to run multiple times ensuring that at least one will complete',
      'They can institute a retry policy for the task that periodically fails',
      'They can utilize a Jobs cluster for each of the tasks in the Job',
    ],
    a: [3],
    x: 'Configurar una política de reintentos únicamente en la tarea que falla de forma intermitente resuelve el problema con el mínimo coste: solo se reintenta esa tarea concreta, no el Job completo, cuando ocurre el fallo transitorio (10% de las veces). Reintentar todo el Job o ejecutarlo varias veces completas desperdiciaría cómputo innecesariamente.',
  },
  {
    n: 38,
    d: 'JOBS',
    m: 0,
    q: 'A data engineer has set up two Jobs that each run nightly. The first Job starts at 12:00 AM and usually completes in about 20 minutes. The second Job depends on the first Job and starts at 12:30 AM. Sometimes, the second Job fails when the first Job does not complete by 12:30 AM.\n\nWhich of the following approaches can the data engineer use to avoid this problem?',
    o: [
      'They can utilize multiple tasks in a single job with a linear dependency',
      'They can use cluster pools to help the Jobs run more efficiently',
      'They can set up a retry policy on the first Job to help it run more quickly',
      'They can limit the size of the output in the second Job so that it will not fail as easily',
      'They can set up the data to stream from the first Job to the second Job',
    ],
    a: [0],
    x: 'Combinar ambos procesos como tareas dependientes dentro de un mismo Job (en vez de dos Jobs programados por horario fijo) hace que la segunda tarea espere automáticamente a que la primera termine con éxito, eliminando la condición de carrera basada en una hora de inicio fija que causa el fallo intermitente.',
  },
  {
    n: 39,
    d: 'CICD',
    m: 0,
    q: "A data engineer has set up a notebook to automatically process using a Job. The data engineer's manager wants to version control the schedule due to its complexity.\n\nWhich of the following approaches can the data engineer use to obtain a version-controllable configuration of the Job's schedule?",
    o: [
      'They can link the Job to notebooks that are a part of a Databricks Repo.',
      'They can submit the Job once on a Job cluster.',
      "They can download the JSON description of the Job from the Job's page.",
      'They can submit the Job once on an all-purpose cluster.',
      "They can download the XML description of the Job from the Job's page.",
    ],
    a: [2],
    x: 'La página de un Job permite descargar su definición completa en formato JSON, incluida la configuración del schedule — ese fichero JSON puede versionarse en Git como cualquier otro artefacto de código. Vincular notebooks a un Repo versiona el código de los notebooks, pero no la configuración del schedule del Job en sí, y Databricks no exporta configuraciones en XML.',
  },
  {
    n: 40,
    d: 'TRO',
    m: 0,
    q: "A data analyst has noticed that their Databricks SQL queries are running too slowly. They claim that this issue is affecting all of their sequentially run queries. The data engineering team notices that each of the queries uses the same SQL endpoint, but the SQL endpoint is not used by any other user.\n\nWhich of the following approaches can the data engineering team use to improve the latency of the data analyst's queries?",
    o: [
      'They can turn on the Serverless feature for the SQL endpoint.',
      "They can increase the maximum bound of the SQL endpoint's scaling range.",
      'They can increase the cluster size of the SQL endpoint.',
      'They can turn on the Auto Stop feature for the SQL endpoint.',
      'They can turn on the Serverless feature for the SQL endpoint and change the Spot Instance Policy to "Reliability Optimized."',
    ],
    a: [2],
    x: 'Como el problema afecta a consultas secuenciales de un único usuario en un endpoint que nadie más usa, no es un problema de concurrencia (que resolvería el scaling range), sino de potencia de cómputo por consulta individual — aumentar el tamaño del clúster del SQL endpoint acelera cada consulta individual. Serverless y Auto Stop afectan al arranque/parada, no a la velocidad de ejecución en sí.',
  },
  {
    n: 41,
    d: 'JOBS',
    m: 0,
    q: "An engineering manager uses a Databricks SQL query to monitor their team's progress on fixes related to customer-reported bugs. The manager checks the results of the query every day, but they are manually rerunning the query each day and waiting for the results.\n\nWhich of the following approaches can the manager use to ensure the results of the query are updated each day?",
    o: [
      'They can schedule the query to run every 1 day from the Jobs UI.',
      "They can schedule the query to refresh every 1 day from the query's page in Databricks SQL.",
      'They can schedule the query to run every 12 hours from the Jobs UI.',
      "They can schedule the query to refresh every 1 day from the SQL endpoint's page in Databricks SQL.",
      "They can schedule the query to refresh every 12 hours from the SQL endpoint's page in Databricks SQL.",
    ],
    a: [1],
    x: 'Las consultas de Databricks SQL tienen su propia opción de programación de refresco directamente en la página de la consulta (no hace falta usar Jobs para esto), y el requisito pide una actualización diaria, no cada 12 horas. El endpoint SQL es el recurso de cómputo, no el lugar donde se programa el refresco de una query concreta.',
  },
  {
    n: 42,
    d: 'TRO',
    m: 0,
    q: "A data engineering team has been using a Databricks SQL query to monitor the performance of an ELT job. The ELT job is triggered by a specific number of input records being ready to process. The query returns the number of minutes since the job's most recent runtime.\n\nWhich of the following approaches can enable the data engineering team to be notified if the ELT job has not been run in an hour?",
    o: [
      'They can set up an Alert for the accompanying dashboard to notify them if the returned value is greater than 60.',
      'They can set up an Alert for the query to notify when the ELT job fails.',
      'They can set up an Alert for the accompanying dashboard to notify when it has not refreshed in 60 minutes.',
      'They can set up an Alert for the query to notify them if the returned value is greater than 60.',
      'This type of alerting is not possible in Databricks.',
    ],
    a: [3],
    x: 'Las alertas de Databricks SQL se configuran sobre el resultado numérico de una consulta concreta, disparándose cuando ese valor cruza un umbral definido (aquí, más de 60 minutos desde la última ejecución). Las alertas no se configuran sobre dashboards ni sobre el estado de fallo de un Job externo, sino directamente sobre el valor devuelto por la query.',
  },
  {
    n: 43,
    d: 'TRO',
    m: 0,
    q: 'A data engineering manager has noticed that each of the queries in a Databricks SQL dashboard takes a few minutes to update when they manually click the "Refresh" button. A team member provides a variety of reasons why the delay might be occurring.\n\nWhich of the following reasons FAILS to explain why the dashboard might be taking a few minutes to update?',
    o: [
      'The SQL endpoint being used by each of the queries might need a few minutes to start up.',
      'The queries attached to the dashboard might take a few minutes to run under normal circumstances.',
      'The queries attached to the dashboard might first be checking to determine if new data is available.',
      'The Job associated with updating the dashboard might be using a non-pooled endpoint.',
      'The queries attached to the dashboard might all be connected to their own, unstarted Databricks clusters.',
    ],
    a: [3],
    x: 'Un dashboard de Databricks SQL se refresca mediante las consultas asociadas a un SQL endpoint, no mediante un "Job" independiente asociado a un endpoint agrupado (pooled) o no — ese concepto de Job no aplica al mecanismo de refresco de un dashboard, por lo que es la explicación inválida. El resto de razones (arranque del endpoint, tiempo normal de ejecución, comprobación de nuevos datos, clústeres sin arrancar) sí son causas reales de latencia.',
  },
  {
    n: 44,
    d: 'GOV',
    m: 0,
    q: 'A new data engineer has started at a company as new.engineer@company.com. They need to be able to query the table sales in the database retail. The new data engineer already has been granted USAGE on the database retail.\n\nWhich of the following commands can be used to grant the appropriate permissions to the new data engineer?',
    o: [
      'GRANT USAGE ON TABLE sales TO new.engineer@company.com;',
      'GRANT CREATE ON TABLE sales TO new.engineer@company.com;',
      'GRANT SELECT ON TABLE sales TO new.engineer@company.com;',
      'GRANT USAGE ON TABLE new.engineer@company.com TO sales;',
      'GRANT SELECT ON TABLE new.engineer@company.com TO sales;',
    ],
    a: [2],
    x: 'Para poder consultar una tabla concreta hace falta el privilegio SELECT sobre esa tabla, además del USAGE ya concedido sobre la base de datos contenedora (equivalente a USE SCHEMA). USAGE sobre una tabla no es válido (se aplica a bases de datos/esquemas), y CREATE permite crear objetos, no consultarlos.',
  },
  {
    n: 45,
    d: 'GOV',
    m: 0,
    q: 'A new data engineer new.engineer@company.com has been assigned to an ELT project. The new data engineer will need full privileges on the table sales to fully manage the project.\n\nWhich of the following commands can be used to grant full permissions on the table to the new data engineer?',
    o: [
      'GRANT ALL PRIVILEGES ON TABLE sales TO new.engineer@company.com;',
      'GRANT USAGE ON TABLE sales TO new.engineer@company.com;',
      'GRANT ALL PRIVILEGES ON TABLE new.engineer@company.com TO sales;',
      'GRANT SELECT ON TABLE sales TO new.engineer@company.com;',
      'GRANT SELECT CREATE MODIFY ON TABLE sales TO new.engineer@company.com;',
    ],
    a: [0],
    x: '`GRANT ALL PRIVILEGES` concede de una sola vez el conjunto completo de permisos (lectura, escritura, gestión) necesario para administrar plenamente la tabla, cumpliendo el requisito de "privilegios completos". SELECT por sí solo es demasiado limitado, y `GRANT SELECT CREATE MODIFY` no es sintaxis válida (los privilegios no se listan así, separados por espacios, en una única cláusula GRANT).',
  },
];
