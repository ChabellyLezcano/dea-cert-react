import type { RawQuestion } from '@/quiz/quiz.types';

export const exam1: RawQuestion[] = [
  {
    n: 1,
    d: 'ING',
    m: 0,
    q: 'A data engineering team is loading CSV files from cloud storage into a Delta table with COPY INTO. Some files were partially ingested due to a pipeline run failure, and now the team wants to reprocess all files, regardless of whether they were already loaded.\n\nWhat should they use to ensure all files are reprocessed?',
    o: [
      "`COPY_OPTIONS('checkpointing' = 'false')`",
      "`COPY_OPTIONS('overwrite' = 'true')`",
      "`COPY_OPTIONS('force' = 'true')`",
      "`COPY_OPTIONS('idempotency' = 'false')`",
    ],
    a: [2],
    x: "COPY INTO es idempotente por defecto: registra los archivos ya procesados para evitar duplicados. La opción 'force' = 'true' desactiva ese filtro de seguridad y obliga a releer todos los archivos de la ubicación de origen, incluidos los ya ingeridos.",
  },
  {
    n: 2,
    d: 'TRA',
    m: 0,
    q: 'Which of the following code blocks can a data engineer use to create a user-defined function (UDF)?',
    o: [
      '```\nCREATE FUNCTION plus_one(value INT)\nRETURNS INT\nRETURN value + 1;\n```',
      '```\nCREATE UDF plus_one(value INT)\nRETURNS INT\nRETURN value + 1;\n```',
      '```\nCREATE UDF plus_one(value INT)\nRETURN value + 1;\n```',
      '```\nCREATE FUNCTION plus_one(value INT)\nRETURN value + 1\n```',
    ],
    a: [0],
    x: 'La sintaxis correcta es CREATE [OR REPLACE] FUNCTION nombre(param tipo) RETURNS tipo_de_dato RETURN expresión. No existe el comando CREATE UDF, y la cláusula RETURNS con el tipo de retorno es obligatoria.',
  },
  {
    n: 3,
    d: 'TRA',
    m: 0,
    q: 'A data analyst works with two PySpark DataFrames: products_df and reviews_df, both with a product_id column. They need a DataFrame that includes all products, even if they have no reviews, enriched with review data when available.\n\nWhich PySpark code achieves this?',
    o: [
      '```\njoined_df = products_df.join(reviews_df, "product_id", "inner")\n```',
      '```\njoined_df = products_df.join(reviews_df, "product_id", "full")\n```',
      '```\njoined_df = products_df.join(reviews_df, "product_id", "cross")\n```',
      '```\njoined_df = products_df.join(reviews_df, "product_id", "left")\n```',
    ],
    a: [3],
    x: 'Un left join conserva todas las filas del DataFrame izquierdo (products_df) haya o no coincidencia en el derecho; si no hay reseña, las columnas se rellenan con null. inner descartaría productos sin reseñas, full añadiría reseñas huérfanas y cross genera el producto cartesiano.',
  },
  {
    n: 4,
    d: 'P',
    m: 0,
    q: 'A data engineering team is building a notebook and needs fast iteration and debugging during development. Which compute should they use in this case?',
    o: ['Instance pools', 'Interactive compute', 'Serverless Job compute', 'Serverless SQL warehouse'],
    a: [1],
    x: 'El cómputo interactivo (all-purpose) está diseñado para desarrollar en notebooks: ejecución celda a celda, depurador de Python, breakpoints e inspección de variables. Job compute es para tareas programadas, el SQL warehouse es para SQL/BI y los instance pools son solo una capa de infraestructura.',
  },
  {
    n: 5,
    d: 'TRO',
    m: 0,
    q: 'A data engineer is troubleshooting a Databricks job cluster that repeatedly fails during startup. The driver logs show frequent "Executor Lost" and "OutOfMemoryError: Java heap space" messages. Switching to a larger instance resolved the issue.\n\nWhat was the most likely reason for this issue?',
    o: ['Schema mismatch', 'Incorrect file format', 'Input data size', 'Misconfigured library'],
    a: [2],
    x: 'Los errores OOM de heap junto a "Executor Lost" indican que la memoria del clúster fue superada por el volumen de datos de entrada (o skew). Un formato/esquema incorrecto produce AnalysisException, y una librería mal configurada da ClassNotFoundException, no errores de memoria.',
  },
  {
    n: 6,
    d: 'CICD',
    m: 0,
    q: 'Which of the following best describes the correct format of a Databricks Asset Bundle configuration file?',
    o: [
      "A YAML file named databricks.yml that defines the bundle's structure, including targets, resources, and configurations.",
      'A JSON file named databricks_asset.json containing cluster definitions and job schedules',
      'An XML file named asset_bundle.xml specifying the workspace path, job IDs, and compute specifications',
      'A YAML file named bundle-config.yml with fields for environment variables and user roles',
    ],
    a: [0],
    x: 'Los DABs usan un archivo YAML llamado databricks.yml como punto de entrada. Define targets (entornos dev/staging/prod), resources (jobs, pipelines, clústeres) y configuraciones, en un formato declarativo ideal para control de versiones y CI/CD.',
  },
  {
    n: 7,
    d: 'CICD',
    m: 0,
    q: 'A data engineer has made changes to a notebook inside a Databricks Git folder on a feature branch and now wants to merge these changes into the main branch.\n\nWhat is the correct workflow for performing this operation?',
    o: [
      'Commit changes and merge the feature branch into main in the Databricks Git folder, then push the changes into the remote main branch.',
      'Commit and push changes to the remote feature branch, then use the GitHub UI or GitHub CLI to create a pull request (PR).',
      'Merge the feature branch into main in the Databricks Git folder, then commit and push the changes into the remote main branch.',
      'Commit and push changes to the remote feature branch, then use the Databricks Repos UI to create a pull request (PR).',
    ],
    a: [1],
    x: 'Los Git folders permiten commit, push, pull y crear ramas, pero NO crear pull requests. El flujo correcto es hacer commit y push a la rama remota y crear el PR desde el proveedor Git (GitHub UI o CLI) para revisión y merge a main.',
  },
  {
    n: 8,
    d: 'JOBS',
    m: 0,
    q: 'A data engineer builds a Lakeflow Job that ingests JSON logs from cloud storage. New files arrive at unpredictable times, and the team wants to automatically run the job as soon as new files land so the data is processed immediately.\n\nWhat trigger configuration should the data engineer use for this job?',
    o: ['File arrival trigger', 'Scheduled trigger', 'Table update trigger', 'Continuous trigger'],
    a: [0],
    x: 'El trigger de llegada de archivos (file arrival) monitoriza una ruta de almacenamiento (volumen UC o ubicación externa) y lanza el job cuando llegan archivos nuevos. Es ideal cuando la llegada es irregular; un scheduled trigger añadiría latencia y el continuous mantendría el job siempre en ejecución.',
  },
  {
    n: 9,
    d: 'P',
    m: 0,
    q: "According to the Databricks Lakehouse architecture, which of the following is located in the customer's cloud account?",
    o: ['Classic compute virtual machines', 'Workflows', 'Databricks web application', 'Notebooks'],
    a: [0],
    x: 'Con cómputo clásico, las máquinas virtuales del clúster se despliegan en el plano de cómputo (data plane) dentro de la cuenta cloud del cliente. La aplicación web, los workflows y los notebooks residen en el control plane gestionado por Databricks.',
  },
  {
    n: 10,
    d: 'CICD',
    m: 0,
    q: 'A data engineering team is developing a complex ETL pipeline on Databricks. They want to ensure that their workflow configurations are version-controlled and can be deployed reliably across staging and production environments.\n\nWhat is the most appropriate solution to achieve this task?',
    o: [
      "Leverage Notebook built-in version history for source control, and deploy jobs using serverless notebook's Environment",
      'Leverage Notebook built-in version history for source control, and create jobs using the Databricks UI',
      'Store the source code in Git folders, and deploy jobs using the Databricks REST API',
      'Store the source code in Git folders, and deploy jobs using Databricks Asset Bundles (DAB)',
    ],
    a: [3],
    x: 'Los DABs permiten definir jobs y flujos de forma declarativa en YAML, versionable en Git, con despliegues reproducibles entre entornos (staging/prod). Es la práctica DevOps recomendada frente al versionado del notebook o la creación manual desde la UI.',
  },
  {
    n: 11,
    d: 'ING',
    m: 0,
    q: 'A data engineer needs to use Lakeflow Connect to incrementally ingest customer data from a supported SaaS application into Unity Catalog-governed tables.\n\nWhich configuration step in Lakeflow Connect is required to achieve this ingestion?',
    o: [
      'Export JSON files from the SaaS application into a Unity Catalog volume and use Auto Loader to incrementally write the exported files into a Delta table.',
      'Export CSV files from the SaaS application into a Unity Catalog volume and use the COPY INTO command to incrementally load the exported files into a Delta table.',
      'Select a Unity Catalog connection that stores the application credentials, then configure a foreign catalog to directly access the SaaS application and load the data.',
      'Select a Unity Catalog connection that stores the application credentials, then configure the destination catalog and schema, and set the schedule and notifications.',
    ],
    a: [3],
    x: 'El flujo de un conector SaaS gestionado es: 1) conexión de Unity Catalog que guarda las credenciales, 2) catálogo y esquema de destino, 3) programación y notificaciones. No requiere exportar archivos ni código manual; los foreign catalogs pertenecen a Lakehouse Federation, no a Lakeflow Connect.',
  },
  {
    n: 12,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs the maximum and minimum transaction amounts for each customer:\n```\nfrom pyspark.sql import functions as F\n\nresult_df = df.____________("customer_id").agg(\n    F.max("transaction_amount").alias("max_transaction"),\n    F.min("transaction_amount").alias("min_transaction")\n)\n```\nWhich function correctly fills in the blank?',
    o: ['withColumn', 'select', 'window', 'groupBy'],
    a: [3],
    x: 'groupBy("customer_id") agrupa por cliente y permite aplicar agregaciones (max, min, sum, avg, count) dentro de cada grupo mediante agg(). Es el patrón estándar para estadísticas por clave.',
  },
  {
    n: 13,
    d: 'P',
    m: 0,
    q: 'Which of the following statements is NOT true about Delta Lake?',
    o: [
      'Delta Lake provides scalable data and metadata handling',
      'Delta Lake provides ACID transaction guarantees',
      'Delta Lake provides audit history and time travel',
      'Delta Lake builds upon standard data formats: Parquet + XML',
    ],
    a: [3],
    x: 'Delta Lake se construye sobre Parquet (archivos de datos) y JSON (log de transacciones), no sobre XML. Las demás afirmaciones (ACID, manejo escalable de datos/metadatos, historial y time travel) sí son características de Delta Lake.',
  },
  {
    n: 14,
    d: 'GOV',
    m: 0,
    q: 'A data engineer wants to apply row filters and column masks for every column tagged confidential_info across many tables in the catalog.\n\nWhich approach should the data engineer use to complete this task?',
    o: [
      'Create dynamic views manually for every table to enforce masking rules individually',
      'Apply Unity Catalog ABAC policies centrally across all relevant tables',
      'Write custom code logic to manage table masking rules dynamically',
      'Partition tables by confidential columns at table creation and restrict access at the partition level',
    ],
    a: [1],
    x: 'Las políticas ABAC (Attribute-Based Access Control) de Unity Catalog aplican filtros de fila y máscaras de columna según etiquetas (tags), propagándose automáticamente a todas las tablas actuales y futuras con ese tag. Las vistas dinámicas manuales no escalan y el particionado es optimización, no seguridad.',
  },
  {
    n: 15,
    d: 'ING',
    m: 0,
    q: 'Which of the following statements best describes Auto Loader?',
    o: [
      'Auto Loader enables efficient inserts, updates, deletes, and rollback capabilities by adding a storage layer that provides better data reliability to data lakes.',
      'Auto Loader allows cloning a source Delta table to a target destination at a specific version.',
      'Auto Loader allows applying a Change Data Capture (CDC) feed to update tables based on changes captured in source data.',
      'Auto Loader monitors a source location, in which files accumulate, to identify and ingest only new arriving files with each command run, while files that have already been ingested in previous runs are skipped.',
    ],
    a: [3],
    x: 'Auto Loader procesa incremental y eficientemente los archivos nuevos que llegan al almacenamiento cloud, omitiendo los ya ingeridos. La primera opción describe Delta Lake, la segunda CLONE y la tercera APPLY CHANGES/CDC.',
  },
  {
    n: 16,
    d: 'CICD',
    m: 0,
    q: 'A DAB defines the variable schema_name (default: demo_schema) used as a notebook base parameter (${var.schema_name}). The engineer deploys with:\n```\ndatabricks bundle deploy -t dev --var="schema_name=finance_schema"\n```\nand later runs:\n```\ndatabricks bundle run -t dev sales_job --var="schema_name=marketing_schema"\n```\nWhat value will be passed to the notebook parameter schema_name during the job run?',
    o: [
      'marketing_schema',
      'finance_schema',
      'The run will fail because variables cannot be passed to bundle run',
      'demo_schema',
    ],
    a: [1],
    x: 'Las variables de bundle se resuelven en tiempo de despliegue: con deploy, la definición del job queda fijada con finance_schema. El flag --var en bundle run no modifica variables ya sustituidas en el job desplegado, por lo que se usa finance_schema.',
  },
  {
    n: 17,
    d: 'ING',
    m: 1,
    q: 'In Lakeflow Connect, which of the following options are considered managed ingestion connectors?\n\n(Choose 2 answers)',
    o: [
      'Auto Loader',
      'Software as a Service (SaaS) connectors',
      '`CREATE TABLE AS (CTAS)`',
      'Database connectors',
      '`COPY INTO`',
    ],
    a: [1, 3],
    x: 'Los conectores gestionados (no-code) de Lakeflow Connect son los de aplicaciones SaaS (Salesforce, Workday, Jira...) y los de bases de datos (SQL Server, MySQL, PostgreSQL) con CDC. Auto Loader es un conector estándar basado en código, y COPY INTO/CTAS son comandos SQL.',
  },
  {
    n: 18,
    d: 'CICD',
    m: 0,
    q: 'A data platform team is setting up a CI/CD pipeline to deploy a Declarative Automation Bundle to the production workspace. The deployment must run non-interactively in an automated pipeline, ensuring that no confirmation prompts block execution.\n\nWhich deployment command should the team use?',
    o: [
      '```\ndatabricks bundle deploy --target prod --non-interactive\n```',
      '```\ndatabricks bundle deploy --target prod --confirm\n```',
      '```\ndatabricks bundle deploy --target prod --force\n```',
      '```\ndatabricks bundle deploy --target prod --auto-approve\n```',
    ],
    a: [3],
    x: 'El flag --auto-approve omite las confirmaciones interactivas, imprescindible en pipelines CI/CD sin terminal humano. --force solo salta la validación de rama Git, y --confirm/--non-interactive no existen.',
  },
  {
    n: 19,
    d: 'GOV',
    m: 0,
    q: 'A data engineering team working in Unity Catalog needs to ensure that hr_group does not retain any existing privileges on main.hr_schema before assigning a new, restricted set of permissions.\n\nWhat command should the team execute first?',
    o: [
      '```\nSHOW GRANTS hr_group ON SCHEMA main.hr_schema;\n```',
      '```\nSHOW GRANTS ON SCHEMA main.hr_schema;\n```',
      '```\nREVOKE ALL PRIVILEGES ON SCHEMA main.hr_schema FROM hr_group;\n```',
      '```\nDENY ALL PRIVILEGES ON SCHEMA main.hr_schema TO hr_group;\n```',
    ],
    a: [2],
    x: 'REVOKE ALL PRIVILEGES elimina todos los privilegios explícitos del grupo sobre el esquema (funciona aunque no tuviera algunos). SHOW GRANTS solo muestra permisos, y DENY es un comando legacy del Hive metastore que además bloquearía los nuevos permisos que se quieren asignar.',
  },
  {
    n: 20,
    d: 'TRO',
    m: 0,
    q: 'A data engineer is trying to use the Delta time travel feature to roll back a table to a previous version, but receives an error that the data files are no longer present.\n\nWhich of the following commands was run on the table that caused the deletion of the data files?',
    o: ['`OPTIMIZE`', '`DELETE`', '`VACUUM`', '`ZORDER BY`'],
    a: [2],
    x: 'VACUUM elimina los archivos de datos no referenciados que superan el periodo de retención, lo que impide viajar en el tiempo a versiones anteriores a ese umbral. OPTIMIZE/ZORDER compactan y reordenan, y DELETE marca lógicamente sin borrar físicamente los ficheros.',
  },
  {
    n: 21,
    d: 'TRO',
    m: 0,
    q: 'In the Spark UI, a stage contains 500 tasks. Most tasks complete in under 20 seconds, but a few tasks handling extremely large partitions take more than 25 minutes.\n\nWhat Spark issue is occurring?',
    o: ['Data skew', 'Executor memory leak', 'Small file problem', 'Network timeout'],
    a: [0],
    x: 'El data skew ocurre cuando los datos se distribuyen de forma desigual entre particiones: unas pocas claves muy populares crean particiones enormes y tareas rezagadas (stragglers). El problema de archivos pequeños daría miles de tareas milimétricas, y un memory leak o timeout provocaría fallos, no lentitud aislada.',
  },
  {
    n: 22,
    d: 'TRA',
    m: 0,
    q: "A data engineer is validating incoming transaction data. Some records contain unexpected symbols in numeric fields. The engineer runs:\n```\nSELECT TRY_CAST('100$' AS INT);\n```\nWhich of the following describes the result of this query?",
    o: ['An error is thrown', 'NULL', '100', '$100'],
    a: [1],
    x: "TRY_CAST funciona como CAST, pero cuando la conversión falla devuelve NULL en lugar de lanzar un error. Como '100$' contiene el símbolo $, no es convertible a entero y el resultado es NULL.",
  },
  {
    n: 23,
    d: 'TRO',
    m: 0,
    q: 'How long is the default retention period of the VACUUM command?',
    o: ['30 days', '365 days', '0 days', '7 days'],
    a: [3],
    x: 'El umbral de retención por defecto de VACUUM es de 7 días: no permite borrar archivos más recientes para evitar romper operaciones de larga duración que aún los referencien.',
  },
  {
    n: 24,
    d: 'TRO',
    m: 0,
    q: 'Which of the following commands can a data engineer use to compact small data files of a Delta table into larger ones?',
    o: ['`ZORDER BY`', '`COMPACT`', '`VACUUM`', '`OPTIMIZE`'],
    a: [3],
    x: 'OPTIMIZE compacta archivos pequeños en otros más grandes para acelerar las lecturas. ZORDER BY es una cláusula de co-localización que acompaña a OPTIMIZE, VACUUM borra archivos obsoletos y COMPACT no existe.',
  },
  {
    n: 25,
    d: 'TRA',
    m: 0,
    q: 'A team is comparing records between two datasets in Databricks SQL to return matching rows. The comparison must also treat NULL values as equal when they appear in both datasets.\n\nWhich join condition satisfies null-safe equality in Databricks SQL?',
    o: [
      'ON a.order_id = b.order_id AND (a.order_id IS NULL OR b.order_id IS NULL)',
      '`ON a.order_id <> b.order_id`',
      '`ON a.order_id <=> b.order_id`',
      '`ON a.order_id = b.order_id`',
    ],
    a: [2],
    x: 'El operador null-safe <=> devuelve true si ambos valores son iguales o si ambos son NULL (equivale a IS NOT DISTINCT FROM). Con = estándar, NULL = NULL evalúa a NULL y esas filas se pierden en el join.',
  },
  {
    n: 26,
    d: 'GOV',
    m: 1,
    q: 'A data engineer runs successfully:\n```\nGRANT SELECT ON TABLE bank_catalog.fraud_schema.alert_cases TO analyst_group;\n```\nHowever, members of analyst_group get an "Insufficient Permissions" error when querying the table.\n\nWhich TWO causes are most likely responsible for this issue?',
    o: [
      'The analyst_group does not have the USE CATALOG privilege on bank_catalog',
      'The data engineer is not the owner of the alert_cases table',
      'The analyst_group does not have the USE SCHEMA privilege on fraud_schema',
      'The analyst_group does not have the SELECT privilege on fraud_schema',
      'The analyst_group does not have the SELECT privilege on bank_catalog',
    ],
    a: [0, 2],
    x: 'Unity Catalog es jerárquico (catálogo → esquema → objeto): además de SELECT sobre la tabla, el principal necesita USE CATALOG sobre el catálogo y USE SCHEMA sobre el esquema para poder atravesar los contenedores padre.',
  },
  {
    n: 27,
    d: 'GOV',
    m: 0,
    q: 'A healthcare data engineer manages a Delta table patient_records (patient_id, name, department, diagnosis) and wants a UDF that masks the diagnosis column so that only doctors can view its values.\n\nWhich function achieves this?',
    o: [
      "```\nCREATE FUNCTION patient_mask(doctors STRING)\n  RETURN CASE WHEN is_account_group_member('diagnosis') THEN doctors ELSE 'CONFIDENTIAL' END;\n```",
      "```\nCREATE FUNCTION patient_mask(diagnosis STRING)\n  RETURN CASE WHEN is_account_group_member('doctors') THEN diagnosis ELSE 'CONFIDENTIAL' END;\n```",
      "```\nCREATE FUNCTION patient_mask(diagnosis STRING)\n  RETURN CASE WHEN is_account_group_member('doctors') THEN 'CONFIDENTIAL' ELSE diagnosis END;\n```",
      "```\nCREATE FUNCTION patient_mask(diagnosis STRING)\n  RETURN CASE WHEN diagnosis IS NOT NULL THEN diagnosis ELSE 'CONFIDENTIAL' END;\n```",
    ],
    a: [1],
    x: "La función correcta comprueba si el usuario pertenece al grupo 'doctors' con is_account_group_member: si es médico devuelve el diagnóstico real; si no, devuelve 'CONFIDENTIAL'. Las otras invierten la lógica o comprueban el grupo/parámetro equivocado.",
  },
  {
    n: 28,
    d: 'TRO',
    m: 0,
    q: 'What is the primary function of Liquid Clustering in Databricks?',
    o: [
      'To automate the creation of new data pipelines',
      'To improve the speed of network connectivity between nodes',
      'To incrementally optimize data layout for improved query performance',
      'To encrypt data stored in Delta Lake',
    ],
    a: [2],
    x: 'Liquid Clustering optimiza incrementalmente la disposición física de los datos según claves de clustering (columnas filtradas con frecuencia), reduciendo los datos leídos y mejorando el rendimiento de las consultas. Sustituye al particionado tradicional y a Z-Order.',
  },
  {
    n: 29,
    d: 'TRO',
    m: 1,
    q: 'Which TWO of the following describe benefits of enabling predictive optimization on managed tables in Unity Catalog?',
    o: [
      'It reduces overall cost by forecasting storage usage and reallocating data across tiers.',
      'It simplifies maintenance by automatically running maintenance operations on the table.',
      'It enhances query performance by collecting statistics as data is written to the table.',
      'It boosts data privacy by automatically encrypting data on write and masking sensitive columns.',
      'It improves data profiling by automatically predicting missing values in the table columns.',
    ],
    a: [1, 2],
    x: 'Predictive Optimization ejecuta automáticamente operaciones de mantenimiento (OPTIMIZE, VACUUM, ANALYZE) en segundo plano y recopila estadísticas al escribir, lo que mejora las decisiones del optimizador de consultas. No cifra datos ni predice valores faltantes.',
  },
  {
    n: 30,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has a DataFrame, df, with a date_str column formatted as year-month, such as "2026-01", and wants a new column, month, containing only the month value.\n\nWhich PySpark code snippet should the data engineer use?',
    o: [
      '```\nresultDf = df.withColumn("month", F.split(F.col("date_str"), "-").getItem(2))\n```',
      '```\nresultDf = df.withColumn("month", F.split(F.col("date_str"), "-0").getItem(2))\n```',
      '```\nresultDf = df.withColumn("month", F.split(F.col("date_str"), "-0").getItem(1))\n```',
      '```\nresultDf = df.withColumn("month", F.split(F.col("date_str"), "-").getItem(1))\n```',
    ],
    a: [3],
    x: 'split con el delimitador "-" convierte "2026-01" en ["2026","01"]; con índice base 0, getItem(1) devuelve el mes. Usar "-0" como delimitador fallaría para meses como octubre ("2026-10").',
  },
  {
    n: 31,
    d: 'GOV',
    m: 0,
    q: '"A feature that illustrates the relationship between different data assets including tables, queries, notebooks, and dashboards, enabling users to trace the origin and flow of data across the entire lakehouse platform."\n\nWhich of the following is being described?',
    o: ['Databricks Lakeflow', 'Databricks Jobs', 'Delta Live Tables DAGs', 'Unity Catalog Data Lineage'],
    a: [3],
    x: 'El linaje de datos (Data Lineage) de Unity Catalog rastrea cómo se crean, transforman y consumen los datos entre tablas, consultas, notebooks y dashboards. Es clave para gobernanza, auditoría y fiabilidad.',
  },
  {
    n: 32,
    d: 'TRA',
    m: 0,
    q: 'In the Medallion Architecture, which of the following statements best describes the Silver layer tables?',
    o: [
      'The table structure in this layer resembles that of the source system table structure with any additional metadata columns like the load time and input file name.',
      'They maintain data that powers analytics, machine learning, and production applications',
      "They provide a more refined view of raw data, where it's filtered, cleaned, and enriched.",
      'They maintain raw data ingested from various sources',
    ],
    a: [2],
    x: 'La capa Silver ofrece una vista refinada de los datos crudos: filtrados, limpios y enriquecidos (por ejemplo, uniendo varias tablas Bronze). La primera y cuarta opciones describen Bronze; la segunda describe Gold.',
  },
  {
    n: 33,
    d: 'ING',
    m: 0,
    q: 'A streaming ETL job uses Auto Loader with the default schema evolution mode "addNewColumns" for ingesting sales data into a Delta table. After deployment, producers start sending a new column named discount.\n\nHow does Auto Loader handle this field?',
    o: [
      'discount is added automatically, and all previous rows that do not match the new schema are deleted from the table.',
      'discount is added automatically as a nullable column, with values populated only for new records.',
      'discount is added automatically and all previous rows are rewritten with default discount = 0.',
      'discount is upserted using an automatic merge operation, enabling previous rows to be updated based on matching keys',
    ],
    a: [1],
    x: 'Con addNewColumns, Auto Loader evoluciona el esquema añadiendo la columna nueva como nullable. No reescribe el histórico: las filas antiguas devuelven null en esa columna. Nunca borra datos ni ejecuta MERGE automáticos.',
  },
  {
    n: 34,
    d: 'CICD',
    m: 0,
    q: 'A data engineer is configuring a bundle project (databricks.yml) and wants to define a target environment "dev" for development, setting the host URL and a root storage path.\n\nWhich configuration meets this requirement?',
    o: [
      '```\nenvironments:\n  dev:\n    mode: development\n    workspace:\n      host: https://adb-1234.net\n      root_path: /Users/dev.user/.bundle/${bundle.name}\n```',
      '```\ntargets:\n  dev:\n    mode: development\n    workspace:\n      host: https://adb-1234.net\n      root_path: /Users/dev.user/.bundle/${bundle.name}\n```',
      '```\nworkspaces:\n  target: dev\n  host: https://adb-1234.net\n  root_path: /Users/dev.user/.bundle/${bundle.name}\n```',
      '```\nhosts:\n  dev:\n    URL: https://adb-1234.net\n    root_path: /Users/dev.user/.bundle/${bundle.name}\n```',
    ],
    a: [1],
    x: 'Los entornos de despliegue se definen bajo la clave de nivel superior targets, y dentro de cada target se anida un bloque workspace con host y root_path. Las claves environments, workspaces y hosts no son válidas en el esquema de DABs.',
  },
  {
    n: 35,
    d: 'TRA',
    m: 0,
    q: 'A retail data analyst ingests transactions continuously with Lakeflow Declarative Pipelines and needs a relational object that efficiently precomputes business-level aggregations (total revenue, average order value, units sold per category) so dashboards can access the data quickly without recalculating it every time.\n\nWhich object is most suitable?',
    o: ['Temporary view', 'Streaming table', 'Streaming view', 'Materialized view'],
    a: [3],
    x: 'Una vista materializada precalcula y almacena agregaciones de negocio para consumo rápido de informes y dashboards, sin recalcular en cada consulta. Las vistas temporales/estándar recalculan o duran una sesión, y las streaming tables sirven para ingesta de eventos, no para agregados.',
  },
  {
    n: 36,
    d: 'GOV',
    m: 0,
    q: "When dropping a Delta table, which of the following explains why only the table's metadata will be deleted, while the data files will be kept in storage?",
    o: [
      'The table is managed',
      'The table is external',
      'The user running the command has no permission to delete the data files',
      'Delta prevents deleting files newer than the retention threshold, to ensure that no long-running operations are still referencing the files to be deleted',
    ],
    a: [1],
    x: 'Las tablas externas (con cláusula LOCATION) mantienen sus datos en una ruta externa: DROP TABLE solo elimina los metadatos y conserva los archivos. En una tabla gestionada se borran metadatos y datos.',
  },
  {
    n: 37,
    d: 'TRA',
    m: 0,
    q: 'A PySpark DataFrame df contains online store visits with columns user_id, product_id, and timestamp. The engineer wants uniqueness at the product-view level, meaning each user should only have one record per product.\n\nWhich PySpark code correctly implements this logic?',
    o: [
      '```\ndf.dropDuplicates()\n```',
      '```\ndf.dropDuplicates(["timestamp"])\n```',
      '```\ndf.dropDuplicates(["user_id", "product_id", "timestamp"])\n```',
      '```\ndf.dropDuplicates(["user_id", "product_id"])\n```',
    ],
    a: [3],
    x: 'dropDuplicates con la lista ["user_id","product_id"] considera duplicadas las filas con la misma combinación usuario-producto y conserva la primera. Incluir timestamp solo eliminaría vistas idénticas al milisegundo, y sin argumentos exige que todas las columnas coincidan.',
  },
  {
    n: 38,
    d: 'JOBS',
    m: 0,
    q: 'A data engineering team has a long-running multi-task Job. The team members need to be notified when the run of this job completes.\n\nWhich of the following approaches can be used to send emails to the team members when the job completes?',
    o: [
      'They can use the Job API to programmatically send emails according to each task status',
      'Only the Job owner can be configured to be notified when the job completes',
      'They can configure email notification settings in the job page',
      'There is no way to notify users when the job completes',
    ],
    a: [2],
    x: 'Los Lakeflow Jobs soportan notificaciones por email en inicio, éxito o fallo. En el panel de detalles del job (Job notifications → Edit notifications) se pueden añadir una o más direcciones de correo.',
  },
  {
    n: 39,
    d: 'GOV',
    m: 0,
    q: 'A data engineer uses the following SQL query:\n```\nGRANT MODIFY ON TABLE employees TO hr_team\n```\nWhich of the following describes the ability given by the MODIFY privilege?',
    o: [
      'It gives the ability to add data to the table',
      'It gives the ability to add, update, or delete data within the table',
      'It gives the ability to delete data from the table',
      'It gives the ability to modify data in the table',
    ],
    a: [1],
    x: 'El privilegio MODIFY otorga la capacidad de añadir (INSERT), actualizar (UPDATE) y eliminar (DELETE) datos del objeto; no se limita a una sola de esas operaciones.',
  },
  {
    n: 40,
    d: 'TRA',
    m: 0,
    q: 'A data engineer is exploring a large dataset in Databricks and executes df.summary() to better understand the data.\n\nWhich of the following best describes the output of this command?',
    o: [
      'A visualization dashboard showing trends and charts, including histograms, pie charts, and interactive graphs generated automatically from the DataFrame.',
      'A DataFrame containing statistical metrics, including count, mean, standard deviation, min, max, and approximate quartile values for each column.',
      'An AI-generated summary of the dataset that provides insights, patterns, and recommendations based on the underlying data values.',
      'A Spark execution plan for DataFrame transformations, including details about logical and physical query optimization steps.',
    ],
    a: [1],
    x: 'df.summary() devuelve un DataFrame con count, mean, stddev, min, max y cuartiles aproximados (25%, 50%, 75%) por columna. Es una herramienta de análisis exploratorio, no genera gráficos ni planes de ejecución.',
  },
  {
    n: 41,
    d: 'JOBS',
    m: 0,
    q: 'A data engineer maintains a Lakeflow Job where Task B is configured with run_if: ALL_SUCCESS, and Task C is configured with run_if: ALL_DONE (both depend on Task A).\n\nWhich of the following correctly describes this logic?',
    o: [
      'Task B runs only if Task A succeeds, and Task C runs only if Task A does not succeed.',
      "Task B runs if Tasks A and C succeed, and Task C runs regardless of other tasks' outcomes.",
      "Task B runs only if Task A succeeds, and Task C runs regardless of Task A's outcome.",
      'Task B runs if Tasks A and C succeed, and Task C runs only if Task A is skipped and retried.',
    ],
    a: [2],
    x: 'ALL_SUCCESS (por defecto) exige que todas las dependencias terminen con éxito, así que B solo corre si A tiene éxito. ALL_DONE se dispara cuando las dependencias terminan sea cual sea su estado (éxito, fallo, omitida), por lo que C corre siempre que A finalice.',
  },
  {
    n: 42,
    d: 'TRO',
    m: 0,
    q: 'A team runs a Spark SQL query joining a large fact table with a small dimension table. After enabling Adaptive Query Execution (AQE), they notice improved performance without changing the query logic.\n\nWhat is the primary reason AQE improves performance in this scenario?',
    o: [
      'AQE automatically converts the join to a broadcast join at runtime based on actual data statistics',
      'AQE disables shuffling entirely for all joins',
      'AQE rewrites the query into Python code for faster execution',
      'AQE forces all joins to use sort-merge join regardless of data size',
    ],
    a: [0],
    x: 'AQE usa estadísticas reales en tiempo de ejecución: si la tabla pequeña queda por debajo del umbral de broadcast, convierte el sort-merge join en broadcast hash join, eliminando el shuffle costoso de la tabla grande. No desactiva shuffles ni reescribe a Python.',
  },
  {
    n: 43,
    d: 'P',
    m: 0,
    q: 'A data engineer needs to schedule a daily job to read data from an on-premises PostgreSQL database hosted inside a corporate data center, and must choose the compute option that ensures reliable connectivity to the on-prem system.\n\nWhich compute type should the data engineer use?',
    o: [
      'Classic job compute',
      'Serverless job compute',
      'All-purpose classic compute',
      'Serverless SQL warehouse',
    ],
    a: [0],
    x: 'El cómputo clásico se despliega en la red virtual del cliente (VNet/VPC), aprovechando VPN o enlaces dedicados hacia el data center on-premises. El serverless corre en la cuenta de Databricks y no llega a redes locales; el all-purpose encarecería un job automatizado.',
  },
  {
    n: 44,
    d: 'ING',
    m: 0,
    q: 'Given the following Structured Streaming query:\n```\n(spark.readStream\n    .format("cloudFiles")\n    .option("cloudFiles.format", "json")\n    .load(ordersLocation)\n  .writeStream\n    .option("checkpointLocation", checkpointPath)\n    .table("uncleanedOrders"))\n```\nWhich of the following best describes the purpose of this query in a Medallion Architecture?',
    o: [
      'The query is performing a hop from a Bronze table to a Silver table',
      'The query is performing a hop from a Silver table to a Gold table',
      'The query is performing raw data ingestion into a Bronze table',
      'The query is performing data transfer from a Gold table into a production application',
    ],
    a: [2],
    x: 'La consulta usa Auto Loader (cloudFiles) para cargar JSON crudo desde una ubicación de almacenamiento a la tabla Bronze uncleanedOrders: es ingesta de datos en bruto, el primer salto de la arquitectura medallion.',
  },
  {
    n: 45,
    d: 'JOBS',
    m: 0,
    q: 'A data engineer designs a Lakeflow Job with multiple notebook tasks. They want to configure the parameter processing_region once for the entire job so that every task receives the same value, and the value should be changeable at runtime from the Jobs UI without modifying notebook code.\n\nWhich configuration approach best satisfies these requirements?',
    o: [
      'Define processing_region as a job-level parameter and reference it in each task using the dynamic value {{job.parameters.processing_region}}',
      'Define processing_region as a cluster environment variable and access it in notebooks using spark.conf.get.',
      'Pass processing_region only to the first notebook task and forward it manually to downstream notebooks using dbutils.notebook.run.',
      'Hard-code processing_region separately inside every notebook to ensure consistency across tasks.',
    ],
    a: [0],
    x: 'Los parámetros a nivel de job se definen una sola vez, se inyectan en cada tarea con {{job.parameters.nombre}} y pueden sobrescribirse desde la UI en "Run now with different parameters". Las variables de clúster requieren reinicios y dbutils.notebook.run rompe la orquestación del DAG.',
  },
  {
    n: 46,
    d: 'GOV',
    m: 0,
    q: 'Which of the following commands can a data engineer use to grant full permissions on the table employees to the HR team?',
    o: [
      '```\nGRANT FULL PRIVILEGES ON TABLE employees TO hr_team\n```',
      '```\nGRANT ALL PRIVILEGES ON employees TO hr_team\n```',
      '```\nGRANT ALL PRIVILEGES ON TABLE employees TO hr_team\n```',
      '```\nGRANT SELECT, MODIFY, CREATE, READ_METADATA ON TABLE employees TO hr_team\n```',
    ],
    a: [2],
    x: 'ALL PRIVILEGES otorga permisos completos sobre un objeto. La sintaxis correcta es GRANT <privilegio> ON TABLE <tabla> TO <principal>; FULL PRIVILEGES no existe y omitir TABLE es incorrecto en esta forma.',
  },
  {
    n: 47,
    d: 'TRO',
    m: 0,
    q: 'A join between clickstream logs and user profiles (on user_id) is slow due to data skew caused by a small subset of users generating a disproportionately large number of events.\n\nWhich of the following approaches is NOT an appropriate solution to mitigate the skew?',
    o: [
      'Broadcast the skewed keys to all worker nodes to avoid shuffle during the join.',
      'Use salting by appending a random prefix to skewed user_id values to distribute the load across partitions.',
      'Repartition the clickstream dataset to increase the number of partitions before the join.',
      'Separate processing of skewed keys by handling high-frequency users in a dedicated job.',
    ],
    a: [0],
    x: 'El broadcast sirve para difundir tablas pequeñas; difundir claves con mucho volumen no resuelve el sesgo y aumenta la presión de memoria en cada executor. Salting, reparticionar y aislar las claves calientes sí son técnicas válidas de mitigación.',
  },
  {
    n: 48,
    d: 'GOV',
    m: 0,
    q: 'A team manages the Delta table infra_catalog.ops_db.device_logs, originally created as an external table, and now wants Databricks to fully manage the table lifecycle, including automatic cleanup of underlying files when the table is dropped.\n\nWhich command converts this external table into a managed table while keeping the same table name, permissions, and history?',
    o: [
      '```\nALTER TABLE infra_catalog.ops_db.device_logs SET MANAGED;\n```',
      '```\nCREATE OR CONVERT TABLE infra_catalog.ops_db.device_logs AS MANAGED;\n```',
      '```\nALTER TABLE infra_catalog.ops_db.device_logs SET type = "MANAGED";\n```',
      '```\nCREATE OR REPLACE TABLE infra_catalog.ops_db.device_logs;\n```',
    ],
    a: [0],
    x: 'ALTER TABLE ... SET MANAGED convierte una tabla externa de Unity Catalog en gestionada conservando historial (time travel), nombre, permisos, tags y vistas asociadas. Las otras sintaxis no existen o recrearían la tabla perdiendo su configuración.',
  },
  {
    n: 49,
    d: 'ING',
    m: 0,
    q: 'Given the following Structured Streaming query:\n```\n(spark.table("orders")\n    .withColumn("total_after_tax", col("total")+col("tax"))\n  .writeStream\n    .option("checkpointLocation", checkpointPath)\n    .outputMode("append")\n    .______________\n    .table("new_orders"))\n```\nFill in the blank to make the query execute a micro-batch to process data every 30 seconds.',
    o: [
      '`trigger("30 seconds")`',
      '`processingTime("30 seconds")`',
      '`trigger(once="30 seconds")`',
      '`trigger(processingTime="30 seconds")`',
    ],
    a: [3],
    x: 'En Structured Streaming, trigger(processingTime="30 seconds") ejecuta micro-batches en el intervalo indicado. processingTime es un argumento de trigger, no un método, y once no acepta duraciones.',
  },
  {
    n: 50,
    d: 'JOBS',
    m: 0,
    q: 'A team builds a Lakeflow Job with an ingestion task followed by two possible execution paths: one for streaming processing and one for batch processing. They want to select the appropriate path dynamically based on a processing_mode parameter.\n\nWhich solution best supports conditional task execution in a Lakeflow Job?',
    o: [
      'Use an if/else conditional task to route execution based on the processing_mode parameter.',
      'Execute both tasks in parallel and evaluate the processing_mode parameter inside each task.',
      'Execute both tasks in parallel and let downstream tasks ignore irrelevant outputs based on the parameter.',
      'Combine streaming and batch logic in a single notebook task and determine the execution flow programmatically within the code.',
    ],
    a: [0],
    x: 'Los Lakeflow Jobs incluyen el tipo de tarea If/else condition para introducir bifurcaciones booleanas en el DAG: evalúa el parámetro en la capa de orquestación y ejecuta solo la rama pertinente, ahorrando cómputo.',
  },
  {
    n: 51,
    d: 'CICD',
    m: 0,
    q: 'Which Databricks CLI command is used to create a new asset bundle project in a local directory?',
    o: [
      '`databricks bundle initialize`',
      '`databricks bundle new`',
      '`databricks bundle create`',
      '`databricks bundle init`',
    ],
    a: [3],
    x: 'databricks bundle init inicializa un proyecto DAB local, pidiendo interactivamente una plantilla (por ejemplo, proyecto Python por defecto) y la configuración inicial. Los demás subcomandos no existen.',
  },
  {
    n: 52,
    d: 'ING',
    m: 0,
    q: 'An Auto Loader stream is configured with .schema(expected_schema) and .option("cloudFiles.schemaEvolutionMode", "failOnNewColumns").\n\nWhat is the expected behavior of this streaming job if a new column appears in the incoming JSON files that is not part of the original schema?',
    o: [
      'The stream fails, and all new columns are saved in a rescued data column for later processing.',
      'The stream fails temporarily but continues by ignoring the new columns without a schema update.',
      'The stream fails, but it automatically restarts after updating the schema with the new columns.',
      'The stream fails and will not restart unless the schema is manually updated or the problematic data file is removed.',
    ],
    a: [3],
    x: 'failOnNewColumns impone consistencia estricta: al detectar columnas nuevas el stream falla y no se reanuda hasta actualizar el esquema manualmente o eliminar el archivo problemático. Evita el schema drift silencioso.',
  },
  {
    n: 53,
    d: 'P',
    m: 0,
    q: 'Which of the following is the benefit of using the Auto Stop feature of Databricks SQL warehouses?',
    o: [
      'Increases the availability of the warehouse by automatically stopping long-running SQL queries',
      'Improves the performance of the warehouse by automatically stopping idle services',
      'Provides higher security by automatically stopping unused ports of the warehouse',
      'Minimizes the total running time of the warehouse',
    ],
    a: [3],
    x: 'Auto Stop detiene el warehouse cuando permanece inactivo durante el número de minutos configurado, minimizando el tiempo total encendido y, con ello, el coste. No detiene consultas ni puertos.',
  },
  {
    n: 54,
    d: 'ING',
    m: 0,
    q: 'A team ingests 100,000 new small JSON files per day into a Delta table from Azure Data Lake Storage. They require low-latency incremental ingestion and want to minimize storage API costs caused by frequent directory scans.\n\nWhich ingestion configuration should the team prioritize?',
    o: [
      'Use Auto Loader in directory listing mode with a reduced interval to frequently check for new files.',
      'Use Spark Structured Streaming with manual file tracking using a metadata Delta table.',
      'Use COPY INTO scheduled every 5 minutes with recursive directory scanning.',
      'Use Auto Loader with cloudFiles.useNotifications = true to enable event-based file detection',
    ],
    a: [3],
    x: 'cloudFiles.useNotifications = true cambia de listado de directorios a detección por eventos (Event Grid en Azure): el almacenamiento avisa a Auto Loader cuando llega un archivo. Reduce latencia y coste de llamadas LIST a gran escala.',
  },
  {
    n: 55,
    d: 'TRA',
    m: 0,
    q: 'A data analyst works with two PySpark DataFrames: colors_df (available product colors) and sizes_df (available product sizes). They need to generate all possible color-size combinations for inventory planning.\n\nWhich PySpark code achieves this?',
    o: [
      '```\njoined_df = colors_df.join(sizes_df, "color_id", "left")\n```',
      '```\njoined_df = colors_df.join(sizes_df, "color_id", "full")\n```',
      '```\njoined_df = colors_df.crossJoin(sizes_df)\n```',
      '```\njoined_df = colors_df.join(sizes_df, "color_id", "inner")\n```',
    ],
    a: [2],
    x: 'Para generar todas las combinaciones posibles (producto cartesiano) se usa crossJoin(): cada fila del primer DataFrame se combina con cada fila del segundo (10 colores × 5 tallas = 50 combinaciones). Los joins por clave solo emparejarían IDs coincidentes.',
  },
];

export const exam2: RawQuestion[] = [
  {
    n: 1,
    d: 'JOBS',
    m: 0,
    q: 'A data engineer analyzes a job run that succeeded but also contained failures: Task A finished successfully, Task B failed, and Task C (which depends on both A and B) finished successfully.\n\nWhich of the following explains why the job run was successful even though not all tasks completed successfully?',
    o: [
      'Task A is configured with run_if: At least one succeeded',
      'Task B is configured with run_if: At least one failed',
      'All tasks are configured with run_if: All done',
      'Task C is configured with run_if: At least one failed',
    ],
    a: [3],
    x: 'Con la condición run_if "At least one failed", el fallo de la Tarea B satisface el disparador de la Tarea C, que se ejecuta y termina con éxito. Con el valor por defecto (All succeeded), C se habría marcado como "Upstream failed".',
  },
  {
    n: 2,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has defined the following data quality constraint in a Lakeflow Spark Declarative Pipeline (formerly Delta Live Tables):\n\nCONSTRAINT valid_id EXPECT (id IS NOT NULL) _____________\n\nFill in the blank so that records violating this constraint are dropped and reported in metrics.',
    o: [
      'There is no need to add the ON VIOLATION clause. By default, records violating the constraint are dropped and reported as invalid in the event log.',
      '`ON VIOLATION FAIL UPDATE`',
      '`ON VIOLATION DELETE ROW`',
      '`ON VIOLATION DROP ROW`',
    ],
    a: [3],
    x: 'ON VIOLATION DROP ROW descarta los registros que incumplen la expectativa y registra las violaciones en el event log (métricas). Por defecto (sin cláusula) los registros inválidos se conservan; FAIL UPDATE haría fallar la actualización.',
  },
  {
    n: 3,
    d: 'TRA',
    m: 0,
    q: "A data engineer is cleaning transaction data before loading it into a Silver table using the following PySpark command:\n```\ndf.dropna(subset=['order_id', 'payment_method'])\n```\nWhat is this command used for?",
    o: [
      'Drop both columns from the DataFrame df.',
      'Drop rows where both columns contain null values.',
      'Replace null values in order_id and payment_method with empty strings.',
      'Drop rows where order_id or payment_method contain null values.',
    ],
    a: [3],
    x: "dropna usa how='any' por defecto: al pasar subset, elimina la fila si encuentra null en order_id O en payment_method. Para exigir que ambas sean null habría que usar how='all'; para reemplazar valores se usa fillna().",
  },
  {
    n: 4,
    d: 'TRA',
    m: 0,
    q: 'A data engineer designs an ETL pipeline with Lakeflow Declarative Pipelines to incrementally process incoming data streams using Auto Loader for near real-time ingestion, continuously capturing and loading new records as they arrive.\n\nWhich type of object would be most suitable for this use case?',
    o: ['Streaming table', 'Materialized view', 'Streaming view', 'Temporary view'],
    a: [0],
    x: 'Las streaming tables están diseñadas para ingesta incremental casi en tiempo real: capturan continuamente registros nuevos vía Auto Loader. Las vistas materializadas son de procesamiento batch/programado y las vistas temporales son efímeras.',
  },
  {
    n: 5,
    d: 'TRA',
    m: 0,
    q: 'A data engineer defines a table in a Lakeflow Spark Declarative Pipeline with the decorators @dp.expect_or_drop("recent_transaction", "transaction_date >= \'2025-01-01\'") and @dp.expect_or_drop("valid_transaction", "transaction_id IS NOT NULL") over silver_sales, reading from bronze_sales.\n\nWhich of the following correctly describes the result of running this pipeline?',
    o: [
      'Rows that violate the defined expectations are deleted from the bronze_sales table.',
      'Rows that violate the defined expectations are filtered out, and only valid rows are written to silver_sales',
      'Rows that violate the defined expectations are streamed into the silver_sales table.',
      'Rows that violate the defined expectations are deleted from both tables.',
    ],
    a: [1],
    x: 'expect_or_drop descarta (filtra) las filas que incumplen la expectativa antes de escribir en la tabla destino: solo las filas válidas llegan a silver_sales. La tabla origen bronze_sales no se modifica nunca.',
  },
  {
    n: 6,
    d: 'TRA',
    m: 0,
    q: 'A data engineer uses the following PySpark code to transform a DataFrame, df:\n```\ndf.na.fill({"source": "unknown"})\n```\nWhich of the following statements best describes this transformation?',
    o: [
      'Rows with source="unknown" are converted into null values',
      'Rows with source="unknown" are filtered out',
      'Null values in the source column are assigned the value "unknown"',
      'All values in the source column are assigned the value "unknown"',
    ],
    a: [2],
    x: 'na.fill (alias de fillna) reemplaza los valores nulos; al pasar un diccionario, la clave indica la columna y el valor el sustituto. Solo los null de la columna source pasan a "unknown"; el resto queda intacto.',
  },
  {
    n: 7,
    d: 'GOV',
    m: 0,
    q: 'A data engineer must create a Unity Catalog table with customer information, including email addresses and phone numbers. Users should be able to query the table, but personally identifiable information (PII) must not be exposed to those without proper access.\n\nWhich method should the engineer use to efficiently enforce this requirement?',
    o: [
      'Use row-level filters to restrict access to region-specific customers',
      'Use table object privileges to revoke access on sensitive PII columns',
      'Use a dynamic view to redact sensitive PII columns',
      'Apply column masks to configure fine-grained access control',
    ],
    a: [3],
    x: 'Las máscaras de columna (column masks) de Unity Catalog son UDFs SQL que transforman el valor en tiempo de consulta según la identidad del usuario (cláusula MASK). Son la solución nativa y eficiente; las vistas dinámicas exigen mantenimiento manual, los privilegios de tabla no actúan por columna y los filtros de fila no protegen columnas.',
  },
  {
    n: 8,
    d: 'TRA',
    m: 0,
    q: 'A data analyst works with two PySpark DataFrames: doctors_df and appointments_df, both containing a doctor_id column. They need a new DataFrame that includes only doctors who have scheduled appointments, combining columns from both DataFrames.\n\nWhich PySpark code achieves this?',
    o: [
      '```\njoined_df = doctors_df.join(appointments_df, "doctor_id", "cross")\n```',
      '```\njoined_df = doctors_df.join(appointments_df, "doctor_id", "full")\n```',
      '```\njoined_df = doctors_df.join(appointments_df, "doctor_id", "left")\n```',
      '```\njoined_df = doctors_df.join(appointments_df, "doctor_id", "inner")\n```',
    ],
    a: [3],
    x: 'El inner join conserva solo las filas con coincidencia en ambos DataFrames: médicos con citas. left incluiría médicos sin citas (con null), full incluiría todo y cross generaría el producto cartesiano.',
  },
  {
    n: 9,
    d: 'GOV',
    m: 0,
    q: "When dropping a Delta table, which of the following explains why both the table's metadata and the data files will be deleted?",
    o: [
      'The table is managed',
      'The data files are older than the default retention period',
      'The table is external',
      'The table is shallow cloned',
    ],
    a: [0],
    x: 'En las tablas gestionadas (managed), Databricks administra metadatos y datos: DROP TABLE elimina ambos. En una tabla externa solo se borran los metadatos y los archivos permanecen.',
  },
  {
    n: 10,
    d: 'TRA',
    m: 0,
    q: 'Two tables, students and enrollments, are joined on student_id. The desired result returns all students with their course_id, showing NULL for students not enrolled in any course:\n```\nSELECT students.name, students.age, enrollments.course_id\nFROM students\n```\n_____________ enrollments\n```\nON students.student_id = enrollments.student_id\n```\nFill in the blank.',
    o: ['`INNER JOIN`', '`RIGHT JOIN`', '`ANTI JOIN`', '`LEFT JOIN`'],
    a: [3],
    x: 'LEFT JOIN devuelve todas las filas de la tabla izquierda y las coincidencias de la derecha, rellenando con NULL cuando no hay match (por ejemplo, un alumno sin matrículas muestra NULL en course_id).',
  },
  {
    n: 11,
    d: 'GOV',
    m: 1,
    q: 'A data engineering team is implementing a new analytics pipeline that needs to read governed tables in Unity Catalog. Which TWO compute types support access to Unity Catalog data?',
    o: [
      'Classic compute that runs Databricks Runtime below 11.3 LTS',
      'SQL warehouse compute',
      'Classic compute that runs Databricks Runtime below 10.4 LTS',
      'Classic compute configured with "No isolation shared" access mode',
      'Classic compute configured with standard or dedicated access mode',
    ],
    a: [1, 4],
    x: 'Unity Catalog está soportado en los SQL warehouses por defecto y en clústeres clásicos con modo de acceso Standard (antes Shared) o Dedicated (antes Single User). El modo "No isolation shared" no cumple el aislamiento requerido y se necesita Runtime 11.3 LTS o superior.',
  },
  {
    n: 12,
    d: 'P',
    m: 0,
    q: 'A data engineer is troubleshooting high cloud costs of a job cluster configured with a fixed size of 15 workers, but the workload typically requires fewer resources except during peak processing periods.\n\nWhich recommendation would best optimize compute usage while reducing cost?',
    o: [
      'Switch to smaller instance types while maintaining a fixed-size cluster configuration',
      'Configure autoscaling with appropriate minimum and maximum worker limits',
      'Replace the job cluster with an all-purpose cluster to improve cluster availability',
      'Configure auto-termination after periods of inactivity during idle times',
    ],
    a: [1],
    x: 'El autoscaling escala los workers hacia arriba en picos y hacia abajo en valles, con límites mínimo y máximo: paga solo lo necesario sin sacrificar rendimiento. Los all-purpose son más caros por DBU, los job clusters ya terminan al acabar y reducir instancias fijas arriesga fallos OOM en picos.',
  },
  {
    n: 13,
    d: 'TRO',
    m: 0,
    q: '"An optimization technique where a copy of a small table is cached on all nodes of the cluster for use in all future queries during the cluster lifetime, eliminating data shuffling."\n\nWhich of the following is being described?',
    o: ['Broadcast Join', 'Cross Join', '`OPTIMIZE`', 'Delta caching'],
    a: [0],
    x: 'El Broadcast Join copia la tabla pequeña a todos los nodos ejecutores para que el join se resuelva localmente sin barajar (shuffle) la tabla grande por la red. OPTIMIZE compacta archivos, Delta caching cachea lecturas y Cross Join es un tipo de join, no una optimización.',
  },
  {
    n: 14,
    d: 'ING',
    m: 0,
    q: 'In Lakeflow Connect, which of the following statements is NOT true about managed ingestion connectors?',
    o: [
      'The resulting ingestion pipeline is governed by Unity Catalog and is powered by serverless compute and Lakeflow Spark Declarative Pipelines.',
      'Managed connectors support ingesting data from enterprise databases and Software as a Service (SaaS) applications.',
      'Managed connectors require writing custom Python code to handle the full authentication flow, data update logic, and network requests.',
      'Managed connectors leverage efficient incremental reads and writes to make data ingestion faster, scalable, and more cost-efficient.',
    ],
    a: [2],
    x: 'Los conectores gestionados eliminan precisamente la necesidad de código propio: se configuran con UI o API declarativa y Databricks gestiona autenticación, peticiones y lógica incremental (CDC). Las otras tres afirmaciones son verdaderas.',
  },
  {
    n: 15,
    d: 'TRO',
    m: 0,
    q: 'What is a key benefit of Liquid Clustering for analytical workloads in Databricks?',
    o: [
      'It prevents data duplication, thereby enhancing query performance',
      'It reduces the volume of scanned data during query execution',
      'It encrypts sensitive data fields automatically during ingestion',
      'It ensures real-time streaming from the input data source',
    ],
    a: [1],
    x: 'Al agrupar físicamente los datos por claves de clustering, Liquid Clustering habilita el data skipping: las consultas leen menos archivos, reduciendo el volumen escaneado y el tiempo de respuesta. No cifra datos ni gestiona streaming.',
  },
  {
    n: 16,
    d: 'TRO',
    m: 0,
    q: 'A data engineer runs df.collect() on a large Spark DataFrame and the job fails with a driver OOM error. What is the most likely cause?',
    o: [
      '```\nspark.driver.memory is insufficient to hold the collected data\n```',
      '```\nspark.executor.cores is too low\n```',
      '```\nspark.sql.autoBroadcastJoinThreshold is too high\n```',
      '```\nspark.memory.fraction is too high\n```',
    ],
    a: [0],
    x: 'collect() trae todas las particiones distribuidas a la memoria del driver; si el dataset supera spark.driver.memory, la JVM del driver lanza OutOfMemoryError. Los cores de executor afectan al paralelismo y las otras opciones al comportamiento de los executors, no del driver.',
  },
  {
    n: 17,
    d: 'ING',
    m: 0,
    q: 'A data engineer builds a pipeline to incrementally ingest new files from Azure Data Lake Storage. The workload is moderate, files arrive at irregular intervals, and the organization wants the simplest setup with no dependency on external event services.\n\nWhich ingestion approach should the data engineer choose?',
    o: [
      'Use COPY INTO with a cron job scheduled every minute.',
      'Use streaming ingestion with manual checkpoint state stored in a Delta table.',
      'Use Auto Loader in directory listing mode to periodically scan the storage container for new files.',
      'Use Auto Loader with file notifications enabled to support event-based file detection.',
    ],
    a: [2],
    x: 'El modo directory listing es el comportamiento por defecto de Auto Loader: no requiere infraestructura de eventos (Event Grid/colas), gestiona checkpoints automáticamente y funciona bien con cargas moderadas e intervalos irregulares (p. ej., con Trigger.AvailableNow).',
  },
  {
    n: 18,
    d: 'CICD',
    m: 0,
    q: 'Which Databricks CLI command is used to check a bundle configuration for errors before deployment?',
    o: [
      '`databricks bundle init`',
      '`databricks bundle check`',
      '`databricks bundle verify`',
      '`databricks bundle validate`',
    ],
    a: [3],
    x: 'databricks bundle validate comprueba que el databricks.yml es sintáctica y estructuralmente correcto antes de desplegar. init crea un proyecto nuevo; check y verify no existen.',
  },
  {
    n: 19,
    d: 'ING',
    m: 0,
    q: 'A financial services company wants to migrate nightly account data from an on-premises PostgreSQL database into Databricks. The ingested records must be stored in a managed Unity Catalog table, while maintaining automatic lineage tracking for compliance audits.\n\nWhich implementation best satisfies these requirements?',
    o: [
      '```\nCOPY INTO finance.main.accounts FROM \'db-host:5432/finance\' FILEFORMAT = TABLE COPY_OPTIONS (name="main.accounts", properties=credentials);\n```',
      'jdbc_url = "jdbc:postgresql://db-host:5432/finance"\n```\ndf = spark.read.jdbc(url=jdbc_url, table="main.accounts", properties=credentials)\ndf.write.mode("overwrite").saveAsTable("finance.main.accounts")\n```',
      '```\nspark.readStream.format("postgresql")... .writeStream.toTable("finance.main.accounts")\n```',
      'CREATE OR REPLACE TABLE finance.main.accounts AS SELECT * FROM postgresql.`db-host:5432/finance/main/accounts`',
    ],
    a: [1],
    x: 'Leer con spark.read.jdbc y escribir con saveAsTable en el espacio de nombres de 3 niveles (catálogo.esquema.tabla) crea una tabla gestionada y Unity Catalog captura el linaje automáticamente para toda operación de la API DataFrame. COPY INTO no lee JDBC y el JDBC no soporta streaming nativo.',
  },
  {
    n: 20,
    d: 'GOV',
    m: 0,
    q: 'A bank manages the Delta table customer_accounts (customer_id, name, account_number, credit_card) and has created the UDF card_mask(credit_card STRING) that reveals values only to the FraudDetectionDept group.\n\nWhich command applies this function as a column mask to the table?',
    o: [
      '```\nALTER TABLE customer_accounts ALTER COLUMN credit_card SET MASK card_mask;\n```',
      '```\nALTER TABLE customer_accounts SET MASK card_mask ON (credit_card);\n```',
      '```\nSET MASK card_mask ON TABLE customer_accounts TO COLUMN credit_card;\n```',
      '```\nALTER TABLE customer_accounts SET MASK card_mask;\n```',
    ],
    a: [0],
    x: 'La sintaxis para asociar una máscara a una columna existente es ALTER TABLE <tabla> ALTER COLUMN <columna> SET MASK <función>. Las demás variantes no son sintaxis válida de Databricks SQL.',
  },
  {
    n: 21,
    d: 'ING',
    m: 0,
    q: 'Given a Structured Streaming query writing to new_orders with outputMode("append"), fill in the blank to make the query execute multiple micro-batches to process all available data, then stop the trigger.',
    o: [
      '`trigger(processingTime="0 seconds")`',
      '`trigger(once=True)`',
      '`trigger(micro-batches=True)`',
      '`trigger(availableNow=True)`',
    ],
    a: [3],
    x: 'trigger(availableNow=True) procesa todos los datos disponibles en varios micro-batches y detiene el stream al terminar: modo batch incremental. trigger(once=True) lo hace en un único batch (legacy) y processingTime=0 dejaría el stream corriendo.',
  },
  {
    n: 22,
    d: 'GOV',
    m: 0,
    q: 'A marketing data scientist requires read-only access to the customer insights table bi.analytics.insights. Following the principle of least privilege, which SQL commands correctly grant access?',
    o: [
      '```\nGRANT SELECT ON TABLE bi.analytics.insights TO marketing_team;\nGRANT USE SCHEMA ON SCHEMA bi.analytics TO marketing_team;\n```',
      '```\nGRANT SELECT ON TABLE bi.analytics.insights TO marketing_team;\n```',
      '```\nGRANT SELECT ON TABLE bi.analytics.insights TO marketing_team;\nGRANT USE SCHEMA ON SCHEMA bi.analytics TO marketing_team;\nGRANT USE CATALOG ON CATALOG bi TO marketing_team;\n```',
      '```\nGRANT SELECT ON TABLE bi.analytics.insights TO marketing_team;\nGRANT USE CATALOG ON CATALOG bi TO marketing_team;\n```',
    ],
    a: [2],
    x: 'Para leer una tabla se necesita la tríada completa: SELECT sobre la tabla, USE SCHEMA sobre el esquema y USE CATALOG sobre el catálogo. Es el mínimo suficiente para lecturas sin sobreaprovisionar.',
  },
  {
    n: 23,
    d: 'CICD',
    m: 0,
    q: 'A data platform team is deploying a Declarative Automation Bundle to production and wants the deployment to abort if any existing jobs or pipelines defined in the bundle are currently running, to prevent overwriting active workloads.\n\nWhich deployment command should the team use?',
    o: [
      '```\ndatabricks bundle deploy -t prod --fail-on-active-runs\n```',
      '```\ndatabricks bundle deploy -t prod --stop-if-running\n```',
      '```\ndatabricks bundle deploy -t prod --force\n```',
      '```\ndatabricks bundle deploy -t prod --check-executions\n```',
    ],
    a: [0],
    x: '--fail-on-active-runs detiene el despliegue si hay ejecuciones activas de los jobs/pipelines del bundle, protegiendo cargas en producción. --force salta la validación de rama Git; los otros flags no existen.',
  },
  {
    n: 24,
    d: 'ING',
    m: 0,
    q: 'A data engineer uses the file upload UI in Databricks to ingest a CSV file into a new Unity Catalog-managed table. The UI automatically detects column types, but the engineer wants all columns created with the STRING data type to avoid incorrect schema inference.\n\nWhich option best meets this requirement?',
    o: [
      'The file upload UI does not provide any setting to update the inferred column data types',
      'Edit the inferred schema using header dropdowns to set all columns to STRING',
      'Under Advanced attributes, disable the setting "Automatically detect column types"',
      'No action required; all CSV columns are created with the STRING data type by default',
    ],
    a: [2],
    x: 'Al desactivar "Automatically detect column types" en Advanced attributes, la UI deja de inferir tipos y trata todas las columnas como STRING de una sola vez. Editar cabecera a cabecera es manual y propenso a errores, y la detección viene activada por defecto.',
  },
  {
    n: 25,
    d: 'P',
    m: 0,
    q: 'A data analyst creates a Python notebook in a Databricks workspace to analyze confidential enterprise data and is concerned about where this notebook is stored within the platform.\n\nWhere is the notebook stored within the Databricks architecture?',
    o: [
      'In the control plane as a securely stored and encrypted workspace object',
      'In the compute plane, inside the Spark cluster filesystem',
      'In Unity Catalog as encrypted data assets',
      'In Delta Lake tables within the data plane',
    ],
    a: [0],
    x: 'Los notebooks son objetos del workspace: se almacenan cifrados en el control plane gestionado por Databricks. El sistema de archivos del clúster es efímero, Unity Catalog gobierna metadatos/permisos y Delta Lake almacena datos tabulares, no código.',
  },
  {
    n: 26,
    d: 'GOV',
    m: 0,
    q: "A data engineer has the following SQL statement:\n```\nCREATE POLICY banking_policy ON SCHEMA bank.safebox\n```\nCOLUMN MASK bank.safebox.mask_card\nTO financial_analysts EXCEPT admins\nFOR TABLES\nMATCH COLUMNS hasTagValue('pii','credit_card') AS cc\n```\nON COLUMN cc;\n```\nWhich of the following correctly describes this statement?",
    o: [
      'Apply column masking using the mask_card UDF for every column tagged pii across all tables in the schema. financial_analysts see masked values; admins see unmasked values.',
      'Apply column masking using the mask_card UDF for every column tagged pii=credit_card across all tables in the schema bank.safebox. Users in financial_analysts see masked values, while users in admins see unmasked values.',
      'Apply row filtering using the mask_card UDF for every column tagged pii=credit_card. financial_analysts see filtered rows; admins see unfiltered rows.',
      'Apply column masking for every column tagged pii=credit_card. financial_analysts see unmasked values, while admins see masked values.',
    ],
    a: [1],
    x: "Es una política ABAC de máscara de columna a nivel de esquema: hasTagValue('pii','credit_card') selecciona las columnas con esa pareja tag=valor exacta; TO financial_analysts aplica la máscara a ese grupo y EXCEPT admins exime a los administradores, que ven los datos sin enmascarar.",
  },
  {
    n: 27,
    d: 'TRA',
    m: 0,
    q: "A data engineer is validating incoming transaction data and runs the following query:\n```\nSELECT CAST('100$' AS INT);\n```\nWhich of the following describes the result of this query?",
    o: ['An error is thrown', '$100', '100', 'NULL'],
    a: [0],
    x: "CAST estándar falla en tiempo de ejecución ([CAST_INVALID_INPUT]) cuando la cadena contiene caracteres no numéricos como '$'. Para tolerar datos malformados y devolver NULL habría que usar TRY_CAST.",
  },
  {
    n: 28,
    d: 'JOBS',
    m: 0,
    q: 'In a Lakeflow Job, the Bronze notebook task computes latest_batch_number and the Silver notebook task must consume that value during the same job run, without writing intermediate results to external storage.\n\nWhich configuration approach best satisfies these requirements?',
    o: [
      'Write latest_batch_number to a temporary view in the Bronze notebook and query that view from Silver notebooks.',
      'Pass latest_batch_number through notebook widgets and manually chain notebook execution using dbutils.notebook.run.',
      'Store latest_batch_number in a cluster environment variable and read it in the Silver notebooks using spark.conf.get.',
      'In the Bronze notebook, call dbutils.jobs.taskValues.set("latest_batch_number", value) and in the Silver task configuration reference {{tasks.BronzeTask.values.latest_batch_number}}',
    ],
    a: [3],
    x: 'Los Task Values están diseñados para pasar valores pequeños entre tareas del mismo run: la tarea aguas arriba hace taskValues.set y la de aguas abajo lo referencia con {{tasks.<Tarea>.values.<clave>}}. Las vistas temporales mueren con la sesión y las variables de clúster son estáticas.',
  },
  {
    n: 29,
    d: 'P',
    m: 0,
    q: 'An e-commerce company experiences rapid data growth due to seasonal traffic spikes. Batch jobs must complete within a fixed timeframe even during peak hours, the team has limited resources for infrastructure management, and they need automated scaling and optimization.\n\nWhich option best fulfills these conditions?',
    o: [
      'All-purpose clusters with autoscaling enabled',
      'Job clusters with maximum resource allocation',
      'Databricks Serverless compute',
      'Dedicated clusters with Photon enabled',
    ],
    a: [2],
    x: 'El cómputo serverless se ajusta automáticamente a cargas variables sin sobredimensionar en horas valle, cumpliendo SLAs en picos y eliminando la gestión de infraestructura. Es la opción con menor overhead operativo.',
  },
  {
    n: 30,
    d: 'CICD',
    m: 0,
    q: 'Which of the following operations can a data engineer use to save local changes of a Git folder to its remote repository?',
    o: ['Merge & Push', 'Merge & Pull', 'Commit & Pull', 'Commit & Push'],
    a: [3],
    x: 'Commit guarda los cambios en el repositorio local y Push sube ese contenido al repositorio remoto. Pull hace lo contrario (descarga cambios remotos).',
  },
  {
    n: 31,
    d: 'JOBS',
    m: 0,
    q: 'Which of the following is NOT a valid task type in Databricks Jobs?',
    o: ['If/else condition', 'REST API call', 'SQL query', 'Python wheel'],
    a: [1],
    x: 'Los tipos de tarea válidos incluyen Python wheel, SQL query, If/else condition, notebook, JAR, dbt, pipeline, etc. "REST API call" no es un tipo de tarea de Lakeflow Jobs.',
  },
  {
    n: 32,
    d: 'TRA',
    m: 0,
    q: 'A team uses the Silver Layer of the Medallion Architecture to join customer data with external lookup tables and apply filters. A team member makes the following claims about the Silver Layer. Which claim is INCORRECT?',
    o: [
      'The Silver Layer handles data de-duplication',
      'The Silver Layer stores raw data enriched with source file details and ingestion timestamps',
      'The Silver Layer is responsible for data cleansing and filtering',
      'The Silver Layer integrates with other sources for data enrichment',
    ],
    a: [1],
    x: 'Almacenar datos crudos con metadatos de origen (archivo, hora de carga) describe la capa Bronze. Silver limpia, filtra, deduplica y enriquece los datos crudos.',
  },
  {
    n: 33,
    d: 'JOBS',
    m: 0,
    q: 'A data engineering team has a multi-task Job in production. Team members need to be notified in case of job failure.\n\nWhich of the following approaches can be used to send emails to the team members in case of job failure?',
    o: [
      'They can configure email notification settings in the job page',
      'They can use the Job API to programmatically send emails according to each task status',
      'There is no way to notify users in case of job failure',
      'Only the Job owner can be configured to be notified in case of job failure',
    ],
    a: [0],
    x: 'En la página del job (Job notifications → Edit notifications) se añaden direcciones de correo para avisos de inicio, éxito o fallo. No hace falta programar nada ni está limitado al propietario.',
  },
  {
    n: 34,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to enforce that transaction_id and transaction_date are never null on an existing Delta table named transactions.\n\nWhich SQL statements correctly add these constraints?',
    o: [
      '```\nALTER TABLE transactions CHECK(transaction_id IS NOT NULL AND transaction_date IS NOT NULL);\n```',
      '```\nALTER TABLE transactions ALTER COLUMNS (transaction_id, transaction_date) SET NOT NULL;\n```',
      '```\nALTER TABLE transactions CHECK(transaction_id IS NOT NULL);\nALTER TABLE transactions CHECK(transaction_date IS NOT NULL);\n```',
      '```\nALTER TABLE transactions ALTER COLUMN transaction_id SET NOT NULL;\nALTER TABLE transactions ALTER COLUMN transaction_date SET NOT NULL;\n```',
    ],
    a: [3],
    x: 'Las restricciones NOT NULL se añaden con ALTER TABLE ... ALTER COLUMN <col> SET NOT NULL, una columna por sentencia. Las variantes con CHECK carecen de la cláusula ADD CONSTRAINT y ALTER COLUMNS en plural no es sintaxis válida.',
  },
  {
    n: 35,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to create a relational object by pulling data from two tables. The object will only be used in the current session, and to save on storage costs the engineer wants to avoid copying and storing physical data.\n\nWhich relational object should the data engineer create?',
    o: ['Global Temporary view', 'Temporary view', 'View', 'Managed table'],
    a: [1],
    x: 'Una vista no almacena datos físicos (es una consulta guardada) y la variante temporal está ligada a la sesión de Spark: se elimina al terminar la sesión, cumpliendo ambos requisitos.',
  },
  {
    n: 36,
    d: 'ING',
    m: 0,
    q: 'In Lakeflow Connect, which of the following options does NOT belong to the set of managed ingestion connectors?',
    o: ['PostgreSQL database', 'Auto Loader', 'Salesforce', 'Workday Reports'],
    a: [1],
    x: 'Salesforce, Workday Reports y PostgreSQL son conectores gestionados (no-code, con CDC y schema drift automáticos). Auto Loader es un conector estándar basado en código para archivos en almacenamiento de objetos.',
  },
  {
    n: 37,
    d: 'ING',
    m: 0,
    q: 'Which of the following code blocks can a data engineer use to query the events table as a streaming source?',
    o: [
      '`spark.readStream("events")`',
      '`spark.readStream.table("events")`',
      '`spark.read.table("events")`',
      '`spark.readStream().table("events")`',
    ],
    a: [1],
    x: 'Delta Lake se integra con Structured Streaming: se carga una tabla como stream con spark.readStream.table("nombre"). readStream es una propiedad (sin paréntesis) y spark.read.table haría una lectura batch.',
  },
  {
    n: 38,
    d: 'TRO',
    m: 0,
    q: '"An optimization technique in Apache Spark that makes use of runtime statistics to choose the most efficient query execution plan. It improves performance by adjusting shuffle partitions, switching join strategies, and handling data skew on the fly."\n\nWhich of the following is being described?',
    o: ['Liquid Clustering', 'Predictive Optimization', 'Photon Engine', 'Adaptive Query Execution (AQE)'],
    a: [3],
    x: 'AQE reoptimiza el plan en tiempo de ejecución con estadísticas reales: fusiona particiones de shuffle, convierte sort-merge joins en broadcast joins y divide particiones sesgadas. Photon es el motor vectorizado en C++, no el optimizador dinámico.',
  },
  {
    n: 39,
    d: 'TRA',
    m: 0,
    q: 'Fill in the following blank to successfully create a table using data from CSV files located at /path/input:\n```\nCREATE TABLE my_table\n```\n(col1 STRING, col2 STRING)\n____________\nOPTIONS (header = "true", delimiter = ";")\n```\nLOCATION = "/path/input"\n```',
    o: ['`AS CSV`', '`USING DELTA`', '`USING CSV`', '`FROM CSV`'],
    a: [2],
    x: 'CREATE TABLE ... USING CSV especifica el formato de origen externo con sus OPTIONS y crea una tabla externa apuntando a los archivos de esa ubicación. USING DELTA no leería los CSV directamente.',
  },
  {
    n: 40,
    d: 'P',
    m: 0,
    q: '"One of the foundational technologies provided by the Databricks Intelligence Platform is an open-source, file-based storage format that brings reliability to data lakes."\n\nWhich technology is being described?',
    o: ['Apache Spark', 'Delta Live Tables (DLT)', 'Delta Lake', 'Unity Catalog'],
    a: [2],
    x: 'Delta Lake es la tecnología open source que extiende archivos Parquet con un log de transacciones basado en archivos, aportando transacciones ACID y fiabilidad a los data lakes.',
  },
  {
    n: 41,
    d: 'TRO',
    m: 0,
    q: 'A data engineer observes in the Spark UI that most partitions in a stage contain around 100 MB of data, but a few partitions contain several gigabytes. Tasks processing these larger partitions take significantly longer to finish.\n\nWhat is the primary performance bottleneck?',
    o: ['Network timeout', 'Small file problem', 'Executor memory leak', 'Data skew'],
    a: [3],
    x: 'Particiones de tamaño muy desigual (100 MB frente a varios GB) son el síntoma clásico de data skew: unas pocas claves concentran demasiados datos y crean tareas rezagadas. El problema de archivos pequeños sería el opuesto (miles de particiones diminutas).',
  },
  {
    n: 42,
    d: 'GOV',
    m: 0,
    q: 'In which of the following locations can a data engineer change the owner of a table?',
    o: [
      "In the Catalog Explorer, from the Owner field in the table's page",
      "In the Catalog Explorer, under the Permissions tab of the table's page",
      "In the Catalog Explorer, under the Permissions tab of the database's page, since owners are set at database-level",
      "In the Catalog Explorer, from the Owner field in the database's page, since owners are set at database-level",
    ],
    a: [0],
    x: 'En Catalog Explorer, dentro de la página de la tabla, el campo Owner permite editar el propietario (Edit owner). La propiedad se gestiona a nivel de objeto, no en la pestaña de permisos ni a nivel de base de datos.',
  },
  {
    n: 43,
    d: 'ING',
    m: 0,
    q: 'A data engineer has uploaded a JSON file to a Unity Catalog volume named "source_files" in the schema "events" under the catalog "analytics", and wants to query the file directly with Spark SQL.\n\nWhich command correctly reads the JSON file?',
    o: [
      'SELECT * FROM json.`/Volumes/source_files/events/analytics/transactions.json`',
      'SELECT * FROM json.`dbfs:/FileStore/analytics/events/source_files/transactions.json`',
      'SELECT * FROM json.`s3://source_files/events/analytics/transactions.json`',
      'SELECT * FROM json.`/Volumes/analytics/events/source_files/transactions.json`',
    ],
    a: [3],
    x: 'La ruta estándar de un volumen de Unity Catalog es /Volumes/<catálogo>/<esquema>/<volumen>/<archivo>: aquí /Volumes/analytics/events/source_files/transactions.json. Las otras rutas invierten la jerarquía o usan DBFS/S3 directo.',
  },
  {
    n: 44,
    d: 'TRO',
    m: 0,
    q: 'Which of the following commands can a data engineer use to purge stale data files of a Delta table?',
    o: ['`VACUUM`', '`OPTIMIZE`', '`CLEAN`', '`DELETE`'],
    a: [0],
    x: 'VACUUM elimina los archivos de datos no utilizados que superan el periodo de retención configurado. OPTIMIZE compacta, DELETE borra filas lógicamente y CLEAN no existe.',
  },
  {
    n: 45,
    d: 'ING',
    m: 0,
    q: 'A data engineer designs a streaming ingestion pipeline with Auto Loader. The pipeline should never fail on schema changes but must capture any new columns that arrive in the data for later inspection.\n\nWhich schema evolution mode should the engineer use?',
    o: ['addNewColumns', 'none', 'failOnNewColumns', 'rescue'],
    a: [3],
    x: 'En modo rescue el esquema no evoluciona y el stream no falla: las columnas nuevas se capturan en la columna de datos rescatados (_rescued_data) para inspección posterior. addNewColumns falla y evoluciona; failOnNewColumns falla sin evolucionar.',
  },
  {
    n: 46,
    d: 'JOBS',
    m: 0,
    q: "A data engineering team wants to update downstream Gold tables every 15 minutes to meet the company's SLA.\n\nWhich trigger configuration should the team use for this job while minimizing compute costs and DBU consumption?",
    o: ['File arrival trigger', 'Table update trigger', 'Continuous trigger', 'Scheduled trigger'],
    a: [3],
    x: 'Un scheduled trigger (cron) con job cluster arranca el cómputo, ejecuta y termina en cada ciclo: sin coste en reposo y con cadencia predecible (incluso cada 12 min para margen de SLA). El continuo mantendría el clúster siempre facturando y los triggers por eventos no respetan el intervalo fijo.',
  },
  {
    n: 47,
    d: 'P',
    m: 0,
    q: 'In Delta Lake tables, which of the following is the primary format for the transaction log files?',
    o: ['Delta', 'JSON', 'XML', 'Parquet'],
    a: [1],
    x: 'Una tabla Delta guarda los datos en archivos Parquet y el log de transacciones (_delta_log) en archivos JSON (con checkpoints en Parquet).',
  },
  {
    n: 48,
    d: 'ING',
    m: 0,
    q: 'A data engineer uses an Auto Loader stream with .option("cloudFiles.inferColumnTypes", "true").\n\nWhich of the following correctly explains this option?',
    o: [
      'It automatically enables schema evolution to add new columns with their inferred data types.',
      'It forces all inferred columns to be treated as strings to ensure schema consistency across batches',
      'It enables the inference of precise data types (e.g., integers, booleans, and timestamps) from the JSON data',
      'It enables the inference of relational column types (e.g., primary key and foreign key) from the JSON data',
    ],
    a: [2],
    x: 'Por defecto, Auto Loader infiere todas las columnas de JSON/CSV como string. Con inferColumnTypes=true muestrea los datos e infiere tipos precisos (enteros, booleanos, timestamps). No controla la evolución del esquema ni claves relacionales.',
  },
  {
    n: 49,
    d: 'CICD',
    m: 0,
    q: "What is the primary purpose of the targets section in a Databricks Asset Bundle's databricks.yml file?",
    o: [
      'To specify different deployment environments with their respective configurations',
      'To configure the version of the Databricks Runtime',
      'To define user roles and access policies for the workspace',
      'To list the external libraries required by the bundle',
    ],
    a: [0],
    x: 'La sección targets define los entornos de despliegue (dev, staging, prod), cada uno con su configuración: rutas del workspace, ajustes de clúster y variables específicas. Facilita el aislamiento por entorno.',
  },
  {
    n: 50,
    d: 'JOBS',
    m: 0,
    q: 'A team builds a Lakeflow Job that processes a streaming workload of incoming IoT sensor data. They need the lowest possible latency and the job must run indefinitely.\n\nWhat trigger configuration should the team use?',
    o: ['File arrival trigger', 'Continuous trigger', 'Table update trigger', 'Scheduled trigger'],
    a: [1],
    x: 'El continuous trigger mantiene el job siempre en ejecución, lanzando una nueva ejecución inmediatamente al terminar (o fallar) la anterior: mínima latencia para streaming continuo. Los triggers programados o por eventos añaden latencia.',
  },
  {
    n: 51,
    d: 'JOBS',
    m: 0,
    q: 'A large Databricks job fails at task 12 of 15 due to a missing configuration file. After resolving the issue, what is the most appropriate action to resume the workflow?',
    o: [
      'Run the notebook associated with task 12 manually',
      'Wait for the next scheduled run',
      'Repair run from task 12',
      'Restart the job',
    ],
    a: [2],
    x: 'Repair run reejecuta solo las tareas fallidas y sus dependientes, sin repetir las que ya tuvieron éxito: ahorra tiempo y recursos frente a relanzar todo el job.',
  },
  {
    n: 52,
    d: 'CICD',
    m: 0,
    q: 'A data engineer is configuring a job using Databricks Asset Bundles and needs to define which user groups can manage or view the job:\n\nresources:\n  jobs:\n    my-job:\n      name: analytics-job\n      tasks: [...]\n      job_clusters: [...]\n      __________:\n        - group_name: devops-team\n          level: CAN_MANAGE\n        - group_name: qa-team\n          level: CAN_VIEW\n\nWhich option correctly fills in the blank?',
    o: ['job_settings', 'roles', 'access_control', 'permissions'],
    a: [3],
    x: 'El mapeo permissions define las listas de control de acceso del recurso: qué usuarios o grupos tienen niveles como CAN_MANAGE o CAN_VIEW sobre el job.',
  },
  {
    n: 53,
    d: 'CICD',
    m: 0,
    q: 'A junior data engineer uses the built-in Databricks Notebooks versioning for source control. A senior data engineer recommended using Git folders instead.\n\nWhich of the following could explain why Git folders are recommended?',
    o: [
      'Git folders store source code files in Unity Catalog for centralized security and governance',
      'Git folders support creating and managing branches for development work.',
      'Git folders support automatic conflict resolution when multiple users edit the same notebook',
      'Git folders automatically sync all notebook changes to the remote Git repository in real time',
    ],
    a: [1],
    x: 'La gran ventaja de los Git folders sobre el versionado integrado de notebooks es el soporte de ramas (crear y gestionar branches) y el flujo Git completo. No hay sincronización automática en tiempo real ni resolución automática de conflictos.',
  },
  {
    n: 54,
    d: 'JOBS',
    m: 0,
    q: 'Which of the following services can a data engineer use for task orchestration in the Databricks platform?',
    o: ['Databricks Jobs', 'Databricks Connect', 'Delta Live Tables', 'Unity Catalog Lineage'],
    a: [0],
    x: 'Databricks Jobs (hoy Lakeflow Jobs) orquesta tareas de procesamiento: permite ejecutar y gestionar múltiples tareas como un grafo acíclico dirigido (DAG) dentro de un job.',
  },
  {
    n: 55,
    d: 'GOV',
    m: 1,
    q: 'A data engineering team wants to migrate a large external table to a managed table using the ALTER TABLE ... SET MANAGED command.\n\nWhat are the key advantages of using SET MANAGED for this conversion? (Choose 2 answers)',
    o: [
      'Keeps the same table configurations, including name, settings, permissions, and views.',
      'Retains table history and supports rolling back to an external table.',
      'Incrementally synchronizes changes from the source to the target location on each run.',
      'Minimizes downtime during migration by copying only the latest table version.',
      'Immediately deletes the original external storage location after conversion to save storage costs.',
    ],
    a: [0, 1],
    x: 'SET MANAGED preserva el historial completo (time travel) con posibilidad de rollback vía UNSET MANAGED, y mantiene nombre, permisos, tags, propiedades y vistas. No sincroniza incrementalmente, no copia solo la última versión y los datos originales se conservan ~14 días antes de limpiarse.',
  },
];

export const exam3: RawQuestion[] = [
  {
    n: 1,
    d: 'CICD',
    m: 0,
    q: "A data engineer is preparing a Databricks Asset Bundle to define a job that runs a notebook located at ./src/my_notebook.py:\n\nbundle:\n  name: my_bundle\n\n____________:\n  jobs:\n    my_job:\n      name: my_notebook_job\n      tasks:\n        - task_key: test_task\n          existing_cluster_id: 1234-911320-xyzwpm999\n          notebook_task:\n            notebook_path: './src/my_notebook.py'\n\nTo ensure the bundle is valid and deployable, which key should correctly replace the blank above?",
    o: ['workflows', 'resources', 'pipelines', 'settings'],
    a: [1],
    x: 'En la sección resources se definen los objetos desplegables del bundle: jobs, pipelines, notebooks, clústeres, etc. settings no existe, pipelines es un tipo de recurso (no una sección de nivel superior) y workflows no forma parte del esquema de databricks.yml.',
  },
  {
    n: 2,
    d: 'JOBS',
    m: 0,
    q: 'A data engineer maintains a Lakeflow Job where Task_1 fans out into three parallel tasks (Task_2, Task_3, Task_4). They want to add a new Task_5 that runs after all parallel tasks (2, 3 and 4) finish.\n\nWhich task configuration meets this requirement?',
    o: [
      'Configure Task 5 to depend on the three parallel tasks using fan-in control flow.',
      'Configure Task 5 to depend only on Task 4, which runs after Tasks 2 and 3 in that order.',
      'Add Task 5 without any dependency but with the condition run_if: ALL_DONE',
      'Configure Task 5 in a separate job and schedule it to run after the current job finishes.',
    ],
    a: [0],
    x: 'Para ejecutar Task_5 tras todas las ramas paralelas hay que configurar un patrón fan-in: su campo "Depends on" debe incluir las tres tareas. Sin dependencias, la tarea arrancaría como raíz al inicio del job; las tareas 2-4 son paralelas, no secuenciales.',
  },
  {
    n: 3,
    d: 'CICD',
    m: 0,
    q: 'Which of the following tasks is NOT supported by Git folders, and must be performed in your Git provider?',
    o: [
      'Create and checkout branches for development work.',
      'Visually compare differences upon commit.',
      'Delete branches',
      'Clone, push to, or pull from a remote Git repository.',
    ],
    a: [2],
    x: 'Los Git folders no soportan crear pull requests ni eliminar ramas: esas operaciones deben hacerse en el proveedor Git (GitHub, GitLab...). Sí soportan crear/cambiar ramas, comparar diferencias, clonar, push y pull.',
  },
  {
    n: 4,
    d: 'TRO',
    m: 0,
    q: 'A team is deciding the optimal data layout strategy (partitioning, Z-ordering, Liquid Clustering) for a growing managed Delta table in Unity Catalog.\n\nWhich scenario best indicates that Automatic Liquid Clustering is the recommended choice?',
    o: [
      'The table experiences diverse, frequently changing query filters across multiple columns, with unpredictable access patterns.',
      'The table is heavily filtered by a consistent, small set of date ranges.',
      'The team has identified stable clustering keys for the table.',
      'None of the listed options are correct. Automatic Liquid Clustering cannot be applied on managed tables.',
    ],
    a: [0],
    x: 'El Automatic Liquid Clustering se adapta dinámicamente a patrones de consulta cambiantes, reorganizando los datos según los filtros recientes. Con filtros estables y predecibles bastan el particionado o Z-order; con claves estables se puede usar Liquid Clustering manual.',
  },
  {
    n: 5,
    d: 'P',
    m: 0,
    q: 'A data engineer currently uses an all-purpose cluster with auto-termination set to 6 hours to run a nightly incremental ETL job. The workloads complete at different times each night (1-3 hours). They want to reduce compute costs while maintaining the same scheduling behavior.\n\nWhich approach should the data engineer use?',
    o: [
      'Decrease the auto-termination setting on the all-purpose cluster to 3 hours',
      'Use a job compute cluster that automatically terminates after workload completion',
      'Use the AvailableNow trigger option to stop the stream processing automatically',
      'Configure the all-purpose cluster to use smaller worker nodes',
    ],
    a: [1],
    x: 'Los job clusters se facturan a un DBU más barato que los all-purpose y terminan justo al acabar la carga, eliminando el 100% del tiempo ocioso sin depender de umbrales arbitrarios. Reducir el auto-termination o el tamaño de nodo sigue pagando ocio o arriesga OOM.',
  },
  {
    n: 6,
    d: 'P',
    m: 0,
    q: 'A data engineer schedules a notebook as a Databricks Job to process daily confidential data and is concerned about where the notebook code actually runs when the job is triggered.\n\nWhich of the following explains where the execution of this job occurs?',
    o: [
      'In the compute plane on a cluster running Spark',
      'In the control plane where the notebook is stored',
      'In Unity Catalog during query planning',
      'In the workspace storage bucket',
    ],
    a: [0],
    x: 'El código se ejecuta en el plano de cómputo (compute plane), sobre clústeres Spark, dentro del perímetro de red del cliente (clásico) o de Databricks (serverless). El control plane almacena el notebook y coordina, pero no procesa los datos.',
  },
  {
    n: 7,
    d: 'TRO',
    m: 0,
    q: 'A data engineer analyzes the Spark UI summary metrics for 27 completed tasks in a stage: the Max task took about 10x the time and read about 5x the data of the 75th-percentile task.\n\nWhich conclusion can the data engineer draw from these statistics?',
    o: [
      'A number of tasks are operating over partitions with larger, skewed amounts of data.',
      'All tasks are operating over partitions with larger, skewed amounts of data.',
      'A number of tasks are operating over empty or near-empty partitions',
      'All tasks are operating over partitions with even amounts of data',
    ],
    a: [0],
    x: 'Si el cómputo fuera simétrico, las métricas estarían agrupadas en torno a la mediana. Que el máximo tarde 10x y lea 5x más que el percentil 75 revela tareas rezagadas (stragglers) sobre particiones sesgadas: data skew en algunas tareas, no en todas.',
  },
  {
    n: 8,
    d: 'TRA',
    m: 0,
    q: 'A data engineer is validating incoming transaction data and runs the following query:\n```\nSELECT INT("100$")\n```\nWhich of the following describes the result of this query?',
    o: ['NULL', 'An error is thrown', '100', '$100'],
    a: [1],
    x: "La función INT() se comporta como un CAST estándar: al encontrar el carácter '$' no numérico, falla con [CAST_INVALID_INPUT]. Para obtener NULL en lugar de error habría que usar TRY_CAST.",
  },
  {
    n: 9,
    d: 'GOV',
    m: 0,
    q: 'Which part of the Databricks platform can a data engineer use to grant and revoke permissions on securable objects for principals?',
    o: ['Data Studio', 'Account console', 'Catalog Explorer', 'Workspace Admin Console'],
    a: [2],
    x: 'Catalog Explorer permite gestionar permisos sobre objetos asegurables (tablas, esquemas, catálogos...) para principales (usuarios, grupos, service principals), además de explorar metadatos y linaje.',
  },
  {
    n: 10,
    d: 'TRO',
    m: 0,
    q: 'A data engineer is joining a very large PySpark DataFrame with a small one:\n```\nlargeDF.join(smallerDF, ["key"], "inner")\n```\nThe join is slow due to data shuffling. They want the smaller DataFrame to be sent to all executor nodes in the cluster.\n\nWhich function can be used to mark a DataFrame as small enough to fit in memory on all executors?',
    o: [
      'pyspark.sql.functions.distribute',
      'pyspark.sql.functions.explode',
      'pyspark.sql.functions.shuffle',
      'pyspark.sql.functions.broadcast',
    ],
    a: [3],
    x: 'broadcast() marca el DataFrame pequeño para un Broadcast Hash Join: Spark lo copia a todos los executors y el join se resuelve localmente sin shuffle de la tabla grande. explode aplana arrays y shuffle permuta elementos de un array.',
  },
  {
    n: 11,
    d: 'JOBS',
    m: 0,
    q: 'In Databricks Jobs, which of the following approaches can a data engineer use to configure a linear dependency between Task A and Task B?',
    o: [
      'They can select Task A in the Depends On field of the Task B configuration',
      'They can visually drag and drop an arrow from Task A to Task B in the Job canvas',
      'They can configure the dependency at the notebook level using the dbutils.jobs utility',
      'They can assign Task A an Order number of 1, and Task B an Order number of 2',
    ],
    a: [0],
    x: 'El orden de ejecución se define con el desplegable "Depends on" de cada tarea, que puede apuntar a una o varias tareas del job. No existen flechas arrastrables ni números de orden.',
  },
  {
    n: 12,
    d: 'CICD',
    m: 1,
    q: 'A data engineer wants to use Declarative Automation Bundles to deploy jobs into the dev workspace via a PAT token from their local machine.\n\nWhich TWO environment variables should the data engineer set in their shell to configure Databricks Unified Authentication?',
    o: [
      'DATABRICKS_TOKEN',
      'DATABRICKS_CLIENT_ID',
      'DATABRICKS_HOST',
      'DATABRICKS_WORKSPACE',
      'DATABRICKS_CLIENT_SECRET',
    ],
    a: [0, 2],
    x: 'Con autenticación por PAT bastan DATABRICKS_HOST (URL del workspace) y DATABRICKS_TOKEN. CLIENT_ID/CLIENT_SECRET corresponden a OAuth de service principals y DATABRICKS_WORKSPACE no existe. (Databricks recomienda OAuth sobre PATs cuando sea posible.)',
  },
  {
    n: 13,
    d: 'P',
    m: 0,
    q: 'In Delta Lake tables, which of the following is the primary format for the data files?',
    o: ['Parquet', 'Delta', 'JSON', 'Both Parquet and JSON'],
    a: [0],
    x: 'Los archivos de datos de una tabla Delta son Parquet; el JSON se usa solo para el log de transacciones. "Delta" no es un formato de archivo físico propio.',
  },
  {
    n: 14,
    d: 'GOV',
    m: 1,
    q: 'A team is migrating a high-traffic external table to a managed table with minimal disruption, deciding between ALTER TABLE ... SET MANAGED and a DEEP CLONE approach.\n\nWhich advantages does SET MANAGED provide over DEEP CLONE for this conversion? (Choose 2 answers)',
    o: [
      '```\nSET MANAGED handles concurrent writes during conversion.\n```',
      'SET MANAGED schedules the conversion process to run during off-peak periods using predictive optimization.',
      '```\nSET MANAGED minimizes reader and writer downtime.\n```',
      'SET MANAGED migrates only the Delta transaction logs to reduce data copy times.',
      'SET MANAGED leaves data in place and just references the existing table location.',
    ],
    a: [0, 2],
    x: 'SET MANAGED usa un proceso en dos fases: copia datos y logs mientras lectores y escritores siguen operando, y luego sincroniza los commits pendientes; así minimiza el downtime y tolera escrituras concurrentes. Sí copia los datos (no solo logs, ni los deja in situ) y Predictive Optimization no programa la migración.',
  },
  {
    n: 15,
    d: 'TRA',
    m: 0,
    q: 'In the Medallion Architecture, which of the following statements best describes the Bronze layer?',
    o: [
      'It maintains raw data ingested from various sources',
      'It provides a business-level aggregated version of data',
      'It represents a filtered, cleaned, and enriched version of data',
      'It maintains data that powers analytics, machine learning, and production applications',
    ],
    a: [0],
    x: 'Bronze contiene los datos en su formato más crudo, ingeridos de las distintas fuentes (JSON, bases de datos operacionales, Kafka...). Silver los limpia y enriquece; Gold ofrece agregados de negocio.',
  },
  {
    n: 16,
    d: 'TRO',
    m: 0,
    q: 'A data engineer got a java.lang.OutOfMemoryError after running df.toPandas() on a large Spark DataFrame.\n\nWhich action should the data engineer take to fix this issue?',
    o: [
      'Increase the number of shuffle partitions',
      'Enable Adaptive Query Execution (AQE)',
      'Increase the driver memory',
      'Increase executor memory',
    ],
    a: [2],
    x: 'toPandas() (como collect()) concentra todos los datos distribuidos en el driver para construir el DataFrame de pandas local. Si supera la memoria del driver, hay OOM: la solución es aumentar spark.driver.memory. La memoria de executors o el AQE no afectan a esta recolección.',
  },
  {
    n: 17,
    d: 'TRA',
    m: 0,
    q: 'A data engineer is validating incoming transaction data and runs the following query:\n```\nSELECT COALESCE(CAST("100$" AS INT), 0)\n```\nWhich of the following describes the result of this query?',
    o: ['An error is thrown', 'NULL', '0', '100'],
    a: [0],
    x: "CAST falla inmediatamente con [CAST_INVALID_INPUT] por el símbolo '$', así que el COALESCE externo nunca llega a evaluarse. Para obtener el 0 de respaldo habría que usar COALESCE(TRY_CAST(...), 0).",
  },
  {
    n: 18,
    d: 'JOBS',
    m: 0,
    q: 'A junior data engineer creates a Databricks job with 15 notebook tasks, each performing the same data validation logic on 15 different tables, each depending on the previous one, making the workflow long and hard to maintain.\n\nWhat would be a more efficient and scalable solution for this use case?',
    o: [
      'Combine all table validations into one large notebook and loop through all tables sequentially',
      'Configure the 15 notebook tasks to run in parallel, each with a separate cluster configuration',
      'Use a foreach task to run the same validation notebook for each table in parallel, passing the table name as a parameter',
      'Schedule 15 separate jobs instead of having multiple tasks in one job',
    ],
    a: [2],
    x: 'La tarea For Each ejecuta una tarea anidada en bucle pasando parámetros distintos a cada iteración (aquí el nombre de tabla), en paralelo. Reduce el mantenimiento y elimina dependencias secuenciales artificiales.',
  },
  {
    n: 19,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has defined the following data quality constraint in a Lakeflow Spark Declarative Pipeline:\n\nCONSTRAINT valid_id EXPECT (id IS NOT NULL) _____________\n\nFill in the blank so records violating this constraint cause the pipeline to fail.',
    o: [
      'There is no need to add the ON VIOLATION clause. By default, records violating the constraint cause the pipeline to fail.',
      '`ON VIOLATION FAIL`',
      '`ON VIOLATION FAIL PIPELINE`',
      '`ON VIOLATION FAIL UPDATE`',
    ],
    a: [3],
    x: 'ON VIOLATION FAIL UPDATE hace fallar la actualización del pipeline ante registros inválidos; hay que corregir el código antes de reejecutar. Por defecto (sin cláusula), los registros inválidos se conservan y solo se reportan en métricas.',
  },
  {
    n: 20,
    d: 'TRA',
    m: 0,
    q: 'A DataFrame df has a location column formatted as "City ZipCode" (e.g., "Paris 75015"). The engineer wants two new columns, city and zip_code, and then to remove the original location column.\n\nWhich PySpark code snippet should be used?',
    o: [
      '```\ndf = (df.withColumn("city", split(col("location"), " ")[0])\n        .withColumn("zip_code", split(col("location"), " ")[1])\n        .drop("location"))\n```',
      "```\ndf = (df.select(split('location', ' ').alias('places'))\n        .select('places[0]', 'places[1]'))\n```",
      '```\ndf = (df.withColumn("city", getItem(col("location"), 0))\n        .withColumn("zip_code", getItem(col("location"), 1))\n        .drop("location"))\n```',
      "```\ndf = (df.select(explode('location').alias('places'))\n        .select('places[0]', 'places[1]')\n        .drop(\"location\"))\n```",
    ],
    a: [0],
    x: 'split divide la cadena en un array por el espacio; la indexación [0]/[1] extrae ciudad y código postal; withColumn añade las columnas y drop elimina la original. getItem no es una función independiente importable y explode es para arrays/mapas, no strings.',
  },
  {
    n: 21,
    d: 'JOBS',
    m: 0,
    q: 'A data pipeline team notices that several scheduled job runs are delayed during peak hours and start later than their scheduled time.\n\nWhat is the most likely explanation for this behavior?',
    o: [
      'The task dependencies are causing circular execution delays',
      'The job is configured with a low timeout_seconds value causing delayed retries',
      'The workflow is configured with queueing enabled and has reached its max_concurrent_runs limit',
      'The cluster is using spot instances that are frequently terminated',
    ],
    a: [2],
    x: 'Con queueing activado, al alcanzar max_concurrent_runs las nuevas ejecuciones quedan en estado QUEUED (hasta 48 h) y arrancan cuando se libera un hueco: por eso empiezan tarde. Los timeouts cancelan jobs, los spot alargan la ejecución (no el arranque) y los DAG no permiten ciclos.',
  },
  {
    n: 22,
    d: 'GOV',
    m: 0,
    q: 'A finance team manages the Unity Catalog table "transactions" (id, amount, region, account_manager). They want row filtering so finance team members see all transactions while other users only see records from the US region.\n\nWhich user-defined function achieves this?',
    o: [
      "```\nCREATE FUNCTION us_filter(region STRING)\nRETURN IF(IS_ACCOUNT_GROUP_MEMBER('finance_team'), true, region='US');\n```",
      "```\nCREATE FUNCTION us_filter(region STRING)\nRETURN CASE WHEN IS_ACCOUNT_GROUP_MEMBER('finance_team') THEN true ELSE region END\n```",
      "```\nCREATE FUNCTION us_filter(region STRING)\nRETURN CASE WHEN IS_ACCOUNT_GROUP_MEMBER('finance_team') THEN region='US' ELSE true END\n```",
      "```\nCREATE FUNCTION us_filter(region STRING)\nRETURN IF(IS_ACCOUNT_GROUP_MEMBER('finance_team'), region='US', true);\n```",
    ],
    a: [0],
    x: "La función correcta devuelve true (todas las filas) si el usuario pertenece a finance_team; en caso contrario, evalúa region='US' y restringe la visibilidad a esa región. Las otras opciones invierten la lógica o devuelven un valor no booleano.",
  },
  {
    n: 23,
    d: 'CICD',
    m: 0,
    q: 'A data engineer configures a Declarative Automation Bundle and wants to push the assets to the defined target workspace.\n\nWhich Databricks CLI command achieves this goal?',
    o: [
      '`databricks bundle deployment`',
      '`databricks bundle validate`',
      '`databricks bundle push`',
      '`databricks bundle deploy`',
    ],
    a: [3],
    x: 'databricks bundle deploy compila la configuración, sube los artefactos y despliega los recursos (jobs, pipelines, notebooks) en el workspace del target definido en databricks.yml. validate solo comprueba la sintaxis; push y deployment no existen.',
  },
  {
    n: 24,
    d: 'JOBS',
    m: 0,
    q: 'A data engineer needs to run a Lakeflow job that aggregates sales data once every night at 2 AM. The job does not require real-time processing and should execute only at a fixed time each day.\n\nWhat trigger configuration should the data engineer use?',
    o: ['Table update trigger', 'Continuous trigger', 'File arrival trigger', 'Scheduled trigger'],
    a: [3],
    x: 'El scheduled trigger ejecuta el job a horas fijas (con cron o selector de tiempo), ideal para procesos batch nocturnos. Los triggers continuos o por eventos responden a llegadas de datos, no a un horario.',
  },
  {
    n: 25,
    d: 'ING',
    m: 0,
    q: 'An Auto Loader stream ingesting JSON from S3 should fail when new columns are detected, but those new columns should still be added to the schema so subsequent runs can resume with the updated schema. Existing columns must retain their data types.\n```\n.option("cloudFiles.schemaEvolutionMode", "_______")\n```\nWhich option correctly fills in the blank?',
    o: ['rescue', 'none', 'failOnNewColumns', 'addNewColumns'],
    a: [3],
    x: 'addNewColumns (modo por defecto) hace fallar el stream al detectar columnas nuevas, pero las añade al esquema: al reiniciar, el job continúa con el esquema actualizado sin alterar los tipos existentes. rescue no evoluciona el esquema y failOnNewColumns no lo actualiza.',
  },
  {
    n: 26,
    d: 'ING',
    m: 0,
    q: 'Which of the following technologies is used by Auto Loader to load data incrementally?',
    o: ['Multi-hop architecture', 'Spark Structured Streaming', '`COPY INTO`', '`DEEP CLONE`'],
    a: [1],
    x: 'Auto Loader se basa en Spark Structured Streaming y expone la fuente de streaming cloudFiles para procesar incrementalmente los archivos que llegan al almacenamiento cloud.',
  },
  {
    n: 27,
    d: 'JOBS',
    m: 0,
    q: 'A data engineer has a Job with multiple tasks that takes more than 2 hours to complete. In the last run, the final task unexpectedly failed.\n\nWhich action can the data engineer perform to complete this Job Run while minimizing execution time?',
    o: [
      'They can keep the failed run and simply start a new run for the Job',
      'They can repair this Job Run so only the failed tasks will be re-executed',
      'They can run the Job in Production mode which automatically retries execution in case of errors',
      'They need to delete the failed run and start a new run for the Job',
    ],
    a: [1],
    x: 'La reparación (repair) de un job multi-tarea reejecuta solo las tareas fallidas y sus dependientes; las exitosas no se repiten, ahorrando las 2 horas ya invertidas.',
  },
  {
    n: 28,
    d: 'TRA',
    m: 0,
    q: 'A table products has the schema: product_id STRING, updated_at TIMESTAMP, details STRUCT<name: STRING, category: STRING, pricing: STRUCT<base_price: DOUBLE, discount: DOUBLE>>.\n\nThe engineer wants to flatten all fields inside details so that name, category, and the nested pricing fields become root-level columns.\n\nWhich SQL query correctly achieves this goal?',
    o: [
      '```\nSELECT product_id, updated_at, details.name, details.category,\n```\n  details.pricing.base_price, details.pricing.discount\n```\nFROM products;\n```',
      '```\nSELECT product_id, updated_at, details.* FROM products;\n```',
      '```\nSELECT * FROM products;\n```',
      '```\nSELECT product_id, updated_at, EXPLODE(details) FROM products;\n```',
    ],
    a: [0],
    x: 'La notación de punto extrae campos de un STRUCT y se encadena para structs anidados (details.pricing.base_price). details.* solo aplana el primer nivel (pricing quedaría como struct), SELECT * no aplana nada y EXPLODE solo acepta ARRAY o MAP (con STRUCT lanza error).',
  },
  {
    n: 29,
    d: 'P',
    m: 0,
    q: 'Which of the following locations completely hosts the customer data?',
    o: ["Customer's cloud account", 'Databricks account', 'Control plane', 'Databricks-managed cluster'],
    a: [0],
    x: 'Según la arquitectura del Lakehouse, el almacenamiento con los datos del cliente se aprovisiona en el data plane, dentro de la cuenta cloud del propio cliente.',
  },
  {
    n: 30,
    d: 'TRO',
    m: 0,
    q: 'A team manages Unity Catalog tables with predictive optimization enabled and wants to know which operations are performed automatically.\n\nWhich of the following operations is NOT handled automatically by predictive optimization for enabled tables?',
    o: ['`VACUUM`', '`OPTIMIZE`', '`ANALYZE`', '`ZORDER`'],
    a: [3],
    x: 'Predictive Optimization ejecuta automáticamente OPTIMIZE, VACUUM y ANALYZE, pero no ZORDER: los archivos con Z-order son ignorados cuando se ejecuta la optimización predictiva.',
  },
  {
    n: 31,
    d: 'ING',
    m: 0,
    q: 'A financial services company wants to migrate nightly account data from an on-premises SQL Server database into a managed Unity Catalog table so analysts can query it immediately, while maintaining automatic lineage tracking.\n\nWhich implementation best satisfies these requirements?',
    o: [
      'Export CSV files to object storage and load them later with a separate ETL pipeline',
      'Extract data through ODBC into temporary files before manually registering tables',
      'Use a local Python script with Pandas and upload Parquet files to a Unity Catalog Volume',
      'Read data using Spark JDBC and write directly into a Unity Catalog managed table',
    ],
    a: [3],
    x: 'Leer con Spark JDBC y escribir con saveAsTable en una tabla gestionada da disponibilidad inmediata para SQL y linaje automático de Unity Catalog (capturado en cualquier operación de la API DataFrame). Los enfoques con archivos intermedios añaden latencia y rompen el linaje; Pandas es mononodo.',
  },
  {
    n: 32,
    d: 'P',
    m: 0,
    q: 'A data engineer wants to review all changes made to a Delta table in the last 30 days, including transaction timestamps, operation types, and executing users. Which command should they use?',
    o: [
      '```\nDESCRIBE EXTENDED my_table\n```',
      '```\nDESCRIBE DETAIL my_table\n```',
      '```\nSELECT * FROM my_table VERSION AS OF <version_number>\n```',
      '```\nDESCRIBE HISTORY my_table\n```',
    ],
    a: [3],
    x: 'DESCRIBE HISTORY devuelve el registro de auditoría completo: timestamp, operación (WRITE, MERGE, OPTIMIZE...), usuario y métricas de cada transacción (retención por defecto: 30 días). DESCRIBE DETAIL/EXTENDED muestran metadatos actuales y VERSION AS OF consulta un snapshot de datos.',
  },
  {
    n: 33,
    d: 'GOV',
    m: 0,
    q: "A data engineer must grant the service principal with Application ID 'fe7bcf95-...' read-only access to the enterprise.reporting.transactions table.\n\nWhich SQL commands correctly grant access with the least privileges?",
    o: [
      'GRANT SELECT ON TABLE enterprise.reporting.transactions TO `<sp-id>`;\nGRANT USE SCHEMA ON SCHEMA enterprise.reporting TO `<sp-id>`;',
      'GRANT SELECT ON TABLE enterprise.reporting.transactions TO `<sp-id>`;',
      'GRANT SELECT ON TABLE enterprise.reporting.transactions TO `<sp-id>`;\nGRANT USE SCHEMA ON SCHEMA enterprise.reporting TO `<sp-id>`;\nGRANT USE CATALOG ON CATALOG enterprise TO `<sp-id>`;',
      'GRANT SELECT ON TABLE enterprise.reporting.transactions TO `<sp-id>`;\nGRANT USE CATALOG ON CATALOG enterprise TO `<sp-id>`;',
    ],
    a: [2],
    x: 'Los service principals son principales de primera clase y siguen la misma jerarquía: SELECT sobre la tabla + USE SCHEMA sobre el esquema + USE CATALOG sobre el catálogo. Sin los permisos de tránsito, el SP no puede ver la tabla aunque tenga SELECT.',
  },
  {
    n: 34,
    d: 'ING',
    m: 0,
    q: 'A data engineer wants to incrementally ingest sales transaction data from multiple enterprise sources (Salesforce, a SQL database, a Kafka stream, and Azure Data Lake Storage) into Unity Catalog tables.\n\nWhich configuration should the data engineer use to meet this requirement?',
    o: [
      'Separate connectors per source with CDC',
      'Single unified Lakeflow connector for all sources with CDC',
      'Single standard connector for all sources with CDC',
      'Single managed connector for all sources with CDC',
    ],
    a: [0],
    x: 'Los conectores de Lakeflow están especializados por tipo de fuente: SaaS para Salesforce, conector de base de datos (CDC) para SQL, y conectores estándar (Structured Streaming/Auto Loader) para Kafka y ADLS. No existe un conector unificado único para tecnologías tan distintas.',
  },
  {
    n: 35,
    d: 'GOV',
    m: 0,
    q: 'A Unity Catalog ABAC policy applies column masking with the UDF mask_credit_card(cc STRING). Every column tagged pii=credit_card across all tables in bank.safebox must be masked (financial_analysts see masked values; admins see unmasked). The policy ends with: ON COLUMN cc;\n\nWhich option correctly fills in the MATCH COLUMNS clause?',
    o: [
      "MATCH COLUMNS hasTagValue('pii') AS credit_card",
      "MATCH COLUMNS hasTag('pii') AS credit_card",
      "MATCH COLUMNS hasTagValue('pii','credit_card') AS cc",
      "MATCH COLUMNS hasTagValue('pii', 'cc') AS credit_card",
    ],
    a: [2],
    x: "hasTagValue('clave','valor') selecciona columnas con la pareja tag=valor exacta (pii=credit_card), y el alias AS cc debe coincidir con el placeholder de ON COLUMN cc. hasTag solo comprueba la existencia de la clave, sin filtrar el valor.",
  },
  {
    n: 36,
    d: 'TRA',
    m: 0,
    q: 'A data analyst works with two PySpark DataFrames: teachers_df and courses_df, both containing a teacher_id column. They need a DataFrame of teachers who do NOT teach any courses.\n\nWhich PySpark code achieves this requirement?',
    o: [
      '```\njoined_df = teachers_df.join(courses_df, "teacher_id", "left_anti")\n```',
      '```\njoined_df = teachers_df.join(courses_df, "teacher_id", "left_semi")\n```',
      '```\njoined_df = teachers_df.join(courses_df, "teacher_id", "full")\n```',
      '```\njoined_df = teachers_df.join(courses_df, "teacher_id", "left")\n```',
    ],
    a: [0],
    x: 'El left_anti join devuelve las filas del DataFrame izquierdo SIN coincidencia en el derecho: profesores sin cursos. left_semi es lo contrario (solo con coincidencia), y left/full requerirían un filtro adicional de nulls.',
  },
  {
    n: 37,
    d: 'TRO',
    m: 0,
    q: 'A team processes a large-scale ETL pipeline joining multiple large datasets with hundreds of columns and billions of records. During the join phase, executors repeatedly spill data to disk and performance degrades due to excessive shuffling.\n\nWhat type of resource optimization should the team prioritize?',
    o: ['Storage Optimized', 'Compute Optimized', 'GPU Optimized', 'Memory Optimized'],
    a: [3],
    x: 'Los joins masivos con shuffle son intensivos en memoria: instancias memory-optimized permiten completar más operaciones en memoria sin derrames a disco (spill), reduciendo el overhead del shuffle.',
  },
  {
    n: 38,
    d: 'TRA',
    m: 0,
    q: 'Which of the following statements best describes the usage of the CREATE SCHEMA command?',
    o: [
      'It\'s used to infer and store schema in "cloudFiles.schemaLocation"',
      "It's used to create a table schema (column names and datatypes)",
      "It's used to create a database",
      "It's used to merge the schema when writing data into a target table",
    ],
    a: [2],
    x: 'CREATE SCHEMA es un alias de CREATE DATABASE: crea una base de datos (esquema). Su uso es intercambiable, aunque SCHEMA es la forma preferida.',
  },
  {
    n: 39,
    d: 'GOV',
    m: 0,
    q: '"A centralized governance layer that manages metadata and access controls across workspaces and clouds. It organizes data using a three-level namespace (catalog.schema.table) and enables fine-grained permissions down to the column level, while also providing audit logging and data lineage for compliance."\n\nWhich of the following is being described?',
    o: ['Delta Lake', 'Unity Catalog', 'Hive metastore', 'Databricks SQL'],
    a: [1],
    x: 'Es la definición de Unity Catalog: gobernanza centralizada multi-workspace y multi-cloud, espacio de nombres de 3 niveles, permisos finos (fila/columna), auditoría y linaje. El Hive metastore es el catálogo legacy de 2 niveles.',
  },
  {
    n: 40,
    d: 'P',
    m: 1,
    q: 'Which TWO of the following are native capabilities of a Delta table?',
    o: [
      'Delta Lake can evolve the table schema to include new optional columns without having to manually recreate the table.',
      'Delta Lake prevents writes that do not conform to the table schema.',
      'Delta Lake eliminates the need for any underlying storage system such as cloud object storage.',
      'Delta Lake automatically deletes old data versions after every write operation to save space.',
      'Delta Lake enables efficient table data storage using JSON format.',
    ],
    a: [0, 1],
    x: 'Delta Lake ofrece schema enforcement (rechaza escrituras que no cumplen el esquema) y schema evolution (añadir columnas con mergeSchema sin recrear la tabla). Los datos se guardan en Parquet (no JSON), Delta se apoya en el almacenamiento cloud existente y las versiones antiguas solo se borran con VACUUM.',
  },
  {
    n: 41,
    d: 'TRO',
    m: 0,
    q: 'A data engineer noticed unused data files in the directory of a Delta table. They executed the VACUUM command, but only some of those unused data files were deleted.\n\nWhich of the following could explain why?',
    o: [
      'The deleted files were older than the default retention threshold, while the remaining files are newer than the threshold and cannot be deleted.',
      'The deleted files were smaller than the default size threshold, while the remaining files are larger and cannot be deleted.',
      'The deleted files were newer than the default retention threshold, while the remaining files are older and cannot be deleted.',
      'The deleted files were larger than the default size threshold, while the remaining files are smaller and cannot be deleted.',
    ],
    a: [0],
    x: 'VACUUM solo borra archivos no utilizados que superan el periodo de retención (7 días por defecto); los más recientes se conservan para no romper operaciones en curso. No existe un umbral por tamaño.',
  },
  {
    n: 42,
    d: 'CICD',
    m: 0,
    q: 'Which of the following operations can a data engineer use to update a Git folder from its remote Git repository?',
    o: ['Push', 'Pull', 'Commit', 'Clone'],
    a: [1],
    x: 'Pull descarga el contenido del repositorio remoto y actualiza inmediatamente el repositorio local para que coincida. Push sube cambios, Commit los guarda localmente y Clone crea la copia inicial.',
  },
  {
    n: 43,
    d: 'ING',
    m: 0,
    q: 'A junior data engineer usually uses the INSERT INTO command to write data into a Delta table. A senior data engineer suggested using another command that avoids writing duplicate records.\n\nWhich command is the one suggested by the senior data engineer?',
    o: ['`INSERT OR OVERWRITE`', '`MERGE INTO`', '`COPY INTO`', '`UPDATE`'],
    a: [1],
    x: 'MERGE INTO combina inserciones, actualizaciones y borrados de una tabla origen sobre una tabla Delta destino, evitando insertar registros duplicados (patrón upsert basado en una clave de coincidencia).',
  },
  {
    n: 44,
    d: 'TRA',
    m: 0,
    q: 'A senior engineer reviews expectation functions used in a Lakeflow Spark Declarative Pipeline for data quality validation.\n\nWhich of the following function calls is NOT a valid expectation function in Lakeflow Declarative Pipelines?',
    o: ['`expect(...)`', '`expect_or_fail(...)`', '`expect_or_drop(...)`', '`expect_or_warn(...)`'],
    a: [3],
    x: 'Las expectativas soportadas son expect() (conserva las filas inválidas y las reporta), expect_or_drop() (las descarta) y expect_or_fail() (falla la actualización). expect_or_warn() no existe.',
  },
  {
    n: 45,
    d: 'CICD',
    m: 0,
    q: 'A data engineer wants to use the Databricks CLI to get the YAML definition of an existing job (downloading its referenced artifacts) and automatically link the generated resource with the existing one in the workspace, so YAML updates modify the linked job rather than creating a new one.\n\nWhich command achieves this?',
    o: [
      '```\ndatabricks bundle get job --existing-job-id <job_id> --link\n```',
      '```\ndatabricks bundle clone job --existing-job-id <job_id> --link\n```',
      '```\ndatabricks bundle generate job --existing-job-id <job_id> --bind\n```',
      '```\ndatabricks bundle download job --existing-job-id <job_id> --bind\n```',
    ],
    a: [2],
    x: 'databricks bundle generate crea la configuración YAML de un recurso existente (job, pipeline, app, dashboard) y descarga sus artefactos; el flag --bind vincula el recurso generado con el existente para que las actualizaciones lo modifiquen en lugar de duplicarlo.',
  },
  {
    n: 46,
    d: 'P',
    m: 0,
    q: 'For production jobs, which of the following cluster types is recommended to use?',
    o: ['Job clusters', 'All-purpose clusters', 'On-premises clusters', 'Production clusters'],
    a: [0],
    x: 'Los job clusters son clústeres dedicados por ejecución: se crean al lanzar el job, terminan al acabar (ahorro de coste frente a all-purpose) y aíslan cada carga en un entorno propio. Es la recomendación de Databricks para producción.',
  },
  {
    n: 47,
    d: 'ING',
    m: 0,
    q: 'A team needs to ingest clickstream events from an Apache Kafka topic into Databricks. The system receives 10,000 events per second, and the data must be processed with a latency of under 60 seconds.\n\nWhich ingestion approach is most appropriate?',
    o: [
      'Batch ingestion using scheduled Spark jobs every minute',
      'Structured Streaming from Kafka into Databricks',
      'Incremental ingestion using a Lakeflow managed connector for Kafka',
      'Full table reload from Kafka every 24 hours',
    ],
    a: [1],
    x: 'Spark Structured Streaming tiene un conector nativo y optimizado para Kafka, con garantías exactly-once y latencias de segundos, ideal para 10k eventos/s. Los batches por cron acumulan retraso ante picos y los conectores gestionados de Lakeflow cubren SaaS/BBDD, no Kafka.',
  },
  {
    n: 48,
    d: 'TRA',
    m: 0,
    q: 'Which of the following statements is NOT true about CTAS statements?',
    o: [
      'With CTAS statements, data will be inserted during the table creation',
      'CTAS statements automatically infer schema information from query results',
      'CTAS statements support manual schema declaration',
      'CTAS statements stand for CREATE TABLE _ AS SELECT statement',
    ],
    a: [2],
    x: 'CREATE TABLE AS SELECT crea y puebla la tabla con el resultado de la consulta, infiriendo el esquema automáticamente: NO admite declaración manual de esquema (columnas y tipos).',
  },
  {
    n: 49,
    d: 'CICD',
    m: 0,
    q: 'A DAB defines the variable catalog_name (default: demo_catalog). The team wants to set catalog_name per target to enable environment-specific behavior without modifying business code.\n\nWhich configuration meets this requirement?',
    o: [
      'variables:\n  catalog_name:\n    default: demo_catalog\n    targets:\n      dev: dev_catalog\n      prod: prod_catalog',
      'variables:\n  catalog_name:\n    default: demo_catalog\n    dev: dev_catalog\n    prod: prod_catalog',
      '```\nvariables:\n  catalog_name:\n    default: demo_catalog\n    target: ${target}_catalog\n```',
      'targets:\n  dev:\n    variables:\n      catalog_name: dev_catalog\n  prod:\n    variables:\n      catalog_name: prod_catalog',
    ],
    a: [3],
    x: 'Las variables se declaran globalmente, pero los valores por entorno se asignan anidando un mapeo variables dentro de cada target. Al desplegar con -t dev, la CLI sobrescribe el default con el valor del target activo.',
  },
  {
    n: 50,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has a DataFrame, employeesDf, with a column named salary and wants to permanently rename this column to base_salary for all future operations on employeesDf.\n\nWhich PySpark code fragment meets this requirement?',
    o: [
      '```\nemployeesDf = employeesDf.withColumn("salary", "base_salary")\n```',
      '```\nemployeesDf = employeesDf.select(col("*"), col("salary").alias("base_salary"))\n```',
      '```\nemployeesDf = employeesDf.select(col("salary").alias("base_salary"))\n```',
      '```\nemployeesDf = employeesDf.withColumnRenamed("salary", "base_salary")\n```',
    ],
    a: [3],
    x: 'withColumnRenamed(nombreActual, nombreNuevo) renombra la columna conservando el resto del esquema; la reasignación (=) hace el cambio permanente al ser los DataFrames inmutables. Las variantes con select duplican o pierden columnas.',
  },
  {
    n: 51,
    d: 'P',
    m: 0,
    q: 'Which scenario is best suited for using spot instances in Databricks?',
    o: [
      'Non-critical batch processing jobs with retry capability',
      'Interactive data analysis by business analysts',
      'Real-time stream processing with strict SLA requirements',
      'Transactional workloads with high concurrency demands',
    ],
    a: [0],
    x: 'Las instancias spot (capacidad sobrante con gran descuento) son ideales para cargas batch tolerantes a fallos y reintentos, sin usuario esperando. Los casos con SLA estricto, interactivos o transaccionales no toleran la terminación inesperada.',
  },
  {
    n: 52,
    d: 'P',
    m: 0,
    q: 'A data engineer wants to increase the cluster size of an existing Databricks SQL warehouse.\n\nWhich of the following is the benefit of increasing the cluster size of Databricks SQL warehouses?',
    o: [
      'Reduces cost since large clusters use Spot instances',
      'Reduces the latency of query execution',
      'The cluster size of SQL warehouses is not configurable; instead, they can increase the number of clusters',
      'Speeds up the startup time of the SQL warehouse',
    ],
    a: [1],
    x: 'El tamaño del clúster (workers y recursos de cómputo) determina la potencia disponible para las consultas: aumentarlo reduce la latencia de ejecución. Escalar el número de clústeres sirve para la concurrencia, no para acelerar una consulta individual.',
  },
  {
    n: 53,
    d: 'TRO',
    m: 0,
    q: 'A team runs a Spark job that processes skewed data during a join operation. Adaptive Query Execution (AQE) is enabled in the cluster.\n\nHow does AQE help address data skew in this case?',
    o: [
      'AQE removes skewed data from the dataset to dynamically mitigate the impact of data skew',
      'AQE converts all joins into Cartesian products to balance load and improve parallelism',
      'AQE automatically detects skewed partitions and splits them into smaller sub-partitions for parallel processing',
      'AQE disables partitioning entirely to improve execution efficiency while preserving distributed processing.',
    ],
    a: [2],
    x: 'Con estadísticas de runtime, AQE detecta particiones desproporcionadas y las divide en subparticiones que se procesan en paralelo, eliminando las tareas rezagadas. Nunca elimina datos válidos ni convierte joins en productos cartesianos.',
  },
  {
    n: 54,
    d: 'ING',
    m: 0,
    q: 'A data engineer needs to decide whether to use Auto Loader or the COPY INTO command to load input data files incrementally.\n\nIn which scenario should the data engineer use Auto Loader over COPY INTO?',
    o: [
      'If they are going to ingest a small number of files, in the order of thousands',
      'If they are going to load a subset of re-uploaded files',
      'If they are going to ingest files in the order of millions or more over time',
      'If the data schema is not going to evolve frequently',
    ],
    a: [2],
    x: 'Regla práctica: miles de archivos → COPY INTO; millones o más a lo largo del tiempo → Auto Loader, que además ofrece mejores primitivas de inferencia y evolución de esquema para esquemas cambiantes.',
  },
  {
    n: 55,
    d: 'GOV',
    m: 0,
    q: 'A data engineer uses the following SQL query:\n```\nGRANT USE SCHEMA ON SCHEMA sales_db TO finance_team\n```\nWhich of the following is the benefit of the USE SCHEMA privilege?',
    o: [
      'Gives the ability to view database objects and their metadata',
      'Gives full permissions on the entire database',
      'Grants read access to the database',
      'It is a prerequisite to perform any action on the database',
    ],
    a: [3],
    x: 'USE SCHEMA (antes USAGE) no concede permisos directos: es el prerrequisito para realizar cualquier acción sobre los objetos del esquema (junto con USE CATALOG en el catálogo padre en Unity Catalog).',
  },
];

export const exam4: RawQuestion[] = [
  {
    n: 1,
    d: 'JOBS',
    m: 0,
    q: 'A team maintains a reporting job that should automatically refresh whenever a Delta table is updated by upstream ingestion pipelines, so downstream reports remain consistent and up to date as new data arrives.\n\nWhat trigger configuration should the team use for this job?',
    o: ['Continuous trigger', 'Table update trigger', 'File arrival trigger', 'Scheduled trigger'],
    a: [1],
    x: 'El table update trigger monitoriza tablas origen y lanza el job en cuanto se detectan cambios (updates, merges, deletes) en la tabla Delta. El file arrival responde a archivos en almacenamiento, no a cambios internos de tablas.',
  },
  {
    n: 2,
    d: 'TRA',
    m: 1,
    q: 'An IoT bronze table has a payload column of type STRING containing JSON like {"device_id":"A1","temperature":22.5,"humidity":60}. A data engineer needs to extract fields for a silver table.\n\nWhich TWO queries correctly extract data from the JSON?',
    o: [
      "```\nSELECT get_json_object(payload, '$.device_id') AS device_id,\n       get_json_object(payload, '$.temperature') AS temperature\nFROM bronze_iot;\n```",
      "```\nSELECT json_extract(payload, 'device_id') AS device_id FROM bronze_iot;\n```",
      '```\nSELECT payload.device_id AS device_id, payload.temperature AS temperature FROM bronze_iot;\n```',
      '```\nSELECT payload:device_id AS device_id,\n       payload:temperature AS temperature\nFROM bronze_iot;\n```',
      "```\nSELECT from_json(payload, 'device_id STRING, temperature STRING) AS parsed_payload FROM bronze_iot;\n```",
    ],
    a: [0, 3],
    x: "Sobre un STRING con JSON funcionan get_json_object(col, '$.campo') (JSONPath) y el operador de dos puntos col:campo, nativo de Databricks SQL. La notación de punto solo vale para STRUCT/VARIANT, json_extract es de Presto/Trino y la opción con from_json tiene la comilla sin cerrar y devolvería un struct, no columnas.",
  },
  {
    n: 3,
    d: 'P',
    m: 0,
    q: 'A data engineer needs to execute a scheduled Python notebook hourly, processing relatively small volumes of data. The execution environment should be efficient, require no cluster pre-warming, and deliver consistent, reliable performance.\n\nWhich compute option is recommended for executing this job?',
    o: [
      'Serverless Job Compute',
      'Classic Job Cluster',
      'Classic All-purpose Cluster',
      'Serverless SQL Warehouse',
    ],
    a: [0],
    x: 'El Serverless Job Compute aprovisiona recursos dinámicamente sin pre-calentamiento de clúster, con rendimiento consistente y sin gestión de infraestructura: ideal para jobs Python programados y ligeros. Databricks lo recomienda para todas las tareas de jobs cuando está soportado.',
  },
  {
    n: 4,
    d: 'TRA',
    m: 0,
    q: 'Given a table faculties with an array column students, fill in the blank to get the students enrolled in fewer than 3 courses:\n```\nSELECT faculty_id, students, ___________ AS few_courses_students\nFROM faculties\n```',
    o: [
      '`TRANSFORM(students, i -> i.total_courses < 3)`',
      '`TRANSFORM(students, total_courses < 3)`',
      '`FILTER(students, i -> i.total_courses < 3)`',
      '`FILTER(students, total_courses < 3)`',
    ],
    a: [2],
    x: 'FILTER(array, lambda) es una función de orden superior que devuelve un array con los elementos que cumplen el predicado (i -> i.total_courses < 3). TRANSFORM aplicaría una transformación a cada elemento, no un filtro.',
  },
  {
    n: 5,
    d: 'CICD',
    m: 0,
    q: 'Which of the following functionalities can be performed in Git folders?',
    o: [
      'Create new remote Git repositories',
      'Create pull requests',
      'Delete branches',
      'Pull changes from a remote Git repository',
    ],
    a: [3],
    x: 'Los Git folders soportan la operación Pull: descargar el contenido del remoto y actualizar el repo local. Crear repositorios remotos, pull requests y borrar ramas debe hacerse en el proveedor Git.',
  },
  {
    n: 6,
    d: 'TRA',
    m: 0,
    q: 'A junior data engineer executes df.describe() to explore a dataset. A senior data engineer suggests using df.summary() instead.\n\nWhich advantage does df.summary() provide over df.describe() for data exploration?',
    o: [
      'df.summary() automatically cleans null values and removes outliers before computing statistics.',
      'df.summary() generates visual charts and interactive plots for each column in the dataset.',
      'df.summary() provides extended statistics such as approximate quartiles (25%, 50%, 75%), which are not available in df.describe().',
      'df.summary() offers an AI-generated summary of the dataset with insights, patterns, and recommendations.',
    ],
    a: [2],
    x: 'describe() calcula count, mean, stddev, min y max; summary() añade los cuartiles aproximados (25%, 50%, 75%) y además permite pedir estadísticas concretas como argumentos (p. ej. df.summary("count", "50%", "95%")).',
  },
  {
    n: 7,
    d: 'GOV',
    m: 0,
    q: "A team manages the Unity Catalog table employees (id, name, salary, region) and created the UDF fr_filter(region STRING) that returns true for hr_team members, otherwise region='FR'.\n\nWhich command applies this function as a row filter to the table?",
    o: [
      '```\nALTER TABLE employees SET ROW FILTER fr_filter ON (region);\n```',
      '```\nALTER TABLE employees ALTER COLUMN region SET ROW FILTER fr_filter;\n```',
      '```\nALTER TABLE employees SET ROW FILTER fr_filter;\n```',
      '```\nSET ROW FILTER fr_filter ON TABLE employees TO COLUMN region\n```',
    ],
    a: [0],
    x: 'La sintaxis para asociar un filtro de filas es ALTER TABLE <tabla> SET ROW FILTER <función> ON (<columnas>): el filtro se aplica a nivel de tabla pasando las columnas como argumentos de la UDF. ALTER COLUMN ... SET se usa para máscaras de columna, no para filtros de fila.',
  },
  {
    n: 8,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has a custom-location schema named db_hr and wants to know where this schema was created in the underlying storage.\n\nWhich command can the data engineer use to complete this task?',
    o: [
      '`DESCRIBE EXTENDED db_hr`',
      '`DESCRIBE db_hr`',
      '`SELECT location FROM db_hr.db`',
      '`DESCRIBE DATABASE db_hr`',
    ],
    a: [3],
    x: 'DESCRIBE DATABASE (o DESCRIBE SCHEMA) devuelve los metadatos del esquema: nombre, comentario y ubicación en el sistema de archivos. DESCRIBE EXTENDED sin la palabra DATABASE se aplica a tablas.',
  },
  {
    n: 9,
    d: 'TRO',
    m: 0,
    q: 'A data engineer analyzes Spark UI summary metrics for 27 completed tasks in a stage: durations range from 10 s (min) to 45 s (max) with a median of 25 s; records per task have a median of 55 and a max of 76; GC time maxes at 59 ms.\n\nWhich conclusion can the data engineer draw from these statistics?',
    o: [
      'The data is not evenly distributed across tasks, indicating data skew.',
      'The stage is running normally without any major issues.',
      'The system is spending too much time on garbage collection (GC)',
      'The cluster does not have enough resources to handle the workload.',
    ],
    a: [1],
    x: 'Las métricas están bien distribuidas: entrada y shuffle homogéneos (mediana 55, máx 76 registros), GC insignificante (59 ms sobre 45 s, muy por debajo del 10%) y duraciones ajustadas. No hay skew ni presión de recursos: la etapa es saludable.',
  },
  {
    n: 10,
    d: 'ING',
    m: 0,
    q: 'Which of the following offers a low-code, fully managed data ingestion solution from popular enterprise applications into Unity Catalog tables?',
    o: ['Lakeflow Connect managed connector', '`COPY INTO`', 'Databricks Connect', 'Auto Loader'],
    a: [0],
    x: 'Los conectores gestionados de Lakeflow Connect ingieren datos de aplicaciones SaaS y bases de datos empresariales con configuración low-code, cómputo serverless, CDC y gobernanza Unity Catalog. Auto Loader es para archivos (con código), COPY INTO es SQL y Databricks Connect conecta IDEs.',
  },
  {
    n: 11,
    d: 'TRA',
    m: 0,
    q: 'Given the following Structured Streaming query:\n```\n(spark.readStream\n    .table("cleanedOrders")\n    .groupBy("productCategory")\n    .agg(sum("totalWithTax"))\n  .writeStream\n    .option("checkpointLocation", checkpointPath)\n    .outputMode("complete")\n    .table("aggregatedOrders"))\n```\nWhich of the following best describes the purpose of this query in a Medallion Architecture?',
    o: [
      'The query is performing a hop from the Silver layer to a Gold table',
      'The query is performing data transfer from a Gold table into a production application',
      'The query is performing raw data ingestion into a Bronze table',
      'The query is performing a hop from a Bronze table to a Silver table',
    ],
    a: [0],
    x: 'La consulta crea agregados de negocio (suma por categoría) a partir de la tabla Silver cleanedOrders y los escribe en la tabla Gold aggregatedOrders: el salto Silver → Gold.',
  },
  {
    n: 12,
    d: 'GOV',
    m: 0,
    q: 'A finance data analyst requires read-only access to all existing and future tables within the Unity Catalog schema enterprise.reporting, limited exclusively to this schema.\n\nWhich SQL commands will correctly grant this access?',
    o: [
      '```\nGRANT SELECT ON SCHEMA enterprise.reporting TO finance-analyst;\nGRANT USE SCHEMA ON SCHEMA enterprise.reporting TO finance-analyst;\nGRANT USE CATALOG ON CATALOG enterprise TO finance-analyst;\n```',
      '```\nGRANT BROWSE ON SCHEMA enterprise.reporting TO finance-analyst;\nGRANT USE SCHEMA ON SCHEMA enterprise.reporting TO finance-analyst;\nGRANT USE CATALOG ON CATALOG enterprise TO finance-analyst;\n```',
      '```\nGRANT USE SCHEMA ON SCHEMA enterprise.reporting TO finance-analyst;\nGRANT USE CATALOG ON CATALOG enterprise TO finance-analyst;\n```',
      '```\nGRANT SELECT ON CATALOG enterprise TO finance-analyst;\nGRANT USE SCHEMA ON CATALOG enterprise TO finance-analyst;\nGRANT USE CATALOG ON CATALOG enterprise TO finance-analyst;\n```',
    ],
    a: [0],
    x: 'SELECT a nivel de esquema se propaga a todas las tablas y vistas actuales y futuras de ese esquema, junto con USE SCHEMA y USE CATALOG para el tránsito. Conceder SELECT a nivel de catálogo excedería el alcance pedido y BROWSE solo permite ver metadatos.',
  },
  {
    n: 13,
    d: 'GOV',
    m: 0,
    q: 'A data engineer converts an external Delta table into a managed table in production using ALTER TABLE ... SET MANAGED. Afterwards, a streaming query stops with the error DELTA_STREAMING_INTERRUPTED_BY_MANAGED_TABLE_CONVERSION.\n\nHow should the data engineer fix this issue?',
    o: [
      'Restart the stream and it will automatically resume from the last committed offset using the converted table.',
      'Run the REPAIR TABLE command to update the checkpointing offset of the converted table.',
      'Run the VACUUM command on the converted table to remove stale files and restore streaming state.',
      'Roll back the converted managed table to an external table as this feature is supported by the SET MANAGED command.',
    ],
    a: [0],
    x: 'La interrupción es un mecanismo de seguridad durante la conversión. Basta reiniciar el stream: con la redirección automática de rutas de Unity Catalog, retoma desde el último offset comprometido. REPAIR TABLE registra particiones, no checkpoints, y VACUUM no restaura estado de streaming.',
  },
  {
    n: 14,
    d: 'TRA',
    m: 0,
    q: "A data engineer is cleaning transaction data before loading it into a silver table using the following PySpark command:\n```\ndf.na.drop(how='all')\n```\nWhat is the effect of this operation?",
    o: [
      'Removes rows where all columns are non-null',
      'Removes rows where any column contains a null value',
      'Removes rows only when all columns in the row are null',
      'Replaces all null values with empty strings',
    ],
    a: [2],
    x: "Con how='all', la fila solo se elimina si TODAS sus columnas son null; con un solo valor válido se conserva. how='any' (el valor por defecto) elimina la fila con cualquier null, y para reemplazar nulls se usa na.fill().",
  },
  {
    n: 15,
    d: 'TRO',
    m: 0,
    q: 'A data engineer must investigate why a Lakeflow Job experienced a gradual increase in execution duration over several production runs, and needs a feature that visualizes execution trends and compares metrics from prior job executions. Which option should be selected?',
    o: ['Query History', 'Dashboards', 'Run History', 'Catalog Explorer'],
    a: [2],
    x: 'El Run History de Lakeflow Jobs (pestaña Runs) muestra gráficos de duración y permite comparar ejecuciones pasadas (filas leídas/escritas, duración por tarea) para detectar regresiones de rendimiento. Query History es para consultas SQL de warehouses y Catalog Explorer para gobernanza.',
  },
  {
    n: 16,
    d: 'ING',
    m: 0,
    q: 'An Auto Loader stream incrementally ingests JSON files from cloud storage into a Delta table. Files may occasionally be re-uploaded under the same path and filename to apply data corrections.\n\nWhich option correctly fills in the blank in order to load those corrections into the table?',
    o: [
      '`.option("cloudFiles.includeExistingFiles", "true")`',
      '`.option("cloudFiles.force", "true")`',
      '`.option("mergeSchema", "true")`',
      '`.option("cloudFiles.allowOverwrites", "true")`',
    ],
    a: [3],
    x: 'Por defecto Auto Loader procesa cada ruta exactamente una vez e ignora reescrituras. Con cloudFiles.allowOverwrites=true tiene en cuenta también el timestamp de modificación y reingiere el archivo completo cuando se sobrescribe con correcciones.',
  },
  {
    n: 17,
    d: 'TRO',
    m: 0,
    q: 'The engineering team manages a large Delta table with frequent updates and deletes, and query performance degrades due to an increase in small files. They decide to run OPTIMIZE with Z-Order indexing on frequently filtered columns.\n\nWhat type of resource optimization should the team prioritize to ensure these commands execute efficiently?',
    o: ['GPU Optimized', 'Storage Optimized', 'Compute Optimized', 'Memory Optimized'],
    a: [2],
    x: 'OPTIMIZE compacta archivos y Z-Order reordena datos: ambas operaciones son intensivas en CPU (escaneo y reescritura masiva). Los recursos compute-optimized proporcionan la potencia y el paralelismo de CPU necesarios.',
  },
  {
    n: 18,
    d: 'ING',
    m: 0,
    q: 'A team needs to sync sales data from Snowflake into Databricks every 6 hours. Only records that have changed should be ingested to avoid unnecessary processing and duplication.\n\nWhich ingestion approach best meets this requirement?',
    o: [
      'Structured Streaming ingestion from Snowflake',
      'Export Snowflake data to files and append it to the target table every 6 hours',
      'Full refresh of the Snowflake table every 6 hours',
      'Incremental ingestion using the Lakeflow managed connector for Snowflake',
    ],
    a: [3],
    x: 'El conector gestionado de Lakeflow Connect hace una carga completa inicial y luego solo trae los cambios (seguimiento incremental/CDC), orquestado por programación (cada 6 h). El full refresh desperdicia cómputo, la exportación a archivos es frágil y JDBC no soporta streaming nativo.',
  },
  {
    n: 19,
    d: 'TRA',
    m: 0,
    q: 'Which of the following will utilize Gold tables as their source?',
    o: ['Bronze tables', 'Dashboards', 'Silver tables', 'Auto Loader'],
    a: [1],
    x: 'Las tablas Gold contienen agregados de negocio que alimentan informes, dashboards y machine learning. El flujo medallion va Bronze → Silver → Gold, nunca al revés.',
  },
  {
    n: 20,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has the following query in a Lakeflow Spark Declarative Pipeline, which fails to start:\n```\nCREATE STREAMING TABLE sales_silver\nAS SELECT store_id, total + tax AS total_after_tax\n   FROM sales_bronze\n```\nWhich change should be made to successfully start the pipeline?',
    o: [
      '```\nFROM STREAMING sales_bronze\n```',
      '```\nCREATE STREAM TABLE ... FROM STREAM(LIVE.sales_bronze)\n```',
      '```\nFROM STREAM(sales_bronze)\n```',
      '```\nFROM STREAMING(sales_bronze)\n```',
    ],
    a: [2],
    x: 'En pipelines declarativos, para leer otra tabla del pipeline como stream se usa la función STREAM(): CREATE STREAMING TABLE ... AS SELECT ... FROM STREAM(tabla_origen). (Una sintaxis reciente también admite STREAM como palabra clave: FROM STREAM tabla.)',
  },
  {
    n: 21,
    d: 'ING',
    m: 0,
    q: 'A data engineer needs Auto Loader to ingest JSON files that may contain new fields over time, allowing the table schema to evolve as new columns are detected during processing.\n\nHow should Auto Loader be configured?',
    o: [
      'Set inferColumnTypes to true to automatically detect new columns with their data types.',
      'Configure cloudFiles.schemaLocation, and set cloudFiles.schemaEvolutionMode=addNewColumns to support automatic schema evolution',
      'Auto Loader cannot detect the addition of new columns in input files during processing.',
      'Set mergeSchema to true to enable Delta Lake merge schemas during writes',
    ],
    a: [1],
    x: 'La evolución de esquema en Auto Loader requiere cloudFiles.schemaLocation (donde persiste y rastrea el esquema) y cloudFiles.schemaEvolutionMode=addNewColumns (añade automáticamente los campos nuevos). mergeSchema actúa en la escritura Delta, no en la lectura del stream, e inferColumnTypes solo infiere tipos.',
  },
  {
    n: 22,
    d: 'TRA',
    m: 0,
    q: 'A data engineer must calculate both the total and average salary of employees, grouped by department:\n```\nresult_df = df.groupBy("department").____________(\n    sum("salary").alias("total_salary"),\n    avg("salary").alias("average_salary")\n)\n```\nWhat function should be used to complete the code?',
    o: ['withColumn', 'filter', 'select', 'agg'],
    a: [3],
    x: 'agg() se usa tras groupBy() para aplicar una o varias funciones de agregación sobre los datos agrupados (aquí sum y avg del salario a la vez).',
  },
  {
    n: 23,
    d: 'TRO',
    m: 0,
    q: 'How does Automatic Liquid Clustering determine which columns to use as clustering keys in a Unity Catalog-managed Delta table?',
    o: [
      'It intelligently selects clustering keys from predefined clustering columns specified during table creation.',
      'It leverages advanced sampling strategies to randomize column selection after uniformly balancing data across all files.',
      'It automatically determines optimal clustering keys based on the type and order of column definition in the schema.',
      'It leverages Predictive Optimization to choose optimal clustering keys based on observed query behavior.',
    ],
    a: [3],
    x: 'El Automatic Liquid Clustering se apoya en Predictive Optimization, que analiza el comportamiento de las consultas (patrones de acceso y estadísticas) para elegir dinámicamente las claves de clustering óptimas.',
  },
  {
    n: 24,
    d: 'ING',
    m: 0,
    q: 'A data engineer must consume a stream of events from a Kafka topic named events_topic, hosted on a remote broker at host:port.\n\nWhich code snippet correctly establishes a streaming DataFrame in PySpark to read from this Kafka topic?',
    o: [
      '```\neventsStream = (spark.readStream\n    .format("kafka")\n    .option("kafka.bootstrap.servers", "host:port")\n    .option("subscribe", "events_topic")\n    .option("startingOffsets", "latest")\n    .load())\n```',
      '```\neventsStream = spark.readStream.format("kafka").load("host:port", "events_topic", "latest")\n```',
      '```\neventsStream = spark.readStream.kafka("host:port", "events_topic", "latest")\n```',
      '```\neventsStream = (spark.readStream.format("cloud_files")\n    .option("cloudFiles.format", "kafka")...)\n```',
    ],
    a: [0],
    x: 'El patrón correcto es readStream con format("kafka") y las opciones kafka.bootstrap.servers (clúster), subscribe (topic) y startingOffsets (punto de inicio: latest/earliest). No existe el método .kafka() ni el formato cloudFiles para Kafka.',
  },
  {
    n: 25,
    d: 'CICD',
    m: 0,
    q: 'A data engineer has deployed a bundle with a job resource keyed as sales_job and wants to execute it in the target workspace.\n\nWhich Databricks CLI command can the data engineer use to achieve this goal?',
    o: [
      '`databricks bundle deploy --run sales_job`',
      '`databricks bundle execute sales_job`',
      '`databricks bundle run sales_job`',
      '`databricks bundle trigger sales_job`',
    ],
    a: [2],
    x: 'databricks bundle run <clave-de-recurso> lanza un job o pipeline ya desplegado del bundle (con -t se elige el target). execute y trigger no existen, y deploy no admite un flag --run.',
  },
  {
    n: 26,
    d: 'P',
    m: 0,
    q: 'Which of the following services provides a data warehousing solution in the Databricks Intelligence Platform?',
    o: ['Lakeflow Connect', 'SQL warehouse', 'Databricks SQL', 'Unity Catalog'],
    a: [2],
    x: 'Databricks SQL (DB SQL) es el data warehouse de la plataforma Lakehouse para ejecutar SQL y aplicaciones de BI a escala. El SQL warehouse es el recurso de cómputo que usa Databricks SQL, no el servicio en sí.',
  },
  {
    n: 27,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to enforce that age is greater than 18 on an existing Delta table named users.\n\nWhich SQL statement correctly adds this constraint?',
    o: [
      'ADD CONSTRAINT eligible_adult ON TABLE users CHECK (age > 18)',
      '```\nALTER TABLE users CHECK (age > 18)\n```',
      '```\nALTER TABLE users ALTER COLUMN age SET (age > 18);\n```',
      '```\nALTER TABLE users ADD CONSTRAINT eligible_adult CHECK (age > 18)\n```',
    ],
    a: [3],
    x: 'La sintaxis para una restricción CHECK es ALTER TABLE <tabla> ADD CONSTRAINT <nombre> CHECK (condición). Delta verifica primero que todas las filas existentes cumplen la condición; si no, el comando falla.',
  },
  {
    n: 28,
    d: 'TRA',
    m: 0,
    q: 'A PySpark DataFrame productsDf contains multiple columns, including price. The engineer wants to return ALL columns while also displaying the price column with a temporary alias unit_price in the query output, without modifying the original DataFrame schema.\n\nWhich PySpark code fragment meets this requirement?',
    o: [
      '```\nproductsDf = productsDf.withColumnRenamed("price", "unit_price")\n```',
      '```\nproductsDf.selectExpr("price as unit_price")\n```',
      '```\nproductsDf.select(col("*"), col("price").alias("unit_price"))\n```',
      '```\nproductsDf.select(col("price").alias("unit_price"))\n```',
    ],
    a: [2],
    x: 'col("*") selecciona todas las columnas originales y col("price").alias("unit_price") añade una copia renombrada solo en la salida, sin tocar el esquema original. Las otras opciones pierden columnas o modifican permanentemente el DataFrame.',
  },
  {
    n: 29,
    d: 'ING',
    m: 0,
    q: 'Which of the following techniques allows Auto Loader to track the ingestion progress and store metadata of the discovered files?',
    o: ['Photon engine', 'Watermarking', 'Checkpointing', '`COPY INTO`'],
    a: [2],
    x: 'Auto Loader registra los archivos descubiertos mediante checkpointing en la checkpoint location, lo que le permite ofrecer garantías de ingesta exactly-once y reanudar tras fallos.',
  },
  {
    n: 30,
    d: 'TRA',
    m: 0,
    q: 'In the Medallion Architecture, which of the following statements best describes the Gold layer tables?',
    o: [
      'They provide business-level aggregations that power analytics, machine learning, and production applications',
      'The table structure resembles the source system table structure with additional metadata columns like load time and input file name.',
      'They maintain raw data ingested from various sources',
      'They represent a filtered, cleaned, and enriched version of data',
    ],
    a: [0],
    x: 'Gold es la capa final del multi-hop: agregados a nivel de negocio para informes, dashboards y ML. Las descripciones de datos crudos corresponden a Bronze y la de datos limpios/enriquecidos a Silver.',
  },
  {
    n: 31,
    d: 'GOV',
    m: 1,
    q: 'A data engineer has converted an external table to a managed table using ALTER TABLE ... SET MANAGED.\n\nWhat are the key advantages of using SET MANAGED for this conversion? (Choose 2 answers)',
    o: [
      'Supports rolling back the converted managed table to an external table.',
      'Continues using the original external location as the single source of truth for both reads and writes after conversion.',
      'Syncs both managed and external locations in parallel to support dual-write consistency after conversion.',
      'Redirects path-based reads and writes to allow legacy code to function after conversion.',
      'Immediately deletes the original external storage location after conversion to save storage costs.',
    ],
    a: [0, 3],
    x: 'SET MANAGED admite rollback seguro con UNSET MANAGED y redirige automáticamente las lecturas/escrituras por ruta a la nueva ubicación gestionada, de modo que el código legacy sigue funcionando. Los datos pasan al almacenamiento gestionado (sin dual-write) y la ubicación original se conserva ~14 días.',
  },
  {
    n: 32,
    d: 'P',
    m: 0,
    q: 'Which of the following locations hosts the Databricks workspace web application?',
    o: ['Control plane', 'Customer Cloud Account', 'Data plane', 'Databricks-managed cluster'],
    a: [0],
    x: 'El workspace y sus servicios (aplicación web/UI, gestor de clústeres, servicio de workflows, notebooks) se despliegan en el control plane gestionado por Databricks.',
  },
  {
    n: 33,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has defined the following data quality constraint in a Delta Live Tables pipeline:\n\nCONSTRAINT valid_id EXPECT (id IS NOT NULL) _____________\n\nFill in the blank so records violating this constraint will be added to the target table, and reported in metrics.',
    o: [
      'There is no need to add the ON VIOLATION clause. By default, records violating the constraint will be kept, and reported as invalid in the event log',
      '`ON VIOLATION ADD ROW`',
      '`ON VIOLATION FAIL UPDATE`',
      '`ON VIOLATION NONE`',
    ],
    a: [0],
    x: 'El comportamiento por defecto (sin cláusula ON VIOLATION) conserva los registros que incumplen la expectativa junto a los válidos, reportando las violaciones en el event log. ADD ROW y NONE no existen.',
  },
  {
    n: 34,
    d: 'P',
    m: 0,
    q: 'Which of the following compute resources is available in Databricks SQL?',
    o: ['Multi-node clusters', 'SQL warehouses', 'Single-node clusters', 'SQL engines'],
    a: [1],
    x: 'El recurso de cómputo de Databricks SQL es el SQL warehouse: la infraestructura que ejecuta comandos SQL sobre los objetos de datos.',
  },
  {
    n: 35,
    d: 'ING',
    m: 0,
    q: 'An S3 bucket receives thousands of image files daily in different formats (.png, .jpg, .gif). A streaming ingestion script with Auto Loader (format binaryFile) must be modified to ensure only .jpg files are processed:\n```\n.option("_____________", "*.jpg")\n```\nWhich option correctly fills in the blank?',
    o: ['cloudFiles.pathGlobFilter', 'pathGlobFilter', 'fileExtension', 'cloudFiles.fileExtension'],
    a: [1],
    x: 'La opción pathGlobFilter (sin prefijo cloudFiles) filtra los archivos de entrada por un patrón glob como *.jpg al usar Auto Loader.',
  },
  {
    n: 36,
    d: 'CICD',
    m: 0,
    q: 'A CI/CD process must verify bundle configurations on dev for every pull request, and on merges to the prod branch the system should automatically deploy to production.\n\nWhich set of actions meets these requirements?',
    o: [
      '```\nIn PR pipelines, execute databricks bundle validate -t dev;\n```\nOn prod, execute databricks bundle deploy -t prod',
      '```\nIn PR pipelines, execute databricks bundle run validate -t dev;\n```\nOn prod, execute databricks bundle run deploy -t prod',
      '```\nIn PR pipelines, execute databricks bundle deploy -t dev;\n```\nOn prod, execute databricks bundle run validate -t prod',
      '```\nIn PR pipelines, execute databricks bundle deploy -t dev --verify;\n```\nOn prod, execute databricks bundle deploy -t prod',
    ],
    a: [0],
    x: 'validate comprueba la configuración sin desplegar (perfecto como gate de CI en cada PR) y deploy despliega los recursos al hacer merge a prod. bundle run ejecuta recursos ya desplegados (run validate/run deploy no son válidos) y --verify no existe.',
  },
  {
    n: 37,
    d: 'TRO',
    m: 0,
    q: 'A data engineer wants Spark to automatically send small tables of up to 100 MB to all executors to improve join performance. What configuration should they set?',
    o: [
      '```\nspark.conf.set("spark.sql.broadcastTimeout", 100)\n```',
      '```\nspark.conf.set("spark.sql.shuffle.partitions", 100)\n```',
      '```\nspark.conf.set("spark.sql.autoBroadcastJoinThreshold", 104857600)\n```',
      '```\nspark.conf.set("spark.executor.memory", 104857600)\n```',
    ],
    a: [2],
    x: 'spark.sql.autoBroadcastJoinThreshold define el tamaño máximo en bytes para difundir automáticamente una tabla en un broadcast join: 100 MB = 100·1024·1024 = 104.857.600 bytes. broadcastTimeout es un tiempo de espera y shuffle.partitions controla las particiones del shuffle.',
  },
  {
    n: 38,
    d: 'TRO',
    m: 1,
    q: 'A data engineer runs a Spark job that performs a wide transformation where data needs to be reshuffled across partitions, but it fails with an ExecutorLostFailure error message.\n\nWhich TWO actions should the data engineer take to fix this issue?',
    o: [
      'Increase the number of shuffle partitions',
      'Increase the number of cores per executor',
      'Increase executor memory',
      'Increase the driver memory',
      'Enable the Photon engine',
    ],
    a: [0, 2],
    x: 'ExecutorLostFailure en un shuffle suele ser un OOM del executor: aumentar la memoria de executor da margen, y aumentar spark.sql.shuffle.partitions divide los datos en trozos más pequeños por tarea. Más cores por executor empeora (más tareas compartiendo la misma memoria) y la memoria del driver no interviene.',
  },
  {
    n: 39,
    d: 'ING',
    m: 0,
    q: 'A data engineer is building a Lakeflow Spark Declarative Pipeline to ingest data from a PostgreSQL database and wants to use a streaming table, assuming streaming tables can directly enable continuous ingestion from JDBC sources.\n\nWhich statement is correct regarding this assumption?',
    o: [
      'JDBC sources are automatically treated as streaming sources in SDP',
      'PostgreSQL ingestion is only supported via batch JDBC, not native streaming',
      'PostgreSQL can be consumed directly like Kafka in streaming tables',
      'Streaming tables support continuous JDBC ingestion from PostgreSQL',
    ],
    a: [1],
    x: 'Las conexiones JDBC son estáticas y orientadas a batch: Spark no puede rastrear offsets ni logs de transacciones vía JDBC, así que la lectura de PostgreSQL con JDBC es solo batch/snapshot. Para streaming casi en tiempo real haría falta Lakeflow Connect con CDC (replicación lógica/WAL).',
  },
  {
    n: 40,
    d: 'ING',
    m: 0,
    q: "A team ingests JSON logs from cloud storage into a Delta table with COPY INTO. The source contains both old and newly updated files, and the team wants to filter by last modification time to load only recent files:\n```\nCOPY INTO target_table\nFROM 'abfss://logs@staging.dfs.core.windows.net/'\nFILEFORMAT = JSON\n____________;\n```\nWhich option correctly fills in the blank?",
    o: [
      "FORMAT_OPTIONS ('modifiedAfter' = 'ts')",
      "FORMAT_OPTIONS (_metadata.file_modification_time > 'ts')",
      "FORMAT_OPTIONS (file.modification_time > 'ts')",
      "FORMAT_OPTIONS ('modificationTime' > 'ts')",
    ],
    a: [0],
    x: "COPY INTO acepta opciones genéricas del lector en FORMAT_OPTIONS con formato clave-valor: 'modifiedAfter' = 'timestamp' ingiere solo archivos modificados después de esa fecha. Las demás variantes no son sintaxis válida.",
  },
  {
    n: 41,
    d: 'TRA',
    m: 0,
    q: 'A data engineer is validating incoming transaction data and runs the following query:\n```\nSELECT COALESCE(TRY_CAST("100$" AS INT), 0)\n```\nWhich of the following describes the result of this query?',
    o: ['100', 'An error is thrown', '0', 'NULL'],
    a: [2],
    x: 'TRY_CAST devuelve NULL al no poder convertir "100$" (por el símbolo $), y COALESCE devuelve el primer valor no nulo de la lista: el 0 de respaldo. Es el patrón recomendado para tolerar datos malformados.',
  },
  {
    n: 42,
    d: 'P',
    m: 0,
    q: 'In Lakeflow Jobs, which compute option helps reduce operational overhead and idle cluster costs?',
    o: ['Serverless jobs compute', 'Classic all-purpose compute', 'SQL warehouses', 'Classic jobs compute'],
    a: [0],
    x: 'Con serverless, Databricks gestiona toda la infraestructura: aprovisiona lo justo, termina al acabar y elimina la configuración manual y los costes de arranque/ocio. El clásico exige dimensionar y sufre 4-6 min de arranque; el all-purpose es el más caro.',
  },
  {
    n: 43,
    d: 'TRA',
    m: 0,
    q: 'A Lakeflow Spark Declarative Pipeline must enforce rules defined in the dictionary valid_products (valid_id, recent_sales, quantity_within_range). Any invalid records should still be written to the target, while metrics about violations are captured.\n\nWhich configuration satisfies these requirements?',
    o: [
      '```\n@dp.table\n@dp.expect_or_fail(valid_products)\ndef silver_sales(): ...\n```',
      '```\n@dp.table\n@dp.expect_all(valid_products)\ndef silver_sales(): ...\n```',
      '```\n@dp.table\n@dp.expect_or_drop(valid_products)\ndef silver_sales(): ...\n```',
      '```\n@dp.table\n@dp.expect(valid_products)\ndef silver_sales(): ...\n```',
    ],
    a: [1],
    x: 'expect_all aplica un diccionario de expectativas en grupo, escribe filas válidas e inválidas en el destino y captura métricas de violaciones. expect es para una expectativa individual, expect_or_drop descartaría filas y expect_or_fail detendría el pipeline (existen también expect_all_or_drop/expect_all_or_fail).',
  },
  {
    n: 44,
    d: 'GOV',
    m: 0,
    q: '"A mechanism to dynamically apply row filters and column masks using governed tags. It offers a single catalog-level definition that automatically applies to all current and future tables without per-table configuration."\n\nWhich Unity Catalog feature is being described?',
    o: [
      'Dynamic views',
      'Attribute-Based Access Control (ABAC) policies',
      'Predictive Optimization',
      'Liquid Clustering',
    ],
    a: [1],
    x: 'Las políticas ABAC usan tags gobernados para aplicar filtros de fila y máscaras de columna con una única definición a nivel de catálogo/esquema, cubriendo tablas actuales y futuras. Las vistas dinámicas requieren mantenimiento individual por tabla.',
  },
  {
    n: 45,
    d: 'P',
    m: 0,
    q: 'What is the primary benefit of using spot instances in Databricks clusters?',
    o: [
      'Increased security and compliance',
      'Guaranteed job execution time',
      'Reduced data storage latency',
      'Significantly lower compute costs',
    ],
    a: [3],
    x: 'Las instancias spot aprovechan capacidad cloud sobrante con descuentos de hasta el 90% frente a on-demand. A cambio, pueden terminarse inesperadamente, por lo que no convienen a cargas críticas o con SLA estrictos.',
  },
  {
    n: 46,
    d: 'TRA',
    m: 0,
    q: 'A supply chain team maintains an append-only streaming table inventory_raw (product_id, quantity, event_timestamp) in Lakeflow Declarative Pipelines. A new table, inventory_latest, must capture near real-time changes in product inventory levels (product_id, current_quantity, updated_timestamp).\n\nWhich type of object is most suitable to implement inventory_latest?',
    o: ['Streaming view', 'Temporary view', 'Streaming table', 'Materialized view'],
    a: [2],
    x: 'Una streaming table captura y aplica continuamente cambios (merge/CDC) desde una fuente append-only, actualizando el estado por product_id en tiempo casi real sin reconstruir la tabla. Las vistas materializadas son batch/programadas y las temporales, efímeras.',
  },
  {
    n: 47,
    d: 'TRO',
    m: 0,
    q: 'A team works with a user activity events table in Unity Catalog. Queries often involve filters on multiple columns like user_id and event_date.\n\nWhich data layout technique should the team implement to avoid expensive table scans?',
    o: [
      'Use Z-order indexing on the user_id',
      'Use liquid clustering on the combination of user_id and event_date',
      'Use partitioning on the user_id column, along with Z-order indexing on the event_date column.',
      'Use partitioning on the event_date column.',
    ],
    a: [1],
    x: 'Liquid clustering sobre ambas columnas optimiza incrementalmente el layout para filtros combinados, evitando escaneos completos. Particionar solo por fecha ignora user_id, Z-order solo por user_id ignora la fecha, y particionar por user_id crearía millones de particiones pequeñas.',
  },
  {
    n: 48,
    d: 'P',
    m: 0,
    q: "A data architect designs a hybrid data platform that must securely connect to on-premises databases, queried using SQL from within Databricks, and must remain within the enterprise's custom-defined network for compliance reasons.\n\nWhich compute type should the architect choose?",
    o: [
      'Serverless compute for jobs',
      'Serverless compute for notebooks',
      'Serverless SQL Warehouse',
      'Pro SQL Warehouse',
    ],
    a: [3],
    x: 'Los Pro SQL Warehouses soportan conectividad segura a redes personalizadas (VPC/VNet propias), incluidas arquitecturas híbridas con acceso privado a bases de datos on-premises. Los serverless corren en la red gestionada de Databricks y no operan en VPCs del cliente.',
  },
  {
    n: 49,
    d: 'GOV',
    m: 0,
    q: 'Using Catalog Explorer, a data engineer grants: USE CATALOG on catalog sales, USE SCHEMA on schema transactions, and SELECT on the table daily_revenue, all to the group retail-analysts.\n\nWhich of the following correctly describes the result of these actions?',
    o: [
      'The permissions configured through the Catalog Explorer UI will not take effect until the engineer executes the generated GRANT SQL commands manually.',
      'The retail-analysts group will be able to create new tables in the transactions schema because they were granted both USE CATALOG and USE SCHEMA.',
      'The retail-analysts group will be able to read data from daily_revenue using SQL warehouses, but will not be able to create, alter, or delete objects in the sales catalog or transactions schema.',
      'The retail-analysts group will be able to query all tables in the transactions schema because USE SCHEMA automatically grants SELECT on every table.',
    ],
    a: [2],
    x: 'La tríada USE CATALOG + USE SCHEMA + SELECT habilita exactamente la lectura de esa tabla. Los privilegios USE solo permiten atravesar contenedores: para crear tablas haría falta CREATE TABLE y para modificar, MODIFY u ownership. La UI aplica los permisos directamente.',
  },
  {
    n: 50,
    d: 'TRA',
    m: 0,
    q: 'A global airline stores flight booking events from multiple countries. Booking timestamps are recorded in local airport time zones.\n\nTo ensure accurate reporting and customer activity tracking, how should timestamps be standardized?',
    o: [
      'Store all timestamps as TIMESTAMP type in UTC',
      'Store all timestamps as STRING type for readability',
      'Keep timestamps in local time and apply timezone conversion only in BI tools',
      'Store timestamps without timezone context',
    ],
    a: [0],
    x: 'Guardar TIMESTAMP en UTC crea una única fuente de verdad: permite secuenciar eventos globales, es inmune al horario de verano y se indexa/ordena eficientemente. Los strings rompen la aritmética de fechas y la hora local sin contexto corrompe el análisis global.',
  },
  {
    n: 51,
    d: 'TRA',
    m: 0,
    q: 'Which of the following SQL commands will append a new row to the existing Delta table users?',
    o: [
      'APPEND INTO users VALUES ("0015", "Adam", 23)',
      'APPEND VALUES ("0015", "Adam", 23) INTO users',
      '`INSERT INTO users VALUES ("0015", "Adam", 23)`',
      '`INSERT VALUES ("0015", "Adam", 23) INTO users`',
    ],
    a: [2],
    x: 'INSERT INTO añade filas nuevas a una tabla Delta, especificadas por expresiones de valores o por el resultado de una consulta. APPEND INTO no existe en SQL.',
  },
  {
    n: 52,
    d: 'TRA',
    m: 0,
    q: 'A team builds a reusable product filtering query in Databricks SQL that must accept a runtime input value for product_id using a named parameter called "pid".\n\nWhich query correctly defines and uses the named parameter?',
    o: [
      '```\nSELECT * FROM products WHERE product_id = $pid;\n```',
      '```\nSELECT * FROM products WHERE product_id = ${var.pid};\n```',
      '```\nSELECT * FROM products WHERE product_id = pid;\n```',
      '```\nSELECT * FROM products WHERE product_id = :pid;\n```',
    ],
    a: [3],
    x: 'Los marcadores de parámetro con nombre en Databricks SQL usan dos puntos: :pid. Sin prefijo se interpretaría como columna, $ no es válido y ${var...} es sintaxis de variables de bundles (DABs), no de SQL.',
  },
  {
    n: 53,
    d: 'JOBS',
    m: 0,
    q: 'Which of the following describes Cron syntax in Databricks Jobs?',
    o: [
      "It's an expression to represent a complex job schedule that can be defined programmatically",
      "It's an expression to represent the maximum concurrent runs of a job",
      "It's an expression to represent the retry policy of a job",
      "It's an expression to represent the run timeout of a job",
    ],
    a: [0],
    x: 'La sintaxis Cron permite representar programaciones complejas de forma programática (alternativa al selector interactivo de periodo y hora de inicio). No tiene relación con concurrencia, reintentos ni timeouts.',
  },
  {
    n: 54,
    d: 'ING',
    m: 0,
    q: 'A healthcare company receives imaging metadata files every few seconds into cloud storage, which has accumulated thousands of archived JSON files over months. The team wants a streaming ingestion solution that processes all archived files exactly once, automatically detects new files in near real time, and minimizes storage API calls and listing overhead.\n\nWhich ingestion strategy is most appropriate?',
    o: [
      'Use COPY INTO initially to process the existing files, then use Auto Loader with file notification mode for new files.',
      'Use a CTAS statement initially to load the existing files in batch mode, then use Auto Loader to incrementally process new files.',
      'Use Auto Loader with file notification mode to enable event-based file detection for current and future files.',
      'Use Auto Loader in directory listing mode because archived files must always be discovered through full directory scans before streaming can begin.',
    ],
    a: [2],
    x: 'Auto Loader en modo file notification hace un backfill inicial de los archivos existentes (exactamente una vez, gracias al checkpoint) y luego detecta los nuevos vía eventos cloud (SNS/SQS, Event Grid, Pub/Sub), minimizando las costosas operaciones LIST. Combinar COPY INTO o CTAS añade complejidad y riesgo de huecos/duplicados.',
  },
  {
    n: 55,
    d: 'ING',
    m: 0,
    q: 'A data engineer develops a standard connector with Lakeflow Connect to ingest sales transactions into a Unity Catalog table nightly. They must retain historical data for 30 days while preventing duplicates across repeated loads.\n\nWhich command can the data engineer use to meet these requirements?',
    o: ['`MERGE INTO`', '`COPY INTO`', '`INSERT OVERWRITE`', '`INSERT INTO`'],
    a: [0],
    x: 'MERGE INTO (upsert) empareja origen y destino por una clave única: si existe, actualiza o ignora; si no, inserta. Evita duplicados en cargas repetidas y conserva intacto el histórico. INSERT INTO duplicaría e INSERT OVERWRITE borraría el histórico.',
  },
];

export const exam5: RawQuestion[] = [
  {
    n: 1,
    d: 'P',
    m: 0,
    q: 'A data engineering team currently runs nightly ETL pipelines on shared all-purpose compute used by analysts and engineers throughout the day.\n\nOver time, the team has observed:\n- resource contention with interactive notebook users\n- increased compute costs from idle resources\n- the ETL pipelines requiring specific runtime version and library setup for each scheduled run\n\nWhich approach would best address these issues around running ETL pipelines?',
    o: [
      'Run the ETL pipelines on single-node compute',
      'Use serverless compute for all workloads',
      'Use job compute for the scheduled ETL pipelines',
      'Increase the size of the all-purpose compute',
    ],
    a: [2],
    x: 'Job compute crea recursos dedicados para cada ejecución programada: aísla producción de los usuarios interactivos, elimina el coste de recursos ociosos (el clúster se termina al acabar el job) y permite fijar runtime y librerías consistentes por pipeline. Single-node no escala, serverless no cubre la configuración específica de runtime/librerías, y agrandar el all-purpose no elimina la contención ni el aislamiento.',
  },
  {
    n: 2,
    d: 'CICD',
    m: 0,
    q: 'A data engineering team manages multiple Lakeflow Jobs and Lakeflow Spark Declarative Pipelines across development, test, and production environments.\n\nThe engineering team wants to:\n- standardize deployments across environments\n- avoid manually recreating jobs and pipelines in each workspace\n- package deployment configurations together with workspace assets\n- support CI/CD-based deployment workflows\n\nWhich approach best meets these requirements?',
    o: [
      'Use Declarative Automation Bundles to package and deploy Lakeflow Jobs, Lakeflow Spark Declarative Pipelines, and related workspace assets across environments.',
      'Use Databricks Git Folders to synchronize Lakeflow Jobs and SDP pipelines across environments.',
      'Export DBC files for Lakeflow Jobs and Lakeflow Spark Declarative Pipelines and import them into each environment.',
      'Develop Lakeflow Jobs and Lakeflow Spark Declarative Pipelines in a common workspace and execute them from each environment.',
    ],
    a: [0],
    x: 'Los Declarative Automation Bundles (DABs) empaquetan jobs, pipelines SDP, notebooks y configuración de despliegue juntos, permitiendo despliegues consistentes y repetibles vía CI/CD y evitando drift entre entornos. Git Folders solo sincronizan código fuente, los DBC son manuales y difíciles de mantener, y un workspace común rompe el aislamiento entre entornos.',
  },
  {
    n: 3,
    d: 'ING',
    m: 0,
    q: 'A healthcare organization ingests semi-structured JSON records using Auto Loader.\n\nOver time:\n- new fields are added to incoming records\n- some existing fields occasionally stop appearing in incoming records\n- nested structures occasionally change\n- ingestion pipelines should continue processing without frequent manual schema updates\n\nWhich Auto Loader capability best supports these requirements?',
    o: ['Schema drift', 'mergeSchema', 'Schema inference', 'Schema evolution'],
    a: [3],
    x: 'Schema evolution permite que Auto Loader adapte el pipeline automáticamente cuando el esquema cambia con el tiempo (nuevos campos, estructuras anidadas que evolucionan) sin mantenimiento manual. Schema drift es el problema, no la capacidad; schema inference solo detecta el esquema inicial; mergeSchema aplica a escrituras en Delta, no a la ingesta con Auto Loader.',
  },
  {
    n: 4,
    d: 'GOV',
    m: 0,
    q: 'A data engineering team creates a Unity Catalog table using the following SQL statement:\n```\nCREATE TABLE finance.gold.monthly_sales\nAS\nSELECT * FROM finance.silver.sales;\n```\nLater, the team runs:\n```\nDROP TABLE finance.gold.monthly_sales;\n```\nWhich statement correctly describes what happens to the underlying data files?',
    o: [
      'The table is automatically converted into an external table before the underlying data files are removed.',
      'The underlying data files are immediately and permanently deleted from cloud storage when the table is dropped.',
      'The table metadata is removed, and the underlying data files are marked for deletion during managed maintenance operations after the recovery retention period.',
      'Only the table metadata is removed, while the underlying data files remain permanently in cloud storage.',
    ],
    a: [2],
    x: 'Sin cláusula LOCATION la tabla es managed. Al hacer DROP se elimina el metadato, pero la tabla puede recuperarse con UNDROP TABLE durante el periodo de retención; los ficheros se eliminan después mediante operaciones de mantenimiento gestionadas, no de forma inmediata. Que solo quede el metadato y los ficheros permanezcan es el comportamiento de las tablas external.',
  },
  {
    n: 5,
    d: 'P',
    m: 0,
    q: 'A retail company currently maintains separate platforms for BI reporting, real-time analytics, and machine learning workloads, leading to:\n- inconsistent business metrics\n- duplicated storage costs\n- separate governance processes across platforms\n\nLeadership wants to modernize the platform while continuing to store data in cloud object storage.\n\nWhich approach best meets these requirements?',
    o: [
      'Use a traditional enterprise data warehouse for reporting and a separate streaming platform for real-time analytics',
      'Implement a cloud-based data platform using Parquet tables in object storage with separate processing and governance workflows for different teams',
      'Implement a Lakehouse architecture using Delta Lake tables',
      'Maintain separate analytics and machine learning platforms while synchronizing data between systems using scheduled ETL pipelines',
    ],
    a: [2],
    x: 'La arquitectura Lakehouse con Delta Lake combina el almacenamiento de objetos en la nube con fiabilidad transaccional, gobernanza centralizada y soporte de BI, streaming y ML sobre los mismos datos, eliminando duplicados y métricas inconsistentes. Las demás opciones mantienen plataformas separadas o gobernanza fragmentada.',
  },
  {
    n: 6,
    d: 'P',
    m: 0,
    q: 'A data engineering team runs several workloads in Databricks with usage patterns that change throughout the day. Some workloads need compute resources quickly during peak periods, but there are also long idle periods when no processing is running.\n\nThe team wants to reduce operational overhead by avoiding manual cluster sizing, startup management, and idle compute management.\n\nWhich compute option is most appropriate?',
    o: ['All-purpose compute', 'Single-node compute', 'Job compute', 'Serverless compute'],
    a: [3],
    x: 'Serverless: Databricks gestiona la infraestructura por ti — arranque rápido, escalado según demanda y sin gestión manual de tamaño ni de recursos ociosos. All-purpose y job compute siguen exigiendo gestionar configuración y ciclo de vida; single-node no aporta elasticidad.',
  },
  {
    n: 7,
    d: 'GOV',
    m: 0,
    q: 'A data engineering team stores raw telemetry data in cloud object storage outside the managed storage location of Unity Catalog.\n\nThe engineering team wants to create a Unity Catalog table that:\n- references the existing data files in cloud storage\n- allows the data files to remain accessible even if the table is dropped\n- enables multiple external systems to continue using the same storage location\n\nWhich approach best meets these requirements?',
    o: [
      'Create a managed table',
      'Create an external table',
      'Create a temporary view referencing the cloud storage path.',
      'Create a managed Delta table and periodically export the files to external storage.',
    ],
    a: [1],
    x: 'Las tablas external (creadas con cláusula LOCATION) dejan los datos en el almacenamiento gestionado externamente: al hacer DROP solo se elimina el metadato y los ficheros quedan intactos y accesibles para otros sistemas. Con una managed, Databricks controla el ciclo de vida y puede acabar borrando los ficheros; una vista temporal no da metadatos persistentes gobernados.',
  },
  {
    n: 8,
    d: 'TRA',
    m: 0,
    q: 'A manufacturing company ingests IoT telemetry events into a Bronze table using PySpark.\n\nDue to intermittent network retries, some telemetry events are written multiple times into the Bronze layer.\n\nEach record contains: machine_id, event_timestamp, temperature.\n\nThe engineering team identifies that a telemetry event should be considered unique based on the combination of machine_id and event_timestamp.\n\nWhich transformation best removes duplicate telemetry events before writing to the Silver table?',
    o: [
      '```\nsilver_df = bronze_df.dropDuplicates()\n```',
      '```\nsilver_df = bronze_df.dropDuplicates(\n```\n    ["machine_id", "event_timestamp"]\n```\n)\n```',
      '```\nsilver_df = bronze_df.drop(\n```\n    "machine_id",\n    "event_timestamp"\n```\n)\n```',
      '```\nsilver_df = bronze_df.distinct(\n```\n    ["machine_id", "event_timestamp"]\n```\n)\n```',
    ],
    a: [1],
    x: 'dropDuplicates(["machine_id","event_timestamp"]) deduplica por la clave de negocio compuesta. Sin argumentos, dropDuplicates() solo elimina filas totalmente idénticas (dos eventos con distinta temperature seguirían duplicados); drop() elimina columnas, no filas; y distinct() no acepta columnas como argumento.',
  },
  {
    n: 9,
    d: 'JOBS',
    m: 0,
    q: "A data engineering team is building a Lakeflow Job that loads daily supplier inventory files into a Bronze table.\n\nThe ingestion task occasionally fails because the supplier's cloud storage service temporarily throttles requests during peak hours. In most cases, rerunning the task a few minutes later succeeds.\n\nThe engineering team wants the job to automatically recover from these temporary failures without requiring manual reruns.\n\nWhich approach best meets these requirements?",
    o: [
      'Create a master notebook that reruns failed notebooks sequentially using %run commands.',
      'Configure retries and retry intervals on the ingestion task.',
      'Increase the auto-scaling limits for the job cluster.',
      'Convert the ingestion process into a continuously running Lakeflow Spark Declarative Pipelines pipeline.',
    ],
    a: [1],
    x: 'Lakeflow Jobs soporta reintentos a nivel de tarea con número de retries e intervalo, ideal para fallos transitorios (throttling, cortes de red). El autoscaling no soluciona fallos externos, %run no tiene gestión nativa de reintentos y el modo continuo es para streaming, no para recuperación de fallos puntuales.',
  },
  {
    n: 10,
    d: 'CICD',
    m: 0,
    q: 'A data engineer modifies several notebooks inside a Databricks Git Folder connected to a shared Git repository.\n\nThe engineer can see all changes inside the Databricks workspace, but other team members cannot see the updates in the remote repository.\n\nWhich action most likely has not yet been completed?',
    o: [
      'The engineer has not yet switched to the main branch.',
      'The engineer has not yet created a pull request for the changes.',
      'The engineer has not yet pulled the latest changes from the remote repository.',
      'The engineer has not yet committed and pushed the changes to the remote repository.',
    ],
    a: [3],
    x: 'Los cambios en un Git Folder son locales hasta que se hace commit y push al repositorio remoto; solo entonces los ve el resto del equipo. El pull request se crea después del push, el pull trae cambios (no los publica) y cambiar de rama no publica nada.',
  },
  {
    n: 11,
    d: 'TRA',
    m: 0,
    q: 'A retail company is building a Silver sales dataset using PySpark.\n\nThe sales DataFrame contains all sales transactions plus a small lookup DataFrame containing product category mappings.\n\nThe engineering team notices that the join operation is causing excessive shuffle activity and increasing execution time. The lookup DataFrame contains only a few hundred rows and can fit easily in memory.\n\nWhich join strategy would best improve the performance of this workload?',
    o: ['Self join', 'Left Join', 'Cross join', 'Broadcast join'],
    a: [3],
    x: 'El broadcast join envía la tabla pequeña (broadcast(lookup_df)) a todos los executors, que hacen el join localmente sin shuffle de la tabla grande: menos tráfico de red y mejor rendimiento. Left/self/cross definen la semántica del join, no la estrategia de ejecución.',
  },
  {
    n: 12,
    d: 'GOV',
    m: 0,
    q: 'A healthcare organization stores patient records in a Unity Catalog table named patients.\n\nThe compliance team requires that:\n- members of the medical_staff group can see the full value of the diagnosis column\n- all other users should see the value REDACTED\n\nWhich function correctly implements this requirement?',
    o: [
      "```\nCREATE FUNCTION diagnosis_mask(diagnosis STRING)\nRETURN CASE\n```\n         WHEN is_account_group_member('medical_staff')\n           THEN diagnosis\n         ELSE 'REDACTED'\n```\n       END;\n```",
      "```\nCREATE FUNCTION diagnosis_mask(diagnosis STRING)\nRETURN CASE\n```\n         WHEN is_user_group_member() = 'medical_staff'\n           THEN 'REDACTED'\n         ELSE diagnosis\n```\n       END;\n```",
      "```\nCREATE FUNCTION diagnosis_mask(diagnosis STRING)\nRETURN CASE\n```\n         WHEN is_account_group_member('medical_staff')\n           THEN 'REDACTED'\n         ELSE diagnosis\n```\n       END;\n```",
      "```\nCREATE FUNCTION diagnosis_mask(diagnosis STRING)\nRETURN CASE\n```\n         WHEN is_user_group_member('medical_staff')\n           THEN diagnosis\n         ELSE 'REDACTED'\n```\n       END;\n```",
    ],
    a: [0],
    x: "La comprobación de grupo se hace con is_account_group_member('grupo'): si el usuario pertenece a medical_staff devuelve el diagnóstico real, si no, 'REDACTED'. is_user_group_member() no es una función válida en Databricks SQL, y las otras opciones invierten la lógica de enmascarado.",
  },
  {
    n: 13,
    d: 'JOBS',
    m: 0,
    q: 'A data engineering team is building a Lakeflow Job with the following tasks:\n- ingest_bronze_data\n- validate_silver_data\n- build_gold_reports\n- send_failure_notification\n\nThe engineering team wants:\n- build_gold_reports to execute if the validation task succeeds\n- send_failure_notification to execute if the validation task fails\n\nWhich approach best meets these requirements?',
    o: [
      'Configure an If/else task after validate_silver_data to control downstream branching.',
      'Configure a Switch task after validate_silver_data to control downstream branching.',
      'Configure a Case when task after validate_silver_data to control downstream branching.',
      'Configure sequential notebook tasks and use notebook-level if statements to control downstream branching.',
    ],
    a: [0],
    x: 'Lakeflow Jobs soporta tareas If/else para bifurcar el flujo según resultados o condiciones (validaciones, éxito/fallo). "Switch" y "Case when" no existen como tipos de tarea, y esconder la lógica en notebooks pierde observabilidad y mantenibilidad del DAG.',
  },
  {
    n: 14,
    d: 'P',
    m: 0,
    q: 'A data engineer adds new environment variables to a Databricks cluster using init scripts to support connectivity to an external storage system.\n\nAfter saving the changes and running a notebook, the new environment variables are not available.\n\nWhat should the engineer do to ensure the updated environment variables are applied?',
    o: [
      'Detach and reattach the notebook to the cluster.',
      'Create a new notebook session.',
      'Re-run all notebook cells from the beginning',
      'Restart the cluster',
    ],
    a: [3],
    x: 'Las variables de entorno, init scripts y librerías se aplican durante el arranque del clúster: hay que reiniciarlo para que el runtime se recree con la nueva configuración. Reatachar el notebook, abrir otra sesión o re-ejecutar celdas siguen usando el entorno antiguo.',
  },
  {
    n: 15,
    d: 'TRO',
    m: 0,
    q: 'A data engineering team wants to reduce the manual operational effort required to maintain performant Unity Catalog managed Delta tables.\n\nThe engineering team wants optimization operations such as file compaction, statistics collection, and cleanup of unused data files to run automatically.\n\nWhich Databricks feature best meets these requirements?',
    o: ['Predictive Optimization', 'Liquid Clustering', '`OPTIMIZE`', 'Adaptive Query Execution'],
    a: [0],
    x: 'Predictive Optimization ejecuta automáticamente OPTIMIZE, VACUUM y ANALYZE sobre tablas managed de Unity Catalog, eliminando el mantenimiento manual. Liquid Clustering gestiona el layout, no el mantenimiento; OPTIMIZE es un comando manual; AQE optimiza queries en tiempo de ejecución, no tablas.',
  },
  {
    n: 16,
    d: 'P',
    m: 0,
    q: 'A financial services company continuously ingests transactions into a Delta table from multiple concurrent pipelines.\n\nThe engineering team wants to ensure:\n- failed writes never leave partially updated data\n- concurrent readers always see consistent results\n- committed changes remain durable after failures\n\nWhich Delta Lake capability directly provides these guarantees?',
    o: ['Liquid Clustering', 'Predictive Optimization', 'ACID transactions', 'Delta Caching'],
    a: [2],
    x: 'Las transacciones ACID de Delta Lake garantizan atomicidad (nada de escrituras parciales visibles), aislamiento para lectores concurrentes y durabilidad de los commits tras fallos. Clustering, Predictive Optimization y caching mejoran rendimiento/mantenimiento, no consistencia transaccional.',
  },
  {
    n: 17,
    d: 'TRO',
    m: 0,
    q: 'A data engineering team notices that a Lakeflow Job that previously completed in 18 minutes is now consistently taking more than 40 minutes to finish.\n\nThe engineering team wants to identify when the performance degradation started by comparing recent executions against historical job runs.\n\nWhich approach best meets this requirement?',
    o: [
      'Use the Lakeflow Jobs Runs tab to compare current and historical job execution metrics.',
      'Use the event history of the job compute to compare cluster performance over time.',
      'Export the Lakeflow Job as a DBC file and compare it with previous exports.',
      'Use the Spark UI to compare the performance of the Lakeflow Job.',
    ],
    a: [0],
    x: 'La pestaña Runs de Lakeflow Jobs muestra el histórico de ejecuciones (duración, estado, detalle por tarea), ideal para detectar tendencias y ubicar cuándo empezó la degradación. El event history del clúster es de infraestructura, el DBC no contiene métricas y la Spark UI analiza una ejecución concreta, no tendencias históricas.',
  },
  {
    n: 18,
    d: 'TRO',
    m: 0,
    q: 'A data engineering team is monitoring a Lakeflow Job that contains multiple dependent tasks.\n\nThe engineering team notices that the job is still running and only some of the output tables are refreshed. The team wants to identify whether an upstream task failure or delay is blocking downstream execution.\n\nWhich approach best meets this requirement?',
    o: [
      'Use the Spark UI to identify which Spark stages are preventing downstream Lakeflow Job tasks from starting.',
      'Use the Lakeflow Jobs Runs tab to compare historical task execution durations across previous job runs.',
      'Use the cluster metrics dashboard to determine whether cluster resource utilization is delaying table refreshes.',
      'Use the Lakeflow Jobs DAG view to inspect task dependencies and identify upstream blockers affecting downstream tasks.',
    ],
    a: [3],
    x: 'La vista DAG de Lakeflow Jobs visualiza dependencias y estados de las tareas en la ejecución activa, mostrando qué tarea upstream (en curso, fallida o con condición no cumplida) bloquea a las de abajo. La Spark UI y las métricas de clúster no muestran dependencias de orquestación, y el Runs tab compara históricos, no bloqueos actuales.',
  },
  {
    n: 19,
    d: 'ING',
    m: 0,
    q: 'A retail company ingests daily product and pricing files into a Bronze layer Delta table using cloud object storage as the landing zone. The ingestion process is fully automated and runs several times a day as new files arrive from multiple regional systems.\n\nDuring an auditing investigation, the team was asked to identify from the Bronze table:\n- which source file produced specific records\n- when individual records were ingested\n- which regional system produced specific records\n\nThe team wants to improve ingestion traceability without redesigning the architecture.\n\nWhich implementation best supports these requirements?',
    o: [
      'Create a separate Bronze lookup table containing file path, ingestion timestamp, and region information during ingestion',
      'Enable Lakeflow Jobs to capture file path, ingestion timestamp, and region information',
      'Enable Delta table history tracking on the Bronze table to capture file path, ingestion timestamp, and region information',
      'Capture source metadata columns such as file path, ingestion timestamp, and region during Bronze table ingestion',
    ],
    a: [3],
    x: 'Guardar columnas de metadatos (ruta del fichero, timestamp de ingesta, región/sistema origen) junto a cada registro Bronze da trazabilidad a nivel de fila para auditorías. El historial de Delta es a nivel de operación de tabla, no de registro; Lakeflow Jobs orquesta pero no persiste metadatos por fila; y una tabla lookup separada añade complejidad y joins innecesarios.',
  },
  {
    n: 20,
    d: 'P',
    m: 0,
    q: 'A security-conscious enterprise is evaluating Databricks and wants to ensure that all customer data, including file reads/writes and notebook execution, remains within their own cloud environment. However, they are okay with Databricks managing the platform services like the web UI and job scheduler.\n\nWhich aspect of the Databricks architecture makes this setup possible?',
    o: [
      'Jobs run on ephemeral clusters with encrypted secrets stored in the control plane.',
      "The control plane is hosted by Databricks, while the data plane runs in the customer's cloud account.",
      'Unity Catalog separates secure and insecure clusters using data tags.',
      'Databricks SQL provides serverless access to files across multiple regions.',
    ],
    a: [1],
    x: 'La arquitectura de Databricks separa el control plane (UI, orquestación, APIs — gestionado por Databricks) del data plane (ejecución de notebooks y acceso a datos — en la cuenta cloud del cliente). Así los datos permanecen en el entorno del cliente mientras Databricks gestiona los servicios de plataforma.',
  },
  {
    n: 21,
    d: 'P',
    m: 0,
    q: 'A company wants to store data in cloud object storage while still supporting:\n- transactional reliability\n- schema enforcement\n- support for both batch and streaming workloads\n- the ability to access historical versions of data\n\nWhich technology provides these capabilities?',
    o: ['Unity Catalog', 'Parquet', 'Delta Lake', 'Delta Live Tables'],
    a: [2],
    x: 'Delta Lake añade sobre el object storage transacciones ACID, schema enforcement, soporte batch+streaming y versionado histórico con Time Travel. Parquet es solo un formato de fichero sin transacciones; Unity Catalog es gobernanza; DLT/SDP es el framework de pipelines, no la capa de almacenamiento transaccional.',
  },
  {
    n: 22,
    d: 'JOBS',
    m: 0,
    q: 'A data engineering team is building a Lakeflow Job with the following tasks: ingest_customers, ingest_orders, build_silver_customers, build_silver_orders, build_gold_sales.\n\nThe engineering team wants:\n- ingest_customers and ingest_orders to run in parallel\n- each Silver task to begin only after its corresponding ingestion task completes successfully\n- build_gold_sales to execute only after both Silver tasks complete successfully\n\nWhich approach best meets these requirements?',
    o: [
      'Configure build_silver_customers to depend on ingest_customers, configure build_silver_orders to depend on ingest_orders, and configure build_gold_sales to depend on both Silver tasks.',
      'Configure all tasks sequentially in the following order: ingest_customers → ingest_orders → build_silver_customers → build_silver_orders → build_gold_sales',
      'Create a master notebook that runs the ingestion and transformation notebooks sequentially using %run commands, then run the master notebook using a lakeflow job.',
      'Configure both Silver tasks to depend on both ingestion tasks, then configure build_gold_sales to depend on both Silver tasks.',
    ],
    a: [0],
    x: 'El DAG óptimo maximiza el paralelismo y evita dependencias innecesarias: cada Silver depende solo de su ingesta correspondiente y el Gold de ambas Silver. La secuencia total pierde paralelismo, %run pierde observabilidad y retries, y hacer que cada Silver espere a ambas ingestas añade esperas innecesarias.',
  },
  {
    n: 23,
    d: 'GOV',
    m: 0,
    q: 'A data engineering team manages a Unity Catalog table named finance.gold.customers.\n\nAnalysts query the table directly using the three-level namespace:\n```\nSELECT customer_id, customer_name, ssn\nFROM finance.gold.customers;\n```\nThe team wants users in the finance department to see the real SSN values, while users outside the finance department should see masked values automatically when querying the same table.\n\nWhich Unity Catalog feature best meets this requirement?',
    o: ['Adaptive Query Execution', 'Row filters', 'Column masks', 'Dynamic views'],
    a: [2],
    x: 'Las column masks se aplican directamente sobre la tabla y enmascaran valores sensibles en tiempo de consulta según el usuario/grupo, sin necesidad de que los usuarios cambien a una vista. Los row filters restringen filas (no valores de columna), las dynamic views obligarían a consultar la vista en lugar de la tabla, y AQE es optimización de Spark.',
  },
  {
    n: 24,
    d: 'TRA',
    m: 0,
    q: 'A data engineering team is building a Silver events table from a Bronze events table using Lakeflow Spark Declarative Pipelines.\n\nNew event records continuously arrive in the Bronze table throughout the day. The team wants the Silver table to:\n- process newly arriving records incrementally\n- remain queryable by downstream analytics teams\n- be managed by Lakeflow Spark Declarative Pipelines\n- integrate with Unity Catalog governance\n\nWhich type of table best meets these requirements?',
    o: ['Managed table', 'Streaming table', 'External table', 'Materialized view'],
    a: [1],
    x: 'Las streaming tables procesan incrementalmente los datos que van llegando y siguen siendo consultables como tablas normales; son el objeto típico de las capas Bronze/Silver en SDP. Managed/external definen dónde se almacena, no cómo se procesa, y las materialized views sirven agregaciones precalculadas (Gold), no streaming continuo.',
  },
  {
    n: 25,
    d: 'CICD',
    m: 0,
    q: 'A data engineering team is developing a new Lakeflow Spark Declarative Pipelines enhancement inside a Databricks Git Folder connected to a shared Git repository.\n\nThe engineer responsible for the enhancement wants to:\n- experiment with pipeline changes safely\n- avoid exposing unfinished code to the rest of the team\n- continue collaborating through the shared repository without exposing unfinished changes\n\nWhich approach best meets these requirements?',
    o: [
      'Create and switch to a feature branch inside the Databricks Git Folder before continuing development.',
      'Continue developing directly on the shared main branch and commit changes incrementally.',
      'Disconnect the Databricks Git Folder from the remote repository while developing the enhancement.',
      'Create a second copy of the project inside the workspace and manually merge notebook changes later.',
    ],
    a: [0],
    x: 'Una feature branch aísla el trabajo del main compartido y mantiene la colaboración vía Git: desarrollar → commit/push → pull request → merge tras revisión. Trabajar sobre main expone código incompleto, desconectar el repo elimina el control de versiones y duplicar el proyecto genera merges manuales propensos a errores.',
  },
  {
    n: 26,
    d: 'CICD',
    m: 0,
    q: 'A data engineering team uses the same Declarative Automation Bundle to deploy a Lakeflow Job across development, test, and production environments.\n\nThe engineering team wants:\n- the same codebase to be reused across all environments\n- different catalog names to be used in each environment\n- different cluster sizes to be used in production compared to development\n\nWhich approach best meets these requirements?',
    o: [
      'Use bundle variables and target-specific overrides for environment-specific configuration values.',
      'Create separate Git repositories for development, test, and production deployments.',
      'Store environment-specific values directly inside notebooks and update them before deployment.',
      'Create separate copies of the bundle configuration for each environment and maintain them independently.',
    ],
    a: [0],
    x: 'Los DABs soportan variables y overrides por target, de modo que un mismo codebase se despliega en dev/test/prod personalizando catálogos, clústeres o schedules por entorno. Repos separados, valores hardcodeados en notebooks o copias del bundle generan duplicación y drift de configuración.',
  },
  {
    n: 27,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to create a SQL user-defined function (UDF) in Databricks that:\n- accepts a first name and last name\n- capitalizes each value\n- returns the formatted full name\n\nWhich statement correctly creates this function?',
    o: [
      '```\nCREATE FUNCTION format_name(fname STRING, lname STRING)\nRETURNS STRING\nAS \'SELECT UPPER(fname) || " " || UPPER(lname)\';\n```',
      "```\nCREATE OR REPLACE FUNCTION format_name(fname, lname)\nRETURN INITCAP(fname) || ' ' || INITCAP(lname);\n```",
      "```\nCREATE OR REPLACE FUNCTION format_name(fname STRING, lname STRING)\nRETURNS STRING\nRETURN INITCAP(fname) || ' ' || INITCAP(lname);\n```",
      "```\nCREATE FUNCTION format_name(fname STRING, lname STRING)\nRETURNS STRING\nUSING 'INITCAP(fname) || INITCAP(lname)';\n```",
    ],
    a: [2],
    x: "La sintaxis correcta de una SQL UDF en Databricks exige: parámetros con tipo, tipo de retorno con RETURNS y la expresión con RETURN. INITCAP capitaliza cada valor y || concatena. Las variantes con AS 'SELECT...', sin tipos en los parámetros o con USING no son sintaxis válidas.",
  },
  {
    n: 28,
    d: 'JOBS',
    m: 0,
    q: 'A data engineering team is building a Lakeflow Job to process Formula 1 race result files uploaded to a Unity Catalog Volume.\n\nNew race result files are uploaded after a race completes during race weekends. Some weekends contain races, while other weekends do not. Occasionally, sprint race and main race result files may arrive multiple times during the same weekend.\n\nThe engineering team wants:\n- the job to start when new files arrive\n- avoid unnecessary job runs when no new files are available\n- minimize unnecessary compute usage\n\nWhich approach best meets these requirements?',
    o: [
      'Configure the Lakeflow Job using a scheduled weekly trigger and check at the beginning of every notebook whether new files exist before processing.',
      'Configure the Lakeflow Job using a file arrival trigger on the Unity Catalog Volume.',
      'Configure the Lakeflow Job to run on demand only through manual execution after each race weekend.',
      'Configure the Lakeflow Job using a scheduled weekly trigger and a dedicated task that checks whether new files exist before continuing the job.',
    ],
    a: [1],
    x: 'El file arrival trigger es event-driven: arranca el job solo cuando llegan ficheros nuevos al Volume, evitando ejecuciones vacías y minimizando cómputo cuando los datos llegan de forma impredecible. Los triggers programados ejecutan el job aunque no haya datos, y la ejecución manual requiere intervención.',
  },
  {
    n: 29,
    d: 'ING',
    m: 0,
    q: 'A data engineer is working with a column containing raw JSON strings and wants to infer the schema from one of the JSON records before parsing the data.\n\nWhich Spark SQL function should the engineer use?',
    o: ['`get_json_schema()`', '`infer_json_schema()`', '`json_schema_extract()`', '`schema_of_json()`'],
    a: [3],
    x: 'schema_of_json(json_string) infiere el esquema de una cadena JSON y lo devuelve en formato struct<...>, que luego se usa con from_json() para parsear los datos. Las otras tres funciones no existen en Spark SQL.',
  },
  {
    n: 30,
    d: 'TRA',
    m: 0,
    q: "A data engineering team is building a Silver customer table using Lakeflow Spark Declarative Pipelines.\n\nThe Bronze customer table contains some records where customer_id is NULL. These records should be treated as data quality issues and dropped from the Silver table, while still being tracked by the pipeline's data quality metrics.\n\nWhich code snippet best meets this requirement?",
    o: [
      '```\n@dp.table\n@dp.expect_or_fail("valid_customer_id", "customer_id IS NOT NULL")\ndef silver_customers():\n    return spark.read.table("formula1.bronze.customers")\n```',
      '```\n@dp.table\n@dp.expect("valid_customer_id", "customer_id IS NOT NULL")\ndef silver_customers():\n    return spark.read.table("formula1.bronze.customers")\n```',
      '```\n@dp.table\n@dp.expect_or_drop("valid_customer_id", "customer_id IS NOT NULL")\ndef silver_customers():\n    return spark.read.table("formula1.bronze.customers")\n```',
      '```\n@dp.table\n@dp.expect_or_drop("valid_customer_id", "customer_id IS NULL")\ndef silver_customers():\n    return spark.read.table("formula1.bronze.customers")\n```',
    ],
    a: [2],
    x: '@dp.expect_or_drop registra la métrica de calidad Y elimina los registros que incumplen la condición (customer_id IS NOT NULL). expect_or_fail pararía el pipeline, expect solo registra métricas sin filtrar, y la última opción invierte la condición (mantendría los NULL y borraría los válidos).',
  },
  {
    n: 31,
    d: 'TRO',
    m: 0,
    q: 'A data engineering team is investigating a slow Spark workload in Databricks.\n\nWhile reviewing the Spark UI, the engineering team notices that:\n- one task in a stage is taking significantly longer than all other tasks\n- most executors finish quickly while one executor continues processing for an extended period\n- shuffle read sizes are highly uneven across tasks\n\nWhich issue is the most likely cause of this behavior?',
    o: [
      'Excessive cluster startup time delaying task scheduling.',
      'Predictive optimization creating too many small files during execution.',
      'Data skew causing uneven partition distribution across executors.',
      'Disk spilling caused by insufficient executor memory.',
    ],
    a: [2],
    x: 'Los síntomas clásicos de data skew: una tarea mucho más larga que el resto, shuffle reads desiguales y executors terminando a tiempos muy distintos, típico en joins y agregaciones con particiones desbalanceadas. El spilling se manifiesta con métricas de spill y disco en muchas tareas, no en una sola.',
  },
  {
    n: 32,
    d: 'ING',
    m: 0,
    q: 'A company wants to ingest ServiceNow data into Unity Catalog governed Delta tables with:\n- minimal custom coding\n- managed ingestion infrastructure\n- centralized governance\n- reliable scheduling support\n\nWhich ingestion approach best meets these requirements?',
    o: [
      'Use COPY INTO from the SaaS application',
      'Use REST API based ingestion',
      'Use Lakeflow Connect managed SaaS connectors',
      'Use Auto Loader for SaaS ingestion',
    ],
    a: [2],
    x: 'Los conectores SaaS gestionados de Lakeflow Connect ingieren datos de aplicaciones como ServiceNow con infraestructura gestionada, mínimo código y gobernanza en Unity Catalog. COPY INTO y Auto Loader son para ficheros en object storage, y la ingesta por REST API exige mucho desarrollo y operación a medida.',
  },
  {
    n: 33,
    d: 'TRA',
    m: 0,
    q: 'A data engineering team is combining historical sales data with newly arrived sales data using PySpark before writing the results into a Silver table.\n\nBoth DataFrames contain the same columns, but the column order is not consistent:\n\nHistorical: ["customer_id", "product_id", "quantity"]\nCurrent: ["product_id", "quantity", "customer_id"]\n\nOnce combined, the team notices that some values are appearing under incorrect columns in the Silver table.\n\nWhich approach best resolves this issue?',
    o: [
      '```\ncombined_df = historical_df.union(current_df)\n```',
      '```\ncombined_df = historical_df.unionByColumn(current_df)\n```',
      '```\ncombined_df = historical_df.unionAll(current_df)\n```',
      '```\ncombined_df = historical_df.unionByName(current_df)\n```',
    ],
    a: [3],
    x: 'union() y unionAll() combinan por POSICIÓN de columna, por lo que un orden distinto mezcla los valores. unionByName() empareja por NOMBRE de columna, evitando el problema. unionByColumn() no existe en PySpark.',
  },
  {
    n: 34,
    d: 'TRA',
    m: 0,
    q: 'A data engineering team is building a Silver customer table from a Bronze dataset using PySpark.\n\nThe Bronze table contains a signup_date column stored as a string in the format yyyy-MM-dd. The analytics team requires this column to be stored as a DATE type in the Silver table.\n\nWhich transformation best meets this requirement?',
    o: [
      '```\nsilver_df = bronze_df.withColumnRenamed(\n```\n    "signup_date",\n```\n    to_date(col("signup_date"), "yyyy-MM-dd")\n)\n```',
      '```\nsilver_df = bronze_df.withColumn(\n```\n    "signup_date",\n```\n    to_date(col("signup_date"), "yyyy-MM-dd")\n)\n```',
      '```\nsilver_df = bronze_df.withColumn(\n```\n    "signup_date",\n```\n    col("signup_date").cast("timestamp")\n)\n```',
      '```\nsilver_df = bronze_df.withColumn(\n```\n    "signup_date",\n```\n    col("signup_date").format("yyyy-MM-dd")\n)\n```',
    ],
    a: [1],
    x: 'withColumn + to_date(col, "yyyy-MM-dd") convierte el string a tipo DATE con el formato de origen explícito. withColumnRenamed solo renombra, cast("timestamp") produce TIMESTAMP (no DATE) y .format() no existe en objetos Column.',
  },
  {
    n: 35,
    d: 'TRO',
    m: 0,
    q: 'A Delta table receives frequent micro-batch writes from a streaming pipeline.\n\nOver time, the table accumulates a large number of very small Parquet files, and query performance begins to degrade due to excessive file scanning overhead.\n\nWhich command should the data engineer use to improve query performance by compacting small files into larger ones?',
    o: ['`VACUUM`', '`MERGE INTO`', '`OPTIMIZE`', '`ANALYZE TABLE`'],
    a: [2],
    x: 'OPTIMIZE compacta muchos ficheros pequeños en pocos grandes, reduciendo el overhead de escaneo (el clásico "small file problem" del streaming). VACUUM borra ficheros obsoletos no referenciados, MERGE hace upserts y ANALYZE actualiza estadísticas — ninguno compacta.',
  },
  {
    n: 36,
    d: 'ING',
    m: 0,
    q: 'A data engineering team is building an Auto Loader pipeline to ingest continuously arriving JSON files from cloud object storage into a Bronze Delta table.\n\nThe team wants the pipeline to:\n- read JSON files incrementally as they arrive\n- use Auto Loader rather than a standard file source\n- track schema information in a dedicated schema location\n\nWhich code snippet best meets these requirements?',
    o: [
      '```\ndf = (\n    spark.readStream\n        .format("cloudFiles")\n        .option("format", "json")\n        .option("schemaLocation", "/Volumes/formula1/checkpoints/orders_schema")\n        .load("/Volumes/formula1/landing/orders")\n)\n```',
      '```\ndf = (\n    spark.readStream\n        .format("cloudFiles")\n        .option("cloudFiles.format", "json")\n        .option("cloudFiles.schemaLocation", "/Volumes/formula1/checkpoints/orders_schema")\n        .load("/Volumes/formula1/landing/orders")\n)\n```',
      '```\ndf = (\n    spark.read\n        .format("cloudFiles")\n        .option("cloudFiles.format", "json")\n        .option("cloudFiles.schemaLocation", "/Volumes/formula1/checkpoints/orders_schema")\n        .load("/Volumes/formula1/landing/orders")\n)\n```',
      '```\ndf = (\n    spark.readStream\n        .format("json")\n        .option("cloudFiles.schemaLocation", "/Volumes/formula1/checkpoints/orders_schema")\n        .load("/Volumes/formula1/landing/orders")\n)\n```',
    ],
    a: [1],
    x: 'Auto Loader = spark.readStream + .format("cloudFiles") + opciones con prefijo cloudFiles. (cloudFiles.format, cloudFiles.schemaLocation). Sin el prefijo las opciones no son válidas, spark.read no es streaming incremental, y format("json") usa la fuente estándar en lugar de Auto Loader.',
  },
  {
    n: 37,
    d: 'ING',
    m: 0,
    q: 'A global retail company needs to continuously ingest changes from a SQL Server operational database into Unity Catalog governed Delta tables in Databricks.\n\nThe ingestion solution must:\n- capture inserts, updates, and deletes incrementally\n- avoid repeatedly reloading unchanged data\n- minimize custom ingestion code\n- support reliable enterprise ingestion at scale\n\nWhich approach best meets these requirements?',
    o: [
      'Use COPY INTO with scheduled database exports',
      'Use nightly JDBC ingestion jobs',
      'Use Auto Loader with JDBC ingestion from the database',
      'Use Lakeflow Connect database connector',
    ],
    a: [3],
    x: 'Los conectores de base de datos de Lakeflow Connect están diseñados para ingesta CDC gestionada desde bases operacionales: capturan inserts/updates/deletes incrementalmente sin recargar datos y con mínimo código. JDBC nocturno requiere lógica incremental a medida, y COPY INTO / Auto Loader son para ficheros, no para bases de datos.',
  },
  {
    n: 38,
    d: 'P',
    m: 0,
    q: 'A global company uses multiple Databricks workspaces across different business units.\n\nThe company currently manages data access separately within each workspace, resulting in:\n- inconsistent governance policies\n- duplicated permission management\n- limited visibility into data lineage across teams\n\nLeadership wants a centralized governance solution while continuing to store data in cloud object storage.\n\nWhich Databricks component best addresses these requirements?',
    o: ['Unity Catalog', 'Delta Lake', 'Hive metastore', 'Delta Live Tables'],
    a: [0],
    x: 'Unity Catalog es la capa de gobernanza centralizada: permisos consistentes, lineage, auditoría y control de acceso a través de múltiples workspaces. Delta Lake da fiabilidad transaccional (no gobernanza), el Hive metastore es el enfoque legacy sin estas capacidades, y DLT/SDP es un framework de pipelines.',
  },
  {
    n: 39,
    d: 'ING',
    m: 0,
    q: 'A data engineer is building a Spark Structured Streaming pipeline to process IoT sensor data.\n\nThe engineering team wants the pipeline to:\n- process new data every 60 seconds\n- use a micro-batch processing model\n- continue running continuously as new data arrives\n\nWhich trigger option should the data engineer use?',
    o: [
      '`Trigger.AvailableNow()`',
      '`Trigger.Continuous("60 seconds")`',
      '`Trigger.ProcessingTime("1 minute")`',
      '`Trigger.Once()`',
    ],
    a: [2],
    x: 'Trigger.ProcessingTime("1 minute") ejecuta micro-batches recurrentes cada 60 segundos de forma continua. Trigger.Continuous usa el modo de procesamiento continuo (no micro-batch), y Trigger.Once() / Trigger.AvailableNow() procesan lo disponible y terminan.',
  },
  {
    n: 40,
    d: 'GOV',
    m: 0,
    q: "A healthcare organization has already created the following column masking function:\n```\nCREATE FUNCTION diagnosis_mask(diagnosis STRING)\nRETURN CASE\n```\n         WHEN is_account_group_member('medical_staff')\n           THEN diagnosis\n         ELSE 'REDACTED'\n```\n       END;\n```\nThe data engineer now wants to apply this mask to the diagnosis column in the patients table.\n\nWhich statement should the engineer use?",
    o: [
      '```\nALTER TABLE patients\nALTER COLUMN diagnosis\nSET MASK diagnosis_mask;\n```',
      '```\nALTER TABLE patients\n```\nMODIFY COLUMN diagnosis\n```\nSET MASK diagnosis_mask;\n```',
      '```\nALTER TABLE patients\nALTER COLUMN diagnosis\nSET DEFAULT diagnosis_mask;\n```',
      '```\nALTER TABLE patients\nSET MASK diagnosis_mask ON (diagnosis);\n```',
    ],
    a: [0],
    x: 'La sintaxis para adjuntar una función de enmascarado a una columna en Unity Catalog es ALTER TABLE ... ALTER COLUMN ... SET MASK función. MODIFY COLUMN y SET MASK ... ON () no son sintaxis válidas, y SET DEFAULT define un valor por defecto, no una máscara.',
  },
  {
    n: 41,
    d: 'TRA',
    m: 0,
    q: 'A data engineering team is building a Gold analytics dataset using Lakeflow Spark Declarative Pipelines.\n\nThe analytics team requires:\n- precomputed revenue aggregations by product category\n- improved dashboard query performance\n- automatic refreshes when upstream Silver tables change\n- integration with Unity Catalog governance\n\nWhich dataset type best meets these requirements?',
    o: ['Global temporary view', 'Streaming table', 'View', 'Materialized view'],
    a: [3],
    x: 'Las materialized views almacenan físicamente el resultado precalculado y SDP las refresca automáticamente cuando cambian los datos upstream — ideales para agregaciones Gold que sirven dashboards. Una vista estándar recalcula en cada consulta, la streaming table es para procesamiento incremental continuo y la vista temporal global es de sesión.',
  },
  {
    n: 42,
    d: 'TRA',
    m: 0,
    q: 'A manufacturing company ingests IoT machine telemetry data into a Bronze table using PySpark.\n\nEach record contains a machine_id, an event_timestamp, and an array column named sensor_readings. For example:\n\n{\n  "machine_id": "M101",\n  "event_timestamp": "2026-05-10T10:15:00",\n  "sensor_readings": [12.5, 13.1, 11.8]\n```\n}\n```\nThe analytics team requires one row per sensor reading for downstream aggregation of sensor metrics.\n\nWhich transformation best meets these requirements?',
    o: [
      '```\nsilver_df = bronze_df.select(\n```\n    "machine_id",\n    "event_timestamp",\n```\n    col_to_rows(col("sensor_readings")).alias("sensor_reading")\n)\n```',
      '```\nsilver_df = bronze_df.select(\n```\n    "machine_id",\n    "event_timestamp",\n```\n    split(col("sensor_readings"), ",").alias("sensor_reading")\n)\n```',
      '```\nsilver_df = bronze_df.select(\n```\n    "machine_id",\n    "event_timestamp",\n```\n    explode(col("sensor_readings")).alias("sensor_reading")\n)\n```',
      '```\nsilver_df = bronze_df.select(\n```\n    "machine_id",\n    "event_timestamp",\n```\n    flatten(col("sensor_readings")).alias("sensor_reading")\n)\n```',
    ],
    a: [2],
    x: 'explode() genera una fila por cada elemento del array — exactamente lo que se pide. split() divide strings en arrays (aquí ya es un array), flatten() aplana arrays anidados pero devuelve un array (no filas), y col_to_rows() no existe.',
  },
  {
    n: 43,
    d: 'ING',
    m: 0,
    q: 'A company receives thousands of JSON files every hour in cloud object storage, and the source folder is expected to grow to millions of files over time.\n\nThe ingestion pipeline must:\n- detect newly arrived files incrementally\n- avoid manual tracking of processed files\n- scale efficiently as file volume grows\n- land data into Unity Catalog governed Delta tables\n\nWhich ingestion approach is most appropriate?',
    o: ['Auto Loader', 'JDBC ingestion', '`COPY INTO`', 'Use external tables'],
    a: [0],
    x: 'Auto Loader está diseñado para ingesta incremental masiva de ficheros desde object storage: descubrimiento automático de ficheros nuevos, escalado a millones de ficheros y sin tracking manual. COPY INTO es más batch y menos eficiente a esa escala, JDBC es para bases de datos y las external tables no ingieren incrementalmente.',
  },
  {
    n: 44,
    d: 'P',
    m: 0,
    q: 'A business intelligence team frequently leaves SQL warehouses running after dashboard usage ends for the day, resulting in unnecessary compute costs during idle periods.\n\nWhich configuration change would most directly help reduce these costs?',
    o: ['Enable Auto Scaling', 'Enable Auto Stop', 'Enable Photon', 'Enable Auto Termination'],
    a: [1],
    x: 'Auto Stop detiene automáticamente los SQL warehouses tras un periodo de inactividad, eliminando el coste ocioso. Ojo a la terminología: Auto Termination es el equivalente para clústeres (all-purpose), no para SQL warehouses. Auto Scaling ajusta recursos según demanda y Photon acelera queries, pero ninguno apaga el warehouse.',
  },
  {
    n: 45,
    d: 'GOV',
    m: 0,
    q: 'A data engineering team creates the following objects in Unity Catalog: catalog finance, schema reporting, table monthly_revenue.\n\nA data analyst is granted the following privilege:\n```\nGRANT SELECT\nON SCHEMA finance.reporting\nTO analysts;\n```\nThe analyst attempts to query the finance.reporting.monthly_revenue table but receives an access error.\n\nWhich additional privilege is required for the analyst to successfully query the table?',
    o: [
      'MODIFY on the monthly_revenue table',
      '`USE CATALOG on the finance catalog`',
      '`SELECT on the monthly_revenue table`',
      'MANAGE on the finance catalog',
    ],
    a: [1],
    x: 'El modelo de Unity Catalog es jerárquico: para consultar una tabla se necesita USE CATALOG + USE SCHEMA + SELECT. El SELECT ya se hereda del grant a nivel de schema, pero falta USE CATALOG sobre finance, por lo que la query falla antes de evaluar el acceso a la tabla.',
  },
  {
    n: 46,
    d: 'GOV',
    m: 0,
    q: 'A data engineering team is configuring a production Lakeflow Job that loads data into Unity Catalog tables every hour.\n\nInitially, the job runs using a personal user account belonging to one of the engineers. During a security review, the team identifies the following concerns:\n- pipelines may fail if the employee leaves the company\n- password changes could interrupt scheduled workloads\n- audit tracking for automated workloads should be separated from individual users\n\nWhich approach best addresses these concerns?',
    o: [
      'Use a service principal for the Lakeflow Job.',
      'Store engineer credentials securely in Azure Key Vault and use them for the job.',
      "Use a personal access token generated from an engineer's user account.",
      'Use a shared personal user account for all production Lakeflow Jobs.',
    ],
    a: [0],
    x: 'Un service principal es una identidad no humana dedicada a cargas automatizadas: sin dependencia de empleados, sin roturas por cambios de contraseña y con auditoría separada. Key Vault, PATs personales o cuentas compartidas siguen dependiendo de credenciales humanas.',
  },
  {
    n: 47,
    d: 'TRA',
    m: 0,
    q: 'A retail company maintains a customer dimension table using Lakeflow Spark Declarative Pipelines.\n\nCustomer records can change over time (address updates, membership tier changes, phone number updates).\n\nThe analytics team needs to:\n- automatically process CDC updates from the source system\n- preserve historical versions of customer records\n- track which customer version was active at a given point in time\n\nWhich SQL definition best supports these requirements?',
    o: [
      '```\nCREATE OR REFRESH STREAMING TABLE dim_customers;\n\nCREATE FLOW customer_flow AS\n```\nAUTO CDC INTO dim_customers\n```\nFROM stream(customers_bronze_cdc)\n```\nKEYS (customer_id)\nSEQUENCE BY operation_timestamp\n```\nSTORED AS SCD TYPE 2;\n```',
      '```\nCREATE OR REFRESH STREAMING TABLE dim_customers;\n\nCREATE FLOW customer_flow AS\n```\nAUTO CDC INTO dim_customers\n```\nFROM stream(customers_bronze_cdc)\n```\nKEYS (customer_id)\nSEQUENCE BY operation_timestamp\n```\nSTORED AS SCD TYPE 1;\n```',
      '```\nCREATE OR REFRESH MATERIALIZED VIEW dim_customers AS\nSELECT * FROM customers_bronze_cdc;\n```',
      '```\nCREATE OR REFRESH STREAMING TABLE dim_customers AS\nSELECT DISTINCT * FROM customers_bronze_cdc;\n```',
    ],
    a: [0],
    x: 'AUTO CDC INTO aplica los cambios CDC usando KEYS (clave de negocio) y SEQUENCE BY (orden). STORED AS SCD TYPE 2 preserva versiones históricas — lo que se pide. SCD TYPE 1 sobrescribe y solo guarda la versión actual; la MV y el SELECT DISTINCT no procesan CDC ni versionan.',
  },
  {
    n: 48,
    d: 'TRO',
    m: 0,
    q: 'A data engineering team is investigating a slow Spark workload in Databricks.\n\nWhile reviewing the Spark UI, the engineering team notices:\n- high spill-to-disk metrics across multiple tasks\n- heavy disk I/O during shuffle operations\n- significantly slower stage execution compared to previous runs\n\nWhich issue is the most likely cause of this behavior?',
    o: [
      'Liquid Clustering reorganizing Delta files during query execution.',
      'Data skew causing uneven partition distribution across executors.',
      'Disk spilling caused by insufficient executor memory during shuffle-heavy processing.',
      'Excessive shuffle partition creation causing Spark to spend more time coordinating shuffle tasks.',
    ],
    a: [2],
    x: 'Spill generalizado en muchas tareas + I/O de disco intenso = falta de memoria en los executors: Spark vuelca a disco los datos intermedios del shuffle. El skew se manifiesta con UNA tarea larga y shuffle reads desiguales; demasiadas particiones de shuffle añaden coordinación, no spill.',
  },
  {
    n: 49,
    d: 'TRA',
    m: 0,
    q: 'A company uses Lakeflow Spark Declarative Pipelines to process IoT sensor data from manufacturing equipment.\n\nNew data arrives in cloud storage approximately every 3 to 4 hours. The engineering team needs to process this data reliably but does not require near real time insights. They also want to minimize compute costs by avoiding unnecessary compute usage between pipeline runs.\n\nWhich pipeline mode best suits this use case?',
    o: [
      'Triggered pipeline mode',
      'Interactive pipeline mode',
      'Streaming Auto Loader mode',
      'Continuous pipeline mode',
    ],
    a: [0],
    x: 'El modo triggered procesa los datos disponibles al arrancar y apaga el cómputo al terminar — perfecto para datos periódicos sin requisito de baja latencia. El modo continuo mantiene el cómputo activo (más coste); "interactive" no existe y Auto Loader es un mecanismo de ingesta, no un modo de pipeline.',
  },
  {
    n: 50,
    d: 'GOV',
    m: 0,
    q: 'A data engineering team grants the following privileges to a group named data_analysts:\n```\nGRANT USE CATALOG ON CATALOG finance TO data_analysts;\nGRANT USE SCHEMA ON SCHEMA finance.reporting TO data_analysts;\nGRANT SELECT ON TABLE finance.reporting.customer_sales TO data_analysts;\n```\nLater, the engineering team runs:\n```\nREVOKE SELECT ON TABLE finance.reporting.customer_sales FROM data_analysts;\n```\nAfter the revoke operation, members of the data_analysts group can still access the table successfully.\n\nWhich situation most likely explains this behavior?',
    o: [
      'The USE SCHEMA privilege automatically includes SELECT access on all tables in the schema.',
      '```\nSELECT privileges cannot be revoked from Delta tables.\n```',
      '```\nREVOKE only removes privileges after the cluster is restarted.\n```',
      'The users belong to another group that also has the SELECT privilege on the table.',
    ],
    a: [3],
    x: 'Los permisos efectivos pueden venir de múltiples vías: grants directos, varios grupos, herencia. Revocar el SELECT de un grupo no elimina el acceso si otro grupo del que son miembros también lo concede. USE SCHEMA no incluye SELECT, los cambios de permisos son inmediatos y SELECT sí es revocable.',
  },
  {
    n: 51,
    d: 'JOBS',
    m: 0,
    q: 'A Lakeflow Job consists of five sequential tasks. The final task generates a critical audit log required for compliance reporting.\n\nThe data engineer wants to receive a Slack notification only if the final audit log task fails, without receiving notifications for failures in earlier tasks.\n\nWhich configuration best meets this requirement?',
    o: [
      'Use cluster-level logging and external monitoring tools to notify Slack when the audit log task fails.',
      'Configure Slack notifications for all tasks and filter unwanted alerts inside Slack.',
      'Configure a job-level notification that triggers on task failures and sends alerts to Slack.',
      'Configure a task-level notification on the audit log task to trigger a Slack alert only if that task fails.',
    ],
    a: [3],
    x: 'Las notificaciones a nivel de TAREA permiten alertar solo sobre la tarea crítica. Una notificación a nivel de JOB dispararía por fallos de cualquier tarea, notificar todo y filtrar en Slack genera ruido, y montar monitorización externa es innecesariamente complejo.',
  },
  {
    n: 52,
    d: 'JOBS',
    m: 0,
    q: 'A data engineering team is building a Lakeflow Job with the following workflow:\n- execute an existing Lakeflow Spark Declarative Pipelines (SDP) pipeline that builds Bronze and Silver tables\n- execute a notebook that performs a post-processing step after the pipeline completes\n\nThe engineering team wants to use the most appropriate Lakeflow Job task types for each workload.\n\nWhich approach best meets these requirements?',
    o: [
      'Use a Run Job task for the SDP pipeline and a notebook task for the post-processing notebook.',
      'Use a pipeline task for the SDP pipeline and a notebook task for the post-processing notebook.',
      'Use a notebook task for the SDP pipeline and a notebook task for the post-processing notebook.',
      'Use a SQL task for the SDP pipeline and a notebook task for the post-processing notebook.',
    ],
    a: [1],
    x: 'Lakeflow Jobs tiene un tipo de tarea "pipeline" específicamente para ejecutar pipelines SDP, y "notebook task" para lógica de notebooks. Run Job es para ejecutar OTRO job, y las tareas SQL son para queries — usar el tipo adecuado mejora monitorización y mantenibilidad.',
  },
  {
    n: 53,
    d: 'ING',
    m: 0,
    q: 'A retail company receives CSV sales files daily in an ADLS container. The engineering team wants to:\n- incrementally ingest only new files\n- avoid reprocessing previously loaded files\n- load the data into Unity Catalog governed Delta tables\n- use a SQL-based ingestion approach\n\nWhich solution best meets these requirements?',
    o: [
      'Use INSERT INTO with overwrite mode',
      'Use an external table',
      'Use COPY INTO',
      'Use CREATE TABLE AS SELECT',
    ],
    a: [2],
    x: 'COPY INTO es la ingesta incremental basada en SQL desde object storage: lleva registro de los ficheros ya cargados y solo procesa los nuevos. INSERT OVERWRITE reemplaza datos, CTAS crea la tabla desde cero sin tracking incremental y una external table no ingiere ficheros nuevos por sí sola.',
  },
  {
    n: 54,
    d: 'CICD',
    m: 0,
    q: 'A data engineering team is implementing a CI/CD pipeline for deploying Declarative Automation Bundles.\n\nBefore deploying changes to production, the engineering team wants the CI/CD workflow to verify the bundle configuration and fail the deployment process if configuration issues are detected.\n\nWhich Databricks CLI command best meets these requirements?',
    o: [
      '`databricks bundle run`',
      '`databricks bundle verify`',
      '`databricks bundle validate`',
      '`databricks bundle deploy`',
    ],
    a: [2],
    x: 'databricks bundle validate verifica la configuración del bundle (valores inválidos, targets ausentes, recursos mal definidos) antes de desplegar; en CI/CD se ejecuta antes de bundle deploy. bundle run ejecuta recursos, bundle deploy despliega sin verificación previa y bundle verify no existe.',
  },
  {
    n: 55,
    d: 'GOV',
    m: 0,
    q: 'A marketing agency wants to share campaign performance data stored in Databricks with a partner that uses Tableau for analytics but does not have a Databricks workspace.\n\nThe partner requires direct access to the shared data from Tableau.\n\nWhich type of Delta Sharing should the agency use?',
    o: [
      'Unity Catalog external locations',
      'Databricks-to-Databricks protocol',
      'Lakehouse Federation',
      'Open Sharing protocol',
    ],
    a: [3],
    x: 'El protocolo Open Sharing de Delta Sharing permite a herramientas externas (Tableau, Power BI, pandas, Spark) acceder en solo lectura a tablas Delta compartidas SIN necesitar workspace de Databricks. Databricks-to-Databricks exige Unity Catalog en ambos lados, y Lakehouse Federation es para consultar fuentes externas desde Databricks (dirección contraria).',
  },
];

export const exam6: RawQuestion[] = [
  {
    n: 1,
    d: 'TRA',
    m: 0,
    q: 'A retail company is building a Gold customer analytics dataset using PySpark.\n\nThe analytics team needs an estimate of the number of unique customers who placed orders during the last 12 months. The dataset contains billions of records, and the engineering team wants a faster aggregation with lower resource usage instead of performing a full exact distinct count.\n\nWhich aggregation function best meets this requirement?',
    o: [
      '```\nsales_df.select(\n    count_distinct("customer_id")\n)\n```',
      '```\nsales_df.select(\n    approx_count_distinct("customer_id")\n)\n```',
      '```\nsales_df.select(\n    count("customer_id")\n)\n```',
      '```\nsales_df.select(\n    count(distinct("customer_id"))\n)\n```',
    ],
    a: [1],
    x: 'approx_count_distinct() estima el número de valores únicos reduciendo shuffle y memoria — ideal cuando basta una estimación sobre miles de millones de filas. count_distinct() es exacto (y caro), count() cuenta filas totales y count(distinct(...)) no es sintaxis válida de PySpark.',
  },
  {
    n: 2,
    d: 'P',
    m: 0,
    q: 'A data engineering team is onboarding data from an external cloud storage into Unity Catalog.\n\nThe security team wants to:\n- define the cloud authentication mechanism in Unity Catalog\n- reuse the same authentication object across multiple containers in that storage account\n\nWhich Unity Catalog object best meets these requirements?',
    o: ['Storage Credential', 'External Storage', 'External Location', 'Unity Catalog Volume'],
    a: [0],
    x: 'El Storage Credential define CÓMO se autentica Unity Catalog contra el cloud (managed identity, IAM role, service principal) y puede reutilizarse por múltiples External Locations. La External Location gobierna UNA ruta concreta (usa un credential, no lo define), y los Volumes gobiernan acceso a ficheros, no autenticación.',
  },
  {
    n: 3,
    d: 'TRO',
    m: 0,
    q: 'An IoT data engineering team is troubleshooting a Lakeflow Job that joins a large sensor_readings table with a small device_metadata lookup table.\n\nWhile reviewing the Spark UI, the engineering team notices:\n- both tables are being shuffled during the join\n- the device_metadata table is only a few megabytes in size\n- the join stage is taking longer than expected\n\nThe engineering team wants to reduce network shuffle during the join operation.\n\nWhich optimization is the most appropriate?',
    o: [
      'Use a broadcast join for the device_metadata lookup table.',
      'Increase the number of shuffle partitions.',
      'Run OPTIMIZE on both tables before executing the join.',
      'Enable Liquid Clustering on the sensor_readings table.',
    ],
    a: [0],
    x: 'Con una lookup de pocos MB, el broadcast join la envía a todos los executors y evita el shuffle de ambas tablas. Aumentar particiones de shuffle no lo elimina, y OPTIMIZE / Liquid Clustering mejoran el layout de ficheros, no la estrategia del join.',
  },
  {
    n: 4,
    d: 'ING',
    m: 0,
    q: 'A finance organization needs to ingest data nightly from an enterprise Oracle database into Delta tables in Databricks.\n\nThe engineering team wants to:\n- execute custom SQL queries against the source database\n- apply custom transformation logic inside notebooks\n\nWhich approach best fits these requirements?',
    o: [
      'Use Auto Loader for database ingestion',
      'Use JDBC ingestion inside notebooks',
      'Use Delta Sharing recipients',
      'Use COPY INTO from the Oracle database',
    ],
    a: [1],
    x: 'La ingesta JDBC en notebooks permite conectarse directamente a la base relacional, ejecutar SQL de extracción a medida y transformar en el propio notebook. Auto Loader y COPY INTO son para ficheros en object storage, y Delta Sharing es para compartir datasets, no para extraer de bases operacionales.',
  },
  {
    n: 5,
    d: 'JOBS',
    m: 1,
    q: 'A data engineering team has a Lakeflow Job with the following task dependencies:\n\ningest_customers ──→ build_silver_customers ──┐\n                                              ↓\n                                           build_gold_sales\n                                              ↑\ningest_orders ─────→ build_silver_orders ─────┘\n\nDuring one execution:\n- build_silver_customers completed successfully\n- build_silver_orders failed after exhausting all configured retries\n\nLater, the engineering team notices that build_gold_sales still executed successfully but produced only partial downstream data.\n\nWhich Run if conditions on the dependencies to build_gold_sales could have caused this behavior?\n\nSelect TWO answers.',
    o: ['At least one succeeded', 'All succeeded', 'All failed', 'All done'],
    a: [0, 3],
    x: '"At least one succeeded" ejecuta el Gold si al menos una dependencia tuvo éxito (aquí, silver_customers). "All done" lo ejecuta cuando todas terminan, con o sin éxito. Ambas permiten ejecución con fallos upstream → datos parciales. "All succeeded" (el default) lo habría bloqueado, y "All failed" exige que fallen todas.',
  },
  {
    n: 6,
    d: 'P',
    m: 0,
    q: 'A company currently stores large volumes of structured and unstructured data in cloud object storage. However, data engineers frequently encounter:\n- unexpected schema changes to tables\n- failed or conflicting updates during concurrent writes\n- downstream reporting inconsistencies caused by partial data updates\n\nThe company wants to continue using cloud object storage while improving reliability for analytical workloads.\n\nWhich technology best addresses these requirements?',
    o: ['Parquet', 'Delta Live Tables', 'Unity Catalog', 'Delta Lake'],
    a: [3],
    x: 'Delta Lake añade sobre el object storage transacciones ACID, schema enforcement y control de concurrencia, evitando escrituras parciales y conflictos. Parquet es solo el formato de fichero, Unity Catalog es gobernanza y DLT/SDP es el framework de pipelines — la fiabilidad transaccional la aporta Delta Lake.',
  },
  {
    n: 7,
    d: 'TRO',
    m: 0,
    q: 'A data engineering team is troubleshooting a slow Lakeflow Job that processes a large fact table join.\n\nWhile reviewing the Spark UI, the engineering team notices:\n- high spill-to-disk metrics during shuffle stages\n- increased disk I/O\n- failed tasks during shuffle processing\n\nWhich issue is the most likely cause of this behavior?',
    o: [
      'Out-of-memory issues caused by insufficient executor memory.',
      'Large number of shuffle partitions.',
      'Excessive task dependency in the Lakeflow Job.',
      'Cluster node failures.',
    ],
    a: [0],
    x: 'Spill a disco + I/O elevado + tareas fallidas en shuffle apuntan a falta de memoria en los executors: Spark no puede procesar en memoria los datos intermedios de joins/agregaciones y vuelca a disco. Muchas particiones de shuffle añaden coordinación (no spill), y las dependencias de tareas son orquestación, no memoria.',
  },
  {
    n: 8,
    d: 'P',
    m: 0,
    q: 'A company currently uses a Hive metastore to manage tables across multiple Databricks workspaces. Different teams manage permissions independently, making it difficult to enforce consistent governance policies and track data lineage centrally.\n\nThe company wants to:\n- centralize access control\n- simplify governance management across workspaces\n- improve visibility into data lineage and auditing\n\nWhich solution best addresses these requirements?',
    o: [
      'Migrate to Unity Catalog',
      'Enable Photon on existing compute resources',
      'Use Delta Live Tables for all ingestion pipelines',
      'Use Delta Lake tables for all workloads',
    ],
    a: [0],
    x: 'Unity Catalog sustituye al Hive metastore como capa de gobernanza centralizada: permisos consistentes entre workspaces, lineage y auditoría centralizados. Photon es rendimiento de queries, DLT/SDP es pipelines y Delta Lake es fiabilidad transaccional — ninguno gobierna.',
  },
  {
    n: 9,
    d: 'ING',
    m: 0,
    q: 'A data engineering team is building a batch ingestion pipeline that incrementally loads CSV files from cloud object storage into a Bronze Delta table.\n\nThe team wants the ingestion process to:\n- load only newly arrived files\n- avoid reprocessing previously loaded files\n- use a SQL-based ingestion approach\n\nWhich code snippet best meets these requirements?',
    o: [
      "```\nCOPY INTO retail.bronze.orders\nFROM '/Volumes/retail/landing/orders'\nFILEFORMAT = CSV\n```\nFORMAT_OPTIONS ('header' = 'true')",
      "```\nCREATE OR REPLACE TABLE retail.bronze.orders AS\nSELECT *\nFROM csv.'/Volumes/retail/landing/orders'\n```",
      "```\nMERGE INTO retail.bronze.orders t\n```\nUSING csv.'/Volumes/retail/landing/orders' s\n```\nON t.order_id = s.order_id\n```",
      "```\nINSERT OVERWRITE retail.bronze.orders\nSELECT *\nFROM csv.'/Volumes/retail/landing/orders'\n```",
    ],
    a: [0],
    x: 'COPY INTO ingiere incrementalmente ficheros desde object storage vía SQL, llevando registro de los ya cargados para no reprocesarlos. CREATE OR REPLACE recrea la tabla entera, MERGE es upsert a nivel de fila (sin tracking de ficheros) e INSERT OVERWRITE reemplaza el contenido.',
  },
  {
    n: 10,
    d: 'JOBS',
    m: 0,
    q: 'A data engineering team is building a Lakeflow Job to process financial reconciliation data.\n\nThe workflow must run:\n- on the 1st and 16th day of every month\n- at both 10:00 AM and 4:00 PM UTC\n\nWhich approach best meets these requirements?',
    o: [
      'Configure a table update trigger on the reconciliation tables.',
      'Configure a scheduled trigger using the standard scheduling options in the Lakeflow Jobs UI.',
      'Configure a scheduled trigger using a CRON expression.',
      'Configure a file arrival trigger on the reconciliation data location.',
    ],
    a: [2],
    x: 'Las expresiones CRON permiten calendarios avanzados: días concretos del mes (1 y 16) y múltiples horas diarias (10:00 y 16:00). Las opciones estándar de la UI no llegan a esa complejidad, y los triggers de file arrival / table update son event-driven, no de calendario.',
  },
  {
    n: 11,
    d: 'TRA',
    m: 0,
    q: 'A data engineering team is building a reporting pipeline using Lakeflow Spark Declarative Pipelines.\n\nThe team needs to maintain an aggregated dataset based on billions of records stored in a Delta table. Business users run the same dashboard queries throughout the day.\n\nThe engineering team wants to:\n- reduce query execution times\n- avoid recalculating the aggregation every time users query the data\n- keep the results automatically refreshed as source data changes\n\nWhich Lakeflow Spark Declarative Pipelines object best meets these requirements?',
    o: ['Materialized View', 'Streaming Table', 'Standard View', 'Temporary View'],
    a: [0],
    x: 'La Materialized View almacena el resultado precalculado y SDP la refresca automáticamente cuando cambian los datos fuente — evita recalcular la agregación en cada consulta del dashboard. La vista estándar recalcula siempre, la temporal es de sesión y la streaming table procesa datos incrementales, no acelera queries repetidas.',
  },
  {
    n: 12,
    d: 'P',
    m: 0,
    q: 'A data engineering team is developing ETL pipelines in Databricks. Several engineers work collaboratively in notebooks throughout the day.\n\nThe team requires a compute environment that supports:\n- interactive notebook development\n- multiple users attaching to the same compute\n- shared library configurations\n- collaborative development workflows\n\nWhich compute option is most appropriate?',
    o: ['Job compute', 'All-purpose compute', 'Serverless compute', 'SQL warehouse'],
    a: [1],
    x: 'All-purpose compute está diseñado para desarrollo interactivo y colaborativo: varios usuarios en el mismo clúster, runtime compartido y control de librerías. Serverless usa runtime y librerías predefinidas por Databricks (menos control), job compute es para ejecución programada y el SQL warehouse es para analítica SQL.',
  },
  {
    n: 13,
    d: 'TRO',
    m: 0,
    q: 'A data engineering team enables Predictive Optimization for several Unity Catalog tables used in a reporting workload.\n\nAfter a few days, the engineering team notices that some tables are automatically optimized while others are not.\n\nWhy is this behavior occurring?',
    o: [
      'Predictive Optimization only runs on tables referenced by Lakeflow Jobs.',
      'Predictive Optimization requires all tables to be partitioned before optimization can occur.',
      'Predictive Optimization only supports Delta tables that use Liquid Clustering.',
      'Predictive Optimization only supports Unity Catalog managed tables.',
    ],
    a: [3],
    x: 'Predictive Optimization solo actúa sobre tablas MANAGED de Unity Catalog (OPTIMIZE, VACUUM, ANALYZE automáticos); las external no se optimizan porque Databricks no gestiona su ciclo de vida de almacenamiento. No requiere particionado ni Liquid Clustering, ni depende de Lakeflow Jobs.',
  },
  {
    n: 14,
    d: 'TRO',
    m: 0,
    q: 'A data engineering team is troubleshooting a Lakeflow Job that uses a job cluster.\n\nThe job fails before any notebook code starts running.\n\nWhich issue is the most likely cause of the job failure?',
    o: [
      'The Lakeflow Job exceeded the configured task retry limit.',
      'A Bronze table referenced by the notebook does not exist.',
      'A Spark transformation inside the notebook caused excessive shuffle operations.',
      'An error in the init script is preventing the cluster from starting successfully.',
    ],
    a: [3],
    x: 'Si el job falla ANTES de ejecutar código de notebook, el problema está en el arranque del clúster: init scripts, configuración, runtime, librerías o permisos. Una tabla inexistente o un shuffle excesivo fallarían DESPUÉS de empezar el notebook, y el límite de retries aplica durante la ejecución de tareas.',
  },
  {
    n: 15,
    d: 'P',
    m: 0,
    q: 'A data engineering team is designing workloads for different usage patterns in Databricks.\n\nThe team has the following requirements:\n- engineers need shared notebook environments with development-specific libraries already installed\n- production ETL pipelines should run in isolated environments\n- lightweight experimentation workloads should minimize infrastructure cost\n\nWhich combination of compute options best matches these requirements?',
    o: [
      'All-purpose compute for engineers, job compute for ETL pipelines, single-node compute for lightweight experimentation',
      'SQL warehouses for engineers, single-node compute for ETL pipelines, job compute for lightweight experimentation',
      'Single-node compute for engineers, serverless compute for ETL pipelines, multi node all-purpose compute for lightweight experimentation',
      'Job compute for engineers, all-purpose compute for ETL pipelines, SQL warehouses for lightweight experimentation',
    ],
    a: [0],
    x: 'Emparejamiento correcto: all-purpose = desarrollo colaborativo con librerías compartidas; job compute = producción programada y aislada; single-node = experimentación ligera y barata. Las demás opciones invierten los roles (SQL warehouses no son para desarrollo en notebooks, single-node no es para ETL productivo escalable).',
  },
  {
    n: 16,
    d: 'GOV',
    m: 0,
    q: 'A data governance team manages a Unity Catalog schema named finance.reporting.\n\nNew reporting tables are created in this schema every week. The team wants analysts to automatically receive query access to any new tables created in the schema, without manually granting SELECT on each table.\n\nWhich approach best meets this requirement?',
    o: [
      'Grant SELECT on the finance.reporting schema to the analysts group.',
      'Create a user-defined function to automatically grant privileges whenever new tables are created.',
      'Grant CREATE TABLE on the finance.reporting schema to the analysts group.',
      'Grant USE SCHEMA on the finance.reporting schema to the analysts group.',
    ],
    a: [0],
    x: 'Los privilegios en Unity Catalog se heredan hacia abajo: un GRANT SELECT a nivel de schema aplica a todas las tablas del schema, incluidas las futuras. CREATE TABLE permite crear (no consultar), USE SCHEMA da acceso a la jerarquía pero no a los datos, y las UDF no gestionan permisos.',
  },
  {
    n: 17,
    d: 'ING',
    m: 0,
    q: 'A media company receives continuously arriving nested JSON clickstream files in a Unity Catalog Volume.\n\nThe data engineering team wants the Auto Loader pipeline to:\n- incrementally process newly arriving files\n- track schema information over time\n- stop the ingestion pipeline if unexpected new columns appear in incoming data\n\nWhich code snippet best meets these requirements?',
    o: [
      '```\ndf = (\n    spark.readStream\n        .format("cloudFiles")\n        .option("cloudFiles.format", "json")\n        .option("cloudFiles.schemaLocation", "/Volumes/media/checkpoints/clickstream_schema")\n        .option("cloudFiles.schemaEvolutionMode", "failOnNewColumns")\n        .load("/Volumes/media/landing/clickstream")\n)\n```',
      '```\ndf = (\n    spark.readStream\n        .format("json")\n        .option("cloudFiles.schemaLocation", "/Volumes/media/checkpoints/clickstream_schema")\n        .option("cloudFiles.schemaEvolutionMode", "failOnNewColumns")\n        .load("/Volumes/media/landing/clickstream")\n)\n```',
      '```\ndf = (\n    spark.readStream\n        .format("cloudFiles")\n        .option("format", "json")\n        .option("schemaLocation", "/Volumes/media/checkpoints/clickstream_schema")\n        .option("schemaEvolutionMode", "failOnNewColumns")\n        .load("/Volumes/media/landing/clickstream")\n)\n```',
      '```\ndf = (\n    spark.read\n        .format("cloudFiles")\n        .option("cloudFiles.format", "json")\n        .option("cloudFiles.schemaEvolutionMode", "failOnNewColumns")\n        .load("/Volumes/media/landing/clickstream")\n)\n```',
    ],
    a: [0],
    x: 'Correcto: spark.readStream + format("cloudFiles") + opciones con prefijo cloudFiles., incluyendo cloudFiles.schemaEvolutionMode = failOnNewColumns para que el stream falle ante columnas nuevas inesperadas. Las demás usan la fuente json estándar, opciones sin prefijo (inválidas) o spark.read (no streaming).',
  },
  {
    n: 18,
    d: 'GOV',
    m: 0,
    q: 'A data engineering team manages a large external Delta table in Unity Catalog named finance.gold.transactions.\n\nThe engineering team wants to migrate the table to a Unity Catalog managed table to take advantage of Predictive Optimization, automatic maintenance operations, and managed storage lifecycle — while avoiding recreating the table with CREATE TABLE AS SELECT, preserving metadata and minimizing disruption.\n\nWhich command best meets these requirements?',
    o: [
      '```\nALTER TABLE finance.gold.transactions\nCHANGE MANAGED;\n```',
      '```\nCONVERT TO DELTA finance.gold.transactions;\n```',
      '```\nALTER TABLE finance.gold.transactions\nSET MANAGED;\n```',
      '```\nALTER TABLE finance.gold.transactions\nAS MANAGED;\n```',
    ],
    a: [2],
    x: 'ALTER TABLE ... SET MANAGED convierte una tabla external Delta en managed de Unity Catalog preservando metadatos y sin recrearla, habilitando Predictive Optimization y el ciclo de vida gestionado. CONVERT TO DELTA es para tablas Parquet (esta ya es Delta), y CHANGE/AS MANAGED no son sintaxis válidas.',
  },
  {
    n: 19,
    d: 'ING',
    m: 0,
    q: 'A logistics company receives continuous vehicle telemetry events through an Apache Kafka topic named vehicle_telemetry.\n\nThe data engineering team wants to build a streaming ingestion pipeline that:\n- reads events continuously from Kafka\n- subscribes to the correct Kafka topic\n- starts processing only new events arriving after the stream starts\n\nWhich code snippet best meets these requirements?',
    o: [
      '```\ndf = (\n    spark.readStream\n        .format("kafka")\n        .option("kafka.bootstrap.servers", "broker1:9092")\n        .option("subscribe", "vehicle_telemetry")\n        .option("startingOffsets", "earliest")\n        .load()\n)\n```',
      '```\ndf = (\n    spark.readStream\n        .format("json")\n        .option("kafka.bootstrap.servers", "broker1:9092")\n        .option("subscribe", "vehicle_telemetry")\n        .option("startingOffsets", "latest")\n        .load()\n)\n```',
      '```\ndf = (\n    spark.readStream\n        .format("kafka")\n        .option("kafka.bootstrap.servers", "broker1:9092")\n        .option("subscribe", "vehicle_telemetry")\n        .option("startingOffsets", "latest")\n        .load()\n)\n```',
      '```\ndf = (\n    spark.readStream\n        .format("kafka")\n        .option("bootstrap.servers", "broker1:9092")\n        .option("topic", "vehicle_telemetry")\n        .option("startingOffset", "latest")\n        .load()\n)\n```',
    ],
    a: [2],
    x: 'La configuración correcta de Kafka en Structured Streaming: format("kafka"), kafka.bootstrap.servers, subscribe para el topic y startingOffsets = "latest" para procesar solo eventos nuevos. "earliest" leería todo el histórico, format("json") no es la fuente Kafka y las opciones bootstrap.servers/topic/startingOffset están mal escritas.',
  },
  {
    n: 20,
    d: 'P',
    m: 0,
    q: 'A data engineering team wants to reduce operational overhead for interactive analytics workloads in Databricks.\n\nThe team prefers a compute option that:\n- automatically manages infrastructure provisioning\n- scales resources based on workload demand\n- reduces manual compute management\n\nWhich compute option best meets these requirements?',
    o: ['Serverless compute', 'All-purpose compute', 'Compute Pools', 'Elastic Compute'],
    a: [0],
    x: 'Serverless: Databricks aprovisiona y escala la infraestructura automáticamente según demanda, minimizando la gestión manual. All-purpose sigue exigiendo gestionar configuración y ciclo de vida, y "Compute Pools" / "Elastic Compute" no son opciones de cómputo de Databricks (terminología trampa).',
  },
  {
    n: 21,
    d: 'TRA',
    m: 0,
    q: 'A retail company maintains a customer dimension table using Lakeflow Spark Declarative Pipelines.\n\nCustomer records are periodically updated from a CDC source system. The analytics team only needs the latest version of each customer record and does not need to preserve historical changes.\n\nWhich SQL definition best supports this requirement?',
    o: [
      '```\nCREATE OR REFRESH STREAMING TABLE dim_customers AS\nSELECT DISTINCT * FROM customers_bronze_cdc;\n```',
      '```\nCREATE OR REFRESH MATERIALIZED VIEW dim_customers AS\nSELECT * FROM customers_bronze_cdc;\n```',
      '```\nCREATE OR REFRESH STREAMING TABLE dim_customers;\n\nCREATE FLOW customer_flow AS\n```\nAUTO CDC INTO dim_customers\n```\nFROM stream(customers_bronze_cdc)\n```\nKEYS (customer_id)\nSEQUENCE BY operation_timestamp\n```\nSTORED AS SCD TYPE 2;\n```',
      '```\nCREATE OR REFRESH STREAMING TABLE dim_customers;\n\nCREATE FLOW customer_flow AS\n```\nAUTO CDC INTO dim_customers\n```\nFROM stream(customers_bronze_cdc)\n```\nKEYS (customer_id)\nSEQUENCE BY operation_timestamp\n```\nSTORED AS SCD TYPE 1;\n```',
    ],
    a: [3],
    x: 'SCD TYPE 1 aplica los cambios CDC sobrescribiendo la versión existente de cada registro (por KEYS y en orden SEQUENCE BY): solo se conserva el estado actual, que es lo pedido. SCD TYPE 2 preserva histórico (innecesario aquí); SELECT DISTINCT y la MV no procesan CDC.',
  },
  {
    n: 22,
    d: 'CICD',
    m: 0,
    q: 'A data engineering team wants to deploy a Declarative Automation Bundle to the production environment as part of an automated CI/CD workflow.\n\nWhich Databricks CLI command best meets this requirement?',
    o: [
      '`databricks bundle deploy --env prod`',
      '`databricks bundle deploy --target prod`',
      '`databricks bundle deploy prod`',
      '`databricks bundle deploy`',
    ],
    a: [1],
    x: 'El target de despliegue se especifica con la opción --target: databricks bundle deploy --target prod usa la configuración del target prod (workspace, clústeres, schedules, catálogos). --env no existe, el target no va como argumento posicional y sin --target se usa el target por defecto.',
  },
  {
    n: 23,
    d: 'TRA',
    m: 0,
    q: 'A data engineering team is creating a Silver customer table from a Bronze table named retail.bronze.customers.\n\nThe Bronze table contains rows where customer_id is NULL. The team wants to remove these records before writing the cleaned data to retail.silver.customers.\n\nWhich PySpark code snippet best meets these requirements?',
    o: [
      '```\ndf = spark.read.table("retail.bronze.customers")\n\ndf.write.mode("overwrite").saveAsTable("retail.silver.customers")\n```',
      '```\ndf = (\n    spark.read.table("retail.bronze.customers")\n    .filter(col("customer_id").isNotNull())\n)\n\ndf.write.mode("overwrite").saveAsTable("retail.silver.customers")\n```',
      '```\ndf = (\n    spark.read.table("retail.bronze.customers")\n    .drop("customer_id")\n)\n\ndf.write.mode("overwrite").saveAsTable("retail.silver.customers")\n```',
      '```\ndf = (\n    spark.read.table("retail.bronze.customers")\n    .filter(col("customer_id").isNull())\n)\n\ndf.write.mode("overwrite").saveAsTable("retail.silver.customers")\n```',
    ],
    a: [1],
    x: 'filter(col("customer_id").isNotNull()) mantiene solo las filas con identificador válido antes de escribir a Silver. Sin filtro se copian los NULL, drop() elimina la COLUMNA entera, e isNull() haría justo lo contrario (solo conservaría los inválidos).',
  },
  {
    n: 24,
    d: 'TRA',
    m: 0,
    q: 'A data engineering team is building a Gold customer analytics table from a Silver transactions table named retail.silver.transactions (columns: customer_id, transaction_amount).\n\nThe business wants a Gold table that contains:\n- the total number of transactions for each customer\n- the average transaction amount for each customer\n\nWhich PySpark code snippet best meets these requirements?',
    o: [
      '```\nfrom pyspark.sql.functions import count, avg\n\ngold_df = (\n    spark.read.table("retail.silver.transactions")\n    .agg(\n        count("*").alias("total_transactions"),\n        avg("transaction_amount").alias("avg_transaction_amount")\n    )\n)\n```',
      '```\nfrom pyspark.sql.functions import count, avg\n\ngold_df = (\n    spark.read.table("retail.silver.transactions")\n    .groupBy("customer_id")\n    .agg(\n        count("*").alias("total_transactions"),\n        avg("transaction_amount").alias("avg_transaction_amount")\n    )\n)\n```',
      '```\nfrom pyspark.sql.functions import count, avg\n\ngold_df = (\n    spark.read.table("retail.silver.transactions")\n    .groupBy("transaction_amount")\n    .agg(\n        count("*").alias("total_transactions"),\n        avg("transaction_amount").alias("avg_transaction_amount")\n    )\n)\n```',
      '```\nfrom pyspark.sql.functions import count, avg\n\ngold_df = (\n    spark.read.table("retail.silver.transactions")\n    .groupBy("customer_id")\n    .agg(\n        countDistinct("transaction_amount").alias("total_transactions"),\n        avg("customer_id").alias("avg_transaction_amount")\n    )\n)\n```',
    ],
    a: [1],
    x: 'Para métricas por cliente hay que agrupar por customer_id y aplicar count("*") y avg("transaction_amount"). Sin groupBy se agregan todos los datos en una sola fila; agrupar por importe o promediar customer_id son lógicas incorrectas.',
  },
  {
    n: 25,
    d: 'GOV',
    m: 0,
    q: 'A financial services company regularly transfers CSV files via SFTP to provide regulators with quarterly reports. This approach is slow, error-prone, and regulators often work with outdated data.\n\nThe company has enabled Unity Catalog in Databricks and wants to provide regulators with live, read-only access to selected Lakehouse tables without creating copies of the data.\n\nWhich feature should they use?',
    o: ['Unity Catalog Volumes', 'Lakehouse Federation', 'External Locations', 'Delta Sharing'],
    a: [3],
    x: 'Delta Sharing comparte datos vivos en solo lectura con destinatarios externos sin copiar ni mover los datos — los reguladores ven siempre la versión más reciente. Lakehouse Federation es la dirección contraria (consultar fuentes externas desde Databricks), y Volumes / External Locations gobiernan almacenamiento, no compartición externa.',
  },
  {
    n: 26,
    d: 'GOV',
    m: 0,
    q: 'A data engineering team manages a Unity Catalog table named sales.gold.orders.\n\nThe engineering team wants to implement the following requirements:\n- regional managers should only see orders for their own region\n- non-manager users should not see the profit_margin column\n- users should query a single governed object instead of accessing the base table directly\n- the filtering and masking logic should be implemented together in one layer\n\nWhich approach best meets these requirements?',
    o: ['Dynamic views', 'Row filters', 'Security views', 'Column masks'],
    a: [0],
    x: 'Las dynamic views combinan filtrado de filas y enmascarado de columnas en un ÚNICO objeto gobernado que los usuarios consultan en lugar de la tabla base — justo los cuatro requisitos. Row filters y column masks aplican directamente sobre la tabla y por separado, y "security views" no es una feature de Unity Catalog.',
  },
  {
    n: 27,
    d: 'JOBS',
    m: 0,
    q: 'A data engineering team is building a Lakeflow Job to process regional sales data.\n\nThe same notebook logic must execute separately for multiple regions: UK, US, Germany, France, Japan.\n\nThe engineering team wants to:\n- reuse the same notebook task logic\n- avoid creating separate notebook tasks for each region\n- dynamically execute the workflow once per region\n\nWhich approach best meets these requirements?',
    o: [
      'Create separate notebook tasks manually for each region inside the Lakeflow Job.',
      'Configure a Run Job task for each region and pass the region value as a job parameter.',
      'Configure a For each task to iterate through the list of regions and execute the notebook task dynamically with the region value.',
      'Configure an If/else task to route execution based on the selected region.',
    ],
    a: [2],
    x: 'La tarea For each itera sobre una colección de valores (las regiones) y ejecuta dinámicamente la misma tarea con cada parámetro — escalable si se añaden regiones. Tareas separadas o Run Jobs por región duplican orquestación, e If/else es para bifurcación condicional, no iteración.',
  },
  {
    n: 28,
    d: 'CICD',
    m: 0,
    q: 'A data engineering team manages deployments using custom Databricks REST API scripts. The process is slow, difficult to maintain, and requires engineers to work directly with low-level API calls.\n\nThe engineering team wants:\n- a native and supported deployment approach\n- repeatable deployments across development, test, and production environments\n- a simpler way to package and deploy Databricks projects within CI/CD workflows\n\nWhich approach best meets these requirements?',
    o: [
      'Continue using Databricks REST APIs and expand the deployment scripts for each environment.',
      'Use Terraform to deploy Lakeflow Jobs, Lakeflow Spark Declarative Pipelines, and notebooks as infrastructure resources.',
      'Use DBX from Databricks Labs to package and deploy Databricks projects.',
      'Use Declarative Automation Bundles to package and deploy Databricks projects across environments.',
    ],
    a: [3],
    x: 'Los Declarative Automation Bundles son el enfoque nativo y recomendado para empaquetar y desplegar proyectos Databricks (jobs, pipelines SDP, notebooks, configuración) en CI/CD. dbx fue el framework anterior de Databricks Labs (superado por los DABs), Terraform es más de infraestructura y los scripts REST son difíciles de mantener.',
  },
];

export const exam7: RawQuestion[] = [
  {
    n: 1,
    d: 'GOV',
    m: 0,
    q: 'Unity Catalog supports both managed and external tables. A data engineer wants to understand how these two table types differ from a storage and lifecycle perspective.\n\nWhich characteristic defines a managed table in Unity Catalog?',
    o: [
      'A managed table is a table whose underlying data files are stored in a location determined by Unity Catalog, and Unity Catalog manages the table lifecycle.',
      'Managed tables require an explicit LOCATION clause when they are created.',
      'Managed tables have fixed schemas and do not support schema evolution.',
      'Managed tables reference user-specified external storage locations, leaving data intact after DROP TABLE.',
    ],
    a: [0],
    x: 'Una tabla managed almacena sus ficheros en la ubicación gestionada por Unity Catalog (definida a nivel de esquema/catálogo/metastore) y su ciclo de vida completo lo controla UC: al hacer DROP TABLE se borran también los datos. Requerir LOCATION explícito, tener esquema fijo o sobrevivir al DROP son rasgos de una tabla external, no managed.',
  },
  {
    n: 2,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to compute DAU (daily active users) as the number of unique customer_id values per order_date. The orders_df DataFrame contains about 1 billion rows.\n\nWhich PySpark pattern is the best exact optimization approach for computing DAU?',
    o: [
      '```\norders_df.repartition(1000, "order_date").groupBy("order_date").count()\n```',
      '```\ndau_df = (orders_df.select("order_date", "customer_id").dropDuplicates(["order_date", "customer_id"]).groupBy("order_date").agg(F.count("*").alias("dau")))\n```',
      '```\norders_df.groupBy("order_date").count()\n```',
      '```\norders_df.withColumn("rn", F.row_number().over(Window.partitionBy("order_date","customer_id").orderBy("order_ts"))).filter("rn = 1").groupBy("order_date").count()\n```',
    ],
    a: [1],
    x: 'Proyectar solo las columnas necesarias y aplicar dropDuplicates(["order_date","customer_id"]) reduce el dataset a claves únicas antes de contar, evitando el coste de countDistinct sobre el dataset completo. La opción con row_number()+Window añade un ordenamiento innecesario (más caro), y las otras opciones cuentan filas, no clientes únicos.',
  },
  {
    n: 3,
    d: 'TRA',
    m: 1,
    q: 'A data engineer is processing a "Bronze" table containing raw customer order logs. The source data contains nested arrays of products purchased. The engineer needs to transform this into a "Silver" table where each row represents a single unit of a product sold, excluding transactions marked as "Returned".\n\nWhich two Spark transformations were executed on the Bronze table to produce this Silver table? (Select two)',
    o: [
      'The flatten() function was used to merge multiple arrays into a single row.',
      'An inner join was performed between the orders table and a products dimension table.',
      'The explode() function was applied to the items_purchased column.',
      "A filter() or where() transformation was applied to exclude rows where status == 'Returned'.",
    ],
    a: [2, 3],
    x: 'explode() convierte cada elemento del array items_purchased en una fila independiente (cambiando la granularidad de "por pedido" a "por artículo"), y un filter/where elimina las filas con status = \'Returned\'. flatten() solo aplana arrays de arrays sin generar filas nuevas, y no hay evidencia de un join porque toda la información ya está en la tabla origen.',
  },
  {
    n: 4,
    d: 'TRA',
    m: 0,
    q: 'A Databricks Structured Streaming query performs a large stateful aggregation over 10-minute event-time windows. Over time, the query slows down and memory usage grows because late data is accepted indefinitely.\n\nWhich change ensures old aggregation state is evicted so the query remains bounded over time?',
    o: [
      '```\nevents_with_watermark = events.withWatermark("event_time", "2 hours"); agg = events_with_watermark.groupBy(window(col("event_time"),"10 minutes"), col("device_id")).count()\n```',
      'Set the output mode of the writeStream to "complete".',
      'Set a processing-time trigger of "1 minute" on the writeStream.',
      'Increase spark.sql.shuffle.partitions to 200.',
    ],
    a: [0],
    x: 'withWatermark("event_time","2 hours") le dice a Spark cuánto tiempo esperar por datos tardíos antes de descartar el estado de las ventanas antiguas, acotando así el crecimiento del state store. El output mode, el trigger o el número de shuffle partitions no definen ningún umbral de "lateness" ni provocan la purga de estado.',
  },
  {
    n: 5,
    d: 'TRO',
    m: 0,
    q: 'A FinOps team needs a workspace-wide report of the top 50 longest-running SQL queries from the last 30 days, including SQL text, user, warehouse, and duration.\n\nWhich Unity Catalog system table should be queried?',
    o: [
      'system.information_schema.tables',
      'system.query.history',
      'system.compute.clusters',
      'system.access.audit',
    ],
    a: [1],
    x: 'system.query.history almacena el histórico de ejecuciones de consultas (texto SQL, duración, usuario, warehouse), justo lo que pide el reporte. information_schema.tables describe la estructura de los objetos (no su actividad), compute.clusters es un inventario de hardware, y access.audit registra eventos de seguridad, no rendimiento de queries.',
  },
  {
    n: 6,
    d: 'P',
    m: 0,
    q: 'A team of BI analysts uses an All-Purpose Compute cluster with a 120-minute auto-termination limit, causing high idle costs, while restart latency (5-10 minutes) hurts responsiveness for their SQL-only workload.\n\nWhich compute resource type best solves this cost vs. latency trade-off?',
    o: [
      'Classic SQL Warehouse',
      'Single Node All-Purpose Cluster',
      'Job compute',
      'Serverless SQL Warehouse',
    ],
    a: [3],
    x: 'Un Serverless SQL Warehouse arranca casi al instante y se apaga tras periodos cortos de inactividad, eliminando el coste de inactividad sin penalizar la latencia. El clásico SQL Warehouse arranca más lento, el Single Node no resuelve el arranque ni escala para BI, y Job compute está pensado para trabajos automatizados, no consultas interactivas.',
  },
  {
    n: 7,
    d: 'P',
    m: 0,
    q: "A team needs to run a scheduled daily PySpark ETL workload writing to Unity Catalog-governed Delta tables. There's no need for interactive notebook collaboration, and the team wants Databricks to manage compute provisioning and scaling automatically with minimal operational overhead.\n\nWhich compute option is the best fit?",
    o: [
      'Classic jobs compute',
      'Serverless SQL Warehouse',
      'All-Purpose Cluster',
      'Serverless compute for jobs',
    ],
    a: [3],
    x: 'Serverless compute for jobs es la opción con menor sobrecarga operativa para cargas ETL programadas: Databricks gestiona el aprovisionamiento y el escalado automáticamente. El SQL Warehouse serverless está pensado para BI/SQL ad-hoc, no para PySpark ETL, y tanto el classic jobs compute como el All-Purpose exigen más gestión manual de infraestructura.',
  },
  {
    n: 8,
    d: 'P',
    m: 0,
    q: 'A team runs ad hoc BI queries from Tableau against a large lakehouse dataset. Query runtimes vary widely, concurrency is unpredictable, and the team wants fast query startup, automatic scaling for concurrent users, and no cluster management.\n\nWhich Databricks compute configuration best fits this workload?',
    o: [
      'Serverless SQL Warehouse',
      'Single-node All-Purpose Cluster',
      'Job compute with autoscaling',
      'All-purpose compute',
    ],
    a: [0],
    x: 'Un Serverless SQL Warehouse está diseñado específicamente para endpoints SQL/BI (Tableau, JDBC/ODBC): arranque casi instantáneo y autoescalado según la concurrencia sin gestión de clúster. Single-node no soporta concurrencia real, y tanto job compute como all-purpose no ofrecen el mismo perfil "cero-gestión" para BI interactivo.',
  },
  {
    n: 9,
    d: 'GOV',
    m: 0,
    q: 'A data analyst shares a view gold.marketing_sales_view (selecting from gold.sales_transactions, excluding sensitive columns) with the Marketing team. Marketing users have USE CATALOG, USE SCHEMA, and SELECT on the view, but not SELECT on the underlying table. The query runs from a SQL warehouse.\n\nWhat happens when a Marketing user runs SELECT * FROM gold.marketing_sales_view?',
    o: [
      'The query fails because the user does not have SELECT on gold.sales_transactions.',
      'The query succeeds, but Unity Catalog automatically masks all source columns.',
      'The query only succeeds if the user also has MODIFY on the view.',
      "The query succeeds because, on a SQL warehouse, Unity Catalog checks the view owner's permissions on the underlying table, while the user only needs access to the view itself.",
    ],
    a: [3],
    x: 'En un SQL warehouse, Unity Catalog valida los permisos del propietario de la vista sobre la tabla subyacente; el usuario solo necesita SELECT sobre la propia vista (más USE CATALOG/USE SCHEMA). No hace falta acceso directo a la tabla base, no hay enmascaramiento automático por defecto, y MODIFY no es necesario para consultar una vista.',
  },
  {
    n: 10,
    d: 'TRA',
    m: 0,
    q: 'A retail company needs a Silver table that keeps the full history of customer address changes as SCD Type 2, using the built-in declarative CDC pattern instead of a manual MERGE, in Lakeflow Spark Declarative Pipelines SQL.\n\nWhich statement should be used?',
    o: [
      'CREATE OR REFRESH STREAMING TABLE silver.customer_addresses AS SELECT * FROM STREAM(bronze.customer_address_changes)',
      'APPLY CHANGES INTO silver.customer_history FROM stream(live.bronze_customers) STORED AS SCD TYPE 1',
      'MERGE INTO silver.customer_history ... WHEN MATCHED AND source.address <> target.address THEN UPDATE SET ...',
      'AUTO CDC INTO silver.customer_addresses FROM stream(bronze.customer_address_changes) KEYS (customer_id) SEQUENCE BY change_timestamp STORED AS SCD TYPE 2',
    ],
    a: [3],
    x: 'AUTO CDC INTO ... STORED AS SCD TYPE 2 es la sintaxis actual de Lakeflow Spark Declarative Pipelines para implementar CDC declarativo con historial completo (cierra el registro anterior e inserta uno nuevo). APPLY CHANGES INTO es la API antigua y aquí además usa SCD TYPE 1 (sobrescribe, no conserva historial), y MERGE manual es justo lo que la pregunta pide evitar.',
  },
  {
    n: 11,
    d: 'ING',
    m: 0,
    q: 'A data engineer uses Auto Loader to incrementally ingest JSON files from S3 into a Delta table, where some files introduce new columns. The pipeline must persist and track schema changes over time and recover correctly across restarts without reprocessing already committed data.\n\nWhich Auto Loader configuration best satisfies both requirements?',
    o: [
      'Set cloudFiles.schemaLocation and cloudFiles.schemaEvolutionMode="addNewColumns", but no checkpointLocation on the write stream.',
      'Set checkpointLocation, but no cloudFiles.schemaLocation.',
      'Set cloudFiles.schemaLocation, checkpointLocation, and cloudFiles.schemaEvolutionMode="none".',
      'Set cloudFiles.schemaLocation, checkpointLocation, and cloudFiles.schemaEvolutionMode="addNewColumns".',
    ],
    a: [3],
    x: 'Se necesitan los tres elementos juntos: schemaLocation persiste y evoluciona el esquema, schemaEvolutionMode="addNewColumns" añade columnas nuevas automáticamente, y checkpointLocation garantiza el procesamiento exactly-once tras reinicios. Omitir cualquiera de ellos rompe uno de los dos requisitos (evolución de esquema o recuperación sin duplicados).',
  },
  {
    n: 12,
    d: 'TRA',
    m: 0,
    q: 'A data engineer is developing a Lakeflow Spark Declarative Pipelines pipeline and iterating quickly. Each update starts compute, runs, and then terminates, slowing down the edit-run-debug cycle.\n\nWhich pipeline configuration change is the best way to speed up iterative development?',
    o: [
      'Delete the pipeline storage location before every run.',
      'Enable enhanced autoscaling on the pipeline compute.',
      'Switch the pipeline from triggered mode to continuous mode.',
      'Set the pipeline to development mode.',
    ],
    a: [3],
    x: 'El modo development mantiene el clúster "caliente" entre ejecuciones, evitando el tiempo de arranque en cada iteración de prueba/depuración. Borrar el storage location es contraproducente, el autoscaling no afecta al tiempo de arranque, y el modo continuo está pensado para ejecución constante en producción, no para iterar rápido en desarrollo.',
  },
  {
    n: 13,
    d: 'JOBS',
    m: 0,
    q: 'A Lakeflow Jobs run fails on Task 3 because of an out-of-memory error, while Task 1 and Task 2 complete successfully. After increasing the memory configuration, the data engineer wants to rerun only the failed task without rerunning the successful upstream tasks.\n\nWhich Lakeflow Jobs feature should the engineer use?',
    o: [
      'Repair Run selecting only Task 3',
      'Clone the job and run only Task 3 manually',
      'Run now',
      'Cancel the failed run and create a new one-time run for Task 3',
    ],
    a: [0],
    x: 'Repair Run permite volver a ejecutar solo las tareas fallidas o seleccionadas dentro del mismo run original, reutilizando las salidas de las tareas ya completadas y preservando el historial. Clonar el job, usar "Run now" o crear un nuevo run puntual no conservan ese contexto ni evitan reprocesar las tareas exitosas.',
  },
  {
    n: 14,
    d: 'JOBS',
    m: 0,
    q: 'A multi-task Lakeflow Jobs workflow has: ingest_data → process_silver (depends on ingest_data) → send_alert (depends on process_silver). The team wants send_alert to run after process_silver finishes, regardless of success or failure.\n\nWhich task setting should be changed?',
    o: [
      'Enable Repair run on process_silver',
      'Change "Run if dependencies" from "All Succeeded" to "At Least One Failed"',
      'Change "Run if dependencies" from "All Succeeded" to "All Done"',
      'Remove the dependency on process_silver',
    ],
    a: [2],
    x: '"All Done" ejecuta la tarea dependiente sin importar si la tarea previa tuvo éxito o falló, que es justo lo que pide un envío de alerta que siempre debe dispararse. "At Least One Failed" solo cubriría el caso de fallo, "Repair run" es un mecanismo manual posterior al fallo, y quitar la dependencia rompería el orden de ejecución.',
  },
  {
    n: 15,
    d: 'CICD',
    m: 0,
    q: 'A team deploys 10 Databricks jobs to dev, staging, and prod by manually editing job settings in the UI. Each deployment takes about 30 minutes per environment. They now need a Git-triggered CI/CD approach with environment-specific cluster configurations for each environment.\n\nWhich Databricks approach should they adopt?',
    o: [
      'Databricks Repos plus manually managed scheduled jobs',
      'Lakeflow Jobs only',
      'Databricks CLI alone',
      'Declarative Automation Bundles (DAB)',
    ],
    a: [3],
    x: 'Los Declarative Automation Bundles (DAB) permiten definir jobs y pipelines como código, con overrides por entorno (targets), y se integran de forma nativa en flujos CI/CD. Repos gestiona solo el código fuente, Lakeflow Jobs orquesta pero no define infraestructura como código, y la CLI por sí sola es solo la herramienta de ejecución, no el modelo de despliegue.',
  },
  {
    n: 16,
    d: 'P',
    m: 1,
    q: 'A junior data engineer accidentally executed a DELETE FROM employees without a WHERE clause, deleting all records. DESCRIBE HISTORY shows Version 2 = last UPDATE (10:10), Version 3 = the bad DELETE (10:15). The goal is to revert the table to the state right after Version 2.\n\nWhich two SQL commands successfully achieve this restoration? (Select two)',
    o: [
      "```\nRESTORE TABLE employees TO TIMESTAMP AS OF '2026-01-07 10:12:00';\n```",
      '```\nROLLBACK TRANSACTION 3;\n```',
      '```\nALTER TABLE employees REVERT TO VERSION 2;\n```',
      '```\nRESTORE TABLE employees TO VERSION AS OF 2;\n```',
    ],
    a: [0, 3],
    x: "Delta Lake permite viajar en el tiempo tanto por versión (RESTORE ... TO VERSION AS OF 2) como por timestamp intermedio entre la versión buena y la mala (RESTORE ... TO TIMESTAMP AS OF '10:12:00'). ROLLBACK TRANSACTION no existe en Delta Lake (los commits son permanentes) y ALTER TABLE ... REVERT TO VERSION no es sintaxis válida; el único comando correcto es RESTORE.",
  },
  {
    n: 17,
    d: 'TRA',
    m: 1,
    q: "A data engineer compares the initial and final state of gold.employees after a nightly job: employee 501's salary increased, employee 503 disappeared, employee 504 is new, and full_name/department changed from lowercase to uppercase.\n\nWhich two operations were applied to produce this result? (Select two)",
    o: [
      'An UPDATE was applied specifically to the salary column where emp_id = 501.',
      'The upper() function was applied to the full_name and department columns.',
      'A MERGE operation was performed where emp_id 501 was updated, 503 was deleted, and 504 was inserted.',
      'A VACUUM command was run to remove the data files for emp_id 503 from physical storage.',
    ],
    a: [1, 2],
    x: 'El paso de minúsculas a MAYÚSCULAS en full_name/department indica el uso de upper(), y la combinación de actualización + baja + alta en una sola pasada (501 actualizado, 503 eliminado, 504 insertado) es el patrón típico de un MERGE con WHEN MATCHED/NOT MATCHED. Un UPDATE aislado no explica la fila nueva ni la eliminada, y VACUUM solo limpia archivos físicos, no cambia el estado lógico de la tabla.',
  },
  {
    n: 18,
    d: 'GOV',
    m: 0,
    q: "A marketing team needs to share live analytics tables with an external advertising agency for reporting in Power BI. The agency should have read-only access without copying data out and without creating accounts in the company's Databricks workspace.\n\nWhich Databricks feature enables this?",
    o: [
      'DBFS file sharing',
      'Lakehouse Federation',
      'Delta Sharing',
      'Unity Catalog grants across workspaces',
    ],
    a: [2],
    x: 'Delta Sharing permite compartir tablas Delta en modo lectura con destinatarios externos que no necesitan cuenta en el workspace del proveedor (open sharing), y herramientas como Power BI pueden consumirlas directamente. DBFS es solo almacenamiento de archivos, Lakehouse Federation sirve para consultar sistemas externos desde Databricks (dirección contraria), y los grants de Unity Catalog solo funcionan dentro del propio metastore.',
  },
  {
    n: 19,
    d: 'CICD',
    m: 0,
    q: 'A team is migrating to Declarative Automation Bundles for CI/CD. They need the same job deployed to dev and prod with different cluster settings in each environment.\n\nWhere are the environment-specific differences between dev and prod clusters defined?',
    o: [
      'In Terraform state files (e.g., terraform.tfstate) generated during deployment.',
      'In the bundle configuration YAML under environment-specific targets.',
      "In the Databricks Jobs UI, by manually editing the job's cluster after each deployment.",
      'Inside the notebook source code, using widgets or conditionals.',
    ],
    a: [1],
    x: 'En un DAB, databricks.yml define los recursos compartidos y la sección targets permite sobrescribir configuración específica de cada entorno (dev, prod), incluyendo el tamaño del clúster. El estado de Terraform es un artefacto de ejecución (no la fuente de verdad editable), y editar en la UI o en el propio notebook rompe el modelo de configuración como código.',
  },
  {
    n: 20,
    d: 'P',
    m: 0,
    q: 'A data team notices that Databricks Search and SQL Editor autocomplete understand company-specific terminology and schemas, combining generative AI with Unity Catalog signals such as lineage and popularity.\n\nWhat is the name of the underlying knowledge engine that powers this?',
    o: ['MLflow', 'MosaicML', 'DatabricksIQ', 'Photon'],
    a: [2],
    x: 'DatabricksIQ es el motor de inteligencia de datos de la plataforma: combina IA generativa con señales de Unity Catalog (lineage, popularidad de tablas, patrones de join) para potenciar la búsqueda inteligente y el asistente. MLflow gestiona el ciclo de vida de modelos ML, MosaicML es la infraestructura de entrenamiento de modelos, y Photon es el motor de ejecución vectorizado, no el motor semántico.',
  },
  {
    n: 21,
    d: 'P',
    m: 0,
    q: 'Which of the following best describes Delta Lake?',
    o: [
      'A serverless compute engine for SQL analytics',
      'A centralized metadata service for data lakes',
      'An open source storage layer that brings ACID transactions and reliability to data lakes.',
      'A Git integration tool for Databricks notebooks',
    ],
    a: [2],
    x: 'Delta Lake es una capa de almacenamiento open source que extiende ficheros Parquet con un log de transacciones basado en ficheros, aportando transacciones ACID, control de esquema y gestión de metadatos a escala. No es un motor de cómputo serverless (eso es SQL Warehouse), ni un servicio de metadatos centralizado (eso lo cubre Unity Catalog), ni una herramienta de integración con Git.',
  },
  {
    n: 22,
    d: 'P',
    m: 0,
    q: 'A data engineer manages a very large Delta table without liquid clustering. Analysts frequently filter it by region, customer_type, and order_date. The engineer wants to improve file pruning and data skipping for these query patterns.\n\nWhich command should be executed?',
    o: [
      '```\nOPTIMIZE delta_table ZORDER BY (region)\n```',
      '```\nOPTIMIZE delta_table ZORDER BY (region, customer_type, order_date)\n```',
      '```\nVACUUM delta_table RETAIN 168 HOURS\n```',
      'REFRESH TABLE delta_table',
    ],
    a: [1],
    x: 'Un ZORDER multi-columna (region, customer_type, order_date) co-localiza los datos según los tres predicados de filtro habituales, maximizando el data skipping cuando se combinan en las consultas. Un ZORDER de una sola columna solo optimiza esa dimensión, VACUUM únicamente limpia archivos antiguos (no reorganiza el layout), y REFRESH TABLE solo actualiza el caché del metastore.',
  },
  {
    n: 23,
    d: 'GOV',
    m: 0,
    q: 'A Unity Catalog storage credential aws_iam_role_finance already exists. An administrator wants a Lead Data Engineer to be able to create new external locations using this credential, but not edit the credential itself.\n\nWhich privilege should be granted on the storage credential?',
    o: ['`READ FILES`', '`CREATE EXTERNAL LOCATION`', '`USE CREDENTIAL`', '`ACCESS`'],
    a: [1],
    x: 'GRANT CREATE EXTERNAL LOCATION ON STORAGE CREDENTIAL <name> es el privilegio documentado que permite crear ubicaciones externas usando esa credencial, sin dar control administrativo sobre la propia credencial. READ FILES da acceso directo por ruta, USE CREDENTIAL no es un privilegio válido en la sintaxis de Unity Catalog, y ACCESS tampoco existe como privilegio estándar.',
  },
  {
    n: 24,
    d: 'ING',
    m: 0,
    q: 'A batch-style ingestion job runs once per day using Structured Streaming to load new IoT log files. Compute must stop after all currently available data is processed, and the backlog may be too large for a single micro-batch (so legacy Trigger.Once cannot be used).\n\nWhich trigger should be used?',
    o: [
      '`.trigger(processingTime="24 hours")`',
      '`Trigger.ProcessingTime("1 minute")`',
      '`.trigger(continuous="1 hour")`',
      '`.trigger(availableNow=True)`',
    ],
    a: [3],
    x: 'Trigger.AvailableNow procesa todo el backlog disponible dividiéndolo en varios micro-batches si es necesario, y luego detiene automáticamente el stream — sustituyendo a Trigger.Once para cargas grandes. Los triggers de processingTime mantienen el stream activo indefinidamente en intervalos fijos, y el modo continuous es para latencia ultra-baja, no para procesar backlog y parar.',
  },
  {
    n: 25,
    d: 'ING',
    m: 0,
    q: 'Auto Loader must: stop with an error if unexpected new columns appear, but still record those new columns in the schema so a later restart can use the updated schema, while existing columns keep their current data types.\n\nWhich cloudFiles.schemaEvolutionMode value should be used?',
    o: ['none', 'rescue', 'addNewColumns', 'failOnNewColumns'],
    a: [2],
    x: 'addNewColumns hace que el stream falle al detectar columnas nuevas, pero antes de fallar actualiza la ubicación del esquema fusionando las columnas nuevas al final — permitiendo que un reinicio posterior use el esquema ya actualizado. "none" no evoluciona ni falla, "rescue" captura los datos nuevos en una columna aparte sin evolucionar el esquema, y "failOnNewColumns" falla pero no persiste automáticamente el esquema nuevo.',
  },
  {
    n: 26,
    d: 'ING',
    m: 0,
    q: 'A logistics company lands hourly CSV files in S3 with shipment tracking data. Because of upstream retries, the same file can appear more than once. The ingestion process must continuously discover new files and provide exactly-once ingestion guarantees.\n\nWhich Databricks ingestion approach best meets these requirements?',
    o: [
      '```\nspark.readStream.format("cloudFiles").option("cloudFiles.format","csv").load(path)\n```',
      '```\nspark.read.csv(path).option("header","true")\n```',
      'Auto Loader CLI command',
      '```\nspark.readStream.format("delta").load(path)\n```',
    ],
    a: [0],
    x: 'Auto Loader (fuente cloudFiles) descubre incrementalmente los nuevos archivos y usa el checkpoint (con RocksDB) para garantizar ingesta exactly-once, evitando reprocesar archivos ya cargados por retries. spark.read.csv() es una lectura batch sin seguimiento de archivos, no existe una "CLI de Auto Loader" separada, y readStream.format("delta") lee cambios de una tabla Delta existente, no archivos CSV en S3.',
  },
  {
    n: 27,
    d: 'TRA',
    m: 0,
    q: 'A data engineer is transforming an orders DataFrame and needs to create a new column gross_amount by multiplying unit_price by quantity.\n\nWhich PySpark expression correctly creates this new column?',
    o: [
      '```\norders_df.withColumn("gross_amount", print(F.col("unit_price") * F.col("quantity")))\n```',
      '```\norders_df.withColumn("gross_amount", F.col("unit_price") * F.col("quantity"))\n```',
      '```\norders_df.select("unit_price","quantity").multiply()\n```',
      '```\norders_df.withColumn("gross_amount", multiply_values("unit_price","quantity"))\n```',
    ],
    a: [1],
    x: 'withColumn acepta una expresión de columna Spark válida; F.col("unit_price") * F.col("quantity") es exactamente eso. print() devuelve None (no una columna), .multiply() no existe como método de DataFrame, y pasar strings de nombre de columna a una función Python arbitraria no produce una expresión Column a menos que esa función esté implementada para ello.',
  },
  {
    n: 28,
    d: 'ING',
    m: 0,
    q: 'A junior data engineer develops PySpark code locally in VS Code and needs to execute Spark code against remote Databricks compute without using notebooks, so they can iterate faster from their IDE.\n\nWhich tool enables this workflow?',
    o: ['Databricks Connect', 'dbutils library', 'Databricks Repos', 'Databricks CLI'],
    a: [0],
    x: 'Databricks Connect actúa como puente entre el IDE local y el clúster remoto: se crea una SparkSession que ejecuta el código Spark local sobre el clúster de Databricks, sin necesidad de notebooks. dbutils es una librería de utilidades en tiempo de ejecución (no un puente de conexión), Repos gestiona el código en Git dentro del workspace, y la CLI automatiza despliegues, no ejecución interactiva remota.',
  },
  {
    n: 29,
    d: 'CICD',
    m: 1,
    q: 'A Lakeflow Jobs run (Extract → Transform → Load, each with its own "New Job Cluster") takes over 30 minutes against a 20-minute SLA. Actual execution time is short, but the job spends significant time waiting for compute provisioning for each task.\n\nWhich two configuration changes would effectively reduce the total job duration by eliminating or minimizing provisioning wait time? (Select two)',
    o: [
      'Configure the job to run on an existing "All-Purpose Compute" cluster kept running 24/7.',
      'Configure the Transform and Load tasks to use the same shared Job Cluster as the Extract task.',
      'Switch the compute type for all tasks to "Serverless Compute for Jobs".',
      'Increase the number of worker nodes in the cluster configuration.',
    ],
    a: [1, 2],
    x: 'Compartir un único Job Cluster entre las tres tareas paga el coste de aprovisionamiento solo una vez (en vez de tres), y el cómputo serverless para jobs arranca casi al instante, eliminando prácticamente el tiempo de espera. Mantener un All-Purpose 24/7 elimina la espera pero es caro e ineficiente, y añadir más workers acelera la ejecución, no el tiempo de aprovisionamiento (que es el cuello de botella real aquí).',
  },
  {
    n: 30,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to calculate the Year-to-Date (YTD) Running Total of revenue for each store, showing accumulated revenue increasing day by day independently per store.\n\nWhich Spark SQL window function syntax correctly calculates this?',
    o: [
      '```\nSUM(daily_revenue) OVER (PARTITION BY report_date ORDER BY store_id)\n```',
      '```\nSUM(daily_revenue) OVER (PARTITION BY store_id ORDER BY report_date)\n```',
      '```\nRANK() OVER (PARTITION BY store_id ORDER BY daily_revenue DESC)\n```',
      '```\nLEAD(daily_revenue) OVER (PARTITION BY store_id ORDER BY report_date)\n```',
    ],
    a: [1],
    x: 'PARTITION BY store_id aísla el cálculo por tienda y ORDER BY report_date define la secuencia de acumulación; una función de agregación (SUM) sobre esa ventana produce el total acumulado correcto. Particionar por fecha mezclaría tiendas distintas, RANK() no suma nada (solo ordena), y LEAD() consulta un valor futuro, no acumula.',
  },
  {
    n: 31,
    d: 'ING',
    m: 1,
    q: 'A data engineer needs to: (1) reuse variables and helper functions defined in another notebook, (2) install a Python package only for the current notebook session, and (3) run a shell command like pwd on the driver.\n\nWhich pair of Databricks magic commands directly supports the first two requirements?',
    o: ['%python', '%pip', '%sh', '%run'],
    a: [1, 3],
    x: '%run ejecuta otro notebook trayendo sus variables y funciones al ámbito del notebook actual (requisito 1), y %pip instala una librería con alcance limitado a la sesión del notebook (requisito 2). %python solo fija el lenguaje de la celda y %sh ejecuta comandos de shell en el driver (útil para el requisito 3, pero no para los dos primeros).',
  },
  {
    n: 32,
    d: 'GOV',
    m: 0,
    q: 'A data engineer needs a Unity Catalog dynamic view for employee data with column-level masking: hr_admins see the real salary, everyone else sees NULL, and emp_id/name/dept remain visible to everyone.\n\nWhich SQL statement correctly defines this dynamic view?',
    o: [
      "```\nSELECT emp_id, name, dept FROM employees GRANT SELECT ON salary TO 'hr_admins';\n```",
      "```\nSELECT emp_id, name, mask(salary, 'hr_admins') AS salary, dept FROM employees;\n```",
      "SELECT emp_id, name, CASE WHEN is_account_group_member('hr_admins') THEN salary ELSE NULL END AS salary, dept FROM employees;",
      "```\nSELECT emp_id, name, salary, dept FROM employees WHERE is_account_group_member('hr_admins');\n```",
    ],
    a: [2],
    x: 'El patrón documentado para vistas dinámicas usa is_account_group_member() dentro de un CASE WHEN en la lista SELECT, devolviendo el valor real o NULL según el grupo del usuario que consulta. GRANT SELECT ON salary no es sintaxis válida, mask() no es una función genérica de vistas dinámicas, y filtrar con WHERE ocultaría filas enteras en vez de enmascarar solo la columna salary.',
  },
  {
    n: 33,
    d: 'GOV',
    m: 0,
    q: 'A team needs to know which role has metastore-level authority to create new catalogs within a given Unity Catalog metastore.\n\nWhich role has this authority by default?',
    o: ['Account Admin', 'Metastore Admin', 'Workspace Admin', 'Catalog Owner'],
    a: [1],
    x: 'Los Metastore Admins tienen por defecto el privilegio CREATE CATALOG a nivel de metastore, que es el requisito exacto para crear catálogos. Account Admin gestiona configuración de cuenta pero no tiene acceso a datos por defecto, Workspace Admin es un rol de ámbito de workspace (no de metastore), y "Catalog Owner" es relevante para gestionar un catálogo ya existente, no para crearlo.',
  },
  {
    n: 34,
    d: 'TRA',
    m: 0,
    q: 'A retail company ingests raw clickstream JSON from Kafka into the Bronze layer, cleans and standardizes it in Silver, and produces business-ready tables in Gold for Tableau dashboards.\n\nIn the Medallion Architecture, what is the primary purpose of the Bronze layer?',
    o: [
      'Land and persist raw, append-oriented data from source systems with minimal transformation.',
      'Apply business transformations and create Tableau-ready tables.',
      'Enforce strict data quality rules and repair invalid records before downstream use.',
      'Store aggregated metrics for machine learning features and executive reporting.',
    ],
    a: [0],
    x: 'Bronze es la capa de aterrizaje de datos crudos, con transformación mínima, que sirve de base duradera y auditable para el resto del pipeline. Las transformaciones de negocio y las tablas listas para BI pertenecen a Gold, la limpieza estricta y validación de calidad pertenece a Silver, y las métricas agregadas para ML/reporting son responsabilidad de Gold, no de Bronze.',
  },
  {
    n: 35,
    d: 'GOV',
    m: 0,
    q: 'A global company sees inconsistent dashboard metrics due to data drift and schema issues. Leadership wants a native Databricks solution that automatically profiles data, detects anomalies (freshness, completeness), and centralizes data quality insights across catalogs and schemas without custom code per team.\n\nWhich Databricks feature best meets these requirements?',
    o: [
      'Custom PySpark validation jobs scheduled in Lakeflow Jobs per pipeline',
      'Databricks Data Quality Monitoring with anomaly detection',
      'Lakeflow Spark Declarative Pipelines expectations',
      'Lakehouse Monitoring metrics defined only in BI dashboards per workspace',
    ],
    a: [1],
    x: 'Data Quality Monitoring con detección de anomalías es la capacidad nativa de Unity Catalog que perfila datos y evalúa frescura/completitud a nivel de catálogo o esquema, centralizando los resultados sin código por equipo. Las validaciones custom en PySpark son justo lo que la pregunta quiere evitar, y las expectations de Lakeflow operan solo a nivel de un pipeline concreto, no de forma centralizada en todo Unity Catalog.',
  },
  {
    n: 36,
    d: 'P',
    m: 0,
    q: "A security auditor requires that raw data in cloud storage must never be persisted in Databricks-managed backend services. Data should only be processed by compute resources running inside the customer's cloud network.\n\nWhich Databricks architectural component contains the compute clusters that process the data and meets this requirement?",
    o: [
      'The JDBC Gateway',
      'The Control Plane',
      'The Customer Managed Data Plane',
      'The Unity Catalog Metastore',
    ],
    a: [2],
    x: 'El data plane (compute plane) clásico reside en la cuenta cloud del propio cliente, incluyendo la VPC y los clústeres que procesan realmente los datos, cumpliendo así el requisito de que el procesamiento nunca salga de la red del cliente. El Control Plane aloja los servicios gestionados por Databricks (no procesa datos ahí), el JDBC Gateway solo enruta conexiones, y el metastore de Unity Catalog almacena metadatos, no los datos en sí.',
  },
  {
    n: 37,
    d: 'CICD',
    m: 0,
    q: 'A team maintains 15 Databricks ETL pipelines deployed to dev, staging, and prod. They want a single Git repository using Declarative Automation Bundles so shared pipeline definitions are maintained once, each environment applies its own cluster/schedule settings, and GitHub Actions runs validate → deploy → run.\n\nWhich repository structure best supports this?',
    o: [
      'Keep shared resource definitions in common bundle configuration files and define environment-specific overrides under targets in databricks.yml.',
      'Store separate YAML roots per environment and select which root file to deploy in CI.',
      'Put environment-specific cluster and schedule logic directly into notebook code using conditionals.',
      'Keep one complete pipeline YAML file per environment by duplicating the full configuration.',
    ],
    a: [0],
    x: 'Un bundle debe tener un único databricks.yml raíz; la configuración compartida vive allí (o en ficheros incluidos) y las diferencias por entorno se definen bajo targets, evitando duplicación. Tener varios "roots" o duplicar el YAML completo por entorno multiplica el mantenimiento, y poner lógica de infraestructura en el propio notebook mezcla responsabilidades que deben estar en configuración declarativa.',
  },
  {
    n: 38,
    d: 'P',
    m: 0,
    q: 'A script accidentally executed DROP TABLE on finance.transactions, a Unity Catalog Managed Table. What is the status of the underlying data files in storage after the DROP TABLE command completes?',
    o: [
      'The table metadata is removed immediately, and the underlying files are retained for a limited recovery window (UNDROP), then physically deleted later by maintenance.',
      'The metadata is removed, but the data files remain in the storage location indefinitely.',
      'The DROP command fails by default on Managed Tables to prevent accidental data loss.',
      'The metadata is removed and the data files are moved to a temporary directory for 7 days managed by Databricks.',
    ],
    a: [0],
    x: 'Para tablas managed de Unity Catalog, el DROP TABLE quita la tabla del catálogo de inmediato, pero los ficheros se conservan un periodo de recuperación (UNDROP TABLE disponible durante 7 días) antes de ser eliminados físicamente en el mantenimiento posterior. Los ficheros no sobreviven indefinidamente (eso es propio de tablas external), el comando no falla por defecto, y no existe un "directorio de undrop" a nivel de almacenamiento gestionado por UC de esa forma.',
  },
  {
    n: 39,
    d: 'TRA',
    m: 0,
    q: "A Delta table ecommerce.orders has a column items of type ARRAY<STRUCT<item_id, price, status>>. The requirement is to keep only elements where status = 'ACTIVE', preserving one row per order without exploding the array.\n\nWhich Spark SQL function should be used?",
    o: [
      "`FILTER(items, i -> i.status = 'ACTIVE')`",
      "`TRANSFORM(items, i -> i.status = 'ACTIVE')`",
      '`POSEXPLODE(items)`',
      "`EXPLODE(items) WHERE status = 'ACTIVE'`",
    ],
    a: [0],
    x: 'FILTER es una función de orden superior que filtra los elementos de un array usando una lambda y devuelve un array del mismo tipo, manteniendo la granularidad de una fila por pedido. TRANSFORM mapea valores pero no elimina elementos, y tanto EXPLODE como POSEXPLODE convierten cada elemento en una fila nueva, rompiendo el requisito de preservar el grano original.',
  },
  {
    n: 40,
    d: 'ING',
    m: 0,
    q: 'A Databricks Structured Streaming job uses Auto Loader and writes to a Delta table. The stream fails with an AnalysisException when the upstream source adds a new column. The engineer wants the target Delta table schema to evolve automatically so the stream can continue appending data.\n\nWhich option should be added to the writeStream configuration?',
    o: [
      '```\n.option("cloudFiles.schemaEvolutionMode", "addNewColumns")\n```',
      '```\n.option("mergeSchema", "true")\n```',
      '```\n.option("overwriteSchema", "true")\n```',
      '```\n.mode("append")\n```',
    ],
    a: [1],
    x: 'mergeSchema="true" es la opción de escritura que permite que el sink Delta evolucione su esquema cuando llegan columnas nuevas en el DataFrame de entrada. cloudFiles.schemaEvolutionMode es una opción del lado de lectura de Auto Loader (no del sink), overwriteSchema está pensado para operaciones de sobrescritura (no de append), y .mode("append") por sí solo no habilita ninguna evolución de esquema.',
  },
  {
    n: 41,
    d: 'JOBS',
    m: 0,
    q: 'A Lakeflow Jobs workflow with five tasks fails at Task 3 (OOM) after Tasks 1 and 2 succeed. After increasing compute, the team wants to rerun only Task 3 using the same upstream outputs, without rerunning Tasks 1 and 2, while preserving the original run history.\n\nWhich Lakeflow Jobs feature supports this?',
    o: [
      'Use the Lakeflow Jobs Repair run feature',
      'Rerun the entire workflow with a larger cluster for Task 3',
      'Clone the successful Task 1 and Task 2 outputs and run Task 3 as a new job',
      'Cancel the current run and manually rerun Task 3 from the Jobs UI',
    ],
    a: [0],
    x: 'Repair run vuelve a ejecutar únicamente las tareas fallidas o seleccionadas dentro del run original, conservando las salidas de las tareas exitosas y el historial del run. Rerun completo desperdicia trabajo ya hecho, clonar outputs es un workaround manual, y cancelar/rerun manual no usa el mecanismo de reparación nativo ni preserva el contexto del run.',
  },
  {
    n: 42,
    d: 'CICD',
    m: 0,
    q: 'A data team is building a churn model using multiple Databricks notebooks. They want a single approach supporting Git-based version control, reusable code across notebooks, parameterized production runs, and automated deployment/scheduling.\n\nWhich Databricks feature best addresses these requirements?',
    o: [
      'Declarative Automation Bundles (DAB)',
      'Multi-task jobs with notebook parameters',
      'Lakeflow Jobs with notebook tasks',
      'Databricks Repos',
    ],
    a: [0],
    x: 'Los DAB combinan control de versiones, empaquetado de código, parametrización y despliegue/programación automatizados en un único enfoque de infraestructura como código. Los jobs multi-tarea o Lakeflow Jobs orquestan pero no proveen por sí solos el empaquetado ni el despliegue como código, y Repos solo cubre el control de versiones del código fuente.',
  },
  {
    n: 43,
    d: 'P',
    m: 0,
    q: "When working with Delta Lake tables in Databricks, data is physically stored on disk using a specific file format.\n\nWhich file format is used as the main storage format for the table's data files?",
    o: ['JSON', 'Delta', 'Parquet', 'ORC'],
    a: [2],
    x: 'Delta Lake extiende ficheros Parquet añadiendo un log de transacciones (en JSON) para aportar ACID y gestión de metadatos escalable; los datos en sí se almacenan como Parquet. "Delta" no es un formato físico de fichero independiente, JSON se usa solo para el log de transacciones, y ORC no es el formato que documenta Databricks para Delta Lake.',
  },
  {
    n: 44,
    d: 'P',
    m: 0,
    q: 'A team deploys a Databricks workspace using the standard non-serverless architecture and needs to configure outbound network access from cluster compute to reach a private internal repository.\n\nWhich statement correctly describes where standard cluster compute resources run in this architecture?',
    o: [
      'They run in the compute/data plane inside a Databricks-managed cloud account.',
      'They run in the control plane inside the Databricks-managed cloud account.',
      'They run in the compute/data plane inside the customer cloud account.',
      'They run in the Databricks control plane, so outbound allowlisting must be configured there.',
    ],
    a: [2],
    x: 'Para el cómputo clásico (no serverless), el compute/data plane incluye una VPC dentro de la cuenta cloud del propio cliente, donde realmente corren las VMs del clúster; por eso el allowlisting de red se configura del lado del cliente. El control plane siempre lo gestiona Databricks (no aloja las VMs de cómputo clásico), y el escenario "compute plane en cuenta de Databricks" corresponde más al modelo serverless, no al estándar no-serverless.',
  },
  {
    n: 45,
    d: 'TRA',
    m: 0,
    q: 'A Lakeflow Spark Declarative Pipelines pipeline ingests streaming order data into a Bronze table. In Silver, the team wants to reject records where order_total < 0, continue processing when violations occur, and not write invalid records to Silver.\n\nWhich expectation syntax should be used?',
    o: [
      '```\n@dp.expect_or_drop("valid_total", "order_total >= 0")\n```',
      'Manually filter with .filter("order_total >= 0") instead of using an expectation decorator.',
      '```\n@dp.expect("valid_total", "order_total >= 0")\n```',
      '```\n@dp.expect_or_fail("valid_total", "order_total >= 0")\n```',
    ],
    a: [0],
    x: 'expect_or_drop descarta las filas que incumplen la condición antes de escribirlas en el dataset destino, mientras el pipeline sigue funcionando con normalidad — exactamente lo que pide el requisito. expect() mantiene las filas inválidas (solo registra métricas), expect_or_fail detiene toda la actualización ante una violación, y un filtro manual no aprovecha el framework declarativo de expectations con sus métricas asociadas.',
  },
];

export const exam8: RawQuestion[] = [
  {
    n: 1,
    d: 'TRA',
    m: 0,
    q: 'In Databricks SQL, CTAS (CREATE TABLE AS SELECT) statements are commonly used to create new tables from query results.\n\nWhich statement about CTAS is NOT correct?',
    o: [
      'When using CTAS, the target table is populated with data when it is created.',
      'CTAS is an abbreviation for CREATE TABLE AS SELECT.',
      'CTAS allows the table schema to be explicitly defined column by column by the user.',
      'CTAS derives the table schema from the output of the SELECT query.',
    ],
    a: [2],
    x: 'En CTAS el esquema se infiere automáticamente a partir de la consulta SELECT; no se declara columna por columna dentro de la propia sentencia CTAS. Las otras tres afirmaciones sí son correctas: CTAS inserta los datos en el mismo paso de creación, es efectivamente el acrónimo de CREATE TABLE AS SELECT, y el esquema proviene de la salida de la query.',
  },
  {
    n: 2,
    d: 'ING',
    m: 1,
    q: 'A company is ingesting millions of small CSV files from ADLS Gen2 unpredictably throughout the day. The ingestion strategy needs idempotency, must scale without listing millions of files on every trigger, and must infer schema/handle new columns automatically.\n\nWhich two technologies or features are most appropriate? (Select two)',
    o: [
      'A daily Python job that uses dbutils.fs.ls() to find new files.',
      'Auto Loader with Schema Inference and Evolution enabled.',
      'The COPY INTO SQL command with a WHERE clause for timestamps.',
      'Auto Loader with File Notification mode.',
    ],
    a: [1, 3],
    x: 'Auto Loader con inferencia/evolución de esquema detecta columnas nuevas automáticamente sin actualizar la tabla manualmente, y el modo de notificación de archivos (colas de eventos cloud) evita el listado costoso del directorio a gran escala. dbutils.fs.ls() no escala a millones de archivos y no da idempotencia nativa, y COPY INTO también depende de listado de directorio, degradándose a esa escala.',
  },
  {
    n: 3,
    d: 'GOV',
    m: 0,
    q: 'A multi-tenant e-commerce platform must share aggregated sales summary tables with external partners for joint forecasting, without copying data, across clouds, while preserving governance.\n\nWhich approach best enables this?',
    o: [
      'Use Delta Sharing to provide recipients a secure share for querying Delta tables without data copying.',
      'Create external tables over shared object storage paths that partners access directly.',
      'Export the data to CSV files and share them through signed cloud storage URLs.',
      'Replicate the tables into partner workspaces using pipelines and then grant access there.',
    ],
    a: [0],
    x: 'Delta Sharing es el protocolo abierto de Databricks para compartir tablas Delta en modo lectura entre organizaciones y nubes distintas sin copiar datos, manteniendo la gobernanza. Las otras opciones implican exponer rutas de almacenamiento directamente, exportar/copiar archivos o replicar datos — justo lo que el requisito quiere evitar.',
  },
  {
    n: 4,
    d: 'GOV',
    m: 0,
    q: 'In the silver schema, the analytics group must read tables, and the data_scientists group must read tables and create new tables in silver, but must not receive MODIFY privileges.\n\nWhich SQL command correctly grants the additional permissions needed for data_scientists?',
    o: [
      '```\nGRANT SELECT ON CATALOG sales_data TO analysts, data_scientists;\n```',
      '```\nGRANT ALL PRIVILEGES ON SCHEMA silver TO analytics;\n```',
      'GRANT USE SCHEMA, SELECT, CREATE TABLE ON SCHEMA silver TO `data_scientists`;',
      'GRANT MODIFY ON SCHEMA silver TO `data_scientists`;',
    ],
    a: [2],
    x: 'USE SCHEMA, SELECT y CREATE TABLE sobre el esquema silver da exactamente lectura + capacidad de crear tablas, sin conceder escritura sobre datos existentes (MODIFY). ALL PRIVILEGES es excesivo (viola el mínimo privilegio), MODIFY otorga insert/update/delete que el requisito prohíbe explícitamente, y SELECT sobre CATALOG apunta al objeto equivocado.',
  },
  {
    n: 5,
    d: 'ING',
    m: 0,
    q: 'Auto Loader must automatically detect new columns in JSON logs and evolve the Auto Loader schema as new columns appear. It is acceptable for the stream to stop with UnknownFieldException and be restarted automatically by the configured job.\n\nWhich configuration option must be added to satisfy this requirement?',
    o: [
      '```\n.option("mergeSchema", "true")\n```',
      '```\n.option("cloudFiles.schemaEvolutionMode", "rescue")\n```',
      '```\n.option("cloudFiles.schemaEvolutionMode", "addNewColumns")\n```',
      '```\n.option("cloudFiles.inferColumnTypes", "true")\n```',
    ],
    a: [2],
    x: 'addNewColumns es el modo de evolución de Auto Loader que actualiza el esquema con las columnas nuevas y provoca que el stream se detenga para que el job lo reinicie con el esquema ya actualizado. mergeSchema es una opción de escritura Delta (no de Auto Loader), "rescue" oculta las columnas nuevas en una columna aparte sin evolucionar el esquema, e inferColumnTypes solo controla el tipado, no la evolución.',
  },
  {
    n: 6,
    d: 'P',
    m: 0,
    q: 'A business analyst wants to connect Tableau Desktop to a Databricks SQL warehouse without manually gathering connection details, using a simplified workflow inside Databricks.\n\nWhich Databricks feature provides this streamlined integration experience?',
    o: ['Lakehouse Federation', 'Databricks Marketplace', 'System Tables', 'Partner Connect'],
    a: [3],
    x: 'Partner Connect es la experiencia guiada dentro de Databricks para conectar un SQL warehouse con herramientas partner como Tableau en pocos clics, incluyendo la descarga del fichero de conexión. Lakehouse Federation sirve para consultar bases de datos externas desde Databricks (dirección contraria), Marketplace es para descubrir datasets/productos, y System Tables son para observabilidad, no para configurar conectores.',
  },
  {
    n: 7,
    d: 'TRA',
    m: 1,
    q: 'A "Silver" table tracks user login sessions with duplicates due to retry logic. The engineer needs to keep only the first occurrence of each session based on login_time.\n\nWhich two operations were applied to produce the deduplicated result? (Select two)',
    o: [
      'The window was ordered by login_time in ascending (ASC) order.',
      'The window was ordered by login_time in descending (DESC) order.',
      'A window function row_number() was created, partitioned by session_id.',
      'A dropDuplicates(["user_id"]) operation was performed.',
    ],
    a: [0, 2],
    x: 'row_number() particionado por session_id agrupa las filas de cada sesión, y ordenar por login_time ASC asigna el número 1 al registro más antiguo (el "primero"), que luego se filtra con row_num = 1. Ordenar DESC daría prioridad al más reciente (lo contrario de lo pedido), y dropDuplicates(["user_id"]) colapsaría todas las sesiones de un mismo usuario, no solo los duplicados de una sesión.',
  },
  {
    n: 8,
    d: 'ING',
    m: 0,
    q: "A data engineer has a utility notebook config_setup containing shared variables and helper functions, and needs those variables and functions to become available in the current notebook's scope.\n\nWhich command satisfies this requirement?",
    o: [
      '`import config_setup`',
      '`dbutils.fs.head("./config_setup")`',
      '`dbutils.notebook.run("./config_setup", 60)`',
      '%run ./config_setup',
    ],
    a: [3],
    x: '%run ejecuta el notebook indicado y trae sus variables, clases y funciones al ámbito del notebook que lo invoca. import no aplica a notebooks de Databricks de esta forma, dbutils.fs.head solo lee el contenido como texto, y dbutils.notebook.run ejecuta el notebook de forma aislada (job efímero), sin exponer sus variables al notebook llamador.',
  },
  {
    n: 9,
    d: 'ING',
    m: 0,
    q: 'A data engineer needs to build a Structured Streaming pipeline in PySpark to continuously ingest event data from Apache Kafka.\n\nWhich code block correctly creates a streaming DataFrame that consumes data from a Kafka topic?',
    o: [
      '```\nspark.readStream.format("cloudFiles").option("cloudFiles.format","kafka")...\n```',
      '```\nspark.readStream.format("kafka").load(host_port, events_topic, "latest")\n```',
      '```\nspark.readStream.format("kafka").option("kafka.bootstrap.servers", host_port).option("subscribe", events_topic).option("startingOffsets","latest").load()\n```',
      '```\nspark.readStream.kafka(host_port, events_topic, "latest")\n```',
    ],
    a: [2],
    x: 'La forma canónica de leer Kafka con Structured Streaming es format("kafka") junto con las opciones kafka.bootstrap.servers, subscribe y startingOffsets, seguido de .load(). cloudFiles es la fuente de Auto Loader para archivos (no Kafka), .load() no acepta argumentos posicionales, y no existe un método .kafka() dedicado en readStream.',
  },
  {
    n: 10,
    d: 'TRA',
    m: 0,
    q: 'In a Lakeflow Spark Declarative Pipelines Silver dataset, records with negative transaction_total must not be written to the target, valid records must continue processing, and the pipeline must remain operational.\n\nWhich syntax correctly implements this requirement?',
    o: [
      '```\n@dp.expect("valid_total", "transaction_total >= 0")\n```',
      'A generic @dlt.constraint decorator with a mode parameter set to "drop".',
      '```\n@dp.expect_or_drop("valid_total", "transaction_total >= 0")\n```',
      '```\n@dp.expect_or_fail("valid_total", "transaction_total >= 0")\n```',
    ],
    a: [2],
    x: 'expect_or_drop descarta las filas que incumplen la condición antes de escribir en el destino, mientras el pipeline sigue procesando el resto sin interrupción. expect() mantiene las filas inválidas en la tabla, expect_or_fail detiene toda la actualización ante cualquier violación, y no existe un decorador genérico @dlt.constraint con parámetro de modo en la API de Lakeflow.',
  },
  {
    n: 11,
    d: 'ING',
    m: 0,
    q: 'A real-time attribution pipeline joins two streaming Delta sources (impressions and clicks). A click can arrive up to 2 hours after the matching impression, and the join state keeps growing, causing memory pressure.\n\nWhich Structured Streaming function should be applied to the event-time columns to bound state?',
    o: [
      '`.repartition(200)`',
      '`.trigger(processingTime="2 hours")`',
      '`.withWatermark("event_time", "2 hours")`',
      '`.checkpoint("dbfs:/checkpoints/attribution")`',
    ],
    a: [2],
    x: 'withWatermark("event_time","2 hours") define hasta cuándo esperar datos tardíos, permitiendo que Spark descarte de forma segura el estado más antiguo que ese umbral y así acote el crecimiento de memoria en el join con estado. repartition solo redistribuye datos, el trigger solo controla la cadencia de ejecución, y checkpoint únicamente persiste el estado en disco para tolerancia a fallos, sin limitar su tamaño.',
  },
  {
    n: 12,
    d: 'CICD',
    m: 1,
    q: 'A team wants Databricks CI/CD with code versioned in Git, the same project promotable across dev/test/prod, environment-specific configuration without duplicating the codebase, and automated validation/deployment.\n\nWhich two actions best align with Databricks CI/CD best practices? (Select two)',
    o: [
      'Store environment-specific cluster settings directly inside notebook code with conditional logic.',
      'Use Declarative Automation Bundles with environment-specific targets and overrides to deploy the same codebase across dev, test, and prod.',
      'Use Databricks Repos to create and switch branches, commit changes, and collaborate through Git-integrated development.',
      'Keep one completely separate copy of the project for each environment to avoid configuration conflicts.',
    ],
    a: [1, 2],
    x: 'Los DAB con targets/overrides permiten desplegar el mismo código a distintos entornos con configuración diferenciada, y Repos aporta el control de versiones Git dentro del workspace (ramas, commits, PRs). Meter configuración de infraestructura en el propio notebook o duplicar el proyecto completo por entorno rompe el principio de "un solo código fuente, múltiples entornos".',
  },
  {
    n: 13,
    d: 'P',
    m: 0,
    q: 'A retail analytics team wants to enrich internal sales data with third-party historical weather and demographic datasets, browsing available external data products from verified providers instead of building custom ingestion.\n\nWhich Databricks feature should the team use?',
    o: [
      'Databricks Marketplace',
      'Lakehouse Federation',
      'Lakeflow Spark Declarative Pipelines',
      'Partner Connect',
    ],
    a: [0],
    x: 'Databricks Marketplace es el foro abierto para descubrir e instalar productos de datos de terceros (datasets, modelos, notebooks) sin construir ingestión personalizada. Lakehouse Federation conecta con bases de datos operacionales propias, Lakeflow es un framework de ETL (no un catálogo de datos externos), y Partner Connect conecta con herramientas partner, no con datasets comerciales.',
  },
  {
    n: 14,
    d: 'ING',
    m: 0,
    q: 'Auto Loader ingests JSON files where the upstream application frequently adds new fields. The engineer wants Auto Loader to automatically record new columns in the schema and allow the pipeline to resume successfully after restart, without manually editing the schema.\n\nWhich Auto Loader option best matches this behavior?',
    o: [
      '```\n.option("mergeSchema", "true")\n```',
      '```\n.option("cloudFiles.format", "delta")\n```',
      '```\n.option("spark.sql.streaming.schemaInference", "true")\n```',
      '```\n.option("cloudFiles.schemaEvolutionMode", "addNewColumns")\n```',
    ],
    a: [3],
    x: 'addNewColumns es la opción de Auto Loader que registra las columnas nuevas en la ubicación del esquema y permite que el pipeline retome tras un reinicio usando ese esquema actualizado. mergeSchema es una opción de escritura Delta (no de lectura Auto Loader), format("delta") sería para leer una tabla Delta existente (no JSON crudo), y la opción genérica de inferencia de esquema de Spark no gestiona la evolución continua.',
  },
  {
    n: 15,
    d: 'P',
    m: 0,
    q: 'A Databricks platform administrator wants to reduce classic compute costs using cloud provider spot/preemptible instances, which can be reclaimed at any time.\n\nWhich workload type is the most appropriate candidate for running on spot instances?',
    o: [
      'High-throughput transactional systems requiring constant availability.',
      'Continuous streaming applications with strict latency guarantees.',
      'Batch-oriented jobs that can tolerate interruptions and be retried.',
      'Ad-hoc exploratory analysis performed interactively by analysts.',
    ],
    a: [2],
    x: 'Los trabajos batch programados suelen poder reintentarse y no son de cara al usuario en tiempo real, por lo que toleran bien la posible interrupción de una instancia spot. Los sistemas transaccionales, el streaming con SLA estricto y el análisis interactivo necesitan disponibilidad y estabilidad constantes, algo que las instancias spot no garantizan.',
  },
  {
    n: 16,
    d: 'GOV',
    m: 0,
    q: 'An external location ext_finance_loc already exists. A lead data engineer needs to create a new external table whose data files live inside this existing external location, without creating new external locations or managing storage credentials.\n\nWhich privilege must the administrator grant on the external location object?',
    o: ['`USE CREDENTIAL`', '`CREATE EXTERNAL TABLE`', '`CREATE EXTERNAL LOCATION`', '`READ FILES`'],
    a: [1],
    x: 'CREATE EXTERNAL TABLE concedido sobre la external location permite crear tablas cuyos ficheros residen dentro de esa ubicación ya existente, sin dar control sobre la credencial ni permitir crear nuevas ubicaciones. USE CREDENTIAL no es un privilegio válido de Unity Catalog, CREATE EXTERNAL LOCATION serviría para crear ubicaciones nuevas (innecesario aquí), y READ FILES solo permite lectura directa por ruta, no la creación de tablas.',
  },
  {
    n: 17,
    d: 'TRA',
    m: 1,
    q: 'A "Silver" table tracks delivery package status changes. The requirement is to show only the latest status event per package, but only for packages whose latest status is "Delivered".\n\nWhich two operations were applied to produce this result? (Select two)',
    o: [
      'A row_number() window function was created over partition by package_id order by update_time DESC.',
      'A filter was applied to keep only rows where row_num = 1 and status = "Delivered".',
      'A limit(2) clause was applied to the final DataFrame.',
      'The dropDuplicates(["package_id"]) method was used after sorting by update_time ascending.',
    ],
    a: [0, 1],
    x: "row_number() particionado por package_id y ordenado por update_time DESC identifica el evento más reciente de cada paquete, y filtrar por row_num = 1 AND status = 'Delivered' combina la selección del último evento con la condición de negocio. limit(2) no aplica ninguna lógica de negocio, y ordenar ascendente con dropDuplicates conservaría el evento más antiguo, no el más reciente.",
  },
  {
    n: 18,
    d: 'TRO',
    m: 0,
    q: 'A query joins a very large orders table with a much smaller customers table. In the Spark UI, most runtime is spent in shuffle-heavy stages with skewed tasks and heavy spill.\n\nWhich Spark UI observation most directly shows that a broadcast join is not being used?',
    o: [
      'High spill metrics in the Exchange operator',
      'In the physical plan, the join path includes Exchange operators and no BroadcastExchange feeding a broadcast join node.',
      'High spill (disk/memory) metrics mean insufficient executor memory, so increasing memory alone fixes it.',
      'Large variance in task duration across executors',
    ],
    a: [1],
    x: 'El plan físico es la evidencia directa de la estrategia de join elegida: si aparecen operadores Exchange sin un BroadcastExchange alimentando un BroadcastHashJoin, Spark optó por una estrategia basada en shuffle en vez de broadcast. El spill y la varianza de duración son síntomas derivados (no la causa raíz visible del tipo de join), y asumir que el problema es solo memoria es una conclusión prematura.',
  },
  {
    n: 19,
    d: 'ING',
    m: 1,
    q: 'A Delta Lake table requires strict isolation/consistency for correct concurrent reads and writes, and partially written batches must never be visible.\n\nWhich two benefits of ACID transactions in Delta Lake directly address these requirements? (Select two)',
    o: [
      'They provide transactional isolation/consistency so concurrent reads and writes remain correct.',
      'They speed up queries by automatically enabling Delta caching.',
      'They guarantee atomic commits, so writes are fully applied or fully reverted.',
      'They automatically compact small Parquet files into larger files.',
    ],
    a: [0, 2],
    x: 'La isolation/consistency garantiza que los lectores vean instantáneas coherentes mientras hay escrituras concurrentes, y la atomicidad asegura que una transacción se aplique por completo o no se aplique en absoluto (nunca un estado parcial visible). El caching de Delta y la compactación automática de archivos son optimizaciones de rendimiento, no propiedades ACID en sí mismas.',
  },
  {
    n: 20,
    d: 'TRA',
    m: 0,
    q: 'A Lakeflow Spark Declarative Pipelines pipeline cleans web logs. Records with user_id IS NULL must be excluded from the target, the pipeline must continue running, and the number of excluded records must be tracked in pipeline metrics.\n\nWhich expectation syntax correctly implements this requirement?',
    o: [
      '```\n@dp.expect_or_drop("valid_user_id", "user_id IS NOT NULL")\n```',
      '```\n.filter("user_id IS NOT NULL") without any expectation decorator.\n```',
      '```\n@dp.expect_or_fail("valid_user_id", "user_id IS NOT NULL")\n```',
      '```\n@dp.expect("valid_user_id", "user_id IS NOT NULL")\n```',
    ],
    a: [0],
    x: 'expect_or_drop descarta las filas que incumplen la condición y registra el conteo de descartes en las métricas del dataset, cumpliendo a la vez exclusión, continuidad y observabilidad. Un filtro manual no aprovecha el framework de expectations (sin métricas nativas), expect_or_fail detiene el pipeline, y expect() mantiene las filas inválidas en el destino.',
  },
  {
    n: 21,
    d: 'P',
    m: 0,
    q: 'A fintech company runs scheduled ETL pipelines, nightly ML training jobs, ad hoc BI queries during market hours, and collaborative notebook exploration. The team wants to reduce cost from overprovisioned all-purpose compute while still meeting SLA and responsiveness requirements.\n\nWhich compute strategy is the best fit?',
    o: [
      'Use spot instances exclusively for all jobs to minimize cost.',
      'Use serverless compute for Lakeflow Jobs, SQL warehouses for BI, ephemeral job compute for ML training, and all-purpose compute only for collaborative notebook work.',
      'Migrate every workload to autoscaling all-purpose compute.',
      'Consolidate all workloads on shared all-purpose compute.',
    ],
    a: [1],
    x: 'Repartir cada carga según su naturaleza (serverless para jobs programados, SQL warehouses para BI, job compute efímero para ML, all-purpose solo para colaboración interactiva) alinea coste y rendimiento con el uso real. Usar spot exclusivamente ignora los requisitos de SLA, y consolidar todo en all-purpose desperdicia el ahorro de coste de las opciones especializadas.',
  },
  {
    n: 22,
    d: 'JOBS',
    m: 0,
    q: 'A multi-task Lakeflow Jobs workflow has two notebook tasks: Calculate_Metrics (computes a KPI) and Send_Slack_Alert (must use that KPI value). The engineer wants to pass a small value directly between tasks without writing intermediate files.\n\nWhich Databricks utility should be used?',
    o: ['dbutils.jobs.taskValues', '`spark.conf.set()`', 'dbutils.widgets', 'os.environ'],
    a: [0],
    x: 'dbutils.jobs.taskValues está diseñado específicamente para pasar valores pequeños entre tareas de un mismo run de Jobs, almacenándolos en el plano de control de Databricks. spark.conf y os.environ son locales a la sesión/proceso (no se propagan entre clústeres distintos), y dbutils.widgets sirve para parametrizar la entrada de un notebook, no para comunicación entre tareas.',
  },
  {
    n: 23,
    d: 'JOBS',
    m: 0,
    q: 'A Lakeflow Spark Declarative Pipeline ingests financial reports uploaded once per day at 02:00 UTC, processed in 15 minutes, but is configured in "Continuous" mode, keeping the cluster active 24/7.\n\nWhich pipeline execution mode should the engineer select to minimize compute costs?',
    o: ['Continuous', 'Auto Scaling', 'Triggered', 'Serverless'],
    a: [2],
    x: 'El modo Triggered procesa todos los datos disponibles (equivalente a AvailableNow) y luego apaga el cómputo automáticamente, reduciendo el tiempo facturado de 24 horas a los ~15 minutos reales de procesamiento diario. Continuous mantiene el clúster activo permanentemente, Auto Scaling solo ajusta el número de workers (no apaga el clúster), y Serverless por sí solo no determina cuándo se detiene si el modo sigue siendo continuo.',
  },
  {
    n: 24,
    d: 'P',
    m: 0,
    q: 'A query failed reading a Delta table in Unity Catalog because the compute resource could not access the storage location. Logs show the compute requesting credentials and then receiving "Access Denied" from the cloud provider.\n\nWhich statement best describes the component responsible for executing the query and the root cause of the failure?',
    o: [
      'The Control Plane executes the query; failure is due to a bug in the notebook interface.',
      "The Data Plane executes the query; failure is due to insufficient cloud IAM permissions on the customer's storage.",
      'The Control Plane executes the query; failure is due to Unity Catalog being unavailable.',
      'The Data Plane executes the query; failure is due to the workspace being in a different region than the data.',
    ],
    a: [1],
    x: 'El Data Plane (en la cuenta cloud del cliente) es donde realmente se ejecutan las queries, y el mensaje "Access Denied" apunta directamente a un problema de permisos IAM del rol usado por el cómputo sobre el bucket. El Control Plane no ejecuta queries de procesamiento de datos, la indisponibilidad de Unity Catalog daría un error distinto, y un problema de región produciría latencia/coste, no "Access Denied".',
  },
  {
    n: 25,
    d: 'ING',
    m: 1,
    q: 'A team needs to ingest data from several SaaS and operational systems with minimal custom code, preferring a managed ingestion experience over notebook-based extraction logic.\n\nWhich two statements best describe when Lakeflow Connect is an appropriate choice? (Select two)',
    o: [
      'It is required for reading JSON files from cloud object storage with schema drift, even when Auto Loader already supports the use case.',
      'It is appropriate when the team wants managed connectors for supported external sources instead of hand-built extraction code.',
      'It is the default solution for implementing custom REST pagination logic against unsupported APIs.',
      'It is appropriate when the team wants to ingest from supported sources while keeping governance aligned with Databricks-native ingestion workflows.',
    ],
    a: [1, 3],
    x: 'Lakeflow Connect encaja cuando existen conectores gestionados para las fuentes soportadas, evitando código de extracción hecho a mano, y cuando se quiere mantener la gobernanza alineada con flujos de ingesta nativos de Databricks. JSON con schema drift en almacenamiento cloud es el caso clásico de Auto Loader (no requiere Lakeflow Connect), y la paginación REST personalizada contra APIs no soportadas es, por definición, un escenario de ingesta custom.',
  },
  {
    n: 26,
    d: 'P',
    m: 0,
    q: 'A team needs to join historical sales data in Delta Lake with real-time support tickets in an external PostgreSQL database, without building a new ETL pipeline, querying Postgres directly and managing access through Unity Catalog.\n\nWhich Databricks capability should the team use?',
    o: [
      'Lakehouse Federation',
      'Lakeflow Spark Declarative Pipelines',
      'Partner Connect',
      'Databricks Marketplace',
    ],
    a: [0],
    x: 'Lakehouse Federation permite consultar sistemas externos como PostgreSQL "en su sitio" desde Databricks, gobernado por Unity Catalog, sin copiar ni mover los datos. Lakeflow es un framework de ETL para mover/transformar datos (lo contrario de lo pedido), Partner Connect conecta herramientas partner, y Marketplace es para productos de datos de terceros, no bases operacionales propias.',
  },
  {
    n: 27,
    d: 'TRO',
    m: 0,
    q: 'A Spark SQL join between a very large fact table and a very small dimension table currently uses a sort-merge join, causing heavy shuffling. The engineer wants Spark to replicate the small table to executors instead.\n\nWhich function should be applied to the small table to force this behavior?',
    o: [
      '`broadcast(small_table)`',
      '`cache(small_table)`',
      '`coalesce(small_table)`',
      '`repartition(small_table)`',
    ],
    a: [0],
    x: 'broadcast(df) marca el DataFrame como suficientemente pequeño para replicarlo a todos los executors, permitiendo un Broadcast Hash Join que evita el shuffle de la tabla grande. cache() solo guarda el resultado en memoria (no cambia la estrategia de join), coalesce() reduce particiones sin forzar broadcast, y repartition() en realidad provoca un shuffle completo, agravando el problema.',
  },
  {
    n: 28,
    d: 'TRA',
    m: 0,
    q: 'A pipeline defines a logical view exposing only a subset of rows from an existing table using @dp.temporary_view in Lakeflow Spark Declarative Pipelines Python.\n\nWhen deployed and executed, what kind of object does this definition create?',
    o: [
      'A materialized view stored as physical data.',
      'A temporary pipeline view whose results are computed when queried and used only inside the pipeline graph.',
      'A continuously updating streaming view.',
      'A temporary Spark session view created only for the current notebook session.',
    ],
    a: [1],
    x: '@dp.temporary_view crea una vista lógica del pipeline cuyo resultado se calcula al consultarla, sin persistir datos físicos, y solo es utilizable dentro del propio grafo del pipeline. No es una vista materializada (que sí persiste datos), no es inherentemente streaming (aquí se usa una lectura batch), y no es un temp view de sesión de notebook estándar de Spark.',
  },
  {
    n: 29,
    d: 'TRA',
    m: 1,
    q: 'A SQL step identifies active subscriptions as of 2026-01-07 and keeps only users on their current plan for more than 30 days, using start_date, end_date and a reference date.\n\nWhich three operations were applied to produce this result? (Select three)',
    o: [
      "The datediff() function was used to calculate tenure_days between start_date and a literal date of '2026-01-07'.",
      'A filter WHERE tenure_days > 30 was applied to the calculated results.',
      'An inner join was performed on user_id to self-join the table.',
      'A filter was applied to keep only records where end_date IS NULL.',
    ],
    a: [0, 1, 3],
    x: 'datediff() calcula la antigüedad respecto a la fecha de referencia, el filtro tenure_days > 30 aplica la regla de negocio de "más de 30 días", y el filtro end_date IS NULL selecciona únicamente las suscripciones activas (sin fecha de fin). No hace falta ningún self-join porque toda la información necesaria ya está en cada fila individual de la tabla.',
  },
  {
    n: 30,
    d: 'GOV',
    m: 0,
    q: "A dynamic view on gold.regional_sales must show: admin_users see all rows, us_users see only region='US' rows, everyone else sees no rows.\n\nWhich SQL logic should be used in the WHERE clause?",
    o: [
      "```\nGRANT SELECT ON gold.regional_sales TO us_users WHERE region = 'US'\n```",
      "```\nWHERE is_account_group_member('admin_users') OR (is_account_group_member('us_users') AND region = 'US')\n```",
      'WHERE region IN (SELECT allowed_region FROM system.access.audit WHERE user = current_user())',
      '```\nWHERE region = current_user()\n```',
    ],
    a: [1],
    x: 'La combinación con is_account_group_member() en una expresión OR/AND implementa exactamente la política pedida: admins ven todo, us_users solo ven US, y cualquier otro usuario no cumple ninguna condición (cero filas). GRANT ... WHERE no es sintaxis válida, audit no es una tabla de asignación de regiones por usuario, y comparar region con current_user() no tiene sentido semántico.',
  },
  {
    n: 31,
    d: 'GOV',
    m: 0,
    q: 'A data architect wants to drop the legacy_code column from gold.products and needs to identify every downstream notebook, dashboard, job, or table that references that specific column.\n\nWhich Unity Catalog system table should be queried?',
    o: [
      'system.lineage.column_lineage',
      'system.information_schema.columns',
      'system.lineage.table_lineage',
      'system.access.audit',
    ],
    a: [0],
    x: 'system.lineage.column_lineage registra el linaje a nivel de columna (columnas de origen y destino), permitiendo identificar con precisión qué objetos dependen específicamente de esa columna. information_schema.columns solo describe el esquema actual, table_lineage solo captura dependencias a nivel de tabla completa (falsos positivos), y audit registra eventos de seguridad, no flujo de datos.',
  },
  {
    n: 32,
    d: 'JOBS',
    m: 0,
    q: 'A Lakeflow Jobs workflow runs A → B → C. The run fails at Task B (OOM) after Task A already completed. After increasing compute for Task B, the engineer wants to continue from the point of failure without rerunning Task A.\n\nWhich Lakeflow Jobs feature should be used?',
    o: [
      'Clone the job and remove Task A',
      'Click Run now on the job',
      'Use Repair run after fixing the task configuration',
      'Manually run only Task B as a separate execution',
    ],
    a: [2],
    x: 'Repair run vuelve a ejecutar solo las tareas fallidas o seleccionadas como parte del run original, evitando reprocesar Task A que ya tuvo éxito. Clonar el job es un workaround manual, "Run now" inicia un run completamente nuevo (reprocesando todo), y ejecutar Task B por separado no preserva el contexto del run original.',
  },
  {
    n: 33,
    d: 'CICD',
    m: 0,
    q: 'A Declarative Automation Bundles configuration already includes the bundle: block. The engineer needs to add a job definition with tasks and a notebook task.\n\nWhich top-level YAML key must be used to define the job correctly?',
    o: ['settings', 'pipelines', 'resources', 'workflows'],
    a: [2],
    x: 'resources es la clave de nivel superior que contiene las definiciones de recursos desplegables, incluidos jobs, pipelines, dashboards y clústeres. "settings" no es una clave documentada para esto, "pipelines" es solo un subnivel dentro de resources (no el contenedor de jobs), y "workflows" no es la clave real usada por los DAB.',
  },
  {
    n: 34,
    d: 'JOBS',
    m: 0,
    q: 'Lakeflow Spark Declarative Pipelines run under Unity Catalog: Bronze→Silver nightly full refresh, Silver→Gold incrementally every hour. Recent failures occurred because a new run starts before the previous one finishes.\n\nWhich approach best prevents overlapping executions?',
    o: [
      'Set max_concurrent_runs = 0',
      'Configure the job to allow only 1 concurrent run so later runs queue instead of overlapping.',
      'Increase the cluster size so each run finishes faster.',
      'Enable "Allow concurrent runs" in pipeline settings.',
    ],
    a: [1],
    x: 'max_concurrent_runs = 1 asegura que solo haya un run activo a la vez; los runs adicionales quedan en cola en lugar de solaparse. max_concurrent_runs = 0 no es un valor válido (el mínimo es 1), aumentar el clúster reduce duración pero no garantiza que nunca se solape, y "permitir runs concurrentes" empeoraría precisamente el problema descrito.',
  },
  {
    n: 35,
    d: 'GOV',
    m: 1,
    q: 'A Silver-layer pipeline in Lakeflow Spark Declarative Pipelines requires: records with user_id IS NULL are critical failures that must stop the pipeline immediately; records with transaction_amount < 0 should be excluded but processing should continue.\n\nWhich two constraint clauses correctly implement these requirements? (Select two)',
    o: [
      'CONSTRAINT valid_amount EXPECT (transaction_amount >= 0) ON VIOLATION DROP ROW',
      'CONSTRAINT valid_amount EXPECT (transaction_amount >= 0) ON VIOLATION FAIL UPDATE',
      'CONSTRAINT valid_user EXPECT (user_id IS NOT NULL) ON VIOLATION DROP ROW',
      'CONSTRAINT valid_user EXPECT (user_id IS NOT NULL) ON VIOLATION FAIL UPDATE',
    ],
    a: [0, 3],
    x: 'ON VIOLATION DROP ROW sobre transaction_amount descarta silenciosamente los importes negativos sin detener el pipeline, y ON VIOLATION FAIL UPDATE sobre user_id detiene toda la actualización ante un valor nulo, tratándolo como un fallo crítico. Usar FAIL UPDATE en el importe sería demasiado agresivo, y usar DROP ROW en user_id sería insuficiente para un caso marcado como crítico.',
  },
  {
    n: 36,
    d: 'P',
    m: 0,
    q: 'A data engineer manages a very large Delta table with partition skew, write conflicts from concurrent updates, heavy maintenance from repeated OPTIMIZE ZORDER BY, and query patterns that change over time across filter columns.\n\nWhich Delta Lake feature best addresses these issues with a flexible, incremental data layout strategy?',
    o: ['Liquid Clustering (CLUSTER BY)', 'Bloom Filter Indexes', 'Generated Columns', 'Change Data Feed'],
    a: [0],
    x: 'Liquid Clustering optimiza el layout de forma incremental (sin reescrituras completas), permite cambiar las claves de clustering en cualquier momento y reduce los conflictos de escritura frente al particionado tradicional o el Z-Order fijo. Los filtros Bloom son índices de nivel de fichero para búsquedas puntuales, las columnas generadas solo derivan valores, y Change Data Feed rastrea cambios fila a fila, no reorganiza el layout físico.',
  },
  {
    n: 37,
    d: 'TRA',
    m: 0,
    q: 'A retail firm builds Bronze/Silver/Gold layers from JSON sales logs ingested via Auto Loader.\n\nWhich transformations are typically performed when building the Bronze layer?',
    o: [
      'Apply business rules and complex transformations',
      'Build fully curated aggregates, KPI tables, and pre-aggregated outputs',
      'Perform extensive data cleansing and validation',
      'Land the data with minimal or no transformations.',
    ],
    a: [3],
    x: 'Bronze existe para aterrizar los datos con fidelidad al origen y transformación mínima, sirviendo de base reproducible para reprocesos. Las reglas de negocio y transformaciones complejas, las agregaciones/KPIs curados y la limpieza/validación exhaustiva son responsabilidades de Silver y Gold, no de Bronze.',
  },
  {
    n: 38,
    d: 'ING',
    m: 0,
    q: 'A data engineer must choose between Auto Loader and COPY INTO for incremental file loading.\n\nIn which scenario should the data engineer choose Auto Loader over COPY INTO?',
    o: [
      'When the environment cannot persist Structured Streaming checkpoints.',
      'When the source schema is expected to evolve over time and the pipeline should automatically handle new columns.',
      'When performing a one-time historical backfill with a stable schema.',
      'When loading a small, static set of files from cloud storage using a simple SQL batch load.',
    ],
    a: [1],
    x: 'Auto Loader ofrece inferencia y evolución de esquema, lo que lo hace preferible cuando se espera que el esquema origen cambie y el pipeline deba adaptarse automáticamente a columnas nuevas. Sin poder persistir checkpoints Auto Loader no funciona bien, y para backfills puntuales o cargas pequeñas y estáticas, COPY INTO suele ser más simple y suficiente.',
  },
  {
    n: 39,
    d: 'TRO',
    m: 0,
    q: 'A platform admin wants a chargeback dashboard aggregating DBUs consumed per job and per cluster, bucketed hourly, across the entire account, queried directly in SQL.\n\nWhich system table contains the detailed billable usage records needed?',
    o: ['system.compute.node_types', 'system.access.audit', 'system.billing.usage', 'system.query.history'],
    a: [2],
    x: 'system.billing.usage almacena registros granulares de uso facturable (ventanas de tiempo, cantidad de uso, metadatos de job/clúster), justo lo necesario para un chargeback por hora. node_types es un inventario estático de hardware, audit registra eventos de seguridad, y query.history mide rendimiento de consultas SQL, no coste/consumo de DBUs en general.',
  },
  {
    n: 40,
    d: 'JOBS',
    m: 0,
    q: 'A Lakeflow Jobs workflow has: Ingest_Source → Transform_Data (runs only if Ingest_Source succeeds) → Send_Failure_Alert (should run only if Ingest_Source fails).\n\nHow should the engineer configure the dependency condition for Send_Failure_Alert?',
    o: [
      'Set the Run if condition to At least one failed.',
      'Remove the dependency and run Send_Failure_Alert independently.',
      'Set the Run if condition to All succeeded.',
      'Use Repair run to trigger the alert after failure.',
    ],
    a: [0],
    x: '"At least one failed" ejecuta la tarea dependiente únicamente cuando al menos una dependencia falló, que es exactamente el comportamiento de una rama de alerta de error. Quitar la dependencia rompe la lógica condicional, "All succeeded" es el valor por defecto (comportamiento opuesto al deseado), y Repair run es un mecanismo manual posterior, no una condición de ejecución del DAG.',
  },
  {
    n: 41,
    d: 'GOV',
    m: 1,
    q: 'A junior data engineer troubleshoots an "Access Denied" error for analyst_user querying prod_catalog.marketing_schema.campaign_results, despite confirmed SELECT privilege on the table.\n\nWhich two SQL commands must be executed to fully resolve this access issue? (Select two)',
    o: [
      '```\nGRANT USE SCHEMA ON SCHEMA prod_catalog.marketing_schema TO analyst_user;\n```',
      '```\nGRANT USAGE ON CATALOG prod_catalog TO analyst_user;\n```',
      '```\nGRANT READ_METADATA ON TABLE prod_catalog.marketing_schema.campaign_results TO analyst_user;\n```',
      '```\nGRANT USE CATALOG ON CATALOG prod_catalog TO analyst_user;\n```',
    ],
    a: [0, 3],
    x: 'Unity Catalog exige permisos de "travesía" en cada nivel del namespace: USE CATALOG sobre el catálogo padre y USE SCHEMA sobre el esquema padre, además del SELECT ya concedido sobre la tabla. USAGE no es la palabra clave válida en Unity Catalog (se usa USE CATALOG/USE SCHEMA), y READ_METADATA es redundante porque SELECT ya implica poder leer los metadatos de esa tabla.',
  },
  {
    n: 42,
    d: 'TRA',
    m: 1,
    q: 'A high-volume event stream has duplicates in silver_events due to upstream retries. The requirement is to keep only the most recent version of each event_id, based on ingestion_timestamp.\n\nWhich two methods are appropriate for removing these duplicates while preserving the most recent record? (Select two)',
    o: [
      'Enable the ignoreChanges option in the read stream to automatically drop duplicate IDs.',
      'Apply a window function row_number() partitioned by event_id and ordered by ingestion_timestamp DESC, then filter for row_number = 1.',
      'Use DISTINCT * in a SQL query to select all unique rows.',
      'Use df.dropDuplicates(["event_id"]) after sorting the DataFrame by ingestion_timestamp descending.',
    ],
    a: [1, 3],
    x: 'row_number() particionado por event_id y ordenado por ingestion_timestamp DESC, filtrando row_number = 1, conserva exactamente el registro más reciente de cada evento; y ordenar descendente antes de dropDuplicates(["event_id"]) logra el mismo efecto porque Spark conserva la primera fila que encuentra. ignoreChanges es una opción de manejo de fuentes streaming (no deduplica), y DISTINCT * solo elimina filas idénticas en todas las columnas, lo cual no ocurre aquí porque el timestamp difiere.',
  },
  {
    n: 43,
    d: 'ING',
    m: 1,
    q: 'A company needs to bring data from multiple external systems into Databricks.\n\nWhich two scenarios are the strongest candidates for Lakeflow Connect rather than another Databricks capability? (Select two)',
    o: [
      'Ingesting supported external data sources when the team wants a Databricks-managed connector experience for recurring loads.',
      'Reading a small CSV file once with a simple SQL command.',
      'Continuously discovering new JSON files arriving in S3 with schema inference and evolution.',
      'Ingesting from a supported SaaS or database source using a managed connector instead of writing extraction notebooks.',
    ],
    a: [0, 3],
    x: 'Lakeflow Connect encaja cuando se necesita una experiencia de conector gestionado por Databricks para cargas recurrentes desde fuentes soportadas, y cuando se prefiere un conector gestionado a escribir notebooks de extracción manual. Un CSV pequeño de una sola vez es mejor con COPY INTO, y el descubrimiento continuo de archivos JSON en S3 con evolución de esquema es el caso de uso clásico de Auto Loader, no de Lakeflow Connect.',
  },
  {
    n: 44,
    d: 'ING',
    m: 0,
    q: 'A team is building a streaming pipeline to ingest JSON events from Amazon S3, using Auto Loader for automatic schema inference and incremental file discovery.\n\nWhich code block correctly initializes a streaming DataFrame using Auto Loader?',
    o: [
      '```\nspark.readStream.format("cloudFiles").option("cloudFiles.format","json").option("cloudFiles.schemaLocation","/tmp/schema_location").load("s3://events/")\n```',
      '```\nspark.readStream.format("json").option("cloudFiles","true").option("cloudFiles.schemaLocation","/tmp/schema_location").load("s3://events/")\n```',
      '```\nspark.readStream.format("delta").option("cloudFiles.format","json").option("cloudFiles.schemaLocation","/tmp/schema_location").load("s3://events/")\n```',
      '```\nspark.read.format("cloudFiles").option("format","json").option("schemaLocation","/tmp/schema_location").load("s3://events/")\n```',
    ],
    a: [0],
    x: 'Auto Loader se inicializa con format("cloudFiles") junto con cloudFiles.format para el tipo de archivo y cloudFiles.schemaLocation para rastrear el esquema. format("json") o format("delta") no activan Auto Loader, y usar spark.read (en vez de readStream) con nombres de opción incorrectos no produce un DataFrame streaming válido.',
  },
  {
    n: 45,
    d: 'ING',
    m: 0,
    q: "A team is building a streaming pipeline to ingest CSV events. They want to use Auto Loader options that keep existing column types unchanged while automatically evolving the schema when new columns appear.\n\nWhich cloudFiles.schemaEvolutionMode setting keeps existing columns' types unchanged while allowing the schema to grow with new columns?",
    o: ['none', 'rescue', 'addNewColumns', 'failOnNewColumns'],
    a: [2],
    x: 'addNewColumns añade las columnas nuevas al esquema sin modificar el tipo de las columnas ya existentes, cumpliendo ambos requisitos a la vez. "none" no evoluciona el esquema en absoluto, "rescue" oculta los campos nuevos en una columna aparte sin incorporarlos al esquema principal, y "failOnNewColumns" detiene el stream sin garantizar que el esquema se actualice automáticamente en el mismo paso.',
  },
];

export const exam9: RawQuestion[] = [
  {
    n: 1,
    d: 'GOV',
    m: 0,
    q: 'A governance team wants to enforce centralized row filtering and column masking across many Unity Catalog tables based on governed tags, without creating separate views for each table and without manually assigning filters one table at a time.\n\nWhich Unity Catalog capability best fits this requirement?',
    o: [
      'Lakehouse Federation',
      'Attribute-based access control (ABAC) policies',
      '`GRANT SELECT on schemas and catalogs only`',
      'Dynamic views with CASE WHEN logic on each table',
    ],
    a: [1],
    x: 'Las políticas ABAC de Unity Catalog se basan en etiquetas gobernadas y permiten aplicar filtros de fila y enmascaramiento de columna de forma centralizada sobre muchas tablas a la vez, sin crear una vista por tabla. Lakehouse Federation sirve para consultar sistemas externos, GRANT SELECT solo controla acceso grueso, y las vistas dinámicas exigen crear y mantener un objeto de vista independiente para cada tabla.',
  },
  {
    n: 2,
    d: 'P',
    m: 0,
    q: 'An organization wants AI-powered capabilities such as natural-language search, generated descriptions, and context-aware assistance across governed data.\n\nWhich Databricks component is the high-performance execution engine that accelerates SQL and DataFrame workloads, but is NOT the semantic intelligence layer behind those AI features?',
    o: ['Photon Engine', 'Lakeflow Connect', 'Lakeflow Spark Declarative Pipelines', 'DatabricksIQ'],
    a: [0],
    x: 'Photon es el motor de ejecución vectorizado de alto rendimiento para cargas SQL y DataFrame, pero no es la capa de inteligencia semántica: esa función la cumple DatabricksIQ. Lakeflow Connect es ingesta gestionada y Lakeflow Spark Declarative Pipelines es el framework de ETL declarativo — ninguno de los dos es el motor de ejecución ni la capa semántica de IA.',
  },
  {
    n: 3,
    d: 'GOV',
    m: 0,
    q: 'A compliance officer needs to investigate potential unauthorized access to a sensitive table, querying Unity Catalog system tables directly in SQL to review historical audit events.\n\nWhich specific system table should they use?',
    o: [
      'system.access.audit',
      'system.information_schema.tables',
      'system.lineage.table_lineage',
      'system.billing.usage',
    ],
    a: [0],
    x: 'system.access.audit almacena los eventos de auditoría (logins, consultas, cambios de permisos) directamente consultables por SQL. information_schema.tables solo describe estructura, no actividad; table_lineage muestra relaciones de flujo de datos (no detalles de acceso puntual); y billing.usage mide consumo de DBUs, no eventos de seguridad.',
  },
  {
    n: 4,
    d: 'P',
    m: 0,
    q: 'A team is migrating BI workloads to Databricks SQL and needs a warehouse with support for advanced SQL features (AI Functions), Databricks-managed compute with very fast startup and autoscaling, and no manual VM provisioning.\n\nWhich warehouse type best satisfies all requirements?',
    o: ['Standard SQL Warehouse', 'Pro SQL Warehouse', 'Serverless SQL Warehouse', 'Classic SQL Warehouse'],
    a: [2],
    x: 'El Serverless SQL Warehouse ofrece todas las funciones avanzadas (Predictive I/O, UDFs de Python, AI Functions) más arranque casi instantáneo y gestión de infraestructura totalmente a cargo de Databricks, sin aprovisionar VMs manualmente. Standard/Classic carecen de las funciones avanzadas, y Pro sí las tiene pero sigue corriendo en la VPC del cliente, sin el arranque instantáneo de Serverless.',
  },
  {
    n: 5,
    d: 'ING',
    m: 1,
    q: 'A team must ingest data from an operational PostgreSQL database over JDBC and a third-party REST API with paginated JSON responses, without using Lakeflow Connect (unsupported for these patterns).\n\nWhich two approaches best fit this requirement? (Select two)',
    o: [
      'Use a notebook or Python code to call the REST API, handle pagination/authentication, and write the results to Delta.',
      'Use a notebook to read the relational source with JDBC and land the results into Delta tables.',
      'Use Lakehouse Federation for both sources because it is the standard ingestion path for JDBC and REST extraction.',
      'Use Delta Sharing because it is the primary mechanism for extracting data from operational systems into Bronze.',
    ],
    a: [0, 1],
    x: 'Para una API REST paginada, código en notebook maneja la autenticación/paginación y escribe a Delta; para una base relacional, una lectura JDBC desde notebook es el patrón estándar de ingesta hacia Bronze. Lakehouse Federation es para consultar en su sitio (no para extraer/aterrizar en Delta) y no cubre REST genérico, y Delta Sharing es un mecanismo de compartición de datos ya existentes, no de extracción desde sistemas operacionales.',
  },
  {
    n: 6,
    d: 'ING',
    m: 1,
    q: 'A Python notebook needs to: (1) define a helper function in Scala, (2) query a Delta table with SQL, and (3) list files in a DBFS folder.\n\nWhich two "Magic Commands" are required to perform tasks 1 and 3? (Select two)',
    o: ['%scala', '%pip', '%run', '%fs'],
    a: [0, 3],
    x: '%scala cambia el lenguaje de una celda concreta a Scala (tarea 1), y %fs es el atajo para dbutils.fs que permite listar archivos en DBFS (tarea 3). %pip solo gestiona instalación de librerías, y %run ejecuta otro notebook completo, sin cambiar el lenguaje de una celda ni navegar el sistema de archivos.',
  },
  {
    n: 7,
    d: 'JOBS',
    m: 0,
    q: 'A Lakeflow Jobs workflow should start automatically whenever new files arrive in a monitored Unity Catalog storage location, without polling on a fixed cron schedule.\n\nWhich trigger type should be configured?',
    o: ['Continuous', 'Table update', 'File arrival', 'Scheduled'],
    a: [2],
    x: 'El trigger de "File arrival" inicia un run de job cuando llegan archivos nuevos a una ubicación de almacenamiento monitorizada por Unity Catalog, sin necesidad de sondeo por cron. "Continuous" mantiene el job siempre activo, "Table update" reacciona a cambios en una tabla (no a archivos), y "Scheduled" es estrictamente temporal, no basado en eventos.',
  },
  {
    n: 8,
    d: 'P',
    m: 0,
    q: 'In the context of modern data platforms, which statement most accurately captures what a Data Lakehouse is designed to provide?',
    o: [
      'A transactional data management system focused exclusively on structured enterprise data.',
      'A cloud-based system for running data lake workloads without maintaining on-premises infrastructure.',
      'A cost-optimization platform primarily intended to lower cloud storage expenses for open data formats.',
      'A unified, high-performance analytics platform that supports data engineering, business intelligence, and machine learning workloads.',
    ],
    a: [3],
    x: 'El lakehouse unifica ingeniería de datos, BI y machine learning sobre los mismos datos, combinando la flexibilidad de un data lake con las garantías transaccionales y el rendimiento de un data warehouse. Las otras opciones describen aspectos parciales o secundarios (solo datos estructurados, solo despliegue en la nube, solo ahorro de costes de almacenamiento), no la propuesta de valor central del lakehouse.',
  },
  {
    n: 9,
    d: 'GOV',
    m: 0,
    q: 'An ML engineer needs to store thousands of PDFs and images in Databricks, governed by Unity Catalog permissions and accessible through standard file paths for Python libraries.\n\nWhich Unity Catalog object should be created to store this non-tabular data?',
    o: [
      'A Managed Delta Table',
      'A Unity Catalog Volume',
      'A Lakeflow Spark Declarative Pipeline',
      'A DBFS (Databricks File System) Mount',
    ],
    a: [1],
    x: 'Los Volumes de Unity Catalog están diseñados para gobernar acceso a datos no tabulares (PDFs, imágenes, audio, vídeo) mediante rutas de archivo estándar (/Volumes/catálogo/esquema/volumen/...). Una tabla Delta es para datos estructurados en filas/columnas, un pipeline de Lakeflow no es un objeto de almacenamiento, y los montajes DBFS son una funcionalidad heredada sin la gobernanza nativa de Unity Catalog.',
  },
  {
    n: 10,
    d: 'P',
    m: 0,
    q: 'A data engineer must create a full copy of a Delta table so that the copy remains usable even if the source table is dropped or its underlying files are vacuumed.\n\nWhich Delta Lake DDL command should be used?',
    o: [
      "RESTORE TABLE ... TO LOCATION 'backup/finance/transactions_snapshot'",
      '```\nCREATE TABLE ... LIKE prod.finance.transactions\n```',
      '```\nCREATE TABLE ... SHALLOW CLONE prod.finance.transactions\n```',
      '```\nCREATE TABLE ... DEEP CLONE prod.finance.transactions\n```',
    ],
    a: [3],
    x: 'DEEP CLONE copia tanto los metadatos como los archivos de datos físicos, produciendo dos tablas totalmente independientes que sobreviven aunque la fuente se elimine o se ejecute VACUUM sobre ella. LIKE copia solo el esquema (sin datos), SHALLOW CLONE solo copia el log de transacciones apuntando a los mismos archivos (vulnerable a VACUUM en el origen), y RESTORE no crea una copia nueva, solo revierte la propia tabla a una versión anterior.',
  },
  {
    n: 11,
    d: 'TRA',
    m: 0,
    q: 'A data engineering team using the Medallion Architecture has raw ingestion and a cleaned/deduplicated "Enterprise View" layer already built. They now need a final layer for high-performance reporting, Tableau dashboards, and pre-computed aggregations.\n\nWhich layer should the engineers implement?',
    o: ['Gold Layer', 'Platinum Layer', 'Bronze Layer', 'Silver Layer'],
    a: [0],
    x: 'Gold es la capa final de consumo, optimizada para reporting de alto rendimiento y agregaciones de negocio precomputadas para BI/dashboards. "Platinum" no existe como capa estándar del patrón Medallion, Bronze es la capa de aterrizaje en crudo, y Silver es la vista empresarial limpiada pero típicamente a nivel de grano transaccional, no pre-agregada para reportes.',
  },
  {
    n: 12,
    d: 'TRA',
    m: 0,
    q: 'A Gold-layer dataset in Lakeflow Spark Declarative Pipelines computes customer-level aggregates from Silver data, queried by multiple downstream dashboards and jobs. The result should be persisted and reusable outside the pipeline.\n\nWhich object type is the best fit?',
    o: ['A materialized view', 'A temporary view', 'A view', 'A streaming table'],
    a: [0],
    x: 'Una materialized view persiste el resultado y es reutilizable por múltiples consumidores externos al pipeline (dashboards, jobs), ideal para agregaciones de Gold consultadas repetidamente. Una temporary view es solo lógica interna del pipeline, una view se recalcula cada vez y solo puede usarse dentro del mismo pipeline, y una streaming table está pensada para fuentes incrementales/continuas, no para este caso de agregación consumida externamente.',
  },
  {
    n: 13,
    d: 'ING',
    m: 0,
    q: 'A Structured Streaming job must count web click events per hour using event_time, accepting late events up to 2 hours after the event time and then stopping old aggregation state beyond that threshold.\n\nWhich PySpark snippet correctly applies this behavior?',
    o: [
      '```\n.withWatermark("event_time","2 hours").groupBy(F.window("event_time","1 hour")).agg(F.count("*").alias("click_count"))\n```',
      '```\n.withWatermark("event_time","1 hour").groupBy(F.window("event_time","2 hours")).agg(F.count("*").alias("click_count"))\n```',
      '```\n.groupBy(F.window("event_time","1 hour")).agg(F.count("*").alias("click_count")) (no watermark)\n```',
      '```\n.filter(F.col("event_time") >= F.current_timestamp() - F.expr("INTERVAL 2 HOURS")).groupBy(F.window("event_time","1 hour")).agg(F.count("*").alias("click_count"))\n```',
    ],
    a: [0],
    x: 'withWatermark("event_time","2 hours") junto con ventanas de 1 hora coincide exactamente con el requisito: agrupar por hora y tolerar hasta 2 horas de retraso. Invertir los valores (ventana de 2h con watermark de 1h) no cumple el enunciado, omitir el watermark deja el estado sin acotar, y filtrar por current_timestamp() usa tiempo de procesamiento en vez de tiempo de evento, lo cual no activa la limpieza de estado del motor.',
  },
  {
    n: 14,
    d: 'JOBS',
    m: 1,
    q: 'A team wants Slack notifications for a nightly job nightly_revenue_calc: notify immediately when it starts, notify immediately if it fails, and do NOT notify on success.\n\nWhich two notification settings should the engineer configure? (Select two)',
    o: [
      'Select the "Start" event trigger.',
      'Select the "Slow Query" event trigger.',
      'Select the "Success" event trigger.',
      'Select the "Failure" event trigger.',
    ],
    a: [0, 3],
    x: 'El evento "Start" notifica en cada arranque del job (para verificar consistencia de horario) y el evento "Failure" notifica solo cuando el job falla, cumpliendo ambos requisitos sin generar ruido en caso de éxito. "Slow Query" es una métrica de rendimiento de consultas SQL (no del ciclo de vida del job), y "Success" violaría explícitamente el requisito de no notificar en ejecuciones correctas.',
  },
  {
    n: 15,
    d: 'GOV',
    m: 0,
    q: 'A retail company needs to join 1 PB of historical sales in Databricks with real-time inventory in an AWS RDS PostgreSQL database, requiring up-to-the-second data, making nightly ETL impractical.\n\nWhich Unity Catalog feature enables querying the PostgreSQL data directly without moving or copying it?',
    o: ['Delta Sharing', 'Lakehouse Federation', 'Lakeflow Spark Declarative Pipelines', 'Auto Loader'],
    a: [1],
    x: 'Lakehouse Federation permite montar la base PostgreSQL como un catálogo externo (foreign catalog) y consultarla "en su sitio" desde Databricks, sin copiar los datos, obteniendo así el estado actual del inventario. Delta Sharing comparte tablas Delta ya existentes (no bases de datos operacionales externas), y Lakeflow/Auto Loader implican mover/copiar datos mediante ETL, exactamente lo que el requisito quiere evitar.',
  },
  {
    n: 16,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to synchronize customer_dimension with a daily source snapshot updates_today using customer_id: update if matched, insert if only in source, leave unchanged if only in target.\n\nWhich Spark SQL command correctly implements this logic?',
    o: [
      '```\nINSERT OVERWRITE TABLE customer_dimension SELECT * FROM updates_today\n```',
      '```\nCOPY INTO customer_dimension FROM updates_today ON DUPLICATE KEY UPDATE\n```',
      'MERGE INTO customer_dimension t USING updates_today s ON t.customer_id = s.customer_id WHEN MATCHED THEN UPDATE SET * WHEN NOT MATCHED THEN INSERT *',
      '```\nSELECT * FROM updates_today UNION SELECT * FROM customer_dimension\n```',
    ],
    a: [2],
    x: 'MERGE INTO con WHEN MATCHED/WHEN NOT MATCHED es la sintaxis estándar de upsert: actualiza las filas coincidentes, inserta las nuevas, y deja intactas las que solo están en destino. INSERT OVERWRITE reemplazaría toda la tabla (perdiendo filas no presentes en el origen), COPY INTO no soporta actualizaciones de filas existentes, y UNION generaría duplicados en vez de actualizar.',
  },
  {
    n: 17,
    d: 'ING',
    m: 0,
    q: 'A data engineer creates a reusable notebook that runs as a job task for multiple datasets, receiving runtime values such as source_path and target_table_name.\n\nWhich Databricks utility should be used inside the notebook to define and retrieve these input parameters?',
    o: ['`spark.conf.get()`', 'sys.argv', 'dbutils.widgets', '%params'],
    a: [2],
    x: 'dbutils.widgets es el mecanismo estándar para parametrizar notebooks: los widgets definidos se mapean automáticamente a los parámetros de la tarea cuando el notebook se ejecuta como parte de un job. sys.argv no aplica en el contexto de ejecución de notebooks de Databricks, spark.conf no está pensado para esto, y %params no es un magic command válido.',
  },
  {
    n: 18,
    d: 'ING',
    m: 0,
    q: 'A team using Auto Loader to ingest millions of image files from ADLS Gen2 experiences high latency and API costs because Auto Loader repeatedly scans a very large directory structure.\n\nWhich Auto Loader file discovery mode should they use to reduce directory-listing overhead?',
    o: [
      'Change the source format from cloudFiles to delta',
      'Force recursive directory listing for every run',
      'Use availableNow=True so all files are processed in one bounded run',
      'Use file notification mode with file events (e.g., cloudFiles.useManagedFileEvents)',
    ],
    a: [3],
    x: 'El modo de notificación de archivos evita listar el directorio completo en cada ejecución, escalando a millones de archivos mediante eventos de la nube (SQS/Event Grid/Pub-Sub) en vez de listados costosos. Cambiar a "delta" implicaría leer una tabla Delta existente (no archivos crudos), el listado recursivo es justamente el problema, y availableNow solo cambia el trigger de ejecución, no el mecanismo de descubrimiento de archivos.',
  },
  {
    n: 19,
    d: 'ING',
    m: 0,
    q: 'A data engineer needs to repeatedly load Parquet files from cloud storage into an existing Delta table with a scheduled batch job. The load must be idempotent (no duplicate ingestion on rerun) without managing Structured Streaming checkpoints.\n\nWhich command or feature should be used?',
    o: [
      "```\nCOPY INTO bronze.events FROM 's3://source/events' FILEFORMAT = PARQUET\n```",
      'INSERT INTO bronze.events SELECT * FROM parquet.`s3://source/events`',
      '```\nspark.readStream.format("cloudFiles")... .writeStream.toTable("bronze.events")\n```',
      'MERGE INTO bronze.events t USING parquet.`s3://source/events` s ON t.id = s.id WHEN NOT MATCHED THEN INSERT *',
    ],
    a: [0],
    x: 'COPY INTO es idempotente por diseño: usa el propio log de transacciones Delta para rastrear qué archivos ya se cargaron, sin necesitar un checkpoint de streaming separado. INSERT INTO inserta todo ciegamente (duplicados en reruns), Auto Loader en modo streaming siempre requiere checkpointLocation, y MERGE funciona pero es más costoso al requerir un join contra la tabla destino solo para verificar duplicados de archivo.',
  },
  {
    n: 20,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to compute three metrics per department from employees_df: total salary, average salary, employee count, all in a single grouped result.\n\nWhich PySpark expression correctly returns all three aggregations?',
    o: [
      '```\n.groupBy("department").agg(F.sum("salary")).agg(F.avg("salary")).agg(F.count("salary"))\n```',
      '```\n.groupBy("department").agg({"salary":"sum","salary":"avg","salary":"count"})\n```',
      '```\n.select("department","salary").groupBy("department").count()\n```',
      '```\n.groupBy("department").agg(F.sum("salary").alias("total_salary"), F.avg("salary").alias("avg_salary"), F.count("salary").alias("salary_count"))\n```',
    ],
    a: [3],
    x: 'Pasar varias expresiones de agregación (cada una con alias) dentro de un único .agg() produce las tres métricas en el mismo resultado agrupado. Encadenar .agg() varias veces no es el patrón correcto, un diccionario Python no puede tener la clave "salary" repetida (solo conservaría la última), y .count() solo cuenta filas, sin sumar ni promediar el salario.',
  },
  {
    n: 21,
    d: 'CICD',
    m: 0,
    q: 'A data engineer works inside a Git folder linked to a remote repository in a Databricks workspace.\n\nWhich action can be performed directly from within the Git folder UI?',
    o: [
      'Create a brand-new remote Git repository',
      'Delete branches from the remote repository',
      'Open or manage pull requests',
      'Pull updates from the existing remote repository into the Git folder',
    ],
    a: [3],
    x: 'Los Git folders de Databricks incluyen una acción documentada de "Pull" para traer los cambios del repositorio remoto al workspace. Crear repositorios nuevos, eliminar ramas remotas o gestionar pull requests son operaciones que se realizan en el proveedor de Git externo (GitHub, GitLab, Azure DevOps), no dentro de la UI del Git folder.',
  },
  {
    n: 22,
    d: 'P',
    m: 0,
    q: 'A BI team needs to connect Tableau to Databricks for high-concurrency SQL queries (hundreds of users), avoiding queuing and poor performance, with instant startup and automatic load balancing.\n\nWhich compute resource should the data engineer provision?',
    o: [
      'Job Compute Cluster with Photon',
      'Single Node Cluster with 128GB RAM',
      'Serverless SQL Warehouse',
      'High Concurrency All-Purpose Cluster',
    ],
    a: [2],
    x: 'Un Serverless SQL Warehouse está optimizado para BI/JDBC con arranque casi instantáneo y escalado automático ante picos de concurrencia. Job Compute está pensado para cargas no interactivas programadas, un Single Node no escala horizontalmente para cientos de usuarios, y un clúster High Concurrency All-Purpose está orientado a notebooks colaborativos, no a servir consultas BI de forma óptima.',
  },
  {
    n: 23,
    d: 'P',
    m: 0,
    q: 'A data engineer finished developing and debugging a PySpark ETL pipeline on interactive compute, and is ready to run it nightly. The goals are to minimize cost and ensure a clean, isolated execution environment for each run.\n\nWhich compute configuration should be selected for the scheduled job?',
    o: [
      'Use GPU-enabled all-purpose compute to accelerate the nightly run.',
      'Configure job compute specifically for this workflow.',
      'Use personal compute assigned to the service principal.',
      'Continue using the existing all-purpose compute so the environment matches development.',
    ],
    a: [1],
    x: 'El job compute es cómputo efímero: se crea para el run y se destruye al terminar, dando un entorno limpio por ejecución y un coste por hora inferior al de all-purpose. Las GPUs añaden coste sin beneficio para un ETL estándar, personal compute es un sandbox individual no pensado para producción, y reutilizar all-purpose eleva el coste y arriesga interferencias entre sesiones.',
  },
  {
    n: 24,
    d: 'TRA',
    m: 0,
    q: 'A data engineer processes raw application logs in a Lakeflow Spark Declarative Pipelines workflow. The raw_payload column stores JSON strings that must be parsed into a structured column parsed_data using a predefined schema object log_schema.\n\nWhich PySpark command correctly creates the new structured column?',
    o: [
      '```\nlogs_df.withColumn("parsed_data", F.from_json("raw_payload", log_schema))\n```',
      '```\nlogs_df.withColumn("parsed_data", to_json("raw_payload", log_schema))\n```',
      '```\nlogs_df.withColumn("parsed_data", parse_json("raw_payload"))\n```',
      '```\nlogs_df.withColumn("parsed_data", get_json_object("raw_payload", log_schema))\n```',
    ],
    a: [0],
    x: 'from_json(col, schema) es la función diseñada para convertir una columna de cadenas JSON en un tipo estructurado (struct) según el esquema indicado. to_json hace lo inverso (struct → string), parse_json no existe en PySpark, y get_json_object extrae un único campo mediante una ruta JSON (no acepta un esquema completo ni devuelve un struct).',
  },
  {
    n: 25,
    d: 'TRA',
    m: 0,
    q: 'A Lakeflow Spark Declarative Pipelines pipeline ingests sales data. A business rule requires order_qty to be greater than zero. Violating records must be excluded from the target dataset, but the pipeline must continue processing valid records.\n\nWhich SQL expectation syntax correctly implements this requirement?',
    o: [
      'CONSTRAINT valid_qty EXPECT (order_qty > 0) ON VIOLATION FAIL UPDATE',
      'ASSERT order_qty > 0 ELSE DROP',
      'CONSTRAINT valid_qty EXPECT (order_qty > 0) ON VIOLATION DROP ROW',
      'CONSTRAINT valid_qty EXPECT (order_qty > 0)',
    ],
    a: [2],
    x: 'ON VIOLATION DROP ROW descarta las filas que incumplen la condición mientras el resto del pipeline continúa procesando sin interrupción. FAIL UPDATE detendría todo el update ante cualquier violación, ASSERT ... ELSE DROP no es sintaxis válida de Lakeflow, y omitir la cláusula ON VIOLATION equivale al modo "warn" por defecto, que mantiene las filas inválidas en el destino.',
  },
  {
    n: 26,
    d: 'JOBS',
    m: 0,
    q: 'A multi-task Lakeflow Jobs workflow has two Python notebook tasks on different clusters: Task 1 calculates a KPI value, and Task 2 must consume that value.\n\nWhich command should Task 1 use to pass the value to Task 2?',
    o: [
      'os.environ["daily_total"] = "54000"',
      '```\nspark.conf.set("daily_total", 54000)\n```',
      '```\nprint(f"Set daily_total={54000}")\n```',
      '```\ndbutils.jobs.taskValues.set(key="daily_total", value=54000)\n```',
    ],
    a: [3],
    x: 'dbutils.jobs.taskValues.set() envía el valor al plano de control de Databricks, donde una tarea posterior (aunque corra en un clúster distinto) puede recuperarlo con taskValues.get(). Las variables de entorno y la configuración de Spark son locales al proceso/sesión, y un print() solo escribe en los logs, sin exponer el valor programáticamente a otra tarea.',
  },
  {
    n: 27,
    d: 'TRO',
    m: 0,
    q: 'A data engineer analyzes a join between a very large fact table and a 45 MB lookup table. The physical plan shows a SortMergeJoin, and spark.sql.autoBroadcastJoinThreshold is set to 10 MB.\n\nWhich optimization action is most strongly suggested by this evidence?',
    o: [
      'Decrease the number of shuffle partitions to reduce SortMergeJoin overhead.',
      'Convert the Lookup table to a global temporary view.',
      'Increase spark.sql.autoBroadcastJoinThreshold above 45 MB, or explicitly use a broadcast hint on the small table.',
      'Apply a ZORDER optimization on the Fact table.',
    ],
    a: [2],
    x: 'Como la tabla de lookup (45 MB) supera el umbral configurado (10 MB), Spark no la difunde automáticamente; subir el umbral por encima de 45 MB o usar un hint broadcast() fuerza el Broadcast Hash Join y evita el shuffle. Reducir particiones no cambia la estrategia de join, el alcance de una vista (global/temporal) no influye en el plan de ejecución, y ZORDER mejora el skipping de archivos pero no evita el shuffle de un SortMergeJoin.',
  },
  {
    n: 28,
    d: 'CICD',
    m: 0,
    q: 'A data engineering team wants to move from manual UI-based job creation to configuration-as-code using source-controlled YAML files and a CLI workflow, to reduce configuration drift and support CI/CD.\n\nWhich Databricks feature is specifically designed for this workflow?',
    o: [
      'Declarative Automation Bundles',
      'Workspace Import/Export API',
      'Notebook scheduled jobs',
      'Databricks Repos',
    ],
    a: [0],
    x: 'Los Declarative Automation Bundles son el enfoque de infraestructura como código de Databricks, definido en YAML (databricks.yml) y gestionado con la CLI para validar, desplegar y ejecutar, encajando exactamente con el flujo de CI/CD pedido. La API de Import/Export solo mueve archivos individuales, programar jobs manualmente en la UI es justo lo que se quiere dejar de hacer, y Repos solo cubre el control de versiones del código, no la definición de infraestructura.',
  },
  {
    n: 29,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to calculate a cumulative sum of daily_revenue for each store_id, ordered by transaction_date.\n\nWhich PySpark code block correctly defines the window specification and computes this running total?',
    o: [
      'Window.partitionBy("store_id") — sum over that window, no orderBy',
      '```\nWindow.groupBy("store_id").orderBy("transaction_date") with F.cumsum(...)\n```',
      '```\nWindow.orderBy("transaction_date") only (no partitionBy)\n```',
      '```\nWindow.partitionBy("store_id").orderBy("transaction_date").rowsBetween(Window.unboundedPreceding, Window.currentRow), then F.sum("daily_revenue").over(w)\n```',
    ],
    a: [3],
    x: 'partitionBy("store_id") mantiene el acumulado independiente por tienda, orderBy("transaction_date") define la secuencia temporal, y el frame de unboundedPreceding a currentRow produce la suma acumulada correcta desde el inicio de cada partición. Sin orderBy no hay secuencia de acumulación, Window.groupBy no existe (las ventanas usan partitionBy) y F.cumsum tampoco es una función estándar de Spark, y sin partitionBy se mezclarían los datos de todas las tiendas.',
  },
  {
    n: 30,
    d: 'GOV',
    m: 0,
    q: 'A security team wants to investigate who changed Unity Catalog permissions, who created external locations, and which workspace actions were performed over the last 90 days, directly in SQL without exporting raw log files.\n\nWhich Databricks object should they use?',
    o: [
      'system.access.audit',
      'information_schema.role_table_grants',
      'system.lineage.table_lineage',
      'system.query.history',
    ],
    a: [0],
    x: 'system.access.audit es la tabla consultable directamente en SQL para analizar actividad de cuenta y eventos de gobernanza (cambios de permisos, creación de external locations) a lo largo del tiempo. role_table_grants describe el estado actual de privilegios (no el historial de cambios), table_lineage cubre relaciones de datos (no acciones administrativas), y query.history se limita al historial de ejecución de consultas SQL.',
  },
  {
    n: 31,
    d: 'ING',
    m: 0,
    q: 'Auto Loader must infer types for most columns automatically, but the zip_code column should always be treated as a string (to preserve leading zeros) while other columns keep automatic inference.\n\nWhich Auto Loader option should be used?',
    o: [
      '```\n.option("cloudFiles.inferColumnTypes", "false")\n```',
      '```\n.option("cloudFiles.schemaHints", "zip_code STRING")\n```',
      '```\n.option("cloudFiles.overrideType", "zip_code STRING")\n```',
      '```\n.option("schema", "zip_code STRING")\n```',
    ],
    a: [1],
    x: 'cloudFiles.schemaHints permite forzar el tipo de columnas concretas (aquí, zip_code como STRING) mientras el resto del esquema se sigue infiriendo automáticamente. Desactivar inferColumnTypes afectaría a todas las columnas (no solo a zip_code), overrideType no es una opción real de Auto Loader, y pasar un esquema parcial con "schema" no es el mecanismo documentado para combinarlo con inferencia automática del resto.',
  },
  {
    n: 32,
    d: 'GOV',
    m: 0,
    q: 'A data governance lead needs to automate a compliance report listing all downstream tables consuming data from a sensitive source table, using Unity Catalog system tables programmatically.\n\nWhich system table should be queried?',
    o: [
      'system.storage.table_usage',
      'system.access.audit',
      'system.access.table_lineage',
      'system.information_schema.table_constraints',
    ],
    a: [2],
    x: 'system.access.table_lineage captura automáticamente las relaciones origen-destino entre tablas cada vez que ocurre una lectura/escritura, exponiéndolas de forma estructurada y consultable por SQL. No existe una tabla estándar "table_usage" con ese propósito, audit registra eventos de seguridad (no dependencias de datos), y table_constraints solo describe claves/restricciones declaradas, no el flujo real de datos.',
  },
  {
    n: 33,
    d: 'TRO',
    m: 0,
    q: 'In the Spark UI "Stages" tab, summary metrics show a median task duration of 8 seconds but a max task duration of 45 minutes, with one task running significantly longer than the rest.\n\nWhich performance issue is most likely causing this behavior?',
    o: ['Small Files problem', 'Data Skew', 'Driver Memory OOM (Out Of Memory)', 'Broadcast Timeout'],
    a: [1],
    x: 'Una brecha enorme entre la duración mediana y la máxima de las tareas de un mismo stage es la firma clásica de data skew: una partición concentra muchos más datos que las demás, convirtiéndose en el cuello de botella. El problema de archivos pequeños suele mostrar muchísimas tareas cortas (no una sola muy larga), un OOM del driver suele hacer fallar el job en vez de completarlo lentamente, y un timeout de broadcast aborta la consulta con un error explícito, no una tarea lenta pero exitosa.',
  },
  {
    n: 34,
    d: 'CICD',
    m: 0,
    q: 'A platform engineer is building a CI/CD pipeline for Declarative Automation Bundles and wants to run the Databricks CLI command that checks whether the bundle configuration is valid before deploying.\n\nWhich Databricks CLI command is correct for this validation step?',
    o: [
      '`databricks bundle deploy`',
      'databricks clusters get',
      '`databricks bundle validate`',
      'databricks jobs create',
    ],
    a: [2],
    x: 'databricks bundle validate es el comando documentado para validar la configuración de un bundle antes de desplegarlo. bundle deploy ya realiza el despliegue (paso posterior a la validación), clusters get consulta información de un clúster (sin relación con bundles), y jobs create crea un job directamente vía CLI sin pasar por el modelo de bundle en absoluto.',
  },
  {
    n: 35,
    d: 'ING',
    m: 0,
    q: 'A data engineer is debugging a Python function in a Databricks notebook and needs to set breakpoints, inspect variable values, and step through code interactively without adding print() statements.\n\nWhich Databricks notebook feature should they use?',
    o: ['Ganglia Metrics', 'Spark UI - Environment Tab', 'Driver Logs', 'Python interactive debugger'],
    a: [3],
    x: 'El depurador interactivo de Python integrado en los notebooks permite fijar breakpoints, inspeccionar variables y avanzar línea a línea sin modificar el código con prints. Ganglia/métricas de clúster monitorizan CPU/memoria (no la lógica del código), el Spark UI muestra configuración y ejecución distribuida (no el estado de variables locales), y los logs del driver solo son útiles si ya se han añadido mensajes explícitos.',
  },
  {
    n: 36,
    d: 'TRA',
    m: 1,
    q: 'A PySpark job joins a very large fact table with a small dimension table and performs a wide aggregation. In the Spark UI, the join uses a shuffle-heavy strategy instead of broadcast, with excessive shuffle overhead, and the configuration still uses generic defaults.\n\nWhich two tuning parameters are the most directly relevant to test first? (Select two)',
    o: [
      '`spark.sql.shuffle.partitions`',
      '`spark.sql.autoBroadcastJoinThreshold`',
      '`spark.sql.catalogImplementation`',
      '`spark.databricks.delta.preview.enabled`',
    ],
    a: [0, 1],
    x: 'spark.sql.autoBroadcastJoinThreshold controla si Spark elige automáticamente un broadcast join, y spark.sql.shuffle.partitions afecta directamente al coste de las agregaciones amplias con mucho shuffle — ambos son los primeros parámetros a probar en este escenario. La implementación del catálogo y las flags de preview de Delta no tienen relación con la estrategia de join ni con el overhead de shuffle descrito.',
  },
  {
    n: 37,
    d: 'TRO',
    m: 0,
    q: 'A data engineer opens the Spark UI for a slow batch job and notices: one task runs far longer than the others, most tasks finish quickly but one processes much more data, spill to disk is high for that task, and shuffle read is heavily unbalanced.\n\nWhat is the most likely root cause?',
    o: [
      'Unity Catalog permission propagation delay',
      'Missing checkpoint location',
      'A VACUUM operation running concurrently',
      'Data skew in one or more partitions',
    ],
    a: [3],
    x: 'Un fuerte desequilibrio entre tareas en duración, lectura de shuffle y spill a disco es la señal característica de data skew: unas pocas particiones concentran mucho más volumen que el resto. Un retraso de propagación de permisos daría errores de acceso (no lentitud desigual), un checkpoint faltante afecta a la tolerancia a fallos, y VACUUM es una operación de mantenimiento que no suele manifestarse como una única tarea desbalanceada dentro de un stage.',
  },
  {
    n: 38,
    d: 'TRA',
    m: 0,
    q: 'A retail company builds a Medallion Architecture: raw JSON events ingested exactly as received, then invalid records cleaned/deduplicated with standardized nulls, then daily sales KPIs computed for dashboards.\n\nWhich mapping of Bronze, Silver, and Gold is correct?',
    o: [
      'Bronze = cleansed and deduplicated tables; Silver = KPI tables; Gold = raw ingestion',
      'Bronze = raw ingestion; Silver = dashboard tables; Gold = duplicate quarantine tables',
      'Bronze = raw ingestion; Silver = cleansed and standardized tables; Gold = business-ready aggregates and KPI tables',
      'Bronze = KPI tables; Silver = raw ingestion; Gold = cleansed and deduplicated tables',
    ],
    a: [2],
    x: 'Bronze aterriza los datos exactamente como llegan, Silver aplica limpieza/estandarización/deduplicación, y Gold produce agregados listos para negocio como los KPIs de dashboards — el orden de refinamiento progresivo del patrón Medallion. Cualquier combinación que invierta ese orden (poner KPIs en Bronze, datos crudos en Silver, o limpieza en Gold) contradice la definición estándar de las tres capas.',
  },
  {
    n: 39,
    d: 'TRA',
    m: 1,
    q: 'A Spark job processing 10 TB shows executors performing massive "Spill (Disk)" operations during the shuffle phase of a wide transformation. The team wants to explain and mitigate this without increasing physical RAM.\n\nWhich two statements correctly describe the behavior/resolution of this shuffle spill? (Select two)',
    o: [
      'The default threshold for spilling is based on total free disk space on the worker node.',
      'Increasing the number of shuffle partitions via spark.sql.shuffle.partitions can reduce spilling by creating smaller, more manageable data chunks.',
      'Spilling to disk is always faster than a shuffle because it avoids network I/O.',
      'Spilling occurs when the size of a single shuffle partition exceeds the available execution memory fraction of the JVM.',
    ],
    a: [1, 3],
    x: 'El spill ocurre cuando una partición de shuffle supera la fracción de memoria de ejecución disponible en la JVM, y aumentar shuffle.partitions crea trozos de datos más pequeños que caben mejor en esa memoria, reduciendo la necesidad de volcar a disco. El spill no se dispara por el espacio libre en disco (sino por memoria), y escribir/leer de disco siempre es más lento que permanecer en memoria, nunca "más rápido".',
  },
  {
    n: 40,
    d: 'ING',
    m: 1,
    q: 'A source system drops ~50,000 small JSON files into S3 every hour. The goal is to move this data into a Bronze Delta table with minimal latency and high cost-efficiency, comparing Auto Loader vs. COPY INTO.\n\nWhich two statements correctly identify the advantages of Auto Loader over COPY INTO for this scenario? (Select two)',
    o: [
      'Auto Loader ensures duplicate data is never ingested, whereas COPY INTO requires manual deduplication logic.',
      'Auto Loader can use Cloud Service Integration (e.g., AWS SQS/SNS) to detect new files without listing the entire directory.',
      'Auto Loader is a SQL-only command, making it easier to manage within SQL Warehouses than COPY INTO.',
      'Auto Loader supports Schema Evolution, allowing the pipeline to automatically adapt if new fields are added to the JSON files.',
    ],
    a: [1, 3],
    x: 'Con notificaciones de eventos cloud, Auto Loader evita listar el directorio completo a esa escala, y su capacidad de evolución de esquema le permite adaptarse automáticamente si aparecen campos nuevos en el JSON. Ambas herramientas ofrecen idempotencia de forma similar (no es una ventaja exclusiva), y Auto Loader es principalmente una fuente de Structured Streaming en Python/Scala, no un comando "solo SQL" (eso describe mejor a COPY INTO).',
  },
  {
    n: 41,
    d: 'P',
    m: 0,
    q: 'A team needs a SQL compute resource for scheduled and interactive SQL workloads with very fast startup, automatic scaling, minimal infrastructure management, and no need to choose VM types or Spark versions manually.\n\nWhich compute configuration best satisfies these requirements?',
    o: [
      'Single-node all-purpose compute with Photon',
      'Serverless SQL warehouse',
      'High concurrency all-purpose compute',
      'All-purpose compute with spot instances enabled',
    ],
    a: [1],
    x: 'Un Serverless SQL Warehouse elimina la elección manual de tipo de instancia/versión de Spark, arranca casi al instante y escala automáticamente, todo gestionado por Databricks. Single-node y all-purpose (con o sin spot) siguen exigiendo configuración explícita de clúster y no ofrecen el mismo perfil de arranque instantáneo orientado a SQL.',
  },
  {
    n: 42,
    d: 'P',
    m: 0,
    q: 'A Delta table performs poorly because it contains thousands of very small data files. To reduce metadata overhead and improve read throughput, the engineer wants to compact these files into fewer larger files.\n\nWhich command should be executed?',
    o: [
      'ANALYZE TABLE table_name COMPUTE STATISTICS',
      '`OPTIMIZE table_name`',
      'REPAIR TABLE table_name',
      '`VACUUM table_name`',
    ],
    a: [1],
    x: 'OPTIMIZE reescribe los archivos pequeños existentes en archivos más grandes (bin-packing), reduciendo el overhead de listar/gestionar metadatos y mejorando el rendimiento de lectura. ANALYZE solo actualiza estadísticas para el optimizador (no reorganiza archivos), REPAIR TABLE recupera particiones que faltan en el metastore (innecesario en Delta), y VACUUM limpia archivos ya obsoletos sin compactar los archivos activos pequeños.',
  },
  {
    n: 43,
    d: 'TRA',
    m: 0,
    q: 'A team is moving from imperative Spark ETL scripts to Lakeflow Spark Declarative Pipelines to reduce orchestration complexity.\n\nWhat is the primary architectural advantage of using this declarative framework over standard imperative Spark scripts?',
    o: [
      'It lets engineers define datasets and transformations declaratively, while the pipeline runtime manages orchestration, dependencies, and execution.',
      'It provides a fully no-code drag-and-drop interface, removing the need for SQL or Python.',
      'It replaces Apache Spark with a proprietary single-node execution engine.',
      'It forces all processing into strict batch mode, removing streaming complexity by design.',
    ],
    a: [0],
    x: 'El framework declarativo deja que el ingeniero defina qué datasets y transformaciones existen, mientras el runtime del pipeline se encarga automáticamente de orquestación, dependencias, gestión de compute y manejo de errores. No es una interfaz sin código (sigue usándose SQL/Python), sigue ejecutándose sobre Apache Spark (no lo sustituye), y admite tanto batch como streaming, no solo batch.',
  },
  {
    n: 44,
    d: 'ING',
    m: 0,
    q: 'A data engineer has a scheduled batch job that loads CSV files from cloud storage into a Delta table using COPY INTO. The team wants reruns to be idempotent so files already loaded in prior runs are not ingested again. During Run 2, one file was already loaded in Run 1 and one file is new.\n\nWhat happens during Run 2?',
    o: [
      'The previously processed file is skipped and only the new file is ingested.',
      'The target table is overwritten with the union of both files.',
      'The command raises an error because one file was already loaded.',
      'Both files are loaded again, creating duplicates for the previously processed file.',
    ],
    a: [0],
    x: 'COPY INTO mantiene metadatos internos (en el log de transacciones Delta) de qué archivos ya se cargaron, así que en la segunda ejecución omite automáticamente el archivo ya procesado e ingiere únicamente el nuevo. No sobrescribe la tabla completa, no lanza error por encontrar un archivo repetido (simplemente lo salta), y por diseño evita generar duplicados en reruns.',
  },
  {
    n: 45,
    d: 'GOV',
    m: 0,
    q: 'A data engineering team plans to deprecate a column and needs to identify all downstream Gold tables, dashboards, jobs, notebooks, and machine learning assets that depend specifically on that column.\n\nWhich Unity Catalog feature allows the team to perform this granular impact analysis programmatically?',
    o: [
      'Query system.access.column_lineage to retrieve downstream dependencies.',
      'Run DESCRIBE EXTENDED on the source table.',
      "Inspect the table's History tab in Catalog Explorer.",
      'Query system.information_schema.columns to find objects with the same column name.',
    ],
    a: [0],
    x: 'El linaje a nivel de columna (system.lineage.column_lineage) captura automáticamente qué objetos consumen una columna concreta, permitiendo un análisis de impacto preciso y programático antes de eliminarla. DESCRIBE EXTENDED solo describe el estado actual de la tabla, la pestaña "History" muestra el historial de operaciones sobre la propia tabla (no consumidores externos), y buscar coincidencias de nombre de columna en information_schema no demuestra una relación de flujo de datos real.',
  },
];

export const exam10: RawQuestion[] = [
  {
    n: 1,
    d: 'P',
    m: 0,
    q: "A notebook's default language is Python. The engineer wants to run a single SQL cell. Which is the correct way?",
    o: [
      'Rewrite the entire notebook in SQL.',
      'Start the cell with `%sql`.',
      'Wrap the SQL in `spark.sql(...)` only — magic commands cannot mix languages.',
      'SQL cannot be used in Python notebooks.',
    ],
    a: [1],
    x: 'Los comandos mágicos como `%sql`, `%scala`, `%r` y `%python` cambian el lenguaje solo para esa celda, sin afectar al resto del notebook. `spark.sql()` también es válido para lanzar SQL desde Python, pero no es "la forma de convertir una celda en SQL" que pide el enunciado, sino una alternativa distinta.',
  },
  {
    n: 2,
    d: 'P',
    m: 0,
    q: 'An engineer wants a streaming job to run at a fixed interval, for example, every 10 minutes. The job must process all data that has arrived since the last run and then shut down the compute until the next interval. Which trigger configuration should be used?',
    o: [
      '`.trigger(availableNow=True)`',
      "`.trigger(processingTime='10 minutes')`",
      '`.trigger(once=True)`',
      "`.trigger(continuous='10 minutes')`",
    ],
    a: [1],
    x: '`processingTime` lanza un micro-batch a intervalos fijos, ideal para procesamiento periódico programado. `availableNow` (y su alias `once`) procesan todo lo disponible en uno o varios micro-batches y después paran el stream por completo, sin repetir el ciclo. `continuous` mantiene el clúster activo de forma continua con latencia mínima, justo lo contrario de apagar el compute entre ejecuciones.',
  },
  {
    n: 3,
    d: 'P',
    m: 0,
    q: 'A user launches a notebook and runs a command on an all-purpose cluster. How do the Databricks control plane and data plane interact during this process?',
    o: [
      'All notebook commands and data processing occur entirely within the control plane infrastructure.',
      'The control plane processes the data, and the data plane manages the user interface and notebook state.',
      'The control plane hosts the notebook and interprets commands, but it orchestrates a cluster in the data plane to actually process the data.',
      'The data plane authenticates the user and hosts the web application, while the control plane manages cloud storage.',
    ],
    a: [2],
    x: 'La arquitectura de Databricks separa gestión de ejecución: el plano de control aloja el backend del notebook, la UI y la orquestación, mientras que el plano de datos (dentro de la cuenta cloud del cliente) contiene los clústeres que realmente ejecutan los jobs de Spark y procesan los datos. Ninguna de las otras combinaciones respeta esa separación.',
  },
  {
    n: 4,
    d: 'P',
    m: 0,
    q: 'A team needs to create a new workspace and assign it to a metastore. Which role is required?',
    o: [
      'Workspace admin in the existing workspace.',
      'Account admin in the Databricks account console.',
      'Metastore admin only.',
      'Catalog owner of `main`.',
    ],
    a: [1],
    x: 'Crear un workspace y asignarlo a un metastore son acciones a nivel de cuenta, no de metastore ni de workspace, así que solo un account admin desde la consola de cuenta puede hacerlo. El workspace admin gestiona la configuración dentro de un workspace ya existente, el metastore admin gestiona objetos dentro del metastore, y el catalog owner gestiona un catálogo concreto: ninguno alcanza el nivel de cuenta.',
  },
  {
    n: 5,
    d: 'ING',
    m: 0,
    q: 'An engineer sets up an Auto Loader stream with `cloudFiles.schemaEvolutionMode` set to `rescue`. What happens when a file arrives with a data type change in one of its columns (e.g., a field changes from an integer to a string)?',
    o: [
      'The stream fails with a data type mismatch error.',
      "The table's column type is automatically widened to accommodate both integers and strings (e.g., to `StringType`).",
      'A new column is added to the table to hold the data with the new type.',
      'The data that does not match the existing schema data type is captured in the `_rescued_data` column.',
    ],
    a: [3],
    x: 'El modo `rescue` está diseñado precisamente para evitar cualquier evolución del schema: en vez de ampliar el tipo de columna o añadir columnas nuevas, captura los datos que no encajan con el schema actual (incluidos los cambios de tipo) en la columna `_rescued_data`, dejando el pipeline correr sin interrupciones ni cambios estructurales en la tabla.',
  },
  {
    n: 6,
    d: 'ING',
    m: 0,
    q: 'When ingesting data incrementally, which of the following approaches relies on a transaction log to determine what new data to process?',
    o: [
      'Reading files from a source directory using high watermarks on file-modified timestamps.',
      'Reading directly from a Delta table as a streaming source.',
      'Querying a source relational database using a monotonically increasing primary key.',
      'Listing a source directory and comparing the file list against a list of previously processed files.',
    ],
    a: [1],
    x: 'Leer una tabla Delta como stream (`spark.readStream.format("delta").load(...)`) se apoya directamente en el transaction log de Delta para identificar y servir de forma eficiente solo los registros confirmados desde el último batch del stream. Las otras opciones dependen de timestamps, claves primarias o listados de ficheros, mecanismos externos al log transaccional.',
  },
  {
    n: 7,
    d: 'ING',
    m: 0,
    q: 'Which `cloudFiles.schemaEvolutionMode` should be used if the team wants to be explicitly notified (via a job failure) when any new column appears, and force a manual review?',
    o: ['`addNewColumns`', '`rescue`', '`failOnNewColumns`', '`none`'],
    a: [2],
    x: '`failOnNewColumns` detiene el stream y lo mantiene fallido hasta que un operador actualice el schema de forma explícita, que es justo el control manual que pide el enunciado. `addNewColumns` se autorepara añadiendo la columna sin intervención; `rescue` oculta el cambio en `_rescued_data` sin fallar; `none` ignora directamente las columnas nuevas.',
  },
  {
    n: 8,
    d: 'ING',
    m: 0,
    q: 'An Auto Loader stream is overwhelming the cluster because each trigger ingests too many files. Which option limits the per-trigger workload?',
    o: [
      '`spark.sql.shuffle.partitions`',
      '`cloudFiles.maxFilesPerTrigger`',
      '`maxOffsetsPerTrigger`',
      '`cloudFiles.backfillInterval`',
    ],
    a: [1],
    x: '`cloudFiles.maxFilesPerTrigger` limita cuántos ficheros se procesan en un mismo micro-batch (1000 por defecto), que es exactamente la palanca para controlar la carga por disparo. `maxOffsetsPerTrigger` es el equivalente para fuentes Kafka, y `backfillInterval` programa listados periódicos para detectar notificaciones perdidas, no limita el volumen por trigger.',
  },
  {
    n: 9,
    d: 'ING',
    m: 0,
    q: 'An Auto Loader streaming job is terminated unexpectedly while processing a batch of files. Upon restart, how does the job ensure exactly-once processing of the files?',
    o: [
      'It deletes all source files that were successfully processed before the failure.',
      'It relies on the `modifiedAfter` option to only read files created after the stream started.',
      'It uses information stored in the specified schema and checkpoint locations to identify which files have already been committed.',
      'It re-scans the entire source directory and compares file hashes against a log in the sink table.',
    ],
    a: [2],
    x: 'Auto Loader garantiza exactly-once apoyándose en el checkpoint, que guarda el registro transaccional de qué ficheros se han descubierto y procesado, permitiendo reanudar de forma fiable justo donde falló. No borra ficheros de origen, no depende de `modifiedAfter` para la garantía de exactly-once, ni recalcula hashes contra la tabla destino.',
  },
  {
    n: 10,
    d: 'ING',
    m: 0,
    q: 'An engineer is ingesting a directory of CSV files with a known, stable schema. How can they provide the schema to the read operation to prevent schema inference and ensure data quality?',
    o: [
      "```\nspark.read.format('csv').option('schema', 'user_id INT, email STRING').load(path)\n```",
      "```\nspark.read.format('csv').load(path).withSchema(user_schema)\n```",
      "```\nspark.read.format('csv').schema(user_schema).load(path)\n```",
      "```\nspark.read.format('csv').schema('path/to/schema.json').load(path)\n```",
    ],
    a: [2],
    x: 'El método `.schema()` recibe un `StructType` y debe encadenarse antes de `.load()`; es la única de las cuatro formas válida en el `DataFrameReader`. Pasar el schema como string dentro de `option()` no es una opción reconocida, `.withSchema()` no existe como método, y no se puede apuntar a un fichero de schema por ruta de esa manera.',
  },
  {
    n: 11,
    d: 'ING',
    m: 0,
    q: 'A data engineer needs to run a data ingestion job every weekday at 9:00 AM. What is the most direct way to automate this process in Databricks?',
    o: [
      "Use the 'Run continuously' trigger on the job.",
      'Configure a schedule for the job using a time-based (cron) trigger.',
      'Manually start the job run every morning at the required time.',
      'Set a cluster policy that restricts compute usage to 9:00 AM.',
    ],
    a: [1],
    x: 'Los jobs se automatizan con schedules basados en sintaxis cron, el método estándar y más directo para ejecuciones a una hora fija o recurrente. Un trigger continuo mantiene el job siempre activo, no lo dispara puntualmente; el arranque manual no es automatización; y una cluster policy solo restringe recursos de cómputo, no programa ejecuciones.',
  },
  {
    n: 12,
    d: 'ING',
    m: 0,
    q: 'Which Delta Lake feature most directly helps make repeated ingestion of the same source files safe (idempotent)?',
    o: [
      '`MERGE INTO` with a deduplication key.',
      '`OPTIMIZE` with Z-Ordering.',
      'Setting the cluster to single-node.',
      'Using only CSV files.',
    ],
    a: [0],
    x: '`MERGE INTO` con una clave de negocio o de evento estable permite que reingestas repetidas del mismo origen produzcan el mismo resultado final, evitando duplicados aunque el proceso se repita. `OPTIMIZE`/Z-Order mejoran el layout físico de los datos pero no afectan a la idempotencia; el tipo de clúster o el formato de fichero tampoco tienen relación con esta garantía.',
  },
  {
    n: 13,
    d: 'TRA',
    m: 0,
    q: 'Which decorator is used to define a streaming table in a Python Lakeflow Spark Declarative Pipeline?\n```\nimport dlt\n\n@___\ndef bronze_events():\n    return spark.readStream.format("cloudFiles")\\\n        .option("cloudFiles.format", "json")\\\n        .load("/Volumes/main/raw/events/")\n```',
    o: ['dlt.view', 'dlt.table', 'dlt.expect', 'dlt.materialized'],
    a: [1],
    x: '`@dlt.table` define una tabla (streaming o materializada) dentro de un pipeline declarativo. `@dlt.view` define una vista no materializada, y `dlt.expect` no es un decorador de tabla sino de calidad de datos, así que no puede usarse para declarar el propio objeto tabla.',
  },
  {
    n: 14,
    d: 'TRA',
    m: 0,
    q: 'Change Data Feed is enabled on a Delta table. Which read returns inserts, updates (pre and post images), and deletes between two versions?',
    o: [
      "```\nspark.read.format('delta').option('versionAsOf', 12).load(path)\n```",
      "```\nspark.read.format('delta').option('readChangeData', 'true').option('startingVersion', 10).option('endingVersion', 12).load(path)\n```",
      "```\nspark.read.format('delta').option('changes', 'true').load(path)\n```",
      '```\nDESCRIBE HISTORY <table> filtered between version 10 and 12.\n```',
    ],
    a: [1],
    x: 'CDF se lee activando `readChangeData` (o `readChangeFeed`) junto con `startingVersion`/`startingTimestamp` (y opcionalmente `endingVersion`), lo que devuelve las filas de inserción, actualización (pre y post) y borrado. `DESCRIBE HISTORY` solo lista metadatos de commits, no filas; `versionAsOf` es time travel a una única versión, no un rango de cambios.',
  },
  {
    n: 15,
    d: 'TRA',
    m: 0,
    q: 'A data engineering team needs to run a data processing job every Monday morning at 8:00 AM. Which feature within Databricks Jobs & Pipelines should they configure?',
    o: ['A schedule trigger', 'A file arrival trigger', 'A continuous trigger', 'A manual run trigger'],
    a: [0],
    x: 'Los schedule triggers ejecutan jobs a horas e intervalos concretos con sintaxis cron, la opción correcta para una ejecución semanal fija. Un file arrival trigger depende de la llegada de ficheros, no del calendario; el continuo mantiene el job siempre activo; y el manual requiere intervención humana cada vez.',
  },
  {
    n: 16,
    d: 'TRA',
    m: 0,
    q: 'You are setting up a basic streaming read from a directory of JSON files. Which code snippet correctly initiates this read?',
    o: [
      "```\nspark.read.format('json').load('/path/to/files')\n```",
      "```\nspark.readStream.format('json').load('/path/to/files')\n```",
      "```\nspark.stream.format('json').path('/path/to/files')\n```",
      "```\nspark.read.stream().json('/path/to/files')\n```",
    ],
    a: [1],
    x: 'Para leer datos como stream hay que usar el atributo `readStream` de la `SparkSession`; el resto de la sintaxis de carga es igual que en una lectura estática. Las demás variantes no existen como API válida (`spark.stream` y `spark.read.stream()` no son constructores reales de Spark).',
  },
  {
    n: 17,
    d: 'TRA',
    m: 0,
    q: 'A DataFrame contains a string column `raw_payload` with JSON data. A data engineer wants to parse this into a struct column named `payload`. What is the most critical piece of information that must be provided to the `from_json` function, besides the input column?',
    o: [
      'The schema of the JSON string.',
      'A dictionary of parsing options, like `mode`.',
      'The character encoding of the string.',
      'The name of the target column for the struct.',
    ],
    a: [0],
    x: '`from_json` necesita un schema (como `StructType` o string DDL) para saber cómo interpretar el JSON y con qué nombres y tipos generar las columnas resultantes; sin él, Spark no puede parsear la estructura. Las opciones adicionales son configurables pero no obligatorias, y el nombre de la columna destino se define en el `select`/`withColumn` que envuelve la llamada, no en la función en sí.',
  },
  {
    n: 18,
    d: 'TRA',
    m: 0,
    q: 'A data engineer is building a Lakeflow Declarative Pipeline and needs to prevent any records with a null `order_id` from being written to the target table. Records that violate this rule should be completely discarded. Which Python decorator should the engineer use to define this data quality rule?\n\nimport dlt\n\n@dlt.table\n@dlt.expect_or_drop("Order ID is not NULL", "order_id IS NOT NULL")\ndef orders_bronze():\n  # ... implementation to read source data',
    o: ['`@dlt.expect`', '`@dlt.expect_or_fail`', '`@dlt.expect_or_drop`', '`@dlt.expect_all`'],
    a: [2],
    x: '`@dlt.expect_or_drop` descarta los registros que violan la condición, mientras que `@dlt.expect` solo los cuenta en las métricas del pipeline pero los deja pasar igualmente. `@dlt.expect_or_fail` aborta todo el run ante cualquier violación, un comportamiento más drástico del que pide el enunciado; `@dlt.expect_all` agrupa varias expectativas, no define una acción por sí sola.',
  },
  {
    n: 19,
    d: 'TRA',
    m: 0,
    q: 'Which Databricks feature automatically rewrites a query plan and chooses join strategies based on actual runtime statistics?',
    o: ['Photon vectorized execution.', 'Adaptive Query Execution (AQE).', 'Z-Ordering.', 'Auto Loader.'],
    a: [1],
    x: 'AQE adapta el plan de ejecución en tiempo real usando estadísticas de las etapas ya completadas, por ejemplo cambiando a un broadcast join o recolocando particiones. Photon acelera la ejecución pero no decide estrategias de plan; Z-Ordering es una técnica de organización física de datos; Auto Loader pertenece a la capa de ingesta.',
  },
  {
    n: 20,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has a DataFrame `orders` with a column `items` which is an array of structs. The engineer wants to create a new row for each element in the `items` array, while keeping the `order_id` associated with each new row. Which PySpark function should be used?\n\nfrom pyspark.sql import functions as F\ndf.withColumn("item", F.explode("items"))',
    o: ['flatten', 'posexplode', 'explode', 'collect_list'],
    a: [2],
    x: '`explode` genera una fila nueva por cada elemento del array y replica correctamente el resto de columnas de la fila original, como `order_id`. `posexplode` añade además la posición del elemento (no pedida aquí); `flatten` convierte un array de arrays en un único array, no genera filas; `collect_list` hace justo lo contrario: agrupa valores en un array.',
  },
  {
    n: 21,
    d: 'TRA',
    m: 0,
    q: 'How does the Lakehouse architecture fundamentally differ from a traditional two-tier architecture that uses a separate data lake and data warehouse?',
    o: [
      'The Lakehouse stores structured data in the data warehouse and unstructured data in the data lake.',
      'The Lakehouse eliminates the need to move or copy data between a data lake and a data warehouse, reducing complexity and latency.',
      'The Lakehouse only supports batch processing, whereas a traditional architecture supports real-time streaming.',
      'The Lakehouse is only for machine learning workloads, while the traditional architecture is for BI and reporting.',
    ],
    a: [1],
    x: 'La ventaja clave del Lakehouse es poder consultar los datos directamente en el data lake con rendimiento y gobernanza propios de un data warehouse, eliminando el ETL de copia entre dos sistemas separados. Las otras opciones describen limitaciones que no son ciertas ni del Lakehouse ni de la arquitectura de dos niveles en general.',
  },
  {
    n: 22,
    d: 'TRA',
    m: 0,
    q: "A data engineer attempts to append a DataFrame to an existing Delta table. The DataFrame contains a new column that is not present in the target table's schema. What is the default behavior?",
    o: [
      "The new column is automatically added to the target table's schema.",
      'The append operation fails with a schema mismatch error.',
      'The new column is silently dropped, and the remaining data is appended.',
      'Rows with the new column are written to a separate exception table.',
    ],
    a: [1],
    x: 'Por defecto, Delta Lake aplica schema enforcement, que impide escrituras con un schema distinto al de la tabla destino, protegiendo la integridad de los datos. Añadir la columna automáticamente solo ocurre si se activa explícitamente `mergeSchema`; descartar la columna en silencio o desviar filas a otra tabla no son comportamientos por defecto de Delta.',
  },
  {
    n: 23,
    d: 'JOBS',
    m: 0,
    q: "A job contains two tasks: 'ingest_data' and 'transform_data'. The 'transform_data' task should only run after the 'ingest_data' task completes successfully. How should this relationship be configured within the job?",
    o: [
      'Link the tasks using a Unity Catalog Volume as a shared dependency marker.',
      'Set both tasks to run on the same cluster and hope for sequential execution.',
      'Configure the job to run continuously so tasks execute as soon as possible.',
      "Define 'transform_data' as a downstream task that explicitly depends on 'ingest_data'.",
    ],
    a: [3],
    x: 'Lakeflow Jobs permite definir dependencias explícitas entre tareas, de forma que la tarea downstream solo arranca tras la finalización correcta de sus dependencias upstream. Compartir clúster no garantiza orden de ejecución, un Volume no es un mecanismo de orquestación, y un trigger continuo no impone secuencia entre tareas.',
  },
  {
    n: 24,
    d: 'JOBS',
    m: 0,
    q: 'For a scheduled production job, why is a job cluster usually preferred over an all-purpose cluster?',
    o: [
      'Job clusters cannot run notebooks',
      'A job cluster is created for the run and terminated after, which is cheaper and isolated',
      'All-purpose clusters are always faster',
      'Job clusters never fail',
    ],
    a: [1],
    x: 'Un job cluster se crea para la ejecución y se apaga al terminar, por lo que solo se paga por el trabajo realizado y se evita interferencia de usuarios interactivos en el mismo clúster. Los all-purpose clusters están pensados para desarrollo interactivo compartido, no para producción programada; no hay ninguna garantía de velocidad ni de ausencia de fallos en ninguno de los dos tipos.',
  },
  {
    n: 25,
    d: 'JOBS',
    m: 0,
    q: 'Which Lakeflow Job trigger type starts a job when an upstream Unity Catalog table is updated?',
    o: ['Scheduled trigger.', 'File arrival trigger.', 'Table update trigger.', 'Continuous trigger.'],
    a: [2],
    x: 'El table update trigger arranca el job cuando cambia la versión de la tabla Delta subyacente, útil para encadenar jobs donde la tabla de salida de uno alimenta a otro. El file arrival vigila rutas de almacenamiento, el scheduled dispara por calendario, y el continuo mantiene el job siempre activo sin depender de eventos.',
  },
  {
    n: 26,
    d: 'JOBS',
    m: 0,
    q: 'A pipeline needs to run the same notebook once per region from a parameterized list. Which Jobs feature is intended for this?',
    o: [
      'A `for_each` task that iterates over the parameter list and runs the inner task per item.',
      'Wrap the notebook in a `for` loop inside Python.',
      'Schedule N separate jobs with different parameters.',
      'Use `dbutils.notebook.run` in a loop.',
    ],
    a: [0],
    x: '`for_each` es el mecanismo nativo de Jobs para hacer fan-out sobre una lista, con control de concurrencia, reintentos y logs por iteración integrados en la orquestación. Las demás alternativas funcionan pero pierden visibilidad y control a nivel de Jobs, gestionando el bucle "a mano" desde el propio código.',
  },
  {
    n: 27,
    d: 'JOBS',
    m: 0,
    q: 'A scheduled job needs to write to a UC table that the original author no longer owns. The job has been failing with permission errors since the author left. What is the typical fix?',
    o: [
      "Change the job's owner; the run-as identity defaults to the job owner.",
      'Add the original author back as a workspace admin.',
      'Set `spark.databricks.acl.dfAclsEnabled = false`.',
      'Disable Unity Catalog for the cluster.',
    ],
    a: [0],
    x: 'Los Lakeflow Jobs se ejecutan por defecto con la identidad del propietario del job (o un service principal configurado), así que transferir la propiedad a una identidad con los privilegios de UC necesarios restaura el acceso. Reincorporar al autor original no es viable ni deseable, y desactivar ACLs de dataframe o Unity Catalog eliminaría la gobernanza en vez de solucionar el permiso puntual.',
  },
  {
    n: 28,
    d: 'CICD',
    m: 0,
    q: 'A CI deploy job needs to deploy bundles into multiple workspaces in the same account. Which identity is most appropriate?',
    o: [
      'A personal access token from one workspace admin.',
      'A separate workspace-scoped PAT per workspace.',
      'An account-scoped service principal granted access to each target workspace.',
      'The shared `databricks` user.',
    ],
    a: [2],
    x: 'Un service principal a nivel de cuenta puede recibir acceso a múltiples workspaces, soporta OAuth M2M y evita el desgaste de rotar y mantener tokens personales por cada workspace. Un PAT individual, sea de un admin o repartido por workspace, ata la automatización a una persona concreta y complica la rotación de credenciales.',
  },
  {
    n: 29,
    d: 'CICD',
    m: 0,
    q: 'Why is it recommended to deploy `dev` bundles into a separate user-scoped path (often `${workspace.current_user.userName}`) rather than a shared production path?',
    o: [
      "To prevent overwriting other developers' in-progress work and to make resources easy to identify by owner.",
      'Because shared paths cannot be deployed to by bundles.',
      'Because UC requires per-user paths.',
      'Because git folders refuse to deploy to shared paths.',
    ],
    a: [0],
    x: 'Es una convención organizativa, no una restricción técnica: aislar cada desarrollador en su propia ruta evita que varios ingenieros se pisen el trabajo en curso y facilita identificar de quién es cada recurso desplegado en dev. Ni Unity Catalog ni los Git folders imponen técnicamente rutas por usuario; producción sí usa una ruta compartida, gestionada por CI.',
  },
  {
    n: 30,
    d: 'CICD',
    m: 0,
    q: 'When a bundle is deployed with the development mode, how are jobs typically treated compared to production mode?',
    o: [
      'Schedules are paused and resources are prefixed so they do not clash with production',
      'Jobs run twice as fast',
      'Jobs automatically use serverless only',
      'Jobs are deleted after each run',
    ],
    a: [0],
    x: 'El modo development pausa los schedules y prefija los nombres de recursos (habitualmente con el usuario) para que un despliegue de desarrollo no dispare ejecuciones reales ni sobrescriba producción. No cambia el tipo de compute ni la velocidad de ejecución, y los jobs no se eliminan automáticamente tras cada run.',
  },
  {
    n: 31,
    d: 'CICD',
    m: 0,
    q: "What is the difference between 'databricks bundle deploy' and 'databricks bundle run'?",
    o: [
      'deploy creates or updates resources; run triggers a defined job or pipeline',
      'They are identical',
      'deploy only works on serverless',
      'run deletes resources',
    ],
    a: [0],
    x: '`deploy` sube y crea o actualiza los recursos definidos en el bundle; `run` dispara la ejecución de un recurso concreto, como arrancar un job. El flujo habitual es desplegar primero y ejecutar después; ninguno de los dos comandos está limitado a serverless ni elimina recursos.',
  },
  {
    n: 32,
    d: 'CICD',
    m: 0,
    q: 'Which Databricks CLI commands are typically used in a CI pipeline for an Automation Bundle?',
    o: [
      '`databricks bundle validate` and `databricks bundle deploy`',
      '`databricks notebooks export` and `databricks notebooks import`',
      '`databricks fs cp` and `databricks fs ls`',
      '`databricks clusters create` and `databricks clusters edit`',
    ],
    a: [0],
    x: '`bundle validate` comprueba que el bundle no tiene errores y `bundle deploy` lo despliega en el target indicado; son los comandos de entrada estándar de un pipeline de CI/CD para bundles. Los comandos de notebooks, `fs` o clústeres operan directamente sobre esos recursos, sin pasar por la capa de bundles.',
  },
  {
    n: 33,
    d: 'TRO',
    m: 0,
    q: 'An engineer sees `Hint: Photon does not support ...` warnings in the query plan for a SQL statement. What does this mean?',
    o: [
      'The cluster does not have Photon enabled.',
      'Parts of the query fell back to non-Photon execution because they used unsupported features (e.g., certain UDFs or operators).',
      'The query failed and produced no results.',
      'Photon is disabled at the workspace level.',
    ],
    a: [1],
    x: 'Photon ejecuta los operadores que soporta; los que no soporta caen a motor estándar, normalmente con este aviso. La query sigue funcionando con normalidad, solo pierde la aceleración de Photon en esa parte concreta del plan; si Photon estuviera desactivado, este mensaje ni siquiera aparecería porque el motor no lo consideraría en absoluto.',
  },
  {
    n: 34,
    d: 'TRO',
    m: 0,
    q: "Where should the engineer look first to diagnose a task that died with no Python traceback but the cluster's executors restarted around that time?",
    o: [
      'The Spark SQL query plan.',
      'The cluster event log and the executor logs (look for OOM, lost executor, decommission events).',
      'The Delta `_delta_log`.',
      'The `system.access.audit` table.',
    ],
    a: [1],
    x: 'Los eventos de OOM o pérdida de executor quedan registrados en el event log del clúster y en los logs de executor, que son la fuente correcta para fallos de infraestructura sin traceback en el propio código. El plan de query no registra fallos en tiempo de ejecución, `_delta_log` es específico de Delta, y las tablas de auditoría de UC cubren eventos de seguridad, no salud de executors.',
  },
  {
    n: 35,
    d: 'TRO',
    m: 0,
    q: 'In the Spark UI, which signal most clearly indicates a skewed shuffle stage?',
    o: [
      'The DAG has many stages.',
      'One or two task durations in a stage are 10-100x the median while others finish quickly.',
      'Total input size is very large.',
      'GC time across executors is uniformly low.',
    ],
    a: [1],
    x: 'El skew se manifiesta como un número pequeño de tareas que tardan muchísimo más que la mediana dentro de la misma etapa, normalmente con lecturas de shuffle mucho mayores. Muchas etapas o un input global grande no implican por sí solos desequilibrio en la distribución de los datos entre particiones.',
  },
  {
    n: 36,
    d: 'TRO',
    m: 0,
    q: 'An `events` table is partitioned by `event_date` (a date column). The table contains five years of data. Which of the following queries will most effectively use partition pruning to limit the amount of data scanned?',
    o: [
      '```\nSELECT * FROM events WHERE year(event_date) = 2025\n```',
      "```\nSELECT * FROM events WHERE event_date_str = '2025-10-20'\n```",
      "```\nSELECT * FROM events WHERE event_date = '2025-10-20'\n```",
      '```\nSELECT * FROM events\n```',
    ],
    a: [2],
    x: 'El partition pruning necesita un filtro directo sobre la columna de partición sin transformarla; `WHERE event_date = ...` deja a Spark navegar directamente a los ficheros de esa partición. Aplicar una función como `year()` sobre la columna suele impedir el pruning y fuerza a escanear todas las particiones; filtrar por una columna distinta (`event_date_str`) no aprovecha el particionado en absoluto.',
  },
  {
    n: 37,
    d: 'TRO',
    m: 0,
    q: 'What is the primary advantage of enabling the Photon compute engine for a Databricks Job & Pipeline run?',
    o: [
      'It reduces data storage costs in Unity Catalog Volumes.',
      'It provides a native, enhanced SQL query editor.',
      'It accelerates Spark SQL and DataFrame API workloads.',
      'It automatically manages Python library dependencies.',
    ],
    a: [2],
    x: 'Photon es un motor nativo en C++ que acelera las llamadas a las APIs de Spark, mejorando notablemente el rendimiento de cargas SQL y DataFrame sin necesidad de cambiar código. No influye en costes de almacenamiento en Volumes, no es una herramienta de edición de SQL, ni gestiona dependencias de librerías Python.',
  },
  {
    n: 38,
    d: 'TRO',
    m: 0,
    q: 'A bad write corrupted a Delta table an hour ago. Which feature lets you read or restore an earlier version?',
    o: ['Delta time travel using VERSION AS OF or TIMESTAMP AS OF', '`VACUUM`', 'A larger driver', 'Photon'],
    a: [0],
    x: 'Delta Lake mantiene un historial transaccional que permite consultar una versión anterior por número o timestamp con time travel, y `RESTORE` puede revertir la tabla a ese punto, lo que ayuda a recuperarse de una escritura defectuosa. `VACUUM` hace justo lo contrario: elimina ficheros antiguos ya no referenciados, reduciendo la ventana de recuperación en vez de ampliarla.',
  },
  {
    n: 39,
    d: 'TRO',
    m: 0,
    q: 'What does `DESCRIBE HISTORY <table>` provide?',
    o: [
      'The current data of every version of the table.',
      'A list of commits with operation type, user, timestamp, and operation metrics; it does not return row data.',
      'Row-level change events similar to CDF.',
      'The Spark plan used for each commit.',
    ],
    a: [1],
    x: '`DESCRIBE HISTORY` lista metadatos de cada commit (operación, usuario, momento, métricas y versión), pero no devuelve filas. Para leer los datos de una versión concreta hace falta time travel (`VERSION AS OF` / `TIMESTAMP AS OF`), y para ver los cambios fila a fila hace falta Change Data Feed, dos funcionalidades distintas de esta.',
  },
  {
    n: 40,
    d: 'TRO',
    m: 0,
    q: 'What is the default retention for `VACUUM` on a Delta table, and why is reducing it risky?',
    o: [
      '1 hour; reducing it has no effect.',
      '7 days; reducing it below the longest-running query risks deleting files that active queries still need (and breaks time travel beyond that point).',
      '30 minutes; reducing it improves time travel.',
      'Forever; `VACUUM` never deletes by default.',
    ],
    a: [1],
    x: 'La retención por defecto de `VACUUM` es de 7 días; reducirla agresivamente puede borrar ficheros de datos que consultas concurrentes todavía necesitan o que hacen falta para time travel más allá de ese punto. La comprobación de seguridad que exige forzar el borrado por debajo de ese umbral existe precisamente para evitar este riesgo.',
  },
  {
    n: 41,
    d: 'GOV',
    m: 0,
    q: 'In Unity Catalog, what is the correct three-level namespace used to reference a table?',
    o: ['database.table', 'catalog.schema.table', 'workspace.folder.table', 'cluster.node.table'],
    a: [1],
    x: 'Unity Catalog organiza los objetos con un namespace de tres niveles: catálogo como contenedor superior, esquema (equivalente a una base de datos) agrupando tablas, y la tabla en sí. Ninguna de las otras combinaciones refleja esta jerarquía real de UC.',
  },
  {
    n: 42,
    d: 'GOV',
    m: 0,
    q: 'A data analyst needs read-only access to the `orders` table in the `sales_data` schema. Which SQL command should an administrator use to grant this permission?',
    o: [
      'GRANT SELECT ON TABLE sales_data.orders TO `analyst_group`',
      'ALLOW READ ON sales_data.orders TO `analyst_group`',
      'PERMIT SELECT ON TABLE sales_data.orders FOR `analyst_group`',
      'SET PERMISSION READ ON sales_data.orders TO `analyst_group`',
    ],
    a: [0],
    x: 'El comando estándar de Unity Catalog para gestionar permisos es `GRANT`, y el privilegio `SELECT` es específicamente el que habilita lectura sobre una tabla. `ALLOW`, `PERMIT` y `SET PERMISSION` no son sintaxis válida de UC.',
  },
  {
    n: 43,
    d: 'GOV',
    m: 0,
    q: 'A user holds `SELECT` on `prod.silver.orders` via both a direct grant and membership in a group that also has `SELECT`. The admin runs `REVOKE SELECT ON TABLE prod.silver.orders FROM "user@co";`. What happens?',
    o: [
      'The user loses access immediately because REVOKE always wins.',
      'The user keeps access via the group grant; only the direct grant was removed.',
      'Both the direct and the group grant are removed.',
      'The user keeps access, but the group also loses the grant.',
    ],
    a: [1],
    x: 'Los grants de UC son aditivos por principal: revocar del usuario solo elimina su concesión directa, y el grant heredado del grupo sigue vigente de forma independiente. Para cortar el acceso por completo haría falta revocar también del grupo, o sacar al usuario del grupo.',
  },
  {
    n: 44,
    d: 'GOV',
    m: 0,
    q: 'An admin wants to make sure a specific user can never read a sensitive table, even if they are added to a group that has SELECT. Which is the correct approach?',
    o: [
      '`REVOKE SELECT from the user.`',
      'DENY SELECT to the user.',
      'Drop the table for that user only.',
      'Set the table to read-only in workspace settings.',
    ],
    a: [1],
    x: '`DENY` bloquea explícitamente un privilegio y tiene prioridad sobre cualquier grant heredado, incluido el de pertenencia a un grupo. `REVOKE` solo elimina una concesión existente, así que el usuario podría seguir teniendo `SELECT` a través del grupo; no existe un "drop" por usuario ni un ajuste de solo lectura por workspace para una tabla concreta.',
  },
  {
    n: 45,
    d: 'GOV',
    m: 0,
    q: 'A data engineer wants to trace how a specific column in a final report table was calculated. What is the most granular level of detail they can expect to see in the Unity Catalog lineage graph?',
    o: [
      'The lineage is only available at the table level.',
      'Lineage is captured at both the table level and column level.',
      'Lineage is only captured at the Job Run level.',
      'Lineage is only captured for tables, not views or dashboards.',
    ],
    a: [1],
    x: 'Unity Catalog captura automáticamente el lineage de todas las consultas ejecutadas en un clúster, y ese grafo llega hasta el nivel de columna individual, no solo de tabla, además de cubrir tablas, vistas, notebooks y dashboards.',
  },
];

export const exam11: RawQuestion[] = [
  {
    n: 1,
    d: 'P',
    m: 0,
    q: 'Which of the following describes a benefit of a data lakehouse that is unavailable in a traditional data warehouse?',
    o: [
      'A data lakehouse provides a relational system of data management.',
      'A data lakehouse captures snapshots of data for version control purposes.',
      'A data lakehouse couples storage and compute for complete control.',
      'A data lakehouse utilizes proprietary storage formats for data.',
      'A data lakehouse enables both batch and streaming analytics.',
    ],
    a: [4],
    x: 'Un data lakehouse soporta de forma nativa cargas de trabajo tanto batch como streaming sobre la misma copia de datos, algo que un data warehouse tradicional no ofrece. Las otras opciones describen características de un warehouse clásico (sistema relacional) o son directamente falsas: un lakehouse desacopla almacenamiento y cómputo, y usa formatos abiertos (no propietarios).',
  },
  {
    n: 2,
    d: 'P',
    m: 0,
    q: 'Which of the following locations hosts the driver and worker nodes of a Databricks-managed cluster?',
    o: [
      'Data plane',
      'Control plane',
      'Databricks Filesystem',
      'JDBC data source',
      'Databricks web application',
    ],
    a: [0],
    x: 'El driver y los workers de un clúster se ejecutan en el data plane (dentro de la cuenta cloud del cliente en compute clásico), mientras que el control plane aloja la aplicación web, el scheduler y el metastore, sin ejecutar código de usuario.',
  },
  {
    n: 3,
    d: 'P',
    m: 0,
    q: 'A data architect is designing a data model that works for both video-based machine learning workloads and highly audited batch ETL/ELT workloads.\n\nWhich of the following describes how using a data lakehouse can help the data architect meet the needs of both workloads?',
    o: [
      'A data lakehouse requires very little data modeling.',
      'A data lakehouse combines compute and storage for simple governance.',
      'A data lakehouse provides autoscaling for compute clusters.',
      'A data lakehouse stores unstructured data and is ACID-compliant.',
      'A data lakehouse fully exists in the cloud.',
    ],
    a: [3],
    x: 'La combinación de soporte para datos no estructurados (video) y garantías ACID (auditoría estricta de ETL/ELT) es precisamente lo que permite a un lakehouse cubrir ambos casos de uso con una sola plataforma. El resto de opciones son verdaderas de forma parcial o irrelevantes para el requisito planteado.',
  },
  {
    n: 4,
    d: 'P',
    m: 0,
    q: 'Which of the following describes a scenario in which a data engineer will want to use a Job cluster instead of an all-purpose cluster?',
    o: [
      'An ad-hoc analytics report needs to be developed while minimizing compute costs.',
      'A data team needs to collaborate on the development of a machine learning model.',
      'An automated workflow needs to be run every 30 minutes.',
      'A Databricks SQL query needs to be scheduled for upward reporting.',
      'A data engineer needs to manually investigate a production error.',
    ],
    a: [2],
    x: 'Un job cluster es el más adecuado para cargas automatizadas y programadas: se crea al iniciar la ejecución y se termina al finalizar, sin coste de tiempo ocioso. El desarrollo ad-hoc, la colaboración o la investigación manual necesitan un clúster all-purpose que permanezca activo entre sesiones.',
  },
  {
    n: 5,
    d: 'GOV',
    m: 0,
    q: 'A data engineer has created a Delta table as part of a data pipeline. Downstream data analysts now need SELECT permission on the Delta table.\n\nAssuming the data engineer is the Delta table owner, which part of the Databricks Lakehouse Platform can the data engineer use to grant the data analysts the appropriate access?',
    o: ['Repos', 'Jobs', 'Data Explorer', 'Databricks Filesystem', 'Dashboards'],
    a: [2],
    x: 'Data Explorer (hoy Catalog Explorer) es la interfaz donde el propietario de una tabla puede gestionar y conceder permisos como SELECT sobre sus objetos. Repos, Jobs y Dashboards no tienen relación con la gestión de permisos de tablas.',
  },
  {
    n: 6,
    d: 'P',
    m: 0,
    q: 'Two junior data engineers are authoring separate parts of a single data pipeline notebook. They are working on separate Git branches so they can pair program on the same notebook simultaneously. A senior data engineer experienced in Databricks suggests there is a better alternative for this type of collaboration.\n\nWhich of the following supports the senior data engineer’s claim?',
    o: [
      'Databricks Notebooks support automatic change-tracking and versioning',
      'Databricks Notebooks support real-time coauthoring on a single notebook',
      'Databricks Notebooks support commenting and notification comments',
      'Databricks Notebooks support the use of multiple languages in the same notebook',
      'Databricks Notebooks support the creation of interactive data visualizations',
    ],
    a: [1],
    x: 'Los notebooks de Databricks permiten coautoría en tiempo real sobre el mismo notebook, evitando la necesidad de ramas Git separadas solo para poder trabajar simultáneamente en el mismo fichero. Las otras opciones son características reales de los notebooks, pero no resuelven el problema concreto de colaborar en tiempo real sobre el mismo documento.',
  },
  {
    n: 7,
    d: 'CICD',
    m: 0,
    q: 'Which of the following describes how Databricks Repos can help facilitate CI/CD workflows on the Databricks Lakehouse Platform?',
    o: [
      'Databricks Repos can facilitate the pull request, review, and approval process before merging branches',
      'Databricks Repos can merge changes from a secondary Git branch into a main Git branch',
      'Databricks Repos can be used to design, develop, and trigger Git automation pipelines',
      'Databricks Repos can store the single-source-of-truth Git repository',
      'Databricks Repos can commit or push code changes to trigger a CI/CD process',
    ],
    a: [4],
    x: 'Databricks Repos permite hacer commit y push de cambios de código, lo que puede disparar un proceso de CI/CD configurado en el proveedor Git. Operaciones como pull requests, merges o el diseño de pipelines de automatización deben realizarse en el propio proveedor Git (GitHub, GitLab...), no dentro de Databricks Repos.',
  },
  {
    n: 8,
    d: 'TRA',
    m: 0,
    q: 'Which of the following statements describes Delta Lake?',
    o: [
      'Delta Lake is an open source analytics engine used for big data workloads.',
      'Delta Lake is an open format storage layer that delivers reliability, security, and performance.',
      'Delta Lake is an open source platform to help manage the complete machine learning lifecycle.',
      'Delta Lake is an open source data storage format for distributed data.',
      'Delta Lake is an open format storage layer that processes data.',
    ],
    a: [1],
    x: 'Delta Lake es una capa de almacenamiento de formato abierto que añade fiabilidad (transacciones ACID), seguridad y rendimiento sobre archivos Parquet. No es un motor de analítica ni una plataforma de gestión del ciclo de vida de ML (eso sería MLflow), y su rol no es "procesar" datos sino gobernar cómo se almacenan y versionan.',
  },
  {
    n: 9,
    d: 'TRA',
    m: 0,
    q: 'A data architect has determined that a table of the following format is necessary: id (STRING), birthDate (DATE), avgRating (FLOAT) — example rows: a1 / 1990-01-06 / 5.5, a2 / 1974-11-21 / 7.1.\n\nWhich of the following code blocks uses SQL DDL commands to create an empty Delta table in the above format regardless of whether a table already exists with this name?',
    o: [
      'CREATE OR REPLACE TABLE table_name AS SELECT id STRING, birthDate DATE, avgRating FLOAT USING DELTA',
      'CREATE OR REPLACE TABLE table_name (id STRING, birthDate DATE, avgRating FLOAT)',
      'CREATE TABLE IF NOT EXISTS table_name (id STRING, birthDate DATE, avgRating FLOAT)',
      'CREATE TABLE table_name AS SELECT id STRING, birthDate DATE, avgRating FLOAT',
      'CREATE OR REPLACE TABLE table_name WITH COLUMNS (id STRING, birthDate DATE, avgRating FLOAT) USING DELTA',
    ],
    a: [1],
    x: '`CREATE OR REPLACE TABLE` con una lista explícita de columnas crea una tabla vacía con ese esquema exacto, sustituyendo cualquier tabla previa con el mismo nombre. Las variantes con `AS SELECT` intentarían poblar la tabla con el resultado de una consulta (sintaxis inválida aquí, ya que se listan tipos de columna, no una query real), y `WITH COLUMNS` no es sintaxis válida de Delta DDL.',
  },
  {
    n: 10,
    d: 'TRA',
    m: 0,
    q: 'Which of the following SQL keywords can be used to append new rows to an existing Delta table?',
    o: ['`UPDATE`', '`COPY`', '`INSERT INTO`', '`DELETE`', '`UNION`'],
    a: [2],
    x: '`INSERT INTO` añade nuevas filas a una tabla existente sin afectar a las ya presentes. `UPDATE` y `DELETE` modifican/eliminan filas existentes, `COPY` no es una keyword SQL de Delta, y `UNION` combina resultados de consultas, no escribe en una tabla.',
  },
  {
    n: 11,
    d: 'TRO',
    m: 0,
    q: 'A data engineering team needs to query a Delta table to extract rows that all meet the same condition. However, the team has noticed that the query is running slowly. The team has already tuned the size of the data files. Upon investigating, the team has concluded that the rows meeting the condition are sparsely located throughout each of the data files.\n\nBased on the scenario, which of the following optimization techniques could speed up the query?',
    o: ['Data skipping', 'Z-Ordering', 'Bin-packing', 'Write as a Parquet file', 'Tuning the file size'],
    a: [1],
    x: 'Z-Ordering co-localiza físicamente los valores similares de una columna dentro de los archivos, de modo que las estadísticas de min/max por archivo permiten saltarse (data skipping) más archivos en la consulta. El data skipping por sí solo ya se usa automáticamente, pero aquí el problema es que las filas relevantes están dispersas dentro de cada archivo; Z-Ordering es la técnica que resuelve exactamente ese problema de dispersión.',
  },
  {
    n: 12,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to create a database called customer360 at the location /customer/customer360. The data engineer is unsure if one of their colleagues has already created the database.\n\nWhich of the following commands should the data engineer run to complete this task?',
    o: [
      "```\nCREATE DATABASE customer360 LOCATION '/customer/customer360';\n```",
      '```\nCREATE DATABASE IF NOT EXISTS customer360;\n```',
      "```\nCREATE DATABASE IF NOT EXISTS customer360 LOCATION '/customer/customer360';\n```",
      "```\nCREATE DATABASE IF NOT EXISTS customer360 DELTA LOCATION '/customer/customer360';\n```",
      "```\nCREATE DATABASE customer360 DELTA LOCATION '/customer/customer360';\n```",
    ],
    a: [2],
    x: '`CREATE DATABASE IF NOT EXISTS ... LOCATION ...` cubre ambos requisitos a la vez: evita el error si la base de datos ya existe (`IF NOT EXISTS`) y especifica la ubicación exacta pedida. La keyword `DELTA` no es válida en `CREATE DATABASE`, ya que ese comando no se asocia a un formato de tabla concreto.',
  },
  {
    n: 13,
    d: 'TRA',
    m: 0,
    q: 'A junior data engineer needs to create a Spark SQL table my_table for which Spark manages both the data and the metadata. The metadata and data should also be stored in the Databricks Filesystem (DBFS).\n\nWhich of the following commands should a senior data engineer share with the junior data engineer to complete this task?',
    o: [
      '```\nCREATE TABLE my_table (id STRING, value STRING) USING org.apache.spark.sql.parquet OPTIONS (PATH "storage-path");\n```',
      '```\nCREATE MANAGED TABLE my_table (id STRING, value STRING) USING org.apache.spark.sql.parquet OPTIONS (PATH "storage-path");\n```',
      '```\nCREATE MANAGED TABLE my_table (id STRING, value STRING);\n```',
      '```\nCREATE TABLE my_table (id STRING, value STRING) USING DBFS;\n```',
      '```\nCREATE TABLE my_table (id STRING, value STRING);\n```',
    ],
    a: [4],
    x: 'Una sentencia `CREATE TABLE` simple, sin especificar una ruta externa (`PATH`) ni usar la keyword inválida `MANAGED`, crea automáticamente una tabla managed cuyos datos y metadatos gestiona Spark en el almacenamiento por defecto (DBFS en este contexto). Especificar un `PATH` externo convertiría la tabla en external, y `CREATE MANAGED TABLE`/`USING DBFS` no son sintaxis válidas.',
  },
  {
    n: 14,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to create a relational object by pulling data from two tables. The relational object must be used by other data engineers in other sessions. In order to save on storage costs, the data engineer wants to avoid copying and storing physical data.\n\nWhich of the following relational objects should the data engineer create?',
    o: ['View', 'Temporary view', 'Delta Table', 'Database', 'Spark SQL Table'],
    a: [0],
    x: 'Una vista (View) es un objeto puramente lógico: no almacena datos físicos y se recalcula bajo demanda, pero al contrario que una vista temporal, persiste y es accesible desde otras sesiones. Una tabla Delta o Spark SQL sí copiaría y almacenaría los datos físicamente, y una vista temporal no sobrevive más allá de la sesión actual.',
  },
  {
    n: 15,
    d: 'TRA',
    m: 0,
    q: 'A data engineering team has created a series of tables using Parquet data stored in an external system. The team is noticing that after appending new rows to the data in the external system, their queries within Databricks are not returning the new rows. They identify the caching of the previous data as the cause of this issue.\n\nWhich of the following approaches will ensure that the data returned by queries is always up-to-date?',
    o: [
      'The tables should be converted to the Delta format',
      'The tables should be stored in a cloud-based external system',
      'The tables should be refreshed in the writing cluster before the next query is run',
      'The tables should be altered to include metadata to not cache',
      'The tables should be updated before the next query is run',
    ],
    a: [0],
    x: 'Convertir las tablas al formato Delta resuelve el problema de raíz: Delta Lake mantiene un log de transacciones que siempre refleja el estado actual de los datos, evitando el problema de caché obsoleta típico de leer directamente archivos Parquet externos sin ese control transaccional.',
  },
  {
    n: 16,
    d: 'TRA',
    m: 0,
    q: 'A table customerLocations exists with the following schema: id STRING, date STRING, city STRING, country STRING. A senior data engineer wants to create a new table using: CREATE TABLE customersPerCountry AS SELECT country, COUNT(*) AS customers FROM customerLocations GROUP BY country;\n\nA junior data engineer asks why the schema is not being declared for the new table. Which of the following responses explains why declaring the schema is not necessary?',
    o: [
      'CREATE TABLE AS SELECT statements adopt schema details from the source table and query.',
      'CREATE TABLE AS SELECT statements infer the schema by scanning the data.',
      'CREATE TABLE AS SELECT statements result in tables where schemas are optional.',
      'CREATE TABLE AS SELECT statements assign all columns the type STRING.',
      'CREATE TABLE AS SELECT statements result in tables that do not support schemas.',
    ],
    a: [0],
    x: 'Una sentencia CTAS (`CREATE TABLE AS SELECT`) adopta automáticamente el esquema resultante de la propia consulta (nombres y tipos de columna calculados por la query), por lo que nunca es necesario declararlo manualmente. No se trata de inferencia por escaneo de datos, ni de que el esquema sea "opcional" o inexistente: siempre existe, solo que se deriva de la query.',
  },
  {
    n: 17,
    d: 'TRA',
    m: 0,
    q: 'A data engineer is overwriting data in a table by deleting the table and recreating the table. Another data engineer suggests that this is inefficient and the table should simply be overwritten instead.\n\nWhich of the following reasons to overwrite the table instead of deleting and recreating the table is INCORRECT?',
    o: [
      'Overwriting a table is efficient because no files need to be deleted.',
      'Overwriting a table results in a clean table history for logging and audit purposes.',
      'Overwriting a table maintains the old version of the table for Time Travel.',
      'Overwriting a table is an atomic operation and will not leave the table in an unfinished state.',
      'Overwriting a table allows for concurrent queries to be completed while in progress.',
    ],
    a: [1],
    x: 'Sobrescribir una tabla Delta NO produce un historial "limpio": al contrario, preserva todo el historial de versiones anteriores en el log de transacciones (permitiendo Time Travel), a diferencia de borrar y recrear la tabla, que sí eliminaría ese historial. Esa es la afirmación incorrecta del enunciado; el resto de razones (atomicidad, eficiencia, no bloquear consultas concurrentes) son ventajas reales de `overwrite`.',
  },
  {
    n: 18,
    d: 'TRA',
    m: 0,
    q: 'Which of the following commands will return records from an existing Delta table my_table where duplicates have been removed?',
    o: [
      '```\nDROP DUPLICATES FROM my_table;\n```',
      '```\nSELECT * FROM my_table WHERE duplicate = False;\n```',
      '```\nSELECT DISTINCT * FROM my_table;\n```',
      'MERGE INTO my_table a USING new_records b ON a.id = b.id WHEN NOT MATCHED THEN INSERT *;',
      '```\nMERGE INTO my_table a USING new_records b;\n```',
    ],
    a: [2],
    x: '`SELECT DISTINCT *` devuelve las filas de la tabla eliminando duplicados exactos en el resultado de la consulta. `DROP DUPLICATES` no es sintaxis SQL válida, y las opciones con `MERGE` sirven para fusionar datos de otra tabla, no para deduplicar la tabla actual.',
  },
  {
    n: 19,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to horizontally combine two tables as a part of a query. They want to use a shared column as a key column, and they only want the query result to contain rows whose value in the key column is present in both tables.\n\nWhich of the following SQL commands can they use to accomplish this task?',
    o: ['`INNER JOIN`', '`OUTER JOIN`', '`LEFT JOIN`', '`MERGE`', '`UNION`'],
    a: [0],
    x: 'Un INNER JOIN devuelve únicamente las filas cuya clave existe en ambas tablas, que es exactamente el requisito planteado. LEFT/OUTER JOIN incluirían además filas sin coincidencia en el otro lado, MERGE se usa para upserts (no para combinar horizontalmente en una consulta), y UNION apila filas en vertical en vez de combinar columnas.',
  },
  {
    n: 20,
    d: 'TRA',
    m: 0,
    q: 'A junior data engineer has ingested a JSON file into a table raw_table with schema: cart_id STRING, items ARRAY<item_id:STRING>. They would like to unnest the items column to result in a new table with schema: cart_id STRING, item_id STRING.\n\nWhich of the following commands should the junior data engineer run to complete this task?',
    o: [
      '```\nSELECT cart_id, filter(items) AS item_id FROM raw_table;\n```',
      '```\nSELECT cart_id, flatten(items) AS item_id FROM raw_table;\n```',
      '```\nSELECT cart_id, reduce(items) AS item_id FROM raw_table;\n```',
      '```\nSELECT cart_id, explode(items) AS item_id FROM raw_table;\n```',
      '```\nSELECT cart_id, slice(items) AS item_id FROM raw_table;\n```',
    ],
    a: [3],
    x: '`explode()` convierte cada elemento de un array en una fila independiente, produciendo exactamente el cambio de grano de "un carrito con varios items" a "una fila por item" que pide el enunciado. `filter`/`reduce`/`slice` no cambian el número de filas, y `flatten` solo aplana arrays anidados sin generar filas nuevas.',
  },
  {
    n: 21,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has ingested a JSON file into a table raw_table with schema: transaction_id STRING, payload ARRAY<customer_id:STRING, date:TIMESTAMP, store_id:STRING>. The data engineer wants to efficiently extract the date of each transaction into a table with schema: transaction_id STRING, date TIMESTAMP.\n\nWhich of the following commands should the data engineer run to complete this task?',
    o: [
      '```\nSELECT transaction_id, explode(payload) FROM raw_table;\n```',
      '```\nSELECT transaction_id, payload.date FROM raw_table;\n```',
      '```\nSELECT transaction_id, date FROM raw_table;\n```',
      '```\nSELECT transaction_id, payload[date] FROM raw_table;\n```',
      '```\nSELECT transaction_id, date from payload FROM raw_table;\n```',
    ],
    a: [1],
    x: 'La notación de punto (`payload.date`) accede directamente al campo `date` dentro de una columna STRUCT anidada, que es la forma correcta y eficiente de extraerlo sin necesidad de explotar el array. `payload[date]` no es sintaxis SQL válida para acceso a campos, y `explode()` cambiaría el grano de la tabla innecesariamente.',
  },
  {
    n: 22,
    d: 'TRA',
    m: 0,
    q: 'A data analyst has provided the query: SELECT district, avg(sales) FROM store_sales_20220101 GROUP BY district; They would like the data engineering team to run this query every day, with the date at the end of the table name (20220101) automatically replaced with the current date each time.\n\nWhich of the following approaches could be used to efficiently automate this process?',
    o: [
      "They could wrap the query using PySpark and use Python's string variable system to automatically update the table name.",
      "They could manually replace the date within the table name with the current day's date.",
      'They could request that the data analyst rewrites the query to be run less frequently.',
      'They could replace the string-formatted date in the table with a timestamp-formatted date.',
      'They could pass the table into PySpark and develop a robustly tested module on the existing query.',
    ],
    a: [0],
    x: 'Envolver la consulta en PySpark y construir dinámicamente el nombre de la tabla con variables de cadena (p. ej. un f-string con la fecha actual) automatiza el proceso sin intervención manual diaria. Reemplazar la fecha manualmente cada día no es una automatización real, y las demás opciones no resuelven el problema concreto planteado.',
  },
  {
    n: 23,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has ingested data from an external source into a PySpark DataFrame raw_df. They need to briefly make this data available in SQL for a data analyst to perform a quality assurance check on the data.\n\nWhich of the following commands should the data engineer run to make this data available in SQL for only the remainder of the Spark session?',
    o: [
      '`raw_df.createOrReplaceTempView("raw_df")`',
      '`raw_df.createTable("raw_df")`',
      '`raw_df.write.save("raw_df")`',
      '`raw_df.saveAsTable("raw_df")`',
      'There is no way to share data between PySpark and SQL.',
    ],
    a: [0],
    x: '`createOrReplaceTempView` registra el DataFrame como una vista temporal, consultable con SQL solo durante la sesión de Spark actual, sin persistir nada en disco. `saveAsTable` y `write.save` sí escriben datos físicamente y persisten más allá de la sesión, lo cual excede lo que pide el enunciado ("briefly", "only for the remainder of the session").',
  },
  {
    n: 24,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to dynamically create a table name string using three Python variables: region, store, and year. Example: region="nyc", store="100", year="2021" should produce nyc100_sales_2021.\n\nWhich of the following commands should the data engineer use to construct the table name in Python?',
    o: [
      '"{region}+{store}+_sales_+{year}"',
      'f"{region}+{store}+_sales_+{year}"',
      '"{region}{store}_sales_{year}"',
      'f"{region}{store}_sales_{year}"',
      '{region}+{store}+"_sales_"+{year}',
    ],
    a: [3],
    x: 'Un f-string de Python (`f"..."`) con las variables directamente dentro de llaves, sin operadores `+` de por medio, produce la concatenación literal esperada (`nyc100_sales_2021`). Las variantes sin el prefijo `f` tratarían las llaves como texto literal, y las que usan `+` insertarían el carácter "+" real en el resultado.',
  },
  {
    n: 25,
    d: 'ING',
    m: 0,
    q: 'A data engineer has developed a code block to perform a streaming read on a data source: spark.read.schema(schema).format("cloudFiles").option("cloudFiles.format", "json").load(dataSource). The code block is returning an error.\n\nWhich of the following changes should be made to the code block to configure the block to successfully perform a streaming read?',
    o: [
      'The .read line should be replaced with .readStream.',
      'A new .stream line should be added after the .read line.',
      'The .format("cloudFiles") line should be replaced with .format("stream").',
      'A new .stream line should be added after the spark line.',
      'A new .stream line should be added after the .load(dataSource) line.',
    ],
    a: [0],
    x: 'Para realizar una lectura de streaming con Auto Loader hay que usar `spark.readStream` en vez de `spark.read` — este último es para lecturas batch/estáticas y no es compatible con la fuente `cloudFiles`, de ahí el error. No existe ningún método `.stream` independiente que se pueda añadir a la cadena.',
  },
  {
    n: 26,
    d: 'ING',
    m: 0,
    q: 'A data engineer has configured a Structured Streaming job to read from a table, manipulate the data, and then perform a streaming write into a new table: spark.table("sales").withColumn(...).writeStream.option("checkpointLocation", checkpointPath).outputMode("complete")._____.table("new_sales")\n\nIf the data engineer only wants the query to execute a single micro-batch to process all of the available data, which line of code should fill in the blank?',
    o: [
      '`trigger(once=True)`',
      '`trigger(continuous="once")`',
      '`processingTime("once")`',
      '`trigger(processingTime="once")`',
      '`processingTime(1)`',
    ],
    a: [0],
    x: '`trigger(once=True)` procesa todos los datos disponibles en un único micro-batch y detiene el stream a continuación — el comportamiento exacto pedido. `processingTime` y `continuous` son parámetros de otros modos de trigger que mantienen el stream corriendo continuamente, no lo detienen tras un solo batch.',
  },
  {
    n: 27,
    d: 'ING',
    m: 0,
    q: 'A data engineer is designing a data pipeline. The source system generates files in a shared directory that is also used by other processes, so files accumulate and should be kept as is. The data engineer needs to identify which files are new since the previous run and ingest only those.\n\nWhich of the following tools can the data engineer use to solve this problem?',
    o: ['Databricks SQL', 'Delta Lake', 'Unity Catalog', 'Data Explorer', 'Auto Loader'],
    a: [4],
    x: 'Auto Loader está diseñado exactamente para este escenario: detecta e ingiere incrementalmente solo los archivos nuevos desde la última ejecución, llevando el seguimiento mediante un checkpoint, sin necesidad de mover ni borrar los archivos originales del directorio compartido.',
  },
  {
    n: 28,
    d: 'ING',
    m: 0,
    q: 'A data engineering team is converting their existing data pipeline to utilize Auto Loader. A data engineer finds this code block in the documentation: streaming_df = spark.readStream.format("cloudFiles").option("cloudFiles.format", "json").option("cloudFiles.schemaLocation", schemaLocation).load(sourcePath)\n\nAssuming schemaLocation and sourcePath are set correctly, which changes are needed to convert this code block to use Auto Loader?',
    o: [
      'The data engineer needs to change the format("cloudFiles") line to format("autoLoader").',
      'There is no change required. Databricks automatically uses Auto Loader for streaming reads.',
      'There is no change required. The inclusion of format("cloudFiles") enables the use of Auto Loader.',
      'The data engineer needs to add the .autoLoader line before the .load(sourcePath) line.',
      'There is no change required. The data engineer needs to ask their administrator to turn on Auto Loader.',
    ],
    a: [2],
    x: 'El código ya usa Auto Loader correctamente: `format("cloudFiles")` es precisamente el identificador de la fuente de streaming de Auto Loader, no un formato distinto que haya que activar. No existe un `format("autoLoader")` ni un método `.autoLoader`, y no requiere ninguna activación administrativa especial.',
  },
  {
    n: 29,
    d: 'TRA',
    m: 0,
    q: 'Which of the following data workloads will utilize a Bronze table as its source?',
    o: [
      'A job that aggregates cleaned data to create standard summary statistics',
      'A job that queries aggregated data to publish key insights into a dashboard',
      'A job that ingests raw data from a streaming source into the Lakehouse',
      'A job that develops a feature set for a machine learning application',
      'A job that enriches data by parsing its timestamps into a human-readable format',
    ],
    a: [4],
    x: 'Enriquecer datos parseando timestamps es una transformación típica de Bronze a Silver, por lo que su fuente es una tabla Bronze (datos crudos, tal cual llegaron). Ingerir datos crudos desde streaming es lo que *produce* la tabla Bronze (no la usa como fuente), y las opciones de agregación/features consumen datos ya limpios (Silver), no Bronze.',
  },
  {
    n: 30,
    d: 'TRA',
    m: 0,
    q: 'Which of the following data workloads will utilize a Silver table as its source?',
    o: [
      'A job that enriches data by parsing its timestamps into a human-readable format',
      'A job that queries aggregated data that already feeds into a dashboard',
      'A job that ingests raw data from a streaming source into the Lakehouse',
      'A job that aggregates cleaned data to create standard summary statistics',
      'A job that cleans data by removing malformatted records',
    ],
    a: [3],
    x: 'Agregar datos ya limpios para crear estadísticas resumen es un trabajo típico de Silver a Gold, por lo que su fuente es una tabla Silver (datos ya conformados y depurados). Limpiar registros mal formados es lo que *produce* la tabla Silver a partir de Bronze, no lo que la consume como fuente.',
  },
  {
    n: 31,
    d: 'TRA',
    m: 0,
    q: 'Which of the following Structured Streaming queries is performing a hop from a Bronze table to a Silver table?',
    o: [
      '```\nspark.table("sales").groupBy("store").agg(sum("sales")).writeStream...outputMode("complete").table("aggregatedSales")\n```',
      '```\nspark.table("sales").agg(sum("sales"), sum("units")).writeStream...outputMode("complete").table("aggregatedSales")\n```',
      '```\nspark.table("sales").withColumn("avgPrice", col("sales") / col("units")).writeStream...outputMode("append").table("cleanedSales")\n```',
      '```\nspark.readStream.load(rawSalesLocation).writeStream...outputMode("append").table("uncleanedSales")\n```',
      '```\nspark.read.load(rawSalesLocation).writeStream...outputMode("append").table("uncleanedSales")\n```',
    ],
    a: [2],
    x: 'La opción que añade una columna calculada (limpieza/enriquecimiento ligero) y escribe en una tabla llamada "cleanedSales" con `outputMode("append")` es el patrón típico de un salto Bronze→Silver. Las que agregan datos (`groupBy`/`agg`) van de Silver a Gold, y la que solo carga datos crudos sin transformar (`uncleanedSales`) es la ingesta hacia Bronze, no un hop hacia Silver.',
  },
  {
    n: 32,
    d: 'TRA',
    m: 0,
    q: 'Which of the following benefits does Delta Live Tables provide for ELT pipelines over standard data pipelines that utilize Spark and Delta Lake on Databricks?',
    o: [
      'The ability to declare and maintain data table dependencies',
      'The ability to write pipelines in Python and/or SQL',
      'The ability to access previous versions of data tables',
      'The ability to automatically scale compute resources',
      'The ability to perform batch and streaming queries',
    ],
    a: [0],
    x: 'La ventaja diferencial de Delta Live Tables (hoy Lakeflow Declarative Pipelines) es declarar el resultado deseado de cada tabla y dejar que la plataforma gestione automáticamente el orden de ejecución y las dependencias entre ellas. Las demás capacidades (Python/SQL, time travel, autoscaling, batch+streaming) ya existen en Spark y Delta Lake sin necesidad de DLT.',
  },
  {
    n: 33,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has three notebooks in an ELT pipeline. The notebooks need to be executed in a specific order for the pipeline to complete successfully. The data engineer would like to use Delta Live Tables to manage this process.\n\nWhich of the following steps must the data engineer take as part of implementing this pipeline using Delta Live Tables?',
    o: [
      'They need to create a Delta Live Tables pipeline from the Data page.',
      'They need to create a Delta Live Tables pipeline from the Jobs page.',
      'They need to create a Delta Live tables pipeline from the Compute page.',
      'They need to refactor their notebook to use Python and the dlt library.',
      'They need to refactor their notebook to use SQL and CREATE LIVE TABLE keyword.',
    ],
    a: [1],
    x: 'Un pipeline de Delta Live Tables se crea desde la página de Jobs/Workflows (donde se gestionan también los pipelines declarativos), no desde Data ni Compute. No es obligatorio migrar a Python+dlt ni a SQL con CREATE LIVE TABLE: cualquiera de los dos lenguajes es válido, no es un paso obligatorio elegir uno en concreto.',
  },
  {
    n: 34,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has written the query: SELECT * FROM json.`/path/to/json/file.json`; They ask a colleague for help to convert this query for use in a Delta Live Tables (DLT) pipeline, as the first table in the pipeline.\n\nWhich of the following describes the change the colleague needs to make to the query?',
    o: [
      'They need to add a COMMENT line at the beginning of the query.',
      'They need to add a CREATE LIVE TABLE table_name AS line at the beginning of the query.',
      'They need to add a live. prefix prior to json. in the FROM line.',
      'They need to add a CREATE DELTA LIVE TABLE table_name AS line at the beginning of the query.',
      'They need to add the cloud_files(...) wrapper to the JSON file path.',
    ],
    a: [1],
    x: 'Para declarar una tabla dentro de un pipeline DLT en SQL, hay que anteponer `CREATE LIVE TABLE table_name AS` a la consulta. `CREATE DELTA LIVE TABLE` no es la sintaxis correcta (la keyword es solo `LIVE TABLE`), y el prefijo `live.` se usa para referenciar tablas de DLT en el FROM, no fuentes de archivos externos como esta.',
  },
  {
    n: 35,
    d: 'TRA',
    m: 0,
    q: "A dataset has been defined using Delta Live Tables and includes an expectations clause: CONSTRAINT valid_timestamp EXPECT (timestamp > '2020-01-01')\n\nWhat is the expected behavior when a batch of data containing data that violates these constraints is processed?",
    o: [
      'Records that violate the expectation are added to the target dataset and recorded as invalid in the event log.',
      'Records that violate the expectation are dropped from the target dataset and recorded as invalid in the event log.',
      'Records that violate the expectation cause the job to fail.',
      'Records that violate the expectation are added to the target dataset and flagged as invalid in a field added to the target dataset.',
      'Records that violate the expectation are dropped from the target dataset and loaded into a quarantine table.',
    ],
    a: [0],
    x: 'Un `EXPECT` simple (sin `ON VIOLATION DROP ROW` ni `FAIL UPDATE`) solo registra la violación en el log de eventos del pipeline para su monitorización, pero **mantiene** el registro en la tabla destino. Para descartar filas hace falta `ON VIOLATION DROP ROW`, y para fallar el pipeline, `ON VIOLATION FAIL UPDATE` — ninguna de esas cláusulas aparece en el enunciado.',
  },
  {
    n: 36,
    d: 'TRA',
    m: 0,
    q: 'A Delta Live Table pipeline includes two datasets defined using STREAMING LIVE TABLE. Three datasets are defined against Delta Lake table sources using LIVE TABLE. The pipeline is configured to run in Development mode using the Triggered Pipeline Mode.\n\nAssuming previously unprocessed data exists and all definitions are valid, what is the expected outcome after clicking Start to update the pipeline?',
    o: [
      'All datasets will be updated once and the pipeline will shut down. The compute resources will be terminated.',
      'All datasets will be updated at set intervals until the pipeline is shut down. The compute resources will be deployed for the update and terminated when the pipeline is stopped.',
      'All datasets will be updated at set intervals until the pipeline is shut down. The compute resources will persist after the pipeline is stopped to allow for additional testing.',
      'All datasets will be updated once and the pipeline will shut down. The compute resources will persist to allow for additional testing.',
      'All datasets will be updated continuously and the pipeline will not shut down. The compute resources will persist with the pipeline.',
    ],
    a: [3],
    x: 'En modo Triggered, el pipeline procesa todos los datos disponibles una sola vez y se detiene. La particularidad del modo Development es que el clúster se mantiene activo (no se termina) tras la ejecución, precisamente para facilitar iteraciones rápidas de desarrollo sin pagar el coste de arranque en cada prueba — a diferencia del modo Production, donde el clúster sí se termina.',
  },
  {
    n: 37,
    d: 'JOBS',
    m: 0,
    q: 'A data engineer has a Job with multiple tasks that runs nightly. One of the tasks unexpectedly fails during 10 percent of the runs.\n\nWhich of the following actions can the data engineer perform to ensure the Job completes each night while minimizing compute costs?',
    o: [
      'They can institute a retry policy for the entire Job',
      'They can observe the task as it runs to try and determine why it is failing',
      'They can set up the Job to run multiple times ensuring that at least one will complete',
      'They can institute a retry policy for the task that periodically fails',
      'They can utilize a Jobs cluster for each of the tasks in the Job',
    ],
    a: [3],
    x: 'Configurar una política de reintentos únicamente en la tarea que falla de forma intermitente resuelve el problema con el mínimo coste: solo se reintenta esa tarea concreta, no el Job completo, cuando ocurre el fallo transitorio (10% de las veces). Reintentar todo el Job o ejecutarlo varias veces completas desperdiciaría cómputo innecesariamente.',
  },
  {
    n: 38,
    d: 'JOBS',
    m: 0,
    q: 'A data engineer has set up two Jobs that each run nightly. The first Job starts at 12:00 AM and usually completes in about 20 minutes. The second Job depends on the first Job and starts at 12:30 AM. Sometimes, the second Job fails when the first Job does not complete by 12:30 AM.\n\nWhich of the following approaches can the data engineer use to avoid this problem?',
    o: [
      'They can utilize multiple tasks in a single job with a linear dependency',
      'They can use cluster pools to help the Jobs run more efficiently',
      'They can set up a retry policy on the first Job to help it run more quickly',
      'They can limit the size of the output in the second Job so that it will not fail as easily',
      'They can set up the data to stream from the first Job to the second Job',
    ],
    a: [0],
    x: 'Combinar ambos procesos como tareas dependientes dentro de un mismo Job (en vez de dos Jobs programados por horario fijo) hace que la segunda tarea espere automáticamente a que la primera termine con éxito, eliminando la condición de carrera basada en una hora de inicio fija que causa el fallo intermitente.',
  },
  {
    n: 39,
    d: 'CICD',
    m: 0,
    q: "A data engineer has set up a notebook to automatically process using a Job. The data engineer's manager wants to version control the schedule due to its complexity.\n\nWhich of the following approaches can the data engineer use to obtain a version-controllable configuration of the Job's schedule?",
    o: [
      'They can link the Job to notebooks that are a part of a Databricks Repo.',
      'They can submit the Job once on a Job cluster.',
      "They can download the JSON description of the Job from the Job's page.",
      'They can submit the Job once on an all-purpose cluster.',
      "They can download the XML description of the Job from the Job's page.",
    ],
    a: [2],
    x: 'La página de un Job permite descargar su definición completa en formato JSON, incluida la configuración del schedule — ese fichero JSON puede versionarse en Git como cualquier otro artefacto de código. Vincular notebooks a un Repo versiona el código de los notebooks, pero no la configuración del schedule del Job en sí, y Databricks no exporta configuraciones en XML.',
  },
  {
    n: 40,
    d: 'TRO',
    m: 0,
    q: "A data analyst has noticed that their Databricks SQL queries are running too slowly. They claim that this issue is affecting all of their sequentially run queries. The data engineering team notices that each of the queries uses the same SQL endpoint, but the SQL endpoint is not used by any other user.\n\nWhich of the following approaches can the data engineering team use to improve the latency of the data analyst's queries?",
    o: [
      'They can turn on the Serverless feature for the SQL endpoint.',
      "They can increase the maximum bound of the SQL endpoint's scaling range.",
      'They can increase the cluster size of the SQL endpoint.',
      'They can turn on the Auto Stop feature for the SQL endpoint.',
      'They can turn on the Serverless feature for the SQL endpoint and change the Spot Instance Policy to "Reliability Optimized."',
    ],
    a: [2],
    x: 'Como el problema afecta a consultas secuenciales de un único usuario en un endpoint que nadie más usa, no es un problema de concurrencia (que resolvería el scaling range), sino de potencia de cómputo por consulta individual — aumentar el tamaño del clúster del SQL endpoint acelera cada consulta individual. Serverless y Auto Stop afectan al arranque/parada, no a la velocidad de ejecución en sí.',
  },
  {
    n: 41,
    d: 'JOBS',
    m: 0,
    q: "An engineering manager uses a Databricks SQL query to monitor their team's progress on fixes related to customer-reported bugs. The manager checks the results of the query every day, but they are manually rerunning the query each day and waiting for the results.\n\nWhich of the following approaches can the manager use to ensure the results of the query are updated each day?",
    o: [
      'They can schedule the query to run every 1 day from the Jobs UI.',
      "They can schedule the query to refresh every 1 day from the query's page in Databricks SQL.",
      'They can schedule the query to run every 12 hours from the Jobs UI.',
      "They can schedule the query to refresh every 1 day from the SQL endpoint's page in Databricks SQL.",
      "They can schedule the query to refresh every 12 hours from the SQL endpoint's page in Databricks SQL.",
    ],
    a: [1],
    x: 'Las consultas de Databricks SQL tienen su propia opción de programación de refresco directamente en la página de la consulta (no hace falta usar Jobs para esto), y el requisito pide una actualización diaria, no cada 12 horas. El endpoint SQL es el recurso de cómputo, no el lugar donde se programa el refresco de una query concreta.',
  },
  {
    n: 42,
    d: 'TRO',
    m: 0,
    q: "A data engineering team has been using a Databricks SQL query to monitor the performance of an ELT job. The ELT job is triggered by a specific number of input records being ready to process. The query returns the number of minutes since the job's most recent runtime.\n\nWhich of the following approaches can enable the data engineering team to be notified if the ELT job has not been run in an hour?",
    o: [
      'They can set up an Alert for the accompanying dashboard to notify them if the returned value is greater than 60.',
      'They can set up an Alert for the query to notify when the ELT job fails.',
      'They can set up an Alert for the accompanying dashboard to notify when it has not refreshed in 60 minutes.',
      'They can set up an Alert for the query to notify them if the returned value is greater than 60.',
      'This type of alerting is not possible in Databricks.',
    ],
    a: [3],
    x: 'Las alertas de Databricks SQL se configuran sobre el resultado numérico de una consulta concreta, disparándose cuando ese valor cruza un umbral definido (aquí, más de 60 minutos desde la última ejecución). Las alertas no se configuran sobre dashboards ni sobre el estado de fallo de un Job externo, sino directamente sobre el valor devuelto por la query.',
  },
  {
    n: 43,
    d: 'TRO',
    m: 0,
    q: 'A data engineering manager has noticed that each of the queries in a Databricks SQL dashboard takes a few minutes to update when they manually click the "Refresh" button. A team member provides a variety of reasons why the delay might be occurring.\n\nWhich of the following reasons FAILS to explain why the dashboard might be taking a few minutes to update?',
    o: [
      'The SQL endpoint being used by each of the queries might need a few minutes to start up.',
      'The queries attached to the dashboard might take a few minutes to run under normal circumstances.',
      'The queries attached to the dashboard might first be checking to determine if new data is available.',
      'The Job associated with updating the dashboard might be using a non-pooled endpoint.',
      'The queries attached to the dashboard might all be connected to their own, unstarted Databricks clusters.',
    ],
    a: [3],
    x: 'Un dashboard de Databricks SQL se refresca mediante las consultas asociadas a un SQL endpoint, no mediante un "Job" independiente asociado a un endpoint agrupado (pooled) o no — ese concepto de Job no aplica al mecanismo de refresco de un dashboard, por lo que es la explicación inválida. El resto de razones (arranque del endpoint, tiempo normal de ejecución, comprobación de nuevos datos, clústeres sin arrancar) sí son causas reales de latencia.',
  },
  {
    n: 44,
    d: 'GOV',
    m: 0,
    q: 'A new data engineer has started at a company as new.engineer@company.com. They need to be able to query the table sales in the database retail. The new data engineer already has been granted USAGE on the database retail.\n\nWhich of the following commands can be used to grant the appropriate permissions to the new data engineer?',
    o: [
      '```\nGRANT USAGE ON TABLE sales TO new.engineer@company.com;\n```',
      '```\nGRANT CREATE ON TABLE sales TO new.engineer@company.com;\n```',
      '```\nGRANT SELECT ON TABLE sales TO new.engineer@company.com;\n```',
      '```\nGRANT USAGE ON TABLE new.engineer@company.com TO sales;\n```',
      '```\nGRANT SELECT ON TABLE new.engineer@company.com TO sales;\n```',
    ],
    a: [2],
    x: 'Para poder consultar una tabla concreta hace falta el privilegio SELECT sobre esa tabla, además del USAGE ya concedido sobre la base de datos contenedora (equivalente a USE SCHEMA). USAGE sobre una tabla no es válido (se aplica a bases de datos/esquemas), y CREATE permite crear objetos, no consultarlos.',
  },
  {
    n: 45,
    d: 'GOV',
    m: 0,
    q: 'A new data engineer new.engineer@company.com has been assigned to an ELT project. The new data engineer will need full privileges on the table sales to fully manage the project.\n\nWhich of the following commands can be used to grant full permissions on the table to the new data engineer?',
    o: [
      '```\nGRANT ALL PRIVILEGES ON TABLE sales TO new.engineer@company.com;\n```',
      '```\nGRANT USAGE ON TABLE sales TO new.engineer@company.com;\n```',
      '```\nGRANT ALL PRIVILEGES ON TABLE new.engineer@company.com TO sales;\n```',
      '```\nGRANT SELECT ON TABLE sales TO new.engineer@company.com;\n```',
      '```\nGRANT SELECT CREATE MODIFY ON TABLE sales TO new.engineer@company.com;\n```',
    ],
    a: [0],
    x: '`GRANT ALL PRIVILEGES` concede de una sola vez el conjunto completo de permisos (lectura, escritura, gestión) necesario para administrar plenamente la tabla, cumpliendo el requisito de "privilegios completos". SELECT por sí solo es demasiado limitado, y `GRANT SELECT CREATE MODIFY` no es sintaxis válida (los privilegios no se listan así, separados por espacios, en una única cláusula GRANT).',
  },
];

export const exam12: RawQuestion[] = [
  {
    n: 1,
    d: 'TRA',
    m: 0,
    q: 'You were asked to create a table to store order data. `orderTime` is a timestamp, but the finance team prefers to query `orderTime` in date format. You want to add a calculated column that converts `orderTime` to a date and stores it. Fill in the blank:\n\n```\nCREATE TABLE orders (\n  orderId int,\n  orderTime timestamp,\n  orderdate date __________,\n  units int\n)\n```',
    o: [
      '`AS DEFAULT (CAST(orderTime as DATE))`',
      '`GENERATED ALWAYS AS (CAST(orderTime as DATE))`',
      '`GENERATED DEFAULT AS (CAST(orderTime as DATE))`',
      '`AS (CAST(orderTime as DATE))`',
      'Delta Lake does not support calculated columns; the value should be inserted as part of the ingestion process',
    ],
    a: [1],
    x: 'La sintaxis correcta para una columna generada en Databricks SQL es `GENERATED ALWAYS AS (expresión)`. `GENERATED DEFAULT AS` no existe, `AS DEFAULT` es para valores por defecto (no columnas calculadas) y `AS (...)` sin `GENERATED ALWAYS` es sintaxis incompleta.',
  },
  {
    n: 2,
    d: 'JOBS',
    m: 0,
    q: 'The data engineering team notices that a job fails randomly as a result of using spot instances. Which feature in Jobs/Tasks can be used to make the job more stable when using spot instances?',
    o: [
      'Use the Databricks REST API to monitor and restart the job',
      'Use the Jobs runs, active runs UI section to monitor and restart the job',
      'Add a second task and a check condition to rerun the first task if it fails',
      'Restart the job cluster manually when it fails',
      'Add a retry policy to the task',
    ],
    a: [4],
    x: 'Las tareas soportan una retry policy nativa que reintenta automáticamente ante fallos transitorios (comunes con spot instances por preemption de executor/driver), sin necesidad de tareas auxiliares ni monitorización manual.',
  },
  {
    n: 3,
    d: 'ING',
    m: 0,
    q: 'Why does Auto Loader require a schema location?',
    o: [
      'Schema location is used to store the user-provided schema',
      'Schema location is used to identify the schema of the target table',
      'Auto Loader does not require a schema location, because it supports schema evolution',
      'Schema location is used to store the schema inferred by Auto Loader',
      'Schema location is used to identify the schema of the target table and the source table',
    ],
    a: [3],
    x: 'Auto Loader infiere el esquema muestreando los archivos de origen y persiste ese esquema inferido en `schemaLocation/_schemas`, para reutilizarlo en cargas incrementales posteriores sin tener que re-inferir cada vez.',
  },
  {
    n: 4,
    d: 'P',
    m: 0,
    q: 'Which of the following statements about the lakehouse is **incorrect**?',
    o: [
      'Supports end-to-end streaming and batch workloads',
      'Supports ACID transactions',
      'Supports diverse data types, both structured and unstructured',
      'Supports BI and Machine Learning',
      'Storage is coupled with compute',
    ],
    a: [4],
    x: 'En el lakehouse, almacenamiento y cómputo están **desacoplados**, lo que permite escalarlos de forma independiente; justo lo contrario de lo que plantea esta opción.',
  },
  {
    n: 5,
    d: 'P',
    m: 0,
    q: 'Which of the following locations in the Databricks product architecture hosts jobs/pipelines and queries?',
    o: [
      'Data plane',
      'Control plane',
      'Databricks Filesystem',
      'JDBC data source',
      'Databricks web application',
    ],
    a: [1],
    x: 'El control plane (en la cuenta cloud de Databricks) almacena la definición de notebooks, jobs y queries, junto con otra configuración del workspace. El data plane (cuenta cloud del cliente) es donde se ejecuta y procesa realmente el dato.',
  },
  {
    n: 6,
    d: 'JOBS',
    m: 0,
    q: 'You are currently working on a notebook that will populate a reporting table for downstream consumption. This process needs to run on a schedule every hour. What type of cluster should you use to set up this job?',
    o: [
      "An all-purpose cluster, since it's just a single job that runs every hour",
      'A job cluster is best suited for this purpose',
      'Use an Azure VM to read and write Delta tables in Python',
      'Use a Delta Live Tables pipeline running in continuous mode',
    ],
    a: [1],
    x: 'Al no necesitar interactuar con el notebook durante la ejecución, un job cluster es más económico (hasta la mitad de coste que un all-purpose cluster) y se aprovisiona y termina automáticamente por ejecución.',
  },
  {
    n: 7,
    d: 'P',
    m: 0,
    q: 'You are currently working on a project that requires the use of SQL and Python in a given notebook. What is the best approach?',
    o: [
      'Create two separate notebooks, one for SQL and the second for Python',
      'A single notebook can support multiple languages; use magic commands to switch between them',
      'Use an all-purpose cluster for Python and a SQL endpoint for SQL',
      'Use a job cluster to run Python and a SQL endpoint for SQL',
    ],
    a: [1],
    x: 'Databricks soporta múltiples lenguajes en un mismo notebook mediante comandos mágicos como `%sql` y `%python`, sin necesidad de separar el trabajo en notebooks o clusters distintos.',
  },
  {
    n: 8,
    d: 'P',
    m: 0,
    q: 'Which of the following statements is correct about how Delta Lake implements a lakehouse?',
    o: [
      'Delta Lake uses a proprietary format to write data, optimized for cloud storage',
      'Delta Lake uses Apache Hadoop on cloud object storage',
      'Delta Lake always stores metadata in memory rather than in storage',
      'Delta Lake uses an open source, open format, optimized for cloud storage with scalable metadata',
      'Delta Lake stores data and metadata in compute memory',
    ],
    a: [3],
    x: 'Delta Lake se basa en Parquet (formato abierto) sobre almacenamiento de objetos en la nube, con un transaction log escalable que aporta transacciones ACID, enforcement de esquema y time travel, sin depender de Hadoop ni de almacenamiento en memoria.',
  },
  {
    n: 9,
    d: 'TRA',
    m: 0,
    q: 'You need to create or overwrite an existing Delta table to store the following transaction data:\n\n```\ntransactionId int, transactionDate timestamp, unitsSold int\n```\n\nWhich of the following is the correct syntax?',
    o: [
      '`CREATE OR REPLACE DELTA TABLE transactions (...)`',
      '`CREATE OR REPLACE TABLE IF EXISTS transactions (...) FORMAT DELTA`',
      '`CREATE IF EXISTS REPLACE TABLE transactions (...)`',
      '`CREATE OR REPLACE TABLE transactions (...)`',
    ],
    a: [3],
    x: 'La sintaxis correcta es `CREATE OR REPLACE TABLE nombre (...)`. Por defecto, toda tabla de Databricks es Delta salvo que se indique otro formato explícitamente; `DELTA TABLE`, `IF EXISTS` (en vez de `IF NOT EXISTS`) y `FORMAT DELTA` no son sintaxis válida en este contexto.',
  },
  {
    n: 10,
    d: 'TRA',
    m: 0,
    q: "A colleague is manually copying data to a backup folder before running an UPDATE command, in case the result isn't as expected, so they can restore the backup. Which Delta Lake feature would you recommend to simplify this process?",
    o: [
      'Use time travel to refer to the old data instead of manually copying it',
      'Use DEEP CLONE to clone the table before the update, as a backup',
      'Use a SHADOW copy of the table as the preferred backup',
      'Cloud object storage automatically retains previous versions of the file',
      'Cloud object storage automatically backs up the data',
    ],
    a: [0],
    x: "El transaction log de Delta Lake conserva versiones anteriores de la tabla, así que se puede consultar el estado previo con `SELECT * FROM tabla TIMESTAMP AS OF '...'` o `VERSION AS OF n`, sin necesidad de copias manuales.",
  },
  {
    n: 11,
    d: 'TRA',
    m: 0,
    q: "Which of the following SQL statements eliminates duplicate rows from a query's results?",
    o: [
      '`SELECT DISTINCT * FROM table_name`',
      '`SELECT DISTINCT * FROM table_name HAVING COUNT(*) > 1`',
      '`SELECT DISTINCT_ROWS (*) FROM table_name`',
      '`SELECT * FROM table_name GROUP BY * HAVING COUNT(*) < 1`',
      '`SELECT * FROM table_name GROUP BY * HAVING COUNT(*) > 1`',
    ],
    a: [0],
    x: '`SELECT DISTINCT` es la forma estándar de eliminar duplicados en el resultado de una consulta; las demás opciones son sintaxis inválida o no logran el objetivo.',
  },
  {
    n: 12,
    d: 'TRA',
    m: 0,
    q: "You need a SQL UDF `udf_convert(temp, measure)` that converts a temperature between Celsius and Fahrenheit, where `measure` is `'F'` or `'C'` depending on the unit to convert to. Which of the following is the correct syntax?",
    o: [
      "`CREATE UDF FUNCTION udf_convert(temp DOUBLE, measure STRING) RETURNS DOUBLE RETURN CASE WHEN measure = 'F' THEN (temp * 9/5) + 32 ELSE (temp - 32) * 5/9 END;`",
      "`CREATE UDF FUNCTION udf_convert(temp DOUBLE, measure STRING) RETURN CASE WHEN measure = 'F' THEN (temp * 9/5) + 32 ELSE (temp - 32) * 5/9 END;`",
      "`CREATE FUNCTION udf_convert(temp DOUBLE, measure STRING) RETURN CASE WHEN measure = 'F' THEN (temp * 9/5) + 32 ELSE (temp - 32) * 5/9 END;`",
      "`CREATE FUNCTION udf_convert(temp DOUBLE, measure STRING) RETURNS DOUBLE RETURN CASE WHEN measure = 'F' THEN (temp * 9/5) + 32 ELSE (temp - 32) * 5/9 END;`",
      "`CREATE USER FUNCTION udf_convert(temp DOUBLE, measure STRING) RETURNS DOUBLE RETURN CASE WHEN measure = 'F' THEN (temp * 9/5) + 32 ELSE (temp - 32) * 5/9 END;`",
    ],
    a: [3],
    x: 'La sintaxis válida en Databricks SQL es `CREATE FUNCTION nombre(parámetros) RETURNS tipo RETURN expresión`. `CREATE UDF FUNCTION` y `CREATE USER FUNCTION` no existen, y omitir `RETURNS DOUBLE` deja la definición incompleta.',
  },
  {
    n: 13,
    d: 'TRA',
    m: 0,
    q: 'You need to calculate the total sales made by all employees by parsing a complex struct data type that stores employee and sales data. How would you approach this in SQL?',
    o: [
      '```\nWITH CTE AS (SELECT EXPLODE(performance) FROM table_name)\nSELECT SUM(performance.sales) FROM CTE\n```',
      '```\nWITH CTE AS (SELECT FLATTEN(performance) FROM table_name)\nSELECT SUM(sales) FROM CTE\n```',
      '```\nSELECT aggregate(flatten(collect_list(performance.sales)), 0, (x, y) -> x + y) AS total_sales FROM sales\n```',
      '`SELECT SUM(SLICE(performance, sales)) FROM employee`',
      '```\nSELECT reduce(flatten(collect_list(performance:sales)), 0, (x, y) -> x + y) AS total_sales FROM sales\n```',
    ],
    a: [2],
    x: 'Las funciones de orden superior `aggregate`, `flatten` y `collect_list` permiten extraer `sales` de cada struct, aplanar el array resultante y sumarlo en una sola expresión, sin necesidad de `EXPLODE` ni sintaxis inválida como `SLICE(performance, sales)`.',
  },
  {
    n: 14,
    d: 'CICD',
    m: 0,
    q: 'Which of the following statements can be used to test that a table has exactly 10 rows in Python?\n\n```\nrow_count = spark.sql("select count(*) from table").collect()[0][0]\n```',
    o: [
      '`assert (row_count = 10, "Row count did not match")`',
      '`assert if (row_count = 10, "Row count did not match")`',
      '`assert row_count == 10, "Row count did not match"`',
      '`assert if row_count == 10, "Row count did not match"`',
      '`assert row_count = 10, "Row count did not match"`',
    ],
    a: [2],
    x: '`assert` en Python usa el operador de comparación `==`, no `=` (asignación), y no lleva `if`. La forma correcta es `assert condición, mensaje`.',
  },
  {
    n: 15,
    d: 'TRA',
    m: 0,
    q: 'How do you handle failures gracefully when writing PySpark code? Fill in the blanks:\n\n```\n_____\nspark.read.table("table_name").select("column").write.mode("append").saveAsTable("new_table_name")\n_____\nprint(f"query failed")\n```',
    o: [
      '`try:` / `failure:`',
      '`try:` / `catch:`',
      '`try:` / `except:`',
      '`try:` / `fail:`',
      '`try:` / `error:`',
    ],
    a: [2],
    x: 'Python maneja excepciones con el bloque `try:`/`except:`; `catch`, `failure` y `fail` no son palabras clave válidas en Python (a diferencia de otros lenguajes como Java).',
  },
  {
    n: 16,
    d: 'TRA',
    m: 0,
    q: 'Which of the following commands results in the successful creation of a view on top of a Delta stream (stream on a Delta table)?',
    o: [
      '`spark.read.format("delta").table("sales").createOrReplaceTempView("streaming_vw")`',
      '`spark.readStream.format("delta").table("sales").createOrReplaceTempView("streaming_vw")`',
      '`spark.read.format("delta").table("sales").mode("stream").createOrReplaceTempView("streaming_vw")`',
      '`spark.read.format("delta").table("sales").trigger("stream").createOrReplaceTempView("streaming_vw")`',
      'You cannot create a view on a streaming data source',
    ],
    a: [1],
    x: '`readStream` habilita el modo streaming en el DataFrame; `.mode("stream")` y `.trigger("stream")` no existen sobre `read` (batch), y sí es posible crear vistas temporales sobre streams.',
  },
  {
    n: 17,
    d: 'TRA',
    m: 0,
    q: 'Which of the following data workloads will utilize a Bronze table as its destination?',
    o: [
      'A job that aggregates cleaned data to create standard summary statistics',
      'A job that queries aggregated data to publish key insights into a dashboard',
      'A job that ingests raw data from a streaming source into the Lakehouse',
      'A job that develops a feature set for a machine learning application',
      'A job that enriches data by parsing its timestamps into a human-readable format',
    ],
    a: [2],
    x: 'La capa Bronze almacena una copia cruda de los datos ingeridos, sin ningún esquema o transformación aplicada; por eso el destino natural de la ingesta de streaming es Bronze.',
  },
  {
    n: 18,
    d: 'TRA',
    m: 0,
    q: 'Which of the following data workloads will utilize a Gold table as its source?',
    o: [
      'A job that enriches data by parsing its timestamps into a human-readable format',
      'A job that queries aggregated data that already feeds into a dashboard',
      'A job that ingests raw data from a streaming source into the Lakehouse',
      'A job that aggregates cleaned data to create standard summary statistics',
      'A job that cleans data by removing malformatted records',
    ],
    a: [1],
    x: 'Gold contiene datos agregados y listos para negocio; consultar esos agregados para un dashboard es el caso de uso típico que usa Gold como fuente. Las demás opciones describen procesos que producen Bronze o Silver, no que consumen Gold.',
  },
  {
    n: 19,
    d: 'TRA',
    m: 0,
    q: 'While investigating a data issue in a Delta table, you want to review when and who updated the table. What is the best way to review this data?',
    o: [
      'Review event logs in the Workspace',
      'Run SQL `SHOW HISTORY table_name`',
      'Check Databricks SQL Audit logs',
      'Run SQL command `DESCRIBE HISTORY table_name`',
      'Review workspace audit logs',
    ],
    a: [3],
    x: '`DESCRIBE HISTORY table_name` devuelve el log de transacciones Delta (versión, timestamp, usuario, operación...), la fuente directa para auditar cambios sobre una tabla. `SHOW HISTORY` no es un comando válido.',
  },
  {
    n: 20,
    d: 'JOBS',
    m: 0,
    q: 'You are working on a project that requires the use of a data source with a lot of data quality issues, and you need to monitor and enforce data quality as part of the ingestion process. Which of the following tools can be used to address this problem?',
    o: [
      'AUTO LOADER',
      'DELTA LIVE TABLES',
      'JOBS and TASKS',
      'Unity Catalog and Data Governance',
      'STRUCTURED STREAMING with MULTI HOP',
    ],
    a: [1],
    x: 'Las expectations de Delta Live Tables permiten definir reglas de calidad de datos (retener, descartar o fallar ante violaciones) y registran las métricas en el event log para su monitorización, algo que Auto Loader o Jobs por sí solos no ofrecen.',
  },
  {
    n: 21,
    d: 'TRO',
    m: 0,
    q: 'While investigating a performance issue, you realize that a table has too many small files. Which command would you run to fix this?',
    o: [
      '`COMPACT table_name`',
      '`VACUUM table_name`',
      '`MERGE table_name`',
      '`SHRINK table_name`',
      '`OPTIMIZE table_name`',
    ],
    a: [4],
    x: '`OPTIMIZE` compacta archivos Parquet pequeños en archivos más grandes, mejorando el rendimiento de lectura. `VACUUM` limpia archivos obsoletos (no compacta), y `COMPACT`/`SHRINK` no son comandos válidos.',
  },
  {
    n: 22,
    d: 'TRO',
    m: 0,
    q: "A particular job keeps getting slower, and the team thinks it started after a recent production change. You are asked to look at the job's history to identify trends and root cause. Where in the workspace UI can you perform this analysis?",
    o: [
      'In the Jobs UI, select the job and view the Runs tab (active runs plus the last 60 days of history)',
      'In the Jobs UI, select the job cluster and view Spark UI logs',
      "In Workspace logs, select the job's logs",
      'In the Compute UI, select the job cluster',
      'Historical job runs can only be accessed via the REST API',
    ],
    a: [0],
    x: 'La pestaña Runs de la UI de Jobs muestra tanto ejecuciones activas como el histórico de hasta 60 días, siendo el lugar recomendado para analizar tendencias de rendimiento y detectar regresiones tras un cambio.',
  },
  {
    n: 23,
    d: 'TRA',
    m: 0,
    q: 'What type of table is created when you run `CREATE TABLE sales (id int, units int)` without specifying any other options?',
    o: [
      'Fails due to missing LOCATION',
      'Fails due to missing format',
      'Managed Delta table',
      'External table',
      'Managed Parquet table',
    ],
    a: [2],
    x: 'Si no se especifica `LOCATION`, la tabla se crea como managed, y por defecto toda tabla managed en Databricks es Delta.',
  },
  {
    n: 24,
    d: 'TRO',
    m: 0,
    q: 'Databricks SQL queries are running slow. All queries run in parallel against a SQL endpoint with a single cluster. Which of the following steps improves query performance/response times?',
    o: [
      'Turn on the Serverless feature for the SQL endpoint',
      "Increase the maximum bound of the SQL endpoint's scaling range",
      'Increase the warehouse size from 2X-Small to 4X-Large',
      'Turn on the Auto Stop feature for the SQL endpoint',
      'Turn on Serverless and change the Spot Instance Policy to "Reliability Optimized"',
    ],
    a: [1],
    x: 'Ante concurrencia (muchas queries en paralelo saturando un único cluster), lo que ayuda es escalar horizontalmente aumentando el máximo del rango de escalado, para que se aprovisionen más clusters. Aumentar el tamaño del cluster ayuda con queries secuenciales pesadas, no con concurrencia.',
  },
  {
    n: 25,
    d: 'TRA',
    m: 0,
    q: 'Define an external SQL table by connecting to a local SQLite database instance using JDBC.',
    o: [
      '`CREATE TABLE users_jdbc USING SQLITE OPTIONS (url = "jdbc:/sample_db", dbtable = "users")`',
      '`CREATE TABLE users_jdbc USING SQL URL = {server:"jdbc:/sample_db", dbtable: "users"}`',
      '`CREATE TABLE users_jdbc USING SQL OPTIONS (url = "jdbc:sqlite:/sample_db", dbtable = "users")`',
      '`CREATE TABLE users_jdbc USING org.apache.spark.sql.jdbc.sqlite OPTIONS (url = "jdbc:/sample_db", dbtable = "users")`',
      '`CREATE TABLE users_jdbc USING org.apache.spark.sql.jdbc OPTIONS (url = "jdbc:sqlite:/sample_db", dbtable = "users")`',
    ],
    a: [4],
    x: 'Databricks se conecta a bases de datos relacionales (SQL Server, MySQL, SQLite, Snowflake...) vía JDBC usando `USING org.apache.spark.sql.jdbc` (o el alias `JDBC`) con una URL de conexión completa, incluyendo el driver (`jdbc:sqlite:...`).',
  },
  {
    n: 26,
    d: 'TRO',
    m: 0,
    q: 'A funnel analysis query takes 30 minutes to run on a small SQL endpoint cluster with max scaling set to 1 cluster. What steps can be taken to improve the performance of this specific query?',
    o: [
      'Turn on the Serverless feature for the SQL endpoint',
      "Increase the maximum bound of the SQL endpoint's scaling range from 1 to 100",
      'Increase the cluster size from X-Small to 3XL',
      'Turn off Auto Stop so it lasts more than 30 minutes',
      'Turn on Serverless and change the Spot Instance Policy to "Reliability Optimized"',
    ],
    a: [2],
    x: 'Para una única query pesada y secuencial, lo que ayuda es escalar verticalmente (más núcleos/memoria por cluster). Aumentar el número de clusters (escalado horizontal) no acelera una única query que ya se ejecuta en un solo cluster.',
  },
  {
    n: 27,
    d: 'GOV',
    m: 0,
    q: 'Unity Catalog simplifies managing multiple workspaces by storing and managing permissions and ACLs at the _______ level.',
    o: ['Workspace', 'Account', 'Storage', 'Data plane', 'Control plane'],
    a: [1],
    x: 'A diferencia de las ACLs clásicas (a nivel de workspace), Unity Catalog gestiona permisos a nivel de cuenta, permitiendo gobernanza unificada sobre todos los workspaces de la organización.',
  },
  {
    n: 28,
    d: 'ING',
    m: 0,
    q: "When defining external tables using formats such as CSV, JSON, TEXT, or BINARY, any query on the external table caches the data and its location for performance reasons, so new files that arrive won't be picked up within a given Spark session. How can this limitation be addressed?",
    o: [
      '`UNCACHE TABLE table_name`',
      '`CACHE TABLE table_name`',
      '`REFRESH TABLE table_name`',
      '`BROADCAST TABLE table_name`',
      '`CLEAR CACHE table_name`',
    ],
    a: [2],
    x: '`REFRESH TABLE` fuerza a Spark a redescubrir los archivos asociados a una tabla externa, incorporando los que hayan llegado desde la última vez que se cacheó.',
  },
  {
    n: 29,
    d: 'TRA',
    m: 0,
    q: 'The data engineering team wants to add a new column to a table, but the QA team wants to test the change before deploying to production. Which of the following options lets you quickly copy the table from Prod to QA, modify it, and run tests?',
    o: ['DEEP CLONE', 'SHADOW CLONE', 'ZERO COPY CLONE', 'SHALLOW CLONE', 'METADATA CLONE'],
    a: [3],
    x: '`SHALLOW CLONE` copia solo los metadatos y referencias a los archivos existentes (sin duplicar datos), por lo que es rápido y eficiente en espacio, ideal para pruebas donde luego se puede modificar el esquema sin afectar a producción.',
  },
  {
    n: 30,
    d: 'TRA',
    m: 0,
    q: 'The sales team wants a report on units sold by date. Fill in the blank with the appropriate array function:\n\n```\nSELECT orderDate, SUM(unitsSold)\nFROM orderDetail od\nJOIN (SELECT orderDate, __________(orderIds) AS orderId FROM orders) o\n  ON o.orderId = od.orderId\nGROUP BY orderDate\n```',
    o: ['`FLATTEN`', '`EXTEND`', '`EXPLODE`', '`EXTRACT`', '`ARRAY_FLATTEN`'],
    a: [2],
    x: '`EXPLODE` es una función de tabla que convierte cada elemento de un array en una fila independiente, permitiendo así hacer el `JOIN` por `orderId` con la tabla de detalle.',
  },
  {
    n: 31,
    d: 'P',
    m: 0,
    q: 'What is the best way to describe a data lakehouse compared to a data warehouse?',
    o: [
      'A data lakehouse provides a relational system of data management',
      'A data lakehouse captures snapshots of data for version control purposes',
      'A data lakehouse couples storage and compute for complete control',
      'A data lakehouse utilizes proprietary storage formats for data',
      'A data lakehouse enables both batch and streaming analytics',
    ],
    a: [4],
    x: 'Entre las características clave de un lakehouse está el soporte end-to-end de batch y streaming, además de transacciones ACID, formatos abiertos y desacoplo de almacenamiento y cómputo — justo lo opuesto a las demás opciones.',
  },
  {
    n: 32,
    d: 'TRA',
    m: 0,
    q: 'You are asked to write a Python function that reads data from a Delta table and returns the DataFrame. Which of the following is correct?',
    o: [
      'A Python function cannot return a DataFrame',
      'You should write a SQL UDF to return a DataFrame',
      'You should write a SQL UDF that can return tabular data',
      'The Python function will result in an out-of-memory error due to data volume',
      'A Python function can return a DataFrame',
    ],
    a: [4],
    x: 'Una función Python normal puede perfectamente devolver un DataFrame de Spark (`return spark.read.table(...)`), ya que sin una acción de por medio Spark no ejecuta nada, solo construye el plan; no hay limitación de memoria en la definición de la función.',
  },
  {
    n: 33,
    d: 'TRO',
    m: 0,
    q: 'You are investigating a production job failure that occurred on a job cluster, caused by a data issue. What cluster do you need to start in order to investigate and analyze the data?',
    o: [
      'A job cluster can be used to analyze the problem',
      'An all-purpose/interactive cluster is the recommended way to run commands and view the data',
      'The existing job cluster can be reused to investigate the issue',
      'A Databricks SQL Endpoint can be used to investigate the issue',
    ],
    a: [1],
    x: 'Un job cluster no permite interactuar con un notebook una vez lanzado el job; un cluster all-purpose/interactivo sí permite ejecutar comandos, ver datos y visualizaciones, lo que lo hace idóneo para depurar el problema.',
  },
  {
    n: 34,
    d: 'CICD',
    m: 0,
    q: 'You notice a colleague manually copying a notebook with a `_bkp` suffix to keep previous versions. Which feature would you recommend instead?',
    o: [
      'Databricks notebooks support automatic change tracking and versioning',
      'Databricks notebooks should be copied to a local machine and versioned with local source control',
      'Databricks notebooks can be exported as .dbc archive files and stored in the data lake',
      'Databricks notebooks can be exported as HTML and imported later',
    ],
    a: [0],
    x: 'Cada cambio en un notebook de Databricks queda registrado automáticamente en su historial de versiones (accesible desde el panel lateral), sin necesidad de copias manuales ni exportaciones.',
  },
  {
    n: 35,
    d: 'TRA',
    m: 0,
    q: 'When using complete mode to write stream data, how does it impact the target table?',
    o: [
      'The entire stream waits for complete data before writing',
      'The stream must complete in full before writing the data',
      'The target table cannot be updated while the stream is pending',
      'The target table is overwritten for each batch',
      'Delta commits the transaction only once the stream is stopped',
    ],
    a: [3],
    x: 'En modo `complete`, el resultado agregado completo se recalcula y se escribe entero en cada trigger/micro-batch, sobrescribiendo la tabla destino cada vez; no hay que esperar a que el stream termine.',
  },
  {
    n: 36,
    d: 'ING',
    m: 0,
    q: 'When working with Auto Loader, you notice that most of the inferred columns are string data types, including columns that should be integers. How can you fix this?',
    o: [
      'Provide the schema of the source table in `cloudFiles.schemaLocation`',
      'Provide the schema of the target table in `cloudFiles.schemaLocation`',
      'Provide schema hints',
      'Update the checkpoint location',
      'Correct the incoming data by explicitly casting the data types',
    ],
    a: [2],
    x: 'La opción `cloudFiles.schemaHints` permite forzar el tipo de dato de columnas concretas durante la inferencia de esquema (p. ej. `"id int, description string"`), sin tener que modificar los datos de origen.',
  },
  {
    n: 37,
    d: 'TRA',
    m: 0,
    q: 'Which of the following Structured Streaming queries performs a hop from a Bronze table to a Silver table?',
    o: [
      '```\nspark.table("sales").groupBy("store").agg(sum("sales"))\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("complete").table("aggregatedSales")\n```',
      '```\nspark.table("sales").agg(sum("sales"), sum("units"))\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("complete").table("aggregatedSales")\n```',
      '```\nspark.table("sales").withColumn("avgPrice", col("sales") / col("units"))\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("cleanedSales")\n```',
      '```\nspark.readStream.load(rawSalesLocation)\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("uncleanedSales")\n```',
      '```\nspark.read.load(rawSalesLocation)\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("uncleanedSales")\n```',
    ],
    a: [2],
    x: 'La opción C lee de `sales` (Bronze), enriquece con una columna derivada (`avgPrice`) y escribe en `cleanedSales` (Silver): es exactamente una transformación Bronze→Silver. Las opciones A y B agregan datos (Silver→Gold), y D/E ingieren datos crudos (→Bronze).',
  },
  {
    n: 38,
    d: 'TRA',
    m: 0,
    q: 'Which of the following statements correctly describes how data is organized in storage for a Delta table?',
    o: [
      'Data is broken down into one or many Parquet files, log files are broken down into one or many JSON files, and each transaction creates new data file(s) and a new log file',
      'Data and log are both stored in a single Parquet file',
      'Data is broken down into Parquet files, but the log is a single JSON file that gets appended to',
      'Data is broken down into Parquet files, and the log file is removed once the transaction is committed',
      'Data is stored in a single Parquet file, log files are broken down into one or many JSON files',
    ],
    a: [0],
    x: 'Una tabla Delta consta de archivos de datos Parquet más un subdirectorio `_delta_log` con archivos JSON de transacción; cada operación de escritura genera nuevos archivos de datos y una nueva entrada de log, nunca se sobrescriben in place.',
  },
  {
    n: 39,
    d: 'TRA',
    m: 0,
    q: 'Which of the following Structured Streaming queries successfully performs a hop from a Silver table to a Gold table?',
    o: [
      '```\nspark.table("sales").groupBy("store").agg(sum("sales"))\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("complete").table("aggregatedSales")\n```',
      '```\nspark.table("sales").writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("complete").table("sales")\n```',
      '```\nspark.table("sales").withColumn("avgPrice", col("sales") / col("units"))\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("cleanedSales")\n```',
      '```\nspark.readStream.load(rawSalesLocation)\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("uncleanedSales")\n```',
      '```\nspark.read.load(rawSalesLocation)\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("uncleanedSales")\n```',
    ],
    a: [0],
    x: 'La capa Gold suele contener datos agregados para negocio; agrupar y sumar ventas por tienda (opción A) es exactamente ese tipo de agregación Silver→Gold. Las demás opciones enriquecen sin agregar (Silver) o ingieren datos crudos (Bronze).',
  },
  {
    n: 40,
    d: 'ING',
    m: 0,
    q: 'What is the underlying technology that makes Auto Loader work?',
    o: ['Loader', 'Delta Live Tables', 'Structured Streaming', 'DataFrames', 'Live DataFrames'],
    a: [2],
    x: 'Auto Loader está construido sobre Structured Streaming, exponiendo una fuente de streaming llamada `cloudFiles` que procesa automáticamente los archivos nuevos que llegan a una ruta de almacenamiento en la nube.',
  },
  {
    n: 41,
    d: 'ING',
    m: 0,
    q: 'Which of the following Auto Loader Structured Streaming commands successfully performs a hop from the landing area into Bronze?',
    o: [
      '```\nspark.readStream.format("csv")\n  .option("cloudFiles.schemaLocation", checkpoint_directory)\n  .load("landing")\n  .writeStream.option("checkpointLocation", checkpoint_directory)\n  .table(raw)\n```',
      '```\nspark.readStream.format("cloudFiles")\n  .option("cloudFiles.format", "csv")\n  .option("cloudFiles.schemaLocation", checkpoint_directory)\n  .load("landing")\n  .writeStream.option("checkpointLocation", checkpoint_directory)\n  .table(raw)\n```',
      '```\nspark.read.format("cloudFiles")\n  .option("cloudFiles.format", "csv")\n  .option("cloudFiles.schemaLocation", checkpoint_directory)\n  .load("landing")\n  .writeStream.option("checkpointLocation", checkpoint_directory)\n  .table(raw)\n```',
      '```\nspark.readStream.load(rawSalesLocation)\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("uncleanedSales")\n```',
      '```\nspark.read.load(rawSalesLocation)\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("uncleanedSales")\n```',
    ],
    a: [1],
    x: 'Auto Loader requiere `format("cloudFiles")` junto con `cloudFiles.format` para indicar el formato real de los archivos (CSV en este caso). La opción A no usa `cloudFiles`, y la C usa `read` (batch) en vez de `readStream`.',
  },
  {
    n: 42,
    d: 'ING',
    m: 0,
    q: 'At the end of an inventory process, a file gets uploaded to cloud object storage. You need to ingest this data incrementally, and the schema of the file is expected to change over time, with the ingestion process handling those changes automatically. Fill in the blanks:\n\n```\nspark.readStream\n  .format("cloudfiles")\n  .option("__________", "csv")\n  .option("__________", \'dbfs:/location/checkpoint/\')\n  .load(data_source)\n  .writeStream\n  .option("__________", \'dbfs:/location/checkpoint/\')\n  .option("__________", "true")\n  .table(table_name)\n```',
    o: [
      '`format`, `checkpointlocation`, `schemalocation`, `overwrite`',
      '`cloudfiles.format`, `checkpointlocation`, `cloudfiles.schemalocation`, `overwrite`',
      '`cloudfiles.format`, `cloudfiles.schemalocation`, `checkpointlocation`, `mergeSchema`',
      '`cloudfiles.format`, `cloudfiles.schemalocation`, `checkpointlocation`, `overwrite`',
      '`cloudfiles.format`, `cloudfiles.schemalocation`, `checkpointlocation`, `append`',
    ],
    a: [2],
    x: 'Se necesita `cloudfiles.format` (formato de origen), `cloudfiles.schemalocation` (persistir el esquema inferido), `checkpointlocation` (progreso del stream) y `mergeSchema` (para que la evolución de esquema se aplique automáticamente); `overwrite` y `append` no gestionan cambios de esquema.',
  },
  {
    n: 43,
    d: 'TRA',
    m: 0,
    q: 'What is the purpose of the Bronze layer in a multi-hop architecture?',
    o: [
      'Can be used to eliminate duplicate records',
      'Used as a data source for machine learning applications',
      'Perform data quality checks, quarantining corrupt data',
      'Contains aggregated data that is to be consumed into Silver',
      'Provides efficient storage and querying of the full, unprocessed history of data',
    ],
    a: [4],
    x: 'Bronze es la capa de ingesta cruda: preserva el histórico completo tal cual llegó de origen, sin deduplicar, validar ni agregar — esas tareas corresponden a Silver y Gold respectivamente.',
  },
  {
    n: 44,
    d: 'TRA',
    m: 0,
    q: 'What is the purpose of the Silver layer in a multi-hop architecture?',
    o: [
      'Replaces a traditional data lake',
      'Provides efficient storage and querying of the full, unprocessed history of data',
      'A schema is enforced, with data quality checks',
      'Refined views with aggregated data',
      'Optimized query performance for business-critical data',
    ],
    a: [2],
    x: 'Silver elimina duplicados, aplica el esquema de producción y realiza comprobaciones de calidad, preservando la granularidad original de los datos (sin agregar), a diferencia de Bronze (crudo) y Gold (agregado).',
  },
  {
    n: 45,
    d: 'TRA',
    m: 0,
    q: 'What is the purpose of the Gold layer in a multi-hop architecture?',
    o: [
      'Optimizes ETL throughput and analytic query performance',
      'Eliminates duplicate records',
      'Preserves the grain of the original data, without any aggregations',
      'Data quality checks and schema enforcement',
      'Powers ML applications, reporting, dashboards, and ad hoc reports',
    ],
    a: [4],
    x: 'Gold contiene vistas refinadas y agregadas, optimizadas para consumo de negocio: ML, dashboards, reporting y analítica ad hoc; las tareas de deduplicación y enforcement de esquema ya se hicieron en Silver.',
  },
  {
    n: 46,
    d: 'TRO',
    m: 0,
    q: 'The data engineering team has provided 10 queries and asked the Data Analyst team to build a dashboard and refresh the data every day at 8 AM. What is the best approach to set up the data refresh for this dashboard?',
    o: [
      'Each query requires a separate task; set up 10 tasks under a single job to run at 8 AM to refresh the dashboard',
      'The entire dashboard with 10 queries can be refreshed at once; a single schedule needs to be set up to refresh at 8 AM',
      'Set up a job with linear dependencies to load all 10 queries into a table so the dashboard can be refreshed at once',
      'A dashboard can only refresh one query at a time; 10 schedules need to be set up',
      'Use incremental refresh to run at 8 AM every day',
    ],
    a: [1],
    x: 'Un dashboard de Databricks SQL se puede programar con un único schedule que refresca todas sus queries a la vez, sin necesidad de jobs ni schedules independientes por query.',
  },
  {
    n: 47,
    d: 'TRO',
    m: 0,
    q: 'You notice a job cluster is taking 6 to 8 minutes to start, which is delaying your job from finishing on time. What steps can you take to reduce the cluster startup time?',
    o: [
      'Set up a second job ahead of the first job so the cluster is already running when the actual job starts',
      'Use an all-purpose cluster instead to reduce cluster startup time',
      'Reduce the size of the cluster; the smaller it is, the faster it starts',
      'Use cluster pools to reduce the startup time of the jobs',
      'Use SQL endpoints to reduce the startup time',
    ],
    a: [3],
    x: 'Los cluster pools reservan instancias por adelantado; cuando se crea un job cluster, toma las VMs ya disponibles del pool en vez de aprovisionarlas desde cero, reduciendo drásticamente el tiempo de arranque.',
  },
  {
    n: 48,
    d: 'TRO',
    m: 0,
    q: "A data engineer is using a Databricks SQL query to monitor the performance of an ELT job. The query returns the number of minutes since the job's most recent runtime. Which of the following approaches lets the team be notified if the ELT job hasn't run in over an hour?",
    o: [
      'Set up an Alert on the accompanying dashboard to notify them if the returned value is greater than 60',
      'Set up an Alert for the query to notify when the ELT job fails',
      "Set up an Alert on the accompanying dashboard to notify when it hasn't refreshed in 60 minutes",
      'Set up an Alert for the query to notify them if the returned value is greater than 60',
      'This type of alert is not possible in Databricks',
    ],
    a: [3],
    x: 'Las alertas en Databricks SQL se configuran sobre una query (no sobre el dashboard), evaluando el valor devuelto frente a un umbral; aquí se dispara cuando ese valor supera 60 (minutos).',
  },
  {
    n: 49,
    d: 'TRO',
    m: 0,
    q: 'You worked with the Data Analysts team to set up a SQL Endpoint (SQL Warehouse) so they can query and analyze data in the gold layer. During peak hours, as the number of users increases, queries take longer to finish. Which of the following steps can be taken to resolve the issue?',
    o: [
      'Turn on the Serverless feature for the SQL endpoint',
      "Increase the maximum bound of the SQL endpoint's scaling range",
      'Increase the cluster size from 2X-Small to 4X-Large',
      'Turn on the Auto Stop feature for the SQL endpoint',
      'Turn on Serverless and change the Spot Instance Policy from "Cost optimized" to "Reliability Optimized"',
    ],
    a: [1],
    x: 'Ante concurrencia (más usuarios lanzando queries a la vez), lo que ayuda es escalar horizontalmente aumentando el máximo del rango de escalado del SQL Warehouse, para que se aprovisionen más clusters durante los picos de uso.',
  },
  {
    n: 50,
    d: 'ING',
    m: 0,
    q: 'You are currently working to ingest millions of files that get uploaded to cloud object storage for consumption, and you are asked to build a process to ingest this data. The schema of the file is expected to change over time, and the ingestion process should be able to handle these changes automatically. Which of the following methods can be used to ingest the data incrementally?',
    o: ['AUTO APPEND', 'AUTO LOADER', 'COPY INTO', 'Structured Streaming', 'Checkpoint'],
    a: [1],
    x: 'Auto Loader está diseñado para descubrir eficientemente millones de archivos (vía directory listing o file notification) y soporta inferencia y evolución de esquema automática, algo que COPY INTO maneja peor a esa escala.',
  },
  {
    n: 51,
    d: 'TRA',
    m: 0,
    q: 'Which of the following is the correct statement for a session-scoped temporary view?',
    o: [
      'Temporary views are lost once the notebook is detached and re-attached',
      'Temporary views are stored in memory',
      'Temporary views can still be accessed even if the notebook is detached and re-attached',
      'Temporary views can still be accessed even if the cluster is restarted',
      'Temporary views are created in the local_temp database',
    ],
    a: [0],
    x: 'Una vista temporal local/de sesión solo existe dentro de la Spark session en la que se creó; si el notebook se desvincula (detach) y se vuelve a vincular (reattach), esa sesión termina y la vista temporal se pierde.',
  },
  {
    n: 52,
    d: 'TRA',
    m: 0,
    q: 'You are reloading the customer_sales table using the following query:\n\n```\nINSERT OVERWRITE customer_sales\nSELECT * FROM customers c\nINNER JOIN sales_monthly s ON s.customer_id = c.customer_id\n```\n\nAfter running this, the marketing team wants to review the old data that was in the table. How does INSERT OVERWRITE impact the data in customer_sales if you want to see the previous version prior to running this statement?',
    o: [
      'Overwrites the data in the table, all historical versions of the data; you cannot time travel to previous versions',
      'Overwrites the data in the table but preserves all historical versions of the data; you can time travel to previous versions',
      'Overwrites the current version of the data but clears all historical versions, so you cannot time travel to previous versions',
      'Appends the data to the current version; you can time travel to previous versions',
      'By default, overwrites the data and schema; you cannot perform time travel',
    ],
    a: [1],
    x: 'Cualquier operación DML/DDL sobre una tabla Delta (excepto DROP TABLE) genera nuevos archivos Parquet sin borrar los anteriores, así que `INSERT OVERWRITE` reemplaza la versión actual pero conserva el histórico completo, accesible con time travel (`... VERSION AS OF n` o `TIMESTAMP AS OF ...`).',
  },
  {
    n: 53,
    d: 'TRO',
    m: 0,
    q: 'You are working with the marketing team to set up a dashboard for ad campaign analysis. Since the team is not sure how often the dashboard should be refreshed, they have decided to do a manual refresh on an as-needed basis. Which of the following steps can be taken to reduce the overall cost of the compute when the team is not using it?',
    o: [
      'Turn on the Serverless feature for the SQL endpoint (SQL Warehouse)',
      "Decrease the maximum bound of the SQL endpoint's scaling range",
      'Decrease the cluster size of the SQL endpoint',
      'Turn on the Auto Stop feature for the SQL endpoint',
      'Turn on Serverless and change the Spot Instance Policy from "Reliability Optimized" to "Cost Optimized"',
    ],
    a: [3],
    x: 'Auto Stop apaga automáticamente el warehouse tras un periodo de inactividad, evitando pagar por cómputo mientras nadie lo está usando entre refrescos manuales; es la opción diseñada específicamente para ese ahorro de costes.',
  },
  {
    n: 54,
    d: 'TRA',
    m: 0,
    q: 'Which of the following SQL commands creates a session-scoped temporary view?',
    o: [
      '`CREATE OR REPLACE TEMPORARY VIEW view_name AS SELECT * FROM table_name`',
      '`CREATE OR REPLACE LOCAL TEMPORARY VIEW view_name AS SELECT * FROM table_name`',
      '`CREATE OR REPLACE GLOBAL TEMPORARY VIEW view_name AS SELECT * FROM table_name`',
      '`CREATE OR REPLACE VIEW view_name AS SELECT * FROM table_name`',
      '`CREATE OR REPLACE LOCAL VIEW view_name AS SELECT * FROM table_name`',
    ],
    a: [0],
    x: '`CREATE OR REPLACE TEMPORARY VIEW` crea, por defecto, una vista de ámbito de sesión: se elimina automáticamente en cuanto termina la sesión o el notebook se desvincula.',
  },
  {
    n: 55,
    d: 'GOV',
    m: 0,
    q: 'Drop the customers database and its associated tables and data. All the tables inside the database are managed tables. Which of the following SQL commands accomplishes this?',
    o: [
      '`DROP DATABASE customers FORCE`',
      '`DROP DATABASE customers CASCADE`',
      '`DROP DATABASE customers INCLUDE`',
      'All the tables must be dropped first before dropping the database',
      '`DROP DELTA DATABASE customers`',
    ],
    a: [1],
    x: '`DROP DATABASE ... CASCADE` elimina la base de datos junto con todas las tablas que contiene; como todas son managed, Databricks también limpia sus archivos de datos automáticamente, sin pasos adicionales.',
  },
];

export const exam13: RawQuestion[] = [
  {
    n: 1,
    d: 'ING',
    m: 0,
    q: 'Scenario: You are loading external CSV files into a Delta table using the COPY INTO command. After running the command once, the schema of the incoming data changes (a new column is added), and the load fails. You must identify the correct SQL command that allows schema evolution to handle this situation.',
    o: [
      "`COPY INTO my_table FROM '/path/to/files' FILEFORMAT = CSV FORMAT_OPTIONS ('overWriteSchema' = 'true') COPY_OPTIONS ('overWriteSchema' = 'true');`",
      "`COPY INTO my_table FROM '/path/to/files' FILEFORMAT = CSV FORMAT_OPTIONS ('mergeSchema' = 'true') COPY_OPTIONS ('mergeSchema' = 'true');`",
      "`COPY INTO my_table FROM '/path/to/files' FILEFORMAT = CSV FORMAT_OPTIONS ('mergeSchema' = 'true') COPY_OPTIONS ('overWriteSchema' = 'true');`",
      "`COPY INTO my_table FROM '/path/to/files' FILEFORMAT = CSV FORMAT_OPTIONS ('overWriteSchema' = 'true') COPY_OPTIONS ('mergeSchema' = 'true');`",
      'None of the above',
    ],
    a: [1],
    x: "`mergeSchema = 'true'` debe indicarse tanto en `FORMAT_OPTIONS` como en `COPY_OPTIONS` para que `COPY INTO` evolucione el esquema automáticamente al detectar columnas nuevas. `overWriteSchema` no es una opción válida de `COPY INTO`.",
  },
  {
    n: 2,
    d: 'P',
    m: 0,
    q: 'Which of the following statements correctly describes the Databricks Lakehouse Platform?',
    o: [
      'Databricks Lakehouse Platform is the replacement of data warehouses',
      'Databricks Lakehouse Platform is only suitable for streaming data workloads to process data in real time',
      'Databricks Lakehouse Platform does not have support for BI use cases',
      'Databricks Lakehouse Platform combines the best elements of data lakes and data warehouses to deliver reliability, strong governance and performance',
      'Databricks Lakehouse Platform is designed only for batch data workloads with huge data',
    ],
    a: [3],
    x: 'El Lakehouse unifica la flexibilidad y escalabilidad de un data lake con la fiabilidad, gobernanza y rendimiento de un data warehouse, soportando BI, ML, batch y streaming a la vez; no sustituye a los warehouses, los converge con los lakes.',
  },
  {
    n: 3,
    d: 'ING',
    m: 0,
    q: 'A data engineer is converting an existing data pipeline to use Auto Loader for incremental processing of JSON files:\n\n```\nstreaming_df = (\n  spark\n  .readStream\n  .format("cloudFiles")\n  .______________________\n  .option("cloudFiles.schemaLocation", schemaLocation)\n  .load(sourcePath)\n)\n```\n\nWhich code snippet fills the blank so Auto Loader can ingest the data?',
    o: [
      '`option("format", "json")`',
      '`option("cloudFiles.format", "json")`',
      '`option("cloudFiles", "json")`',
      '`option(cloudFiles.format, json)`',
      '`option("json")`',
    ],
    a: [1],
    x: 'Auto Loader requiere `cloudFiles.format` para indicar el formato real de los archivos de origen (aquí, JSON); `format` a secas es la opción estándar de Spark, no la que espera `cloudFiles`.',
  },
  {
    n: 4,
    d: 'TRA',
    m: 0,
    q: 'You want to execute a block of code only when the value of variable isValid is 1 and the value of variable isRunnable is True. Which of the following Python control flow statements achieves this?',
    o: [
      '`if isValid = 1 and isRunnable = True:`',
      '`if isValid == 1 and isRunnable:`',
      '`if isValid == 1 and isRunnable = "True":`',
      '`if isValid = 1 and isRunnable:`',
      '`if isValid == 1 && isRunnable = True:`',
    ],
    a: [1],
    x: 'Python compara con `==` (no `=`, que es asignación) y usa `and` (no `&&`); como `isRunnable` ya es booleano, basta con escribirlo directamente en la condición.',
  },
  {
    n: 5,
    d: 'JOBS',
    m: 0,
    q: 'A data engineering team has noticed that one of their ETL jobs, which runs every midnight, fails intermittently due to one of its tasks. Every time, they have to manually rerun the job in the morning. Which of the following ways can the team use to ensure the job completes every night while minimizing compute costs?',
    o: [
      'Observe the task as it runs to try to determine why it is failing',
      'Institute a retry policy for the task that periodically fails',
      'Institute a retry policy for the entire job',
      'Set up the job to run multiple times, ensuring that at least one will complete',
      'Utilize a separate jobs cluster for each of the tasks in the job',
    ],
    a: [1],
    x: 'Aplicar una retry policy solo a la tarea que falla de forma intermitente resuelve el problema automáticamente sin relanzar todo el job, minimizando el coste de cómputo frente a reintentar el job completo o duplicar ejecuciones.',
  },
  {
    n: 6,
    d: 'TRA',
    m: 0,
    q: 'You have a view employees_updates that shows employees who joined or resigned last month. Column type indicates whether the employee is "new" or "former". You want to update the employees Delta table by inserting new employees and deleting former employees. Which query accomplishes this CDC task?',
    o: [
      '`MERGE INTO employees e USING employees_updates eu ON e.id = eu.id WHEN MATCHED AND eu.type = "former" THEN DELETE * WHEN NOT MATCHED AND eu.type = "new" THEN INSERT ALL` / `UPSERT INTO employees e USING employees_updates eu ON e.id = eu.id WHEN MATCHED AND eu.type = "former" THEN DELETE WHEN NOT MATCHED AND eu.type = "new" THEN INSERT *`',
      '`MERGE INTO employees e USING employees_updates eu ON e.id = eu.id WHEN MATCHED AND eu.type = "former" THEN DELETE WHEN NOT MATCHED AND eu.type = "new" THEN INSERT *`',
      '`MERGE INTO employees e USING employees_updates eu ON e.id = eu.id IF MATCHED AND eu.type = "former" THEN DELETE IF NOT MATCHED AND eu.type = "new" THEN INSERT *`',
      '`MERGE INTO employees e USING employees_updates eu ON e.id = eu.id WHEN MATCHED AND eu.type = "former" THEN DELETE ALL WHEN NOT MATCHED AND eu.type = "new" THEN INSERT *`',
    ],
    a: [1],
    x: 'La sintaxis válida de Delta Lake es `MERGE INTO ... WHEN MATCHED ... THEN DELETE` y `WHEN NOT MATCHED ... THEN INSERT *`. `UPSERT INTO`, `IF MATCHED` e `DELETE ALL` no son sintaxis válida en Databricks SQL.',
  },
  {
    n: 7,
    d: 'P',
    m: 0,
    q: 'You want the ability to break down your code into simpler, reusable components by placing custom functions in a separate notebook that can be used from any other notebook. Which magic command can be used to import that notebook into your current notebook?',
    o: ['`%import`', '`%execute`', '`%run`', '`%load`', '`%include`'],
    a: [2],
    x: '`%run` ejecuta otro notebook dentro del actual, dejando disponibles sus funciones, variables y clases en la sesión actual; `%import`, `%execute`, `%load` e `%include` no son comandos mágicos válidos en Databricks.',
  },
  {
    n: 8,
    d: 'JOBS',
    m: 0,
    q: 'In the context of Databricks workflows, which pattern would you use to handle data at different stages of refinement, from raw to ready-for-use?',
    o: ['Fan-out', 'Sequence', 'Loop', 'Funnel'],
    a: [1],
    x: 'El patrón Sequence procesa datos a través de varias etapas en orden definido, encajando con el refinamiento progresivo (raw → limpio → enriquecido); Fan-out y Funnel son para paralelismo/convergencia, y Loop para repetición condicional.',
  },
  {
    n: 9,
    d: 'TRA',
    m: 0,
    q: 'A junior data engineer has created a global temporary view called orders_vw. They run `SELECT * FROM orders_vw` and get an error. What correction should you suggest so the query runs?',
    o: [
      '`SELECT * FROM global_temporary.orders_vw`',
      '`SELECT * FROM global_temp.orders_vw`',
      '`SELECT * FROM gbl_temp.orders_vw`',
      '`SELECT * FROM gbl_temporary.orders_vw`',
      '`SELECT * FROM gbl_tmp.orders_vw`',
    ],
    a: [1],
    x: 'Las vistas temporales globales se almacenan en la base de datos reservada `global_temp`; hay que prefijar el nombre de la vista con `global_temp.`, no con `global_temporary` ni ninguna otra variante.',
  },
  {
    n: 10,
    d: 'TRA',
    m: 0,
    q: 'Which of the below SQL DDL statements can be used to create a UDF to convert a distance value from Miles to KM and vice-versa? (If any other distance measure is provided, the function should return the same value.)',
    o: [
      '`CREATE UDF convert_distance(distance DOUBLE, measure STRING) RETURN CASE WHEN measure = "Miles" THEN (distance * 1.60934) WHEN measure = "KM" THEN (distance * 0.621371) ELSE distance END;`',
      '`CREATE UDF FUNCTION convert_distance(distance DOUBLE, measure STRING) RETURN CASE WHEN measure = "Miles" THEN (distance * 1.60934) WHEN measure = "KM" THEN (distance * 0.621371) ELSE distance END;`',
      '`CREATE FUNCTION convert_distance(distance DOUBLE, measure STRING) RETURN CASE WHEN measure = "Miles" THEN (distance * 1.60934) WHEN measure = "KM" THEN (distance * 0.621371) ELSE distance END;`',
      '`CREATE FUNCTION convert_distance(distance DOUBLE, measure STRING) RETURNS DOUBLE RETURN CASE WHEN measure = "Miles" THEN (distance * 1.60934) WHEN measure = "KM" THEN (distance * 0.621371) ELSE distance END;`',
      '`CREATE UDF convert_distance(distance DOUBLE, measure STRING) RETURNS DOUBLE RETURN CASE WHEN measure = "Miles" THEN (distance * 1.60934) WHEN measure = "KM" THEN (distance * 0.621371) ELSE distance END;`',
    ],
    a: [3],
    x: 'La sintaxis correcta requiere `CREATE FUNCTION` (no `CREATE UDF`, que no existe) junto con `RETURNS <tipo>` explícito antes del `RETURN`; solo la opción con ambos elementos es válida.',
  },
  {
    n: 11,
    d: 'JOBS',
    m: 0,
    q: 'Which of the following must be specified when creating a new Delta Live Tables pipeline?',
    o: [
      'A key-value pair configuration',
      'The preferred DBU/hour cost',
      'A path to a cloud storage location for the written data',
      'A location of a target database for the written data',
      'At least one notebook library to be executed',
    ],
    a: [4],
    x: 'Un pipeline de Delta Live Tables necesita, como mínimo, al menos un notebook o library que contenga la lógica de transformación; la ubicación de almacenamiento, la base de datos destino y la configuración clave-valor son opcionales.',
  },
  {
    n: 12,
    d: 'TRA',
    m: 0,
    q: 'How can you inject the Python variables table_name and database_name into a SQL query and execute it using PySpark?',
    o: [
      '`spark.sql(f"SELECT * FROM {database_name}+{table_name}")`',
      '`spark.sql(f"SELECT * FROM [database_name].[table_name]")`',
      '`spark.sql(f"SELECT * FROM {database_name}.{table_name}")`',
      '`spark.sql(f"SELECT * FROM (database_name).(table_name)")`',
      '`spark.sql("SELECT * FROM <database_name>.<table_name>")`',
    ],
    a: [2],
    x: 'Un f-string de Python (`f"...{database_name}.{table_name}"`) interpola las variables directamente, y el punto es el separador válido entre base de datos y tabla en Spark SQL; corchetes, paréntesis y `+` no son sintaxis válida ahí.',
  },
  {
    n: 13,
    d: 'TRA',
    m: 0,
    q: 'A team of data analysts is testing data in a table called sales using SQL, but the data engineering team prefers Python in their notebooks. How can the data engineers query this table using PySpark?',
    o: [
      '`spark.table("select * from sales")`',
      '`"select * from sales"`',
      '`spark.sql("select * from sales")`',
      '`spark.sql("sales")`',
      'There is no way to access the sales table from a Python notebook',
    ],
    a: [2],
    x: '`spark.sql()` ejecuta una consulta SQL directamente desde Python y devuelve un DataFrame; `spark.table()` espera solo un nombre de tabla, no una sentencia SQL.',
  },
  {
    n: 14,
    d: 'ING',
    m: 0,
    q: 'What tool does Auto Loader utilize to process data in increments?',
    o: ['Checkpointing', 'Spark Structured Streaming', 'Unity Catalog', 'Databricks SQL', 'Data Explorer'],
    a: [1],
    x: 'Auto Loader se apoya en Spark Structured Streaming (mediante la fuente `cloudFiles`) como motor de procesamiento incremental; el checkpointing es un mecanismo interno de soporte, no la tecnología subyacente en sí.',
  },
  {
    n: 15,
    d: 'TRA',
    m: 0,
    q: 'Which of the following views cannot be referenced outside of the notebook in which they are declared?',
    o: [
      'View and Temporary View',
      'Global Temporary View and Temporary View',
      'View',
      'Global Temporary View',
      'Temporary View',
    ],
    a: [4],
    x: 'Una vista temporal (de sesión) solo vive dentro del notebook/sesión donde se creó; ni siquiera es accesible tras desvincular y volver a vincular el mismo notebook. La vista temporal global sí es accesible desde otros notebooks del mismo cluster.',
  },
  {
    n: 16,
    d: 'TRA',
    m: 0,
    q: 'You need to create a table employees with a calculated column age inferred from dateOfBirth:\n\n```\nCREATE TABLE employees (\n  id INT,\n  name STRING,\n  gender STRING,\n  dateOfBirth DATE,\n  age INT ___________,\n  salary LONG\n)\nUSING DELTA;\n```\n\nWhich statement correctly fills the blank?',
    o: [
      '`GENERATED DEFAULT AS (CAST(DATEDIFF(CURRENT_DATE, dateOfBirth) / 365) AS INT)`',
      '`GENERATED ALWAYS AS (CAST(DATEDIFF(CURRENT_DATE, dateOfBirth) / 365) AS FLOAT)`',
      '`GENERATED DEFAULT AS (CAST(DATEDIFF(CURRENT_DATE, dateOfBirth) / 365) AS FLOAT)`',
      '`GENERATED ALWAYS AS (CAST(DATEDIFF(CURRENT_DATE, dateOfBirth) / 365) AS INT)`',
      '`GENERATED AS (CAST(DATEDIFF(CURRENT_DATE, dateOfBirth) / 365) AS FLOAT)`',
    ],
    a: [3],
    x: 'Una columna generada requiere la palabra clave `GENERATED ALWAYS AS (...)`, y como `age` se declaró `INT`, el cast final debe ser a `INT` para que coincida con el tipo de columna.',
  },
  {
    n: 17,
    d: 'P',
    m: 0,
    q: 'In a Databricks notebook, all cells contain Python code and you want to add a cell with a SQL statement. You change the default language of the notebook to accomplish this. What changes (if any) appear in the already-existing Python cells?',
    o: [
      'Magic command %python is added at the end of all cells that contain Python code',
      "Python cells are grayed out and won't run until you change the default language back to Python",
      'Magic command %python is added at the beginning of all cells that contain Python code',
      'Magic command %sql is added at the beginning of all cells that contain Python code',
      'There is no change in any cell; the notebook remains the same',
    ],
    a: [2],
    x: 'Al cambiar el lenguaje por defecto del notebook, Databricks antepone automáticamente `%python` a las celdas ya escritas en Python, para que sigan ejecutándose correctamente aunque el lenguaje por defecto ya no lo sea.',
  },
  {
    n: 18,
    d: 'TRA',
    m: 0,
    q: 'A junior data engineer accidentally executed DELETE FROM products, removing all records. DESCRIBE HISTORY products shows the latest version is 21. Which SQL statement restores the table to its state right before the delete, with all data?',
    o: [
      '`ROLLBACK TABLE products TO VERSION 20`',
      '`RESTORE products TO VERSION AS OF 19`',
      '`ROLLBACK TABLE products TO VERSION 20`',
      '`RESTORE products TO VERSION 19`',
      '`RESTORE TABLE products TO VERSION AS OF 20`',
    ],
    a: [4],
    x: '`RESTORE TABLE ... TO VERSION AS OF 20` revierte la tabla a la versión inmediatamente anterior al DELETE (que generó la versión 21). `ROLLBACK TABLE` no existe en Delta Lake, y la versión 19 sería un estado anterior, no el previo al borrado.',
  },
  {
    n: 19,
    d: 'JOBS',
    m: 0,
    q: 'In which of the following scenarios would you want to use a job cluster instead of an all-purpose cluster?',
    o: [
      'A team of data scientists needs to collaborate on testing a machine learning model',
      'A data engineer needs to manually debug and fix a production error',
      'An ad-hoc analytics report needs to be developed using data visualizations',
      'An automated workflow needs to run every hour',
      'A data analyst needs to schedule a Databricks SQL query for upward reporting',
    ],
    a: [3],
    x: 'Un job cluster está pensado para workloads programados y efímeros: se aprovisiona, ejecuta y termina automáticamente, ideal para un flujo automatizado horario. Colaboración interactiva, debugging o análisis ad hoc requieren un cluster all-purpose.',
  },
  {
    n: 20,
    d: 'TRO',
    m: 0,
    q: "A data engineer notices that one of the notebooks within a Job (which runs two notebooks as separate tasks) is performing slowly during the Job's current execution. Which approach can a tech lead take to determine why the notebook is running slowly?",
    o: [
      'Navigate to the Runs tab in the Jobs UI and click on the active run to review the processing notebook',
      'Navigate to the Tasks tab in the Jobs UI to immediately review the processing notebook',
      'Navigate to the Runs tab in the Jobs UI to immediately review the processing notebook',
      'Navigate to the Tasks tab in the Jobs UI and click on the active run to review the processing notebook',
      'There is no way to determine why a Job task is running slowly',
    ],
    a: [0],
    x: 'La pestaña Runs de la UI de Jobs permite entrar en la ejecución activa y revisar logs, métricas de rendimiento y detalles de ejecución del notebook; la pestaña Tasks solo muestra configuración y dependencias, no datos de rendimiento en tiempo real.',
  },
  {
    n: 21,
    d: 'JOBS',
    m: 0,
    q: 'What is a critical step in orchestrating a task within a Databricks Workflow Job?',
    o: [
      'Assigning a unique identifier to each task manually',
      'Choosing a color scheme for task visualization',
      'Configuring the maximum allowable downtime for the job',
      'Setting up task dependencies to control execution order',
    ],
    a: [3],
    x: 'Las dependencias entre tareas determinan el orden de ejecución (secuencial o en paralelo) y garantizan que las tareas downstream solo se ejecuten tras completarse con éxito las upstream; es el mecanismo central de orquestación en Databricks Jobs.',
  },
  {
    n: 22,
    d: 'TRO',
    m: 0,
    q: 'You need to retain only versions of the sales Delta table that are not older than the last 7 days; the directory has a large number of historical data files. Which query deletes files older than 1 week?',
    o: [
      '`VACUUM sales RETAIN 168 HOURS`',
      '`VACUUM sales RETAIN 7 DAYS`',
      '`VACUUM sales RETAIN 10080 MINUTES`',
      '`VACUUM sales RETAIN 1 WEEK`',
      '`VACUUM sales RETAIN 604800 SECONDS`',
    ],
    a: [0],
    x: 'La cláusula `RETAIN` de `VACUUM` solo acepta un valor en HORAS; 7 días equivalen a 168 horas, por lo que es la única sintaxis válida entre las opciones (días, minutos, semanas y segundos no son unidades soportadas).',
  },
  {
    n: 23,
    d: 'TRA',
    m: 0,
    q: 'A team has two tables, march_transactions and april_transactions, with no duplicate records between them. They want a new table, all_transactions, that merges records from both without introducing duplicates. Which command should be executed?',
    o: [
      '`CREATE TABLE all_transactions AS SELECT * FROM march_transactions OUTER JOIN SELECT * FROM april_transactions;`',
      '`CREATE TABLE all_transactions AS SELECT * FROM march_transactions INTERSECT SELECT * FROM april_transactions;`',
      '`CREATE TABLE all_transactions AS SELECT * FROM march_transactions INNER JOIN SELECT * FROM april_transactions;`',
      '`CREATE TABLE all_transactions AS SELECT * FROM march_transactions MERGE SELECT * FROM april_transactions;`',
      '`CREATE TABLE all_transactions AS SELECT * FROM march_transactions UNION SELECT * FROM april_transactions;`',
    ],
    a: [4],
    x: '`UNION` combina los resultados de ambas tablas eliminando duplicados automáticamente, que es exactamente lo pedido; `INTERSECT` devolvería filas comunes (ninguna, ya que no hay duplicados), y `OUTER/INNER JOIN`/`MERGE` no son formas válidas de simplemente concatenar dos SELECT.',
  },
  {
    n: 24,
    d: 'JOBS',
    m: 0,
    q: 'A data engineer has set up two Jobs, A and B. Job A runs before Job B, and the output of Job A is used as input for Job B. If Job A fails, Job B cannot run until Job A is fixed and rerun successfully. Which statement is logically correct about Jobs A and B?',
    o: [
      'Job A has a dependency on Job B to get completed',
      'Both jobs are dependent on each other to get completed',
      'Job B has a dependency on Job A to get completed',
      'Both jobs are independent of each other and can be run independently',
      'None of the above',
    ],
    a: [2],
    x: 'Job B depende de que Job A termine con éxito, ya que consume su salida como entrada; es una dependencia de un solo sentido (downstream), no circular ni bidireccional.',
  },
  {
    n: 25,
    d: 'TRA',
    m: 0,
    q: 'Which of the following Structured Streaming queries is performing a hop from a Silver table to a Gold table?',
    o: [
      '```\nspark.table("sales").groupBy("store").agg(sum("sales"))\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("complete").table("newSales")\n```',
      '```\nspark.table("sales").withColumn("avgPrice", col("sales") / col("units"))\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("newSales")\n```',
      '```\nspark.read.load(rawSalesLocation)\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("newSales")\n```',
      '```\nspark.table("sales").filter(col("units") > 0)\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("newSales")\n```',
      '```\nspark.readStream.load(rawSalesLocation)\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("newSales")\n```',
    ],
    a: [0],
    x: 'Leer de `sales` (Silver), agregar por tienda (`groupBy`+`sum`) y escribir en `newSales` es exactamente una transformación de agregación Silver→Gold. Las demás opciones enriquecen sin agregar o ingieren datos crudos (→Bronze).',
  },
  {
    n: 26,
    d: 'TRO',
    m: 0,
    q: "A data engineer tries to use Delta Lake's time travel feature to revert a table to its state from three days ago, but the attempt fails because the data files were deleted. What is the reason for the missing data files?",
    o: [
      'The table was cleaned using the VACUUM command',
      'The HISTORY command was run on the table',
      'The DELETE HISTORY command was executed on the table',
      'The TIME TRAVEL feature was utilized on the table',
      'The OPTIMIZE command was applied to the table',
    ],
    a: [0],
    x: '`VACUUM` elimina permanentemente archivos antiguos ya no referenciados por el log de transacciones; si se ejecuta con un periodo de retención menor a la ventana de time travel necesaria, borra los archivos requeridos para volver a versiones anteriores.',
  },
  {
    n: 27,
    d: 'TRO',
    m: 0,
    q: 'Which of the following events will NOT happen when a cluster terminates in Databricks?',
    o: [
      'Associated virtual machines and operational memory will be purged',
      'Attached volume storage will be deleted',
      'Network connections between nodes will be removed',
      'Execution state and cache will be cleared out',
      'Code and data files which you have saved earlier will be lost',
    ],
    a: [4],
    x: 'Al terminar un cluster solo se liberan los recursos de cómputo (VMs, memoria, caché, red); el código y los datos guardados en almacenamiento persistente (DBFS, cloud storage) permanecen intactos, así que esta opción describe lo que NO ocurre.',
  },
  {
    n: 28,
    d: 'TRA',
    m: 0,
    q: 'A data engineer configures a Structured Streaming job:\n\n```\n(spark.readStream\n  .table("sales")\n  .withColumn("avg_price", col("sales") / col("units"))\n  .writeStream\n  .option("checkpointLocation", checkpointPath)\n  .outputMode("complete")\n  ._______\n  .table("new_sales")\n)\n```\n\nIf they only want the query to process all available data in as many batches as required, which line fills the blank?',
    o: [
      '`trigger(parallelBatch=True)`',
      '`trigger(availableNow=True)`',
      '`processingTime(1)`',
      '`trigger(processingTime="once")`',
      '`trigger(continuous="once")`',
    ],
    a: [1],
    x: '`trigger(availableNow=True)` procesa todos los datos disponibles al arrancar la query, repartidos en tantos micro-batches como haga falta, y luego se detiene; `parallelBatch` no existe, y `continuous="once"` no es una configuración válida del modo continuo.',
  },
  {
    n: 29,
    d: 'TRO',
    m: 0,
    q: 'A data engineer notices that the data files linked to a Delta table are unusually small, leading to suboptimal performance. Which keyword consolidates these small files into fewer, larger files?',
    o: ['REDUCE', 'OPTIMIZE', 'REPARTITION', 'COMPACTION', 'VACUUM'],
    a: [1],
    x: '`OPTIMIZE` compacta archivos pequeños de una tabla Delta en archivos más grandes, mejorando el rendimiento de lectura sin borrar ningún dato; `VACUUM` limpia archivos obsoletos pero no compacta.',
  },
  {
    n: 30,
    d: 'TRA',
    m: 0,
    q: 'A data engineer is implementing a data pipeline requiring updates, deletions, and merges into a large dataset, with reliable and consistent handling of concurrent modifications. Which feature guarantees atomic, consistent, isolated, and durable (ACID) transactions in this pipeline?',
    o: ['Delta Lake', 'File compaction', 'Spark Streaming', 'Data partitioning', 'Dataframe caching'],
    a: [0],
    x: 'Delta Lake aporta transacciones ACID sobre Spark mediante su transaction log, garantizando integridad ante updates, deletes y merges concurrentes; las demás opciones son técnicas de rendimiento, no de garantías transaccionales.',
  },
  {
    n: 31,
    d: 'JOBS',
    m: 0,
    q: "You have a scheduled weekly job that compiles analytics reports for Monday morning meetings. It's crucial that failures are addressed promptly and stakeholders are updated on status. How should you set up the job to keep stakeholders promptly informed?",
    o: [
      'Manually check the job status every Monday and send emails to stakeholders',
      'Schedule the job run for Monday morning and hope it completes on time',
      'Disable all alerts to avoid flooding stakeholders with notifications',
      "Configure the job to send completion alerts to the stakeholders' email addresses",
    ],
    a: [3],
    x: 'Configurar alertas de finalización dirigidas a los emails de los stakeholders automatiza la notificación del estado del job, sin depender de comprobaciones manuales ni de que todo salga bien por defecto.',
  },
  {
    n: 32,
    d: 'JOBS',
    m: 0,
    q: 'A daily pipeline includes ingestion, transformation, and loading tasks. One morning, an alert reports the job failed during the transformation task due to an unexpected cluster outage. What action minimizes downtime and ensures data integrity?',
    o: [
      'Use the repair feature to rerun only the failed transformation task after resolving the cluster issue',
      'Manually restart the entire job from the beginning',
      'Ignore the alert and wait for the next scheduled run',
      'Permanently remove the transformation task from the pipeline to avoid future failures',
    ],
    a: [0],
    x: 'La función repair permite relanzar solo la tarea que falló, conservando el trabajo ya completado por la tarea de ingesta exitosa, en vez de reprocesar todo el job desde cero.',
  },
  {
    n: 33,
    d: 'JOBS',
    m: 0,
    q: "A dataset defined with Delta Live Tables includes an expectations clause: `CONSTRAINT valid_timestamp EXPECT (timestamp > '2022-01-01') ON VIOLATION DROP ROW`. What is the expected behavior when a batch containing violating data is processed?",
    o: [
      'Records that violate the expectation cause the job to fail',
      'Records that violate the expectation are added to the target dataset and flagged as invalid in a field added to the target dataset',
      'Records that violate the expectation are dropped from the target dataset and loaded into a quarantine table',
      'Records that violate the expectation are added to the target dataset and recorded as invalid in the event log',
      'Records that violate the expectation are dropped from the target dataset and recorded as invalid in the event log',
    ],
    a: [4],
    x: '`ON VIOLATION DROP ROW` elimina del dataset destino los registros que incumplen la expectation, y el detalle de esas violaciones queda registrado en el event log para auditoría, sin fallar el job ni añadir registros inválidos.',
  },
  {
    n: 34,
    d: 'JOBS',
    m: 0,
    q: 'In which scenario should a predecessor task be set up?',
    o: [
      'When all tasks need to run in parallel',
      'When a task must wait for the completion of another task before it starts',
      'When tasks are scheduled at different times',
      'When tasks are completely independent of each other',
    ],
    a: [1],
    x: 'Un predecessor task se configura cuando una tarea debe esperar a que otra termine antes de arrancar, garantizando el orden correcto en el flujo de trabajo (como mezclar los ingredientes antes de hornear).',
  },
  {
    n: 35,
    d: 'JOBS',
    m: 0,
    q: "How does Databricks' 'Simple Workflow Authoring' feature cater to teams?",
    o: [
      'It is exclusively available for users with administrative privileges',
      'It requires advanced programming skills for all data team members',
      'It provides a point-and-click authoring experience accessible to all team members',
      'It only allows workflow editing in command-line interfaces',
    ],
    a: [2],
    x: 'Simple Workflow Authoring ofrece una experiencia visual de apuntar y hacer clic, accesible para miembros del equipo con distintos niveles técnicos, sin requerir habilidades avanzadas de programación ni acceso administrativo.',
  },
  {
    n: 36,
    d: 'TRO',
    m: 0,
    q: 'A task in your Databricks job fails unexpectedly. What is the first step you should take to debug this failed task?',
    o: [
      'Immediately escalate the issue to Databricks support',
      "Check the task's logs for error messages and clues about the failure",
      'Restart the job and hope it works this time',
      'Increase the resources allocated to the job without further investigation',
    ],
    a: [1],
    x: 'Revisar los logs de la tarea es el primer paso lógico para entender qué falló: contienen mensajes de error y contexto de ejecución necesarios antes de intentar cualquier solución.',
  },
  {
    n: 37,
    d: 'ING',
    m: 0,
    q: 'A data engineer is designing a pipeline where the source system generates files in a shared directory used by other processes; files accumulate and should be kept as-is. The engineer needs to identify which files are new since the previous run and ingest only those. Which tool solves this?',
    o: ['Data Explorer', 'Databricks SQL', 'Auto Loader', 'Delta Lake', 'Unity Catalog'],
    a: [2],
    x: 'Auto Loader detecta automáticamente los archivos nuevos en un directorio (por listado o notificaciones de archivo) e ingiere solo los que no se han procesado aún, sin necesidad de lógica manual de comparación.',
  },
  {
    n: 38,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to make a new table from different data sources, applying some transformations, in one step without changing the original data. Which SQL command is best for creating a new table and transforming the data at the same time?',
    o: ['INSERT INTO', 'CREATE TABLE AS SELECT (CTAS)', 'MERGE INTO', 'UPDATE', 'ALTER TABLE'],
    a: [1],
    x: 'CTAS permite crear una tabla nueva a partir del resultado de un `SELECT`, aplicando transformaciones (joins, cálculos, filtros) en un solo paso, sin modificar los datos de origen.',
  },
  {
    n: 39,
    d: 'TRA',
    m: 0,
    q: "A data engineer needs to add a new record to an existing Delta table my_table: id STRING = 'c01', rank INTEGER = 9, rating FLOAT = 6.2. Which SQL command appends this record?",
    o: [
      "`my_table UNION VALUES ('c01', 9, 6.2)`",
      "`INSERT VALUES ('c01', 9, 6.2) INTO my_table`",
      "`INSERT INTO my_table VALUES ('c01', 9, 6.2)`",
      "`UPDATE my_table VALUES ('c01', 9, 6.2)`",
      "`UPDATE VALUES ('c01', 9, 6.2) my_table`",
    ],
    a: [2],
    x: '`INSERT INTO tabla VALUES (...)` es la sintaxis estándar para añadir una fila nueva a una tabla existente, respetando el orden de columnas del esquema.',
  },
  {
    n: 40,
    d: 'TRA',
    m: 0,
    q: 'In which of the following scenarios should a data engineer use the MERGE INTO command instead of INSERT INTO?',
    o: [
      'When the source table can be deleted',
      'When the location of the data needs to be changed',
      'When the source is not a Delta table',
      'When the target table cannot contain duplicate records',
      'When the target table is an external table',
    ],
    a: [3],
    x: '`MERGE INTO` permite hacer upsert (insertar, actualizar o borrar condicionalmente) evitando duplicados en la tabla destino, algo que `INSERT INTO` no controla por sí solo.',
  },
  {
    n: 41,
    d: 'TRO',
    m: 0,
    q: 'A data engineer uses a Databricks SQL dashboard to monitor an ELT job, with a query that identifies orders with quantity 0. They want the whole team notified via a messaging webhook whenever this happens. Which method should they use?',
    o: [
      'Set up an Alert with a custom template',
      'Set up an Alert with a new webhook alert destination',
      'Set up an Alert with one-time notifications',
      'Set up an Alert with a new email alert destination',
      'Set up an Alert without notifications',
    ],
    a: [1],
    x: 'Configurar la Alert con un destino de tipo webhook permite integrarla directamente con plataformas de mensajería, enviando notificaciones automáticas en tiempo real cuando se cumple la condición, a diferencia de un destino de email.',
  },
  {
    n: 42,
    d: 'JOBS',
    m: 0,
    q: 'For what reason would a data engineer specify a task in the "Depends On" field while configuring a new task in a Databricks Job?',
    o: [
      'When another task has the same dependency libraries as the new task',
      'When another task needs to fail before the new task begins',
      'When another task needs to successfully complete before the new task begins',
      'When another task needs to be replaced by the new task',
      'When another task needs to use as little compute resources as possible',
    ],
    a: [2],
    x: 'El campo "Depends On" establece que la nueva tarea solo arranca después de que la tarea indicada termine con éxito, manteniendo el orden lógico correcto del workflow.',
  },
  {
    n: 43,
    d: 'GOV',
    m: 0,
    q: 'How can a data engineer perform audit logging and examine the lineage of datasets within Delta Live Tables pipelines?',
    o: [
      'Reviewing the Delta Lake transaction log for each table to trace back the lineage and audit changes',
      "Utilizing the DLT pipeline's settings to automatically generate audit logs for review",
      'Querying the event log for user_action events and flow_definition to audit user actions and understand dataset lineage',
      'Enabling detailed logging at the cluster level and examining system logs',
    ],
    a: [2],
    x: 'El event log de Delta Live Tables registra eventos `user_action` (auditoría de acciones del usuario) y `flow_definition` (linaje de cómo fluyen los datos entre datasets), siendo la fuente oficial para auditoría y linaje en pipelines DLT.',
  },
  {
    n: 44,
    d: 'TRA',
    m: 0,
    q: 'Which command allows data to be inserted into a Delta table while ensuring duplicates are not written?',
    o: ['INSERT', 'UPDATE', 'MERGE', 'APPEND', 'DROP'],
    a: [2],
    x: '`MERGE` permite condicionar la inserción según coincidencias con un criterio (upsert), evitando escribir filas duplicadas; `INSERT` y `APPEND` no comprueban duplicados por sí solos.',
  },
  {
    n: 45,
    d: 'JOBS',
    m: 0,
    q: 'A data engineer needs to apply a complex run schedule from one Job to others without manually setting each one in Databricks. Which tool allows them to define and apply this schedule through code (programmatically)?',
    o: [
      'Cron syntax',
      'pyspark.sql.types.DateType',
      'pyspark.sql.types.TimestampType',
      'datetime',
      'There is no way to represent and submit this information programmatically',
    ],
    a: [0],
    x: 'La sintaxis cron permite expresar programaciones complejas y reutilizarlas de forma programática en la configuración de un job; los tipos de datos de PySpark son para representar fechas en columnas, no para programar jobs.',
  },
  {
    n: 46,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to update an existing Delta table with fresh data from a recent batch process, replacing all existing records in a single operation. Which SQL command should be used?',
    o: ['INSERT OVERWRITE', 'UPDATE', 'INSERT INTO', 'ALTER TABLE', 'MERGE INTO'],
    a: [0],
    x: '`INSERT OVERWRITE` sustituye todos los registros existentes por los nuevos datos en una sola operación, ideal cuando se quiere refrescar la tabla completa; `UPDATE`/`MERGE` son para cambios condicionales, e `INSERT INTO` solo añade filas.',
  },
  {
    n: 47,
    d: 'TRA',
    m: 0,
    q: 'Which of the following statements does Databricks use to allow insert, update, and delete operations to run as a single atomic transaction on a Delta table?',
    o: ['UPSERT', 'MERGE', 'UPDATE', 'INSERT INTO', 'DELETE'],
    a: [1],
    x: '`MERGE` es el único comando que combina insert, update y delete en una sola transacción atómica sobre una tabla Delta; los demás solo cubren una operación individual.',
  },
  {
    n: 48,
    d: 'TRA',
    m: 0,
    q: 'As part of ingesting data, which of the following options helps you overwrite the existing data AND schema of a Delta table?',
    o: ['INSERT OVERWRITE', 'INSERT INTO', 'MERGE INTO', 'COPY INTO', 'CREATE OR REPLACE TABLE'],
    a: [4],
    x: '`CREATE OR REPLACE TABLE` (CRAS) reemplaza atómicamente tanto los datos como el esquema de la tabla en cada ejecución; `INSERT OVERWRITE` por defecto solo sobrescribe datos, no el esquema, salvo que se habilite `autoMerge`.',
  },
  {
    n: 49,
    d: 'TRA',
    m: 0,
    q: 'Which of the following SQL commands returns the count of distinct product names from a Delta table called products?',
    o: [
      '`SELECT COUNT(DISTINCT product_name) FROM products`',
      '`SELECT COUNT(UNIQUE product_name) FROM products`',
      '`SELECT UNIQUE(COUNT(product_name)) FROM products`',
      '`SELECT DISTINCT(COUNT product_name) FROM products`',
      '`SELECT DISTINCT(product_name) FROM products`',
    ],
    a: [0],
    x: '`COUNT(DISTINCT columna)` es la sintaxis estándar para contar valores únicos; `UNIQUE` no es una palabra clave válida en este contexto, y `SELECT DISTINCT(product_name)` devolvería los valores, no la cuenta.',
  },
  {
    n: 50,
    d: 'TRA',
    m: 0,
    q: 'Which of the following techniques does Structured Streaming use to ensure end-to-end, exactly-once semantics under any failure condition?',
    o: [
      'Write Ahead Logging and Water Marking',
      'Checkpointing, Write Ahead Logs and Idempotent Sinks',
      'Write Ahead Logging and Idempotent Sinks',
      'Checkpointing and Write Ahead Logging',
      'Idempotent Sinks and Water Marking',
    ],
    a: [1],
    x: 'Structured Streaming combina checkpointing y write-ahead logs (para registrar de forma fiable el rango de offsets procesado) con sinks idempotentes (para que reescrituras del mismo dato no generen duplicados), logrando semántica exactly-once end-to-end.',
  },
  {
    n: 51,
    d: 'ING',
    m: 0,
    q: 'What is the name of the column Auto Loader automatically adds to capture data that might be malformed and not fit into the table during incremental ingestion?',
    o: ['rescued_data', 'rescued_data_', '_rescued_data', '_rescued_data_', 'rescuedData'],
    a: [2],
    x: 'Auto Loader añade automáticamente la columna `_rescued_data` cuando infiere el esquema, para rescatar en formato JSON las columnas que no encajan (por columnas faltantes, discrepancias de tipo o de mayúsculas/minúsculas) en lugar de descartarlas.',
  },
  {
    n: 52,
    d: 'TRO',
    m: 0,
    q: 'How many days of historical job runs are preserved by Databricks?',
    o: ['30', '45', '60', '90', '120'],
    a: [2],
    x: 'Databricks conserva el histórico de ejecuciones de un job durante 60 días; si se necesita conservarlo más tiempo, se recomienda exportar los resultados antes de que expiren.',
  },
];

export const exam14: RawQuestion[] = [
  {
    n: 1,
    d: 'TRA',
    m: 0,
    q: 'What is the purpose of using schema_of_json in SQL?',
    o: [
      'To validate the structure of a JSON string',
      'To generate a schema that can be used to parse a JSON string into a struct',
      'To create a new JSON string from a struct',
      'To compress the JSON data for storage efficiency',
    ],
    a: [1],
    x: '`schema_of_json` analiza una cadena JSON y devuelve el esquema inferido, que luego se puede usar con `from_json` para convertir esa cadena en un `struct`; no valida el JSON ni lo comprime.',
  },
  {
    n: 2,
    d: 'TRA',
    m: 0,
    q: 'Which benefit is offered by utilizing array functions in Spark SQL?',
    o: [
      'The capability to process complex, hierarchical data structures sourced from JSON documents',
      'The capability to manipulate data across specific partitions and window frames',
      'The capability to interact with a collection of tables for streamlined automation',
      'The capability to manage time-based data over defined periods',
      'The capability to handle multiple data types simultaneously',
    ],
    a: [0],
    x: 'Las funciones de array permiten manipular directamente estructuras anidadas y jerárquicas típicas de JSON (extraer, transformar, agregar elementos) sin tener que aplanar los datos primero a formato relacional.',
  },
  {
    n: 3,
    d: 'TRA',
    m: 0,
    q: 'What distinguishes a materialized view from a standard database view?',
    o: [
      'There is no difference; both are terms used interchangeably in the context of database management',
      'A materialized view automatically updates in real-time as the underlying data changes, whereas a standard view requires manual refresh',
      'A materialized view stores the output of a query physically on disk, whereas a standard view does not store any data but provides a virtual view based on a query',
      'A materialized view can only be used in relational databases, while a standard view is specific to NoSQL databases',
    ],
    a: [2],
    x: 'Una vista materializada persiste físicamente el resultado de la query en disco, lo que mejora el rendimiento en lecturas frecuentes; una vista estándar es virtual y ejecuta la query subyacente cada vez que se consulta.',
  },
  {
    n: 4,
    d: 'TRA',
    m: 0,
    q: 'Which data type is typically involved when using the explode function?',
    o: ['BOOLEAN', 'STRING', 'ARRAY', 'INTEGER'],
    a: [2],
    x: '`explode` opera sobre tipos `ARRAY` (o `MAP`), tomando cada elemento del array y generando una fila independiente por cada uno.',
  },
  {
    n: 5,
    d: 'TRA',
    m: 0,
    q: 'Which function is typically used to convert a JSON string into a struct in SQL?',
    o: ['JSON_TO_STRUCT', 'CAST_AS_STRUCT', 'FROM_JSON', 'TO_STRUCT'],
    a: [2],
    x: '`FROM_JSON` parsea una cadena JSON y la convierte en un tipo `struct`, permitiendo acceder a sus campos como si fueran columnas normales.',
  },
  {
    n: 6,
    d: 'TRA',
    m: 0,
    q: 'A data engineering team needs a weekly report aggregating sales data from millions of transactions in Delta Lake. The query is complex and slow. To mirror the concept of a materialized view in Databricks, which strategy best achieves this?',
    o: [
      'Create a new Delta Lake table that pre-aggregates the sales data on a daily basis and query this table for the weekly report',
      'Directly query the transactional data each time the report is needed, ensuring the most up-to-date information is used',
      'Increase the compute resources of the Databricks cluster before running the weekly report to reduce execution time',
      'Store the query results in blob storage and update it manually every month to ensure data freshness',
    ],
    a: [0],
    x: 'Pre-agregar los datos en una tabla Delta separada, refrescada periódicamente por un job, emula el comportamiento de una vista materializada: evita reprocesar todo el dataset cada vez y acelera el informe semanal.',
  },
  {
    n: 7,
    d: 'TRA',
    m: 0,
    q: "Given a table 'events' with a column 'items' that contains an array of values, which SQL query transforms each item of the array into a separate row?",
    o: [
      '`SELECT user_id, event_timestamp, event_name, ARRAY(items) AS item FROM events;`',
      '`SELECT user_id, event_timestamp, event_name, items FROM events;`',
      '`SELECT user_id, event_timestamp, event_name, explode(items) AS item FROM events;`',
      '`SELECT user_id, event_timestamp, event_name, UNNEST(items) AS item FROM events;`',
    ],
    a: [2],
    x: '`explode(items)` genera una fila por cada elemento del array `items`; `UNNEST` no es una función soportada en Spark SQL, y `ARRAY(items)` simplemente envolvería la columna en otro array.',
  },
  {
    n: 8,
    d: 'TRA',
    m: 0,
    q: 'What SQL data type should be used to store an ordered collection of multiple names in a table?',
    o: ['MAP', 'STRUCT', 'VARCHAR', 'ARRAY'],
    a: [3],
    x: '`ARRAY` almacena una colección ordenada de elementos del mismo tipo (aquí, cadenas con nombres); `MAP` es para pares clave-valor y `STRUCT` para campos heterogéneos con nombre.',
  },
  {
    n: 9,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to create a relational object by joining data from two tables. It does not need to be used by other engineers in other sessions, and to save on storage costs the engineer wants to avoid copying and storing physical data. Which relational object should they create?',
    o: ['View', 'Delta Table', 'Database', 'Temporary View', 'Spark SQL Table'],
    a: [3],
    x: 'Una vista temporal combina datos de otras tablas sin persistir físicamente el resultado y solo dura la sesión actual, cumpliendo exactamente el requisito de no compartirla entre sesiones y no gastar almacenamiento.',
  },
  {
    n: 10,
    d: 'TRA',
    m: 0,
    q: 'Which of the following SQL keywords can be used to convert a table from a long format to a wide format?',
    o: ['TRANSFORM', 'PIVOT', 'CONVERT', 'SUM', 'WHERE'],
    a: [1],
    x: '`PIVOT` rota valores únicos de una columna en múltiples columnas de salida, transformando datos de formato largo (filas) a formato ancho (columnas), típico de tablas resumen.',
  },
  {
    n: 11,
    d: 'TRA',
    m: 0,
    q: "A data engineer creates a temporary view of a dataset in their current Spark session for preliminary exploration, then tries to access it from a different Spark session. Why can't they access the temporary view from the other session?",
    o: [
      'The network connection between the two Spark sessions was lost, preventing access to the temporary view',
      'Temporary views are automatically deleted after a certain period, and the time limit was exceeded',
      'The data engineer did not correctly create the temporary view, resulting in an error when trying to access it from another session',
      'Temporary views require special permissions to be accessed from different sessions',
      'Temporary views in Spark SQL are session-scoped and cannot be accessed from different sessions',
    ],
    a: [4],
    x: 'Una vista temporal solo existe durante la sesión de Spark en la que se creó; para compartirla entre sesiones habría que usar una vista temporal global (`GLOBAL TEMP VIEW`) en su lugar.',
  },
  {
    n: 12,
    d: 'TRA',
    m: 0,
    q: 'When querying data from a JSON file in Databricks SQL, what prefix should be included after the FROM keyword?',
    o: ['json', 'JSON', 'FILE', 'TEXT'],
    a: [0],
    x: 'Debe usarse el prefijo en minúsculas `json.` (p. ej. `FROM json.\\`/mnt/data/file.json\\``); Databricks SQL distingue mayúsculas/minúsculas en los prefijos de formato de archivo.',
  },
  {
    n: 13,
    d: 'TRA',
    m: 0,
    q: 'What syntax does Spark SQL use to query subfields within STRUCT data types?',
    o: ['Dot (.)', 'Semicolon (;)', 'Comma (,)', 'Colon (:)'],
    a: [0],
    x: 'Spark SQL usa notación de punto para acceder a subcampos de un `STRUCT`, por ejemplo `person.address.city`; los dos puntos se usan en otros contextos (como JSONPath) pero no aquí.',
  },
  {
    n: 14,
    d: 'TRA',
    m: 0,
    q: 'When querying data from a CSV file in Databricks SQL, what prefix should be included after the FROM keyword to correctly interpret the source as a CSV file?',
    o: ['CSV', 'csv', 'TEXT', 'FILE'],
    a: [1],
    x: 'El prefijo debe ir en minúsculas: `csv.` (p. ej. `FROM csv.\\`/mnt/data/file.csv\\``); Databricks SQL es sensible a mayúsculas/minúsculas en estos prefijos de formato.',
  },
  {
    n: 15,
    d: 'TRA',
    m: 0,
    q: 'A data engineer working in Databricks wants to create a User-Defined Function (UDF) to convert temperatures from Celsius to Fahrenheit for a dataset with global weather information. What should they do within a Databricks notebook?',
    o: [
      'Export the dataset to a CSV file, convert the temperatures using an external tool, and then re-import the dataset into Databricks',
      'Create a UDF in Databricks that applies the conversion formula and use it in a Spark SQL query to transform the temperature column',
      'Use a built-in Spark SQL function that automatically converts temperatures from Celsius to Fahrenheit',
      'Directly modify the dataset in the data lake to convert all temperatures to Fahrenheit before importing it into Databricks',
    ],
    a: [1],
    x: 'No existe una función nativa de Spark SQL para esta conversión; hay que definir una UDF con la fórmula (`C * 9/5 + 32`), registrarla, y aplicarla en la query, en lugar de exportar los datos o modificarlos fuera de Databricks.',
  },
  {
    n: 16,
    d: 'P',
    m: 0,
    q: "What is the primary role of a 'Driver' in a cluster environment?",
    o: [
      'To control access to the data storage',
      'To manage cloud storage',
      'To distribute workloads across workers',
      'To serve as the primary compute instance for executing code',
    ],
    a: [2],
    x: 'El driver orquesta la ejecución de la aplicación Spark: construye el plan de ejecución y distribuye las tareas entre los worker nodes, que son quienes hacen el grueso del cómputo.',
  },
  {
    n: 17,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to implement custom logic on the string column city in the location table. Which code snippet correctly defines this SQL UDF?',
    o: [
      '`CREATE FUNCTION combine_california (city STRING) RETURNS STRING RETURN CASE WHEN city = "San Francisco" THEN "California" ELSE city END;`',
      '`CREATE UDF combine_california (city STRING) RETURN CASE WHEN city = "San Francisco" THEN "California" ELSE city END;`',
      '`CREATE FUNCTION combine_california (city STRING) RETURN CASE WHEN city = "San Francisco" THEN "California" ELSE city END;`',
      '`CREATE FUNCTION combine_california (city STRING) RETURNS STRING CASE WHEN city = "San Francisco" THEN "California" ELSE city END;`',
      '`CREATE UDF combine_california (city STRING) RETURN CASE WHEN city = "San Francisco" THEN "California" ELSE city END;`',
    ],
    a: [0],
    x: 'La sintaxis válida es `CREATE FUNCTION nombre(parámetros) RETURNS tipo RETURN expresión`; `CREATE UDF` no existe, y omitir `RETURNS STRING` o el `RETURN` antes del `CASE` invalida la definición.',
  },
  {
    n: 18,
    d: 'TRA',
    m: 0,
    q: 'A data analyst has developed a query against a Delta table. The data engineering team (using Python/PySpark) must implement tests to ensure the query returns clean data. Which operation could they use to run the query and work with the results in PySpark?',
    o: [
      'There is no way to share data between PySpark and SQL',
      'spark.sql',
      'spark.delta.table',
      'spark.table',
      'SELECT * FROM sales',
    ],
    a: [1],
    x: '`spark.sql()` ejecuta la consulta SQL directamente desde Python y devuelve un DataFrame que se puede validar con lógica Python; `spark.delta.table` no existe, y `spark.table()` solo carga una tabla por nombre, no ejecuta SQL arbitrario.',
  },
  {
    n: 19,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to decide between a temporary view and a materialized-view-like strategy for a task that aggregates large volumes of data into a dashboard, which needs daily updates with minimal latency. Which approach is best?',
    o: [
      'Implement a materialized-view-like strategy for both the dashboard and historical data analysis to reduce computational load',
      'Implement a materialized-view-like strategy for the dashboard to ensure quick access to pre-aggregated data, and use a temporary view for ad-hoc, real-time analysis tasks that do not require data persistence',
      'Use a temporary view for both the dashboard and historical data analysis to maximize data freshness and minimize storage costs',
      'Use a temporary view for the dashboard to ensure data is always up-to-date, and a materialized-view-like strategy for historical data analysis where latency is acceptable',
    ],
    a: [1],
    x: 'El dashboard necesita datos pre-agregados de baja latencia, ideal para una estrategia tipo vista materializada con refresco periódico; el análisis ad hoc encaja mejor con vistas temporales, que no requieren persistencia.',
  },
  {
    n: 20,
    d: 'P',
    m: 0,
    q: "Which of the following is stored in the Databricks customer's cloud account?",
    o: ['Repos', 'Cluster management metadata', 'Data', 'Databricks web application', 'Notebooks'],
    a: [2],
    x: 'Los datos del cliente siempre residen en su propia cuenta cloud (AWS/Azure/GCP); Repos, notebooks, metadatos de clusters y la aplicación web viven en el control plane, gestionado por Databricks.',
  },
  {
    n: 21,
    d: 'TRA',
    m: 0,
    q: 'A data analyst has a Delta table named sales used by the whole analysis team. The data engineering team prefers Python for testing instead of SQL. Which command lets them access the sales table in PySpark?',
    o: [
      'SELECT * FROM sales',
      '`spark.delta.table("sales")`',
      '`spark.sql("SELECT * FROM sales")`',
      'Data sharing between PySpark and SQL is not possible',
      '`spark.table("sales")`',
    ],
    a: [4],
    x: '`spark.table("sales")` carga la tabla Delta directamente como DataFrame de PySpark, sin necesidad de escribir SQL, que es justo lo que pide el enunciado (preferencia por Python).',
  },
  {
    n: 22,
    d: 'TRA',
    m: 0,
    q: 'Which of the following best describes the difference between Gold tables and Silver tables?',
    o: [
      'Gold tables are more likely to contain a less refined view of data than Silver tables',
      'Gold tables are more likely to contain aggregations than Silver tables',
      'Gold tables are more likely to contain more data than Silver tables',
      'Gold tables are more likely to contain valuable data than Silver tables',
      'Gold tables are more likely to contain truthful data than Silver tables',
    ],
    a: [1],
    x: 'Silver contiene datos limpios y conformados a nivel de detalle; Gold suele contener agregaciones de negocio construidas a partir de Silver, orientadas a dashboards, reporting y ML.',
  },
  {
    n: 23,
    d: 'TRA',
    m: 0,
    q: 'Which benefit is offered by Delta Lake within the Databricks Lakehouse Platform?',
    o: [
      'The capability to execute complex data operations at scale',
      'The capability to handle both batch and streaming data processing tasks',
      'The capability to configure notifications for when queries do not execute successfully',
      'The capability for simultaneous collaboration within a single notebook',
      'The capability to work with the same dataset across multiple programming languages',
    ],
    a: [1],
    x: 'Delta Lake ofrece una capa de almacenamiento unificada con transacciones ACID que soporta batch y streaming a la vez de forma consistente; las notificaciones, la colaboración en notebooks y el multilenguaje son funcionalidades de otras piezas de Databricks, no de Delta Lake en sí.',
  },
  {
    n: 24,
    d: 'TRA',
    m: 0,
    q: 'You have a Delta Lake table customer_purchases with duplicate records based on customer_id. You want to deduplicate, keeping only the latest purchase record per customer based on purchase_date. How would you use MERGE in Databricks SQL to achieve this?',
    o: [
      '`MERGE INTO customer_purchases AS target USING (SELECT customer_id, MAX(purchase_date) AS latest_purchase_date FROM customer_purchases GROUP BY customer_id) AS source ON target.customer_id = source.customer_id WHEN MATCHED AND target.purchase_date < source.latest_purchase_date THEN DELETE WHEN NOT MATCHED THEN INSERT *;`',
      '`MERGE INTO customer_purchases AS target USING customer_purchases AS source ON target.customer_id = source.customer_id WHEN MATCHED AND target.purchase_date < source.purchase_date THEN DELETE;`',
      '`MERGE INTO customer_purchases AS target USING (SELECT customer_id, purchase_date FROM customer_purchases WHERE purchase_date = (SELECT MAX(purchase_date) FROM customer_purchases GROUP BY customer_id)) AS source ON target.customer_id = source.customer_id WHEN MATCHED THEN UPDATE SET target.* = source.* WHEN NOT MATCHED THEN INSERT *;`',
      '`MERGE INTO customer_purchases AS target USING (SELECT customer_id, MAX(purchase_date) AS latest_purchase_date FROM customer_purchases GROUP BY customer_id) AS source ON target.customer_id = source.customer_id AND target.purchase_date < source.latest_purchase_date WHEN MATCHED THEN DELETE;`',
    ],
    a: [3],
    x: 'Comparar cada fila con la fecha máxima por `customer_id` (subquery agregada) y borrar (`DELETE`) las que sean anteriores a esa fecha máxima elimina los duplicados dejando solo el registro más reciente; las demás opciones tienen sintaxis inválida o comparan mal el criterio de "más reciente".',
  },
  {
    n: 25,
    d: 'TRA',
    m: 0,
    q: 'What does the following Databricks Spark SQL code accomplish?\n\n```\nCREATE OR REPLACE TEMPORARY VIEW daily_sales AS\nSELECT date, SUM(amount) AS total_sales\nFROM sales\nGROUP BY date;\n\nCREATE OR REPLACE GLOBAL TEMP VIEW global_daily_sales AS\nSELECT * FROM daily_sales;\n```',
    o: [
      'Creates a Temporary View daily_sales and a Global Temporary View global_daily_sales with the same data, but global_daily_sales can be accessed in any session within the application',
      'Creates a Materialized View daily_sales that is stored on disk and a Global Temporary View global_daily_sales that is stored in memory',
      'Throws an error because a Global Temporary View cannot be created from a Temporary View',
      'Creates two identical views, daily_sales and global_daily_sales, both of which are accessible across multiple Spark sessions and applications',
    ],
    a: [0],
    x: '`daily_sales` es una vista de sesión (solo accesible en la sesión actual), mientras que `global_daily_sales` se guarda en la base de datos `global_temp` y sí es accesible desde cualquier sesión de la misma aplicación Spark; no hay error al crear una global a partir de una vista de sesión.',
  },
  {
    n: 26,
    d: 'CICD',
    m: 0,
    q: 'What limitation does Databricks Notebooks have regarding version control integration compared to Databricks Repos?',
    o: [
      'Notebooks have built-in conflict resolution tools that Repos lack',
      'Notebooks support direct integration with all major version control systems, while Repos do not',
      'Repos allow for full Git integration, including branching and merging, which is not directly supported within Notebooks',
      'Notebooks can only be linked to a single branch in a Git repository, whereas Repos can be linked to multiple branches simultaneously',
    ],
    a: [2],
    x: 'Los notebooks solo ofrecen historial básico de versiones (snapshots automáticos), sin soporte de Git (branching, merging, pull requests); Repos sí se integra directamente con proveedores Git como GitHub o GitLab con esas capacidades completas.',
  },
  {
    n: 27,
    d: 'P',
    m: 0,
    q: 'What are the two main components of the Data Plane in Databricks Workspace and Services?',
    o: [
      'Notebooks, Repos, DBSQL',
      'Workflow Manager and Jobs',
      'Workspace clusters and SQL Warehouses',
      'Metastore and Access Control',
    ],
    a: [2],
    x: 'El data plane es donde ocurre el cómputo real: los clusters del workspace (procesamiento distribuido) y los SQL Warehouses (ejecución de queries BI/analíticas) son sus dos componentes principales; notebooks, jobs, metastore y control de acceso pertenecen al control plane.',
  },
  {
    n: 28,
    d: 'TRO',
    m: 0,
    q: 'What is the impact of terminating a cluster in Databricks?',
    o: [
      'Terminating a cluster automatically archives all its notebooks and configurations for future restoration',
      'Terminating a cluster removes all user access permissions and configurations permanently',
      "Terminating a cluster permanently deletes all data stored in the cluster's associated storage",
      'Terminating a cluster releases the compute resources but retains the data in attached storage, allowing for data persistence',
    ],
    a: [3],
    x: 'Al terminar un cluster se liberan las VMs y se limpia el estado en memoria/almacenamiento local efímero, pero los datos en almacenamiento persistente (DBFS, cloud storage) permanecen intactos.',
  },
  {
    n: 29,
    d: 'JOBS',
    m: 0,
    q: 'What is a key difference between all-purpose clusters and job clusters in Databricks?',
    o: [
      'All-purpose clusters can only run SQL commands, while job clusters can run commands in multiple languages',
      'All-purpose clusters do not support autoscaling, whereas job clusters do',
      'All-purpose clusters are designed for interactive analysis by multiple users, whereas job clusters are optimized for running specific jobs and are terminated after the job is complete',
      'Job clusters are permanent and cannot be terminated, unlike all-purpose clusters',
    ],
    a: [2],
    x: 'Los all-purpose clusters están pensados para trabajo interactivo compartido por varios usuarios; los job clusters se crean, ejecutan un job específico y se terminan automáticamente al acabar, optimizando coste y aislamiento.',
  },
  {
    n: 30,
    d: 'CICD',
    m: 0,
    q: 'How do Databricks Notebooks and Repos differ in terms of file size limitations for version control?',
    o: [
      'Notebooks allow for larger file sizes in version control than Repos',
      'Both Notebooks and Repos have the same file size limitations for version control',
      'Notebooks have no file size limitations, while Repos have a strict limit',
      'Repos support larger file sizes for version control compared to Notebooks',
    ],
    a: [3],
    x: 'Repos está diseñado para manejar de forma más eficiente el control de versiones de proyectos, incluyendo archivos más grandes, en comparación con el versionado nativo integrado directamente en Notebooks.',
  },
  {
    n: 31,
    d: 'P',
    m: 0,
    q: 'If you are analyzing data in a Databricks notebook using Python, how can you switch to SQL for a specific visualization task?',
    o: [
      'Use the %sql magic command in a new cell where you want to start using SQL',
      'Export the data to a SQL database and create a new SQL notebook',
      'Rewrite the Python code in SQL and run it in a new Python cell',
      'Convert the entire notebook to SQL mode from the notebook settings',
    ],
    a: [0],
    x: 'El comando mágico `%sql` cambia el contexto de ejecución a SQL solo para esa celda concreta, sin necesidad de exportar datos ni convertir el notebook entero.',
  },
  {
    n: 32,
    d: 'GOV',
    m: 0,
    q: "What is the role of a storage credential in Databricks' Unity Catalog?",
    o: [
      'To restrict access to data based on user roles',
      'To provide encryption for data at rest',
      'To ensure data is backed up across multiple regions',
      'To enable Unity Catalog to connect to an external cloud storage',
    ],
    a: [3],
    x: 'Una storage credential autentica y autoriza a Unity Catalog para acceder a servicios de almacenamiento cloud externos (como S3 o Azure Storage), permitiéndole gestionar e interactuar con esos datos.',
  },
  {
    n: 33,
    d: 'TRO',
    m: 0,
    q: 'A data team is considering adopting Cluster Pools within their Databricks environment. In which scenario would a data team most benefit from implementing cluster pools?',
    o: [
      'To guarantee the reproducibility of an automated report',
      'To conduct error detection tests on an automated report',
      'An automated report needs to be refreshed as quickly as possible',
      'To manage version control for an automated report with multiple contributors',
      'To enable all stakeholders to execute an automated report',
    ],
    a: [2],
    x: 'Los cluster pools reducen drásticamente el tiempo de arranque manteniendo instancias pre-calentadas, lo que resulta clave cuando la velocidad de refresco de un informe automatizado es crítica; no resuelven reproducibilidad, testing ni control de versiones.',
  },
  {
    n: 34,
    d: 'GOV',
    m: 0,
    q: 'When integrating the Unity Catalog with existing systems, what is a key advantage?',
    o: [
      'It requires a complete overhaul of existing data catalogs',
      'It necessitates the use of Databricks-exclusive storage systems',
      'It operates solely on non-standard SQL variants for legacy systems',
      'It works in concert with existing data, storage, and catalogs, without the need for hard migration',
    ],
    a: [3],
    x: 'Unity Catalog está diseñado para integrarse con la infraestructura existente (datos, almacenamiento, catálogos) sin exigir una migración completa y disruptiva, facilitando su adopción.',
  },
  {
    n: 35,
    d: 'GOV',
    m: 0,
    q: 'What is the purpose of Data Access Control within Data Governance?',
    o: [
      'To automate the data ingestion process',
      'To monitor the performance of data processing jobs',
      'To ensure that data processing is as fast as possible',
      'To control who has access to which data within the Databricks environment',
    ],
    a: [3],
    x: 'El control de acceso a datos gestiona permisos y derechos de acceso para que solo usuarios autorizados puedan consultar ciertos datasets, un pilar central de la gobernanza de datos.',
  },
  {
    n: 36,
    d: 'GOV',
    m: 0,
    q: "What is the purpose of an 'External Location' in Databricks?",
    o: [
      'It is used to log the performance metrics of the Databricks runtime environment',
      'It refers to a cloud storage path combined with storage credentials to access specific data',
      'It is a dedicated space for Delta Live Tables',
      'It is a location where Databricks stores its internal metadata',
    ],
    a: [1],
    x: 'Una External Location combina una ruta de almacenamiento en la nube con las credenciales necesarias, permitiendo un acceso controlado y granular a ubicaciones concretas de ese almacenamiento externo.',
  },
  {
    n: 37,
    d: 'GOV',
    m: 0,
    q: 'What is the role of Data Lineage in Databricks Data Governance?',
    o: [
      'To capture upstream sources and downstream consumers of data',
      'To replicate data across multiple cloud providers',
      'To compress data for efficient storage',
      'To convert data into different formats automatically',
    ],
    a: [0],
    x: 'El linaje de datos da visibilidad sobre el ciclo de vida completo de un dato: su origen, sus transformaciones y hacia dónde fluye, esencial para calidad de datos y troubleshooting.',
  },
  {
    n: 38,
    d: 'P',
    m: 0,
    q: 'How can users filter clusters in Databricks to view only those that are accessible to them?',
    o: [
      'By contacting the Databricks administrator to receive a custom list of clusters that the user has access to',
      'By using the Databricks CLI to manually list all clusters and applying filters based on user permissions',
      'By navigating to the Clusters UI in Databricks and using the built-in filtering options to display only the clusters they have permissions to access',
      "By executing a specific SQL query in Databricks SQL Analytics to retrieve a list of accessible clusters based on the user's role",
    ],
    a: [2],
    x: 'La UI de Clusters incluye opciones de filtrado integradas que muestran directamente solo los clusters a los que el usuario tiene permiso de acceso, sin pasos manuales adicionales.',
  },
  {
    n: 39,
    d: 'CICD',
    m: 0,
    q: 'How can you enable collaborative editing of a Databricks notebook by multiple team members within your organization?',
    o: [
      'Assign the notebook to a shared folder and grant edit permissions to the team',
      'Set the notebook to "Public" mode, allowing anyone to edit',
      'Email the notebook as an attachment to team members for sequential editing',
      'Use an external version control system to manage changes and synchronize with the Databricks notebook',
    ],
    a: [0],
    x: 'Colocar el notebook en una carpeta compartida del workspace y asignar permisos de edición al equipo es la forma nativa y directa de habilitar la edición colaborativa en Databricks.',
  },
  {
    n: 40,
    d: 'GOV',
    m: 0,
    q: 'Which component in the Databricks Control Plane is responsible for user and data access management?',
    o: ['Unity Catalog', 'Web Application', 'Cloud Storage', 'Workflow Manager'],
    a: [0],
    x: 'Unity Catalog es el componente del control plane responsable de la gestión de acceso a usuarios y datos, incluyendo metastore, control de acceso y linaje de datos.',
  },
  {
    n: 41,
    d: 'TRO',
    m: 0,
    q: 'Why would you consider restarting a Databricks cluster?',
    o: [
      'To apply updates or changes to Spark configurations or cluster-level libraries',
      "When the cluster's Spark version needs to be downgraded for compatibility reasons",
      "To increase the storage capacity of the cluster's attached storage",
      'Restarting is required to apply software updates or patches to the cluster',
    ],
    a: [0],
    x: 'Reiniciar el cluster aplica cambios de configuración de Spark o de librerías a nivel de cluster y limpia posibles fugas de memoria o estados corruptos, sin ser necesario para parches de software ni para cambiar la capacidad de almacenamiento.',
  },
  {
    n: 42,
    d: 'TRO',
    m: 0,
    q: 'Your organization runs multiple data engineering projects simultaneously, each requiring different cluster configurations and sizes. How would Cluster Pools facilitate better resource utilization across multiple projects?',
    o: [
      'By enabling projects to share a common pool of instances, improving overall resource utilization',
      'By allowing each project to have its dedicated pool, preventing resource sharing',
      'By providing a fixed number of instances that can only be used by the highest priority project',
      "By locking specific instances to specific projects, based on the project's budget",
    ],
    a: [0],
    x: 'Los cluster pools permiten que varios proyectos compartan un mismo pool de instancias pre-calentadas, asignando dinámicamente los recursos disponibles en vez de reservar pools dedicados e infrautilizados por proyecto.',
  },
  {
    n: 43,
    d: 'TRA',
    m: 0,
    q: 'Which job uses a Gold table as its source?',
    o: [
      'A job that cleans data by removing malformatted records',
      'A job that enriches data by parsing its timestamps into a human-readable format',
      'A job that queries aggregated data designed to feed into a dashboard',
      'A job that ingests raw data from a streaming source into the Lakehouse',
      'A job that aggregates uncleaned data to create standard summary statistics',
    ],
    a: [2],
    x: 'Consultar datos ya agregados para alimentar un dashboard es el caso de uso típico que consume una tabla Gold como fuente; limpieza y enriquecimiento ocurren en Silver, e ingesta cruda produce Bronze.',
  },
  {
    n: 44,
    d: 'TRA',
    m: 0,
    q: 'In which of the following file formats is data from Delta Lake tables primarily stored?',
    o: ['Delta', 'JSON', 'A proprietary, optimized format specific to Databricks', 'Parquet', 'CSV'],
    a: [3],
    x: 'Delta Lake almacena sus datos en archivos Parquet (formato columnar abierto), añadiendo encima un transaction log para ACID, esquema y time travel; "Delta" no es en sí un formato de archivo, es la capa que envuelve a Parquet.',
  },
  {
    n: 45,
    d: 'GOV',
    m: 0,
    q: 'What level of access does a Metastore Admin have in the Databricks Unity Catalog?',
    o: [
      'They can manage users and groups as well as integrate with IdP',
      'They can create or drop, grant privileges on, and change the ownership of catalogs and other data objects',
      'They can only view metadata and user management settings',
      'They have full access to all data objects',
    ],
    a: [1],
    x: 'El Metastore Admin gestiona tareas de catálogo de datos: crear/eliminar objetos, conceder privilegios y cambiar la propiedad de catálogos y otros objetos, con un alcance centrado en gobernanza de datos, no en gestión de usuarios/IdP.',
  },
  {
    n: 46,
    d: 'P',
    m: 0,
    q: 'How can you execute Python and SQL code within the same Databricks notebook?',
    o: [
      'By installing a special library that allows cross-language execution',
      'By configuring the notebook settings to enable multi-language support before execution',
      'By writing all code in Python and using API calls to execute SQL queries',
      'By using the %python and %sql magic commands at the beginning of each cell to specify the language',
    ],
    a: [3],
    x: 'Los comandos mágicos `%python` y `%sql` al inicio de cada celda indican qué lenguaje usar en esa celda concreta, permitiendo mezclar lenguajes en un mismo notebook sin librerías ni configuración adicional.',
  },
  {
    n: 47,
    d: 'CICD',
    m: 0,
    q: 'A data engineer is evaluating whether to use the native versioning feature of Databricks Notebooks or manage their project with Databricks Repos. What is a benefit of Databricks Repos over the built-in versioning of Notebooks?',
    o: [
      'Databricks Repos allows for commenting on specific alterations',
      'Databricks Repos ensures continuous saving of work in progress',
      'Databricks Repos offers the option to go back to earlier versions of a notebook',
      'Databricks Repos enables working with multiple branches',
      'Databricks Repos operates entirely within the Databricks Lakehouse ecosystem',
    ],
    a: [3],
    x: 'La ventaja clave de Repos es el soporte de múltiples ramas (branching/merging al estilo Git), crucial para trabajo colaborativo en paralelo; el versionado nativo de Notebooks solo permite volver a versiones anteriores, sin ramas.',
  },
  {
    n: 48,
    d: 'P',
    m: 0,
    q: 'Which of the following can be used to simplify and unify siloed data architectures that are specialized for specific use cases?',
    o: ['All of these', 'Data lake', 'Data warehouse', 'Data lakehouse', 'None of these'],
    a: [3],
    x: 'El data lakehouse combina la capacidad de almacenamiento masivo de un data lake con las garantías de gestión y transacciones ACID de un data warehouse, unificando arquitecturas antes fragmentadas en un solo sistema.',
  },
  {
    n: 49,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has a Python variable table_name to use in a SQL query: `____(f"SELECT customer_id, spend FROM {table_name}")`. Which fills in the blank to complete the task?',
    o: ['spark.delta.sql', 'spark.sql', 'dbutils.sql', 'spark.delta.table', 'spark.table'],
    a: [1],
    x: '`spark.sql()` ejecuta directamente la cadena SQL construida con el f-string de Python; `dbutils.sql` y `spark.delta.sql/table` no son métodos válidos para este propósito.',
  },
  {
    n: 50,
    d: 'TRA',
    m: 0,
    q: 'What is the primary use of the PIVOT clause in Spark SQL?',
    o: [
      'To split a single column into multiple columns based on column values',
      'To perform aggregation and convert rows into columns, transforming data from a long format to a wide format',
      'To filter rows based on a specified condition',
      'To merge multiple rows into a single row by creating a wider table with more columns',
    ],
    a: [1],
    x: '`PIVOT` agrega datos y rota filas en columnas, transformando la tabla de un formato largo a uno ancho, típico para tablas resumen y reporting.',
  },
  {
    n: 51,
    d: 'TRA',
    m: 0,
    q: "A team automates a Spark SQL query compiling monthly sales data from a table named monthly_sales_YYYYMM. Each execution must target the previous month's table (e.g., in March 2023, it should target monthly_sales_202302). Which strategy should the team employ?",
    o: [
      'Develop a PySpark script that calculates the date for the previous month and dynamically inserts this date into the table name within the query string',
      'Revise the database design to consolidate sales data into a single table with a month column, avoiding the necessity for separate monthly tables',
      "Propose altering the query's execution frequency to quarterly, thereby reducing the need for monthly table name updates",
      "Manually update the table name in the query to reflect the previous month's data before executing it each time",
    ],
    a: [0],
    x: 'Un script PySpark que calcule el mes anterior con `datetime` y lo inserte dinámicamente en el nombre de la tabla automatiza el proceso sin intervención manual ni rediseño del esquema existente.',
  },
  {
    n: 52,
    d: 'TRA',
    m: 0,
    q: 'Which statement correctly describes the difference between a Temporary View and a Global Temporary View?',
    o: [
      'There is no difference; both have the same scope and visibility across Spark sessions',
      'Temporary Views can be created on disk, making them persistent across Spark sessions, whereas Global Temporary Views are in-memory only and lost after the session ends',
      'Temporary Views are stored in the Hive metastore and are accessible across all sessions, whereas Global Temporary Views are session-scoped and not stored in the Hive metastore',
      'Temporary Views are session-scoped and cannot be accessed by other Spark sessions, whereas Global Temporary Views are accessible across multiple Spark sessions within the same Spark application',
    ],
    a: [3],
    x: 'Las vistas temporales solo son visibles en la sesión que las creó; las vistas temporales globales son visibles en todas las sesiones de la misma aplicación Spark, aunque tampoco se almacenan en el metastore de Hive.',
  },
  {
    n: 53,
    d: 'TRA',
    m: 0,
    q: 'A data engineer tries to remove a Spark SQL table named my_table, intending to erase both its metadata and its data, by executing `DROP TABLE IF EXISTS my_table;`. Afterward, my_table no longer appears in the table list, but the data files remain. What explains this?',
    o: [
      "The table's data exceeded 10 GB",
      'The table was specified without a location',
      "The table's data was under 10 GB",
      'The table was a managed table',
      'The table was set up as an external table',
    ],
    a: [4],
    x: 'Al hacer DROP de una tabla externa, Spark solo elimina los metadatos y deja intactos los archivos de datos en su ubicación original; una tabla managed, en cambio, sí borraría ambos.',
  },
];

export const exam15: RawQuestion[] = [
  {
    n: 1,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to make a new table from different data sources and change the data a bit, in one step without changing the original data. Which SQL command is best for making a new table and changing the data at the same time?',
    o: ['CREATE TABLE AS SELECT (CTAS)', 'MERGE INTO', 'UPDATE', 'INSERT INTO', 'ALTER TABLE'],
    a: [0],
    x: 'CTAS crea una tabla nueva a partir del resultado de un `SELECT`, permitiendo aplicar transformaciones en el mismo paso sin tocar los datos originales.',
  },
  {
    n: 2,
    d: 'TRO',
    m: 0,
    q: 'Your team is working on a project with a high volume of data transformations and model training tasks. To optimize resource allocation and improve efficiency, which Databricks feature should you use to isolate these tasks in separate environments?',
    o: [
      'Implement isolated clusters with different configurations for each task',
      'Utilize Databricks Workspace to organize tasks by project folders',
      'Use Databricks Repos to version control the entire project',
      'Create separate notebooks for data transformations and model training',
    ],
    a: [0],
    x: 'Clusters aislados con configuraciones distintas por tarea (p. ej. más cómputo para entrenar modelos, menos para transformaciones ETL) evitan la contención de recursos; organizar en carpetas, usar Repos o separar notebooks no aísla el cómputo en sí.',
  },
  {
    n: 3,
    d: 'JOBS',
    m: 0,
    q: 'What is a benefit of using multiple tasks in Databricks Jobs?',
    o: [
      'Limits the scope of data processing',
      'Increases the complexity of jobs',
      'Allows for sequential task execution',
      'Reduces the overall execution time',
    ],
    a: [2],
    x: 'Múltiples tareas encadenadas con dependencias permiten ejecutar un flujo en orden secuencial (o paralelo), clave para orquestar pipelines ETL o de ML; no garantiza menor tiempo de ejecución ni limita el alcance.',
  },
  {
    n: 4,
    d: 'P',
    m: 0,
    q: 'Your organization is transitioning from a traditional data lake to a data lakehouse architecture to improve data analytics capabilities. Which of the following benefits is a primary reason for this transition?',
    o: [
      'To reduce the amount of data stored',
      'To eliminate the need for data processing',
      'To increase the complexity of data queries',
      'To enhance data quality and governance with schema enforcement',
    ],
    a: [3],
    x: 'El lakehouse combina la flexibilidad del data lake con la fiabilidad y gobernanza (enforcement de esquema) propias de un warehouse, mejorando calidad y consistencia de los datos; no reduce el volumen almacenado ni elimina el procesamiento.',
  },
  {
    n: 5,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to run the last part of a Python script only if the variable day_of_week is 1 and review_period is True. Which line of code should they use to start this part of the script?',
    o: [
      '`if day_of_week == 1 and review_period == "True"`',
      '`if day_of_week = 1 and review_period`',
      '`if day_of_week = 1 & review_period: = "True"`',
      '`if day_of_week == 1 and review_period`',
      '`if day_of_week = 1 and review_period = "True"`',
    ],
    a: [3],
    x: '`==` es el operador de comparación correcto en Python (no `=`), y como `review_period` ya es booleano, se comprueba directamente sin compararlo con la cadena `"True"`.',
  },
  {
    n: 6,
    d: 'ING',
    m: 0,
    q: 'In SQL queries that involve file data, how are text and binary files processed differently?',
    o: [
      'Binary files generate a column with a string value for each line, whereas text files have columns for path, modification time, length, and content',
      'Text and binary files both generate columns for path, modification time, length, and content',
      'Text files generate a column with a string value for each line, while binary files have columns for path, modification time, length, and content',
      'Text files create a single column for each byte of data, whereas binary files create a column for each bit',
    ],
    a: [2],
    x: 'Los archivos de texto se leen línea a línea, generando una única columna `value` con cada línea como cadena; los archivos binarios generan columnas de metadatos (`path`, `modificationTime`, `length`, `content`).',
  },
  {
    n: 7,
    d: 'TRA',
    m: 0,
    q: 'A table needs the format `employeeId | startDate | avgRating` (e.g. `a1 | 2009-01-06 | 5.5`). Which code block uses SQL DDL to create an empty Delta table in this format regardless of whether a table already exists with this name?',
    o: [
      '`CREATE OR REPLACE TABLE table_name (employeeId STRING, startDate DATE, avgRating FLOAT)`',
      '`CREATE TABLE IF NOT EXISTS table_name (employeeId STRING, startDate DATE, avgRating FLOAT)`',
      '`CREATE TABLE table_name AS SELECT employeeId STRING, startDate DATE, avgRating FLOAT`',
      '`CREATE OR REPLACE TABLE table_name AS SELECT employeeId STRING, startDate DATE, avgRating FLOAT USING DELTA`',
      '`CREATE OR REPLACE TABLE table_name WITH COLUMNS (employeeId STRING, startDate DATE, avgRating FLOAT) USING DELTA`',
    ],
    a: [0],
    x: '`CREATE OR REPLACE TABLE` reemplaza la tabla si ya existe con esa definición vacía y ese esquema; `IF NOT EXISTS` no la sobrescribiría, y `AS SELECT`/`WITH COLUMNS` no son sintaxis válidas para este propósito.',
  },
  {
    n: 8,
    d: 'JOBS',
    m: 0,
    q: "How does Databricks' workflow feature 'Simple Workflow Authoring' cater to teams?",
    o: [
      'It only allows workflow editing in command-line interfaces',
      'It requires advanced programming skills for all data team members',
      'It is exclusively available for users with administrative privileges',
      'It provides a point-and-click authoring experience accessible to all team members',
    ],
    a: [3],
    x: 'Ofrece una experiencia visual de apuntar y hacer clic, accesible a todo el equipo sin requerir programación avanzada ni privilegios de administrador.',
  },
  {
    n: 9,
    d: 'TRA',
    m: 0,
    q: 'Which command allows for data to be inserted into a Delta table while ensuring duplicates are not written?',
    o: ['MERGE', 'INSERT', 'APPEND', 'UPDATE', 'DROP'],
    a: [0],
    x: '`MERGE` permite condicionar la inserción a que no exista coincidencia previa, evitando duplicados; `INSERT`/`APPEND` simplemente añaden filas sin verificar duplicados.',
  },
  {
    n: 10,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to create a UDF to convert temperatures from Celsius to Fahrenheit for a dataset with global weather information. What should they do within a Databricks notebook?',
    o: [
      'Export the dataset to a CSV file, convert the temperatures using an external tool, and then re-import the dataset into Databricks',
      'Use a built-in Spark SQL function that automatically converts temperatures from Celsius to Fahrenheit',
      'Directly modify the dataset in the data lake to convert all temperatures to Fahrenheit before importing it into Databricks',
      'Create a UDF in Databricks that applies the conversion formula and use it in a Spark SQL query to transform the temperature column',
    ],
    a: [3],
    x: 'No existe función nativa para esta conversión; hay que definir una UDF con la fórmula (`C * 9/5 + 32`) y aplicarla en la query, manteniendo la transformación dentro de Databricks.',
  },
  {
    n: 11,
    d: 'TRA',
    m: 0,
    q: "A data engineering team automates a Spark SQL query on a table named monthly_sales_YYYYMM. The automation must ensure each execution targets the previous month's table (e.g., in March 2023, target monthly_sales_202302). Which strategy should they employ?",
    o: [
      'Develop a PySpark script that calculates the date for the previous month and dynamically inserts this date into the table name within the query string',
      'Revise the database design to consolidate sales data into a single table with a month column, avoiding the necessity for separate monthly tables',
      "Propose altering the query's execution frequency to quarterly, thereby reducing the need for monthly table name updates",
      "Manually update the table name in the query to reflect the previous month's data before executing it each time",
    ],
    a: [0],
    x: 'Un script PySpark que calcula el mes anterior con `datetime` e inserta ese valor dinámicamente en el nombre de la tabla automatiza el proceso sin intervención manual.',
  },
  {
    n: 12,
    d: 'JOBS',
    m: 0,
    q: 'You have a scheduled weekly job that compiles analytics reports for Monday morning meetings. Failures must be addressed promptly, and stakeholders kept updated. How should you set up the job to ensure stakeholders are promptly informed?',
    o: [
      "Configure the job to send completion alerts to the stakeholders' email addresses",
      'Manually check the job status every Monday and send emails to stakeholders',
      'Disable all alerts to avoid flooding stakeholders with notifications',
      'Schedule the job run for Monday morning and hope it completes on time',
    ],
    a: [0],
    x: 'Configurar alertas de finalización dirigidas a los emails de los stakeholders automatiza la notificación del estado del job sin depender de comprobaciones manuales.',
  },
  {
    n: 13,
    d: 'TRA',
    m: 0,
    q: 'Given the task of dynamically generating a table name in Python using variables for region, store, and year, where the desired format is like "la200_sales_2022" for region="la", store="200", year="2022", which Python command correctly constructs this table name?',
    o: [
      '`f"{region}+{store}+_sales_+{year}"`',
      '`f"{region}{store}_sales_{year}"`',
      '`"{region}+{store}+_sales_+{year}"`',
      '`"{region}{store}_sales_{year}"`',
    ],
    a: [1],
    x: 'Un f-string con las variables directamente concatenadas (`f"{region}{store}_sales_{year}"`) produce `"la200_sales_2022"`; sin el prefijo `f` no hay interpolación, y usar `+` como carácter literal rompe el formato.',
  },
  {
    n: 14,
    d: 'GOV',
    m: 0,
    q: 'What functionality does Data Access Audit provide in a data governance framework?',
    o: [
      'It restricts any access to the data',
      'It allows users to modify data without tracking',
      'It provides a mechanism to capture and record all access to data',
      'It hides data access details from administrators',
    ],
    a: [2],
    x: 'El audit de acceso a datos registra quién accedió, cuándo y cómo, siendo clave para cumplimiento normativo y detección de actividad sospechosa; no bloquea el acceso ni oculta información a los administradores.',
  },
  {
    n: 15,
    d: 'TRA',
    m: 0,
    q: "A data engineer creates a temporary view in their current Spark session for preliminary exploration, then tries to access it from a different Spark session. Why can't they access the temporary view from the other session?",
    o: [
      'The network connection between the two Spark sessions was lost, preventing access to the temporary view',
      'Temporary views in Spark SQL are session-scoped and cannot be accessed from different sessions',
      'The data engineer did not correctly create the temporary view, resulting in an error when trying to access it from another session',
      'Temporary views require special permissions to be accessed from different sessions',
      'Temporary views are automatically deleted after a certain period, and the time limit was exceeded',
    ],
    a: [1],
    x: 'Las vistas temporales existen solo durante la sesión que las creó; para compartirlas entre sesiones haría falta una vista temporal global.',
  },
  {
    n: 16,
    d: 'TRO',
    m: 0,
    q: 'A data engineer notices that one of the notebooks within a Job, which runs two notebooks as separate tasks, is performing slowly. Which approach can a tech lead take to determine why the notebook is running slowly?',
    o: [
      'Navigate to the Tasks tab in the Jobs UI and click on the active run to review the processing notebook',
      'Navigate to the Runs tab in the Jobs UI and click on the active run to review the processing notebook',
      'There is no way to determine why a Job task is running slowly',
      'Navigate to the Tasks tab in the Jobs UI to immediately review the processing notebook',
      'Navigate to the Runs tab in the Jobs UI to immediately review the processing notebook',
    ],
    a: [1],
    x: 'La pestaña Runs, al entrar en la ejecución activa, permite revisar logs y métricas de rendimiento del notebook; la pestaña Tasks solo muestra configuración, no detalles de ejecución.',
  },
  {
    n: 17,
    d: 'GOV',
    m: 0,
    q: "What is the role of Data Lineage in Databricks' Data Governance?",
    o: [
      'To convert data into different formats automatically',
      'To replicate data across multiple cloud providers',
      'To compress data for efficient storage',
      'To capture upstream sources and downstream consumers of data',
    ],
    a: [3],
    x: 'El linaje de datos da visibilidad sobre el origen y destino de los datos y sus transformaciones, esencial para auditoría, cumplimiento y troubleshooting.',
  },
  {
    n: 18,
    d: 'TRA',
    m: 0,
    q: 'A data engineer tries to remove a Spark SQL table my_table with `DROP TABLE IF EXISTS my_table;`, intending to erase both metadata and data. Afterward, my_table no longer appears in the table list, but the data files remain. What explains this?',
    o: [
      "The table's data exceeded 10 GB",
      'The table was a managed table',
      'The table was set up as an external table',
      "The table's data was under 10 GB",
      'The table was specified without a location',
    ],
    a: [2],
    x: 'Al hacer DROP de una tabla externa, Spark solo elimina los metadatos y deja intactos los archivos de datos en su ubicación original.',
  },
  {
    n: 19,
    d: 'TRO',
    m: 0,
    q: 'Your team is working on a project with a high volume of data transformations and model training tasks. Which Databricks feature should you use to isolate these tasks in separate environments?',
    o: [
      'Implement isolated clusters with different configurations for each task',
      'Create separate notebooks for data transformations and model training',
      'Use Databricks Repos to version control the entire project',
      'Utilize Databricks Workspace to organize tasks by project folders',
    ],
    a: [0],
    x: 'Clusters aislados con configuraciones distintas por tarea evitan que workloads intensivos interfieran entre sí, optimizando la utilización de recursos por tipo de carga.',
  },
  {
    n: 20,
    d: 'TRA',
    m: 0,
    q: 'What is the purpose of the explode function in SQL?',
    o: [
      'To create a backup of the database',
      'To expand the elements of an array field into separate rows',
      'To delete elements from an array field',
      'To combine multiple rows into a single array field',
    ],
    a: [1],
    x: '`explode` toma un array (o map) y genera una fila independiente por cada elemento, típico para aplanar estructuras anidadas en análisis de datos.',
  },
  {
    n: 21,
    d: 'TRO',
    m: 0,
    q: 'How does setting up an alert for a failed task in Databricks benefit you?',
    o: [
      'It keeps you informed so you can take immediate action',
      'It increases the execution speed of future tasks',
      'It automatically fixes any issues with the task',
      'It prevents tasks from failing in the future',
    ],
    a: [0],
    x: 'Una alerta ante un fallo notifica de inmediato para poder actuar rápido, minimizando el tiempo de inactividad; no arregla el problema automáticamente ni evita fallos futuros.',
  },
  {
    n: 22,
    d: 'TRO',
    m: 0,
    q: 'What is a benefit of reusing clusters for multiple tasks in a workflow?',
    o: [
      'It increases the complexity of the task',
      'It eliminates the need for monitoring tasks',
      'It allows for unlimited task execution',
      'It reduces cost and startup time',
    ],
    a: [3],
    x: 'Reutilizar un cluster ya activo entre varias tareas evita el coste y el tiempo de aprovisionar uno nuevo desde cero para cada una.',
  },
  {
    n: 23,
    d: 'TRA',
    m: 0,
    q: 'What function in SQL is used to ensure that the array of values returned is composed of unique elements for each group of rows?',
    o: ['array_distinct', 'collect_set', 'array_combine', 'collect_all'],
    a: [1],
    x: '`collect_set` agrupa valores de una columna en un array eliminando duplicados por cada grupo de un `GROUP BY`; `array_distinct` opera sobre un array ya existente, no agrega filas.',
  },
  {
    n: 24,
    d: 'JOBS',
    m: 0,
    q: 'Why might you set up a retry policy for Databricks jobs?',
    o: [
      'To automatically attempt to rerun tasks that fail, improving reliability',
      'To ensure that a job runs continuously without stopping',
      'To comply with data processing regulations',
      "To increase the job's execution time for detailed analysis",
    ],
    a: [0],
    x: 'Una retry policy relanza automáticamente una tarea que falla, según la política configurada, reduciendo intervención manual y aumentando la fiabilidad del pipeline.',
  },
  {
    n: 25,
    d: 'TRA',
    m: 0,
    q: 'In which of the following scenarios should a data engineer use the MERGE INTO command instead of the INSERT INTO command?',
    o: [
      'When the target table is an external table',
      'When the location of the data needs to be changed',
      'When the target table cannot contain duplicate records',
      'When the source table can be deleted',
      'When the source is not a Delta table',
    ],
    a: [2],
    x: '`MERGE INTO` permite condicionar inserciones/actualizaciones para evitar duplicados en la tabla destino, algo que `INSERT INTO` no controla.',
  },
  {
    n: 26,
    d: 'TRA',
    m: 0,
    q: 'Which of the following commands will return the number of null values in the member_id column?',
    o: [
      '`SELECT null(member_id) FROM my_table;`',
      '`SELECT count(member_id) - count_null(member_id) FROM my_table;`',
      '`SELECT count_if(member_id IS NULL) FROM my_table;`',
      '`SELECT count(member_id) FROM my_table;`',
      '`SELECT count_null(member_id) FROM my_table;`',
    ],
    a: [2],
    x: '`count_if(condición)` cuenta cuántas filas cumplen una condición booleana; `count_if(member_id IS NULL)` cuenta directamente los nulos. `count_null` no es una función válida.',
  },
  {
    n: 27,
    d: 'GOV',
    m: 0,
    q: 'Which of the following statements is true about the Workspace Admin role in the Databricks Unity Catalog?',
    o: [
      'Workspace Admins cannot manage user permissions or roles',
      'Workspace Admins are responsible for creating and deleting metastores only',
      'Workspace Admins have the ability to manage permissions on workspace assets and change job ownership',
      'Workspace Admins are limited to data discovery and cannot alter permissions',
    ],
    a: [2],
    x: 'Los Workspace Admins gestionan permisos sobre activos del workspace (notebooks, clusters, jobs) y pueden reasignar la propiedad de jobs, dentro de su alcance de gobernanza a nivel de workspace.',
  },
  {
    n: 28,
    d: 'P',
    m: 0,
    q: 'How can you run one Databricks notebook from within another notebook?',
    o: [
      'By preceding each cell in the target notebook with %run to execute its code',
      'By using the %run command followed by the path to the notebook stored in DBFS',
      "By appending the %run command with the notebook's URL",
      'By using the %run command followed by the relative or absolute path to the notebook within the Databricks workspace',
    ],
    a: [3],
    x: '`%run` ejecuta otro notebook indicando su ruta relativa o absoluta dentro del workspace de Databricks, dejando disponibles sus funciones y variables en la sesión actual.',
  },
  {
    n: 29,
    d: 'TRO',
    m: 0,
    q: 'How does the VACUUM command manage deletes in Delta Lake tables?',
    o: [
      'By marking deleted records as inactive for immediate purging',
      'By encrypting deleted records for security purposes',
      'By immediately removing all deleted records from the table',
      'By cleaning up older, unnecessary files after a retention period',
    ],
    a: [3],
    x: '`VACUUM` elimina archivos de datos que ya no son necesarios (no referenciados por ninguna versión dentro de la ventana de retención) tras un periodo determinado, sin borrar registros de forma inmediata.',
  },
  {
    n: 30,
    d: 'GOV',
    m: 0,
    q: "A newly formed data engineering team 'team' needs complete access to the 'customers' database for an ELT project. Which command grants the team full permissions on the database?",
    o: [
      '`GRANT SELECT CREATE MODIFY USAGE PRIVILEGES ON DATABASE customers TO team;`',
      '`GRANT SELECT PRIVILEGES ON DATABASE customers TO teams;`',
      '`GRANT ALL PRIVILEGES ON DATABASE customers TO team;`',
      '`GRANT USAGE ON DATABASE customers TO team;`',
      '`GRANT ALL PRIVILEGES ON DATABASE team TO customers;`',
    ],
    a: [2],
    x: '`GRANT ALL PRIVILEGES ON DATABASE customers TO team` concede de una vez todos los permisos necesarios (lectura, escritura, gestión de estructura y acceso), cumpliendo el requisito de acceso completo.',
  },
  {
    n: 31,
    d: 'P',
    m: 0,
    q: "What is primarily managed within the control plane of Databricks' architecture?",
    o: [
      'Physical hardware and network configuration',
      'Development of machine learning models',
      'User authentication and access control',
      'Data storage and computation tasks',
    ],
    a: [2],
    x: 'El control plane gestiona servicios de backend como autenticación de usuarios y control de acceso, además de metadatos de notebooks y jobs; el cómputo y almacenamiento de datos ocurre en el data plane.',
  },
  {
    n: 32,
    d: 'GOV',
    m: 0,
    q: 'In the Databricks Unity Catalog, which role is primarily responsible for managing underlying cloud resources such as storage accounts/buckets and IAM roles/service principals/managed identities?',
    o: ['Account Admin', 'Cloud Admin', 'Metastore Admin', 'Data Owner'],
    a: [1],
    x: 'El rol Cloud Admin gestiona la infraestructura cloud subyacente (cuentas de almacenamiento, roles IAM, identidades gestionadas) que Unity Catalog necesita para funcionar de forma segura, algo fuera del alcance del Metastore Admin o el Account Admin.',
  },
  {
    n: 33,
    d: 'GOV',
    m: 0,
    q: 'How can a data engineer find out who owns "new_table" if they lack the necessary permissions and don\'t know the table\'s owner?',
    o: [
      "Review the Owner field in the table's page in Data Explorer",
      "Review the Permissions tab in the table's page in Data Explorer",
      'All of these options can be used to identify the owner of the table',
      'There is no way to identify the owner of the table',
      "Review the Owner field in the table's page in the cloud storage solution",
    ],
    a: [0],
    x: 'El campo Owner siempre aparece visible en la página de la tabla en Data Explorer, incluso sin permisos para consultarla; la propiedad de la tabla se rastrea en el metastore de Databricks, no en el almacenamiento cloud.',
  },
  {
    n: 34,
    d: 'TRA',
    m: 0,
    q: 'Which of the following queries is performing a streaming hop from raw data to a Bronze table?',
    o: [
      '```\nspark.readStream.load(rawSalesLocation)\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("newSales")\n```',
      '```\nspark.table("sales").groupBy("store").agg(sum("sales"))\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("complete").table("newSales")\n```',
      '```\nspark.table("sales").withColumn("avgPrice", col("sales") / col("units"))\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("newSales")\n```',
      '```\nspark.table("sales").filter(col("units") > 0)\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("newSales")\n```',
      '```\nspark.read.load(rawSalesLocation)\n  .write.mode("append").table("newSales")\n```',
    ],
    a: [0],
    x: 'Leer con `readStream` datos crudos y escribirlos con `writeStream` en modo append es la ingesta streaming clásica de raw→Bronze; las demás opciones agregan, transforman o usan batch en vez de streaming.',
  },
  {
    n: 35,
    d: 'JOBS',
    m: 0,
    q: 'A data engineer needs to apply a complex run schedule from one Job to others without manually setting each one. Which tool allows them to define and apply this schedule programmatically?',
    o: [
      'datetime',
      'There is no way to represent and submit this information programmatically',
      'pyspark.sql.types.TimestampType',
      'Cron syntax',
      'pyspark.sql.types.DateType',
    ],
    a: [3],
    x: 'La sintaxis cron permite expresar programaciones complejas de forma reutilizable y programática; los tipos de datos de PySpark son para columnas, no para programar jobs.',
  },
  {
    n: 36,
    d: 'TRA',
    m: 0,
    q: 'A data engineer aims to implement custom logic on the string column city in the location table. Which code snippet correctly defines this SQL UDF?',
    o: [
      '`CREATE UDF combine_california (city STRING) RETURN CASE WHEN city = "San Francisco" THEN "California" ELSE city END;`',
      '`CREATE FUNCTION combine_california (city STRING) RETURNS STRING RETURN CASE WHEN city = "San Francisco" THEN "California" ELSE city END;`',
    ],
    a: [1],
    x: 'La sintaxis válida requiere `CREATE FUNCTION` (no `CREATE UDF`, que no existe) junto con `RETURNS STRING` y el `RETURN` antes del `CASE`.',
  },
  {
    n: 37,
    d: 'ING',
    m: 0,
    q: 'When creating a table in Databricks using a JDBC connection, which SQL command correctly attempts to create a new table and throws an error if the table with that name already exists?',
    o: [
      "`INSERT INTO new_table SELECT * FROM jdbc('jdbc:mysql://database.example.com:3306/mydb', 'existing_table', 'username', 'password')`",
      "`CREATE TABLE new_table AS SELECT * FROM jdbc('jdbc:mysql://database.example.com:3306/mydb', 'table_name', 'username', 'password')`",
      "`CREATE TABLE new_table USING jdbc OPTIONS (url 'jdbc:mysql://database.example.com:3306/mydb', dbtable 'new_table', user 'username', password 'password')`",
      "`CREATE TABLE IF NOT EXISTS new_table USING jdbc OPTIONS (url 'jdbc:mysql://database.example.com:3306/mydb', dbtable 'existing_table', user 'username', password 'password')`",
    ],
    a: [2],
    x: '`CREATE TABLE` (sin `IF NOT EXISTS`) usando el conector JDBC falla si la tabla ya existe, que es justo el comportamiento pedido; `IF NOT EXISTS` evitaría ese error, e `INSERT INTO`/`CREATE TABLE AS SELECT` no garantizan el fallo requerido.',
  },
];

export const exam16: RawQuestion[] = [
  {
    n: 1,
    d: 'P',
    m: 0,
    q: 'How does Lakehouse replace the dependency on using Data lakes and Data warehouses in a Data and Analytics solution?',
    o: [
      'Open, direct access to data stored in standard data formats',
      'Supports ACID transactions',
      'Supports BI and Machine learning workloads',
      'Support for end-to-end streaming and batch workloads',
      'All the above',
    ],
    a: [4],
    x: 'El lakehouse combina las ventajas de un data lake y un data warehouse: formatos abiertos, transacciones ACID, soporte de BI/ML y de batch/streaming a la vez, todas las opciones son ciertas simultáneamente.',
  },
  {
    n: 2,
    d: 'TRO',
    m: 0,
    q: 'Which of the following is true when building a Databricks SQL dashboard?',
    o: [
      'A dashboard can only use results from one query',
      'Only one visualization can be developed with one query result',
      'A dashboard can only connect to one schema/Database',
      'More than one visualization can be developed using a single query result',
      'A dashboard can only have one refresh schedule',
    ],
    a: [3],
    x: 'Desde el editor de queries, la pestaña +Add visualization permite crear múltiples visualizaciones a partir del resultado de una misma query.',
  },
  {
    n: 3,
    d: 'ING',
    m: 0,
    q: 'Which of the following is used by Auto Loader to load data incrementally?',
    o: ['DEEP CLONE', 'Multi-hop architecture', 'COPY INTO', 'Spark Structured Streaming', 'Databricks SQL'],
    a: [3],
    x: 'Auto Loader se apoya en Spark Structured Streaming (mediante la fuente `cloudFiles`) para procesar incrementalmente los archivos nuevos que llegan a un almacenamiento en la nube.',
  },
  {
    n: 4,
    d: 'TRO',
    m: 0,
    q: 'The operations team wants to monitor product sales and set up an email alert when the number of units sold increases by more than 10,000 units, checked every 5 minutes. What are the steps to configure this in Databricks SQL?',
    o: [
      'Python, Job, SQL Cluster, email address',
      'SQL, Alert, Refresh, email address',
      'SQL, Job, SQL Cluster, email address',
      'SQL, Job, Refresh, email address',
      'Python, Job, Refresh, email address',
    ],
    a: [1],
    x: 'Se necesita una query SQL que calcule las unidades vendidas, una Alert que evalúe la condición, un Refresh cada 5 minutos y un email como destino; no hace falta Python ni un Job independiente.',
  },
  {
    n: 5,
    d: 'GOV',
    m: 0,
    q: 'A new user without access to the catalog or schema requests access to the customer table in the sales schema, which contains sensitive information. You create a view excluding sensitive columns and grant `GRANT SELECT ON view_name TO user@company.com`, but the user gets a "view does not exist" error. What is the issue and how do you fix it?',
    o: [
      'User requires SELECT on the underlying table',
      'User requires to be put in a special group that has access to PII data',
      'User has to be the owner of the view',
      'User requires USAGE privilege on the sales schema',
      'User needs ADMIN privilege on the view',
    ],
    a: [3],
    x: 'En Unity Catalog, además del `SELECT` sobre la vista, el usuario necesita el privilegio `USAGE` sobre el esquema contenedor; sin él, la vista aparece como inexistente. Se soluciona con `GRANT USAGE ON SCHEMA sales TO user@company.com`.',
  },
  {
    n: 6,
    d: 'TRO',
    m: 0,
    q: 'You have a notebook that generates a summary dataset for reporting, scheduled using a job cluster, but it takes an average of 8 minutes to start the cluster. What feature can be used to start the cluster in a timely fashion?',
    o: [
      'Set up an additional job to run ahead of the actual job so the cluster is already running when it starts',
      'Use the Databricks cluster pools feature to reduce the startup time',
      'Use Databricks Premium edition instead of Databricks standard edition',
      'Pin the cluster in the cluster UI page so it is always available to the jobs',
      'Disable auto termination so the cluster is always running',
    ],
    a: [1],
    x: 'Los cluster pools mantienen instancias pre-calentadas listas para asignarse a un job cluster, reduciendo drásticamente el tiempo de arranque frente a aprovisionar una VM nueva.',
  },
  {
    n: 7,
    d: 'GOV',
    m: 0,
    q: 'Which of the following tools provides Data Access control, Access Audit, Data Lineage, and Data discovery?',
    o: ['DELTA LIVE Pipelines', 'Unity Catalog', 'Data Governance', 'DELTA lake', 'Lakehouse'],
    a: [1],
    x: 'Unity Catalog es la solución de gobernanza unificada de Databricks: ofrece control de acceso granular, auditoría, linaje y descubrimiento de datos en un solo lugar.',
  },
  {
    n: 8,
    d: 'GOV',
    m: 0,
    q: 'How do you access or use tables in the Unity Catalog?',
    o: [
      'schema_name.table_name',
      'schema_name.catalog_name.table_name',
      'catalog_name.table_name',
      'catalog_name.database_name.schema_name.table_name',
      'catalog_name.schema_name.table_name',
    ],
    a: [4],
    x: 'Unity Catalog usa un espacio de nombres de tres niveles: catálogo → esquema → tabla (`catalog_name.schema_name.table_name`); no existe un nivel adicional de "database_name".',
  },
  {
    n: 9,
    d: 'GOV',
    m: 0,
    q: 'The data engineering team needs to share data with the data science team, and both teams use different workspaces in the same organization. Which technique simplifies sharing data across workspaces?',
    o: [
      'Data Sharing',
      'Unity Catalog',
      'DELTA lake',
      'Use a single storage location',
      'DELTA LIVE Pipelines',
    ],
    a: [1],
    x: 'Unity Catalog permite gestionar de forma centralizada el acceso a tablas y vistas entre distintos workspaces de la misma cuenta, sin duplicar datos ni gestionar permisos por separado en cada uno.',
  },
  {
    n: 10,
    d: 'P',
    m: 0,
    q: 'Where are interactive notebook results stored in the Databricks product architecture?',
    o: [
      'Data plane',
      'Control plane',
      'Data and Control plane',
      'JDBC data source',
      'Databricks web application',
    ],
    a: [2],
    x: 'El data plane ejecuta el código y genera los resultados; el control plane almacena metadatos y los resultados para mostrarlos en la interfaz web. Ambos planos participan en el flujo completo.',
  },
  {
    n: 11,
    d: 'P',
    m: 0,
    q: 'Which of the following statements is true about a lakehouse?',
    o: [
      'Lakehouse only supports Machine learning workloads and Data warehouses support BI workloads',
      'Lakehouse only supports end-to-end streaming workloads and Data warehouses support Batch workloads',
      'Lakehouse does not support ACID',
      'Lakehouse do not support SQL',
      'Lakehouse supports Transactions',
    ],
    a: [4],
    x: 'El lakehouse (vía Delta Lake) soporta transacciones ACID, garantizando consistencia con lecturas/escrituras concurrentes; además soporta SQL, BI, ML, batch y streaming, contradiciendo las demás opciones.',
  },
  {
    n: 12,
    d: 'P',
    m: 0,
    q: 'Which of the following locations in the Databricks product architecture hosts the notebooks and jobs?',
    o: [
      'Data plane',
      'Control plane',
      'Databricks Filesystem',
      'JDBC data source',
      'Databricks web application',
    ],
    a: [1],
    x: 'El control plane aloja notebooks, jobs y metadatos, gestionando la interfaz de usuario y la orquestación; el data plane se encarga de la ejecución/procesamiento real.',
  },
  {
    n: 13,
    d: 'JOBS',
    m: 0,
    q: "A dataset defined with Delta Live Tables includes an expectations clause: `CONSTRAINT valid_timestamp EXPECT (timestamp > '2020-01-01') ON VIOLATION FAIL`. What is the expected behavior when a batch containing violating data is processed?",
    o: [
      'Records that violate the expectation are added to the target dataset and recorded as invalid in the event log',
      'Records that violate the expectation are dropped from the target dataset and recorded as invalid in the event log',
      'Records that violate the expectation cause the job to fail',
      'Records that violate the expectation are added to the target dataset and flagged as invalid in a field added to the target dataset',
      'Records that violate the expectation are dropped from the target dataset and loaded into a quarantine table',
    ],
    a: [2],
    x: '`ON VIOLATION FAIL` detiene inmediatamente el pipeline en cuanto aparece un registro que incumple la expectation, sin escribir nada en el dataset destino; es la acción más estricta de las tres disponibles (WARN, DROP, FAIL).',
  },
  {
    n: 14,
    d: 'TRA',
    m: 0,
    q: 'Which of the following SQL commands can be used to insert, update, or delete rows based on a condition to check if row(s) exist?',
    o: [
      'MERGE INTO table_name',
      'COPY INTO table_name',
      'UPDATE table_name',
      'INSERT INTO OVERWRITE table_name',
      'INSERT IF EXISTS table_name',
    ],
    a: [0],
    x: '`MERGE INTO` es el único comando que permite condicionar insert, update y delete según si existe coincidencia con la tabla origen, en una sola operación atómica.',
  },
  {
    n: 15,
    d: 'TRA',
    m: 0,
    q: "While investigating a data issue, you realize a process accidentally updated a table, and you want to query yesterday's version of the data to review the prior state. What is the best way to query historical data for this analysis?",
    o: [
      "`SELECT * FROM TIME_TRAVEL(table_name) WHERE time_stamp = 'timestamp'`",
      '`TIME_TRAVEL FROM table_name WHERE time_stamp = date_sub(current_date(), 1)`',
      '`SELECT * FROM table_name TIMESTAMP AS OF date_sub(current_date(), 1)`',
      '`DESCRIBE HISTORY table_name AS OF date_sub(current_date(), 1)`',
      '`SHOW HISTORY table_name AS OF date_sub(current_date(), 1)`',
    ],
    a: [2],
    x: '`TIMESTAMP AS OF` es la sintaxis correcta de time travel para consultar una tabla tal como estaba en un momento concreto; `TIME_TRAVEL` no es una función válida, y `DESCRIBE/SHOW HISTORY` solo muestran metadatos, no los datos en sí.',
  },
  {
    n: 16,
    d: 'TRA',
    m: 0,
    q: "You have accidentally deleted records from a table called transactions. The version prior to the delete was 3, and after the delete it's 4. What is the easiest way to restore the previous state of the table?",
    o: [
      '`RESTORE TABLE transactions FROM VERSION as of 4`',
      '`RESTORE TABLE transactions TO VERSION as of 3`',
      'Combine `INSERT INTO OVERWRITE transactions SELECT * FROM transactions VERSION AS OF 3` with a `MINUS`/`SELECT * FROM transactions`/`INSERT INTO OVERWRITE transactions SELECT * FROM transactions VERSION AS OF 4`/`INTERSECT` chain',
    ],
    a: [1],
    x: '`RESTORE TABLE table_name TO VERSION AS OF n` es el comando directo y soportado para volver a una versión anterior de una tabla Delta; aquí, la versión 3 (previa al DELETE) restaura los datos perdidos.',
  },
  {
    n: 17,
    d: 'GOV',
    m: 0,
    q: "Create a schema called bronze using location '/mnt/delta/bronze', checking if the schema exists before creating it.",
    o: [
      "`CREATE SCHEMA IF NOT EXISTS bronze LOCATION '/mnt/delta/bronze'`",
      "`CREATE SCHEMA bronze IF NOT EXISTS LOCATION '/mnt/delta/bronze'`",
      "`if IS_SCHEMA('bronze'): CREATE SCHEMA bronze LOCATION '/mnt/delta/bronze'`",
      'Schema creation is not available in the metastore; it can only be done in the Unity Catalog UI',
      'Cannot create a schema without a database',
    ],
    a: [0],
    x: 'La sintaxis correcta es `CREATE SCHEMA [IF NOT EXISTS] schema_name [LOCATION schema_directory]`, con `IF NOT EXISTS` inmediatamente después de `SCHEMA`, no al final.',
  },
  {
    n: 18,
    d: 'TRA',
    m: 0,
    q: 'You want to create an object by joining two tables that is accessible to the data scientist team, so it does not get dropped if the cluster restarts or the notebook is detached. What type of object should you create?',
    o: [
      'Temporary view',
      'Global Temporary view',
      'Global Temporary view with cache option',
      'External view',
      'View',
    ],
    a: [4],
    x: 'Una View normal es un objeto persistente en el metastore: sobrevive a reinicios de cluster y desvinculación de notebooks, y puede compartirse entre equipos, a diferencia de las vistas temporales (incluso las globales, atadas al ciclo de vida del cluster).',
  },
  {
    n: 19,
    d: 'TRA',
    m: 0,
    q: 'A table temp_data has a column raw containing JSON data recording temperature every four hours for Chicago. Calculate the maximum temperature recorded at 12:00 PM across all days.',
    o: [
      '`select max(raw.chicago.temp[3]) from temp_data`',
      '`select array_max(raw.chicago[*].temp[3]) from temp_data`',
      "`select array_max(from_json(raw['chicago'].temp[3], 'array<int>')) from temp_data`",
      "`select array_max(from_json(raw:chicago[*].temp[3], 'array<int>')) from temp_data`",
      "`select max(from_json(raw:chicago[3].temp[3], 'array<int>')) from temp_data`",
    ],
    a: [3],
    x: 'Hay que parsear el JSON con `from_json` usando notación de dos puntos (`raw:chicago[*].temp[3]`) para extraer el array de temperaturas de las 12:00 PM de todos los días, y luego aplicar `array_max`; acceder directamente sin parsear o usar corchetes falla.',
  },
  {
    n: 20,
    d: 'TRA',
    m: 0,
    q: 'Which of the following SQL statements can be used to update a transactions table, setting a flag from Y to N?',
    o: [
      "`MODIFY transactions SET active_flag = 'N' WHERE active_flag = 'Y'`",
      "`MERGE transactions SET active_flag = 'N' WHERE active_flag = 'Y'`",
      "`UPDATE transactions SET active_flag = 'N' WHERE active_flag = 'Y'`",
      "`REPLACE transactions SET active_flag = 'N' WHERE active_flag = 'Y'`",
    ],
    a: [2],
    x: '`UPDATE` es la sentencia SQL estándar soportada por Delta Lake para modificar valores de columnas de forma ACID según una condición.',
  },
  {
    n: 21,
    d: 'TRA',
    m: 0,
    q: 'When you drop a managed table using `DROP TABLE table_name`, how does it impact metadata, history, and data stored in the table?',
    o: [
      'Drops the table from the metastore, dropping metadata, history, and data in storage',
      'Drops the table from the metastore and data from storage but keeps metadata and history in storage',
      'Drops the table from the metastore, metadata and history, but keeps the data in storage',
      'Drops the table but keeps metadata, history and data in storage',
      'Drops the table and history but keeps metadata and data in storage',
    ],
    a: [0],
    x: 'Para una tabla managed, el DROP elimina absolutamente todo: metadatos del metastore, historial y los archivos de datos en el almacenamiento; esto contrasta con una tabla externa, donde solo se borran los metadatos.',
  },
  {
    n: 22,
    d: 'TRA',
    m: 0,
    q: 'A cartId/items table records every array of items added to a cart per session. Create a unique list of items ever added to the cart by each customer:\n\n```\nSELECT cartId, ___(___(items)) as items\nFROM carts GROUP BY cartId\n```\n\nWhich pair of functions fills the blanks?',
    o: [
      'FLATTEN, COLLECT_UNION',
      'ARRAY_UNION, FLATTEN',
      'ARRAY_UNION, ARRAY_DISTINT',
      'ARRAY_UNION, COLLECT_SET',
      'ARRAY_DISTINCT, ARRAY_UNION',
    ],
    a: [3],
    x: '`COLLECT_SET(items)` agrega los arrays de todas las filas de un `cartId` eliminando duplicados, y `ARRAY_UNION(...)` los fusiona en un único array de valores únicos; `FLATTEN`/`COLLECT_UNION` no son funciones válidas para este caso.',
  },
  {
    n: 23,
    d: 'GOV',
    m: 0,
    q: 'The team wants to use table properties to identify a business owner for each table. Which DDL syntax populates a table property identifying the business owner?',
    o: [
      "`CREATE TABLE inventory (id INT, units FLOAT) SET TBLPROPERTIES business_owner = 'supply chain'`",
      "`CREATE TABLE inventory (id INT, units FLOAT) TBLPROPERTIES (business_owner = 'supply chain')`",
      "`CREATE TABLE inventory (id INT, units FLOAT) SET (business_owner = 'supply chain')`",
      "`CREATE TABLE inventory (id INT, units FLOAT) SET PROPERTY (business_owner = 'supply chain')`",
      "`CREATE TABLE inventory (id INT, units FLOAT) SET TAG (business_owner = 'supply chain')`",
    ],
    a: [1],
    x: 'La cláusula `TBLPROPERTIES (...)` va directamente tras la definición de columnas en `CREATE TABLE`, sin `SET`; `SET TBLPROPERTIES` es sintaxis de `ALTER TABLE` sobre tablas ya existentes, no de creación.',
  },
  {
    n: 24,
    d: 'TRA',
    m: 0,
    q: 'The data science team requests a missing column called average price, calculated from units sold and sales amount. Which SQL statement reloads the data with the additional column?',
    o: [
      '`INSERT OVERWRITE sales SELECT *, salesAmt/unitsSold as avgPrice FROM sales`',
      '`CREATE OR REPLACE TABLE sales AS SELECT *, salesAmt/unitsSold as avgPrice FROM sales`',
      '`MERGE INTO sales USING (SELECT *, salesAmt/unitsSold as avgPrice FROM sales)`',
      '`OVERWRITE sales AS SELECT *, salesAmt/unitsSold as avgPrice FROM sales`',
      '`COPY INTO SALES AS SELECT *, salesAmt/unitsSold as avgPrice FROM sales`',
    ],
    a: [1],
    x: '`CREATE OR REPLACE TABLE` (CRAS) sí puede modificar el esquema (añadir columnas nuevas); `INSERT OVERWRITE` por defecto solo sobrescribe datos, no añade columnas nuevas sin habilitar auto-merge de esquema.',
  },
  {
    n: 25,
    d: 'ING',
    m: 0,
    q: "You are loading external CSV files into a Delta table using COPY INTO. After running it a second time, no data was loaded. Why?\n\n```\nCOPY INTO table_name\nFROM 'dbfs:/mnt/raw/*.csv'\nFILEFORMAT = CSV\n```",
    o: [
      'COPY INTO only works for a one-time data load',
      'Run REFRESH TABLE sales before running COPY INTO',
      'COPY INTO did not detect new files after the last load',
      'Use the incremental = TRUE option to load new files',
      'COPY INTO does not support incremental load; use AUTO LOADER instead',
    ],
    a: [2],
    x: "`COPY INTO` lleva registro de los archivos ya cargados y los omite en ejecuciones posteriores; si no hay archivos nuevos desde la última carga, no carga nada. Se puede forzar la recarga con `COPY_OPTIONS ('force' = 'true')`.",
  },
  {
    n: 26,
    d: 'TRA',
    m: 0,
    q: "You have an orders table (2021 data) and orders_archive (2020 data), possibly with overlapping rows. You need to combine both tables' data and eliminate duplicate rows. Which SQL statement accomplishes this?",
    o: [
      '`SELECT * FROM orders UNION SELECT * FROM orders_archive`',
      '`SELECT * FROM orders INTERSECT SELECT * FROM orders_archive`',
      '`SELECT * FROM orders UNION ALL SELECT * FROM orders_archive`',
      '`SELECT * FROM orders_archive MINUS SELECT * FROM orders`',
      '`SELECT DISTINCT * FROM orders JOIN orders_archive ON order.id = orders_archive.id`',
    ],
    a: [0],
    x: '`UNION` combina los resultados de ambas tablas eliminando duplicados automáticamente; `UNION ALL` los conservaría, `INTERSECT` solo devolvería filas comunes, y `MINUS`/`JOIN` no combinan el conjunto completo de ambas tablas.',
  },
  {
    n: 27,
    d: 'TRA',
    m: 0,
    q: 'You are working with IoT data where each device has 5 readings in an array collected in Celsius. Convert each individual reading to Fahrenheit:\n\n```\nSELECT deviceId, __(deviceTempC, i -> (i * 9/5) + 32) as deviceTempF\nFROM sensors\n```\n\nWhich function fills the blank?',
    o: ['APPLY', 'MULTIPLY', 'ARRAYEXPR', 'TRANSFORM', 'FORALL'],
    a: [3],
    x: '`transform(array, función)` aplica una función lambda a cada elemento de un array y devuelve un nuevo array con los resultados, exactamente lo necesario para convertir cada lectura de temperatura.',
  },
  {
    n: 28,
    d: 'TRA',
    m: 0,
    q: 'What is the main difference between these two commands?\n\n```\nINSERT OVERWRITE table_name SELECT * FROM table\nCREATE OR REPLACE TABLE table_name AS SELECT * FROM table\n```',
    o: [
      'INSERT OVERWRITE replaces data by default, CREATE OR REPLACE replaces data and schema by default',
      'INSERT OVERWRITE replaces data and schema by default, CREATE OR REPLACE replaces data by default',
      'INSERT OVERWRITE maintains historical data versions by default, CREATE OR REPLACE clears the historical data versions by default',
      'INSERT OVERWRITE clears historical data versions by default, CREATE OR REPLACE maintains the historical data versions by default',
      'Both are the same and result in identical outcomes',
    ],
    a: [0],
    x: '`INSERT OVERWRITE` solo reemplaza los datos por defecto; `CREATE OR REPLACE TABLE` (CRAS) puede además modificar el esquema (añadir columnas, cambiar tipos), siendo la diferencia clave entre ambos comandos.',
  },
  {
    n: 29,
    d: 'TRA',
    m: 0,
    q: 'Which of the following array functions takes an input column and returns a unique list of values in an array?',
    o: ['COLLECT_LIST', 'COLLECT_SET', 'COLLECT_UNION', 'ARRAY_INTERSECT', 'ARRAY_UNION'],
    a: [1],
    x: '`COLLECT_SET` toma los valores de una columna y devuelve un array con valores únicos (sin duplicados) por grupo; `COLLECT_LIST` conserva duplicados, y `ARRAY_INTERSECT`/`ARRAY_UNION` operan sobre dos arrays, no sobre una columna.',
  },
  {
    n: 30,
    d: 'P',
    m: 0,
    q: 'Which of the following statements is incorrect when choosing between a lakehouse and a data warehouse?',
    o: [
      'Lakehouse can have special indexes and caching optimized for Machine Learning',
      'Lakehouse cannot serve low query latency with high reliability for BI workloads, only suitable for batch workloads',
      'Lakehouse can be accessed through various APIs including but not limited to Python/R/SQL',
      'Traditional data warehouses have storage and compute coupled',
      'Lakehouse uses standard data formats like Parquet',
    ],
    a: [1],
    x: 'El lakehouse sí puede ofrecer baja latencia y alta fiabilidad para cargas BI gracias a optimizaciones de almacenamiento y cómputo (como el caching), por lo que afirmar que solo sirve para batch es la afirmación incorrecta.',
  },
  {
    n: 31,
    d: 'P',
    m: 0,
    q: 'Which of the following statements is correct about a Lakehouse?',
    o: [
      'Lakehouse only supports Machine learning workloads and Data warehouses support BI workloads',
      'Lakehouse only supports end-to-end streaming workloads and Data warehouses support Batch workloads',
      'Lakehouse does not support ACID',
      'In Lakehouse, Storage and compute are coupled',
      'Lakehouse supports schema enforcement and evolution',
    ],
    a: [4],
    x: 'Una característica definitoria del lakehouse es el soporte de schema enforcement (integridad al escribir) y schema evolution (cambios como añadir columnas sin romper cargas existentes).',
  },
  {
    n: 32,
    d: 'P',
    m: 0,
    q: 'Which of the following are stored in the control plane of Databricks Architecture?',
    o: [
      'Job Clusters',
      'All Purpose Clusters',
      'Databricks Filesystem',
      'Databricks Web Application',
      'Delta tables',
    ],
    a: [3],
    x: 'El control plane aloja la aplicación web, notebooks y servicios de gestión; los clusters (job o all-purpose), DBFS y las tablas Delta residen en el data plane, en la cuenta cloud del cliente.',
  },
  {
    n: 33,
    d: 'TRO',
    m: 0,
    q: 'You have written a notebook to generate a summary data set for reporting. The notebook was scheduled using a job cluster, but it takes 8 minutes to start the cluster. What feature can be used to start the cluster in a timely fashion so your job can run immediately?',
    o: [
      'Set up an additional job to run ahead of the actual job so the cluster is already running when the second job starts',
      'Use the Databricks cluster pools feature to reduce the startup time',
      'Use Databricks Premium edition instead of Databricks standard edition',
      'Pin the cluster in the cluster UI page so it is always available to the jobs',
      'Disable auto termination so the cluster is always running',
    ],
    a: [1],
    x: 'Los cluster pools preasignan VMs para que el job cluster las tome ya listas, evitando el aprovisionamiento desde cero que causa el retraso de arranque.',
  },
  {
    n: 34,
    d: 'ING',
    m: 0,
    q: 'Which of the following scenarios is the best fit for Auto Loader?',
    o: [
      'Efficiently process new data incrementally from cloud object storage',
      'Efficiently move data incrementally from one Delta table to another Delta table',
      'Incrementally process new data from streaming data sources like Kafka into Delta Lake',
      'Incrementally process new data from relational databases like MySQL',
      'Efficiently copy data from one data lake location to another data lake location',
    ],
    a: [0],
    x: 'Auto Loader solo ingiere archivos almacenados en object storage en la nube; no procesa fuentes de streaming como Kafka (para eso se usa Structured Streaming directamente) ni bases de datos relacionales.',
  },
  {
    n: 35,
    d: 'ING',
    m: 0,
    q: 'You are asked to set up Auto Loader to process incoming JSON data dropped into cloud object storage, processing the data as soon as it arrives. Which statement is correct?',
    o: [
      'Auto Loader is native to Delta Lake and cannot support external cloud object storage',
      'Auto Loader has to be triggered from an external process when the file arrives in cloud storage',
      'Auto Loader needs to be converted to a Structured Streaming process',
      'Auto Loader can only process continuous data when stored in Delta Lake',
      'Auto Loader can support the file notification method so it can process data as it arrives',
    ],
    a: [4],
    x: 'El modo de notificación de archivos (file notification) de Auto Loader usa servicios de cola/notificación de la nube para detectar y procesar archivos en tiempo real conforme llegan, sin depender de listados periódicos ni disparadores externos.',
  },
  {
    n: 36,
    d: 'TRA',
    m: 0,
    q: 'What is the main difference between the bronze layer and silver layer in a medallion architecture?',
    o: [
      'Duplicates are removed in bronze, schema is applied in silver',
      'Silver may contain aggregated data',
      'Bronze is a raw copy of ingested data, silver contains data with production schema and is optimized for ELT/ETL throughput',
      'Bad data is filtered in Bronze, silver is a copy of bronze data',
    ],
    a: [2],
    x: 'Bronze almacena los datos tal cual llegan, sin validar; Silver aplica el esquema de producción, limpia y optimiza los datos para el throughput de ETL/ELT posterior.',
  },
  {
    n: 37,
    d: 'TRA',
    m: 0,
    q: 'Which of the following SQL commands is used to append rows to an existing Delta table?',
    o: [
      'APPEND INTO DELTA table_name',
      'APPEND INTO table_name',
      'COPY DELTA INTO table_name',
      'INSERT INTO table_name',
      'UPDATE table_name',
    ],
    a: [3],
    x: '`INSERT INTO` añade filas a una tabla existente, igual que en una base de datos tradicional; `APPEND INTO`/`COPY DELTA INTO` no son sintaxis válida.',
  },
  {
    n: 38,
    d: 'TRA',
    m: 0,
    q: 'What is the main difference between the silver layer and gold layer in medallion architecture?',
    o: [
      'Silver optimized to perform ETL, Gold is optimized for query performance',
      'Gold is optimized to perform ETL, Silver is optimized for query performance',
      'Silver is a copy of Bronze, Gold is a copy of Silver',
      'Silver is stored in Delta Lake, Gold is stored in memory',
      'Silver may contain aggregated data, gold may preserve the granularity of original data',
    ],
    a: [0],
    x: 'Silver está optimizada para el throughput de ETL/limpieza; Gold está optimizada para el rendimiento de consultas de negocio (dashboards, ML, reporting), siendo típicamente agregada.',
  },
  {
    n: 39,
    d: 'JOBS',
    m: 0,
    q: "A dataset defined with Delta Live Tables includes an expectations clause: `CONSTRAINT valid_timestamp EXPECT (timestamp > '2020-01-01')` (no ON VIOLATION clause specified). What is the expected behavior when a batch containing violating data is processed?",
    o: [
      'Records that violate the expectation are added to the target dataset and recorded as invalid in the event log',
      'Records that violate the expectation are dropped from the target dataset and recorded as invalid in the event log',
      'Records that violate the expectation cause the job to fail',
      'Records that violate the expectation are added to the target dataset and flagged as invalid in a field added to the target dataset',
      'Records that violate the expectation are dropped from the target dataset and loaded into a quarantine table',
    ],
    a: [0],
    x: 'Sin cláusula `ON VIOLATION` explícita, el comportamiento por defecto (equivalente a `EXPECT`/WARN) conserva los registros que violan la expectation en el dataset destino, pero los registra como inválidos en el event log, sin frenar el pipeline.',
  },
  {
    n: 40,
    d: 'TRA',
    m: 0,
    q: 'How are Delta tables stored?',
    o: [
      'A directory where Parquet data files are stored, a subdirectory _delta_log where metadata and the transaction log are stored as JSON files',
      'A directory where Parquet data files are stored; all of the metadata is stored in memory',
      'A directory where Parquet data files are stored in the data plane, a subdirectory _delta_log where metadata, history and log is stored in the control plane',
      'A directory where Parquet data files are stored; all of the metadata is stored in Parquet files',
      'Data is stored in the data plane and metadata and delta log are stored in the control plane',
    ],
    a: [0],
    x: 'Una tabla Delta consiste en archivos de datos Parquet más un subdirectorio `_delta_log` con el transaction log en formato JSON; todo reside junto en el mismo almacenamiento (data plane), no repartido entre planos.',
  },
  {
    n: 41,
    d: 'JOBS',
    m: 0,
    q: "A dataset defined with Delta Live Tables includes an expectations clause: `CONSTRAINT valid_timestamp EXPECT (timestamp > '2020-01-01') ON VIOLATION DROP ROW`. What is the expected behavior when a batch containing violating data is processed?",
    o: [
      'Records that violate the expectation are added to the target dataset and recorded as invalid in the event log',
      'Records that violate the expectation are dropped from the target dataset and recorded as invalid in the event log',
      'Records that violate the expectation cause the job to fail',
      'Records that violate the expectation are added to the target dataset and flagged as invalid in a field added to the target dataset',
      'Records that violate the expectation are dropped from the target dataset and loaded into a quarantine table',
    ],
    a: [1],
    x: '`ON VIOLATION DROP ROW` elimina del dataset destino los registros que incumplen la constraint y registra el detalle en el event log, permitiendo que el pipeline continúe.',
  },
  {
    n: 42,
    d: 'TRO',
    m: 0,
    q: 'You are asked to debug a Databricks job that is taking too long to run on Sundays. What steps do you take to identify the step that is taking longer to run?',
    o: [
      "A notebook's activity within a job run is only visible when using an all-purpose cluster",
      'Under the Workflow UI and Jobs, select the job you want to monitor and select the run to view notebook activity',
      'Enable debug mode in Jobs to see the output activity of a job',
      "Once a job is launched, you cannot access the job's notebook activity",
      "Use the compute's Spark UI to monitor the job activity",
    ],
    a: [1],
    x: 'Desde la UI de Workflows/Jobs, al seleccionar el job y la ejecución concreta se puede ver la actividad del notebook, tanto en ejecuciones activas como completadas, sin necesitar un "modo debug" inexistente.',
  },
  {
    n: 43,
    d: 'TRO',
    m: 0,
    q: 'A colleague shows you a job setup with a warning: "Jobs running on all-purpose cluster are considered all purpose compute." How do you best explain this warning?',
    o: [
      'All-purpose clusters cannot be used for job clusters, due to performance issues',
      'All-purpose clusters take longer to start than a job cluster',
      'All-purpose clusters are less expensive than job clusters',
      'All-purpose clusters are more expensive than job clusters',
      'All-purpose clusters provide interactive messages that cannot be viewed in a job',
    ],
    a: [3],
    x: 'La advertencia señala un tema de coste: los clusters all-purpose están pensados para trabajo interactivo y cuestan más por DBU que los job clusters, optimizados para cargas programadas y efímeras.',
  },
  {
    n: 44,
    d: 'TRO',
    m: 0,
    q: "Your team has hundreds of jobs running, but it's difficult to track the cost of each job run. What is the recommendation for monitoring and tracking cost across various workloads?",
    o: [
      'Create jobs in different workspaces so cost can be tracked easily',
      'Use Tags during job creation so cost can be easily tracked',
      'Use job logs to monitor and track costs',
      'Use workspace admin reporting',
      'Use a single cluster for all jobs so cost can be tracked easily',
    ],
    a: [1],
    x: 'Etiquetar (tags) los jobs al crearlos permite desglosar y atribuir el coste de cómputo por etiqueta en los informes de uso de Databricks, sin necesidad de separar workspaces ni compartir un único cluster.',
  },
  {
    n: 45,
    d: 'JOBS',
    m: 0,
    q: 'You are asked to set up two tasks in a Databricks job: the first runs a notebook to download data from a remote system, and the second is a DLT pipeline that processes this data. How do you configure this in the Jobs UI?',
    o: [
      'A single job cannot have both a notebook task and a DLT pipeline task; use two different jobs with a linear dependency',
      'The Jobs UI does not support DLT pipelines; set up the first task in the Jobs UI and run the DLT pipeline in continuous mode',
      'The Jobs UI does not support DLT pipelines; set up the first task in the Jobs UI and run the DLT pipeline in trigger mode',
      'A single job can be used to set up both the notebook and the DLT pipeline, using two different tasks with a linear dependency',
      'Add the first step inside the DLT pipeline and run the DLT pipeline in triggered mode in the Jobs UI',
    ],
    a: [3],
    x: 'La UI de Jobs sí soporta tareas de tipo DLT pipeline junto con tareas de notebook dentro del mismo job; basta con crear ambas tareas y configurar la dependencia lineal entre ellas.',
  },
  {
    n: 46,
    d: 'TRO',
    m: 0,
    q: 'You are asked to set up an alert to notify by email every time a KPI indicator increases beyond a threshold value, and to include the actual value in the alert email notification.',
    o: [
      'Use a notebook and Python code to run every minute, using Python variables to send the information by email',
      "Set up an alert but use the default template to notify the message in the email's subject",
      "Set up an alert but use a custom template to notify the message in the email's subject",
      'Use the webhook destination instead so the alert message can be customized',
      'Use a custom email hook to customize the message',
    ],
    a: [2],
    x: 'La plantilla personalizada de una Alert soporta variables (como `QUERY_RESULT_VALUE`) para insertar el valor real de la métrica en el cuerpo o asunto del email, sin necesidad de scripts externos ni webhooks.',
  },
  {
    n: 47,
    d: 'TRO',
    m: 0,
    q: 'One of the queries in a Databricks SQL Dashboard takes a long time to refresh. Which of the following steps can be taken to identify the root cause of this issue?',
    o: [
      'Restart the SQL endpoint',
      'Select the SQL endpoint cluster, Spark UI, SQL tab to see the execution plan and time spent in each step',
      'Run OPTIMIZE and Z-ordering',
      'Change the Spot Instance Policy from "Cost optimized" to "Reliability Optimized"',
      'Use Query History to view queries, select the query, and check the query profile for time spent in each step',
    ],
    a: [4],
    x: 'El Query History junto con el query profile de Databricks SQL desglosa claramente el tiempo invertido en cada etapa de la ejecución, siendo más específico y directo que el Spark UI genérico para este diagnóstico.',
  },
  {
    n: 48,
    d: 'TRO',
    m: 0,
    q: 'The operations team uses a centralized data quality monitoring system where a user can publish data quality metrics through a webhook. You need to send messages via webhook if there is at least one duplicate record. Which approach integrates an alert with the current monitoring system?',
    o: [
      'Use a notebook and Jobs with Python to publish DQ metrics',
      'Set up an alert to send an email, then use Python to parse the email and publish a webhook message',
      'Set up an alert with a custom template',
      'Set up an alert with a custom webhook destination',
      'Set up an alert with a dynamic template',
    ],
    a: [3],
    x: 'Las Alerts de Databricks SQL soportan destinos de tipo webhook directamente, permitiendo integrarse con el sistema de monitorización existente sin necesidad de notebooks ni de parsear emails.',
  },
  {
    n: 49,
    d: 'TRO',
    m: 0,
    q: 'A SQL Dashboard for the supply chain team shows timestamps in UTC, but they want the time zone changed to New York. How would you approach resolving this issue for the whole dashboard at once?',
    o: [
      'Move the workspace from the Central US zone to the East US zone',
      'Change the timestamp on the Delta tables to America/New_York format',
      'Change the Spark configuration of the SQL endpoint to format the timestamp to America/New_York',
      'Under the SQL Admin Console, set the SQL configuration parameter time zone to America/New_York',
      'Add SET Timezone = America/New_York to every SQL query in the dashboard',
    ],
    a: [3],
    x: 'Configurar el parámetro de zona horaria en el SQL Admin Console aplica el cambio a nivel de todo el SQL Warehouse (y por tanto a todas las queries del dashboard), sin editar cada query individualmente ni mover el workspace de región.',
  },
  {
    n: 50,
    d: 'TRO',
    m: 0,
    q: 'You set up a SQL Endpoint for an application team. During peak hours, as concurrent users increase, you see degradation in query performance and queries taking longer. Which steps can resolve the issue?',
    o: [
      'Turn on the Serverless feature for the SQL endpoint',
      "Increase the maximum bound of the SQL endpoint's scaling range",
      'Increase the cluster size (2X-Small to 4X-Large) of the SQL endpoint',
      'Turn on the Auto Stop feature for the SQL endpoint',
      'Turn on the Serverless feature for the SQL endpoint and change the Spot Instance Policy from "Cost optimized" to "Reliability Optimized"',
    ],
    a: [1],
    x: 'Ante degradación por más usuarios concurrentes, el problema es de concurrencia, no de tamaño de query individual: aumentar el máximo del rango de escalado añade más clusters para atender queries en paralelo, en vez de escalar verticalmente un único cluster.',
  },
  {
    n: 51,
    d: 'TRO',
    m: 0,
    q: 'The data engineering team uses several SQL queries to review data quality and monitor an ETL job every day. Which approach can be used to set up a schedule and automate this process?',
    o: [
      'Schedule the query to run every 1 day from the Jobs UI',
      "Schedule the query to refresh every 1 day from the query's page in Databricks SQL",
      'Schedule the query to run every 12 hours from the Jobs UI',
      "Schedule the query to refresh every 1 day from the SQL endpoint's page in Databricks SQL",
      "Schedule the query to refresh every 12 hours from the SQL endpoint's page in Databricks SQL",
    ],
    a: [1],
    x: 'Cada query de Databricks SQL se puede programar directamente desde su propia página (pestaña de info → Refresh Schedule), sin necesidad de un Job separado ni de configurarlo desde la página del SQL endpoint.',
  },
  {
    n: 52,
    d: 'GOV',
    m: 0,
    q: 'Unity Catalog helps you manage the following resources in Databricks at the account level:',
    o: ['Tables', 'ML Models', 'Dashboards', 'Meta Stores and Catalogs', 'All of the above'],
    a: [4],
    x: 'Unity Catalog gestiona de forma centralizada a nivel de cuenta tablas, modelos de ML, dashboards y metastores/catálogos, ofreciendo gobernanza unificada sobre todos estos activos.',
  },
  {
    n: 53,
    d: 'GOV',
    m: 0,
    q: 'What is the top-level object in Unity Catalog?',
    o: ['Catalog', 'Table', 'Workspace', 'Database', 'Metastore'],
    a: [4],
    x: 'El Metastore es el objeto de nivel superior en Unity Catalog: contiene los catálogos, que a su vez contienen esquemas y tablas.',
  },
  {
    n: 54,
    d: 'GOV',
    m: 0,
    q: 'Kevin is the owner of the sales schema. Steve wants to create a new table called regional_sales in the sales schema, so Kevin grants Steve create table permissions. Steve creates the table. Who is the owner of regional_sales?',
    o: [
      'Kevin is the owner of the sales schema, so all tables in the schema will be owned by Kevin',
      'Steve is the owner of the table',
      'By default, ownership is assigned to DBO',
      'By default, ownership is assigned to DEFAULT_OWNER',
      'Kevin and Smith are both owners of the table',
    ],
    a: [1],
    x: 'En Databricks, el creador de un objeto se convierte en su propietario, independientemente de quién sea dueño del esquema contenedor; Steve creó la tabla, así que Steve es su owner.',
  },
  {
    n: 55,
    d: 'TRO',
    m: 0,
    q: 'You were asked to set up a new all-purpose cluster, but the cluster is unable to start. Which of the following steps do you need to take to identify the root cause?',
    o: [
      'Check the cluster driver logs',
      'Check the cluster event logs',
      'Check workspace logs',
      'Check the storage account',
      'Check the data plane',
    ],
    a: [1],
    x: 'Los cluster event logs muestran directamente por qué un cluster no pudo arrancar (por ejemplo, límites de cuota de VM o subred agotada), siendo el lugar indicado para este tipo de diagnóstico.',
  },
  {
    n: 56,
    d: 'TRO',
    m: 0,
    q: 'A team member started using an all-purpose cluster to develop a notebook and used the same cluster to set up a job that runs every 30 minutes to update tables used in a dashboard. What would you recommend to reduce the overall cost of this approach?',
    o: [
      'Reduce the size of the cluster',
      'Reduce the number of nodes and enable autoscale',
      'Enable auto termination after 30 minutes',
      'Change the cluster from all-purpose to job cluster when scheduling the job',
      'Change the cluster mode from all-purpose to single-mode',
    ],
    a: [3],
    x: 'Un job programado no necesita interactividad, así que usar un job cluster (más barato por DBU y que se termina automáticamente) en vez del all-purpose cluster reduce el coste, que puede llegar a ser el doble con all-purpose.',
  },
  {
    n: 57,
    d: 'P',
    m: 0,
    q: 'Which of the following commands can be used to run one notebook from another notebook?',
    o: [
      '`notebook.utils.run("full notebook path")`',
      '`execute.utils.run("full notebook path")`',
      '`dbutils.notebook.run("full notebook path")`',
      'Only job clusters can run a notebook',
      '`spark.notebook.run("full notebook path")`',
    ],
    a: [2],
    x: '`dbutils.notebook.run(path, timeout_seconds, arguments)` es la API soportada para ejecutar un notebook desde otro, pudiendo pasar parámetros y un timeout.',
  },
];

export const exam17: RawQuestion[] = [
  {
    n: 1,
    d: 'TRA',
    m: 0,
    q: "A job runs daily at 12:00 AM and loads data into the orders table, usually taking 30 minutes. One day it ran for 5 minutes and then failed, leaving the table in an inconsistent state. At 8:00 AM, a data engineer discovers the issue, verifies source and target counts don't match, and decides to restore the table to its previous state before re-running the job. Which SQL statement accomplishes this?",
    o: [
      '`RESTORE orders TO TIMESTAMP AS OF current_timestamp() - 8 HOURS;`',
      '`ROLLBACK TABLE orders TO TIMESTAMP AS OF current_timestamp() - INTERVAL 8 HOURS;`',
      "`RESTORE TABLE orders TO TIMESTAMP AS OF current_timestamp() - INTERVAL '8' HOURS;`",
      '`ROLLBACK orders TO TIMESTAMP current_timestamp() - 8 HOURS;`',
      '`RESTORE TABLE orders TO TIMESTAMP current_timestamp() - INTERVAL 8 HOURS;`',
    ],
    a: [2],
    x: "La sintaxis válida de Delta Lake es `RESTORE TABLE` (no `ROLLBACK`, que no existe), con la cláusula `AS OF` obligatoria y la aritmética de tiempo escrita como `INTERVAL '8' HOURS`.",
  },
  {
    n: 2,
    d: 'TRA',
    m: 0,
    q: 'As part of ingesting data, which of the following options helps you overwrite the existing data of a Delta table, assuming its schema remains the same?',
    o: ['COPY INTO', 'INSERT OVERWRITE', 'CREATE OR REPLACE TABLE', 'INSERT', 'MERGE'],
    a: [1],
    x: '`INSERT OVERWRITE` trunca la tabla (o las particiones afectadas) antes de insertar, sobrescribiendo los datos existentes, siempre que el esquema de los nuevos registros coincida con el actual.',
  },
  {
    n: 3,
    d: 'ING',
    m: 0,
    q: 'A data engineer has to register a table called orders on an external CSV source where fields are pipe-delimited (|) and the first row is the header with column names. Which Spark SQL DDL creates that table?',
    o: [
      '`CREATE TABLE orders_csv (...) USING CSV TABLEPROPERTIES (header = "false", delimiter = "|") LOCATION "dbfs:/user/username/retail/orders-csv"`',
      '`CREATE TABLE orders_csv (...) STORED AS CSV PROPERTIES (header = "true", delimiter = "|") LOCATION "dbfs:/user/username/retail/orders-csv"`',
      '`CREATE CSV TABLE orders_csv (...) OPTIONS (header = "false", delimiter = "|") LOCATION "dbfs:/user/username/retail/orders-csv"`',
      '`CREATE TABLE orders_csv USING CSV (...) OPTIONS (header = "true", delimiter = "|") LOCATION "dbfs:/user/username/retail/orders-csv"`',
      '`CREATE TABLE orders_csv (...) USING CSV OPTIONS (header = "true", delimiter = "|") LOCATION "dbfs:/user/username/retail/orders-csv"`',
    ],
    a: [4],
    x: 'La sintaxis estándar es `CREATE TABLE nombre (columnas) USING formato OPTIONS (clave = valor, ...) LOCATION ruta`, con `header = "true"` porque la primera fila es la cabecera; `TABLEPROPERTIES`, `STORED AS`/`PROPERTIES` y `CREATE CSV TABLE` no son sintaxis válida para este propósito.',
  },
  {
    n: 4,
    d: 'TRA',
    m: 0,
    q: "A data engineer has two Delta tables, customers and orders, and wants to find the ids of customers who haven't placed any orders yet. Which query accomplishes this?",
    o: [
      '`SELECT customer_id FROM customers INTERSECT SELECT order_customer_id FROM orders`',
      '`SELECT customer_id FROM customers UNION SELECT order_customer_id FROM orders`',
      '`SELECT customer_id FROM customers c INNER JOIN orders o ON c.customer_id = o.order_customer_id`',
      '`SELECT customer_id FROM customers UNION ALL SELECT order_customer_id FROM orders`',
      '`SELECT customer_id FROM customers EXCEPT SELECT order_customer_id FROM orders`',
    ],
    a: [4],
    x: '`EXCEPT` (o `MINUS`) devuelve las filas del primer `SELECT` que no aparecen en el segundo, exactamente los customer_id que nunca aparecen como order_customer_id; `INTERSECT` daría los que sí han comprado, e `INNER JOIN`/`UNION` no aíslan esa diferencia.',
  },
  {
    n: 5,
    d: 'ING',
    m: 0,
    q: 'What are the two file detection modes available in Auto Loader?',
    o: [
      'Replayable Data Sources and Idempotent Sinks',
      'Checkpointing and Write Ahead Logs',
      'Directory Listing and File Notification',
      'Watermarking and Checkpointing',
      'Idempotent Sinks and Structured Streaming',
    ],
    a: [2],
    x: 'Auto Loader soporta dos modos de detección de archivos nuevos: Directory Listing (listando el directorio de entrada) y File Notification (usando servicios de notificación/cola de la nube), pudiendo cambiar entre ellos sin perder la garantía de exactly-once.',
  },
  {
    n: 6,
    d: 'JOBS',
    m: 0,
    q: 'Which of the following describes when to use CREATE STREAMING LIVE TABLE over CREATE LIVE TABLE while creating a Delta Live Tables pipeline?',
    o: [
      'CREATE STREAMING LIVE TABLE should be used to process the entire data every time',
      'CREATE STREAMING LIVE TABLE should be used when working with streaming data sources and to process the data incrementally',
      'CREATE STREAMING LIVE TABLE should be used when the previous step in the DLT pipeline is static',
      'CREATE STREAMING LIVE TABLE should be used now because CREATE LIVE TABLE is going to be deprecated soon',
      'CREATE STREAMING LIVE TABLE should be used when the next step in the DLT pipeline is static',
    ],
    a: [1],
    x: 'Una streaming live table procesa solo los datos añadidos desde la última actualización del pipeline (con estado), ideal para fuentes que llegan continuamente y grandes volúmenes, reduciendo coste y latencia frente a recomputar todo cada vez.',
  },
  {
    n: 7,
    d: 'TRA',
    m: 0,
    q: 'Which of the following statements best describes the Silver layer tables in the Medallion Architecture?',
    o: [
      "Silver tables' structure is similar to the source system table structure, along with additional metadata columns that capture load time, process id, etc.",
      'Silver tables are those where all data from external source systems is landed',
      'Silver tables provide different use cases like Inventory Analytics, Product Recommendations, Customer Segmentation, Sales Analytics, etc.',
      "Silver tables provide a more refined view of raw data, where it's filtered, cleansed, and enriched",
      'Silver tables provide a business-level aggregated version of data used for reporting',
    ],
    a: [3],
    x: 'Silver ofrece una vista más refinada de los datos: filtrada, limpiada y enriquecida (p. ej. uniendo campos de varias tablas Bronze), a diferencia de Bronze (copia cruda) y Gold (agregados de negocio).',
  },
  {
    n: 8,
    d: 'ING',
    m: 0,
    q: 'A data engineer developed this code block to perform a streaming read, but it returns an error:\n\n```\n(\n  spark\n  .read\n  .schema(schema)\n  .format("cloudFiles")\n  .option("cloudFiles.format", "json")\n  .load(dataSource)\n)\n```\n\nWhich change configures the block to successfully perform a streaming read?',
    o: [
      'A new .stream line should be added after the spark line',
      'The .read line should be replaced with .readStream',
      'A new .stream line should be added after the .read line',
      'A new .stream line should be added after the .load(dataSource) line',
      'The .format("cloudFiles") line should be replaced with .format("streamingSource")',
    ],
    a: [1],
    x: '`readStream` es el método que crea un DataFrame de streaming; `read` es para datasets estáticos. Sustituir `.read` por `.readStream` habilita el procesamiento incremental que Auto Loader (`cloudFiles`) espera.',
  },
  {
    n: 9,
    d: 'TRA',
    m: 0,
    q: 'Which of the following statements correctly describes Delta Lake?',
    o: [
      'Delta Lake is an open source platform to help manage the complete machine learning lifecycle',
      'Delta Lake is an open source data storage format for distributed data',
      'Delta Lake is an open source analytics engine used for big data workloads',
      'Delta Lake is an open format storage layer that delivers reliability, security, and performance',
      'Delta Lake is an open format storage layer that processes data',
    ],
    a: [3],
    x: 'Delta Lake es una capa de almacenamiento de formato abierto sobre data lakes que aporta transacciones ACID, gestión escalable de metadatos y unifica batch/streaming, entregando fiabilidad, seguridad y rendimiento; no es un motor de analítica ni una plataforma de ML.',
  },
  {
    n: 10,
    d: 'TRA',
    m: 0,
    q: 'Which of the following statements is incorrect about a managed table in Databricks?',
    o: [
      'Managed tables are the default when creating a table in Databricks',
      'Data for a managed table does not reside in the LOCATION of the database it is registered to',
      'Databricks manages both the metadata and the data for a managed table',
      'The managed relationship between the data location and the database means that moving a managed table to a new database requires rewriting all data to the new location',
      'Dropping a managed table deletes both the metadata and underlying data of that table',
    ],
    a: [1],
    x: 'Es al revés: los datos de una tabla managed sí residen en la ubicación (LOCATION) de la base de datos donde está registrada; esta es la afirmación falsa entre las opciones, por lo que es la respuesta correcta a "cuál es incorrecta".',
  },
  {
    n: 11,
    d: 'TRO',
    m: 0,
    q: 'A query on the transactions Delta table (which has huge data) runs too slowly. The engineer finds many small files, and the column transactionId (high cardinality, used for filtering) is sparsely located throughout each data file. Which command performs the needed optimization?',
    o: [
      '`OPTIMIZE TABLE transactions ZORDER BY (transactionId)`',
      '`OPTIMIZE transactions ZORDER BY COLUMN transactionId`',
      '`OPTIMIZE TABLE transactions ZORDER BY COLUMN (transactionId)`',
      '`OPTIMIZE TABLE transactions ZORDER BY transactionId`',
      '`OPTIMIZE transactions ZORDER BY (transactionId)`',
    ],
    a: [4],
    x: 'La sintaxis real es `OPTIMIZE nombre_tabla ZORDER BY (columna)`, sin la palabra clave `TABLE` y con la columna entre paréntesis; Z-Order coloca físicamente juntos los datos relacionados según una columna de alta cardinalidad usada en filtros, reduciendo drásticamente el dato leído.',
  },
  {
    n: 12,
    d: 'TRA',
    m: 0,
    q: 'Which of the following data workloads will utilize a Gold table as its destination?',
    o: [
      'A job that enriches data by parsing its timestamps into a human-readable format',
      'A job that queries aggregated data that already feeds into a dashboard',
      'A job that cleans data by removing malformatted records',
      'A job that ingests raw data from a streaming source into the Lakehouse',
      'A job that aggregates cleaned data to create standard summary statistics',
    ],
    a: [4],
    x: 'Agregar datos ya limpios para producir estadísticas resumen estándar es exactamente el tipo de workload que produce una tabla Gold; enriquecer/limpiar corresponde a Silver, ingerir datos crudos a Bronze, y consultar agregados ya existentes es un uso de Gold, no su producción.',
  },
  {
    n: 13,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to process data based on a parameter called department provided to a Databricks notebook at run time, with a default value of "all". Which code snippet creates and assigns the parameter to a Python variable?',
    o: [
      '`dbutils.widgets.options("department", "all", "Department")` / `current_department = dbutils.widgets.get("department")`',
      '`dbutils.widgets.text("department", "all", "Department")` / `current_department = dbutils.widgets.get("department")`',
      '`dbutils.widgets.variable("department", "Department", "all")` / `current_department = dbutils.widgets.get("department")`',
      '`dbutils.widgets.text("department", "Department", "all")` / `current_department = dbutils.widgets.get("department")`',
      '`dbutils.widgets.parameter("department", "all", "Department")` / `current_department = dbutils.widgets.get("department")`',
    ],
    a: [1],
    x: '`dbutils.widgets.text(nombre, valor_por_defecto, etiqueta)` es la sintaxis correcta; `options`, `variable` y `parameter` no son métodos válidos, y la opción con el orden de argumentos invertido coloca la etiqueta como valor por defecto.',
  },
  {
    n: 14,
    d: 'GOV',
    m: 0,
    q: 'A data engineer creates a Delta table called customers that will contain PII. A senior engineer suggests adding a description "Table contains PII" so anyone accessing the table\'s metadata knows what it contains. Which code snippet achieves this?',
    o: [
      '`TBLPROPERTIES ("Table contains PII")`',
      '`DESCRIPTION {"Table contains PII"}`',
      '`COMMENT "Table contains PII"`',
      '`OPTIONS ("Table contains PII")`',
      '`TEXT "Table contains PII"`',
    ],
    a: [2],
    x: 'La cláusula `COMMENT "texto"` es la forma correcta de documentar una tabla en Databricks SQL, visible al hacer `DESCRIBE TABLE`; `TBLPROPERTIES` requiere pares clave-valor, y `DESCRIPTION`/`OPTIONS`/`TEXT` no son cláusulas válidas para este propósito.',
  },
  {
    n: 15,
    d: 'TRA',
    m: 0,
    q: 'Which of the following code blocks creates a lambda function in Python to find out whether a number is even or odd?',
    o: [
      "`my_func = lambda x: if x % 2 == 0 'Even' else 'Odd'`",
      "`my_func = lambda x: if x / 2 == 0 'Even' else 'Odd'`",
      "`my_func = lambda x: 'Even' if x % 2 == 0 else 'Odd'`",
      "`my_func = lambda x: 'Even' if x % 2 = 0 else 'Odd'`",
      "`my_func = lambda x: if x % 2 == 0 'Even' elif x % 2 != 0 'Odd'`",
    ],
    a: [2],
    x: 'Una lambda con expresión condicional en Python se escribe `valor_si_verdadero if condición else valor_si_falso`, sin la palabra clave `if` al principio de la expresión ni `elif`; el operador de comparación debe ser `==`, no `=`.',
  },
  {
    n: 16,
    d: 'GOV',
    m: 0,
    q: 'A data engineer wants to figure out whether the Delta table called transactions is an external table or a managed table. Which command(s) help find out the type of table?',
    o: [
      '`SHOW CREATE TABLE transactions`',
      '`DESCRIBE EXTENDED transactions`',
      "`SHOW TABLE EXTENDED LIKE 'transactions'`",
      '`DESCRIBE FORMATTED transactions`',
      'All of the above',
    ],
    a: [4],
    x: 'Los cuatro comandos exponen metadatos de la tabla, incluyendo su `LOCATION`; revisando esa propiedad (dentro del directorio warehouse por defecto = managed, fuera = external) con cualquiera de ellos se puede determinar el tipo de tabla.',
  },
  {
    n: 17,
    d: 'TRA',
    m: 0,
    q: "A data engineer wants to query the orders Delta table and extract the date part from order_timestamp in the format '31-12-2022' (day-month-year, hyphen-separated). Which query accomplishes this task?",
    o: [
      "`SELECT order_id, date_format(order_timestamp, 'DD-MM-YYYY') as order_date FROM orders;`",
      "`SELECT order_id, date_format(order_timestamp, 'MM-DD-YYYY') as order_date FROM orders;`",
      "`SELECT order_id, date_format(order_timestamp, 'dd-MM-YYYY') as order_date FROM orders;`",
      "`SELECT order_id, date_format(order_timestamp, 'MM-dd-YYYY') as order_date FROM orders;`",
      "`SELECT order_id, date_fmt(order_timestamp, 'dd-MM-YYYY') as order_date FROM orders;`",
    ],
    a: [2],
    x: 'Spark SQL usa `dd` (minúscula) para día del mes, `MM` para mes, y `YYYY` para año; `DD` en mayúscula es inválido, `MM-DD`/`MM-dd` producen mes-día en vez de día-mes, y `date_fmt` no es una función válida (es `date_format`).',
  },
  {
    n: 18,
    d: 'GOV',
    m: 0,
    q: 'A data engineer who owns the retail database is leaving the organization. As an administrator, you need to assign ownership of this database to a user called user@myteam.com. Which command accomplishes this task?',
    o: [
      "`GRANT OWNERSHIP TO 'user@myteam.com' ON retail`",
      "`ALTER SCHEMA retail OWNER TO 'user@myteam.com'`",
      "`CHANGE OWNER OF retail TO 'user@myteam.com'`",
      "`ALTER DATABASE retail OWNER TO 'user@myteam.com'`",
      "`TRANSFER OWNERSHIP OF retail TO 'user@myteam.com'`",
    ],
    a: [1],
    x: 'En Databricks las "databases" se implementan como schemas, y la transferencia de propiedad se hace con `ALTER SCHEMA ... OWNER TO ...`; `ALTER DATABASE` usa la palabra clave equivocada, y `GRANT OWNERSHIP`/`CHANGE OWNER`/`TRANSFER OWNERSHIP` no son comandos válidos.',
  },
  {
    n: 19,
    d: 'TRO',
    m: 0,
    q: 'A data analysis team reports slow Databricks SQL queries. All queries run sequentially on the same SQL endpoint, which is not shared with other users. How can the data engineering team improve latency?',
    o: [
      "Increase the maximum bound of the SQL endpoint's scaling range",
      'Turn on the Serverless feature for the SQL endpoint',
      'Turn on the Auto Stop feature for the SQL endpoint',
      'Increase the cluster size of the SQL endpoint',
      'Turn on the Serverless feature for the SQL endpoint and change the Spot Instance Policy to Reliability Optimized',
    ],
    a: [3],
    x: 'Como las queries se ejecutan secuencialmente en un endpoint dedicado (no hay concurrencia de otros usuarios), lo que ayuda es escalar verticalmente aumentando el tamaño del cluster, dándole más CPU/memoria por query; el rango de escalado (horizontal) solo ayuda con concurrencia.',
  },
  {
    n: 20,
    d: 'TRO',
    m: 0,
    q: 'A senior data engineer runs `VACUUM orders RETAIN 0 HOURS` on the orders Delta table. Before it finishes, a junior engineer starts ingesting data from a source with non-zero records into the same table. After ingestion completes successfully, querying the table shows no data, with no obvious error. The table history shows only VACUUM and INSERT. What is the likely reason?',
    o: [
      'Configuration spark.databricks.delta.retentionDurationCheck.enabled was set to true',
      'The data ingestion process to load data into the orders Delta table completed only partially',
      'No records were present in the source table, so no data was ingested into the orders Delta table',
      'Someone might have deleted all the records from the orders Delta table',
      'VACUUM with RETAIN 0 HOURS can delete committed, uncommitted, and temporary files for concurrent transactions',
    ],
    a: [4],
    x: 'Configurar `RETAIN 0 HOURS` desactiva la comprobación de seguridad de retención y puede borrar archivos activos que una transacción concurrente (el INSERT) todavía estaba escribiendo, explicando por qué la ingesta "tuvo éxito" pero los datos desaparecieron.',
  },
  {
    n: 21,
    d: 'GOV',
    m: 0,
    q: 'Which of the following is not a privilege in Databricks?',
    o: ['READ_METADATA', 'SELECT', 'USAGE', 'CREATE', 'UPDATE'],
    a: [4],
    x: '`UPDATE` no forma parte del modelo de privilegios de Databricks (SELECT, USAGE, CREATE, READ_METADATA, etc. sí lo son); la modificación de datos se controla mediante comandos como INSERT/DELETE/MERGE, no un privilegio explícito "UPDATE".',
  },
  {
    n: 22,
    d: 'TRO',
    m: 0,
    q: "A team wants to receive notifications via HTTP request about a Databricks Job's start, success, or failure, including all job run details. How can they achieve this?",
    o: [
      'They can set up an Alert with a new webhook alert destination',
      'They can set up an Alert with a custom template',
      'They can set up an Alert with a new email address',
      'They can set up an Alert with one-time notifications',
      'This type of alerting is not possible in Databricks',
    ],
    a: [0],
    x: 'Un destino de alerta de tipo webhook envía peticiones HTTP a un endpoint personalizado, permitiendo integrar las notificaciones de ejecución del job con sistemas externos; email y plantillas personalizadas no cubren la entrega vía HTTP.',
  },
  {
    n: 23,
    d: 'GOV',
    m: 0,
    q: 'A data engineer needs to drop the retail database along with all the tables inside it, provided all tables are managed tables. Which command accomplishes this task?',
    o: [
      '`DROP DATABASE retail RESTRICT`',
      '`DROP DATABASE retail CASCADE`',
      '`DROP DATABASE retail`',
      '`DROP DATABASE retail INCLUDE ALL`',
      '`DROP DATABASE retail FORCE`',
    ],
    a: [1],
    x: '`CASCADE` elimina la base de datos junto con todos los objetos que contiene; `RESTRICT` (o el `DROP DATABASE` simple) falla si hay tablas dentro, e `INCLUDE ALL`/`FORCE` no son sintaxis válida.',
  },
  {
    n: 24,
    d: 'TRA',
    m: 0,
    q: 'A team of data scientists trained a machine learning model on a particular version of the credit_scores Delta table. They want to archive this version for future testing or ad-hoc analysis. Which option clones just the Delta transaction logs and not the data files?',
    o: ['DEEP CLONE', 'SHALLOW CLONE', 'DEEP COPY', 'SHALLOW COPY', 'None of the above'],
    a: [1],
    x: '`SHALLOW CLONE` copia solo los metadatos del transaction log, referenciando los mismos archivos de datos que el original, siendo ligero y rápido; `DEEP CLONE` copiaría también los archivos de datos físicamente, y `DEEP COPY`/`SHALLOW COPY` no son comandos válidos de Delta Lake.',
  },
  {
    n: 25,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to horizontally combine two tables using a shared key column. He wants rows where the key is present in both tables, plus the non-matching rows from both tables as well. Which SQL command accomplishes this?',
    o: ['UNION', 'INNER JOIN', 'LEFT OUTER JOIN', 'MERGE', 'FULL OUTER JOIN'],
    a: [4],
    x: '`FULL OUTER JOIN` devuelve tanto las filas coincidentes entre ambas tablas como las no coincidentes de cada una (rellenando con NULL donde no hay match); `INNER JOIN` solo devolvería las coincidencias, y `LEFT OUTER JOIN` solo añadiría las no coincidentes de un lado.',
  },
  {
    n: 26,
    d: 'JOBS',
    m: 0,
    q: 'A team of data engineers has created a Databricks Job in production with multiple tasks. They want to reduce compute costs and optimize execution time for each task. What is the best option to fulfill their requirements?',
    o: [
      'They should use a single all-purpose cluster for all the tasks in the job',
      'They should use a shared job cluster for all the tasks in the job',
      'They should use a new all-purpose cluster for each task in the job',
      'They should use a new job cluster for each task in the job',
      'They should use a few all-purpose clusters and a few job clusters for different tasks',
    ],
    a: [1],
    x: 'Un job cluster compartido entre todas las tareas de un mismo job evita el tiempo de arranque repetido y el coste de mantener un all-purpose cluster activo; crear un cluster nuevo por tarea introduce sobrecarga innecesaria.',
  },
  {
    n: 27,
    d: 'TRA',
    m: 0,
    q: 'What is the default output mode for a Structured Streaming Query in Spark, provided that the target table is fault-tolerant and existing rows are never going to change?',
    o: ['Merge', 'Update', 'Complete', 'Append', 'Overwrite'],
    a: [3],
    x: 'El modo Append (por defecto) solo emite las filas nuevas añadidas a la tabla de resultados desde el último trigger, y solo está soportado cuando las filas ya emitidas nunca van a cambiar (p. ej. select, where, filter, join sin agregación).',
  },
  {
    n: 28,
    d: 'TRA',
    m: 0,
    q: 'A data analyst wants to select the name of a single country at a time from a dropdown and assign it to a Python variable to process weather data for that country, with a default value of "India". Which code block accomplishes this task?',
    o: [
      '`dbutils.widgets.list("country", "India", ["India", "Canada", "England", "USA"])` / `current_country = dbutils.widgets.get("country")`',
      '`dbutils.widgets.dropdown("country", "India", ["India", "Canada", "England", "USA"])` / `current_country = dbutils.widgets.get("country")`',
      '`dbutils.widgets.options("country", "India", ["India", "Canada", "England", "USA"])` / `current_country = dbutils.widgets.get("country")`',
      '`dbutils.widgets.select("country", "India", ["India", "Canada", "England", "USA"])` / `current_country = dbutils.widgets.get("country")`',
      '`dbutils.widgets.checkbox("country", "India", ["India", "Canada", "England", "USA"])` / `current_country = dbutils.widgets.get("country")`',
    ],
    a: [1],
    x: '`dbutils.widgets.dropdown(nombre, valor_por_defecto, lista_opciones)` crea un widget desplegable para seleccionar un único valor de una lista; `list`, `options`, `select` y `checkbox` no son métodos válidos de widgets en Databricks.',
  },
  {
    n: 29,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has to manually delete all the files and directory associated with the sales table located at "/databricks-datasets/retail-org/sales". Which command accomplishes this task?',
    o: [
      '`dbutils.fs.remove("/databricks-datasets/retail-org/sales", True)`',
      '`dbutils.fs.delete("/databricks-datasets/retail-org/sales", True)`',
      '`dbutils.fs.drop("/databricks-datasets/retail-org/sales", True)`',
      '`dbutils.fs.rm("/databricks-datasets/retail-org/sales", True)`',
      '`dbutils.fs.mv("/databricks-datasets/retail-org/sales", True)`',
    ],
    a: [3],
    x: '`dbutils.fs.rm(ruta, recurse)` elimina un archivo o directorio, y con `recurse=True` lo hace de forma recursiva; `remove`, `delete`, `drop` y `mv` no son métodos válidos de `dbutils.fs` para este propósito.',
  },
  {
    n: 30,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to create a data entity that persists between multiple sessions, can be used by other users, and is stored in a physical location. Which entity should they use?',
    o: ['Global Temporary View', 'Temporary View', 'Table', 'View', 'Function'],
    a: [2],
    x: 'Una Table persiste físicamente los datos y su definición en el metastore, disponible entre sesiones y para otros usuarios; las vistas (temporales, globales o normales) no almacenan datos físicos propios, y una Function tampoco es una entidad de datos.',
  },
  {
    n: 31,
    d: 'TRA',
    m: 0,
    q: 'What is the format in which data files are stored for a table in Delta Lake?',
    o: ['JSON', 'Parquet', 'ORC', 'Text', 'Avro'],
    a: [1],
    x: 'Delta Lake almacena los archivos de datos en formato Parquet (columnar, eficiente), añadiendo un transaction log en JSON aparte para las garantías ACID.',
  },
  {
    n: 32,
    d: 'TRA',
    m: 0,
    q: 'Which of the following commands helps you find the count of null values in the column alternate_email of an existing Delta table customers?',
    o: [
      '`SELECT count_if(alternate_email IS NULL) AS missing_alternate_emails FROM customers`',
      '`SELECT count_when(alternate_email IS NULL) AS missing_alternate_emails FROM customers`',
      '`SELECT count_where(alternate_email IS NULL) AS missing_alternate_emails FROM customers`',
      '`SELECT count_only(alternate_email IS NULL) AS missing_alternate_emails FROM customers`',
      '`SELECT count(alternate_email IS NULL) AS missing_alternate_emails FROM customers`',
    ],
    a: [0],
    x: '`count_if(condición)` cuenta cuántas filas cumplen una condición booleana; es la función soportada en Databricks SQL para este propósito. `count_when`, `count_where` y `count_only` no existen.',
  },
  {
    n: 33,
    d: 'ING',
    m: 0,
    q: 'You need to create a table users in Databricks by extracting data from a table in an external SQLite database called ecommerce. Which query creates this table?',
    o: [
      '`CREATE TABLE users USING SQLite OPTIONS (url = "jdbc:sqlite:/${da.username}_ecommerce.db", dbtable = "users")`',
      '`CREATE TABLE users USING ODBC OPTIONS (url = "jdbc:sqlite:/${da.username}_ecommerce.db", dbtable = "users")`',
      '`CREATE TABLE users USING JDBC OPTIONS (url = "jdbc:sqlite:/${da.username}_ecommerce.db", dbtable = "users")`',
      '`CREATE TABLE users USING SQL OPTIONS (url = "jdbc:sqlite:/${da.username}_ecommerce.db", dbtable = "users")`',
      '`CREATE TABLE users USING DATABASE OPTIONS (url = "jdbc:sqlite:/${da.username}_ecommerce.db", dbtable = "users")`',
    ],
    a: [2],
    x: 'Databricks se conecta a bases de datos SQL externas mediante `USING JDBC` con una URL de conexión que incluye el driver (`jdbc:sqlite:...`); `SQLite`, `ODBC`, `SQL` y `DATABASE` no son palabras clave válidas para `USING` en este contexto.',
  },
  {
    n: 34,
    d: 'TRA',
    m: 0,
    q: 'Which of the following code snippets prints the numbers from 1 to 10 in Python?',
    o: [
      '`for i in range(1 to 10): print(i)`',
      '`for i <- 1 to 10: print(i)`',
      '`for i in range(10): print(i)`',
      '`for i in range(1, 11): print(i)`',
      '`for(int i = 1; i <= 10; i++) print(i)`',
    ],
    a: [3],
    x: '`range(1, 11)` genera los números del 1 al 10 (el límite superior no se incluye); `range(10)` daría 0-9, y las demás opciones no son sintaxis válida de Python.',
  },
  {
    n: 35,
    d: 'TRA',
    m: 0,
    q: 'Which of the following SQL keywords can be used to delete all the rows from an existing Delta table efficiently, without deleting the table itself?',
    o: ['DROP', 'UPDATE', 'DELETE', 'ALTER', 'TRUNCATE'],
    a: [4],
    x: '`TRUNCATE TABLE` elimina todas las filas (o las particiones indicadas) de forma más eficiente que `DELETE`, sin borrar la definición de la tabla; `DROP` eliminaría la tabla entera, y `UPDATE`/`ALTER` no borran filas.',
  },
  {
    n: 36,
    d: 'CICD',
    m: 0,
    q: 'A data engineer wants to unit test that the number of rows returned by a query is more than 0:\n\n```\nrow_count = spark.sql("select count(*) from table_name").collect()[0][0]\n```\n\nWhich statement tests this and confirms the row count is more than 0?',
    o: [
      '`assert that row_count > 0, else "Row count should be more than 0"`',
      '`assert(row_count > 0, "Row count should be more than 0")`',
      '`assert row_count > 0, "Row count should be more than 0"`',
      '`assert if row_count > 0, else "Row count should be more than 0"`',
      '`assert(if row_count > 0, else "Row count should be more than 0")`',
    ],
    a: [2],
    x: 'La sintaxis correcta de `assert` en Python es `assert condición, mensaje`, sin paréntesis envolviendo ambos como tupla ni palabras clave `if`/`else`/`that` adicionales.',
  },
  {
    n: 37,
    d: 'ING',
    m: 0,
    q: 'A data pipeline must ingest data from a source generating millions of files daily in cloud storage, detect which files are new since the previous run, ingest incrementally, and handle a schema that changes over time. Which technique solves this?',
    o: ['COPY INTO', 'Databricks SQL', 'MERGE', 'Delta Lake', 'Auto Loader'],
    a: [4],
    x: 'Para volúmenes del orden de millones de archivos con esquema cambiante, Auto Loader es más eficiente y escalable que COPY INTO (pensado para miles de archivos), y ofrece mejores primitivas de inferencia y evolución de esquema.',
  },
  {
    n: 38,
    d: 'ING',
    m: 0,
    q: 'Which of the following Structured Streaming queries performs a hop from a source location to a Bronze table?',
    o: [
      '```\nspark.read.load(rawSalesLocation)\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("uncleanedSales")\n```',
      '```\nspark.table("sales").withColumn("avgPrice", col("sales") / col("units"))\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("cleanedSales")\n```',
      '```\nspark.table("sales").groupBy("store").agg(sum("sales"))\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("complete").table("aggregatedSales")\n```',
      '```\nspark.readStream.load(rawSalesLocation)\n  .format("cloudFiles").option("cloudFiles.format", sourceFormat)\n  .option("cloudFiles.schemaLocation", checkpointPath)\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("append").table("uncleanedSales")\n```',
      '```\nspark.table("sales").agg(sum("sales"), sum("units"))\n  .writeStream.option("checkpointLocation", checkpointPath)\n  .outputMode("complete").table("aggregatedSales")\n```',
    ],
    a: [3],
    x: 'Leer con `readStream` usando Auto Loader (`cloudFiles`) desde la ubicación de origen y escribir en `uncleanedSales` sin transformar es la ingesta streaming clásica de fuente→Bronze; las demás opciones usan lectura batch (`read`) o ya transforman/agregan los datos (Silver/Gold).',
  },
  {
    n: 39,
    d: 'TRA',
    m: 0,
    q: 'Which of the following data workloads will utilize a Silver table as its destination?',
    o: [
      'A job that aggregates cleaned data to create standard summary statistics',
      'A job that ingests raw data from a streaming source into the Lakehouse',
      'A job that checks the performance metrics of a machine learning application',
      'A job that enriches data by joining it with a static lookup table to add new columns',
      'A job that queries aggregated data that already feeds into a dashboard',
    ],
    a: [3],
    x: 'Enriquecer datos uniéndolos con una tabla de referencia estática para añadir columnas nuevas es una transformación típica de Silver; agregar es Gold, ingerir datos crudos es Bronze.',
  },
  {
    n: 40,
    d: 'TRO',
    m: 0,
    q: 'Which configuration property do you need to set before deleting files for a Delta table that are less than 7 days old?',
    o: [
      '`SET spark.databricks.delta.retentionDurationCheck.enabled = true`',
      '`SET spark.databricks.delta.retentionDurationFlag.enabled = false`',
      '`SET spark.databricks.delta.retentionDurationConstraint.enabled = true`',
      '`SET spark.databricks.delta.retentionDurationCheck.enabled = false`',
      '`SET spark.databricks.delta.retentionDurationFlag.enabled = true`',
    ],
    a: [3],
    x: 'Por defecto, `VACUUM` impide borrar archivos de menos de 7 días como medida de seguridad; desactivar `spark.databricks.delta.retentionDurationCheck.enabled` (poniéndolo a `false`) es necesario para saltarse esa comprobación, algo que debe hacerse con mucha precaución.',
  },
  {
    n: 41,
    d: 'TRA',
    m: 0,
    q: 'Given `spark.readStream.table("recordings_bronze").createOrReplaceTempView("recordings_tmp_vw")`, which of the following operations is NOT supported on this streaming view?',
    o: [
      '`SELECT * FROM recordings_tmp_vw`',
      '`SELECT device_id, count(device_id) AS total_recordings FROM recordings_tmp_vw GROUP BY device_id`',
      '`SELECT *, current_timestamp() as receipt_time FROM recordings_tmp_vw`',
      '`SELECT * FROM recordings_tmp_vw WHERE heartrate > 0`',
      '`SELECT * FROM recordings_tmp_vw ORDER BY device_id`',
    ],
    a: [4],
    x: 'Ordenar (`ORDER BY`) sobre un DataFrame/vista de streaming solo está soportado tras una agregación y en modo de salida `Complete`; aplicado directamente sobre un stream sin agregar, no está soportado. `SELECT`, `GROUP BY`, añadir columnas y `WHERE` sí lo están.',
  },
  {
    n: 42,
    d: 'JOBS',
    m: 0,
    q: "A dataset defined with Delta Live Tables includes an expectations clause: `CONSTRAINT valid_timestamp EXPECT (timestamp > '2022-01-01') ON VIOLATION DROP ROW`. What is the expected behavior when an incoming batch violates this constraint?",
    o: [
      'Records that violate the expectation are added to the target dataset and recorded as invalid in the event log',
      'Records that violate the expectation are dropped from the target dataset and loaded into a quarantine table',
      'Records that violate the expectation are added to the target dataset and flagged as invalid in a field added to the target dataset',
      'Records that violate the expectation cause the job to fail',
      'Records that violate the expectation are dropped from the target dataset and recorded as invalid in the event log',
    ],
    a: [4],
    x: '`ON VIOLATION DROP ROW` elimina del dataset destino los registros que incumplen la constraint, registrando el detalle en el event log; no falla el pipeline ni mueve los registros a una tabla de cuarentena automáticamente.',
  },
  {
    n: 43,
    d: 'ING',
    m: 0,
    q: 'Which of the following is NOT a feature of Auto Loader in Databricks?',
    o: [
      'Auto Loader incrementally and efficiently processes new data files as they arrive in cloud storage, and can process billions of files to migrate or backfill a table',
      'Auto Loader can detect schema drifts, notify you when schema changes happen, and rescue data that would have otherwise been ignored or lost',
      "Auto Loader's file notification mode can help reduce cloud costs further by avoiding directory listing altogether",
      'Auto Loader can resume from where it left off using the checkpoint location and continue to provide exactly-once guarantees when writing into Delta Lake',
      'Auto Loader provides a SQL warehouse, a compute resource that lets you run SQL commands on data objects within Databricks SQL',
    ],
    a: [4],
    x: 'Un SQL warehouse es un recurso de cómputo independiente para ejecutar SQL en Databricks SQL, no una funcionalidad de Auto Loader; las demás opciones sí describen capacidades reales de Auto Loader (procesamiento incremental, detección de schema drift, notificaciones de archivo, checkpointing exactly-once).',
  },
  {
    n: 44,
    d: 'TRO',
    m: 0,
    q: 'A team running a social media campaign uses a Databricks SQL dashboard to view key metrics, refreshing it manually on an as-needed basis rather than on a schedule. Which step reduces the overall cost of compute resources?',
    o: [
      "They can increase the maximum bound of the SQL endpoint's scaling range",
      'They can increase the cluster size of the SQL endpoint',
      'They can turn on the Serverless feature for the SQL endpoint and change the Spot Instance Policy to Reliability Optimized',
      'They can turn on the Auto Stop feature for the SQL endpoint',
      'They can turn on the Serverless feature for the SQL endpoint',
    ],
    a: [3],
    x: 'Auto Stop termina automáticamente el SQL endpoint tras un periodo de inactividad, evitando cargos de DBU/instancia mientras nadie usa el dashboard entre refrescos manuales.',
  },
  {
    n: 45,
    d: 'JOBS',
    m: 0,
    q: 'A data engineer sets up a Databricks notebook as a job to process data for five departments, taking an input parameter department_name, with no dependency between departments (tasks can run concurrently). Which approach should they take?',
    o: [
      'A task in the job cannot take an input parameter, so create five notebooks with hardcoded department_name and set up five tasks with linear dependency',
      'Use a single notebook as a task in the job and use dbutils.notebook.run() to run each notebook with input parameter department_name in a different cell',
      'A parameter can only be passed at the job level; create five jobs, passing department_name to each with no job dependency',
      'A task accepts key-value pair parameters; create five tasks, passing department_name as a parameter for each task with no dependency, so they can all run in parallel',
      'A parameter can only be passed at the job level; create five jobs, passing department_name to each with a linear job dependency',
    ],
    a: [3],
    x: 'Un único job puede tener cinco tareas basadas en el mismo notebook, cada una con un valor distinto del parámetro `department_name` pasado a nivel de tarea, y sin dependencias entre ellas para que se ejecuten en paralelo.',
  },
  {
    n: 46,
    d: 'JOBS',
    m: 0,
    q: 'A data engineering team created a Databricks Job with multiple tasks and wants to be notified by email when the job either succeeds or fails. Which approach sends emails to the team members?',
    o: [
      'They can develop a Python script to send the email every time a job succeeds or fails',
      'They can add multiple email addresses in email notification settings and select both the Success and Failure checkboxes',
      'They can manually check the status of the job every day depending on the time it takes to run',
      'Only the job owner can be configured to be notified in case of job success or failure',
      'There is no such way in the Databricks Lakehouse Platform to notify users when a job completes',
    ],
    a: [1],
    x: 'Los jobs de Databricks permiten añadir varias direcciones de email en la configuración de notificaciones y marcar las casillas de éxito y fallo para cada una, sin necesidad de scripts personalizados ni comprobaciones manuales.',
  },
  {
    n: 47,
    d: 'GOV',
    m: 0,
    q: 'Which of the following statements is incorrect about the Unity Catalog in Databricks?',
    o: [
      'Unity Catalog offers a single place to administer data access policies that apply across all workspaces and personas',
      "Unity Catalog's security model is based on standard ANSI SQL and allows administrators to grant permissions using familiar syntax, at the level of catalogs, databases, tables, and views",
      'Unity Catalog automatically captures user-level audit logs that record access to your data',
      'External tables are the default way to create tables in Unity Catalog; these tables are stored in the root storage location configured when creating a metastore, and use the Delta table format',
      'Unity Catalog captures runtime data lineage across queries in any language executed on a Databricks cluster or SQL warehouse, down to the column level',
    ],
    a: [3],
    x: 'Es al revés: las tablas managed (no las externas) son la forma por defecto de crear tablas en Unity Catalog, almacenándose en la ubicación raíz configurada del metastore; esta es la afirmación falsa, por lo que es la respuesta correcta a "cuál es incorrecta".',
  },
  {
    n: 48,
    d: 'TRO',
    m: 0,
    q: 'A data engineer scheduled a Databricks notebook as a job to create a summary of a dataset for reporting, but notices it takes around 10 minutes to start the cluster. Which option solves this problem?',
    o: [
      "Disable the auto terminate option of the cluster so it's always up and running",
      'Set up an additional job to run ahead of the actual job so the cluster is running before the actual job starts',
      'Use the Job Cluster for creating a new cluster to reduce the startup time',
      'Select a High Concurrency cluster instead of a Standard cluster to solve this issue',
      'Use the Databricks cluster pools feature to reduce the startup time',
    ],
    a: [4],
    x: 'Los cluster pools mantienen instancias inactivas y listas para usar, reduciendo drásticamente el tiempo de arranque y auto-escalado al crear un nuevo cluster a partir del pool en vez de aprovisionar VMs desde cero.',
  },
  {
    n: 49,
    d: 'TRA',
    m: 0,
    q: "A junior data engineer manually copies data files to a backup folder before running an UPDATE command on a Delta table, in case the update doesn't give the expected result, so they can restore from the backup. Which Delta Lake feature would you recommend to ease this task?",
    o: [
      'Use SHALLOW CLONE to copy the data, run tests directly on the main table, and in case of issues restore data from the clone table',
      'Cloud object storage supports automatic change-tracking and versioning',
      'Use DEEP CLONE to copy the data, run tests on the clone table, and once satisfied delete the main table and use the clone as the main table',
      'Use the time travel feature of Delta tables to roll back to a previous version in case of any issues',
      'Cloud object storage automatically backs up the data',
    ],
    a: [3],
    x: 'El time travel de Delta Lake permite volver a una versión anterior de la tabla (por timestamp o número de versión) en caso de que el UPDATE no salga como se esperaba, sin necesidad de copiar manualmente archivos a una carpeta de backup.',
  },
  {
    n: 50,
    d: 'TRA',
    m: 0,
    q: 'A data engineer working on a project with multiple interdependent entities wants to call another notebook from the current notebook, passing arguments used to set widget values and processing parameters. Which command achieves this in Python?',
    o: [
      '`dbutils.notebook.call("path", timeout_seconds, {"argument1": "data1", "argument2": "data2"})`',
      '`dbutils.notebook.execute("path", timeout_seconds, {"argument1": "data1", "argument2": "data2"})`',
      '`dbutils.notebook.trigger("path", timeout_seconds, {"argument1": "data1", "argument2": "data2"})`',
      '`dbutils.notebook.implement("path", timeout_seconds, {"argument1": "data1", "argument2": "data2"})`',
      '`dbutils.notebook.run("path", timeout_seconds, {"argument1": "data1", "argument2": "data2"})`',
    ],
    a: [4],
    x: '`dbutils.notebook.run(path, timeout_seconds, arguments)` lanza un job efímero que ejecuta el notebook indicado, pasando el diccionario `arguments` para fijar los valores de sus widgets; `call`, `execute`, `trigger` e `implement` no son métodos válidos.',
  },
  {
    n: 51,
    d: 'CICD',
    m: 0,
    q: 'Two junior data engineers working on separate parts of a single Databricks notebook, on separate Git branches, want to pair program on the same notebook simultaneously. A senior engineer suggests a better alternative. What is it?',
    o: [
      'Databricks Notebooks support the use of multiple languages in the same notebook',
      'Databricks Notebooks support automatic change-tracking and versioning',
      'Databricks Notebooks support commenting and notification comments',
      'Databricks Notebooks support real-time coauthoring on a single notebook',
      'Databricks Notebooks support the creation of interactive data visualizations',
    ],
    a: [3],
    x: 'La coautoría en tiempo real permite que varias personas editen el mismo notebook simultáneamente, sin necesidad de ramas Git separadas para hacer pair programming.',
  },
  {
    n: 52,
    d: 'TRA',
    m: 0,
    q: 'A data engineer runs `DROP TABLE orders` and confirms via `SHOW TABLES` that the table is no longer listed. However, its data files still persist in DBFS, even though the engineer wanted to delete both metadata and data. What is the possible reason?',
    o: [
      'Table orders was a managed table',
      'Table orders was not dropped properly',
      'Table orders was an external table',
      'Table orders was partially dropped',
      'Table orders should be truncated first before dropping',
    ],
    a: [2],
    x: 'Al hacer DROP de una tabla externa, solo se elimina la información de metadatos del catálogo; los archivos de datos en DBFS permanecen intactos. En una tabla managed, en cambio, el DROP habría borrado también los datos.',
  },
];

export const exam18: RawQuestion[] = [
  {
    n: 1,
    d: 'ING',
    m: 0,
    q: 'A team is converting their pipeline to use Auto Loader in Delta Live Tables to ingest tab-delimited CSV files where the first line is the header:\n\n```\nCREATE OR REFRESH STREAMING LIVE TABLE customers AS\nSELECT * FROM cloud_files("/databricks-datasets/retail-org/customers/", "csv", ____)\n```\n\nWhich code snippet fills the blank in `cloud_files()` to use Auto Loader for ingesting the data?',
    o: [
      '`map("delimiter", "\\t", "header", "true"))`',
      '`options("delimiter", "\\t", "header", "true"))`',
      '`map("lineSep", "\\t", "header", "true"))`',
      '`options("lineSep", "\\t", "header", "true"))`',
      '`map("delimiter", " ", "header", "true"))`',
    ],
    a: [0],
    x: '`cloud_files()` espera un `map` de opciones; para CSV con tabulador como separador se usa `"delimiter" -> "\\t"` (no `lineSep`, que define separadores de línea, no de columna) junto con `"header" -> "true"`.',
  },
  {
    n: 2,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to identify employees whose salary is greater than USD 500000 within each department, given a company table with an array-of-struct column called employees (each with employee_salary). Which code snippet accomplishes this?',
    o: [
      '`SELECT department_id, FETCH(employees, e -> e.employee_salary > 500000) as employees_with_high_salary FROM company;`',
      '`SELECT department_id, TRANSFORM(employees, e -> e.employee_salary > 500000) as employees_with_high_salary FROM company;`',
      '`SELECT department_id, EXIST(employees, e -> e.employee_salary > 500000) as employees_with_high_salary FROM company;`',
      '`SELECT department_id, REDUCE(employees, e -> e.employee_salary > 500000) as employees_with_high_salary FROM company;`',
      '`SELECT department_id, FILTER(employees, e -> e.employee_salary > 500000) as employees_with_high_salary FROM company;`',
    ],
    a: [4],
    x: '`FILTER(array, lambda)` recorre el array aplicando la condición y devuelve solo los elementos que la cumplen, exactamente lo necesario para quedarse con los empleados de salario alto; `TRANSFORM` mapea valores (no filtra), y `FETCH`/`EXIST`/`REDUCE` no encajan con este propósito.',
  },
  {
    n: 3,
    d: 'TRA',
    m: 0,
    q: 'Which of the following SQL keywords can be used to change the schema or properties of an existing Delta table?',
    o: ['UPDATE', 'MODIFY', 'CHANGE', 'ALTER', 'REPAIR'],
    a: [3],
    x: '`ALTER TABLE` es el comando para modificar el esquema o las propiedades de una tabla existente (añadir/eliminar columnas, cambiar propiedades, ubicación, etc.); `UPDATE` modifica datos (no esquema), y `MODIFY`/`CHANGE`/`REPAIR` no son palabras clave DDL válidas para este propósito.',
  },
  {
    n: 4,
    d: 'TRA',
    m: 0,
    q: 'A Structured Streaming job reads from a table, adds a column, and writes to a new table:\n\n```\n(\n  spark.table("sales")\n  .withColumn("avg_price", col("sales") / col("units"))\n  .writeStream\n  .option("checkpointLocation", checkpointPath)\n  .outputMode("complete")\n  ._________\n  .table("new_sales")\n)\n```\n\nIf the engineer wants the query to execute in multiple micro-batches to process all available data and then stop on its own, which line fills the blank?',
    o: [
      '`trigger(availableNow=True)`',
      '`trigger(once=True)`',
      '`trigger(available_Now="True")`',
      '`trigger(processingTime="now")`',
      '`trigger(processingTime="500ms")`',
    ],
    a: [0],
    x: '`trigger(availableNow=True)` procesa todos los datos disponibles al arrancar la query, repartidos en tantos micro-batches como haga falta, deteniéndose al terminar; `once=True` procesa en un único batch, y las demás opciones no son sintaxis válida o no cumplen el requisito.',
  },
  {
    n: 5,
    d: 'JOBS',
    m: 0,
    q: 'A Databricks Job has 5 tasks with a linear dependency and takes about 1 hour to run. It failed: the first 3 tasks succeeded, task 4 failed, and task 5 was skipped. Which approach repairs this job run while minimizing execution time and compute resources?',
    o: [
      'Manually execute the code for the last 2 tasks to repair the job run',
      'Repair this job so that all 5 tasks are re-run',
      'Repair this job so that only the last 2 tasks are re-run',
      'Delete the failed job run and trigger a new run for this job',
      'Keep the failed job run and simply start a new run for the job',
    ],
    a: [2],
    x: 'La función "Repair run" de Databricks relanza solo las tareas que fallaron y las que dependen de ellas (aquí, la 4 y la 5), sin reejecutar las 3 que ya tuvieron éxito, ahorrando tiempo y cómputo frente a relanzar todo el job.',
  },
  {
    n: 6,
    d: 'TRA',
    m: 0,
    q: "A data analyst wants a Databricks SQL query's results to dynamically change based on a selected state, for a dashboard showing e-commerce KPIs by region. What is the best approach?",
    o: [
      'Create a view for every state and query it directly for that state',
      'Create a SQL UDF and pass the state parameter at runtime',
      'Use a dropdown list with single-value select option as a query parameter, substituting the selected state into the query at runtime',
      'Manually provide the state value in the WHERE clause and run the query each time',
      'This type of runtime filtering is not possible in Databricks',
    ],
    a: [2],
    x: 'Los query parameters de Databricks SQL (texto entre `{{ }}`) permiten sustituir valores en tiempo de ejecución; un parámetro de tipo Dropdown List de valor único es la forma soportada de dejar que el usuario elija el estado y la query se adapte automáticamente.',
  },
  {
    n: 7,
    d: 'GOV',
    m: 0,
    q: 'A new data engineer (new.engineer@company.com) needs to query and update the sales table in the retail database, and already has USAGE on the database. Which command grants the appropriate permissions?',
    o: [
      '`GRANT USAGE ON TABLE sales TO new.engineer@company.com;`',
      '`GRANT SELECT, UPDATE TO new.engineer@company.com ON TABLE sales;`',
      '`GRANT SELECT, MODIFY ON TABLE sales TO new.engineer@company.com;`',
      '`GRANT SELECT, UPDATE ON TABLE sales TO new.engineer@company.com;`',
      '`GRANT SELECT, MODIFY TO new.engineer@company.com ON TABLE sales;`',
    ],
    a: [2],
    x: 'La sintaxis correcta es `GRANT privilegios ON objeto TO principal`; los privilegios válidos para leer y modificar filas son `SELECT` y `MODIFY` (no `UPDATE`, que no es un privilegio reconocido), y el orden `ON TABLE ... TO ...` debe respetarse.',
  },
  {
    n: 8,
    d: 'GOV',
    m: 0,
    q: 'Tom Hanks currently has read access to the inventory table but needs the ability to insert and delete rows as well. Which command grants him this access?',
    o: [
      '`GRANT INSERT, DELETE ON TABLE inventory TO tom.hanks@organization.com`',
      '`GRANT USAGE ON TABLE inventory TO tom.hanks@organization.com`',
      '`GRANT MODIFY TO tom.hanks@organization.com ON inventory`',
      '`GRANT INSERT, DELETE TO tom.hanks@organization.com ON TABLE inventory`',
      '`GRANT MODIFY ON TABLE inventory TO tom.hanks@organization.com`',
    ],
    a: [4],
    x: 'El privilegio `MODIFY` en Databricks SQL cubre insertar, actualizar y borrar filas de una tabla, siendo la forma correcta y más eficiente de conceder ese acceso; `INSERT`/`DELETE` no son privilegios separados válidos en esta sintaxis, y `USAGE` no otorga modificación de datos.',
  },
  {
    n: 9,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has to create an empty Delta table called students in a specific format, regardless of whether a table already exists with this name. Which SQL DDL command accomplishes this?',
    o: [
      '`CREATE TABLE IF NOT EXISTS students (id STRING, name STRING, dateOfBirth DATE, email STRING)`',
      '`CREATE OR REPLACE TABLE students (id STRING, name STRING, dateOfBirth DATE, email STRING)`',
      '`CREATE TABLE students (id STRING, name STRING, dateOfBirth DATE, email STRING)`',
      '`CREATE OR REPLACE TABLE IF EXISTS students (id STRING, name STRING, dateOfBirth DATE, email STRING)`',
      '`CREATE OR REPLACE TABLE students AS SELECT id STRING, name STRING, dateOfBirth DATE, email STRING USING DELTA`',
    ],
    a: [1],
    x: '`CREATE OR REPLACE TABLE` reemplaza la tabla si ya existe con la definición dada; `IF NOT EXISTS` no la sobrescribiría, `CREATE TABLE` a secas fallaría si ya existe, `OR REPLACE` combinado con `IF EXISTS` es sintaxis inválida, y `AS SELECT` no admite definir columnas directamente así.',
  },
  {
    n: 10,
    d: 'GOV',
    m: 0,
    q: 'What is the first level of object hierarchy used to reference data by three-level namespace in Unity Catalog?',
    o: ['Schema', 'Table', 'Catalog', 'Metastore', 'View'],
    a: [2],
    x: 'El espacio de nombres de tres niveles es `catalog.schema.table`; el catálogo es el contenedor de nivel superior de esa jerarquía. El metastore existe por encima, pero no forma parte del namespace de tres niveles en sí.',
  },
  {
    n: 11,
    d: 'GOV',
    m: 0,
    q: 'Mike owns the sales table and grants Ross the privilege to create a view called regional_sales_vw. After creating it, David asks Ross to grant him access to query the view. Ross grants it, but David still gets an error when accessing the view. What resolves this?',
    o: [
      'David needs to be the owner of regional_sales_vw to access it',
      'Ross needs to take ownership transfer of the sales table from Mike',
      'Mike needs to provide SELECT privilege to David on the sales table',
      'David needs to be the owner of the sales table to access regional_sales_vw',
      'Ross needs to provide SELECT privilege to David on the sales table',
    ],
    a: [2],
    x: 'Para consultar una vista, el usuario también necesita `SELECT` sobre la tabla base subyacente; como Ross no es dueño de `sales`, no puede conceder ese privilegio — solo Mike, el propietario de la tabla, puede hacerlo.',
  },
  {
    n: 12,
    d: 'TRA',
    m: 0,
    q: "A data engineer wants to join customers and orders on c.customer_id = o.order_customer_id, getting details of customers with their orders, but also wants to include customers who haven't placed any orders yet. Which join accomplishes this?",
    o: [
      'LEFT OUTER JOIN',
      'RIGHT OUTER JOIN',
      'INNER JOIN',
      'FULL OUTER JOIN',
      'Either LEFT OUTER JOIN or RIGHT OUTER JOIN can be used',
    ],
    a: [0],
    x: '`LEFT OUTER JOIN` con customers como tabla izquierda conserva todos los customers, con o sin pedidos; `RIGHT OUTER JOIN` priorizaría orders (excluyendo customers sin pedidos), `INNER JOIN` excluiría a los customers sin pedidos, y `FULL OUTER JOIN` añadiría filas innecesarias de pedidos sin cliente.',
  },
  {
    n: 13,
    d: 'TRO',
    m: 0,
    q: "A Databricks SQL dashboard refreshes automatically every hour to monitor a new feature's KPIs for the first 7 days of a marketing campaign. Which option reduces the cost of this refresh schedule over time?",
    o: [
      'Manually refresh the dashboard every hour instead of an automatic schedule',
      'Temporarily pause scheduled updates when it has been refreshed recently',
      'Increase the cluster size of the SQL endpoint',
      "Increase the maximum bound of the SQL endpoint's scaling range",
      'Stop automatically updating the dashboard after one week to reduce SQL warehouse cost',
    ],
    a: [4],
    x: 'Como el dashboard solo es necesario durante los primeros 7 días de campaña, detener el refresco automático después de esa semana evita seguir gastando cómputo innecesariamente; aumentar tamaño/rango de escalado incrementaría el coste, no lo reduciría.',
  },
  {
    n: 14,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to transform quarterly sales data into a view with a separate column for each quarter (q1, q2, q3, q4), from a sales table with year, region, quarter, and sales columns. Which SQL command accomplishes this?',
    o: [
      '`CREATE OR REPLACE VIEW sales_across_quarter AS SELECT year, region, q1, q2, q3, q4 FROM sales TRANSFORM (sum(sales) AS sales FOR quarter IN (1 AS q1, 2 AS q2, 3 AS q3, 4 AS q4));`',
      '`CREATE OR REPLACE VIEW sales_across_quarter AS SELECT year, region, q1, q2, q3, q4 FROM sales PIVOT (sum(sales) AS sales FOR quarter IN (1 AS q1, 2 AS q2, 3 AS q3, 4 AS q4));`',
      '`CREATE OR REPLACE VIEW sales_across_quarter AS SELECT year, region, q1, q2, q3, q4 FROM sales REDUCE (sum(sales) AS sales FOR quarter IN (1 AS q1, 2 AS q2, 3 AS q3, 4 AS q4));`',
      '`CREATE OR REPLACE VIEW sales_across_quarter AS SELECT year, region, q1, q2, q3, q4 FROM sales EXIST (sum(sales) AS sales FOR quarter IN (1 AS q1, 2 AS q2, 3 AS q3, 4 AS q4));`',
      '`CREATE OR REPLACE VIEW sales_across_quarter AS SELECT year, region, q1, q2, q3, q4 FROM sales ROTATE (sum(sales) AS sales FOR quarter IN (1 AS q1, 2 AS q2, 3 AS q3, 4 AS q4));`',
    ],
    a: [1],
    x: '`PIVOT` es la cláusula de Spark SQL que rota valores únicos de una columna (aquí, `quarter`) en columnas separadas, aplicando una función de agregación (`sum(sales)`); `TRANSFORM`, `REDUCE`, `EXIST` y `ROTATE` no son cláusulas válidas para este propósito.',
  },
  {
    n: 15,
    d: 'TRO',
    m: 0,
    q: 'A sales team wants an email alert when units sold increases by more than 1000 for any store on a single day. Fill in the blanks:\n\nCreate a ____ query that calculates total units sold for each store every day.\nSet up an ____ with trigger condition total units sold > 1000.\nSchedule the ____ to run every 1 day.\nAdd the destination ____.',
    o: [
      'SQL, Alert, Job, Email Address',
      'SQL, Alarm, Update, Email Address',
      'SQL, Alert, Refresh, Email Address',
      'SQL, Alarm, Renew, Email Address',
      'SQL, Job, Refresh, Email Address',
    ],
    a: [2],
    x: 'Se necesita una query SQL, una Alert que evalúa la condición del umbral, un Refresh diario que reevalúa la query, y una dirección de email como destino; "Alarm"/"Job"/"Renew" no son los términos ni mecanismos correctos de Databricks SQL para alertas.',
  },
  {
    n: 16,
    d: 'TRO',
    m: 0,
    q: 'A manager wants to accurately monitor Databricks costs across different business units, with many jobs running daily, weekly, and monthly, making individual job cost tracking difficult. Which option enables monitoring and tracking costs for various workloads?',
    o: [
      'Use a single cluster for all jobs across business units',
      'Ask the workspace admin to manually monitor DBU usage and prepare an analysis sheet',
      'Create a different workspace for every business unit',
      'Investigate job logs to figure out run time and estimate cost from cluster pricing',
      'Use appropriate tags while creating a cluster used for every job across business units',
    ],
    a: [4],
    x: 'Etiquetar (tags) los clusters al crearlos integra la atribución de coste con los informes de facturación de Databricks, permitiendo desglosar el gasto por unidad de negocio de forma escalable y precisa, sin necesitar workspaces separados ni análisis manual.',
  },
  {
    n: 17,
    d: 'TRA',
    m: 0,
    q: 'Using the same Structured Streaming code block as before, if the engineer wants the query to execute in micro-batches kicked off every 5 minutes, which line fills the blank?',
    o: [
      '`trigger(processingTime=5 minutes)`',
      '`trigger(once=True)`',
      '`trigger(processingTime="5 minutes")`',
      '`trigger(availableNow=True)`',
      '`processingTime(5)`',
    ],
    a: [2],
    x: 'El valor de `processingTime` debe pasarse como cadena entre comillas (`"5 minutes"`); sin comillas es un error de sintaxis, y `once`/`availableNow` no son triggers recurrentes por intervalo.',
  },
  {
    n: 18,
    d: 'TRA',
    m: 0,
    q: 'If you need to associate a numerical value with each key in a SQL table, which data type is most appropriate?',
    o: ['STRUCT', 'ARRAY', 'MAP', 'INTEGER'],
    a: [3],
    x: 'Si el requisito es simplemente asociar un valor numérico por clave dentro de columnas de una tabla SQL, `INTEGER` es el tipo más simple y eficiente; `MAP` sería para pares clave-valor dentro de una única columna, y `STRUCT`/`ARRAY` no encajan con un solo valor numérico.',
  },
  {
    n: 19,
    d: 'TRA',
    m: 0,
    q: 'What is the output of the following Python function?\n\n```\ndef my_func(x, y=1):\n    if x < y:\n        x = 2 * x\n    if x == y:\n        x += 1\n    y -= 2\n    return (x, y)\n\noutput = my_func(1, 2)\n```',
    o: ['(2, 1)', '(1, 2)', '(3, 0)', '(1, 1)', '(2, 2)'],
    a: [2],
    x: 'x=1, y=2: como 1<2, x pasa a ser 2. Luego, como x==y (2==2), x se incrementa a 3. Finalmente y se decrementa en 2 (2-2=0). El resultado es (3, 0).',
  },
  {
    n: 20,
    d: 'TRO',
    m: 0,
    q: 'A data engineer wants to schedule a query in Databricks SQL to run automatically every week to share data with the analysis team. Which approach accomplishes this?',
    o: [
      'Schedule the query to refresh every week from the Workflows UI',
      'Schedule the query to refresh every week from the Dashboards page',
      'Schedule the query to run every week from the Queries page in Databricks SQL',
      'Schedule the query to refresh every week from the Alerts page',
      'There is no way to automatically refresh a query in Databricks SQL',
    ],
    a: [2],
    x: 'Las queries individuales de Databricks SQL se programan directamente desde su propia página, con un schedule semanal; Workflows es para jobs/notebooks, Dashboards refresca visualizaciones (no una query aislada), y Alerts es para notificaciones basadas en condiciones.',
  },
  {
    n: 21,
    d: 'P',
    m: 0,
    q: 'Which of the following is NOT a key feature of the Databricks Lakehouse Platform?',
    o: [
      'Support for ACID transactions',
      'Support for diverse data types ranging from unstructured to structured data',
      'Schema enforcement and governance',
      'Support for diverse workloads like Machine Learning and SQL analytics',
      'Storage is coupled with compute',
    ],
    a: [4],
    x: 'En el lakehouse, almacenamiento y cómputo están desacoplados (no acoplados), lo que permite escalarlos de forma independiente; el acoplamiento es característico de los data warehouses tradicionales, no del lakehouse.',
  },
  {
    n: 22,
    d: 'TRA',
    m: 0,
    q: 'In Spark SQL, what is necessary to perform a PIVOT operation?',
    o: [
      'A DISTINCT clause to ensure unique values',
      'An aggregate function to summarize the data',
      'A LIMIT clause to restrict the number of rows returned',
      'A JOIN clause to merge data from different tables',
    ],
    a: [1],
    x: '`PIVOT` requiere una función de agregación (como `SUM`, `COUNT`, `AVG`) para resumir los valores al rotar filas en columnas; sin ella, Spark SQL no puede determinar cómo combinar múltiples valores en cada celda pivotada.',
  },
  {
    n: 23,
    d: 'CICD',
    m: 0,
    q: "A junior data engineer wants to revert code changes made two weeks ago to a data processing notebook but can't remember all the details. A senior engineer says there's a better solution. What is the senior engineer referring to?",
    o: [
      'Databricks Notebooks support commenting and notification comments',
      'Databricks Notebooks support real-time coauthoring on a single notebook',
      'Databricks Notebooks support automatic change-tracking and versioning',
      'Databricks Notebooks support the use of multiple languages in the same notebook',
      'Databricks Notebooks support the creation of interactive data visualizations',
    ],
    a: [2],
    x: 'El historial de versiones automático de los notebooks permite ver, comparar y restaurar versiones anteriores del código sin depender de recordar manualmente los cambios realizados.',
  },
  {
    n: 24,
    d: 'ING',
    m: 0,
    q: 'A data engineer ingests heart rate recordings in JSON format using Auto Loader and creates a temp view; the column time is inferred as string instead of timestamp. Which code block enforces the correct schema for this column?',
    o: [
      '`.option("cloudFiles.enforceSchema", "time TIMESTAMP")`',
      '`.option("cloudFiles.inferColumnTypes", "time TIMESTAMP")`',
      '`.option("cloudFiles.schemaHints", "time TIMESTAMP")`',
      '`.option("cloudFiles.schemaDetails", "time TIMESTAMP")`',
      '`.option("cloudFiles.schemaHint", "time TIMESTAMP")`',
    ],
    a: [2],
    x: '`cloudFiles.schemaHints` permite forzar el tipo de dato de columnas específicas sobre el esquema inferido por Auto Loader (útil para formatos como JSON/CSV, que por defecto infieren todo como string); `enforceSchema`, `inferColumnTypes`, `schemaDetails` y `schemaHint` (singular) no son opciones válidas.',
  },
  {
    n: 25,
    d: 'ING',
    m: 0,
    q: "A data engineer loads data from external CSV files into a Delta table using `COPY INTO my_table FROM 'dbfs:/mnt/retail/bronze/*.csv' FILEFORMAT = CSV`. After running it successfully once, running it again immediately loads no data. What is the possible reason?",
    o: [
      'Run REFRESH TABLE before running COPY INTO again',
      "Auto Loader should be used since COPY INTO doesn't work with incremental data ingestion",
      'COPY INTO is used to ingest data only once into a Delta table',
      'COPY INTO is idempotent and files in the source location that have already been loaded are skipped',
      'INSERT OVERWRITE should be used instead of COPY INTO',
    ],
    a: [3],
    x: "`COPY INTO` es idempotente: lleva registro de qué archivos ya se cargaron y los omite en ejecuciones posteriores, evitando duplicados; se puede forzar la recarga con `COPY_OPTIONS ('force' = 'true')`.",
  },
  {
    n: 26,
    d: 'TRA',
    m: 0,
    q: 'How can you robustly handle errors in this Python code to avoid unexpected program termination?\n\n```\ndef division(num1, num2):\n  ________\n  result = num1 / num2\n  ________\n  if num2 == 0:\n    print("Cannot divide by 0. Please try again!")\n    result = None\n  return result\n\noutput = division(5, 0)\n```',
    o: [
      '`try:` / `catch:`',
      '`try:` / `error:`',
      '`try:` / `except:`',
      '`try:` / `fail:`',
      '`try:` / `exception:`',
    ],
    a: [2],
    x: 'Python maneja excepciones con el bloque `try:`/`except:` (aquí capturando `ZeroDivisionError`); `catch`, `error`, `fail` y `exception` a secas no son palabras clave válidas en Python (a diferencia de otros lenguajes).',
  },
  {
    n: 27,
    d: 'TRA',
    m: 0,
    q: 'What are the two types of enforced constraints supported on a Delta table in Databricks?',
    o: [
      'NOT NULL, CHECK',
      'PRIMARY KEY, FOREIGN KEY',
      'UNIQUE, NOT NULL',
      'DEFAULT, CHECK',
      'CREATE INDEX, NOT NULL',
    ],
    a: [0],
    x: 'Databricks soporta dos constraints forzados: `NOT NULL` (impide valores nulos) y `CHECK` (una expresión booleana debe cumplirse por fila); `PRIMARY KEY`/`FOREIGN KEY` existen solo como informativos (no forzados), y `UNIQUE`/`DEFAULT`/`CREATE INDEX` no son constraints forzados soportados.',
  },
  {
    n: 28,
    d: 'TRA',
    m: 0,
    q: 'A data engineer is working with two Delta tables having the same schema and wants to find the records that are common and present in both tables. Which SQL command accomplishes this?',
    o: ['MINUS', 'UNION', 'INTERSECT', 'UNION ALL', 'COPY INTO'],
    a: [2],
    x: '`INTERSECT` devuelve únicamente las filas que aparecen en ambas consultas; `UNION`/`UNION ALL` combinan todas las filas (con o sin duplicados), `MINUS` daría la diferencia, y `COPY INTO` no tiene relación con comparar conjuntos de filas.',
  },
  {
    n: 29,
    d: 'TRA',
    m: 0,
    q: 'What are the output modes available in Spark Structured Streaming?',
    o: [
      'Append, Complete, Merge',
      'Merge, Overwrite, Append',
      'Merge, Update, Complete',
      'Overwrite, Append, Update',
      'Append, Complete, Update',
    ],
    a: [4],
    x: 'Los tres modos de salida soportados son Append (solo filas nuevas), Complete (toda la tabla de resultados en cada trigger, para agregaciones) y Update (solo filas actualizadas); "Merge" y "Overwrite" no son modos de salida de Structured Streaming.',
  },
  {
    n: 30,
    d: 'ING',
    m: 0,
    q: 'What is the underlying mechanism used by Auto Loader to incrementally and efficiently process new data files as they arrive in cloud storage?',
    o: ['Delta Live Tables', 'Databricks SQL', 'Multi-hop Architecture', 'COPY INTO', 'Structured Streaming'],
    a: [4],
    x: 'Auto Loader se construye sobre Structured Streaming, mediante la fuente `cloudFiles`, que procesa automáticamente los archivos nuevos según llegan a la ubicación de almacenamiento en la nube.',
  },
  {
    n: 31,
    d: 'ING',
    m: 0,
    q: "An Auto Loader pipeline ingesting hourly data was originally partitioned by year/month/day; a new hour partition column was added recently, causing the pipeline to fail for a few hours before the code was fixed. What should the team do to ingest and reprocess the data that wasn't loaded due to this issue?",
    o: [
      'Manually reload and process the data that got skipped, then trigger the pipeline again',
      'Start ingesting the data from scratch and reprocess everything again',
      'Auto Loader uses checkpointing and write-ahead logs to allow a terminated stream to restart and continue from where it left off, ensuring end-to-end exactly-once semantics under any failure condition',
      'Create a separate Auto Loader pipeline for the new data files arriving in cloud storage',
      'Move the unprocessed data files to another location and manually run queries to process and merge them into the original target location',
    ],
    a: [2],
    x: 'Gracias al checkpointing y los write-ahead logs, Auto Loader puede reanudar automáticamente desde donde se quedó tras un fallo, garantizando exactly-once sin que el equipo tenga que gestionar el estado o reprocesar manualmente.',
  },
  {
    n: 32,
    d: 'P',
    m: 0,
    q: 'What is the normalized unit of processing power on the Databricks Lakehouse Platform used for measurement and pricing of different workloads?',
    o: [
      'Databricks Normalized Unit (DNU)',
      'Databricks Lakehouse Platform Unit (DLPU)',
      'Databricks Unit (DBU)',
      'Databricks Cluster Unit (DCU)',
      'Databricks Standard Unit (DSU)',
    ],
    a: [2],
    x: 'El DBU (Databricks Unit) es la unidad normalizada de potencia de cómputo usada para medir y facturar el consumo de los distintos workloads en la plataforma.',
  },
  {
    n: 33,
    d: 'GOV',
    m: 0,
    q: 'James, owner of the marketing schema, grants Michael the CREATE TABLE privilege, and Michael creates a new table called digital_marketing inside that schema. Who is the owner of digital_marketing?',
    o: [
      'James and Michael are both owners of the new table',
      'James is the owner of the marketing schema, so all tables in it will be owned by him',
      'Michael is the owner of the digital_marketing table',
      'The workspace admin is the owner of the new table by default',
      'The account admin is the owner of the new table by default',
    ],
    a: [2],
    x: 'En Unity Catalog, el creador de un objeto se convierte en su propietario inicial, independientemente de quién sea dueño del esquema contenedor; Michael creó la tabla, así que Michael es su owner.',
  },
  {
    n: 34,
    d: 'GOV',
    m: 0,
    q: 'A data engineer needs to upgrade a schema called retail from the default Hive metastore to the Unity Catalog metastore. Which option inside the Databricks Lakehouse Platform UI accomplishes this?',
    o: ['Workspace', 'Repos', 'Workflows', 'Compute', 'Data Explorer'],
    a: [4],
    x: 'El asistente de actualización dentro de Data Explorer permite copiar esquemas completos y tablas externas desde el Hive metastore por defecto al metastore de Unity Catalog, siguiendo un flujo guiado en la UI.',
  },
  {
    n: 35,
    d: 'GOV',
    m: 0,
    q: 'A new data engineer (peter.parker@mycompany.com) needs full privileges on the products table to fully manage an ELT project. Which command grants full permissions?',
    o: [
      '`GRANT ALL PRIVILEGES ON TABLE products TO peter.parker@mycompany.com;`',
      '`GRANT USAGE TO peter.parker@mycompany.com ON products;`',
      '`GRANT ALL PRIVILEGES TO peter.parker@mycompany.com ON products;`',
      '`GRANT SELECT ON TABLE products TO peter.parker@mycompany.com;`',
      '`GRANT ALL PRIVILEGES ON TABLE peter.parker@mycompany.com TO customers;`',
    ],
    a: [0],
    x: 'La sintaxis correcta es `GRANT privilegios ON objeto TO principal`; `GRANT ALL PRIVILEGES ON TABLE products TO peter.parker@mycompany.com` concede de una vez todos los permisos sobre la tabla, con el orden de cláusulas correcto.',
  },
  {
    n: 36,
    d: 'P',
    m: 0,
    q: 'Which of the following is present in the Control Plane of Databricks Architecture?',
    o: [
      'Databricks Web Application',
      'Databricks File System',
      'Driver Node',
      'Worker Node',
      'None of the above',
    ],
    a: [0],
    x: 'La aplicación web de Databricks reside en el control plane (gestionado por Databricks); DBFS, el driver y los worker nodes forman parte del data plane, donde se ejecuta y almacena el cómputo del cliente.',
  },
  {
    n: 37,
    d: 'TRA',
    m: 0,
    q: 'Using the same Structured Streaming code block, if the engineer does not specify a trigger method, what is the default value of processingTime the system uses for processing the next set of data?',
    o: ['50 seconds', '5 minutes', '500 milliseconds', '0.5 hours', '5000 microseconds'],
    a: [2],
    x: 'Si no se especifica un trigger, Spark usa por defecto `processingTime="500ms"`, procesando automáticamente todos los datos añadidos a la fuente desde el último trigger cada medio segundo.',
  },
  {
    n: 38,
    d: 'TRA',
    m: 0,
    q: 'Given orders (order_id, order_customer_id, items array<int>) and customers (customer_id, customer_name, customer_email), a data engineer wants customer details along with the number of items in each order they placed. Which code block accomplishes this?',
    o: [
      'Using `PIVOT(items) as item_id` in a subquery before joining to customers',
      'Using `FLATTEN(items) as item_id` in a subquery before joining to customers',
      'Using `EXPLODE(items) as item_id` in a subquery before joining to customers, then `COUNT(o.item_id)` grouped by customer and order',
      'Using `COLLECT_SET(items) as item_id` in a subquery before joining to customers',
      'Using `ARRAY_DISTINCT(items) as item_id` in a subquery before joining to customers',
    ],
    a: [2],
    x: '`EXPLODE(items)` convierte cada elemento del array `items` en una fila independiente, permitiendo luego usar `COUNT(item_id)` agrupado por pedido para contar cuántos ítems tiene cada orden; `PIVOT`/`FLATTEN`/`COLLECT_SET`/`ARRAY_DISTINCT` no logran ese desglose fila a fila.',
  },
  {
    n: 39,
    d: 'CICD',
    m: 0,
    q: "A data engineer set up a notebook to run as a Databricks Job. The manager wants to version control the job's schedule due to its complexity. Which approach obtains a version-controllable configuration of the job's schedule?",
    o: [
      'Download the XML description of the Job from the Jobs page',
      'Submit the Job once on a Job cluster',
      'Link the Job to notebooks that are part of a Databricks Repo',
      'Download the JSON description of the Job from the Jobs page',
      'Submit the Job once on an all-purpose cluster',
    ],
    a: [3],
    x: 'Databricks permite descargar la definición completa de un job (incluyendo su schedule) como JSON desde la página del job, que se puede guardar y versionar en un repositorio Git.',
  },
  {
    n: 40,
    d: 'TRO',
    m: 0,
    q: "A dashboard's visualization takes a long time to refresh because one of its underlying queries is slow. Which step helps find the root cause of this issue?",
    o: [
      'Upgrade the existing SQL warehouse to a serverless SQL warehouse',
      'Restart the SQL warehouse and manually refresh the dashboard',
      'Limit the amount of data fetched by the query',
      "Use Query History, click the query's name, then click View Query Profile to see the time spent in each step",
      'Perform Optimization and Z-Ordering on the table used by the query',
    ],
    a: [3],
    x: 'El Query Profile (accesible desde Query History) desglosa visualmente el tiempo, filas y memoria de cada etapa de ejecución de la query, permitiendo identificar exactamente dónde está el cuello de botella antes de aplicar cualquier optimización.',
  },
  {
    n: 41,
    d: 'P',
    m: 0,
    q: 'Where are interactive notebook results stored in the Databricks Architecture?',
    o: [
      'Databricks Web Application',
      'Control Plane',
      'Data Plane',
      'Customer Managed Cloud Storage',
      'Control Plane & Customer Managed Cloud Storage',
    ],
    a: [4],
    x: 'Los resultados de notebooks interactivos se almacenan en una combinación del control plane (resultados parciales para la UI) y el almacenamiento en la nube del cliente; algunos metadatos de los resultados también permanecen en el control plane.',
  },
  {
    n: 42,
    d: 'TRA',
    m: 0,
    q: 'Given an events table with a struct column geo containing city, state, and country subfields, which query filters based on the country subfield?',
    o: [
      "`SELECT geo.country FROM events WHERE geo.country = 'India'`",
      "`SELECT geo:country FROM events WHERE geo:country = 'India'`",
      "`SELECT struct.country FROM events WHERE struct.country = 'India'`",
      "`SELECT geo*country FROM events WHERE geo*country = 'India'`",
      "`SELECT country FROM events WHERE country = 'India'`",
    ],
    a: [0],
    x: 'Los subcampos de un `STRUCT` se acceden con notación de punto (`geo.country`); los dos puntos (`geo:country`) se usan para extraer campos de JSON almacenado como string, no de un STRUCT tipado, y las demás opciones no son sintaxis válida.',
  },
  {
    n: 43,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to convert food grain quantities from Kg to Ton (1 ton = 1000 kg) for each item in a food_grains array-of-struct column (each with food_grain_qty). Which higher-order function fills the blank?\n\n```\nSELECT warehouse_id, food_grains, ____________ as food_grains_qty_in_tons\nFROM food_items;\n```',
    o: [
      '`MAP(food_grains, f -> f.food_grain_qty / 1000)`',
      '`CONVERT(food_grains, f -> f.food_grain_qty / 1000)`',
      '`TRANSFORM(food_grains, f -> f.food_grain_qty / 1000)`',
      '`FLAT_MAP(food_grains, f -> f.food_grain_qty / 1000)`',
      '`REDUCE(food_grains, f -> f.food_grain_qty / 1000)`',
    ],
    a: [2],
    x: '`TRANSFORM(array, lambda)` aplica una función a cada elemento de un array y devuelve un nuevo array con los resultados, ideal para convertir la cantidad de cada elemento de Kg a Ton; `MAP`/`CONVERT`/`FLAT_MAP`/`REDUCE` no son las funciones de orden superior correctas para este caso.',
  },
  {
    n: 44,
    d: 'TRA',
    m: 0,
    q: 'Given an events table where users perform actions (register, search, add_to_cart, checkout, etc.) and items is an array of struct with item_id, a data engineer wants a view with the unique collection of actions and cart items per user. Which query accomplishes this?',
    o: [
      '`SELECT user_id, array_distinct(event_name) AS event_history, flatten(array_distinct(collect_set(items.item_id))) AS cart_history FROM events GROUP BY user_id;`',
      '`SELECT user_id, collect_set(event_name) AS event_history, array_distinct(flatten(collect_set(items.item_id))) AS cart_history FROM events GROUP BY user_id;`',
      '`SELECT user_id, flatten(event_name) AS event_history, collect_set(flatten(array_distinct(items.item_id))) AS cart_history FROM events GROUP BY user_id;`',
      '`SELECT user_id, array_distinct(event_name) AS event_history, collect_set(array_distinct(flatten(items:item_id))) AS cart_history FROM events GROUP BY user_id;`',
      '`SELECT user_id, array_distinct(event_name) AS event_history, collect_set(flatten(explode(items.*))) AS cart_history FROM events GROUP BY user_id;`',
    ],
    a: [1],
    x: '`collect_set(event_name)` agrega los eventos únicos por usuario; para `items.item_id` (array por fila), primero hay que agregar con `collect_set` (que produce un array de arrays) y luego aplanarlo con `flatten`, seguido de `array_distinct` para eliminar duplicados entre filas — exactamente el orden de la opción B.',
  },
  {
    n: 45,
    d: 'TRO',
    m: 0,
    q: "A Databricks SQL query monitors an ELT job by returning the number of hours since its most recent run. Which approach notifies the team if the ELT job hasn't run in a day?",
    o: [
      "Set up an Alert for the accompanying dashboard to notify when it hasn't refreshed in 24 hours",
      'Set up an Alert for the accompanying dashboard to notify if the returned value is greater than 24',
      'Set up an Alert for the query to notify when the ELT job fails',
      'Set up an Alert for the query to notify if the returned value is greater than 24',
      'This type of alerting is not possible in Databricks',
    ],
    a: [3],
    x: 'Las alertas de Databricks SQL se configuran sobre una query (no sobre el dashboard), comparando el valor devuelto contra un umbral; aquí se dispara si el valor (horas desde la última ejecución) supera 24.',
  },
  {
    n: 46,
    d: 'JOBS',
    m: 0,
    q: "Two Jobs run every night: Job 1 starts at 12:00 AM and usually completes in 30 minutes; Job 2 depends on Job 1 and starts at 12:45 AM. Sometimes Job 2 fails because Job 1 hasn't finished by 12:45 AM. Which approach avoids this problem?",
    o: [
      'Use cluster pools to help the jobs run more efficiently',
      'Set up a retry policy on the first job to help it run more quickly',
      'Utilize multiple tasks in a single job with a linear dependency',
      "Limit the size of the output in the second job so it won't fail as easily",
      'Set up the data to stream from the first job to the second job',
    ],
    a: [2],
    x: 'Combinar ambos notebooks como tareas de un único job con dependencia lineal hace que la segunda tarea espere a que la primera termine realmente (en vez de arrancar a una hora fija), eliminando la condición de carrera entre ambos jobs independientes.',
  },
  {
    n: 47,
    d: 'GOV',
    m: 0,
    q: 'What is the top-level container of objects in Unity Catalog that stores data assets and the permissions governing access to them?',
    o: ['Catalog', 'View', 'Schema', 'Metastore', 'Table'],
    a: [3],
    x: 'El metastore es el contenedor de nivel superior en Unity Catalog: almacena los assets de datos (tablas y vistas) y los permisos que gobiernan el acceso a ellos, exponiendo el namespace de tres niveles catalog.schema.table.',
  },
  {
    n: 48,
    d: 'TRA',
    m: 0,
    q: 'Which of the following formats is used to store transaction log files for a table in Delta Lake?',
    o: ['Parquet', 'ORC', 'Text', 'JSON', 'Avro'],
    a: [3],
    x: 'Cada commit del transaction log de Delta Lake (`_delta_log`) se escribe como un archivo JSON ordenado numéricamente (000000.json, 000001.json...); los datos en sí se almacenan en Parquet, pero el log de transacciones va en JSON.',
  },
  {
    n: 49,
    d: 'TRA',
    m: 0,
    q: 'Which method in a Databricks notebook can be used to view the contents of a Spark DataFrame df in a tabular format, with the option of creating embedded visualizations on the data?',
    o: ['view(df)', 'show(df)', 'print(df)', 'take(df)', 'display(df)'],
    a: [4],
    x: '`display(df)` muestra la tabla en formato tabular dentro del notebook y añade opciones integradas de visualización (gráficos, mapas, etc.); `show()`, `print()`, `take()` y `view()` no ofrecen esa capa de visualización embebida en Databricks.',
  },
  {
    n: 50,
    d: 'TRA',
    m: 0,
    q: 'What is the output of the following function when executed with input parameters (1, 3)?\n\n```\ndef check_input(x, y):\n    if x < y:\n        x = x + 1\n    if x > y:\n        x = x + 1\n    if x < y:\n        x = x + 1\n    return x\n```',
    o: ['1', '2', '3', '4', '5'],
    a: [2],
    x: 'x=1, y=3: como 1<3, x pasa a 2. Como 2>3 es falso, x no cambia. Como 2<3, x pasa a 3. El resultado final es 3.',
  },
  {
    n: 51,
    d: 'JOBS',
    m: 0,
    q: 'Two notebooks need to run on a schedule, the second depending on the first, but each needs a different type of compute to run optimally. What is the best way to set these up as jobs?',
    o: [
      'Use Delta Live Tables pipelines instead of notebook tasks',
      'A job can only use a single cluster; set up a separate job for each notebook and use job dependency to link them',
      'Each task can use a different cluster; add both notebooks as two tasks in a single job with linear dependency, modifying the cluster as needed for each task',
      'Use a single job with both notebooks as individual tasks, but use the cluster API to set up the second cluster before the start of the second task',
      'Use a very large cluster to run both tasks in a single job',
    ],
    a: [2],
    x: 'Dentro de un mismo job, cada tarea puede tener su propio cluster configurado; añadir ambos notebooks como dos tareas con dependencia lineal permite adaptar el cómputo de cada una a sus necesidades, sin necesidad de jobs separados ni llamadas manuales a la Cluster API.',
  },
  {
    n: 52,
    d: 'TRA',
    m: 0,
    q: 'What is the output of the following function when executed with input parameters (1, 3)?\n\n```\ndef check_input(x, y):\n    if x < y:\n        x = x + 1\n        if x < y:\n            x = x + 1\n            if x < y:\n                x = x + 1\n    return x\n\ncheck_input(1, 3)\n```',
    o: ['1', '2', '3', '4', '5'],
    a: [2],
    x: 'x=1, y=3: primera condición (1<3) incrementa x a 2. Segunda condición (2<3) incrementa x a 3. Tercera condición (3<3) es falsa, así que x se queda en 3. El resultado es 3.',
  },
  {
    n: 53,
    d: 'TRA',
    m: 0,
    q: 'What would be the output when you run the following query on a table called customers?\n\n```\nSELECT COUNT(DISTINCT *) FROM customers;\n```\n\nSample data in customers:\n\n| customer_id | name | email |\n|---|---|---|\n| 1 | Alice | alice@example.com |\n| 1 | Alice | alice@example.com |\n| 2 | Bob | bob@example.com |',
    o: ['4', '3', '2', '1'],
    a: [2],
    x: '`COUNT(DISTINCT *)` es sintaxis válida en Databricks SQL y cuenta el número de filas distintas (no de valores por columna). Aunque la tabla tiene 3 filas, la fila de Alice está duplicada exactamente, así que solo hay 2 filas distintas.',
  },
];

export const exam19: RawQuestion[] = [
  {
    n: 1,
    d: 'TRA',
    m: 0,
    q: 'A table called customers was registered against a Kafka payload containing binary-encoded JSON values. After casting key and value to String, the value column contains JSON as a string. A data engineer wants to access fields like name, email, country, and age. Which SQL statement accomplishes this?',
    o: [
      '`SELECT key, value.* FROM customers`',
      '`SELECT key, value.name, value.email, value.country, value.age FROM customers`',
      '`SELECT key, EXPLODE(value) FROM customers`',
      '`SELECT key, value:name, value:email, value:country, value:age FROM customers`',
      '`SELECT key, value:* FROM customers`',
    ],
    a: [1],
    x: 'Tras parsear el JSON a struct, se accede a subcampos con notación de punto (`value.name`); `value` sigue siendo STRING hasta que se parsea, y Spark SQL no usa `:` para acceder a structs.',
  },
  {
    n: 2,
    d: 'TRA',
    m: 0,
    q: "You need to synchronize a Delta table 'customer_data' with a staging table 'staging_customer_updates' on 'customer_id', applying inserts and updates but the source never includes deletions. Which approach should you use?",
    o: [
      'Execute DELETE FROM to clear customer_data, then INSERT INTO the entire staging table',
      'Use MERGE INTO to compare customer_data and staging_customer_updates on customer_id, applying updates and inserts as needed',
      'Manually review staging_customer_updates daily with custom SQL scripts',
      'Implement APPLY CHANGES INTO specifying customer_id as the key',
    ],
    a: [1],
    x: '`MERGE INTO` es la operación estándar de Delta Lake para upserts: compara por clave y aplica updates/inserts según corresponda, ideal cuando no hay borrados que gestionar.',
  },
  {
    n: 3,
    d: 'JOBS',
    m: 0,
    q: 'A DLT dataset includes: `CONSTRAINT valid_order EXPECT (order_amount >= 0)`. What is the expected behavior when a batch contains records violating this constraint?',
    o: [
      'Records are added to the target and flagged as invalid in a field added to the target dataset',
      'Records cause the job to fail',
      'Records are dropped and loaded into a quarantine table',
      'Records are added to the target dataset and recorded as invalid in the event log',
      'Records are dropped from the target dataset and recorded as invalid in the event log',
    ],
    a: [3],
    x: 'Sin `ON VIOLATION`, el comportamiento por defecto de `EXPECT` conserva los registros en el dataset destino, registrando la violación en el event log, sin frenar el pipeline.',
  },
  {
    n: 4,
    d: 'TRA',
    m: 0,
    q: 'You need to convert a column of JSON strings to structs with no null fields, using a schema generated from an example JSON string. Which SQL command does this?',
    o: [
      '`CREATE TABLE parsed_data AS SELECT FROM_JSON(json_column) AS struct_column FROM original_table;`',
      "`CREATE OR REPLACE TABLE parsed_events AS SELECT FROM_JSON(json_column, schema_of_json('...')) AS struct_column FROM original_table;`",
      '`CREATE TABLE parsed_data AS SELECT TO_STRUCT(json_column) FROM original_table;`',
      '`CREATE TABLE parsed_data AS SELECT CAST(json_column AS STRUCT) FROM original_table;`',
    ],
    a: [1],
    x: "`schema_of_json('<json_ejemplo>')` genera un esquema explícito a partir de un JSON de muestra, que `FROM_JSON` usa para parsear de forma consistente sin campos nulos por inferencia; `TO_STRUCT` no existe y no se puede castear JSON directamente a STRUCT.",
  },
  {
    n: 5,
    d: 'TRA',
    m: 0,
    q: 'Which of the following is NOT a Higher Order Function in Spark SQL?',
    o: ['FILTER', 'EXIST', 'TRANSFORM', 'MAP', 'REDUCE'],
    a: [1],
    x: '`EXIST` no es una función válida (la función real es `EXISTS`, y de todos modos no se clasifica como higher-order function); `FILTER`, `TRANSFORM`, `MAP` y `REDUCE` sí lo son.',
  },
  {
    n: 6,
    d: 'GOV',
    m: 0,
    q: 'Which command revokes all privileges from the group former_employees on the schema retail?',
    o: [
      '`REVOKE ALL PRIVILEGES FROM former_employees ON SCHEMA retail;`',
      '`REMOVE ALL PRIVILEGES ON SCHEMA retail FROM former_employees;`',
      '`REVOKE ALL PRIVILEGES ON SCHEMA retail FROM former_employees;`',
      "`REMOVE ALL PRIVILEGES FROM 'former_employees' ON SCHEMA retail;`",
      "`DELETE ALL PRIVILEGES ON SCHEMA retail FROM 'former_employees';`",
    ],
    a: [2],
    x: 'La sintaxis correcta es `REVOKE privilegios ON objeto FROM principal`; `REMOVE` y `DELETE` no son comandos válidos para gestionar privilegios.',
  },
  {
    n: 7,
    d: 'ING',
    m: 0,
    q: "An external CSV table 'sales' has new files appended daily, but querying the next day shows the same row count as before. How do you solve this?",
    o: [
      'Check the directory manually, create a temp view, then load into the actual table',
      'Drop and recreate the table daily',
      'Refresh the table to invalidate cache before running the next query',
      'Convert the table to Delta format',
      'Store the table in a cloud-based external system',
    ],
    a: [2],
    x: '`REFRESH TABLE` fuerza a Spark a redescubrir los archivos de una tabla externa cacheada, incorporando los que hayan llegado desde la última consulta.',
  },
  {
    n: 8,
    d: 'TRA',
    m: 0,
    q: 'Which two approaches does Spark use to reliably record the offset range of data processed in each trigger, enabling Structured Streaming to recover from failures?',
    o: [
      'Checkpointing and Idempotent Sinks',
      'Replayable Sources and Idempotent Sinks',
      'Checkpointing and Write-ahead Logs',
      'Structured Streaming cannot record the offset range',
      'Write-ahead Logs and Idempotent Sinks',
    ],
    a: [2],
    x: 'Structured Streaming usa checkpointing (persistir progreso/offsets) junto con write-ahead logs (registrar el rango de offsets antes de ejecutar), garantizando recuperación consistente ante fallos.',
  },
  {
    n: 9,
    d: 'TRA',
    m: 0,
    q: 'What prefix is used in Databricks SQL to query data directly from Parquet files?',
    o: ['DATA', 'PARQUET', 'parquet', 'FILE'],
    a: [2],
    x: 'El prefijo debe ir en minúsculas: `parquet.` (p. ej. `FROM parquet.\\`/mnt/data/file.parquet\\``); Databricks SQL distingue mayúsculas/minúsculas en estos prefijos.',
  },
  {
    n: 10,
    d: 'TRA',
    m: 0,
    q: 'A data engineer creates a table using `CREATE TABLE employees_per_department AS SELECT department_name, COUNT(*) AS employee_count FROM department GROUP BY department_name;`. Which statement is true about CTAS?',
    o: [
      'CTAS statements result in tables where schemas are optional',
      'CTAS statements derive schema details from the query and you must not specify a column_specification',
      'CTAS statements result in tables that do not support schemas',
      'CTAS statements infer the schema by scanning the data',
      'CTAS statements assign all columns the type STRING by default',
    ],
    a: [1],
    x: 'CTAS deriva el esquema automáticamente del resultado de la consulta (nombres y tipos de las expresiones); no se puede especificar una definición de columnas explícita junto con `AS SELECT`.',
  },
  {
    n: 11,
    d: 'TRA',
    m: 0,
    q: 'Which syntax is used in Spark SQL to access subfields in JSON strings?',
    o: ['Semicolon (;)', 'Colon (:)', 'Comma (,)', 'Dot (.)'],
    a: [3],
    x: 'Spark SQL usa notación de punto para navegar subcampos, tanto en JSON parseado como en structs; los dos puntos se usan en JSONPath de otros sistemas, pero en Spark SQL es el punto.',
  },
  {
    n: 12,
    d: 'TRO',
    m: 0,
    q: 'Which of the following statements is incorrect about an all-purpose cluster in Databricks?',
    o: [
      'An all-purpose cluster can be manually terminated when not in use',
      'An all-purpose cluster can be created using the UI, CLI or REST API',
      'An all-purpose cluster can be shared by multiple users for collaborative interactive analysis',
      'An all-purpose cluster can be used to run fast and robust automated jobs',
      'An all-purpose cluster can be manually restarted when required',
    ],
    a: [3],
    x: 'Los clusters all-purpose están pensados para trabajo interactivo y colaborativo; para jobs automatizados y robustos se recomienda un job cluster, más económico y aislado.',
  },
  {
    n: 13,
    d: 'TRO',
    m: 0,
    q: 'An always-on SQL endpoint shows slow performance during periods of high concurrency with many small queries, all directed to the same endpoint. Which method reduces query latency?',
    o: [
      "Turn on Serverless and change the Spot Instance Policy to 'Reliability Optimized'",
      "Increase the maximum bound of the SQL endpoint's scaling range",
      'Turn on the Serverless feature for the SQL endpoint',
      'Increase the cluster size of the SQL endpoint',
      'Turn on the Auto Stop feature for the SQL endpoint',
    ],
    a: [2],
    x: 'Los SQL warehouses Serverless están diseñados para absorber ráfagas de muchas queries pequeñas concurrentes, asignando cómputo bajo demanda y reduciendo la latencia frente a un endpoint clásico siempre encendido.',
  },
  {
    n: 14,
    d: 'TRA',
    m: 0,
    q: 'Which entities are persisted when a Databricks cluster is restarted?',
    o: ['Global Temporary View', 'View', 'Temporary View', 'Table', 'All of the above'],
    a: [3],
    x: 'Las tablas se almacenan de forma persistente (Delta Lake/metastore) independientemente del ciclo de vida del cluster; las vistas temporales (locales y globales) están atadas a la sesión o al cluster y se pierden al reiniciar.',
  },
  {
    n: 15,
    d: 'TRA',
    m: 0,
    q: 'Which statement accurately describes the relationship between Bronze tables and raw data?',
    o: [
      'Bronze tables contain more accurate data than raw data',
      'Bronze tables contain less data than raw data files',
      'Bronze tables contain a less refined view of data than raw data',
      'Bronze tables contain raw data with a schema applied',
      'Bronze tables contain aggregates while raw data is unaggregated',
    ],
    a: [3],
    x: 'Bronze conserva los datos crudos tal cual llegan de la fuente, pero con un esquema aplicado para poder consultarlos, sin limpieza ni agregación adicional.',
  },
  {
    n: 16,
    d: 'GOV',
    m: 0,
    q: "A data engineer creates a table `customersInUSA` from a SELECT on Locations WHERE country='USA'. Company guidelines require any table storing PII to be marked with a specific table property. Which snippet meets this requirement?",
    o: [
      "`TBLPROPERTIES ('PII'='true')`",
      "`COMMENT 'PII'`",
      '`PII`',
      '`COMMENT "Contains PII"`',
      'Marking a table as containing PII is not supported',
    ],
    a: [0],
    x: '`TBLPROPERTIES` permite añadir metadatos clave-valor personalizados a una tabla, ideal para marcar de forma consultable programáticamente que contiene PII; `COMMENT` solo añade texto descriptivo, sin ser una propiedad consultable de forma estructurada.',
  },
  {
    n: 17,
    d: 'JOBS',
    m: 0,
    q: 'When a data engineer needs to monitor the performance and status of Delta Live Tables (DLT) pipelines, which option allows them to query the event log for detailed metrics and update history?',
    o: [
      'Directly accessing the DLT UI for real-time metrics without querying',
      'Querying the Delta Lake table directly for pipeline metrics without using the event log',
      'Using SELECT * FROM event_log to retrieve all event log entries',
      'Creating a temporary view on the event log path and querying this view for specific metrics',
    ],
    a: [3],
    x: 'El event log de DLT es en sí una tabla Delta; la práctica recomendada es crear una vista temporal sobre su ruta y consultarla con filtros/agregaciones específicas, en vez de traer todos los registros crudos.',
  },
  {
    n: 18,
    d: 'TRA',
    m: 0,
    q: 'What does ACID stand for in the context of Delta Lake transactions on Databricks?',
    o: [
      'Atomic, Consistent, Isolated, Durable',
      'Atomicity, Consistency, Isolation, Durability',
      'Automatic, Consistent, Integrated, Durable',
      'Automated, Consistency, Isolation, Dynamic',
    ],
    a: [1],
    x: 'La terminología precisa es Atomicity, Consistency, Isolation, Durability; las demás opciones mezclan términos incorrectos o no relacionados con las garantías transaccionales.',
  },
  {
    n: 19,
    d: 'TRA',
    m: 0,
    q: 'In order for Structured Streaming to reliably track the exact progress of processing so it can handle failures by restarting/reprocessing, which two approaches does Spark use to record the offset range of data processed in each trigger?',
    o: [
      'Checkpointing and Write-ahead Logs',
      'Replayable Sources and Idempotent Sinks',
      'Checkpointing and Idempotent Sinks',
      'Structured Streaming cannot record the offset range',
      'Write-ahead Logs and Idempotent Sinks',
    ],
    a: [0],
    x: 'Checkpointing persiste el progreso y estado, y los write-ahead logs registran el rango de offsets antes de procesarlos, permitiendo reanudar de forma consistente tras un fallo.',
  },
  {
    n: 20,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to normalize text data by creating logic that converts all strings to lowercase and removes spaces. Which approach should they take within Databricks?',
    o: [
      'Utilize a Databricks built-in function for string normalization in a Spark SQL query',
      'Manually edit each string in the dataset before loading it into Databricks',
      'Implement a UDF in Databricks that lowercases and trims spaces, applying it via a Spark SQL query',
      'Export the dataset, process it with a script outside Databricks, and re-import the cleaned data',
    ],
    a: [0],
    x: 'Funciones nativas como `lower()` y `regexp_replace()` cubren esta normalización sin el overhead de una UDF; se recomienda usar funciones built-in siempre que sea posible en vez de UDFs personalizadas.',
  },
  {
    n: 21,
    d: 'TRO',
    m: 0,
    q: "A manager checks a Databricks SQL dashboard every morning for the previous day's revenue and orders, but currently refreshes it manually each day. Which approach ensures the dashboard updates automatically each day?",
    o: [
      'Schedule the dashboard to refresh every 1 day from the Alerts page',
      'Schedule the dashboard to refresh every 1 day from the Workflows UI',
      'Schedule the dashboard to refresh every 1 day from the SQL Warehouses page',
      'Schedule the dashboard to run every 1 day from the Queries page',
      'Schedule the dashboard to refresh every 1 day from the Dashboards page',
    ],
    a: [4],
    x: 'Los dashboards de Databricks SQL tienen su propia programación de refresco, configurable directamente desde la página del Dashboard, sin necesidad de Workflows ni de tocar cada query por separado.',
  },
  {
    n: 22,
    d: 'CICD',
    m: 0,
    q: 'Which of the following operations can only be implemented through a Git provider when using Databricks Repos?',
    o: [
      'Commit and push to feature branch',
      'Create a new branch based on main branch',
      'Create and edit code',
      'Pull request and review process',
      'Clone remote repository to user folder',
    ],
    a: [3],
    x: 'Los pull requests y el proceso de revisión de código se gestionan en el proveedor Git externo (GitHub, GitLab, Bitbucket); commits, push, creación de ramas y clonado sí se pueden hacer directamente desde Databricks Repos.',
  },
  {
    n: 23,
    d: 'GOV',
    m: 0,
    q: "How does the 'Share' functionality within the Catalog framework in Databricks facilitate collaboration?",
    o: [
      'By restricting access to data objects to a single user',
      'By creating duplicate copies of data objects for each user',
      'By enabling data objects in the Catalog to be shared with designated recipients',
      'By preventing any sharing of data objects to maintain data isolation',
    ],
    a: [2],
    x: 'La funcionalidad Share de Unity Catalog permite conceder permisos específicos sobre objetos de datos (tablas, vistas, volúmenes, funciones) a usuarios, grupos o service principals concretos, sin duplicar los datos.',
  },
  {
    n: 24,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to run the last part of a Python script only if day_of_week is 1 and review_period is True. Which line of code should they use?',
    o: [
      '`if day_of_week = 1 & review_period: = "True"`',
      '`if day_of_week = 1 and review_period = "True"`',
      '`if day_of_week = 1 and review_period`',
      '`if day_of_week == 1 and review_period`',
      '`if day_of_week == 1 and review_period == "True"`',
    ],
    a: [3],
    x: '`==` es el operador de comparación correcto en Python (no `=`), y como `review_period` ya es booleano, se evalúa directamente sin compararlo con la cadena `"True"`.',
  },
  {
    n: 25,
    d: 'GOV',
    m: 0,
    q: 'John Smith in Marketing currently has read access to sales tables but not update access. Which command grants him update access?',
    o: [
      '`GRANT UPDATE ON TABLE table_name TO john.smith@marketing.com`',
      '`GRANT USAGE ON TABLE table_name TO john.smith@marketing.com`',
      '`GRANT MODIFY ON TABLE table_name TO john.smith@marketing.com`',
      '`GRANT UPDATE TO TABLE table_name ON john.smith@marketing.com`',
      '`GRANT MODIFY TO TABLE table_name ON john.smith@marketing.com`',
    ],
    a: [2],
    x: 'En Unity Catalog, el privilegio `MODIFY` engloba insert/update/delete; `UPDATE` no existe como privilegio independiente, y `USAGE` solo permite referenciar el objeto sin modificar datos.',
  },
  {
    n: 26,
    d: 'P',
    m: 0,
    q: 'You are storing highly unstructured survey data that changes over time. Why is a Lakehouse a better choice compared to a Data Warehouse for this scenario?',
    o: [
      'Lakehouse supports schema enforcement and evolution; traditional data warehouses lack schema evolution',
      'Lakehouse supports SQL',
      'Lakehouse supports ACID',
      'Lakehouse enforces data integrity',
      'Lakehouse supports primary and foreign keys like a data warehouse',
    ],
    a: [0],
    x: 'Los warehouses tradicionales exigen un esquema rígido definido de antemano (schema-on-write); el lakehouse permite un enfoque schema-on-read más flexible junto con evolución de esquema, ideal para datos no estructurados y cambiantes.',
  },
  {
    n: 27,
    d: 'TRO',
    m: 0,
    q: 'Which of the following statements is correct about cluster pools?',
    o: [
      'Cluster pools allow you to perform load balancing',
      'Cluster pools allow you to create a cluster',
      'Cluster pools allow you to save time when starting a new cluster',
      'Cluster pools are used to share resources among multiple teams',
      'Cluster pools allow you to have all the nodes in the cluster from a single physical server rack',
    ],
    a: [2],
    x: 'Los cluster pools mantienen instancias inactivas y listas, reduciendo el tiempo de arranque al crear un cluster nuevo a partir del pool en vez de aprovisionar VMs desde cero.',
  },
  {
    n: 28,
    d: 'TRO',
    m: 0,
    q: 'Once a cluster is deleted, what additional actions need to be performed by the administrator?',
    o: [
      'Remove virtual machines but storage and networking are automatically dropped',
      'Drop storage disks but virtual machines and networking are automatically dropped',
      'Remove networking but virtual machines and storage disks are automatically dropped',
      'Remove logs',
      'No action needs to be performed; all resources are automatically removed',
    ],
    a: [4],
    x: 'Al terminar/eliminar un cluster, Databricks limpia automáticamente VMs, almacenamiento y red asociados; no se requiere ninguna acción manual del administrador.',
  },
  {
    n: 29,
    d: 'CICD',
    m: 0,
    q: 'Two team members are looking to modify the same notebook, and one is copying the notebook to a personal folder to edit and replace the collaboration notebook. Which feature makes this easier?',
    o: [
      'Copy notebooks to a local machine and set up source control locally',
      'Databricks notebooks support automatic change tracking and versioning',
      'Databricks Notebooks support real-time coauthoring on a single notebook',
      'Databricks notebooks can be exported into dbc archive files and stored in a data lake',
      'Databricks notebook can be exported as HTML and imported at a later time',
    ],
    a: [2],
    x: 'La coautoría en tiempo real permite que varias personas editen el mismo notebook simultáneamente viendo los cambios del otro en vivo, evitando copias manuales y conflictos de fusión.',
  },
  {
    n: 30,
    d: 'TRO',
    m: 0,
    q: "You query `SELECT * FROM table_name TIMESTAMP AS OF date_sub(current_date(), 1)` to review yesterday's version, but you can no longer access historical data even though DESCRIBE HISTORY shows an update yesterday. What is the likely reason?",
    o: [
      'You currently do not have access to view historical data',
      'By default, historical data is cleaned every 180 days in Delta',
      'A command VACUUM table_name RETAIN 0 was run on the table',
      'Time travel is disabled',
      'Time travel must be enabled before you query previous data',
    ],
    a: [2],
    x: '`VACUUM ... RETAIN 0` elimina inmediatamente todos los archivos históricos, dejando sin datos disponibles para el time travel aunque el log de transacciones siga mostrando el historial de operaciones.',
  },
  {
    n: 31,
    d: 'TRA',
    m: 0,
    q: 'Which of the following statements can query a Delta table in the PySpark DataFrame API?',
    o: [
      '`Spark.read.mode("delta").table("table_name")`',
      '`Spark.read.table.delta("table_name")`',
      '`Spark.read.table("table_name")`',
      '`Spark.read.format("delta").LoadTableAs("table_name")`',
      '`Spark.read.format("delta").TableAs("table_name")`',
    ],
    a: [2],
    x: '`spark.read.table("table_name")` es el método correcto y directo para leer una tabla registrada (Delta u otro formato) como DataFrame; las demás opciones no son APIs válidas.',
  },
  {
    n: 32,
    d: 'TRA',
    m: 0,
    q: 'When you drop an external Delta table using `DROP TABLE table_name`, how does it impact metadata (delta log, history) and data stored in storage?',
    o: [
      'Drops table from metastore, metadata (delta log, history) and data in storage',
      'Drops table from metastore, data but keeps metadata (delta log, history) in storage',
      'Drops table from metastore, metadata (delta log, history) but keeps the data in storage',
      'Drops table from metastore, but keeps metadata (delta log, history) and data in storage',
      'Drops table from metastore and data in storage but keeps metadata',
    ],
    a: [3],
    x: 'Al hacer DROP de una tabla externa, solo se elimina la entrada del metastore; tanto los archivos de datos como el transaction log (`_delta_log`, historial) permanecen intactos en su ubicación.',
  },
  {
    n: 33,
    d: 'TRA',
    m: 0,
    q: 'Which of the following commands can be used to query a Delta table?',
    o: [
      '`%python` / `spark.sql("select * from table_name")`',
      '`%sql` / `Select * from table_name`',
      'Both A & B',
      '`%python` / `execute.sql("select * from table")`',
      '`%python` / `delta.sql("select * from table")`',
    ],
    a: [2],
    x: 'Tanto `spark.sql()` desde una celda `%python` como una consulta directa en una celda `%sql` son formas válidas de consultar una tabla Delta; `execute.sql`/`delta.sql` no son métodos existentes.',
  },
  {
    n: 34,
    d: 'GOV',
    m: 0,
    q: 'How do you check the location of an existing schema in Delta Lake?',
    o: [
      'Run SQL command SHOW LOCATION schema_name',
      'Check Unity Catalog UI',
      'Use Data Explorer',
      'Run SQL command DESCRIBE SCHEMA EXTENDED schema_name',
      'Schemas are internally stored in external hive metastores like MySQL or SQL Server',
    ],
    a: [3],
    x: '`DESCRIBE SCHEMA EXTENDED schema_name` es el comando oficial que expone metadatos detallados del esquema, incluida su ubicación de almacenamiento; `SHOW LOCATION` no es sintaxis válida.',
  },
  {
    n: 35,
    d: 'TRA',
    m: 0,
    q: 'You have a JSON temp_data table with a raw column recording Chicago temperatures every 4 hours. Calculate the maximum temperature recorded at 12:00 PM across all days.',
    o: [
      '`select max(raw.chicago.temp[3]) from temp_data`',
      '`select array_max(raw.chicago[*].temp[3]) from temp_data`',
      "`select array_max(from_json(raw['chicago'].temp[3], 'array<int>')) from temp_data`",
      "`select array_max(from_json(raw:chicago[*].temp[3], 'array<int>')) from temp_data`",
      "`select max(from_json(raw:chicago[3].temp[3], 'array<int>')) from temp_data`",
    ],
    a: [3],
    x: 'Hay que parsear el JSON con `from_json` usando notación de dos puntos (`raw:chicago[*].temp[3]`) para extraer el array de temperaturas de las 12:00 PM de todos los días, y luego aplicar `array_max`.',
  },
  {
    n: 36,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to replace the schema name and table name dynamically in a Python query string. Which Python statement correctly does this?',
    o: [
      '`query = f"select * from schema_name.table_name"`',
      '`query = "select * from {schema_name}.{table_name}"`',
      '`query = f"select * from {schema_name}.{table_name}"`',
      '`query = f"select * from " + schema_name + "." + table_name`',
    ],
    a: [2],
    x: 'Un f-string con las variables entre llaves (`f"select * from {schema_name}.{table_name}"`) interpola correctamente los valores; sin el prefijo `f` las llaves se toman literalmente, y usando texto literal `schema_name.table_name` sin `f` tampoco se interpola.',
  },
  {
    n: 37,
    d: 'TRA',
    m: 0,
    q: "You need to check two conditions: department is 'supply chain', and process flag is True. Which line of code correctly checks both?",
    o: [
      '`if department = "supply chain" & process:`',
      '`if department == "supply chain" && process:`',
      '`if department == "supply chain" and process == TRUE:`',
      '`if department == "supply chain" & if process == TRUE:`',
      '`if department == "supply chain" and process:`',
    ],
    a: [4],
    x: '`==` para comparar, `and` para combinar condiciones, y evaluar `process` directamente (ya es booleano) sin compararlo con `TRUE` (Python usa `True`, no `TRUE`).',
  },
  {
    n: 38,
    d: 'TRA',
    m: 0,
    q: 'Which of the following techniques does Structured Streaming use to ensure recovery from failures during stream processing?',
    o: [
      'Checkpointing and Watermarking',
      'Write ahead logging and watermarking',
      'Checkpointing and write-ahead logging',
      'Delta time travel',
      'The stream will failover to available nodes in the cluster',
      'Checkpointing and Idempotent sinks',
    ],
    a: [2],
    x: 'Structured Streaming garantiza tolerancia a fallos combinando checkpointing (persistir offsets/estado) con write-ahead logs, permitiendo reanudar el procesamiento sin pérdida ni duplicación de datos.',
  },
  {
    n: 39,
    d: 'TRA',
    m: 0,
    q: 'Which statement correctly reads a notebook widget and passes the Python variable into a SQL statement in a Python notebook cell?',
    o: [
      '`spark.sql(f"SELECT * FROM sales WHERE orderDate = \'f{order_date}\'")`',
      '`spark.sql(f"SELECT * FROM sales WHERE orderDate = \'order_date\' ")`',
      '`spark.sql(f"SELECT * FROM sales WHERE orderDate = \'${order_date}\' ")`',
      '`spark.sql(f"SELECT * FROM sales WHERE orderDate = \'{order_date}\' ")`',
      '`spark.sql("SELECT * FROM sales WHERE orderDate = order_date")`',
    ],
    a: [3],
    x: 'Un f-string de Python con `{order_date}` entre comillas simples interpola correctamente el valor de la variable en la cadena SQL, que luego se ejecuta con `spark.sql()`.',
  },
  {
    n: 40,
    d: 'TRA',
    m: 0,
    q: 'You want to build a Spark Structured Streaming process to read from Kafka and write to a Delta table every 15 minutes. What is the correct trigger option?',
    o: [
      '`trigger("15 minutes")`',
      '`trigger(process "15 minutes")`',
      '`trigger(processingTime = 15)`',
      '`trigger(processingTime = "15 minutes")`',
      '`trigger(15)`',
    ],
    a: [3],
    x: 'El trigger de micro-batch a intervalo fijo se especifica con `trigger(processingTime = "15 minutes")`, pasando el intervalo como cadena de texto legible.',
  },
  {
    n: 41,
    d: 'JOBS',
    m: 0,
    q: 'You need to query a table based on a batch date parameter that changes every run. What is the best way to parameterize the query without manually changing the batch date?',
    o: [
      'Create a notebook parameter for batch date, assign it to a Python variable, and use a Spark DataFrame to filter based on that variable',
      'Create a dynamic view that calculates the batch date automatically',
      'There is no way to combine a Python variable and Spark code',
      'Manually edit code every time to change the batch date',
      'Store the batch date in the Spark configuration and filter based on the Spark configuration',
    ],
    a: [0],
    x: 'Un parámetro de notebook (widget) permite pasar el valor de batch date en cada ejecución programada del job, asignarlo a una variable Python y usarlo para filtrar el DataFrame, sin editar código manualmente.',
  },
  {
    n: 42,
    d: 'ING',
    m: 0,
    q: 'You need to ingest data incrementally with Auto Loader, where the schema is expected to change over time and should be handled automatically. Fill in the blanks:\n\n```\nspark.readStream\n  .format("cloudfiles")\n  .option("_______", "csv")\n  .option("_______", "dbfs:/location/checkpoint/")\n  .load(data_source)\n  .writeStream\n  .option("_______", "dbfs:/location/checkpoint/")\n  .option("_______", "true")\n  .table(table_name)\n```',
    o: [
      '`format`, `checkpointlocation`, `schemalocation`, `overwrite`',
      '`cloudfiles.format`, `checkpointlocation`, `cloudfiles.schemalocation`, `overwrite`',
      '`cloudfiles.format`, `cloudfiles.schemalocation`, `checkpointlocation`, `mergeSchema`',
      '`cloudfiles.format`, `cloudfiles.schemalocation`, `checkpointlocation`, `overwrite`',
      '`cloudfiles.format`, `cloudfiles.schemalocation`, `checkpointlocation`, `append`',
    ],
    a: [2],
    x: 'Se necesitan `cloudfiles.format`, `cloudfiles.schemalocation`, `checkpointlocation` y `mergeSchema` para que la evolución de esquema se aplique automáticamente; `overwrite` y `append` no gestionan cambios de esquema.',
  },
  {
    n: 43,
    d: 'ING',
    m: 0,
    q: 'Auto Loader is used to process millions of files a day; scaling up the Databricks cluster did not improve performance. What is the best way to resolve this?',
    o: [
      'Auto Loader is not suitable to process millions of files a day',
      'Set up a second Auto Loader process to process the data',
      'Increase the maxFilesPerTrigger option to a sufficiently high number',
      'Copy the data from cloud storage to local disk on the cluster for faster access',
      'Merge files into one large file',
    ],
    a: [2],
    x: 'El valor por defecto de `maxFilesPerTrigger` (1000) limita cuántos archivos se procesan por micro-batch; aumentarlo reduce el overhead de descubrimiento por archivo y aprovecha mejor un cluster más grande.',
  },
  {
    n: 44,
    d: 'TRA',
    m: 0,
    q: 'Which of the following techniques does Structured Streaming use to create end-to-end fault tolerance?',
    o: [
      'Checkpointing and Watermarking',
      'Write ahead logging and watermarking',
      'Checkpointing and idempotent sinks',
      'Write ahead logging and idempotent sinks',
      'The stream will failover to available nodes in the cluster',
    ],
    a: [2],
    x: 'La tolerancia a fallos end-to-end exactly-once se logra combinando checkpointing (progreso/estado recuperable) con sinks idempotentes (evitan duplicados al reescribir el mismo dato).',
  },
  {
    n: 45,
    d: 'ING',
    m: 0,
    q: 'Which two options are supported for identifying the arrival of new files and incremental data from cloud object storage using Auto Loader?',
    o: [
      'Directory listing, File notification',
      'Checkpointing, watermarking',
      'Write ahead logging, read ahead logging',
      'File hashing, dynamic file lookup',
      'Checkpointing and write ahead logging',
    ],
    a: [0],
    x: 'Auto Loader detecta archivos nuevos mediante Directory Listing (listando el directorio) o File Notification (usando servicios de notificación de la nube), los dos modos oficialmente soportados.',
  },
  {
    n: 46,
    d: 'TRA',
    m: 0,
    q: 'What is the purpose of the silver layer in a Multi-hop architecture?',
    o: [
      'Replaces a traditional data lake',
      'Efficient storage and querying of full, unprocessed history of data',
      'Eliminates duplicate data, quarantines bad data',
      'Refined views with aggregated data',
      'Optimized query performance for business-critical data',
    ],
    a: [2],
    x: 'Silver limpia y estandariza los datos: elimina duplicados y pone en cuarentena los datos de mala calidad, preservando la granularidad original sin agregar (eso corresponde a Gold).',
  },
  {
    n: 47,
    d: 'JOBS',
    m: 0,
    q: 'A DLT Pipeline is configured to run in Development mode using Triggered Pipeline Mode. What is the expected outcome after clicking Start to update the pipeline?',
    o: [
      'All datasets are updated once and the pipeline shuts down; compute resources are terminated',
      'All datasets are updated at set intervals until shut down; compute is deployed for the update and terminated when stopped',
      'All datasets are updated at set intervals until shut down; compute persists after the pipeline is stopped for additional development/testing',
      'All datasets are updated once and the pipeline shuts down; compute resources persist to allow for additional development and testing',
      'All datasets are updated continuously and the pipeline does not shut down; compute persists with the pipeline',
    ],
    a: [3],
    x: 'En modo Development con Triggered Pipeline Mode, se realiza una única actualización y luego se detiene, pero el cómputo permanece activo (a diferencia de producción) para facilitar la depuración y pruebas iterativas.',
  },
  {
    n: 48,
    d: 'JOBS',
    m: 0,
    q: 'A DLT Pipeline is configured to run in Production mode using Continuous Pipeline Mode. What is the expected outcome after clicking Start to update the pipeline?',
    o: [
      'All datasets are updated once and the pipeline shuts down; compute resources are terminated',
      'All datasets are updated at set intervals until shut down; compute is deployed for the update and terminated when the pipeline is stopped',
      'All datasets are updated at set intervals until shut down; compute persists after the pipeline is stopped for additional testing',
      'All datasets are updated once and the pipeline shuts down; compute persists to allow for additional testing',
      'All datasets are updated continuously and the pipeline does not shut down; compute persists with the pipeline',
    ],
    a: [1],
    x: 'En modo continuo de producción, el pipeline procesa datos de forma continua hasta que se detiene explícitamente; el cómputo se despliega durante la ejecución y se libera al pararlo, optimizando coste en producción.',
  },
  {
    n: 49,
    d: 'JOBS',
    m: 0,
    q: 'What are the different ways you can schedule a job in a Databricks workspace?',
    o: [
      'Continuous, Incremental',
      'On-Demand runs, File notification from Cloud object storage',
      'Cron, On-Demand runs, File notification from Cloud object storage',
      'Cron, File notification from Cloud object storage',
      'Once, Continuous',
    ],
    a: [2],
    x: 'Databricks Jobs se pueden programar con expresiones Cron, ejecutar bajo demanda (On-Demand), o activarse mediante notificaciones de archivos nuevos en almacenamiento en la nube.',
  },
  {
    n: 50,
    d: 'TRO',
    m: 0,
    q: 'A dashboard takes a long time to load because each visualization contains a large amount of data. Which approach addresses this issue?',
    o: [
      'Increase size of the SQL endpoint cluster',
      'Increase the scale of the maximum range of the SQL endpoint cluster',
      'Use a Databricks SQL query filter to limit the amount of data in each visualization',
      'Remove data from Delta Lake',
      'Use Delta cache to store intermediate results',
    ],
    a: [2],
    x: 'Filtrar la query para acotar el volumen de datos devuelto a cada visualización (p. ej. por rango de fechas) es la solución directa y recomendada frente a simplemente escalar cómputo o cachear.',
  },
  {
    n: 51,
    d: 'GOV',
    m: 0,
    q: 'John Smith in Marketing has read access to sales tables but not delete access. Which command grants him this access?',
    o: [
      '`GRANT USAGE ON TABLE table_name TO john.smith@marketing.com`',
      '`GRANT DELETE ON TABLE table_name TO john.smith@marketing.com`',
      '`GRANT DELETE TO TABLE table_name ON john.smith@marketing.com`',
      '`GRANT MODIFY TO TABLE table_name ON john.smith@marketing.com`',
      '`GRANT MODIFY ON TABLE table_name TO john.smith@marketing.com`',
    ],
    a: [4],
    x: '`MODIFY` es el privilegio compuesto que incluye insert/update/delete en Databricks Unity Catalog y las tablas ACL heredadas; `DELETE` no es un privilegio independiente soportado.',
  },
  {
    n: 52,
    d: 'GOV',
    m: 0,
    q: 'A team member is leaving and owns several tables. You want to transfer ownership to a group instead of an individual, so anyone in the group can manage permissions in the future. Which command accomplishes this?',
    o: [
      "`ALTER TABLE table_name OWNER to 'group'`",
      "`TRANSFER OWNER table_name to 'group'`",
      "`GRANT OWNER table_name to 'group'`",
      "`ALTER OWNER ON table_name to 'group'`",
      "`GRANT OWNER On table_name to 'group'`",
    ],
    a: [0],
    x: "`ALTER TABLE table_name OWNER TO 'group'` es la sintaxis válida en Databricks SQL para transferir la propiedad de una tabla, incluso a un grupo; `TRANSFER OWNER`/`GRANT OWNER` no son comandos válidos.",
  },
  {
    n: 53,
    d: 'TRO',
    m: 0,
    q: 'A team uses a SQL query to review data completeness every day to monitor an ETL job, with the query output used in multiple dashboards. Which approach sets up a schedule to automate this?',
    o: [
      'Schedule the query to run every day from the Jobs UI',
      "Schedule the query to refresh every day from the query's page in Databricks SQL",
      'Schedule the query to run every 12 hours from the Jobs UI',
      "Schedule the query to refresh every day from the SQL endpoint's page",
      "Schedule the query to refresh every 12 hours from the SQL endpoint's page",
    ],
    a: [1],
    x: 'Cada query de Databricks SQL se programa directamente desde su propia página (Refresh Schedule), lo que mantiene los dashboards que la consumen actualizados sin necesidad de un job independiente.',
  },
];

export const exam20: RawQuestion[] = [
  {
    n: 1,
    d: 'TRO',
    m: 0,
    q: 'Which of the following statements is incorrect about autoscaling a cluster in Databricks?',
    o: [
      'Workloads can run faster compared to a constant-sized under-provisioned cluster',
      'Autoscaling clusters can reduce overall costs compared to a statically-sized cluster',
      'You need to configure the Min Workers and Max Workers to enable autoscaling for a cluster',
      "Autoscaling makes it easier to achieve high cluster utilization, because you don't need to provision the cluster to match a workload",
      'Cluster size cannot go below the minimum number of workers selected while enabling autoscaling',
    ],
    a: [4],
    x: 'Un cluster con autoscaling sí puede reducir su tamaño por debajo del mínimo configurado cuando está inactivo (llegando incluso a terminarse si el auto-termination está habilitado); el mínimo aplica durante carga activa, no de forma absoluta.',
  },
  {
    n: 2,
    d: 'TRA',
    m: 0,
    q: 'How do you create a permanent view in Databricks SQL that references data from a CSV file stored in DBFS?',
    o: [
      'Use CREATE VIEW followed by a SELECT query that uses the csv prefix to reference the CSV file',
      'Permanent views cannot reference CSV files directly; they must be loaded into a table first',
      'Use CREATE VIEW with the USING csv option followed by the path to the CSV file',
      'Use CREATE PERMANENT VIEW and specify the CSV file path directly after the FROM keyword',
    ],
    a: [0],
    x: 'Databricks SQL permite consultar archivos directamente con el prefijo `csv.` (p. ej. `FROM csv.\\`dbfs:/path/file.csv\\``); envolviendo esa consulta en `CREATE VIEW` se obtiene una vista permanente sin necesidad de cargar los datos primero en una tabla.',
  },
  {
    n: 3,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to create an unmanaged table called inventory using an existing directory path dbfs:/user/username/retail. Which SQL statement creates this external table?',
    o: [
      "`CREATE OR REPLACE TABLE inventory USING DELTA LOCATION 'dbfs:/user/username/retail'`",
      "`CREATE TABLE inventory USING DELTA LOCATION 'dbfs:/user/username/retail'`",
      "`CREATE TABLE inventory AS DELTA LOCATION 'dbfs:/user/username/retail'`",
      "`CREATE UNMANAGED TABLE inventory USING DELTA LOCATION 'dbfs:/user/username/retail'`",
      "`CREATE inventory LOCATION 'dbfs:/user/username/retail'`",
    ],
    a: [1],
    x: "`CREATE TABLE ... USING DELTA LOCATION '...'` crea automáticamente una tabla externa (no managed) apuntando al directorio existente; `UNMANAGED TABLE` y `AS DELTA` no son sintaxis válida.",
  },
  {
    n: 4,
    d: 'JOBS',
    m: 0,
    q: 'A DLT pipeline with STREAMING LIVE TABLE and LIVE TABLE datasets is configured for Production mode with Continuous Pipeline Mode. What is the expected outcome after clicking Start, assuming unprocessed data exists?',
    o: [
      'All datasets are updated once and the pipeline persists without processing; compute persists unused',
      'All datasets are updated at set intervals until shut down; compute is deployed for the update and terminated when stopped',
      'All datasets are updated once and the pipeline shuts down; compute is terminated',
      'All datasets are updated once and the pipeline shuts down; compute persists for additional testing',
      'All datasets are updated at set intervals until shut down; compute persists for additional testing',
    ],
    a: [1],
    x: 'En modo continuo de producción, el pipeline procesa continuamente hasta detenerse explícitamente; el cómputo se asigna durante la ejecución y se libera al pararlo.',
  },
  {
    n: 5,
    d: 'TRO',
    m: 0,
    q: "An attempt to use Delta Lake's time travel feature to revert a table to its state from three days ago failed because the data files had already been deleted. What is the reason for the missing data files?",
    o: [
      'The TIME TRAVEL feature was utilized on the table',
      'The HISTORY command was run on the table',
      'The OPTIMIZE command was applied to the table',
      'The table was cleaned using the VACUUM command',
      'The DELETE HISTORY command was executed on the table',
    ],
    a: [3],
    x: '`VACUUM` elimina permanentemente archivos de datos antiguos según el periodo de retención (7 días por defecto); si ese periodo es más corto que la ventana de time travel deseada, los archivos necesarios ya no existen.',
  },
  {
    n: 6,
    d: 'TRO',
    m: 0,
    q: 'A data analyst set up an alert for when a query that loads aggregated sales data contains duplicate records, and wants to include the number of duplicate records in the email notification. Which option achieves this?',
    o: [
      'Set up an alert using a custom template and include QUERY_RESULT_ROWS',
      'Set up an alert using the default template and include the specific information in that',
      'Set up an alert using a custom template and include QUERY_RESULT_COLS',
      'Use Python libraries to send the email when the alert is triggered and customize the details',
      'Set up an alert using a custom template and include QUERY_RESULT_VALUE',
    ],
    a: [4],
    x: 'La variable de plantilla `QUERY_RESULT_VALUE` inserta el valor numérico real devuelto por la query (aquí, el conteo de duplicados) directamente en la notificación personalizada.',
  },
  {
    n: 7,
    d: 'CICD',
    m: 0,
    q: 'Which of the following operations is done through Databricks Repos?',
    o: [
      'Merge into main branch',
      'Git automation calls Databricks Repos API',
      'Pull request and review process',
      'Create a new branch based on main branch',
      'Databricks Repos API call to bring Repo in Production folder to the latest version',
    ],
    a: [0],
    x: 'Databricks Repos soporta operaciones Git interactivas nativas como el merge a la rama principal; los pull requests y su revisión se gestionan en el proveedor Git externo, no dentro de Databricks.',
  },
  {
    n: 8,
    d: 'JOBS',
    m: 0,
    q: 'Which of the following statements is incorrect about a job cluster in Databricks?',
    o: [
      'Databricks job scheduler creates a job cluster when you run a job',
      'Job cluster can be created using the UI, CLI or REST API',
      'Job cluster terminates when the job is completed',
      'Job cluster cannot be restarted',
      'Job cluster is used to run fast and robust automated jobs',
    ],
    a: [3],
    x: 'Aunque un job cluster ya terminado no se puede reiniciar tal cual, decir que "no se puede reiniciar" de forma absoluta es engañoso: al relanzar el job, Databricks aprovisiona un job cluster nuevo automáticamente; esta es la afirmación incorrecta entre las opciones.',
  },
  {
    n: 9,
    d: 'TRO',
    m: 0,
    q: 'VACUUM has a default retention threshold of 7 days. The audit team requires certain tables to maintain at least 365 days of retention for compliance. Which setting implements this?',
    o: [
      "`ALTER TABLE table_name set TBLPROPERTIES (delta.deletedFileRetentionDuration = 'interval 365 days')`",
      "`MODIFY TABLE table_name set TBLPROPERTY (delta.maxRetentionDays = 'interval 365 days')`",
      "`ALTER TABLE table_name set EXTENDED TBLPROPERTIES (delta.deletedFileRetentionDuration = 'interval 365 days')`",
      "`ALTER TABLE table_name set EXTENDED TBLPROPERTIES (delta.vaccum.duration = 'interval 365 days')`",
    ],
    a: [0],
    x: 'La propiedad soportada es `delta.deletedFileRetentionDuration`, configurada con `ALTER TABLE ... SET TBLPROPERTIES`; `maxRetentionDays` y `vaccum.duration` no son propiedades válidas, y `EXTENDED TBLPROPERTIES` no es sintaxis correcta.',
  },
  {
    n: 10,
    d: 'TRA',
    m: 0,
    q: 'Which of the following functions can be used to convert a JSON string into a Struct data type in Databricks?',
    o: [
      '`TO_STRUCT(json value)`',
      '`FROM_JSON(json value)`',
      '`FROM_JSON(json value, schema of json)`',
      '`CONVERT(json value, schema of json)`',
      '`CAST(json value as STRUCT)`',
    ],
    a: [2],
    x: '`FROM_JSON` requiere tanto el string JSON como el esquema para parsear correctamente a `STRUCT`; sin el esquema, o usando funciones inexistentes como `TO_STRUCT`/`CONVERT`, no es posible.',
  },
  {
    n: 11,
    d: 'TRA',
    m: 0,
    q: 'Which command can be used to get a list of all active Spark streams currently running, so they can be stopped?',
    o: [
      '`Spark.getActiveStreams()`',
      '`spark.streams.active`',
      '`activeStreams()`',
      '`getActiveStreams()`',
      '`spark.streams.getActive`',
    ],
    a: [1],
    x: '`spark.streams.active` es la propiedad soportada que devuelve la lista de streaming queries activas, que se pueden recorrer y parar con `.stop()`.',
  },
  {
    n: 12,
    d: 'CICD',
    m: 1,
    q: 'A team using notebook versioning with Git integration is considering switching to Databricks Repos. Which reasons justify the switch? (Select two)',
    o: [
      'Databricks Repos allows multiple users to make changes',
      'Databricks Repos allows merge and conflict resolution',
      'Databricks Repos has a built-in version control system',
      'Databricks Repos automatically saves changes',
      'Databricks Repos allows you to add comments and select the changes you want to commit',
    ],
    a: [0, 4],
    x: 'Repos permite colaboración multiusuario con ramas de Git, y ofrece un flujo tipo Git donde se pueden seleccionar cambios específicos para confirmar con mensajes de commit; el merge/resolución de conflictos ocurre en el proveedor Git externo, no dentro de Repos.',
  },
  {
    n: 13,
    d: 'GOV',
    m: 0,
    q: "You need to implement fine-grained access control to rows and columns of a Delta table in Databricks, based on the user's identity. Which approach should you use?",
    o: [
      'Use Unity Catalog to grant access to rows and columns',
      'Row and column access control lists',
      'Use dynamic view functions',
      'Data access control lists',
      'Dynamic Access control lists with Unity Catalog',
    ],
    a: [2],
    x: 'Las vistas dinámicas (usando funciones como `current_user()` en cláusulas condicionales) son el mecanismo soportado para filtrar filas o enmascarar columnas según la identidad del usuario; Unity Catalog gestiona permisos a nivel de objeto, no filtrado fila/columna.',
  },
  {
    n: 14,
    d: 'GOV',
    m: 0,
    q: 'Kevin owns both the sales table and the regional_sales_vw view built on it. Kevin wants to grant SELECT privilege on the view to Steven, a new team member. What is true?',
    o: [
      'Kevin cannot grant access to Steven since he does not have security admin privilege',
      'Kevin, although the owner, does not have ALL PRIVILEGES permission',
      'Kevin can grant access to the view, because he is the owner of the view and the underlying table',
      'Kevin cannot grant access to Steven since he does not have workspace admin privilege',
      'Steven will also require SELECT access on the underlying table',
    ],
    a: [2],
    x: 'Como propietario tanto de la vista como de la tabla subyacente, Kevin tiene control total y puede conceder el privilegio `SELECT` sobre la vista a Steven sin necesitar roles de administrador adicionales.',
  },
];

export const exam21: RawQuestion[] = [
  {
    n: 1,
    d: 'GOV',
    m: 0,
    q: 'A provider organization uses a Unity Catalog-enabled Databricks workspace to share data with multiple recipients. What happens if a recipient is deleted from the Unity Catalog metastore?',
    o: [
      'The recipient retains access to the data until the share itself is deleted',
      'The recipient immediately loses access to all shares they previously had',
      'The recipient can still access the data using their existing credentials for a limited time',
      'The recipient loses access only if the provider deletes the share',
    ],
    a: [1],
    x: 'Eliminar al recipient del metastore revoca de inmediato todo acceso a los shares asociados con él (tablas, vistas, volúmenes, modelos), sin necesidad de borrar el share en sí.',
  },
  {
    n: 2,
    d: 'JOBS',
    m: 0,
    q: 'A data platform team needs to choose appropriate cluster types for various use cases in Databricks. Which of the following statements is correct?',
    o: [
      'A job cluster is ideal for scheduled ETL pipelines because it is created for the job run and terminates automatically after completion',
      'An interactive (all-purpose) cluster is designed for non-interactive scheduled jobs only and cannot be shared by multiple users',
      'A single-user cluster allows multiple users to run notebooks concurrently while isolating permissions',
      'A shared cluster restricts usage to one user only for strict data access control',
    ],
    a: [0],
    x: 'Un job cluster se crea automáticamente para la ejecución de un job y se termina al completarse, siendo la opción idónea (eficiente y aislada) para pipelines ETL programados.',
  },
  {
    n: 3,
    d: 'GOV',
    m: 0,
    q: 'A new data engineer, alex.smith@dataco.com, needs full control over the transactions table to manage ingestion, transformation, and updates. Which command correctly grants full access?',
    o: [
      '`GRANT ALL PRIVILEGES ON TABLE transactions TO alex.smith@dataco.com;`',
      '`GRANT USAGE ON TABLE transactions TO alex.smith@dataco.com;`',
      '`GRANT ALL PRIVILEGES ON TABLE alex.smith@dataco.com TO transactions;`',
      '`GRANT SELECT ON TABLE transactions TO alex.smith@dataco.com;`',
    ],
    a: [0],
    x: '`GRANT ALL PRIVILEGES ON TABLE ... TO ...` concede de una vez SELECT, INSERT, UPDATE y DELETE; `USAGE` no da acceso a los datos, y `SELECT` por sí solo es demasiado limitado para el requisito.',
  },
  {
    n: 4,
    d: 'JOBS',
    m: 0,
    q: "A DLT constraint ensures all transaction amounts are non-negative. What is the consequence of using 'ON VIOLATION DROP ROW' for this constraint?",
    o: [
      'The entire DLT job will fail, stopping data processing',
      'The rows violating the constraint will be omitted from the output',
      'The rows violating the constraint will be updated with default values',
      'An alert will be triggered, but the violating rows will remain in the dataset',
    ],
    a: [1],
    x: '`ON VIOLATION DROP ROW` descarta silenciosamente los registros que incumplen la expectation antes de escribirlos en el dataset destino, permitiendo que el pipeline continúe con los datos válidos.',
  },
  {
    n: 5,
    d: 'JOBS',
    m: 0,
    q: 'Which statement correctly describes the default behaviors of development mode versus production mode in Databricks Delta Live Tables (DLT) pipelines?',
    o: [
      'In development mode, the cluster remains active after updates for rapid testing, while in production mode the cluster shuts down immediately after job completion',
      'Production mode allows overriding cluster configurations on each run for fast iteration, while development mode uses locked, pre-defined compute for consistency',
      'Both development and production modes keep the cluster alive post-update to support debugging',
      'In development mode, scheduled pipeline triggers and concurrent job runs are prevented, while in production mode they are always enabled',
    ],
    a: [0],
    x: 'El modo Development mantiene el cluster activo tras cada actualización para facilitar la iteración rápida; el modo Production prioriza fiabilidad y coste, apagando el cómputo inmediatamente al terminar.',
  },
  {
    n: 6,
    d: 'GOV',
    m: 0,
    q: 'A team is setting up a new data pipeline on Databricks using Unity Catalog. What should they do to simplify data layout decisions and optimize query performance?',
    o: [
      'Enable predictive optimization',
      'Disable AQE',
      'Rely on default configurations without adjustments',
      'Avoid using Unity Catalog managed tables',
    ],
    a: [0],
    x: 'Predictive Optimization gestiona automáticamente compactación de archivos, data skipping y estadísticas para tablas managed de Unity Catalog, simplificando el layout de datos sin ajustes manuales.',
  },
  {
    n: 7,
    d: 'CICD',
    m: 0,
    q: 'A data engineer needs to deploy a set of Databricks resources, including jobs and pipelines, consistently across multiple environments. Which Databricks feature should be used to programmatically define and manage these resources?',
    o: ['Databricks Asset Bundles', 'Structured Streaming', 'Delta Lake', 'Git folders'],
    a: [0],
    x: 'Databricks Asset Bundles permiten declarar jobs, pipelines y otros recursos en YAML versionado, desplegándolos de forma repetible y consistente entre entornos (dev/test/prod).',
  },
  {
    n: 8,
    d: 'ING',
    m: 0,
    q: 'A company uses Auto Loader to ingest CSV files into Delta Lake and needs exactly-once processing even after failures. Where does Auto Loader store metadata to support this requirement?',
    o: [
      'In the Delta Lake metadata',
      'In a relational database',
      'In a key-value store at the checkpoint location',
      'In an external NoSQL database',
    ],
    a: [2],
    x: 'Auto Loader persiste el estado y los metadatos de archivos descubiertos en un almacén clave-valor (basado en RocksDB) dentro de la ubicación de checkpoint, garantizando exactly-once tras reinicios o fallos.',
  },
  {
    n: 9,
    d: 'JOBS',
    m: 0,
    q: 'A data engineer is setting up an ETL pipeline using Delta Live Tables (DLT) and needs to enforce constraints on incoming data quality. Which feature of DLT should they use?',
    o: ['Data expectations', 'Data deduplication', 'Schema inference', 'Data partitioning'],
    a: [0],
    x: 'Las expectations de DLT son el mecanismo diseñado específicamente para definir reglas/restricciones de calidad de datos sobre los registros que fluyen por el pipeline.',
  },
  {
    n: 10,
    d: 'GOV',
    m: 1,
    q: 'A Delta table is marked as a managed table within Databricks. What implications does this have for the control and management of the underlying data files? (Select two)',
    o: [
      'The data files are stored in a location managed by Databricks, and users do not have control over the physical storage path',
      'The data files can be directly accessed and modified in their storage location by users',
      'The user must specify the exact location of data files during table creation',
      'Data files are automatically deleted when the managed table is dropped',
    ],
    a: [0, 3],
    x: 'Databricks controla la ubicación de almacenamiento de una tabla managed (el usuario no la elige) y, al eliminarla, borra automáticamente tanto los metadatos como los archivos de datos subyacentes.',
  },
  {
    n: 11,
    d: 'GOV',
    m: 0,
    q: 'Which of the following is NOT a feature provided by Unity Catalog in Databricks?',
    o: ['Access Control', 'Data Lineage', 'Real-time Streaming', 'Data Discovery'],
    a: [2],
    x: 'Unity Catalog se centra en gobernanza (control de acceso, linaje, descubrimiento de datos); el procesamiento de streaming en tiempo real es una capacidad de Spark/Structured Streaming, no de Unity Catalog.',
  },
  {
    n: 12,
    d: 'GOV',
    m: 0,
    q: 'A data engineer needs to ensure only specific columns from a table are shared with a partner organization using Delta Sharing. Which action should be taken?',
    o: [
      'Create a view with the required columns and share the view',
      'Share the entire table and instruct the partner to ignore unnecessary columns',
      'Use Unity Catalog to restrict columns during sharing',
      'Create a new table with only the required columns and share it',
    ],
    a: [0],
    x: 'Delta Sharing no permite filtrar columnas directamente sobre una tabla completa; la práctica soportada es crear una vista con solo las columnas necesarias y compartir esa vista.',
  },
  {
    n: 13,
    d: 'GOV',
    m: 0,
    q: 'A company wants to ensure sensitive data is only accessible to specific users within their Databricks Lakehouse Platform. Which strategy should they use to manage data access effectively?',
    o: [
      'Assign broad privileges at the catalog level to simplify access management',
      'Use external location securable objects to manage access to cloud storage',
      'Implement the principle of least privilege by granting minimum necessary permissions at the required level',
      'Create multiple Hive metastores to isolate data access for different teams',
    ],
    a: [2],
    x: 'El principio de mínimo privilegio (conceder solo los permisos estrictamente necesarios, al nivel adecuado) es la estrategia de gobernanza recomendada para proteger datos sensibles en Unity Catalog.',
  },
  {
    n: 14,
    d: 'GOV',
    m: 0,
    q: 'Which of the following is a primary advantage of using Delta Sharing in Databricks for data governance?',
    o: [
      'It allows real-time data updates to be pushed to recipients',
      'It enhances data security and privacy through integrated access control and auditing',
      'It automatically transforms data into formats compatible with recipient platforms',
      'It provides mechanisms for data deduplication before sharing',
    ],
    a: [1],
    x: 'Delta Sharing se integra con Unity Catalog para aportar control de acceso granular, auditoría y trazabilidad centralizada sobre los datos compartidos, incluso con partners externos.',
  },
  {
    n: 15,
    d: 'GOV',
    m: 0,
    q: 'What is a primary advantage of using Databricks-to-Databricks Delta Sharing over open sharing with bearer tokens?',
    o: [
      'It allows for write access to shared datasets',
      'It eliminates the need for token-based credentials for secure access',
      'It allows sharing with recipients without Databricks workspaces',
      'It provides faster data transfer speeds',
    ],
    a: [1],
    x: 'En el sharing Databricks-a-Databricks, la identidad y autenticación se gestionan de forma nativa dentro de la plataforma, eliminando la necesidad de distribuir y rotar tokens bearer manualmente.',
  },
  {
    n: 16,
    d: 'TRO',
    m: 0,
    q: 'A data engineering team wants to migrate from classic SQL warehouses to serverless to reduce cost and improve scalability, moving workloads gradually. Which is the best first step?',
    o: [
      'Move the least frequent dashboard and ad-hoc analytics workloads to serverless first to test performance and cost savings',
      'Migrate all Python notebooks immediately to serverless to reduce manual job cost',
      'Move Scala-based ETL pipelines first since serverless is optimized for Scala workloads',
      'Replace production-critical dashboards with serverless to realize maximum savings upfront',
    ],
    a: [0],
    x: 'La mejor práctica es empezar con cargas de trabajo poco frecuentes y no críticas para validar rendimiento y ahorro de costes antes de migrar workloads de producción de alta frecuencia.',
  },
  {
    n: 17,
    d: 'ING',
    m: 0,
    q: 'A data pipeline handles real-time data ingestion using Auto Loader. Which is a unique feature of Auto Loader that provides a significant advantage over traditional file source ingestion methods?',
    o: [
      'Auto Loader can automatically infer schema changes and apply them without any manual intervention',
      'Auto Loader provides exactly-once processing guarantees without any additional configuration',
      'Auto Loader can scale to accommodate high volumes of data using cloud-native optimizations',
      'Auto Loader requires the data to be pre-partitioned before ingestion',
    ],
    a: [2],
    x: 'Auto Loader aprovecha mecanismos nativos de la nube (listado eficiente o notificaciones de archivo) para escalar a grandes volúmenes de datos sin cuellos de botella, su ventaja diferencial frente a la ingesta de archivos tradicional.',
  },
  {
    n: 18,
    d: 'CICD',
    m: 0,
    q: 'In the context of Databricks Asset Bundles, which command should a data engineer use to create a YAML configuration file for existing resources?',
    o: [
      '`databricks bundle init`',
      '`databricks bundle validate`',
      '`databricks bundle generate`',
      '`databricks bundle deploy`',
    ],
    a: [2],
    x: '`databricks bundle generate` escanea recursos existentes y produce el YAML de configuración correspondiente; `init` crea un proyecto nuevo desde cero, `validate` comprueba sintaxis, y `deploy` aplica una configuración ya existente.',
  },
  {
    n: 19,
    d: 'TRO',
    m: 0,
    q: 'A Databricks pipeline runs very slowly due to memory spills. What is the most cost-effective action to fix the pipeline performance?',
    o: [
      'Enable autoscaling on the cluster',
      'Enable Photon',
      'Upgrade to memory-optimized workers/drivers',
      'Manually increase the cluster size without autoscaling',
    ],
    a: [0],
    x: 'El autoscaling añade recursos dinámicamente solo cuando se necesitan, mitigando los memory spills de forma más económica que aumentar permanentemente el tamaño del cluster o usar instancias optimizadas en memoria.',
  },
  {
    n: 20,
    d: 'JOBS',
    m: 0,
    q: 'A single-task workflow that loads data into a Delta table fails due to a temporary connectivity issue that has since been resolved, with no code changes needed. What is the best way to re-run the job?',
    o: [
      'Use the Repair run to retry the failed task',
      'Use the Run Now button to manually trigger a new run',
      'Clone the notebook and create a new workflow',
      'Delete the failed run and create a new job definition',
    ],
    a: [1],
    x: 'Para un workflow de una sola tarea sin cambios de código, simplemente disparar una nueva ejecución con "Run Now" es la forma más simple y eficiente; "Repair" está pensado para workflows multi-tarea.',
  },
  {
    n: 21,
    d: 'GOV',
    m: 0,
    q: "Which command successfully revokes the CREATE TABLE privilege from a group named 'analysts' on schema 'reporting' within catalog 'finance'?",
    o: [
      "`REVOKE CREATE TABLE ON TABLE finance.reporting FROM 'analysts';`",
      "`REVOKE CREATE TABLE ON SCHEMA finance.reporting FROM 'analysts';`",
      "`REVOKE CREATE ON SCHEMA finance.reporting FROM 'analysts';`",
      "`REVOKE ALL PRIVILEGES ON SCHEMA finance.reporting FROM 'analysts';`",
    ],
    a: [1],
    x: '`CREATE TABLE` es un privilegio a nivel de esquema, no de tabla; la sintaxis correcta y mínima es `REVOKE CREATE TABLE ON SCHEMA ... FROM ...`, sin revocar otros privilegios innecesariamente.',
  },
  {
    n: 22,
    d: 'TRO',
    m: 0,
    q: 'A Databricks cluster configured for a batch ETL job with multiple joins and aggregations frequently causes OOM errors due to insufficient memory. Which cluster configuration strategy should be prioritized?',
    o: [
      'Increase the number of workers to distribute the load',
      'Switch to a single node with a large instance type',
      'Use general-purpose instances that support Photon',
      'Increase the size of the instances and use fewer workers',
    ],
    a: [3],
    x: 'Los errores OOM ocurren cuando cada executor no tiene suficiente memoria; usar instancias más grandes (más memoria por nodo) con menos workers resuelve el problema directamente, mientras que solo añadir más workers pequeños no lo hace.',
  },
  {
    n: 23,
    d: 'TRO',
    m: 0,
    q: 'Which cluster configuration is most suitable for a pipeline with data union operations across multiple large datasets, where minimizing data shuffle is a priority?',
    o: [
      'Configure a cluster with a large number of small instances',
      'Use a single-node cluster with a very large node type',
      'Select storage-optimized instances with local storage and disk caching enabled',
      'Increase instance size and decrease the number of workers',
    ],
    a: [3],
    x: 'Menos nodos pero más grandes reduce el movimiento de datos por red entre nodos (shuffle) en operaciones de unión sobre datasets grandes, frente a muchas instancias pequeñas que aumentan el tráfico de red.',
  },
  {
    n: 24,
    d: 'GOV',
    m: 0,
    q: 'An organization needs to ensure all data lineage information is accurately captured in Databricks. Which service name relates to tracking data lineage events in the audit logs?',
    o: ['dataMonitoring', 'lineageTracking', 'databrickssql', 'database'],
    a: [1],
    x: '`lineageTracking` es el nombre de servicio específico en los audit logs de Databricks para capturar eventos de linaje de datos.',
  },
  {
    n: 25,
    d: 'ING',
    m: 0,
    q: 'A data engineer needs to process new files as they arrive in a cloud storage directory. Which feature should they use to efficiently handle this task?',
    o: ['Lakeflow Declarative Pipelines', 'Auto Loader', 'Delta Lake', 'Databricks Terraform provider'],
    a: [1],
    x: 'Auto Loader está diseñado específicamente para detectar e ingerir de forma incremental y eficiente archivos nuevos según llegan a almacenamiento en la nube.',
  },
  {
    n: 26,
    d: 'TRA',
    m: 0,
    q: 'Given a DataFrame with columns Region, Product, Sales, which code correctly returns the total sales for each region?',
    o: [
      '`df.select("region").agg(sum("sales").alias("total_sales")).show()`',
      '`df.groupBy("region").agg(sum("sales").alias("total_sales")).show()`',
      '`df.groupBy("region").sum("sales").alias("total_sales").show()`',
      '`df.groupBy().agg(sum("sales")).show()`',
    ],
    a: [1],
    x: '`groupBy("region").agg(sum("sales").alias(...))` agrupa correctamente por región y calcula el total por grupo; `select` sin `groupBy` no agrupa, y aplicar `.alias()` tras `.sum()` directamente sobre el DataFrame es sintaxis inválida.',
  },
  {
    n: 27,
    d: 'TRO',
    m: 0,
    q: 'A job on a job cluster fails with an error related to output size. What is the maximum notebook output size supported by Databricks job clusters?',
    o: ['10 MB', '15 MB', '25 MB', '30 MB'],
    a: [3],
    x: 'El límite máximo de salida de un notebook en un job cluster de Databricks es 30 MB; salidas mayores deben escribirse a almacenamiento en la nube en vez de devolverse directamente como output del notebook.',
  },
  {
    n: 28,
    d: 'TRA',
    m: 0,
    q: 'A data engineer drops a managed Delta table named customer_orders using `DROP TABLE customer_orders;`. What happens to metadata and data files?',
    o: [
      'Only the metadata is dropped; the underlying data files remain in storage',
      'Both the metadata and the data files are deleted permanently',
      'The metadata is moved to a trash folder; data remains accessible',
      'The metadata is deleted, but the data is automatically backed up to Unity Catalog',
    ],
    a: [1],
    x: 'Para una tabla managed, Databricks controla tanto los metadatos como los archivos de datos; al hacer DROP, ambos se eliminan de forma permanente.',
  },
  {
    n: 29,
    d: 'TRO',
    m: 0,
    q: 'A managed Delta table has Predictive I/O Optimization enabled, but query performance is still poor for selective filters on high-cardinality columns. What is the most effective next step?',
    o: [
      'Convert the table to an external table and re-run the query',
      'Enable Z-Ordering on frequently filtered columns',
      'Enable Liquid Clustering and define clustering keys',
      'Disable Predictive Optimization and re-enable it manually after running OPTIMIZE',
    ],
    a: [2],
    x: 'Liquid Clustering está diseñado para optimizar consultas sobre columnas de alta cardinalidad, donde Z-Ordering es menos efectivo; gestiona dinámicamente el layout de archivos para reducir el escaneo de datos.',
  },
  {
    n: 30,
    d: 'ING',
    m: 0,
    q: 'A data engineer uses Auto Loader to incrementally ingest JSON files from cloud storage. Which configuration should be used to stop the pipeline and fail immediately if new columns are detected, until the team investigates?',
    o: [
      '`.option("cloudFiles.schemaEvolutionMode", "none")`',
      '`.option("cloudFiles.schemaEvolutionMode", "failOnNewColumn")`',
      '`.option("cloudFiles.inferColumnChanges", "true")`',
      '`.option("mergeSchema", "false")`',
    ],
    a: [1],
    x: '`schemaEvolutionMode = "failOnNewColumn"` fuerza al pipeline a fallar inmediatamente ante columnas nuevas no aprobadas, exactamente el control estricto requerido; `"none"` ignoraría las columnas sin fallar.',
  },
  {
    n: 31,
    d: 'CICD',
    m: 0,
    q: 'A Databricks workspace is integrated with GitHub. What is the best approach to promote and deploy workloads to production in a repeatable, automated way?',
    o: [
      'Manually copy notebooks and workflows to the production environment',
      'Use GitHub Actions to deploy a Databricks Asset Bundle (DAB) using the Databricks CLI',
      'Use Terraform to export and apply notebook source files directly without Git',
      'Export notebooks as HTML and archive them in the production environment',
    ],
    a: [1],
    x: 'Un pipeline de CI/CD con GitHub Actions desplegando un Databricks Asset Bundle vía CLI es el flujo reproducible, auditable y automatizado recomendado para promocionar recursos a producción.',
  },
  {
    n: 32,
    d: 'ING',
    m: 0,
    q: 'A data engineer uses Auto Loader to incrementally load JSON files, ensuring only newly arrived files are processed. Which option should be configured?',
    o: [
      'Set `cloudFiles.inferColumnTypes` to `true`',
      'Use the `cloudFiles.maxFilesPerTrigger` option',
      'Enable the `cloudFiles.validationMode` option',
      'Configure `cloudFiles.includeExistingFiles` to `false`',
    ],
    a: [3],
    x: '`cloudFiles.includeExistingFiles = false` hace que Auto Loader ignore los archivos ya presentes al iniciar el stream, procesando solo los que lleguen a partir de ese momento.',
  },
  {
    n: 33,
    d: 'CICD',
    m: 0,
    q: "A data engineer is debugging a Python script in a Databricks notebook that processes a Spark DataFrame and produces unexpected output. What is the most effective way to identify issues using Databricks' debugging tools?",
    o: [
      'Add breakpoints where the DataFrame is created and transformed, and use the debugger to inspect variable values',
      'Use print statements to output the values of the DataFrame at different stages',
      'Reload the entire notebook to ensure all changes are applied and rerun the script',
      'Enable the Databricks Assistant to automatically correct errors in the script',
    ],
    a: [0],
    x: 'El depurador interactivo integrado en los notebooks Python de Databricks permite fijar breakpoints e inspeccionar variables paso a paso, siendo la forma más efectiva de aislar el problema.',
  },
  {
    n: 34,
    d: 'TRA',
    m: 0,
    q: 'A data engineer is implementing a data pipeline using the Medallion Architecture. In which layer would they typically create a star schema for analytics purposes?',
    o: ['Bronze layer', 'Silver layer', 'Gold layer', 'Data Lake layer'],
    a: [2],
    x: 'Gold es la capa orientada a negocio donde se modelan esquemas analíticos (estrella/copo de nieve) optimizados para dashboards y herramientas de BI.',
  },
  {
    n: 35,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to ensure data is cleansed and validated before being available for analytics and reporting, following the Medallion Architecture. Which layer should they focus on?',
    o: ['Bronze Layer', 'Silver Layer', 'Gold Layer', 'Raw Layer'],
    a: [1],
    x: 'Silver es responsable de limpiar, deduplicar, validar e integrar los datos crudos de Bronze, produciendo datasets conformados listos para modelarse en Gold.',
  },
  {
    n: 36,
    d: 'ING',
    m: 0,
    q: 'A data engineer uses Lakeflow Declarative Pipelines and Auto Loader for streaming data. What is one significant advantage of using Auto Loader for this task?',
    o: [
      'Auto Loader requires manual intervention to detect new data files',
      'Auto Loader can only process data from on-premise storage systems',
      'Auto Loader provides automatic schema inference and evolution',
      'Auto Loader processes data in batch mode only',
    ],
    a: [2],
    x: 'Auto Loader infiere automáticamente el esquema de los archivos nuevos y se adapta a cambios de esquema, siendo una de sus ventajas clave para ingesta continua.',
  },
  {
    n: 37,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to calculate average revenue per customer from a sales DataFrame with columns customer_id, transaction_id, amount. Which PySpark function should be used?',
    o: [
      "`groupBy(['customer_id']).agg({'amount': 'avg'})`",
      "`select(['customer_id', 'avg(amount)'])`",
      "`groupBy('transaction_id').avg('amount')`",
      "`withColumn('avg_revenue', avg('amount'))`",
    ],
    a: [0],
    x: "Agrupar por `customer_id` y aplicar `agg({'amount': 'avg'})` calcula correctamente el promedio de ingresos por cliente; agrupar por `transaction_id` o usar `select`/`withColumn` sin agregación no logra el resultado requerido.",
  },
  {
    n: 38,
    d: 'GOV',
    m: 0,
    q: 'A company wants to securely share a subset of their Delta tables with an external partner using Delta Sharing within Unity Catalog. What is a prerequisite step before they can share these tables?',
    o: [
      'Ensure the tables are stored in a Unity Catalog-enabled metastore and that Delta Sharing is enabled for the metastore by an account admin',
      'Enable Structured Streaming in the existing workspace',
      "Create external tables in the partner's workspace",
      "Bind cloud storage access to the partner's workspace",
    ],
    a: [0],
    x: 'Las tablas deben residir en un metastore habilitado para Unity Catalog, y Delta Sharing debe activarse explícitamente para ese metastore por un account admin antes de poder compartir nada.',
  },
  {
    n: 39,
    d: 'GOV',
    m: 0,
    q: 'A data engineering team needs to share Delta tables with an external partner who does not use Databricks. Which Delta Sharing protocol should be used?',
    o: [
      'Databricks-to-Databricks sharing protocol',
      'Databricks open sharing protocol',
      'Customer-managed open-source Delta Sharing server',
      'HTTP-based REST API',
    ],
    a: [1],
    x: 'El protocolo Open Sharing de Delta Sharing está diseñado justamente para compartir datos con destinatarios que no usan Databricks, mediante conectores abiertos (Spark, pandas, Power BI, etc.).',
  },
  {
    n: 40,
    d: 'GOV',
    m: 0,
    q: 'A data engineer needs to share data with a partner company using a different Databricks account on Azure, where the partner also has Unity Catalog enabled. Which method should be used?',
    o: [
      'Use Delta Sharing via Databricks-to-Databricks sharing',
      'Use open-source Delta Sharing with token-based authentication',
      'Use a direct network connection between the two Databricks workspaces',
      "Use Unity Catalog's built-in sharing capabilities between different accounts",
    ],
    a: [0],
    x: 'Cuando ambas partes tienen Unity Catalog habilitado, el sharing Databricks-a-Databricks ofrece la integración más segura y gobernada, gestionando identidad y acceso de forma nativa sin tokens manuales.',
  },
  {
    n: 41,
    d: 'GOV',
    m: 0,
    q: 'A data engineer needs the most cost-effective way to share Lakehouse data across different cloud platforms while maintaining governance and security using Unity Catalog. What should they do?',
    o: [
      'Use Delta Sharing to share data with external platforms without copying data',
      'Copy data to each cloud platform and manage permissions separately',
      'Deploy a multi-cloud data warehouse to centralize data management',
      'Utilize third-party data sharing services',
    ],
    a: [0],
    x: 'Delta Sharing evita duplicar o mover los datos entre nubes, compartiendo tablas Delta en vivo con gobernanza centralizada de Unity Catalog, minimizando coste de almacenamiento y complejidad operativa.',
  },
  {
    n: 42,
    d: 'GOV',
    m: 0,
    q: 'A company wants to enable analysts to query data from an external Oracle database without data migration, using Lakehouse Federation. Which step should the data engineer take?',
    o: [
      'Create a connection to the Oracle database using the Databricks REST API',
      'Migrate all Oracle data to Databricks Delta Lake before querying',
      'Use Unity Catalog to create a foreign catalog that mirrors the Oracle database',
      'Set up a real-time data replication system from Oracle to Databricks',
    ],
    a: [2],
    x: 'Lakehouse Federation se configura creando una connection en Unity Catalog y, a partir de ella, un foreign catalog que refleja los esquemas/tablas de Oracle, consultables directamente sin migrar los datos.',
  },
  {
    n: 43,
    d: 'GOV',
    m: 0,
    q: 'A team using Lakehouse Federation notices that some table names from an external source are not being imported into the foreign catalog. What could be the reason?',
    o: [
      'The table names contain case-sensitive identifiers',
      'The table names do not comply with Unity Catalog naming rules',
      'The tables are exceeding the resource quotas set by Databricks',
      'The tables use uppercase letters in their names',
    ],
    a: [1],
    x: 'Unity Catalog omite tablas/esquemas cuyos nombres no cumplen sus reglas de nomenclatura (caracteres no permitidos, etc.); esas tablas simplemente no aparecen en el foreign catalog.',
  },
  {
    n: 44,
    d: 'TRA',
    m: 0,
    q: 'A data engineer writes a Databricks notebook using both PySpark and SQL. In a %sql cell, they try to reference a Python variable defined earlier, but get an error. What is the most likely reason?',
    o: [
      'Databricks notebooks only support Scala and SQL, not Python',
      'Databricks does not allow using Python variables inside SQL cells directly without using a special syntax',
      'Only one programming language is allowed per notebook in Databricks',
      'Databricks allows using Python variables in SQL cells directly without any conversion',
    ],
    a: [1],
    x: 'Compartir variables entre Python y SQL en un mismo notebook requiere sintaxis especial (p. ej. widgets, `spark.conf`, o registrar una vista temporal); sin ella, SQL no puede acceder directamente a una variable de Python.',
  },
  {
    n: 45,
    d: 'CICD',
    m: 0,
    q: 'A data engineer is learning about Databricks Asset Bundles (DAB). Which of the following best describes the structure and configuration file used by a DAB?',
    o: ['YAML', 'Docker', 'Terraform', 'JSON'],
    a: [0],
    x: 'Un Databricks Asset Bundle se define en un archivo raíz `databricks.yml` en formato YAML, que describe recursos, targets de entorno y variables.',
  },
  {
    n: 46,
    d: 'ING',
    m: 0,
    q: 'A data engineering team needs to efficiently load a large volume of data incrementally and idempotently from cloud object storage to the data lakehouse. Which Databricks tool should they use?',
    o: ['Databricks SQL', 'MLflow', 'Auto Loader', 'Delta Live Tables'],
    a: [2],
    x: 'Auto Loader está diseñado para ingesta incremental e idempotente de grandes volúmenes de archivos desde almacenamiento en la nube, con checkpointing que garantiza exactly-once.',
  },
  {
    n: 47,
    d: 'GOV',
    m: 0,
    q: 'A data engineer needs a secure and scalable mechanism to share a subset of Delta tables with external partners not on the same Databricks workspace or account. Which feature should they use?',
    o: [
      'Unity Catalog with Delta Sharing',
      'Unity Catalog Lineage',
      'Databricks Jobs API',
      'Mount ADLS folder with access credentials',
    ],
    a: [0],
    x: 'Delta Sharing (integrado con Unity Catalog) permite compartir tablas Delta en vivo con recipients externos de forma segura, sin replicar datos ni exponer credenciales de almacenamiento directamente.',
  },
  {
    n: 48,
    d: 'GOV',
    m: 0,
    q: 'How are audit logs saved in the Databricks Data Intelligence Platform by default?',
    o: ['Plain text', 'CSV', 'JSON', 'XML'],
    a: [2],
    x: 'Los audit logs de Databricks se generan y entregan nativamente en formato JSON, facilitando su procesamiento estructurado en herramientas de big data.',
  },
  {
    n: 49,
    d: 'GOV',
    m: 0,
    q: 'If you need to find all dependencies (upstream tables, notebooks, jobs) of a data object in Databricks using data lineage, what should you do?',
    o: [
      "Don't use lineage, as it only captures 3 months of data",
      'Use data lineage — it captures all table, notebook, job, and pipeline dependencies',
      'Manually trace dependencies using queries outside Databricks',
      'Use lineage, but it only works for column dependencies',
    ],
    a: [1],
    x: 'El linaje de Unity Catalog captura de forma automática y visual las dependencias entre tablas, columnas, notebooks, jobs y dashboards, siendo la herramienta recomendada para este análisis (con limitaciones solo en periodo de retención, no en cobertura).',
  },
  {
    n: 50,
    d: 'TRA',
    m: 0,
    q: 'A data engineering team using Python in a Databricks notebook needs to perform advanced statistical analysis using R in the same notebook. How should they proceed?',
    o: [
      "Embed R code within Python code using the 'rpy2' package",
      'Open a new notebook dedicated to R and export data from the Python notebook',
      "Use the '%r' magic command to execute R code directly in the same notebook",
      'Use a Databricks connector to send data to an external R environment',
    ],
    a: [2],
    x: 'El comando mágico `%r` permite ejecutar código R directamente en una celda del mismo notebook multi-lenguaje, sin necesidad de exportar datos ni crear notebooks separados.',
  },
  {
    n: 51,
    d: 'ING',
    m: 0,
    q: 'A data engineer\'s Auto Loader code throws an error during execution:\n\n```\nstreaming_df = (\n  spark\n  .read\n  .schema(schema)\n  .format("cloudFiles")\n  .option("cloudFiles.format", "json")\n  .load(dataSource)\n)\n```\n\nWhat modification allows this code to perform a streaming read successfully?',
    o: [
      'Replace .read with .readStream',
      'Add a .stream method call after .read',
      'Change .format("cloudFiles") to .format("stream")',
      'Add a .stream method after the spark object',
    ],
    a: [0],
    x: '`readStream` es el método que crea un DataFrame de streaming; `read` es para lecturas estáticas y es incompatible con Auto Loader configurado para procesamiento incremental.',
  },
  {
    n: 52,
    d: 'GOV',
    m: 0,
    q: 'A data engineer needs to build a dashboard combining on-premises PostgreSQL data and Azure Synapse Analytics data, with minimal data movement and no duplication. Which approach should they use?',
    o: [
      'Export both datasets to a Delta Lake table in ADLS using scheduled ETL jobs',
      'Copy the data from both sources into Databricks and create temporary views',
      'Use Databricks Query Federation to directly query the external sources',
      'Write separate queries for each source and join the results manually in a notebook',
    ],
    a: [2],
    x: 'Query Federation permite consultar (con pushdown) múltiples fuentes externas como PostgreSQL y Synapse en una sola query, sin mover ni duplicar los datos en Databricks.',
  },
  {
    n: 53,
    d: 'ING',
    m: 0,
    q: 'You want to ingest a large number of .png image files from cloud object storage using Auto Loader. Which code snippet correctly configures Auto Loader to efficiently ingest only PNG images?',
    o: [
      '`spark.readStream.format("cloudFiles").option("cloudFiles.format", "binaryFile").option("pathGlobfilter", "*.png").load("...")`',
      '`spark.readStream.format("cloudFiles").option("cloudFiles.format", "csv").load("...")`',
      '`spark.readStream.format("cloudFiles").option("cloudFiles.format", "binaryFile").load("...*.png")`',
      '`spark.read.format("csv").load("...")`',
    ],
    a: [0],
    x: 'Para imágenes se debe usar `cloudFiles.format = "binaryFile"`, y la opción `pathGlobfilter` filtra por patrón (`*.png`) de forma soportada, en vez de incrustar el patrón directamente en la ruta de `.load()`.',
  },
  {
    n: 54,
    d: 'TRA',
    m: 0,
    q: 'Which of the following best describes a common characteristic of the gold layer in the medallion architecture?',
    o: [
      'The gold layer contains raw source data with all its original structure and duplicates',
      'The gold layer is normalized, write-optimized, and used primarily for ETL staging',
      'The gold layer is denormalized, business-ready, and read-optimized for analytics and reporting',
      'The gold layer is used to capture failed records and corrupted data for later remediation',
    ],
    a: [2],
    x: 'Gold contiene datos desnormalizados, listos para negocio y optimizados para lectura, orientados a analítica, BI y machine learning, a diferencia de Bronze (crudo) o Silver (limpio, normalizado).',
  },
  {
    n: 55,
    d: 'JOBS',
    m: 0,
    q: 'A multi-task Databricks workflow has three tasks: Ingest Data, Transform Data, Generate Report. The second task fails, causing the third to be skipped. After fixing the bug, the engineer wants to re-run only the failed and skipped tasks. What should they do?',
    o: [
      'Manually trigger the entire job again using Run Now',
      'Use the Repair option and select failed and skipped tasks only',
      'Clone the job and re-run it from the beginning',
      'Delete the job run history and re-execute from the start',
    ],
    a: [1],
    x: 'La función Repair Run permite relanzar solo las tareas que fallaron o se saltaron, conservando el resultado de las tareas exitosas, ahorrando tiempo y cómputo frente a rehacer todo el job.',
  },
  {
    n: 56,
    d: 'JOBS',
    m: 0,
    q: 'Which of the following best captures the key advantages of using Delta Live Tables (DLT) in Databricks?',
    o: [
      'Supports only batch pipelines with SQL-based transformations and schema drift handling',
      'Offers CDC tracking, real-time data quality checks via expectations, and unified batch & streaming processing',
      'Provides fully manual ETL orchestration, Spark-native notebooks, and file-level schema editing',
      'Allows only Python-based pipeline definitions and requires external tools for data validation',
    ],
    a: [1],
    x: 'DLT ofrece un marco declarativo que soporta batch y streaming unificados, seguimiento de CDC, y calidad de datos integrada vía expectations, con gestión automática de dependencias, reintentos y monitorización.',
  },
  {
    n: 57,
    d: 'TRA',
    m: 0,
    q: 'Which of the following is a DDL statement that defines a new object in the database?',
    o: [
      "`INSERT INTO sales (region, amount) VALUES ('North', 100);`",
      "`DELETE FROM customers WHERE country = 'US';`",
      '`CREATE TABLE products (id INT, name STRING, price DECIMAL(10,2));`',
      "`UPDATE orders SET status = 'Completed' WHERE order_id = 101;`",
    ],
    a: [2],
    x: '`CREATE TABLE` es una sentencia DDL (Data Definition Language) que define un nuevo objeto en la base de datos; `INSERT`/`UPDATE`/`DELETE` son DML, manipulan datos, no definen esquema.',
  },
  {
    n: 58,
    d: 'TRO',
    m: 0,
    q: 'A large Delta table with billions of records has columns customer_id, transaction_date, amount. Many queries filter on both customer_id and transaction_date together. Predictive I/O Optimization is enabled, but queries remain slow. Which clustering strategy is most effective?',
    o: [
      'Enable Liquid Clustering on customer_id and transaction_date together as clustering keys',
      'Enable Liquid Clustering only on customer_id',
      'Enable Liquid Clustering only on transaction_date',
      'Use no clustering and rely on Z-Ordering with periodic OPTIMIZE commands',
    ],
    a: [0],
    x: 'Cuando las queries filtran consistentemente por varias columnas juntas, Liquid Clustering con ambas columnas como claves reorganiza los datos incrementalmente para maximizar el data skipping en ambas dimensiones.',
  },
  {
    n: 59,
    d: 'TRO',
    m: 0,
    q: 'A data engineer needs to process a 10 GB input file with lightweight ETL operations (no major joins/shuffles) and write it as a Delta table, minimizing cost. Which compute configuration is best?',
    o: [
      'Use an all-purpose cluster with Photon enabled and on-demand instances',
      'Use a job cluster with Photon enabled and on-demand instances',
      'Use a job cluster with spot instances, without Photon, for cost savings',
      'Use an all-purpose cluster with spot instances, without Photon',
    ],
    a: [2],
    x: 'Para procesamiento ligero, un job cluster (temporal, se paga solo durante la ejecución) con spot instances minimiza el coste; Photon no aporta beneficio medible sin joins/agregaciones pesadas.',
  },
  {
    n: 60,
    d: 'CICD',
    m: 0,
    q: "A data engineering team wants to develop and test Databricks code locally using their preferred IDEs and deploy to Databricks. Which statement best describes Databricks Connect's requirements and capabilities?",
    o: [
      'Databricks Connect can only be used with the online Databricks notebook interface',
      'Databricks Connect requires the use of dbx-compatible IDEs only',
      'Databricks Connect allows developers to use any Python IDE locally to interact with remote Databricks clusters, as long as the environment matches the Databricks Runtime version',
      'Databricks Connect is only supported in a single environment and cannot be used to develop locally and deploy to test or production',
    ],
    a: [2],
    x: 'Databricks Connect permite usar cualquier IDE local compatible con Python para ejecutar código contra un cluster remoto de Databricks, siempre que la versión local de Python coincida con la del Databricks Runtime del cluster.',
  },
];

export const exam22: RawQuestion[] = [
  {
    n: 1,
    d: 'TRO',
    m: 0,
    q: 'A team decides to use Z-ordering to optimize query performance on a column that is frequently filtered. Which column attribute is most critical for Z-ordering to be effective in this scenario?',
    o: [
      'The column should have a high cardinality',
      'The column should have statistics collected on it',
      'The column should have few unique values',
      'The column should be the first column in the table schema',
    ],
    a: [1],
    x: 'Z-Ordering se basa en data skipping, que usa estadísticas (min/max) por archivo; sin esas estadísticas recolectadas, el clustering de Z-Order no puede optimizar el escaneo de datos.',
  },
  {
    n: 2,
    d: 'GOV',
    m: 0,
    q: 'You must grant read access to shared Delta tables for an external non-Databricks workspace. Which authentication approach is best practice for cross-org access?',
    o: [
      'Generate a long-lived bearer token and share it with the recipient',
      "Use OIDC federation to issue short-lived Databricks OAuth tokens based on the recipient's IdP JWT",
      'Share the Databricks workspace password with the recipient',
      'Provide an SSH key for cluster nodes to the recipient',
    ],
    a: [1],
    x: 'La federación OIDC con tokens OAuth de corta duración evita distribuir secretos estáticos, delega la confianza en el IdP del recipient, y permite rotación y auditoría más seguras que un token bearer de larga duración.',
  },
  {
    n: 3,
    d: 'GOV',
    m: 0,
    q: 'A new data-analyst group must have read-only access to a specific schema in Unity Catalog. What minimum privileges should you grant so they can query tables but cannot create/modify anything?',
    o: [
      'SELECT only on the tables',
      'USE CATALOG on the catalog + USE SCHEMA on the schema + SELECT on the tables/views',
      'CREATE TABLE, MODIFY, and SELECT on the schema',
      'OWNERSHIP on the schema',
    ],
    a: [1],
    x: 'Para navegar la jerarquía y leer datos hace falta `USE CATALOG` en el catálogo, `USE SCHEMA` en el esquema, y `SELECT` en las tablas/vistas concretas; sin `USE CATALOG`/`USE SCHEMA`, el `SELECT` por sí solo no basta.',
  },
  {
    n: 4,
    d: 'GOV',
    m: 0,
    q: 'A new data engineering team needs full privileges on the table sales to fully manage an ELT project. Which command grants full permissions on the table to the new team?',
    o: [
      '`GRANT SELECT ON TABLE sales TO team;`',
      '`GRANT USAGE ON TABLE sales TO team;`',
      '`GRANT ALL PRIVILEGES ON TABLE team TO sales;`',
      '`GRANT ALL PRIVILEGES ON TABLE sales TO team;`',
    ],
    a: [3],
    x: '`GRANT ALL PRIVILEGES ON TABLE sales TO team` concede de una vez todos los permisos necesarios sobre la tabla, con el orden correcto de cláusulas (`ON TABLE ... TO ...`).',
  },
  {
    n: 5,
    d: 'TRA',
    m: 0,
    q: 'Which of the below options is correct about the medallion layers?',
    o: [
      'Raw layer: an official medallion layer used for BI dashboards; data is stored as CSV and frequently overwritten for freshness',
      'Gold layer: highly normalized and optimized for fast writes during ETL; not intended for analytics',
      "Silver layer: exact copy of source with no cleaning or enrichment; schema never changes and it's kept only for long-term archiving",
      'Bronze layer: data is kept very close to the source (column names/types largely preserved) with minimal transformations and stored in Delta format',
    ],
    a: [3],
    x: 'Bronze conserva los datos casi tal cual llegan de la fuente, con transformaciones mínimas y almacenados en formato Delta; las demás opciones describen mal los roles reales de Raw/Gold/Silver.',
  },
  {
    n: 6,
    d: 'TRA',
    m: 0,
    q: "How do you calculate the average value of the 'salary' column grouped by 'department' in PySpark?",
    o: [
      '`df.groupBy("department").avg("salary")`',
      '`df.groupBy("department").sum("salary")`',
      '`df.groupBy("department").count("salary")`',
      '`df.groupBy("department").max("salary")`',
    ],
    a: [0],
    x: '`groupBy("department").avg("salary")` calcula directamente el promedio de salario por departamento; las demás opciones aplican una agregación distinta (suma, conteo, máximo).',
  },
  {
    n: 7,
    d: 'TRA',
    m: 0,
    q: 'Given a DataFrame of store sales, you want to group by Store and calculate total sales, max sales for any category, and count of rows per store. Which code achieves this?',
    o: [
      '`df.groupBy("Category").agg(F.sum("Sales").alias("TotalSales"), F.max("Sales").alias("MaxSales"), F.count("Category").alias("CategoryCount"))`',
      '`df.groupBy("Store").agg(F.sum("Sales").alias("TotalSales"), F.max("Sales").alias("MaxSales"), F.count("Category").alias("RowCount"))`',
      '`df.groupBy("Store", "Category").agg(F.sum("Sales").alias("TotalSales"), F.max("Sales").alias("MaxSales"))`',
      '`df.groupBy("Store").agg(F.sum("Sales").alias("TotalSales"), F.countDistinct("Category").alias("CategoryCount"), F.avg("Sales").alias("AvgSales"))`',
    ],
    a: [1],
    x: 'Agrupar por `Store` y aplicar `sum`, `max` y `count` en una sola `agg()` produce exactamente las tres métricas solicitadas por tienda; agrupar por `Category` o por ambas columnas no cumple el requisito.',
  },
  {
    n: 8,
    d: 'TRO',
    m: 0,
    q: 'A gold table sales_gold used by BI dashboards commonly filters on customer_id and order_date, and has many small files due to frequent writes. Which command best improves read performance while compacting files?',
    o: [
      '`OPTIMIZE sales_gold ZORDER BY (customer_id, order_date);`',
      '`VACUUM sales_gold;`',
      '`ANALYZE TABLE sales_gold COMPUTE STATISTICS;`',
      '`OPTIMIZE sales_gold;`',
    ],
    a: [0],
    x: '`OPTIMIZE ... ZORDER BY (...)` compacta archivos pequeños y además reordena los datos según las columnas de filtro frecuente, mejorando el data skipping; `VACUUM`/`ANALYZE` no compactan, y `OPTIMIZE` sin Z-Order no maximiza el skipping.',
  },
  {
    n: 9,
    d: 'TRA',
    m: 0,
    q: 'A data engineer creates a DataFrame using Python and wants to process it using SQL in the same notebook. What is a valid approach?',
    o: [
      'Convert the DataFrame to a temporary view and then use SQL magic commands to query it',
      'Use the %run magic command to execute SQL code in the Python cell',
      'Import the DataFrame directly into SQL using a special import statement',
      "Switch the notebook's language to SQL and access the DataFrame directly",
    ],
    a: [0],
    x: 'Registrar el DataFrame como vista temporal (`createOrReplaceTempView`) y luego consultarla con `%sql` es el patrón estándar para combinar Python y SQL sobre los mismos datos en un notebook.',
  },
  {
    n: 10,
    d: 'ING',
    m: 0,
    q: 'You need to extract data from a directory of Parquet files in an S3 bucket and load it into a Delta Lake table. Which step should be part of your ETL pipeline?',
    o: [
      'Create a new Delta Lake table from an empty DataFrame',
      'Use Databricks SQL to extract data from Parquet files and insert into Delta Lake',
      'Use the COPY INTO command to load data directly from S3 into a Delta table',
      'Manually copy files from S3 to a local directory and load them using Spark',
    ],
    a: [2],
    x: '`COPY INTO` es la forma eficiente y soportada de cargar archivos desde una ubicación como S3 directamente en una tabla Delta, especificando formato y patrón de origen.',
  },
  {
    n: 11,
    d: 'TRA',
    m: 0,
    q: "While inspecting the directory structure of Delta Lake files, a data engineer notices multiple '_delta_log' directories. What is the primary purpose of these directories?",
    o: [
      'To store the actual data files',
      'To maintain a version history of the table',
      'To store backup copies of data',
      'To organize data into partitions',
    ],
    a: [1],
    x: '`_delta_log` contiene el transaction log (archivos JSON ordenados) que registra cada commit sobre la tabla, permitiendo mantener el historial de versiones, ACID y time travel.',
  },
  {
    n: 12,
    d: 'TRA',
    m: 0,
    q: "A team needs to convert 'event_time' from string format 'yyyy-MM-dd HH:mm:ss' to timestamp, ensuring malformed timestamps return NULL instead of raising an error. Which function should they use?",
    o: [
      '`CAST(event_time AS TIMESTAMP)`',
      "`TO_TIMESTAMP(event_time, 'yyyy-MM-dd HH:mm:ss')`",
      '`try_to_timestamp(event_time)`',
      "`STR_TO_TIMESTAMP(event_time, 'yyyy-MM-dd HH:mm:ss')`",
    ],
    a: [2],
    x: '`try_to_timestamp()` intenta la conversión y devuelve `NULL` de forma segura ante un formato inválido, en vez de lanzar un error como `CAST`/`TO_TIMESTAMP`; `STR_TO_TIMESTAMP` no existe en Databricks SQL.',
  },
  {
    n: 13,
    d: 'GOV',
    m: 0,
    q: 'A company needs to share data between systems on AWS and Azure while keeping egress costs minimal. Which approach is most suitable?',
    o: [
      'Set up a VPN connection between the two systems',
      'Use Cloudflare R2 for cross-cloud storage',
      'Use Delta Sharing to share data without replication',
      'Generate read-only signed URLs for the files',
    ],
    a: [2],
    x: 'Delta Sharing permite consultar los datos directamente desde su ubicación original sin replicarlos entre nubes, minimizando el coste de egress frente a VPN, R2 o URLs firmadas que igualmente implicarían transferencia/duplicación.',
  },
  {
    n: 14,
    d: 'GOV',
    m: 0,
    q: 'You need to identify objects and dependencies (tables/views/columns, notebooks, jobs, dashboards) across your Databricks lakehouse. Which statement about Unity Catalog data lineage is correct?',
    o: [
      "Don't use lineage; it only keeps data for 3 months",
      'Use lineage only for reports/dashboards',
      'Use lineage only for notebooks',
      'Use Unity Catalog lineage to analyze dependencies across reports, notebooks, jobs, SQL queries, and tables/columns (via Catalog Explorer or lineage system tables)',
    ],
    a: [3],
    x: 'El linaje de Unity Catalog cubre de forma unificada tablas, columnas, notebooks, jobs, dashboards y queries SQL, visualizable en Catalog Explorer o consultable vía tablas de sistema de linaje.',
  },
  {
    n: 15,
    d: 'TRO',
    m: 0,
    q: 'A Delta table is partitioned by date with a high-cardinality customer_id. Most queries filter by customer_id first, then by date. What is the best layout change to optimize reads?',
    o: [
      'Enable Liquid Clustering on (customer_id, date) and drop partitions',
      'Enable Liquid Clustering on (customer_id) but keep date partitioning',
      'Keep only date partitioning; do not use Liquid Clustering',
      'Enable Liquid Clustering on (date) only and drop partitions',
    ],
    a: [0],
    x: 'Liquid Clustering en ambas columnas (customer_id, date), sustituyendo el particionado tradicional, evita el problema de demasiadas particiones pequeñas por alta cardinalidad y optimiza el data skipping para el patrón de filtro real.',
  },
  {
    n: 16,
    d: 'ING',
    m: 0,
    q: 'You need to ingest data from a JSON file in S3 without Auto Loader and write it as a DLT table. Which code correctly implements this?',
    o: [
      '`spark.read.format("parquet").load("s3://my-bucket/data.json")`',
      '`spark.read.format("csv").load("s3://my-bucket/data.json")`',
      '`spark.read.format("json").load("s3://my-bucket/data.json")`',
      '`spark.read.json("s3://my-bucket/data.json")` used inside a @dlt.table function without .format()',
    ],
    a: [2],
    x: 'El archivo es JSON, así que hay que usar `.format("json")` explícitamente; usar `parquet` o `csv` no coincide con el formato real del archivo.',
  },
  {
    n: 17,
    d: 'GOV',
    m: 0,
    q: 'How can you add a comment to a Databricks table to provide additional context or documentation?',
    o: [
      "`ALTER TABLE <table> ADD COMMENT 'This is a table comment'`",
      "`ALTER TABLE <table> COMMENT 'This is a table comment'`",
      "`COMMENT ON TABLE <table> IS 'This is a table comment'`",
      "`CREATE TABLE <table> WITH COMMENT 'This is a table comment'`",
    ],
    a: [2],
    x: "`COMMENT ON TABLE ... IS '...'` es la sintaxis soportada en Databricks SQL para documentar una tabla; `ADD COMMENT`/`WITH COMMENT` no son cláusulas válidas.",
  },
  {
    n: 18,
    d: 'TRO',
    m: 0,
    q: 'Which of the following statements correctly describes the different types of Databricks SQL Warehouses available for running SQL queries?',
    o: [
      'Classic SQL Warehouse supports basic SQL execution but lacks advanced features like Predictive I/O and workload management available in Pro SQL Warehouses',
      'Pro SQL Warehouses run in your cloud account, support Photon and Predictive IO, but not Intelligent Workload Management, and have slower startup and limited autoscaling',
      'Serverless SQL Warehouses remove the need for cluster management, automatically scale compute resources, and reduce startup latency for SQL queries',
      'All of the above',
    ],
    a: [3],
    x: 'Cada afirmación describe correctamente una característica real de Classic, Pro y Serverless SQL Warehouse respectivamente; juntas cubren el espectro completo de tipos de warehouse en Databricks.',
  },
  {
    n: 19,
    d: 'GOV',
    m: 0,
    q: 'How can data lineage in Unity Catalog help reduce the costs associated with cross-cloud data sharing?',
    o: [
      'By providing detailed cost analysis reports',
      'By optimizing data placement to minimize transfer fees',
      'By identifying unused data assets that can be archived',
      'By automatically compressing data before transfer',
    ],
    a: [2],
    x: 'El linaje muestra qué datasets realmente se usan y cuáles no, permitiendo identificar y archivar assets no utilizados para reducir coste de almacenamiento y transferencia; no genera informes de coste ni comprime datos automáticamente.',
  },
  {
    n: 20,
    d: 'JOBS',
    m: 0,
    q: 'Which of the following is a key difference between all-purpose clusters and jobs clusters in Databricks?',
    o: [
      'All-purpose clusters can be shared among multiple users, while jobs clusters are dedicated to a single job run',
      'Jobs clusters remain active indefinitely unless manually terminated',
      'All-purpose clusters are automatically terminated after a job completes',
      'Jobs clusters are ideal for interactive analytics and ad-hoc data exploration',
    ],
    a: [0],
    x: 'Los clusters all-purpose están pensados para uso interactivo compartido entre varios usuarios; los job clusters se crean y destinan a la ejecución de un job concreto, terminándose automáticamente al finalizar.',
  },
  {
    n: 21,
    d: 'TRO',
    m: 0,
    q: 'A data engineering team processing a large dataset with complex transformations wants to maximize performance. Which engine should they leverage, and why?',
    o: [
      'Use the default Python interpreter for processing data',
      'Enable Photon engine for enhanced query performance',
      'Use Apache Hive for its compatibility with SQL',
      'Use Apache Spark for its distributed processing capabilities',
    ],
    a: [1],
    x: 'Photon es el motor de ejecución vectorizado de nueva generación de Databricks, optimizado sobre Spark para acelerar consultas SQL/DataFrame en transformaciones complejas y grandes volúmenes.',
  },
  {
    n: 22,
    d: 'GOV',
    m: 0,
    q: 'A team wants to share a Delta table of sales data with an external business partner who does not have a Databricks account but uses a BI tool supporting open protocols. Which approach should they use?',
    o: [
      'Export the table as a CSV file and send it manually to the partner weekly',
      'Use Open Delta Sharing and provide secure access via either a bearer token or OIDC federation',
      'Set up a cross-account mount in Azure Databricks to share the table directly',
      "Use Unity Catalog grants to the partner's email address even without a Databricks account",
    ],
    a: [1],
    x: 'Open Delta Sharing permite a un partner sin cuenta Databricks acceder a datos en vivo desde su herramienta BI, autenticándose mediante bearer token u OIDC, sin exportar archivos manualmente ni requerir cuenta Databricks.',
  },
  {
    n: 23,
    d: 'CICD',
    m: 0,
    q: 'A PySpark job is failing because a function breaks whenever the input data type changes. What is the best way to find out what data type is being passed so you can identify the issue?',
    o: [
      'Use a variable watcher tool to track the variable during execution',
      'Add print() statements or log the schema/data type before the function is called',
      'Check the Spark UI for the data type information',
      'Set a breakpoint in the function to inspect the variable',
    ],
    a: [1],
    x: 'En un entorno distribuido como PySpark, la forma más práctica y fiable de diagnosticar el tipo de dato es registrar/imprimir el esquema (`printSchema()`) antes de la llamada; watchers y breakpoints tradicionales no funcionan bien entre executors distribuidos.',
  },
  {
    n: 24,
    d: 'CICD',
    m: 0,
    q: 'What does the basic structure of a Databricks Asset Bundle (DAB) project look like?',
    o: [
      'A YAML file (e.g., bundle.yml) defining artifacts, configurations, and resources, with supporting files in folders',
      'A .zip file containing all cluster configs and code uploaded directly to Databricks',
      'A worker_config.json file with Spark cluster settings and notebooks',
      'A Docker image with code, dependencies, and runtime settings',
    ],
    a: [0],
    x: 'Un proyecto DAB se basa en un archivo YAML raíz (`databricks.yml`/`bundle.yml`) que define artefactos, configuraciones y recursos, con notebooks/scripts organizados en carpetas de apoyo; no se distribuye como zip, JSON de cluster ni imagen Docker.',
  },
  {
    n: 25,
    d: 'GOV',
    m: 0,
    q: 'A group named qa_team needs read-only access to all existing and future tables within a schema. Which SQL statements correctly provide the minimum required privileges?',
    o: [
      "`GRANT SELECT ON SCHEMA manufacturing.quality TO 'group' qa_team;`",
      "`GRANT USE CATALOG ON CATALOG manufacturing TO 'group' qa_team; GRANT USE SCHEMA ON SCHEMA manufacturing.quality TO 'group' qa_team; GRANT SELECT ON ALL TABLES IN SCHEMA manufacturing.quality TO 'group' qa_team;`",
      "`GRANT ALL PRIVILEGES ON SCHEMA manufacturing.quality TO 'group' qa_team; GRANT SELECT ON CATALOG manufacturing TO 'group' qa_team;`",
      "`GRANT SELECT ON CATALOG manufacturing TO 'group' qa_team;`",
    ],
    a: [1],
    x: '`SELECT` no se puede conceder directamente sobre un esquema; hace falta `USE CATALOG` + `USE SCHEMA` para navegar la jerarquía, más `SELECT ON ALL TABLES IN SCHEMA` para el acceso de lectura granular a todas las tablas presentes y futuras.',
  },
  {
    n: 26,
    d: 'GOV',
    m: 0,
    q: 'A team named manufacturing-team needs write access to an existing table in catalog manufacturing, schema quality. What is the minimum set of grants you should give?',
    o: [
      "`GRANT USE CATALOG ON CATALOG manufacturing TO 'manufacturing-team'; GRANT USE SCHEMA ON SCHEMA manufacturing.quality TO 'manufacturing-team'; GRANT MODIFY ON TABLE manufacturing.quality.<table> TO 'manufacturing-team';`",
      "`GRANT USE CATALOG ON CATALOG manufacturing TO 'manufacturing-team'; GRANT SELECT ON TABLE manufacturing.quality.<table> TO 'manufacturing-team'; GRANT READ_METADATA ON TABLE manufacturing.quality.<table> TO 'manufacturing-team';`",
      "`GRANT OWNERSHIP ON SCHEMA manufacturing.quality TO 'manufacturing-team';`",
      "`GRANT USE CATALOG ON CATALOG manufacturing TO 'manufacturing-team'; GRANT USE SCHEMA ON SCHEMA manufacturing.quality TO 'manufacturing-team'; GRANT CREATE TABLE ON SCHEMA manufacturing.quality TO 'manufacturing-team';`",
    ],
    a: [0],
    x: 'Se necesita `USE CATALOG` + `USE SCHEMA` para navegar la jerarquía, y `MODIFY` sobre la tabla concreta para poder insertar/actualizar/borrar; `SELECT`+`READ_METADATA` solo da lectura, y `OWNERSHIP`/`CREATE TABLE` conceden más de lo necesario o no dan acceso de escritura sobre la tabla existente.',
  },
  {
    n: 27,
    d: 'GOV',
    m: 0,
    q: 'Which of the following best describes Open Delta Sharing in Databricks?',
    o: [
      'A feature that allows sharing Delta tables only with other Databricks workspaces within the same account',
      'A feature that allows securely sharing Delta tables with external recipients, even if they do not use Databricks',
      'A service used to replicate data across regions for disaster recovery',
      'A feature for exporting Delta tables to CSV for non-Databricks users',
    ],
    a: [1],
    x: 'Open Delta Sharing permite a organizaciones o herramientas externas (sin cuenta Databricks) acceder de forma segura y en vivo a tablas Delta compartidas, vía el protocolo abierto, sin exportar archivos ni replicar datos.',
  },
  {
    n: 28,
    d: 'GOV',
    m: 0,
    q: 'You implement Delta Sharing where recipients access data using Databricks-to-Databricks sharing. What is a key requirement for the recipient workspace?',
    o: [
      'The recipient workspace must have Unity Catalog enabled',
      'The recipient must be on the same cloud provider as the data provider',
      'The recipient must use OIDC token federation',
      "The recipient must have a separate account in the provider's Databricks workspace",
    ],
    a: [0],
    x: 'El sharing Databricks-a-Databricks requiere que el workspace del recipient tenga Unity Catalog habilitado; no exige coincidencia de nube ni cuentas compartidas.',
  },
  {
    n: 29,
    d: 'GOV',
    m: 0,
    q: 'Using Lakehouse Federation to connect to an external MySQL database, a data engineer notices queries being throttled. What could be a potential cause and solution?',
    o: [
      'The data engineer should increase the Databricks SQL concurrent query limit',
      'Ensure the foreign catalog is properly configured with the correct data type mappings',
      'Check if the MySQL server has reached its connection limit and increase it if necessary',
      'Reduce the number of active queries running across all SQL warehouses',
    ],
    a: [2],
    x: 'El throttling en Lakehouse Federation suele originarse en los límites de conexión/concurrencia del sistema externo (MySQL en este caso), no en la configuración de Databricks; revisar y aumentar el límite de conexiones de MySQL es la solución directa.',
  },
  {
    n: 30,
    d: 'TRA',
    m: 0,
    q: 'Which optimized storage layer in the Databricks Data Intelligence Platform provides full ACID transaction support?',
    o: ['Data Lake', 'Delta Lake', 'File System', 'Parquet Files'],
    a: [1],
    x: 'Delta Lake añade sobre el data lake un transaction log que aporta atomicidad, consistencia, aislamiento y durabilidad (ACID); un data lake, sistema de archivos o Parquet por sí solos no ofrecen estas garantías.',
  },
  {
    n: 31,
    d: 'ING',
    m: 0,
    q: 'A team uses Auto Loader for incremental JSON file processing with `.option("cloudFiles.format", "json")` and a configured schemaLocation. To ensure this correctly uses Auto Loader for ingestion, what change is required?',
    o: [
      'Change format("cloudFiles") to format("autoLoader")',
      'No change is needed; Databricks automatically uses Auto Loader for all streaming reads',
      'No change is needed; using format("cloudFiles") is the correct way to enable Auto Loader',
      'Add a .autoLoader method before .load(sourcePath)',
    ],
    a: [2],
    x: '`format("cloudFiles")` junto con `cloudFiles.format` y `cloudFiles.schemaLocation` es exactamente la configuración correcta y suficiente para activar Auto Loader; no requiere ningún método adicional.',
  },
  {
    n: 32,
    d: 'TRO',
    m: 0,
    q: 'A Databricks job task fails with an error indicating insufficient compute resources. Which action is most appropriate to resolve this issue?',
    o: [
      'Increase the cluster size for the task and repair the failed job run',
      'Check if the upstream data source is available and repair the job run',
      'Re-run the entire job without any changes and hope it succeeds',
      'Modify the task to use a different notebook path and repair the job run',
    ],
    a: [0],
    x: 'Ante un error explícito de recursos de cómputo insuficientes, la acción directa es aumentar el tamaño del cluster de la tarea (más workers o instancias mayores) y luego reparar la ejecución fallida.',
  },
  {
    n: 33,
    d: 'ING',
    m: 0,
    q: 'A data engineer using Auto Loader to process JSON files notices all columns are inferred as STRING types. What could be a reason for this behavior?',
    o: [
      'The cloudFiles.inferColumnTypes option is not set to true',
      'The JSON files are malformed and contain syntax errors',
      'The Auto Loader is configured with schemaEvolutionMode set to rescue',
      'The schema of the JSON files contains nested structures',
    ],
    a: [0],
    x: 'Por defecto, Auto Loader infiere todas las columnas de formatos como JSON/CSV como STRING; hay que activar explícitamente `cloudFiles.inferColumnTypes = "true"` para que infiera los tipos reales.',
  },
  {
    n: 34,
    d: 'TRA',
    m: 0,
    q: 'Given a DataFrame with Region, Product, Sales columns, which code correctly returns total sales and average sales for each product?',
    o: [
      '`df.groupBy("product").agg(sum("sales").alias("total_sales")).show()`',
      '`df.select("product", sum("sales"), avg("sales")).show()`',
      '`df.groupBy("product").agg(sum("sales").alias("total_sales"), avg("sales").alias("avg_sales")).show()`',
      '`df.groupBy("product").sum("sales", "avg(sales)").show()`',
    ],
    a: [2],
    x: 'Agrupar por `product` y calcular ambas agregaciones (`sum` y `avg`) dentro de una única `agg()` con alias produce el resultado correcto por producto; las demás opciones calculan solo una métrica o usan sintaxis inválida.',
  },
  {
    n: 35,
    d: 'P',
    m: 0,
    q: 'Which of the following best describes language support in Databricks notebooks?',
    o: [
      'Only SQL and Scala are supported in Databricks notebooks',
      'You can mix multiple languages in a single cell',
      'You can mix multiple languages in a notebook, but only one language per cell',
      'Each notebook is restricted to a single language type',
    ],
    a: [2],
    x: 'Los notebooks de Databricks permiten mezclar Python, SQL, Scala y R en distintas celdas del mismo notebook usando comandos mágicos, pero cada celda individual solo ejecuta un lenguaje.',
  },
  {
    n: 36,
    d: 'ING',
    m: 0,
    q: 'Which of the following statements accurately describes Databricks Auto Loader?',
    o: [
      'Auto Loader only supports batch ingestion of data',
      'Auto Loader only supports streaming ingestion and does not support batch mode',
      'Auto Loader supports both streaming and batch ingestion of data using optimized APIs',
      'Auto Loader does not support schema evolution',
    ],
    a: [2],
    x: 'Auto Loader soporta tanto streaming continuo como modos tipo batch (`trigger(availableNow=True)` o `once=True`), además de inferencia y evolución de esquema.',
  },
  {
    n: 37,
    d: 'GOV',
    m: 0,
    q: 'To grant all privileges on a specific table to a user or group in Databricks Unity Catalog, which SQL statement would you use?',
    o: [
      '`GRANT ALL PRIVILEGES ON TABLE table_name TO user_or_group;`',
      '`ALLOW ALL RIGHTS ON TABLE table_name TO user_or_group;`',
      '`GIVE FULL ACCESS ON TABLE table_name TO user_or_group;`',
      '`GRANT ALL ON DATABASE database_name TO user_or_group;`',
    ],
    a: [0],
    x: '`GRANT ALL PRIVILEGES ON TABLE ... TO ...` es la sintaxis correcta y estándar de Unity Catalog para conceder todos los privilegios sobre una tabla específica.',
  },
  {
    n: 38,
    d: 'GOV',
    m: 0,
    q: 'A data engineer, owner of a Delta table, wants to grant read-only access to a group of data analysts. Which component of the Databricks Lakehouse Platform should they use?',
    o: ['Repos', 'Jobs', 'Data Explorer', 'Databricks File System (DBFS)'],
    a: [2],
    x: 'Data Explorer (Catalog Explorer) ofrece una interfaz visual para gestionar permisos sobre objetos de datos, incluyendo asignar `SELECT` a analistas desde la pestaña de permisos de la tabla.',
  },
  {
    n: 39,
    d: 'JOBS',
    m: 0,
    q: "A DLT dataset has an expectations clause: `CONSTRAINT check_valid_date EXPECT (event_time > '2021-01-01')`. What happens when a batch includes records that fail this constraint, with no ON VIOLATION action specified?",
    o: [
      'The records that do not meet the condition are still written to the output table and marked as invalid in the event log',
      'The invalid records are excluded from the output table and are captured as invalid entries in the event log',
      'The job fails when it encounters records that violate the constraint',
      'The records are written to the output table but flagged as invalid using an extra column',
    ],
    a: [0],
    x: 'El comportamiento por defecto (equivalente a `warn`) sin `ON VIOLATION` explícito conserva los registros en la tabla destino, registrando la violación en el event log, sin fallar el pipeline ni añadir columnas extra.',
  },
  {
    n: 40,
    d: 'GOV',
    m: 0,
    q: 'A data engineer wants to share Delta tables with a third-party organization that does not use Databricks. What is the appropriate approach to enable this data sharing while ensuring security?',
    o: [
      'Use Databricks-to-Databricks Delta Sharing',
      'Implement open-source Delta Sharing with bearer-token-based credentials',
      'Export the data as CSV files and email them to the third-party organization',
      'Create a REST API to handle data requests from the third-party organization',
    ],
    a: [1],
    x: 'Para un recipient sin Databricks, el protocolo de Open Delta Sharing con credenciales basadas en bearer-token es el enfoque soportado, seguro y auditable; Databricks-to-Databricks requiere que el recipient también tenga Databricks.',
  },
  {
    n: 41,
    d: 'GOV',
    m: 0,
    q: 'Which of the following are valid authentication methods supported by Open Delta Sharing in Databricks?',
    o: [
      'Long-lived bearer tokens and OpenID Connect (OIDC) federation',
      'Shared database credentials (username/password) and CSV file passwords',
      'Temporary SSH keys and SAML-based session tokens only',
      'Only Databricks personal access tokens',
    ],
    a: [0],
    x: 'Open Delta Sharing soporta autenticación mediante bearer tokens (más simple) u OIDC federation (tokens de corta duración más seguros); credenciales de BD, contraseñas de CSV o claves SSH no son métodos soportados.',
  },
  {
    n: 42,
    d: 'CICD',
    m: 0,
    q: 'A Python UDF fails at runtime after a schema change introduced integer values in a previously string-only input column. Which is the best approach to debug this issue in Databricks?',
    o: [
      'Use an interactive debugger directly on the Databricks cluster to step through UDF execution',
      'Check cluster logs and driver/executor error messages to trace the failure and identify the unexpected data types',
      'Rerun the notebook manually with %run magic command to see if it works next time',
      'Enable Photon execution engine to optimize the UDF and automatically handle schema changes',
    ],
    a: [1],
    x: 'En un entorno distribuido, revisar los logs del driver/executor es la forma práctica de rastrear el traceback (p. ej. TypeError) que revela el tipo de dato inesperado que rompió la UDF.',
  },
  {
    n: 43,
    d: 'GOV',
    m: 0,
    q: 'A group named analysts should have read access only to specific external tables, not to all tables or the raw storage layer. Which approach best follows the principle of least privilege?',
    o: [
      'Grant read access directly on the ADLS storage container to analysts',
      'Grant SELECT on all tables in the schema to analysts',
      'Use Unity Catalog GRANT SELECT only on the required external tables for analysts',
      'Manually copy only the required data into a separate Delta table and share that with analysts',
    ],
    a: [2],
    x: 'Conceder `SELECT` de Unity Catalog únicamente sobre las tablas externas necesarias respeta el mínimo privilegio, sin exponer todo el esquema ni el almacenamiento subyacente ni duplicar datos innecesariamente.',
  },
  {
    n: 44,
    d: 'TRO',
    m: 0,
    q: 'A Spark job performs wide transformations (joins, groupBy, aggregations) on a small dataset (~50MB) but takes longer than expected due to excessive shuffling. Which Spark configuration should be tuned?',
    o: [
      '`spark.conf.set("spark.sql.shuffle.partitions", 50)` to reduce the number of shuffle partitions to match the dataset size',
      '`spark.conf.set("spark.dynamicAllocation.enabled", false)` to disable autoscaling',
      '`spark.conf.set("spark.executor.memory", "50g")` to increase executor memory',
      '`spark.conf.set("spark.sql.adaptive.enabled", false)` to disable AQE',
    ],
    a: [0],
    x: 'El valor por defecto de 200 particiones de shuffle es excesivo para un dataset de 50MB; reducir `spark.sql.shuffle.partitions` a un número acorde al tamaño de datos disminuye el overhead de tareas pequeñas y mejora el rendimiento.',
  },
  {
    n: 45,
    d: 'TRO',
    m: 0,
    q: 'In the Spark UI, CPU Time is much higher than Task Time across multiple stages for a PySpark job. What should the data engineer do to address this issue?',
    o: [
      'High CPU Time vs Task Time means the cluster is being efficiently utilized, hence no action is required',
      'High CPU Time vs Task Time means overutilized memory and something needs to be done',
      'High CPU Time vs Task Time means we need to fine-tune the worker nodes by improving the compute',
      'High CPU Time vs Task Time means we need to partition so that parallelism can be achieved',
    ],
    a: [2],
    x: 'Un CPU Time mucho mayor que el Task Time indica tareas limitadas por CPU (código ineficiente o CPUs insuficientes); la solución es mejorar el cómputo de los worker nodes (más/mejores CPUs) u optimizar el código, no simplemente particionar más.',
  },
  {
    n: 46,
    d: 'GOV',
    m: 0,
    q: 'You need to share tables and views between internal and external users: internal with read/write access, external with read-only access. What is the most suitable approach?',
    o: [
      'Set up Delta Sharing with read-only access for external users, and grant read/write permissions directly to internal users in Unity Catalog',
      'Grant both internal and external users read/write access via Delta Sharing',
      'Use Delta Sharing for internal users and direct Unity Catalog access for external users',
      'Export tables to files and share them with external users via cloud storage',
    ],
    a: [0],
    x: 'Combinar Unity Catalog (permisos read/write para usuarios internos) con Delta Sharing (acceso read-only para externos) cubre ambos requisitos sin duplicar datos ni comprometer la gobernanza.',
  },
  {
    n: 47,
    d: 'GOV',
    m: 0,
    q: 'A data engineer needs to share data from one Databricks workspace to another used by an external team. What is the first piece of information needed to enable secure sharing?',
    o: [
      'IP address of the Unity Catalog cluster',
      'Metastore identifier of the Unity Catalog',
      'Workspace password',
      'Personal access token (PAT) of the recipient',
    ],
    a: [1],
    x: 'El identificador del metastore de Unity Catalog del workspace receptor es el dato necesario para configurar un share Databricks-a-Databricks; no se requieren contraseñas, IPs ni PATs.',
  },
  {
    n: 48,
    d: 'TRA',
    m: 0,
    q: 'You are ingesting streaming data from Kafka into Databricks and want to maintain the full history of the data while keeping storage costs low. In which layer should you store this data?',
    o: ['Raw layer', 'Bronze layer', 'Silver layer', 'Gold layer'],
    a: [1],
    x: 'Bronze es la capa de append-only que conserva el histórico completo tal cual llega, en un formato comprimido y económico, sirviendo de base auditable antes de refinarse en Silver/Gold.',
  },
  {
    n: 49,
    d: 'ING',
    m: 0,
    q: 'Which of the following best describes Auto Loader in Databricks?',
    o: [
      'Supports batch ingestion only and schema evolution, but not streaming',
      'Supports both batch and streaming ingestion, but does not support schema evolution',
      'Supports streaming ingestion only, along with schema evolution, but not batch',
      'Supports both batch and streaming ingestion, and also supports schema evolution',
    ],
    a: [3],
    x: 'Auto Loader soporta tanto ingesta en streaming continuo como modos tipo batch, además de inferencia y evolución de esquema, cubriendo ambos escenarios.',
  },
  {
    n: 50,
    d: 'TRO',
    m: 0,
    q: 'A Databricks job fails with `java.lang.OutOfMemoryError: Java heap space`. What is the most appropriate action to fix this issue?',
    o: [
      'Increase the worker node memory or use a larger cluster instance type',
      'Add more partitions to increase parallelism without changing memory',
      'Ignore the error and re-run the job',
      'Switch the cluster to serverless mode',
    ],
    a: [0],
    x: 'Un error de heap space del executor indica falta de memoria disponible; aumentar la memoria de los worker nodes (o usar instancias más grandes) es la acción directa y más apropiada.',
  },
  {
    n: 51,
    d: 'TRA',
    m: 1,
    q: 'In the medallion architecture, which two of the following correctly describe the gold layer?',
    o: ['Normalized', 'Denormalized', 'Read-optimized', 'Legacy of history', 'Raw format'],
    a: [1, 2],
    x: 'Gold contiene datasets desnormalizados y optimizados para lectura, orientados a consumo por BI y dashboards; "normalized", "raw" y "legacy of history" describen mejor a Bronze o a esquemas de warehouse tradicional.',
  },
  {
    n: 52,
    d: 'TRA',
    m: 0,
    q: 'Given a PySpark DataFrame df with columns region and sales, which code correctly calculates total sales, average sales, and total number of sales per region?',
    o: [
      '`df.groupBy("region").agg(sum("sales").alias("total_sales"), avg("sales").alias("avg_sales"), count("sales").alias("total_count")).show()`',
      '`df.groupBy().agg(sum("sales").alias("total_sales"), avg("sales").alias("avg_sales"), count("sales").alias("total_count")).show()`',
      '`df.groupBy("region").select(sum("sales").alias("total_sales"), avg("sales").alias("avg_sales"), count("sales").alias("total_count")).show()`',
      '`df.agg(sum("sales").alias("total_sales"), avg("sales").alias("avg_sales"), count("sales").alias("total_count")).show()`',
    ],
    a: [0],
    x: 'Agrupar por `region` y calcular las tres métricas dentro de `agg()` con alias es la sintaxis correcta; `groupBy()` vacío o `.agg()` sin `groupBy` agregarían sobre todo el DataFrame en vez de por región, y `.select()` tras `groupBy` no es válido para aplicar funciones de agregación así.',
  },
];

export const exam23: RawQuestion[] = [
  {
    n: 1,
    d: 'JOBS',
    m: 0,
    q: 'A data engineer configures a Databricks Job with one task that runs daily to process log data. They want an ephemeral compute environment that starts when the task begins and terminates when it ends, to optimize cost and isolation. Which configuration should they use?',
    o: [
      'Select a high-concurrency cluster from the cluster pool',
      'Configure the task to use a job cluster defined in the task settings',
      'Attach the task to a shared all-purpose cluster with auto-scaling enabled',
      'Use an interactive cluster with auto-termination set',
      'Attach the task to a workspace default cluster and enable cluster reuse',
    ],
    a: [1],
    x: 'Un job cluster definido en la configuración de la tarea se aprovisiona automáticamente al arrancar y se termina al completarse, ofreciendo aislamiento total y coste mínimo frente a clusters interactivos compartidos.',
  },
  {
    n: 2,
    d: 'TRA',
    m: 0,
    q: 'A data engineer is working with a column named items containing arrays of item names, and wants to convert each item name to uppercase without changing the structure of the array. Which Spark SQL function should they use?',
    o: ['filter', 'explode', 'flatten', 'array_upper', 'transform'],
    a: [4],
    x: '`transform(array, lambda)` aplica una función a cada elemento del array y devuelve un nuevo array con la misma estructura, ideal para convertir cada nombre a mayúsculas sin aplanar ni filtrar.',
  },
  {
    n: 3,
    d: 'CICD',
    m: 0,
    q: 'How do Databricks Repos support CI/CD workflows in Databricks?',
    o: [
      'By allowing users to define CI/CD pipelines using notebook widgets and job parameters within the workspace',
      'By enabling automated cluster restarts after every notebook edit',
      'By syncing notebooks and code with Git repositories to enable version control, code reviews, and automated deployment through external CI/CD tools',
      'By storing notebook outputs as data tables for auditing purposes',
      'By automatically deploying notebooks as REST APIs with every commit',
    ],
    a: [2],
    x: 'Databricks Repos sincroniza notebooks y código con proveedores Git, habilitando ramas, revisiones de código y despliegue automatizado mediante herramientas de CI/CD externas (GitHub Actions, Azure DevOps, etc.).',
  },
  {
    n: 4,
    d: 'CICD',
    m: 0,
    q: 'A developer has defined a Databricks Asset Bundle in a databricks.yml file and wants to check for syntax errors and configuration issues before deploying. Which Databricks CLI command should they use?',
    o: [
      '`databricks bundle inspect`',
      '`databricks bundle validate`',
      '`databricks bundle test`',
      '`databricks bundle verify`',
      '`databricks bundle check`',
    ],
    a: [1],
    x: '`databricks bundle validate` comprueba errores de sintaxis y configuración antes del despliegue; `inspect`/`test`/`verify`/`check` no son subcomandos válidos del CLI de bundles.',
  },
  {
    n: 5,
    d: 'JOBS',
    m: 0,
    q: 'A Databricks Job has five sequential tasks. The final task generates a critical audit log. The engineer wants a Slack notification only if the final task fails, without alerts for earlier task failures. Which configuration meets this requirement?',
    o: [
      'Configure a job-level alert that triggers on task failures and sends notifications to Slack',
      'Configure a task-level notification specifically on the audit log task to trigger on failure and send a Slack alert',
      'Add Slack notifications to all tasks and use Slack filtering to focus only on the audit log task',
      'Add a custom task after the audit log task to manually send Slack alerts if it detects a failure',
      'Use cluster-level logging and external monitoring tools to notify Slack when the audit log fails',
    ],
    a: [1],
    x: 'Configurar una notificación a nivel de tarea, específicamente en la tarea del audit log, permite avisar por Slack solo cuando esa tarea concreta falle, sin ruido de las tareas anteriores.',
  },
  {
    n: 6,
    d: 'TRA',
    m: 0,
    q: 'A data engineer has registered a temporary view named transactions_view and wants to retrieve rows where amount > 1000 using SQL, in a PySpark notebook cell. Which code correctly executes this and returns a Spark DataFrame?',
    o: [
      '`spark.sql("SELECT * FROM transactions_view WHERE amount > 1000")`',
      '`spark.query("SELECT * FROM transactions_view WHERE amount > 1000")`',
      '`spark.read.sql("SELECT * FROM transactions_view WHERE amount > 1000")`',
      '`sqlContext.read("SELECT * FROM transactions_view WHERE amount > 1000")`',
      '`transactions_view.sql("SELECT * FROM transactions_view WHERE amount > 1000")`',
    ],
    a: [0],
    x: '`spark.sql("...")` es el método estándar para ejecutar SQL desde PySpark y obtener un DataFrame; `spark.query`, `spark.read.sql` y las demás variantes no son APIs válidas.',
  },
  {
    n: 7,
    d: 'CICD',
    m: 0,
    q: 'A data engineer working in a Databricks Repo linked to GitHub creates a feature branch and edits a notebook. After a few hours, they try to commit and push, but get an error that their branch is behind the remote. What should they do next?',
    o: [
      'Commit the notebook changes again using a different commit message',
      'Force push their local changes to overwrite the remote branch',
      'Pull the latest changes from the remote repository to merge updates into their branch before pushing again',
      'Restart their cluster and reinitialize the Databricks Repo to fetch updates',
      'Delete their branch and recreate it from the main branch with a new name',
    ],
    a: [2],
    x: 'Git bloquea el push si la rama local está detrás de la remota; hay que hacer pull para integrar los cambios remotos (resolviendo conflictos si los hay) antes de poder pushear de nuevo.',
  },
  {
    n: 8,
    d: 'TRA',
    m: 1,
    q: 'A data engineer wants to count how many records have a NULL value in the return_reason column of the returns_data table. Which queries correctly return this count? (Select two)',
    o: [
      '`SELECT COUNT(*) FROM returns_data WHERE return_reason IS NULL;`',
      '`SELECT COUNT(return_reason IS NULL) FROM returns_data;`',
      '`SELECT COUNT(return_reason) FROM returns_data WHERE return_reason IS NULL;`',
      '`SELECT COUNT(*) FROM returns_data WHERE return_reason = NULL;`',
      '`SELECT COUNT_IF(return_reason IS NULL) FROM returns_data;`',
    ],
    a: [0, 4],
    x: '`COUNT(*) WHERE ... IS NULL` filtra explícitamente por nulos antes de contar, y `COUNT_IF(condición)` cuenta directamente cuántas filas cumplen la condición booleana; comparar con `= NULL` nunca es verdadero en SQL.',
  },
  {
    n: 9,
    d: 'P',
    m: 0,
    q: 'A security-conscious enterprise wants all customer data, including file reads/writes and notebook execution, to remain within their own cloud environment, while Databricks manages platform services like the web UI and job scheduler. Which aspect of the architecture makes this possible?',
    o: [
      "The control plane is hosted by Databricks, while the data plane runs in the customer's cloud account",
      'The driver node is managed independently from the compute cluster',
      'Unity Catalog separates secure and insecure clusters using data tags',
      'Databricks SQL provides serverless access to files across multiple regions',
      'Jobs run on ephemeral clusters with encrypted secrets stored in the control plane',
    ],
    a: [0],
    x: 'La separación control plane (gestionado por Databricks: UI, scheduler) / data plane (en la cuenta cloud del cliente: cómputo, datos) garantiza que los datos del cliente nunca salen de su propio entorno cloud.',
  },
  {
    n: 10,
    d: 'TRA',
    m: 0,
    q: 'A logistics company collects raw JSON sensor data with inconsistent timestamps and missing values, and wants to detect delivery delay patterns and monitor performance dashboards. How should they organize their data using the Medallion Architecture?',
    o: [
      'Ingest data directly into the Gold layer and use dashboards to clean the data dynamically',
      'Store raw data in CSV format, clean it using SQL Warehouses, and visualize it with Power BI',
      'Use streaming jobs to continuously merge raw and aggregated data into one unified Silver table',
      'Ingest raw sensor data into the Bronze layer, clean and standardize it in the Silver layer, and create aggregated performance metrics in the Gold layer',
      'Load all sensor data into a single Delta table and add tags to distinguish raw vs processed records',
    ],
    a: [3],
    x: 'El flujo natural del Medallion es Bronze (ingesta cruda) → Silver (limpieza/estandarización) → Gold (métricas agregadas para dashboards), exactamente lo que necesita este caso de datos de sensores.',
  },
  {
    n: 11,
    d: 'TRA',
    m: 0,
    q: 'A Spark table has a column product_categories that is an array of arrays, e.g. [["electronics","audio"],["kitchen"]]. The engineer wants to flatten this into a single array of all elements. Which Spark SQL function should they use?',
    o: ['flatten_array', 'flatten_values', 'flat_map', 'flatten', 'explode'],
    a: [3],
    x: '`flatten()` convierte un array de arrays en un único array plano preservando el orden; `explode` generaría filas en vez de un array, y las demás no son funciones válidas.',
  },
  {
    n: 12,
    d: 'JOBS',
    m: 0,
    q: 'A DLT pipeline includes: `CONSTRAINT valid_age EXPECT (age > 0)` with no ON VIOLATION action specified. What is the default behavior if a row from raw_users contains a negative or null age?',
    o: [
      'The row is silently ignored, and no violation is logged',
      'The row is dropped before being written to the target table',
      'The row is moved to a quarantine table for manual inspection',
      'The row is written to the target, and the violation is recorded in dataset metrics',
      'The row causes the pipeline to fail immediately',
    ],
    a: [3],
    x: 'Sin `ON VIOLATION` especificado, el comportamiento por defecto (warn) escribe la fila en la tabla destino y registra la violación en las métricas/event log del dataset, sin frenar el pipeline.',
  },
  {
    n: 13,
    d: 'CICD',
    m: 0,
    q: 'A team notices job configurations in dev, test, and prod workspaces drift apart because engineers make manual changes. They want a Databricks native declarative way to manage all environments from source control. Which solution best addresses this?',
    o: [
      'Use Databricks Asset Bundles',
      'Use dbx from Databricks Labs to apply job changes manually',
      'Copy jobs between environments using the Databricks UI',
      'Manually export and import job definitions between workspaces',
      'Use Delta Sharing to replicate job metadata across environments',
    ],
    a: [0],
    x: 'Los Databricks Asset Bundles declaran jobs, clusters y pipelines en YAML versionado en Git, con targets por entorno, eliminando el drift de configuraciones causado por cambios manuales.',
  },
  {
    n: 14,
    d: 'GOV',
    m: 0,
    q: 'A data analyst wants to explore tables and views using a visual interface, browsing catalogs/schemas and viewing column-level comments and tags without writing SQL. Which Unity Catalog feature should they use?',
    o: [
      'Unity Metadata Tracker',
      'Discovery Hub',
      'Data Map Viewer',
      'Asset Insight Panel',
      'Catalog Explorer',
    ],
    a: [4],
    x: 'Catalog Explorer ofrece una interfaz visual para navegar catálogos, esquemas, tablas y vistas, viendo comentarios y tags de columnas sin necesidad de escribir SQL.',
  },
  {
    n: 15,
    d: 'GOV',
    m: 0,
    q: 'Which statement correctly distinguishes Query Federation from Catalog Federation in Databricks Lakehouse Federation?',
    o: [
      'Both Query Federation and Catalog Federation push queries down to the external system',
      'Query Federation only works with Snowflake, while Catalog Federation works with all external systems',
      'In Query Federation, queries are executed on Databricks compute, while in Catalog Federation they run on the external catalog',
      'In Query Federation, queries are pushed down and executed on the external database; in Catalog Federation, data is accessed from external catalogs and queries run on Databricks compute',
      'Both Query Federation and Catalog Federation run queries on Databricks compute',
    ],
    a: [3],
    x: 'La diferencia clave es dónde se ejecuta la query: en Query Federation se envía (pushdown) y ejecuta en la base de datos externa; en Catalog Federation, los metadatos vienen de un catálogo externo pero la ejecución ocurre en el cómputo de Databricks.',
  },
  {
    n: 16,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to refresh a Delta table customer_data with new data from a staging table, replacing all rows, keeping the existing schema and metadata, and avoiding dropping/recreating the table. Which SQL command should they use?',
    o: [
      '`OVERWRITE TABLE customer_data SELECT * FROM staging_table`',
      '`MERGE INTO customer_data USING staging_table`',
      '`INSERT OVERWRITE TABLE customer_data SELECT * FROM staging_table`',
      '`CREATE OR REPLACE TABLE customer_data AS SELECT * FROM staging_table`',
      '`REPLACE INTO customer_data SELECT * FROM staging_table`',
    ],
    a: [2],
    x: '`INSERT OVERWRITE TABLE` reemplaza todos los datos en una operación atómica sin tocar el esquema/metadatos ni recrear la tabla, cumpliendo exactamente los tres requisitos.',
  },
  {
    n: 17,
    d: 'TRA',
    m: 0,
    q: 'A Spark table has a column items structured as an array of structs with id and tags (array of strings). The engineer wants one row per tag, keeping the associated id. Which function should be used?',
    o: [
      '`explode(tags)`',
      '`transform(tags, x -> x)`',
      '`flatten(tags)`',
      '`group_by(tags)`',
      '`map_values(tags)`',
    ],
    a: [0],
    x: '`explode(tags)` genera una fila por cada elemento del array `tags`, duplicando las demás columnas (como `id`) en cada fila resultante.',
  },
  {
    n: 18,
    d: 'TRO',
    m: 0,
    q: 'A job is configured with a custom cron expression. The engineer wants to monitor execution history, including run status, duration, and logs for each scheduled run. Which location in the UI provides this information?',
    o: [
      'Monitoring tab in the SQL Warehouse',
      "Revisions tab in the job's associated notebook",
      'Driver logs in the Cluster UI',
      'Metrics dashboard in the Admin Console',
      'Job Runs tab in the Job details page',
    ],
    a: [4],
    x: 'La pestaña Runs en la página de detalles del Job muestra el historial completo de ejecuciones con estado, duración y logs, siendo el lugar central para monitorizar jobs programados.',
  },
  {
    n: 19,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to create a SQL UDF that concatenates a first and last name, capitalizes each part, and returns the full name. Which statement correctly defines this function?',
    o: [
      '`CREATE FUNCTION format_name(fname STRING, lname STRING) RETURNS STRING AS \'SELECT UPPER(fname) || " " || UPPER(lname)\';`',
      "`CREATE FUNCTION format_name(fname STRING, lname STRING) RETURNS STRING USING 'INITCAP(fname) || INITCAP(lname)';`",
      "`CREATE OR REPLACE FUNCTION format_name(fname STRING, lname STRING) RETURNS STRING RETURN INITCAP(fname) || ' ' || INITCAP(lname);`",
      "`CREATE TEMPORARY FUNCTION format_name AS (fname STRING, lname STRING) -> INITCAP(fname) + ' ' + INITCAP(lname);`",
      "`CREATE OR REPLACE FUNCTION format_name(fname, lname) RETURN INITCAP(fname) || ' ' || INITCAP(lname);`",
    ],
    a: [2],
    x: 'La sintaxis correcta de una UDF SQL en Databricks es `CREATE [OR REPLACE] FUNCTION nombre(parámetros con tipos) RETURNS tipo RETURN expresión`; `INITCAP` capitaliza cada parte, y las demás opciones tienen sintaxis inválida o tipos de parámetro faltantes.',
  },
  {
    n: 20,
    d: 'JOBS',
    m: 0,
    q: 'Which of the following best describes the difference between all-purpose clusters and job clusters in Databricks?',
    o: [
      'Job clusters support SQL only, while all-purpose clusters support Python and Scala',
      'All-purpose clusters are serverless, while job clusters require manual configuration',
      'All-purpose clusters are for collaborative, interactive use and persist across sessions; job clusters are created for a single run and terminate after completion',
      'All-purpose clusters: shared, reusable, ideal for development and collaboration',
      'Job clusters: created automatically for job runs, isolated per job, and terminate on completion',
    ],
    a: [2],
    x: 'Los clusters all-purpose son para trabajo interactivo compartido que persiste entre sesiones; los job clusters se crean para una única ejecución y se terminan al completarla, siendo esta la distinción fundamental entre ambos.',
  },
  {
    n: 21,
    d: 'TRO',
    m: 0,
    q: 'An SQL query against a Databricks SQL warehouse is experiencing slow performance, identified as caused by a single complex query on a large table, not concurrency. What is the most appropriate configuration change?',
    o: [
      'Increase SQL warehouse size (scale up compute resources)',
      'Enable query result caching',
      'Increase the size of the cluster to improve compute resources for the query',
      'Enable Photon acceleration for the warehouse',
      'Increase the number of clusters in the scaling configuration',
    ],
    a: [0],
    x: 'Ante una sola query pesada (no concurrencia), escalar verticalmente el tamaño del SQL warehouse aporta más CPU/memoria/IO para esa query concreta; aumentar el número de clusters solo ayuda con concurrencia.',
  },
  {
    n: 22,
    d: 'TRO',
    m: 0,
    q: 'An analyst wants to run ad hoc SQL queries and build dashboards within the Databricks SQL workspace. Which compute resource should they select?',
    o: [
      'Delta Live Tables Pipeline Cluster',
      'Databricks Jobs Compute',
      'SQL Warehouse',
      'Notebook Cluster',
      'All-purpose Compute Cluster',
    ],
    a: [2],
    x: 'Los SQL Warehouses son el cómputo dedicado y optimizado para queries interactivas y dashboards dentro de Databricks SQL, con soporte de concurrencia e integración con herramientas de BI.',
  },
  {
    n: 23,
    d: 'ING',
    m: 0,
    q: 'You want to load data from an SQLite database into a Databricks table using the Spark JDBC API. What value should replace the blank in `.format("_____")` to connect via JDBC?',
    o: ['`"org.apache.spark.sql.jdbc"`', '`"jdbc"`', '`"sqlite"`', '`"delta"`'],
    a: [1],
    x: 'La fuente de datos de Spark se especifica como `"jdbc"`; el tipo de base de datos real (SQLite) se indica dentro de la URL de conexión (`jdbc:sqlite:...`), no en el formato.',
  },
  {
    n: 24,
    d: 'TRO',
    m: 0,
    q: 'A finance analytics team forgets to shut down their Databricks SQL warehouse, causing unnecessary costs. Which feature configures the warehouse to automatically shut down after being idle for a specified period?',
    o: [
      'Query Execution Timeout',
      'Auto Termination',
      'Compute Scaling Policy',
      'Cluster Timeout',
      'Auto-Stop',
    ],
    a: [4],
    x: 'Auto-Stop (Auto-Suspend) es la configuración específica de los SQL Warehouses que apaga automáticamente el cómputo tras un periodo de inactividad definido; "Auto Termination" es el término equivalente para clusters, no warehouses.',
  },
  {
    n: 25,
    d: 'TRA',
    m: 1,
    q: "A data engineer needs a report of sales where the product belongs to the 'Electronics' category, using sales (sale_id, product_id, sale_amount) and products (product_id, product_name, category). Which queries correctly return this? (Select two)",
    o: [
      "`SELECT s.sale_id, s.sale_amount FROM sales s JOIN products p ON s.product_id = p.product_id WHERE p.category = 'Electronics';`",
      "`SELECT * FROM sales WHERE category = 'Electronics';`",
      "`SELECT p.product_id, p.product_name FROM products p WHERE p.category = 'Electronics';`",
      "`SELECT s.sale_id, s.sale_amount FROM sales s INNER JOIN products p ON s.product_id = p.product_id WHERE p.category = 'Electronics';`",
      '`SELECT s.sale_id, s.product_id FROM sales s LEFT JOIN products p ON s.product_id = p.product_id;`',
    ],
    a: [0, 3],
    x: "Un `JOIN`/`INNER JOIN` explícito entre `sales` y `products` filtrando por `category = 'Electronics'` es la forma correcta; `sales` no tiene columna `category` directamente, y consultar solo `products` no da un reporte de ventas.",
  },
  {
    n: 26,
    d: 'TRA',
    m: 0,
    q: "A data engineering team needs to find the total number of rows written during a recent MERGE operation on a Delta table. Which metric should they refer to in the operation's metadata?",
    o: ['numSourceRows', 'numTargetRowsInserted', 'numOutputRows', 'numTargetRowsUpdated'],
    a: [2],
    x: '`numOutputRows` representa el total de filas escritas (insertadas+actualizadas+borradas) durante el MERGE; las demás métricas capturan solo una porción parcial de esa operación.',
  },
  {
    n: 27,
    d: 'GOV',
    m: 1,
    q: 'A data engineer wants to verify where the data of table sales.orders is physically stored in cloud storage. Which SQL commands can be used? (Select two)',
    o: [
      '`DESCRIBE DETAIL sales.orders`',
      '`SELECT * FROM sales.orders LOCATION`',
      '`SELECT location FROM sales.orders`',
      '`DESCRIBE TABLE sales.orders`',
      '`DESCRIBE EXTENDED sales.orders`',
    ],
    a: [0, 4],
    x: '`DESCRIBE DETAIL` y `DESCRIBE EXTENDED` exponen metadatos de la tabla incluyendo su ubicación física de almacenamiento; `DESCRIBE TABLE` solo muestra el esquema, y las otras opciones no son sintaxis SQL válida.',
  },
  {
    n: 28,
    d: 'TRA',
    m: 0,
    q: 'You want to enforce a new constraint on a Delta Lake table to ensure a column, age, always has values greater than zero. Which command would you use?',
    o: [
      '`ALTER TABLE my_table ADD PRIMARY KEY (age);`',
      '`ALTER TABLE my_table ADD CONSTRAINT age_greater_than_zero CHECK (age > 0);`',
      "`ALTER TABLE my_table SET TBLPROPERTIES ('age_greater_than_zero' = 'true');`",
      '`CREATE TABLE my_table WITH CONSTRAINT age_greater_than_zero CHECK (age > 0);`',
    ],
    a: [1],
    x: 'Delta Lake soporta constraints `CHECK` añadidos con `ALTER TABLE ADD CONSTRAINT`, que verifican que una expresión booleana se cumpla en cada fila; Delta Lake no soporta `PRIMARY KEY` forzado, y `TBLPROPERTIES` no valida datos.',
  },
  {
    n: 29,
    d: 'TRA',
    m: 0,
    q: 'A data analyst wants to reuse the result of a complex query several times in the current session, without writing data to permanent storage, discarded automatically when the notebook detaches from the cluster. Which is the most suitable option?',
    o: ['Standard View', 'Global Temporary View', 'External Table', 'Managed Table', 'Temporary View'],
    a: [4],
    x: 'Una vista temporal (de sesión) existe solo durante la sesión actual del notebook, sin persistir datos físicos, y se descarta automáticamente al desvincularse; las demás opciones son persistentes o de mayor alcance.',
  },
  {
    n: 30,
    d: 'P',
    m: 0,
    q: 'A team using the medallion architecture stores commonly used configurations and utility functions in a separate notebook /Shared/Includes/config, to avoid duplicating code. What is the best way to reuse this shared code across Bronze, Silver, and Gold notebooks?',
    o: [
      'Copy and paste the code from the config notebook into each stage notebook',
      'Use the %run /Shared/Includes/config command at the top of each notebook',
      'Clone the config notebook into each Medallion stage directory',
      'Reference the config notebook via Unity Catalog schema mappings',
      'Create a separate SQL warehouse for each layer and reference config as a parameter',
    ],
    a: [1],
    x: '`%run` importa el contenido de otro notebook (variables, funciones) directamente en la sesión del notebook actual, siendo la forma estándar de reutilizar código compartido entre etapas.',
  },
  {
    n: 31,
    d: 'TRA',
    m: 0,
    q: "In a Databricks Lakehouse, you're setting up tables for a new pipeline optimizing for performance and governance. Which scenario is best suited for using a managed table over an external table?",
    o: [
      'You have data stored in a cloud-based data lake that you want to query without moving it',
      'You need to ensure that the lifecycle of the data is automatically managed along with the table',
      'You need to share the data with other systems that do not use Databricks',
      'You want to directly manage the location and format of the underlying data files',
    ],
    a: [1],
    x: 'Las tablas managed delegan a Databricks la gestión completa del ciclo de vida de los datos (incluyendo su eliminación al hacer DROP), ideal cuando no se necesita control externo sobre la ubicación o el formato.',
  },
  {
    n: 32,
    d: 'GOV',
    m: 0,
    q: 'A data engineer is setting up Delta Sharing for a new partner without a Databricks workspace. What is a limitation to consider when using open sharing with OIDC token federation?',
    o: [
      'OIDC token federation requires the recipient to have a Databricks workspace',
      'It does not support real-time data updates',
      'It requires the recipient to manage their own authentication mechanism',
      'OIDC tokens allow unlimited access to the shared data without requiring token renewal',
    ],
    a: [2],
    x: 'Con OIDC federation en open sharing, el recipient debe gestionar su propio mecanismo de autenticación (cliente compatible con Delta Sharing, renovación de tokens de corta duración), a diferencia de un simple token bearer estático.',
  },
  {
    n: 33,
    d: 'GOV',
    m: 0,
    q: 'A data provider has shared several Delta tables with a partner via Databricks-to-Databricks Delta Sharing and wants to monitor who is accessing the shared data for governance and auditing. Which feature should they use?',
    o: [
      'Enable OIDC federation to capture external IdP-level access logs',
      'Use Unity Catalog audit logs to track access to the shared Delta tables',
      'Configure cluster driver logs to record queries on shared data',
      'Use Delta Sharing profiles to capture recipient usage statistics',
    ],
    a: [1],
    x: 'Los audit logs de Unity Catalog proporcionan un registro centralizado a nivel de cuenta de quién accedió a qué datos compartidos, cuándo y desde qué workspace, cubriendo el requisito de gobernanza.',
  },
  {
    n: 34,
    d: 'TRO',
    m: 0,
    q: 'What is the primary purpose of an alert in the Databricks SQL environment?',
    o: [
      'To trigger notifications when the result of a scheduled query meets a condition',
      'To automatically apply cost-based optimization to SQL queries',
      'To periodically refresh SQL dashboards and apply real-time visual filters',
      'To track changes to Delta tables',
      'To enforce access control policies on SQL dashboards using tags and ACLs',
    ],
    a: [0],
    x: 'Las Alerts de Databricks SQL monitorizan el resultado de una query programada y notifican automáticamente cuando se cumple una condición definida (p. ej. un umbral superado).',
  },
  {
    n: 35,
    d: 'TRA',
    m: 0,
    q: 'Raw data is already ingested into a Bronze Delta table at /mnt/bronze. The engineer wants to stream this data into a Silver Delta table, filtering out null event_type and selecting only user_id, event_type, timestamp. Which query correctly performs this Bronze-to-Silver hop?',
    o: [
      '`spark.readStream.format("delta").load("/mnt/bronze").filter("event_type IS NOT NULL").select("user_id","event_type").writeStream.format("delta").option("checkpointLocation","/mnt/checkpoints/silver").start("/mnt/silver")`',
      '`spark.readStream.format("delta").load("/mnt/bronze").filter("event_type IS NOT NULL").select("user_id","event_type","timestamp").write.format("delta").mode("append").save("/mnt/silver")`',
      '`spark.readStream.format("delta").load("/mnt/bronze").filter("event_type IS NOT NULL").select("user_id","event_type","timestamp").writeStream.format("delta").option("checkpointLocation","/mnt/checkpoints/silver").start("/mnt/silver")`',
      '`spark.readStream.format("delta").load("/mnt/bronze").select("user_id","event_type","timestamp").writeStream.format("delta").option("checkpointLocation","/mnt/checkpoints/silver").start("/mnt/silver")`',
      '`spark.read.format("delta").load("/mnt/bronze").filter("event_type IS NOT NULL").select("user_id","event_type","timestamp").write.format("delta").mode("append").save("/mnt/silver")`',
    ],
    a: [2],
    x: 'La opción correcta usa `readStream`/`writeStream` (streaming real, no batch), filtra por `event_type IS NOT NULL`, selecciona las tres columnas requeridas, y especifica `checkpointLocation` para tolerancia a fallos.',
  },
  {
    n: 36,
    d: 'TRA',
    m: 1,
    q: 'What is a key benefit of ACID transactions in Delta Lake? (Choose two)',
    o: [
      'They allow faster query execution using Delta caching',
      'They ensure data consistency during concurrent reads and writes',
      'They guarantee that write operations are either fully committed or rolled back',
      'They automatically compact small Parquet files',
      'They remove the need for schema enforcement',
    ],
    a: [1, 2],
    x: 'ACID garantiza aislamiento/consistencia entre lecturas y escrituras concurrentes, y atomicidad (todo o nada) en cada transacción; el caching y la compactación son funcionalidades separadas, no parte de las garantías ACID.',
  },
  {
    n: 37,
    d: 'P',
    m: 0,
    q: 'A machine learning engineer collaborating with a data science team needs everyone to use the same Spark/Python versions and have MLflow/scikit-learn available without manual installation. What is the most effective way to meet these requirements?',
    o: [
      'Use a Delta Live Table pipeline to sync library versions across teams',
      'Use a shared cluster configured with Databricks Runtime for Machine Learning',
      'Create a Unity Catalog schema with all required ML packages attached',
      "Install libraries manually on each user's cluster to ensure consistency",
      'Configure a serverless SQL warehouse with MLflow installed as a custom library',
    ],
    a: [1],
    x: 'Databricks Runtime for Machine Learning viene preinstalado con MLflow, scikit-learn y otras librerías ML populares, garantizando el mismo entorno para todo el equipo sin instalación manual.',
  },
  {
    n: 38,
    d: 'GOV',
    m: 0,
    q: 'Which of the following is a limitation of Delta Sharing?',
    o: [
      'Recipients must have access to a Databricks workspace to query shared data',
      'Delta Sharing only enables sharing of data; other Databricks assets cannot be shared',
      'Recipients can only access data in Parquet format',
      'Both provider and recipient must be on the same cloud provider',
      'Recipients can only query shared data in read-only mode',
    ],
    a: [4],
    x: 'La limitación fundamental de Delta Sharing es que el acceso del recipient es siempre de solo lectura; puede compartirse sin cuenta Databricks, entre distintas nubes, y (en Databricks-to-Databricks) también otros assets además de tablas.',
  },
  {
    n: 39,
    d: 'JOBS',
    m: 0,
    q: 'A DLT pipeline processes IoT sensor data arriving approximately every 3-4 hours; the team needs reliability but not real-time insights, and wants to minimize compute costs between runs. Which DLT pipeline mode best suits this?',
    o: [
      'Continuous mode',
      'Streaming with Auto Loader mode',
      'Triggered mode',
      'Interactive mode',
      'Real-time mode',
    ],
    a: [2],
    x: 'Triggered mode ejecuta una actualización, procesa los datos disponibles y luego apaga el cómputo, ideal para cargas periódicas cada pocas horas donde no se necesita baja latencia continua.',
  },
  {
    n: 40,
    d: 'GOV',
    m: 0,
    q: 'A marketing agency wants to share campaign performance data with a partner using Tableau but no Databricks workspace, requiring direct access from their BI tool. Which type of Delta Sharing should be used?',
    o: [
      'Databricks REST API',
      'Open Sharing protocol',
      'Lakehouse Federation',
      'Databricks-to-Databricks protocol',
      'Unity Catalog external locations',
    ],
    a: [1],
    x: 'El Open Sharing protocol de Delta Sharing permite conectar herramientas externas como Tableau directamente sin necesidad de un workspace Databricks para el recipient.',
  },
  {
    n: 41,
    d: 'P',
    m: 0,
    q: 'Which of the following best describes the relationship between a Data Lakehouse and a traditional data warehouse?',
    o: [
      'A Data Lakehouse is simply a cloud-hosted version of a Data Warehouse',
      'A Data Lakehouse combines the capabilities of Data Lakes and Data Warehouses, enabling ACID transactions and support for both structured and unstructured data',
      'A Data Lakehouse uses the same architecture as a Data Warehouse but adds streaming capabilities',
      'A Data Lakehouse completely replaces traditional Data Warehouses by storing only structured data',
      'A Data Lakehouse separates raw and processed data into different storage systems',
    ],
    a: [1],
    x: 'El lakehouse unifica lo mejor de los data lakes (flexibilidad, datos no estructurados) y los data warehouses (ACID, esquema, rendimiento), sin ser simplemente una versión cloud de un warehouse ni limitarse a datos estructurados.',
  },
  {
    n: 42,
    d: 'P',
    m: 0,
    q: 'Which feature allows users to write and execute code in multiple languages within the same Databricks notebook?',
    o: [
      'Cross-language SQL translation engine',
      'Magic commands engine using default notebook language setting',
      'Databricks Repos with integrated CI/CD pipelines',
      'Use of language-specific magic commands like %python, %sql, %scala, and %sh',
      'Multi-kernel runtime attached to the notebook',
    ],
    a: [3],
    x: 'Los comandos mágicos por celda (`%python`, `%sql`, `%scala`, `%sh`) permiten mezclar lenguajes dentro del mismo notebook, cada celda ejecutándose en el lenguaje indicado.',
  },
  {
    n: 43,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to infer the schema from a raw JSON string to help parse it later. Which Spark SQL function should they use?',
    o: [
      '`parse_schema()`',
      '`infer_json_schema()`',
      '`json_schema_extract()`',
      '`get_json_schema()`',
      '`schema_of_json()`',
    ],
    a: [4],
    x: '`schema_of_json(cadena_json)` devuelve el esquema inferido de una cadena JSON de ejemplo, comúnmente usado luego con `from_json()`.',
  },
  {
    n: 44,
    d: 'TRO',
    m: 0,
    q: 'A Delta table has accumulated many small Parquet files due to frequent streaming appends, degrading read performance. What command should the data engineer use to compact these small files?',
    o: ['VACUUM', 'ANALYZE TABLE', 'MERGE INTO', 'COMPACT', 'OPTIMIZE'],
    a: [4],
    x: '`OPTIMIZE` compacta los archivos pequeños en archivos más grandes, reduciendo el overhead de lectura causado por escrituras frecuentes en streaming; `VACUUM` solo limpia archivos obsoletos, no compacta.',
  },
  {
    n: 45,
    d: 'TRA',
    m: 0,
    q: 'You need to revert a Delta Lake table to a previous state due to an erroneous update, ensuring no data is lost during this process. Which operation should you perform?',
    o: ['VACUUM', 'RESTORE', 'DROP TABLE', 'CLONE'],
    a: [1],
    x: '`RESTORE TABLE ... TO VERSION/TIMESTAMP AS OF ...` revierte la tabla a un estado anterior de forma atómica, aprovechando el transaction log de Delta Lake sin perder datos.',
  },
  {
    n: 46,
    d: 'JOBS',
    m: 0,
    q: 'A scheduled job sometimes fails due to temporary cloud storage access issues that resolve on retry. The engineer wants Databricks to automatically retry the failed task up to 5 times before marking it failed. Which configuration meets this requirement?',
    o: [
      'Job retry configuration',
      'Notebook-level retry configuration',
      'Cluster recovery configuration',
      'Task retry configuration',
      'Job timeout configuration',
    ],
    a: [3],
    x: 'La configuración de reintentos a nivel de tarea (`max_retries`) permite que Databricks relance automáticamente una tarea que falla por errores transitorios, sin intervención manual.',
  },
  {
    n: 47,
    d: 'CICD',
    m: 0,
    q: "A healthcare research team wants to build Spark applications in PyCharm with interactive debugging, but their laptops can't process large datasets stored in the Lakehouse. Which approach best meets their needs?",
    o: [
      'Run Spark locally with sampled datasets for development',
      'Upload Python files to Databricks and run them as scheduled jobs',
      'Use Databricks Connect to link their IDE with a Databricks cluster for execution',
      'Export their code as a JAR and run it from the Databricks CLI',
      'Switch entirely to Databricks notebooks for development',
    ],
    a: [2],
    x: 'Databricks Connect permite seguir usando PyCharm con debugging interactivo local, mientras el código Spark se ejecuta realmente en un cluster remoto de Databricks a escala completa.',
  },
  {
    n: 48,
    d: 'JOBS',
    m: 0,
    q: 'A manufacturing company manages a daily ETL process that must run at 5AM without manual intervention, with failure notifications and success notifications to business users. Which Databricks feature should be used?',
    o: [
      'Databricks Schedulers',
      'Databricks Jobs',
      'Databricks Triggers',
      'Databricks Pipelines',
      'Databricks Task Scheduler',
    ],
    a: [1],
    x: 'Databricks Jobs (Workflows) programa la ejecución automática de notebooks/tareas y soporta notificaciones configurables de éxito y fallo, cubriendo todo el requisito de automatización descrito.',
  },
  {
    n: 49,
    d: 'GOV',
    m: 0,
    q: 'A global enterprise has data distributed across multiple cloud regions and wants to implement Unity Catalog in a scalable way. What is the recommended best practice for configuring Unity Catalog metastores?',
    o: [
      'Assign each business unit a separate catalog inside a single global metastore',
      'Use Hive metastores with custom audit logging for each region',
      'Create one metastore per region to minimize egress costs and improve latency',
      'Use a single Unity Catalog metastore across all regions to simplify management',
      'Deploy one metastore per workspace and synchronize them weekly',
    ],
    a: [2],
    x: 'Databricks permite (y recomienda) un metastore de Unity Catalog por región, alineado con el cómputo y almacenamiento locales, minimizando latencia y coste de egress entre regiones.',
  },
  {
    n: 50,
    d: 'ING',
    m: 0,
    q: 'A data engineer sees a streaming source defined with `spark.readStream.format("cloudFiles").option("cloudFiles.format", "json").load("abfss://landing@datalake.dfs.core.windows.net/raw/")`. What does this indicate about the data ingestion method?',
    o: [
      'The pipeline uses a batch read from Azure Data Lake',
      "The pipeline uses Delta Lake's transaction log for ingestion",
      'The pipeline uses Kafka for real-time JSON ingestion',
      'The pipeline uses Auto Loader to ingest streaming JSON files',
      'The pipeline uses COPY INTO to ingest files from the raw zone',
    ],
    a: [3],
    x: '`format("cloudFiles")` es la fuente de Auto Loader; junto con `readStream` y `cloudFiles.format="json"`, indica ingesta streaming incremental de archivos JSON desde Azure Data Lake.',
  },
  {
    n: 51,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to apply a 20% tax to all item prices in a map column item_prices (item names as keys, prices as values), keeping keys unchanged. Which SQL expression correctly does this?',
    o: [
      '`transform_keys(item_prices, (k, v) -> v * 1.20)`',
      '`map_values(item_prices, v -> v * 1.20)`',
      '`transform(item_prices, (k, v) -> struct(k, v * 1.20))`',
      '`map(item_prices, (k, v) -> (k, v * 1.20))`',
      '`transform_values(item_prices, (k, v) -> round(v * 1.20, 2))`',
    ],
    a: [4],
    x: '`transform_values` aplica una función solo a los valores de un MAP, preservando las claves intactas; `transform_keys` modificaría las claves, `transform` es para arrays, y `map`/`map_values` no encajan aquí.',
  },
  {
    n: 52,
    d: 'GOV',
    m: 0,
    q: 'The reporting_team group has SELECT on orders_summary and USE SCHEMA on sales_q1 (in catalog analytics), but still gets permission errors querying the table. Which step resolves the issue?',
    o: [
      'Enable lineage tracking for the reporting_team group in the Unity Catalog metastore',
      'Grant ALL PRIVILEGES on the sales_q1 schema to the reporting_team group',
      'Grant the SELECT privilege again using the ALL PRIVILEGES keyword on the orders_summary table',
      'Grant the USE CATALOG privilege on the analytics catalog to the reporting_team group',
      'Assign the reporting_team group as the owner of the orders_summary table',
    ],
    a: [3],
    x: 'Falta `USE CATALOG` sobre el catálogo `analytics`: sin ese privilegio de nivel superior, el grupo no puede atravesar la jerarquía hasta la tabla, aunque tenga `SELECT` y `USE SCHEMA` correctos.',
  },
  {
    n: 53,
    d: 'TRA',
    m: 0,
    q: 'A data engineer creates a table with `CREATE TABLE bronze.sales (...)` without specifying a storage location. Which statement about this table is most likely true?',
    o: [
      'The table is a Delta Live Table',
      'The table is a global temporary object',
      'The table is an external table',
      'The table is a temporary view',
      'The table is a managed table',
    ],
    a: [4],
    x: 'Sin cláusula `LOCATION`, `CREATE TABLE` crea por defecto una tabla managed, cuyo ciclo de vida (datos y metadatos) gestiona Databricks por completo.',
  },
  {
    n: 54,
    d: 'TRO',
    m: 0,
    q: "A data engineer adds new environment variables to a cluster's configuration via init scripts, but after saving and running the notebook, the variables are not available. What should the engineer do?",
    o: [
      'Enable Unity Catalog to propagate cluster configurations',
      'Create a new notebook session to inherit the cluster changes',
      'Restart the cluster to apply the updated environment variables',
      'Detach and reattach the notebook to refresh the configurations',
      'Re-run all cells in the notebook from the beginning',
    ],
    a: [2],
    x: 'Los cambios de configuración de cluster (init scripts, variables de entorno) solo se aplican al arrancar el cluster; hay que reiniciarlo para que los nuevos valores tomen efecto.',
  },
  {
    n: 55,
    d: 'TRA',
    m: 0,
    q: 'A team wants a Structured Streaming pipeline for IoT data to process new data every 60 seconds using micro-batches. Which trigger configuration is correct?',
    o: [
      '`Trigger.AvailableNow("60 seconds")`',
      '`Trigger.ProcessingTime("1 minute")`',
      '`Trigger.Once()`',
      '`Trigger.ProcessingTime("60000")`',
      '`Trigger.Continuous("60 seconds")`',
    ],
    a: [1],
    x: '`Trigger.ProcessingTime("1 minute")` (equivalente a 60 segundos) ejecuta micro-batches a intervalo fijo; `Continuous` es un modo distinto (procesamiento continuo con soporte limitado de operaciones), y `Once`/`AvailableNow` no son recurrentes.',
  },
  {
    n: 56,
    d: 'CICD',
    m: 0,
    q: 'A developer runs a PySpark application through Databricks Connect in VS Code, reading a Delta table, performing Spark SQL aggregations, then using Python list comprehensions on collected results. Where does each part of the code run?',
    o: [
      'Both Spark and Python code execute on the Databricks cluster',
      'Only SQL queries can be executed with Databricks Connect',
      'Spark code executes on the Databricks cluster, while Python code executes locally',
      'Both Spark and Python code execute locally in the IDE',
      'Spark code executes locally in the IDE, while Python code executes on the cluster',
    ],
    a: [2],
    x: 'Databricks Connect ejecuta las operaciones Spark de forma remota en el cluster, mientras que el código Python puro (bucles, list comprehensions) corre localmente en el IDE del desarrollador.',
  },
  {
    n: 57,
    d: 'CICD',
    m: 0,
    q: "Which of the following is a limitation of Databricks' built-in notebook version history compared to using Databricks Repos?",
    o: [
      'It automatically syncs notebook changes with Git repositories without user intervention',
      'It cannot be used with notebooks written in multiple languages',
      'It lacks full Git version control features like branching, pull requests, and commit messages',
      'It does not allow multiple users to view or edit the notebook at the same time',
      'It does not support running the same notebook in different environments',
    ],
    a: [2],
    x: 'El historial de versiones nativo de los notebooks solo permite ver/restaurar versiones anteriores, sin las capacidades completas de Git (ramas, pull requests, mensajes de commit) que sí ofrece Databricks Repos.',
  },
  {
    n: 58,
    d: 'CICD',
    m: 0,
    q: "Two engineers work on the same notebook in a Databricks Repo. One pushes changes to the remote branch; the other, unaware, tries to push their own changes without pulling first, getting a 'branch is behind' error. Why, and how should they resolve it?",
    o: [
      'Their local cluster is out of sync with the control plane, and they need to restart it to apply Git changes',
      'The engineer tried to push to a read-only branch and must request write access from the repo owner',
      'Databricks does not support simultaneous editing in Repos, so the notebook must be locked for exclusive use',
      'They committed the notebook with the wrong Git user settings and should configure Git credentials again',
      "The remote branch has new commits they haven't pulled; they must perform a Git Pull to merge before pushing",
    ],
    a: [4],
    x: 'Git bloquea el push si hay commits nuevos en la rama remota que aún no se han integrado localmente; la solución es hacer pull, fusionar los cambios (resolviendo conflictos si los hay), y luego pushear.',
  },
];

export const exam24: RawQuestion[] = [
  {
    n: 1,
    d: 'TRA',
    m: 0,
    q: 'In Delta Lake, which component is responsible for maintaining transaction metadata such as table versions, data file paths, and schema changes?',
    o: [
      'Table access control list',
      '_delta_log transaction log',
      'Parquet file footer',
      'Delta caching layer',
      'Unity Catalog',
    ],
    a: [1],
    x: 'El directorio `_delta_log` guarda el transaction log (JSON y checkpoints Parquet) que registra versiones, rutas de archivos y cambios de esquema, siendo el núcleo de las garantías ACID de Delta Lake.',
  },
  {
    n: 2,
    d: 'P',
    m: 0,
    q: "Which of the following technologies form the core of Databricks' Data Lakehouse architecture?",
    o: [
      'Apache Airflow, Delta Lake, Unity Catalog, and Amazon Redshift',
      'Apache Spark, Delta Lake, Azure Synapse, and GitHub Actions',
      'Apache Spark, Parquet, MLflow, and Hadoop HDFS',
      'Apache Hive, Delta Lake, Power BI, and Unity Catalog',
      'Apache Spark, Delta Lake, Unity Catalog, and Databricks SQL',
    ],
    a: [4],
    x: 'Spark (cómputo), Delta Lake (almacenamiento ACID), Unity Catalog (gobernanza) y Databricks SQL (BI) forman conjuntamente el núcleo de la arquitectura Lakehouse de Databricks.',
  },
  {
    n: 3,
    d: 'GOV',
    m: 0,
    q: 'A company discovers sensitive customer data may have been accessed by unauthorized users, with no record of who accessed it or when. Which governance gap is most likely responsible?',
    o: [
      'Inadequate access controls and missing audit logs',
      'Schema evolution during ingestion and lack of version control',
      'Inefficient job scheduling and lack of cluster pooling',
      'Misconfigured task dependencies and low task retries',
      'Lack of query result caching and auto-termination policies',
    ],
    a: [0],
    x: 'Acceso no autorizado sin rastro de quién/cuándo apunta directamente a controles de acceso débiles y ausencia de audit logs, ambos cubiertos por Unity Catalog cuando se configura correctamente.',
  },
  {
    n: 4,
    d: 'CICD',
    m: 0,
    q: 'A developer runs a PySpark app via Databricks Connect on a cluster with Databricks Runtime 15.4 LTS and Python 3.11, and sees runtime errors when starting the session. What could be the reason?',
    o: [
      'The Databricks Connect library only supports SQL, not PySpark',
      'The developer must use notebooks instead of an IDE with Connect',
      'The cluster needs to be restarted after every Connect session',
      "The developer's local Python version is incompatible with the cluster runtime",
      'The cluster does not support Databricks Connect',
    ],
    a: [3],
    x: 'Databricks Connect requiere que la versión local de Python coincida con la del Databricks Runtime del cluster; un desajuste de versión (p. ej. local 3.10/3.12 vs cluster 3.11) provoca errores de runtime al iniciar la sesión.',
  },
  {
    n: 5,
    d: 'TRA',
    m: 0,
    q: 'A team uses a numeric payment_status column across multiple ETL pipelines and wants to convert these codes to descriptive strings without repeating logic in every query. Which is the best solution?',
    o: [
      'Use a JOIN with a lookup table in every query',
      'Use a CASE WHEN statement in each query to convert the status code',
      'Create a SQL user-defined function (UDF) that maps status codes to descriptive text and use the UDF in every query',
      'Manually document the mappings and inform all team members to apply them consistently',
      'Replace all numeric codes with strings directly in the source table',
    ],
    a: [2],
    x: 'Una UDF SQL centraliza la lógica de mapeo en un único lugar reutilizable en todas las queries, facilitando el mantenimiento frente a repetir `CASE WHEN` o `JOIN`s en cada consulta.',
  },
  {
    n: 6,
    d: 'GOV',
    m: 0,
    q: 'A data analyst wants to visually trace the data flow that led to a gold-level column (calculated from item_price * item_quantity), including upstream tables, transformations, and column-level derivation. Which built-in Databricks feature should they use?',
    o: [
      'System information_schema',
      'Global Search',
      'Lineage tab within Catalog Explorer',
      'SQL Dashboard audit history',
      'Delta Lake transaction log',
    ],
    a: [2],
    x: 'La pestaña Lineage de Catalog Explorer (Unity Catalog) ofrece linaje visual a nivel de columna, mostrando tablas de origen y transformaciones que derivan en una columna concreta.',
  },
  {
    n: 7,
    d: 'TRA',
    m: 0,
    q: 'A Spark SQL table order_items has an array column items, each element a struct with name (STRING) and price (DOUBLE). Add 10% tax to each price and uppercase the name, preserving the array structure. Which query achieves this?',
    o: [
      '`SELECT map(items, x -> struct(upper(x.name), round(x.price * 1.10, 2))) FROM order_items;`',
      '`SELECT explode(items) FROM order_items;`',
      '`SELECT transform(items, x -> struct(x.name, round(x.price * 1.10, 2))) FROM order_items;`',
      '`SELECT transform(items, x -> struct(x.name, x.price * 1.10)) FROM order_items;`',
      '`SELECT transform(items, x -> struct(upper(x.name), round(x.price * 1.10, 2))) FROM order_items;`',
    ],
    a: [4],
    x: '`transform` recorre cada elemento del array aplicando una función; construir un `struct` con `upper(x.name)` y `round(x.price * 1.10, 2)` cumple ambos requisitos preservando la estructura del array; `map()` es para MAPs, no arrays.',
  },
  {
    n: 8,
    d: 'JOBS',
    m: 0,
    q: 'A DLT pipeline should automatically discard rows where email is NULL, continue processing valid rows, and track how many rows were discarded. Which SQL statement correctly implements this?',
    o: [
      '`CONSTRAINT valid_email EXPECT (email IS NOT NULL) ON VIOLATION WARN`',
      '`CONSTRAINT valid_email EXPECT (email IS NOT NULL) ON VIOLATION DROP RECORD`',
      '`CONSTRAINT valid_email EXPECT (email IS NOT NULL)`',
      '`CONSTRAINT valid_email EXPECT (email IS NOT NULL) ON VIOLATION DROP ROW`',
    ],
    a: [3],
    x: '`ON VIOLATION DROP ROW` descarta las filas inválidas antes de escribirlas, dejando pasar solo las válidas, mientras DLT registra automáticamente el conteo de descartes en el event log; `DROP RECORD` no es una acción válida, y `WARN`/sin cláusula conservaría las filas inválidas.',
  },
  {
    n: 9,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to make a cleaned dataset from one notebook temporarily available to other notebooks on the same cluster, without writing to cloud storage or registering in Unity Catalog/Hive Metastore, but persisting beyond the current notebook. Which object should they create?',
    o: ['External table', 'Managed table', 'Materialized view', 'Temporary view', 'Global temporary view'],
    a: [4],
    x: 'Una vista temporal global persiste mientras el cluster esté activo y es accesible desde cualquier notebook del mismo cluster, sin escribir a almacenamiento externo ni registrarse en un catálogo.',
  },
  {
    n: 10,
    d: 'TRO',
    m: 0,
    q: 'A data scientist steps away from an interactive notebook session on an all-purpose cluster, and returns to find the cluster terminated, causing errors when running cells. What is the most likely reason?',
    o: [
      'The cluster was configured with auto termination and was shut down due to inactivity',
      'The notebook lost its Git connection and cannot sync',
      'A runtime version mismatch caused the kernel to crash silently',
      'The cluster switched to maintenance mode and queued all commands',
      'Unity Catalog policies prevented further cell execution without user approval',
    ],
    a: [0],
    x: 'Los clusters all-purpose suelen tener auto termination habilitada para ahorrar costes; tras el periodo de inactividad configurado, el cluster se apaga automáticamente, provocando errores al intentar ejecutar celdas.',
  },
  {
    n: 11,
    d: 'GOV',
    m: 0,
    q: 'Analysts need quick access to join operational SQL Server data with clinical data in the Lakehouse, without building pipelines or duplicating the SQL Server data. Which approach best meets this requirement?',
    o: [
      'Export SQL Server data as CSV files and upload them to Databricks',
      'Replicate the SQL Server database into cloud storage',
      'Build an ETL pipeline to ingest SQL Server data daily',
      'Use Delta Sharing',
      'Use Lakehouse Federation to connect to the SQL Server database',
    ],
    a: [4],
    x: 'Lakehouse Federation permite consultar SQL Server directamente desde Databricks (con pushdown), sin duplicar datos ni construir pipelines de ingesta, cumpliendo el requisito de acceso rápido en tiempo real.',
  },
  {
    n: 12,
    d: 'TRA',
    m: 0,
    q: 'You have sales (sale_id, product_id, sale_amount) and products (product_id, product_name) tables. Retrieve all sales rows with matching product names, including sales rows where the product no longer exists (NULL for missing names). Which query does this?',
    o: [
      '`... JOIN products p ON s.product_id = p.product_id;` (INNER JOIN)',
      '`... RIGHT JOIN products p ON s.product_id = p.product_id;`',
      '`... LEFT JOIN products p ON s.product_id = p.product_id;` (sales as left table)',
      '`FROM products p LEFT JOIN sales s ON s.product_id = p.product_id;`',
      '`... FULL OUTER JOIN products p ON s.product_id = p.product_id;`',
    ],
    a: [2],
    x: '`LEFT JOIN` con `sales` como tabla izquierda conserva todas las filas de sales, con `NULL` en `product_name` cuando no hay producto coincidente; `INNER JOIN` excluiría esas filas, y las demás opciones priorizan la tabla equivocada o añaden filas de más.',
  },
  {
    n: 13,
    d: 'P',
    m: 0,
    q: 'A media analytics company has data spread across a Data Lake and a Data Warehouse, causing rising costs and operational complexity, while needing to support both data science and BI workloads. Which solution unifies their data and reduces costs?',
    o: [
      'Adopt a Data Lakehouse architecture',
      'Implement a data virtualization layer to combine both systems',
      'Replace the Data Lake with a larger Data Warehouse cluster',
      'Migrate all analytics workloads to a real-time streaming platform',
      'Create separate pipelines optimized for each workload',
    ],
    a: [0],
    x: 'El lakehouse unifica capacidades de data lake (datos crudos, ciencia de datos) y data warehouse (curado, BI) en una sola plataforma, reduciendo duplicación de infraestructura y coste operativo.',
  },
  {
    n: 14,
    d: 'CICD',
    m: 0,
    q: 'A team uses GitHub for version control with feature branches, peer review before merging to main, and automated testing/deployment after merge. How can Databricks Repos help enable this workflow?',
    o: [
      'By publishing notebook changes directly to dashboards and Power BI workspaces for testing',
      'By syncing notebooks with Git branches to support branch-based development, peer review, and external CI/CD integration',
      'By allowing users to schedule pull requests and cluster restarts from the Databricks UI',
      'By enabling team members to manually export notebooks as HTML for peer reviews',
      'By automatically converting notebooks into production jobs after a pull request is created',
    ],
    a: [1],
    x: 'Databricks Repos sincroniza notebooks con ramas Git, permitiendo desarrollo por rama, mientras que pull requests y revisiones ocurren en GitHub, y el CI/CD externo se encarga de test/despliegue tras el merge.',
  },
  {
    n: 15,
    d: 'TRO',
    m: 0,
    q: 'Why would a data engineer apply Z-Ordering to a column in a Delta table that is frequently used in query filters?',
    o: [
      'To improve data compression by grouping similar values',
      'To enable schema evolution during streaming writes',
      'To increase the number of partitions and improve parallelism',
      'To organize data in a way that enhances data skipping and read efficiency',
      'To reduce the need for running OPTIMIZE on the table',
    ],
    a: [3],
    x: 'Z-Ordering coloca físicamente juntos los datos relacionados según las columnas especificadas, mejorando el data skipping y reduciendo el I/O de las consultas que filtran por esas columnas.',
  },
  {
    n: 16,
    d: 'JOBS',
    m: 0,
    q: 'A team uses Databricks Jobs to refresh dashboards for three business units, each via a parallel task, and wants a single notification only after all refreshes complete, without adding tasks that perform no data processing. Best configuration?',
    o: [
      'Configure each task with individual notifications and aggregate the responses manually',
      'Use cluster event logs to detect when all tasks are completed and send a custom alert',
      'Set a job-level notification to trigger on success',
      'Add a fourth task that depends on all three dashboard refresh tasks, and send a notification from that task',
      'Add a separate monitoring task at the end of the job that triggers only if all previous tasks succeed',
    ],
    a: [2],
    x: 'Una notificación a nivel de job se dispara una sola vez cuando todo el job (todas las tareas paralelas) termina, sin necesitar tareas adicionales de monitorización que no procesen datos.',
  },
  {
    n: 17,
    d: 'TRA',
    m: 0,
    q: 'Bronze and Silver layers of a DLT pipeline are implemented with streaming tables. The team now wants a Gold layer with aggregated metrics (daily active users, clicks per region) that refreshes automatically as new data arrives, but low-latency is not required. Which definition is most appropriate?',
    o: [
      '`CREATE STREAMING TABLE gold_metrics ...`',
      '`CREATE OR REFRESH STREAMING VIEW gold_metrics ...`',
      '`CREATE TABLE gold_metrics ...`',
      '`CREATE OR REFRESH MATERIALIZED VIEW gold_metrics ...`',
      '`CREATE OR REFRESH MATERIALIZED TABLE gold_metrics ...`',
    ],
    a: [3],
    x: 'Una vista materializada en DLT se refresca incrementalmente de forma automática sin exigir la semántica de baja latencia de una streaming table, encajando perfectamente con métricas agregadas de Gold.',
  },
  {
    n: 18,
    d: 'JOBS',
    m: 0,
    q: 'A Databricks Job has Task A (financial data) and Task B (marketing data) that can run independently, and Task C (consolidated report) that should only run after both A and B succeed, and not run if either fails. How should the engineer configure this?',
    o: [
      'Implement try-catch logic within Task C to handle dependency on Task A and B',
      'Create Task D that monitors Task A and Task B, and triggers Task C only if both complete successfully',
      'Configure all tasks to run in parallel with no dependencies, and handle the dependency in the code logic',
      'Use task condition expressions in Task C to check success status of Task A and B before execution',
      'Configure Task C to depend on both Task A and Task B directly in the job settings',
    ],
    a: [4],
    x: 'Configurar Task C con dependencia directa sobre ambas Task A y Task B en la configuración del job es la forma nativa y correcta; Databricks garantiza que Task C solo arranque si ambas predecesoras tienen éxito.',
  },
  {
    n: 19,
    d: 'ING',
    m: 0,
    q: 'Which of the following scenarios is most appropriate for using Auto Loader?',
    o: [
      'Ingesting JSON data in real-time from a Kafka topic',
      'Reading a static Parquet file to create a temporary Spark view',
      'Migrating a large volume of historical records from an on-premises SQL Server',
      'Periodically ingesting new files that arrive in cloud storage into a Bronze table',
      'A one-time load of a large CSV file from Azure Data Lake into a Delta table',
    ],
    a: [3],
    x: 'Auto Loader está diseñado para detectar e ingerir de forma incremental los archivos nuevos que llegan a almacenamiento en la nube; no es la herramienta para Kafka, migraciones de bases de datos, ni cargas puntuales estáticas.',
  },
  {
    n: 20,
    d: 'CICD',
    m: 0,
    q: 'Data engineers across regions prefer different local IDEs (VS Code, PyCharm), want to run tests and debug interactively, but still use a centralized Databricks cluster for Spark execution against the Lakehouse, without notebooks-only development. Which feature enables this?',
    o: [
      'Lakehouse Federation',
      'Unity Catalog',
      'Delta Sharing',
      'Databricks REST APIs',
      'Databricks Connect',
    ],
    a: [4],
    x: 'Databricks Connect permite trabajar desde cualquier IDE local compatible, con debugging interactivo, mientras el código Spark se ejecuta en un cluster Databricks remoto centralizado.',
  },
  {
    n: 21,
    d: 'GOV',
    m: 0,
    q: 'A global retailer shares product inventory tables from AWS US-East with a supplier consuming data daily from Azure EU-West using Delta Sharing. What cost consideration should the retailer be aware of?',
    o: [
      'Delta Sharing charges extra licensing fees per table shared across accounts',
      'Delta Sharing automatically eliminates all network transfer costs through compression',
      'Delta Sharing usage is free of network costs, regardless of cloud or region',
      'All costs are billed to the data recipient, not the provider',
      'Cross-cloud or cross-region data sharing may incur additional egress charges',
    ],
    a: [4],
    x: 'Delta Sharing en sí no cobra licencias por tabla, pero transferir datos entre nubes/regiones distintas (AWS a Azure aquí) puede generar cargos de egress del proveedor cloud de origen.',
  },
  {
    n: 22,
    d: 'TRA',
    m: 0,
    q: "A data engineer wants a report with one row per region and separate columns for each month's total sales, instead of separate rows per month. Which SQL clause restructures this from long to wide format?",
    o: ['EXPLODE', 'WINDOW', 'UNION', 'GROUP BY', 'PIVOT'],
    a: [4],
    x: '`PIVOT` transforma filas en columnas agregando valores (p. ej. `SUM(sales_amount)`) para cada valor distinto de una columna (aquí, mes), produciendo el formato ancho deseado.',
  },
  {
    n: 23,
    d: 'TRA',
    m: 1,
    q: 'Which of the following code snippets correctly loads an existing table silver.customers into a PySpark DataFrame? (Select two)',
    o: [
      '`df = spark.table("silver.customers")`',
      '`df = spark.read.table("silver.customers")`',
      '`df = spark.sql("SELECT * FROM silver.customers")`',
      '`df = read.table("silver.customers")`',
      '`df = spark.read.load("silver.customers")`',
    ],
    a: [0, 2],
    x: '`spark.table(nombre)` carga directamente una tabla registrada como DataFrame, y `spark.sql("SELECT * FROM ...")` logra lo mismo vía SQL; `spark.read` no tiene método `.table()`, `read` no es un objeto independiente, y `.load()` espera una ruta de archivo, no un nombre de tabla.',
  },
  {
    n: 24,
    d: 'TRA',
    m: 0,
    q: 'Raw JSON data is ingested into a Bronze Delta table with Auto Loader, cleaned into a Silver table via streaming. Now a new streaming query should read from Silver, perform a time-based aggregation, and write results into a Gold table. Which code correctly implements this Silver→Gold hop?',
    o: [
      '`spark.readStream.format("delta").load("/mnt/bronze_data").groupBy("device_id").count().writeStream.format("delta").start("/mnt/gold_data")`',
      '`spark.readStream.format("json").load("/mnt/silver_data").groupBy("device_id").count().writeStream.format("delta").start("/mnt/gold_data")`',
      '`spark.read.format("delta").load("/mnt/silver_data").groupBy("device_id").count().write.format("delta").mode("append").save("/mnt/gold_data")`',
      '`spark.readStream.format("delta").load("/mnt/silver_data").groupBy("device_id").count().writeStream.format("delta").option("checkpointLocation", "/mnt/checkpoints/gold").start("/mnt/gold_data")`',
      '`spark.readStream.format("delta").load("/mnt/silver_data").groupBy("device_id").count().writeStream.format("delta").save("/mnt/gold_data")`',
    ],
    a: [3],
    x: 'La opción correcta lee en streaming desde Silver (Delta), agrega, y escribe en streaming a Gold con `checkpointLocation` especificado (obligatorio para tolerancia a fallos); las demás usan la fuente equivocada, batch en vez de streaming, o falta el checkpoint.',
  },
  {
    n: 25,
    d: 'CICD',
    m: 0,
    q: 'Which of the following Git operations are available directly within Databricks Repos?',
    o: [
      'Automatically resolving merge conflicts across multiple branches',
      'Rebasing and cherry-picking commits across branches',
      'Running automated unit tests before pushing code',
      'Pulling, committing, and pushing changes to a connected Git repo',
      'Creating pull requests and managing code reviews directly from Databricks',
    ],
    a: [3],
    x: 'Databricks Repos soporta operaciones Git básicas (pull, commit, push) de forma nativa; rebase/cherry-pick avanzados, resolución automática de conflictos, tests automatizados y pull requests requieren un cliente Git externo o el proveedor Git.',
  },
  {
    n: 26,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to convert JSON strings in a column into a structured format using a predefined schema. Which Spark SQL function should they use?',
    o: ['`to_json()`', '`json_tuple()`', '`get_json_object()`', '`schema_of_json()`', '`from_json()`'],
    a: [4],
    x: '`from_json(columna, esquema)` parsea cadenas JSON a una estructura tipada según el esquema dado; `to_json` hace lo inverso, y las demás funciones extraen valores individuales, no la estructura completa.',
  },
  {
    n: 27,
    d: 'TRO',
    m: 0,
    q: 'Analysts frequently run concurrent queries against a Databricks SQL warehouse, and performance degrades as usage increases due to high query concurrency. What is the most appropriate configuration change to improve performance?',
    o: [
      'Increase the cluster size',
      'Increase the maximum number of clusters',
      'Enable query result caching',
      'Restart the SQL warehouse',
      'Extend the auto-stop timeout',
    ],
    a: [1],
    x: 'Ante degradación por concurrencia (muchas queries simultáneas), aumentar el máximo de clusters del warehouse permite escalar horizontalmente y repartir la carga entre más clusters, en vez de escalar un único cluster más grande.',
  },
  {
    n: 28,
    d: 'TRA',
    m: 0,
    q: 'An analyst and a data scientist collaborate on a single Databricks notebook, one preferring SQL and the other Python, wanting to avoid switching notebooks. How can they accomplish this?',
    o: [
      'Use Unity Catalog to assign different language permissions to each notebook cell',
      'Export Python cells as scripts and import them into a SQL dashboard',
      'Use language-specific magic commands like %sql and %python in the same notebook',
      'Set the default language of the notebook to Scala to support both users',
      'Use a notebook widget to switch the default language between SQL and Python',
    ],
    a: [2],
    x: 'Los comandos mágicos `%sql` y `%python` permiten a cada celda del mismo notebook usar el lenguaje preferido de cada colaborador, sin necesidad de notebooks separados ni exportaciones.',
  },
  {
    n: 29,
    d: 'TRA',
    m: 0,
    q: 'A data engineer creates a Delta table using CREATE TABLE without specifying LOCATION, then uses DROP TABLE to remove it. What happens to the underlying data files?',
    o: [
      'The files will be deleted automatically from managed storage',
      'The files will be archived to the _delta_log folder',
      'The files will remain only if the table was cached',
      'The files will be removed from the catalog but data files are retained on storage',
      'The files will persist, as the table was created externally',
    ],
    a: [0],
    x: 'Sin `LOCATION`, la tabla es managed; al hacer `DROP TABLE`, Databricks elimina automáticamente tanto los metadatos como los archivos de datos en el almacenamiento gestionado.',
  },
  {
    n: 30,
    d: 'TRA',
    m: 0,
    q: 'What is the primary purpose of using a Medallion Architecture in a Data Lakehouse environment?',
    o: [
      'To store data from different departments in physically isolated locations',
      'To improve data availability by duplicating all tables across cloud regions',
      'To organize data into progressively refined layers for scalability, quality, and analytics readiness',
      'To enforce strict schema rules during initial ingestion to avoid transformation steps',
      'To ensure data is stored in multiple file formats for compatibility',
    ],
    a: [2],
    x: 'El propósito central del Medallion es organizar los datos en capas de refinamiento progresivo (Bronze→Silver→Gold), mejorando calidad, trazabilidad y preparación para analítica, no aislar departamentos ni duplicar regiones.',
  },
  {
    n: 31,
    d: 'CICD',
    m: 0,
    q: 'A developer is creating a new Databricks Asset Bundle, needing to declare the bundle name, resources like jobs/clusters, and environment targets for dev/prod. Which configuration format must they use?',
    o: ['INI', 'XML', 'YAML', 'JSON', 'TOML'],
    a: [2],
    x: 'Los Databricks Asset Bundles se definen obligatoriamente en YAML (`databricks.yml`), el único formato soportado para declarar nombre, recursos y targets de entorno del bundle.',
  },
  {
    n: 32,
    d: 'GOV',
    m: 0,
    q: 'A data engineer wants to allow the model_training group to only use a UDF called predict_score() in the ml_utils schema of the projects catalog, without allowing them to modify it. Which privilege should be granted?',
    o: ['MODIFY', 'SELECT', 'ALL PRIVILEGES', 'USAGE', 'EXECUTE'],
    a: [4],
    x: 'El privilegio `EXECUTE` permite ejecutar una función sin poder modificarla ni acceder a otros objetos, cumpliendo el principio de mínimo privilegio para este caso de uso.',
  },
  {
    n: 33,
    d: 'TRA',
    m: 0,
    q: 'A data engineer needs to update a Delta table customers with new data from staging_customers: update existing records if customer_id matches, insert new records otherwise, atomically. Which SQL command should they use?',
    o: ['UPSERT', 'INSERT', 'MERGE', 'UPDATE', 'REPLACE'],
    a: [2],
    x: '`MERGE INTO ... WHEN MATCHED THEN UPDATE ... WHEN NOT MATCHED THEN INSERT ...` realiza ambas operaciones (update+insert) en una única transacción atómica; `UPSERT` no es un comando válido de Databricks SQL.',
  },
  {
    n: 34,
    d: 'TRA',
    m: 0,
    q: 'A data engineer wants to register a reusable object of raw JSON customer data for ad-hoc analysis by analysts, that persists beyond the session, is discoverable in the catalog with lineage/governance, supports RBAC, and does not store physical data (reflects a SQL query). Which object type should they create?',
    o: ['Global Temporary View', 'Managed Table', 'View', 'External Table', 'Temporary View'],
    a: [2],
    x: 'Una View persiste en el metastore (visible en el catálogo con linaje/gobernanza), soporta control de acceso, y no almacena datos físicos, reflejando siempre el resultado de la query subyacente; las tablas managed/external sí almacenan datos, y las vistas temporales no persisten.',
  },
  {
    n: 35,
    d: 'P',
    m: 0,
    q: 'Which command is used in a Databricks notebook to run another notebook and import its contents into the current session?',
    o: ['%include', '%import', '%run', '%attach', '%notebook'],
    a: [2],
    x: '`%run` ejecuta otro notebook e importa sus variables/funciones a la sesión del notebook actual; las demás no son comandos mágicos válidos.',
  },
  {
    n: 36,
    d: 'TRO',
    m: 0,
    q: 'A team is exploring a dataset collaboratively in real time using shared notebooks with SQL/Python, installing libraries and visualizing data interactively, with workloads not automated or scheduled. Which type of cluster is most appropriate?',
    o: [
      'Delta Live Table pipeline cluster',
      'Job cluster with ephemeral lifetime',
      'Kubernetes cluster designed for containerized microservices',
      'All-purpose cluster',
      'Serverless SQL Warehouse',
    ],
    a: [3],
    x: 'Los clusters all-purpose están diseñados justo para este caso: exploración interactiva y colaborativa en notebooks compartidos, sin automatización ni programación.',
  },
  {
    n: 37,
    d: 'GOV',
    m: 0,
    q: "A financial services company transfers CSV files via SFTP for quarterly regulator reports, which is slow and error-prone. They've enabled Unity Catalog and want to provide live, read-only access to Lakehouse tables without copies. Which feature should they use?",
    o: [
      'Delta Sharing',
      "Replicating the data into the regulator's cloud account",
      'Lakehouse Federation',
      'Databricks REST APIs',
      'Exporting parquet files into cloud storage',
    ],
    a: [0],
    x: 'Delta Sharing permite ofrecer acceso en vivo y de solo lectura a las tablas del Lakehouse, sin copiar datos, resolviendo directamente el problema de transferencias lentas y datos desactualizados vía SFTP.',
  },
  {
    n: 38,
    d: 'CICD',
    m: 0,
    q: 'A team manages deployments with custom REST API scripts, which is slow and error-prone. They want a native, supported, repeatable way to package code and deploy Databricks projects across environments. Which feature best addresses this?',
    o: [
      'Manually copying notebooks',
      'Databricks Asset Bundles',
      'Terraform provider',
      'Databricks REST APIs',
      'dbx from Databricks Labs',
    ],
    a: [1],
    x: 'Los Databricks Asset Bundles son el mecanismo nativo y soportado para empaquetar y desplegar recursos de forma declarativa y repetible entre entornos, reemplazando scripts REST API manuales.',
  },
  {
    n: 39,
    d: 'TRA',
    m: 0,
    q: 'A data engineer creates an external table in Databricks pointing to a folder with existing Parquet files, then runs `DROP TABLE bronze.products;`. What is the most likely outcome?',
    o: [
      'The DROP TABLE command fails because external tables cannot be dropped',
      'The DROP TABLE command converts the table to a managed table',
      'The table is renamed as bronze.products_backup',
      'Only the table metadata is deleted; the data files in the specified location remain intact',
      'The metadata and the underlying data files are both deleted from storage',
    ],
    a: [3],
    x: 'Para una tabla externa, el DROP solo elimina la entrada de metadatos del metastore; los archivos Parquet en la ubicación de almacenamiento permanecen intactos.',
  },
  {
    n: 40,
    d: 'TRO',
    m: 0,
    q: "A business analyst's dashboard tracks weekly customer churn (query scheduled every Monday), and they want an automatic Slack notification if churn exceeds 5% in any week. Which Databricks feature should they use?",
    o: [
      'Use conditional formatting in the dashboard and configure Slack integration from the dashboard settings',
      'Configure a Databricks SQL alert on the scheduled query and set the destination to a Slack webhook',
      'Schedule a notebook that queries the churn rate and sends a Slack message using dbutils.notebook.run()',
      'Enable alerting through Unity Catalog audit logs with Slack as a configured sink',
      "Export the dashboard to Slack weekly using the 'Share via Slack' option and manually monitor churn",
    ],
    a: [1],
    x: 'Una Alert de Databricks SQL configurada sobre la query programada, con destino un webhook de Slack, notifica automáticamente cuando la condición (churn > 5%) se cumple, sin scripts manuales.',
  },
  {
    n: 41,
    d: 'TRA',
    m: 0,
    q: 'Customer transaction data is stored as monthly JSON files (transactions_2023_oct.json, ..., transactions_2024_jan.json). Which query selects only data from the 2023 files using a wildcard pattern in Spark SQL?',
    o: [
      '`SELECT * FROM json.\\`/Volumes/commerce/landing/transactions/transactions_2023_*.json\\``',
      '`SELECT * FROM json.\\`/Volumes/.../transactions_2023%.json\\``',
      "`SELECT * FROM json.\\`/Volumes/.../transactions_*.json\\` WHERE file_name LIKE '%2023%'`",
      '`SELECT * FROM json.\\`/Volumes/.../transactions_2023_*.parquet\\``',
      "`SELECT * FROM json.\\`/Volumes/.../\\` WHERE file_name LIKE 'transactions_2023_%'`",
    ],
    a: [0],
    x: 'Spark SQL soporta comodines `*` directamente en la ruta de archivo; `transactions_2023_*.json` selecciona exactamente los archivos de 2023, sin necesitar una columna `file_name` (que no existe por defecto sin `INPUT_FILE_NAME()`).',
  },
  {
    n: 42,
    d: 'TRA',
    m: 1,
    q: 'A Spark table user_activity has a column session_duration. Which queries correctly count only rows where session_duration is NOT NULL? (Select two)',
    o: [
      '`SELECT COUNT(*) FROM user_activity WHERE session_duration IS NOT NULL;`',
      '`SELECT COUNT(*) FROM user_activity WHERE session_duration <> NULL;`',
      '`SELECT COUNT(*) FROM user_activity WHERE session_duration > NULL;`',
      '`SELECT COUNT(session_duration) FROM user_activity;`',
      '`SELECT COUNT(*) FROM user_activity WHERE session_duration != NULL;`',
    ],
    a: [0, 3],
    x: '`IS NOT NULL` es la comparación correcta con nulos, y `COUNT(columna)` ignora automáticamente los valores nulos de esa columna; comparar con `<>`/`!=` NULL nunca es verdadero en SQL.',
  },
  {
    n: 43,
    d: 'CICD',
    m: 0,
    q: 'A data science team prototyping in notebooks wants to track code changes, collaborate in parallel, review work before merging, and integrate with CI/CD. Which version control approach best meets these requirements?',
    o: [
      'Save versioned copies of notebooks using DBFS',
      'Periodically export notebooks and manually upload them to a shared cloud drive',
      'Enable built-in notebook revision history in the workspace',
      'Use Databricks Repos linked to a Git provider like GitHub or Azure DevOps',
      'Rely on cluster event logs and job history to review changes to code',
    ],
    a: [3],
    x: 'Databricks Repos con integración Git nativa soporta ramas, pull requests, revisiones de código e integración con CI/CD, cubriendo todos los requisitos frente a copias manuales o el historial básico de notebooks.',
  },
  {
    n: 44,
    d: 'JOBS',
    m: 0,
    q: 'An engineering team needs to schedule a job to run on the first Monday of every month at 7:00 AM, natively within Databricks without external tools. What is the best way to configure this?',
    o: [
      "Use the Databricks Job UI and select 'Monthly' scheduling mode with weekday filters",
      'Use a custom cron expression in the job configuration',
      'Trigger the job manually on the first Monday of each month',
      "Create a notebook task that checks the date and runs only if it's the first Monday of the month",
      "Use the Retry setting to run the job periodically until it's the correct date",
    ],
    a: [1],
    x: 'Una expresión cron personalizada en la configuración del job permite programar exactamente "primer lunes de cada mes a las 07:00", algo que el modo simple de programación de la UI no cubre con esa precisión.',
  },
  {
    n: 45,
    d: 'TRO',
    m: 0,
    q: 'A DLT pipeline in continuous mode processes IoT event data, but during a long period of inactivity with no new events, compute costs continue to accrue. What is the most likely explanation?',
    o: [
      'The Auto Loader source has gone stale and blocked shutdown',
      'The cluster is caching Auto Loader state in memory for faster startup',
      'The pipeline is waiting to checkpoint before releasing compute',
      'Continuous mode keeps compute resources active regardless of data arrival',
      'The pipeline is caching data in memory, causing charges',
    ],
    a: [3],
    x: 'En modo continuo, el cómputo permanece activo de forma constante monitorizando la llegada de datos, independientemente de si hay eventos nuevos o no, hasta que el pipeline se detiene explícitamente.',
  },
];
